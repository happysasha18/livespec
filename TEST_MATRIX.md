# live-spec — Test Matrix

Derived from PRODUCT_SPEC.md **through the proven ARCHITECTURE.md**; the package version has one home,
the VERSION file, and is not pinned here where it would read stale (row 265, which supersedes the old
"headers re-pin at each milestone" line this header used to carry). Last reconciled with the spec:
2026-07-14. The first derivation and its prover records: `docs/prover/2026-07-05-lost-layers.md`,
`docs/prover/2026-07-05-architecture.md`; the full milestone-by-milestone derivation history — which
spec versions and landings kept it current — lives in JOURNAL.md and the git log rather than frozen in this
header. Rows are organized
**architecture node × spec fact**: every index anchor sits in ≥ 1 row under its owning node, every row
pins a test level, and the derivation closes with the coverage validation at the bottom — walked, and
mechanized in `tests/test_traceability.py` so it re-walks at every run. [E-15]

**Test levels, adapted honestly to a text product** (recorded interpretation — prover record F6):
live-spec ships documents and skills, no browser surface, so almost every fact here sits at `string`.
The "rendered level" for a text artifact is a `string` assertion **against the SHIPPED file on disk**
(never a source fragment or a memory of it).

**The rung above it, and what fills it here** (2026-07-17, rows 386/412). Almost every fact here is a
fact about the pack's own text, and a string assertion against the shipped file sees it. The branch road
[E-34, T-23, INV-198..201] states facts of another kind: their truth lives in an **external engine's
runtime behaviour**. Git refuses another worktree's move of a checked-out main, a merge-base predicate
separates a stale lane from a rebased one, a rebase halts on a textual conflict. A string assertion
against a shipped file sees none of them, since the spec's sentence reads the same on the day git stops
refusing. So the ladder's real-engine rung (`browser-computed`, "real behaviour in a real engine") is
filled for this project by **a real `git` process driven against a throwaway two-worktree repo**, and
the rows needing it say so (M-374, M-375, M-377, M-379, M-380). The ladder is a kind-abstract shape and
each project fills its rungs with its own concrete proofs [INV-135]; this is live-spec's fill for the
engine rung, and the rung keeps the ladder's own name. **Owed:** `.live-spec/profile.md`'s
`project.proofs` line still reads that no fact needs a browser-computed rung, which this road makes
stale. That line's home is the host profile, so its correction belongs to the hand that holds it.

Two kinds of fact get two honest treatments:
- **structural facts** (a doc exists, carries its sections, cites its law) — level `string`, testable now;
- **behavioral facts** (a discipline holds during work: one landing at a time over time, attic on a real
  adopt run) — the row names its future mechanical owner (the E-6 host-facing gates (rows 55+), snapshot row 55, the next
  adopt run, or the milestone audit) and stays TODO until that owner exists. A TODO row is a named debt,
  never a silent one.

**Status vocabulary:** BUILT (test exists and runs green) · TODO (owner named in the row) · RETIRED (kept, never deleted).

---

## Artifact inventory

Every file a host (or this flagship repo's reader) receives. Each entry is checked shipped-and-non-empty
by `test_artifact_inventory` — the test parses THIS table, so adding an entry auto-extends the check.

| Artifact | Path | Type | Owning test |
|---|---|---|---|
| Base rulebook skill | `skills/live-spec-base/SKILL.md` | shipped text | `test_artifact_inventory` |
| Spec-author skill | `skills/spec-author/SKILL.md` | shipped text | `test_artifact_inventory` |
| Product-prover skill | `skills/product-prover/SKILL.md` | shipped text | `test_artifact_inventory` |
| Build-pipeline skill | `skills/build-pipeline/SKILL.md` | shipped text | `test_artifact_inventory` |
| Communicator skill | `skills/communicator/SKILL.md` | shipped text | `test_artifact_inventory` |
| Spec template | `templates/PRODUCT_SPEC.template.md` | shipped text | `test_artifact_inventory` |
| Architecture template | `templates/ARCHITECTURE.template.md` | shipped text | `test_artifact_inventory` |
| Matrix template | `templates/TEST_MATRIX.template.md` | shipped text | `test_artifact_inventory` |
| Roadmap template | `templates/ROADMAP.template.md` | shipped text | `test_artifact_inventory` |
| Agent card template | `templates/agent.template.md` | shipped text | `test_artifact_inventory` |
| Journal template | `templates/JOURNAL.template.md` | shipped text | `test_artifact_inventory` |
| Next-steps template | `templates/NEXT_STEPS.template.md` | shipped text | `test_artifact_inventory` |
| Profile template | `templates/profile.template.md` | shipped text | `test_artifact_inventory` |
| Problem-ledger template | `templates/PROBLEMS.template.md` | shipped text | `test_artifact_inventory` + `test_problems_template_shape` |
| Kill-list template | `templates/KILL_LIST.template.md` | shipped text | `test_artifact_inventory` + `test_kill_list_mechanical` |
| Bootstrap suite scaffold | `templates/test_scaffold.template.py` | shipped script | `test_artifact_inventory` + `test_scaffold_bootstrap_runs` (real simulated bootstrap, both ways) |
| Dev-machine skill sync | `scripts/sync-skills.sh` | shipped script | `test_artifact_inventory` + `test_sync_skills_script` (real run, twice) |
| Adoption procedure | `adopt/ADOPT.md` | shipped text | `test_artifact_inventory` |
| Installer | `install.sh` | shipped script | `test_artifact_inventory` |
| Migration note (rename) | `MIGRATION.md` | shipped text | `test_artifact_inventory` |
| Front door | `README.md` | shipped text | `test_artifact_inventory` |
| The one-page map | `OVERVIEW.md` | shipped text | `test_artifact_inventory` |
| Plugin manifest | `.claude-plugin/plugin.json` | shipped config | `test_artifact_inventory` |
| Plugin marketplace ref | `.claude-plugin/marketplace.json` | shipped config | `test_artifact_inventory` |
| Plugin icon | `.claude-plugin/icon.png` | shipped image | `test_artifact_inventory` |
| Mirror sync script | `scripts/sync-mirrors.sh` | shipped script | `test_artifact_inventory` |
| Doc renderer | `scripts/render-doc.py` | shipped script | `test_artifact_inventory`, `test_render_doc_smoke` |
| Base skill README | `skills/live-spec-base/README.md` | shipped text | `test_artifact_inventory` |
| Base skill license | `skills/live-spec-base/LICENSE` | legal | `test_artifact_inventory` |
| Spec-author README | `skills/spec-author/README.md` | shipped text | `test_artifact_inventory` |
| Spec-author license | `skills/spec-author/LICENSE` | legal | `test_artifact_inventory` |
| Product-prover README | `skills/product-prover/README.md` | shipped text | `test_artifact_inventory` |
| Product-prover license | `skills/product-prover/LICENSE` | legal | `test_artifact_inventory` |
| Test-author README | `skills/test-author/README.md` | shipped text | `test_artifact_inventory` |
| Test-author license | `skills/test-author/LICENSE` | legal | `test_artifact_inventory` |
| Feedback-intake README | `skills/feedback-intake/README.md` | shipped text | `test_artifact_inventory` |
| Feedback-intake license | `skills/feedback-intake/LICENSE` | legal | `test_artifact_inventory` |
| Feedback-collector README | `skills/feedback-collector/README.md` | shipped text | `test_artifact_inventory` |
| Feedback-collector license | `skills/feedback-collector/LICENSE` | legal | `test_artifact_inventory` |
| Build-pipeline README | `skills/build-pipeline/README.md` | shipped text | `test_artifact_inventory` |
| Build-pipeline license | `skills/build-pipeline/LICENSE` | legal | `test_artifact_inventory` |
| Communicator README | `skills/communicator/README.md` | shipped text | `test_artifact_inventory` |
| Communicator license | `skills/communicator/LICENSE` | legal | `test_artifact_inventory` |
| Inbox door + law | `inbox/README.md` | shipped text | `test_artifact_inventory` |
| Guardrails scaffold text | `scaffold/guardrails/README.md` | shipped text | `test_artifact_inventory` |
| CI mirror workflow | `.github/workflows/gates.yml` | shipped config | `test_artifact_inventory` + `TestCIMirror` |
| The pack's own spec | `PRODUCT_SPEC.md` | flagship doc | `test_artifact_inventory` |
| The pack's own architecture | `ARCHITECTURE.md` | flagship doc | `test_artifact_inventory` |
| The pack's own matrix | `TEST_MATRIX.md` | flagship doc | `test_artifact_inventory` |
| The queue | `ROADMAP.md` | flagship doc | `test_artifact_inventory` |
| The journal | `JOURNAL.md` | flagship doc | `test_artifact_inventory` |
| The resume file | `NEXT_STEPS.md` | flagship doc | `test_artifact_inventory` |
| Package version | `VERSION` | version home | `test_artifact_inventory` |
| Host profile (dogfood) | `.live-spec/profile.md` | settings instance | `test_artifact_inventory` |
| License | `LICENSE` | legal | `test_artifact_inventory` |
| Prover records | `docs/prover/` | records dir (non-empty) | `test_artifact_inventory` |
| Guardrails (pack gates + fence) | `guardrails/` | scripts dir (non-empty) | `test_artifact_inventory` |
| Decision archives | `docs/decisions/` | records dir (non-empty) | `test_artifact_inventory` |
| Research reports | `docs/research/` | records dir (non-empty) | `test_artifact_inventory` |
| Queue archives | `docs/queue-archive/` | records dir (non-empty) | `test_artifact_inventory` |
| Audit records | `docs/audit/` | records dir (non-empty) | `test_artifact_inventory` |
| Prior-art survey | `docs/prior-art.md` | shipped text | `test_artifact_inventory` |
| Adoption guide | `docs/adoption.md` | shipped text | `test_artifact_inventory` |
| Pair adoption guide | `docs/pair-adoption.md` | shipped text | `test_artifact_inventory` |
| Architecture method | `docs/architecture-method.md` | shipped text | `test_artifact_inventory` |
| Onboarding and settings | `docs/onboarding-and-settings.md` | shipped text | `test_artifact_inventory` |
| Pipeline walk | `docs/pipeline.md` | shipped text | `test_artifact_inventory` |
| Push law | `docs/push-law.md` | shipped text | `test_artifact_inventory` |
| Test method | `docs/test-method.md` | shipped text | `test_artifact_inventory` |
| Worker liveness | `docs/worker-liveness.md` | shipped text | `test_artifact_inventory` |
| Publish skill | `skills/publish/SKILL.md` | shipped text | `test_artifact_inventory` |
| Publish README | `skills/publish/README.md` | shipped text | `test_artifact_inventory` |
| Publish license | `skills/publish/LICENSE` | legal | `test_artifact_inventory` |
| Test-author skill | `skills/test-author/SKILL.md` | shipped text | `test_artifact_inventory` |
| Feedback-intake skill | `skills/feedback-intake/SKILL.md` | shipped text | `test_artifact_inventory` |
| Feedback-collector skill | `skills/feedback-collector/SKILL.md` | shipped text | `test_artifact_inventory` |
| Design-reviewer skill | `skills/design-reviewer/SKILL.md` | shipped text | `test_artifact_inventory` |
| Skill evals — method + honest boundary | `evals/README.md` | shipped text | `test_artifact_inventory`, `test_eval_readme_states_honest_boundary` |
| Skill evals — one per working skill | `evals/` | shipped text dir | `test_skill_evals_present` (self-closing over skills/) |
| Eval run records | `docs/evals/` | records dir (non-empty) | `test_artifact_inventory` |

---

## Matrix rows — grouped by architecture node

### [node: base-rulebook]

| ID | Fact (from spec) | Spec ref | Test level | Owning test | Status |
|---|---|---|---|---|---|
| M-155 | A known, owned problem never blocks unrelated work: it parks (ledger line / owning row / expected-red note) and unrelated lanes keep rolling; hand-fix loops cap at two-strikes; a mechanically-owned defect is serviced in batch (silent fence fixes, one session-end ledger append); a real NEW bug still preempts; never a per-instance ceremony interrupting the flow, never a park without its written holder | INV-56 | string | `test_limp_never_dams_flow` (red proven against HEAD's shipped state) | BUILT |
| M-166 | Skill discovery: SPEC states the search's two triggers (INV-65: setup beside the founding questions; struggle at a second occurrence or a twice-rejected taste artifact) with the borrowing practice (invoke as shipped · paraphrase + named credit · verbatim only under license), base rule 20 and ADOPT's setup line carry it; never an adoption without the human's word, never republished unlicensed text | INV-65 | string | `test_skill_discovery` (red proven against HEAD — clause and rule absent there) | BUILT |
| M-001 | Every working skill opens with the base-inherit pin (base name + the base version it was written against); never a working skill without its pin | E-12 | string | `test_skills_inherit_base_pin` | BUILT |
| M-093 | One name-collision law, stated once (base rule 18): semantic mark first, then numeric ordinal -2/-3; concurrent homes add a session token; the attic, the inbox and ADOPT all CITE the law rather than restate half of it; never overwrite, never a third scheme, never a lost file | E-12 | string | `test_collision_law_one_home` | BUILT |
| M-002 | Settings live in four nested scopes and resolve narrowest-out: session > host > personal > package default, broader values inherited until overridden on the human's word; an unrecognized profile line is ignored ALOUD, never silently dropped and never an error | E-13 | string | `test_settings_ladder_documented` (structural clause: ladder + resolution order stated in the base skill); behavioral resolution: milestone audit (M-1) + the E-6 host-facing gates (rows 55+) | BUILT |
| M-003 | Every choice not in the wish is asked or recorded-and-surfaced; never decided-and-buried | INV-5 | string | snapshot declared-scope diff (row 55) — the mechanical fence | TODO |
| M-004 | Proactivity mode and trust are written only on the human's word; the agent never raises its own level | INV-9 | string | milestone audit (M-1) | TODO |
| M-005 | Before every write and commit: re-check `git status` + HEAD; never write over changes the agent did not make | INV-11 | string | `guardrails/pre-commit` (opt-in fence): `test_armed_stale_head_blocks_commit`, `test_unarmed_fence_passes_silently` | BUILT |
| M-006 | A shared rule has exactly one normative home (the base skill); never two full statements | INV-13 | string | milestone compaction pass (M-1) | TODO |
| M-007 | An override exists only as a written profile line + a dated journal note in the home it governs; never an unwritten divergence | INV-14 | string | `test_host_profile_recorded_override` | BUILT |
| M-008 | The human owns taste, design, irreversible calls, push gates, domain wording, their contract; never the agent | ACT-1 | string | process — every decision page + milestone gate-list (M-1) | TODO |
| M-009 | The senior agent owns judgment (spec deltas, levels, triage); never delegated to a worker | ACT-2 | string | process — journal audit at milestones (M-1) | TODO |
| M-010 | Workers run mechanical execution with persistent checkpoints in `.live-spec/checkpoints/`; never in a system temp dir | ACT-3 | string | checkpoint-location fact; routing decided advisory not mechanical (row 56, INV-69) | TODO |
| M-095 | The worker contract: write-ownership narrowed to the brief's named files; same-session sibling files fence-benign (fence alarms on FOREIGN sessions); live session-scope lines ride into the brief verbatim; failed acceptance escalates one tier, logged; never a silent same-tier retry, never a worker resolving the ladder itself | ACT-3 | string | `test_worker_contract_stated` (SPEC clause + pipeline elaboration) | BUILT |
| M-119 | The brief arms the worker for the workshop: it carries the host's problem-ledger path with the WATCHED-line duty (worker noise lands in its checkpoint as a ledger line; the senior carries it into the ledger at verify) AND the clock read at briefing (a worker's stamps come off the brief's clock); never a worker silently retrying workshop noise, never a worker inventing an hour | ACT-3 | string | `test_brief_carries_ledger_and_clock` | BUILT |
| M-102 | Every delegation's landing report carries the savings line (what went to the worker, roughly what senior work it saved); never a delegation that lands unreported — the missing line is the habit dying | ACT-3 | string | `test_gap6_delegation_savings_line` | BUILT |
| M-104 | The workshop-noise law (base rule): first sight of operational noise = one WATCHED ledger line, never a silent retry; the SECOND occurrence gets an owner that moment — a queue row or the human's dated agreed non-problem (his word alone, INV-9); a third unowned recurrence is a METHOD defect that goes to the pack's queue; never ledger noise preempting the bug lane | INV-23 | string | `test_base_rule_problem_ledger` | BUILT |
| M-011 | A safe breakpoint = LIVE STATE replaced (one block, never stacked) + dated journal entry + commit | M-2 | string | `test_next_steps_live_state` (shape; the discipline audits at M-1) | BUILT |
| M-012 | Versions have named homes: VERSION file, a version line under `metadata:` in its SKILL.md frontmatter, host record; never scattered or absent | M-7 | string | `test_version_homes` | BUILT |
| M-261 | A derived doc's header (ARCHITECTURE.md, TEST_MATRIX.md) carries no frozen spec-version number that reads as current — it names what it derives from, points at the version's one home (VERSION), and carries a dated "Last reconciled" provenance line instead; never a header pinning a stale `(vX.Y, date)` a reader mistakes for the current version (row 265) | M-7 | string | `test_no_frozen_version_pin_in_header` + `test_header_points_at_version_and_reconcile_date` (red proven against the pre-delta headers, 2026-07-12) | BUILT |
| M-066 | The base skill states each shared rule once and no working skill restates one normatively; never a second normative home for a shared rule | E-12 | string | milestone compaction pass (M-1) | TODO |
| M-069 | A prototype lives fenced (own home) with a PROTOTYPE label per artifact kind, opened only by the assigned senior, shown only as a sketch; never wired into or presented as the product | E-17 | string | `test_base_rules_door_and_prototype`, `test_working_skills_carry_the_door` | BUILT |
| M-134 | The economy ladder: SPEC names `budget.pressure` (full [default] · lean · tight) moved only by the human's word and asked — or the default told — at a project's setup (founding/adoption) alongside project.kind, each rung's legal sheds (node-scoped mid-work test runs with the full suite kept at landing gates; CROSS-LINK with an owed FULL deferrable as a dated debt line; batched landing gates whose batch-end red bisects by landing order; cheaper worker tiers), and the base skill's settings table carries the `budget.pressure` row; never a rung moved by the agent's own word, never a shed taken silently — every taken shed named in the landing report | T-19 | string | `test_economy_ladder` | BUILT |
| M-135 | The never-bend list at every rung, stated once: the door law + tripwires, red-before-fix, the human's gates, the landing report with named sheds, landing purity, the push gate at full rigor, the safety net, narration whole, and an explicit host line outliving any rung; never a rung that sheds the push gate, the door law, or a host profile's tighter line | INV-40 | string | `test_economy_ladder` | BUILT |
| M-186 | A background worker outlives a memory wipe and the resume protocol proves it dead or alive: SPEC and the base rules carry the protocol — the handoff note records the worker's id (→ its checkpoint), its briefed write-set, and the two liveness checks (file times over ~30 s [default] + one message, ~2 min [default]); the process list and the harness task list are never proof of death; a prior-context worker is foreign until verified; no second worker onto a shared tree until the first halts by its own reply or is declared dead; prefer halting workers before a wipe; never a handoff framing a possibly-running worker's output as finished | INV-76 | string | `test_worker_liveness_protocol` (red proven pre-code: the base rules spoke no liveness) | BUILT |
| M-198 | Human-facing prose is drafted by a clean writer: SPEC INV-84 and base rule 21 state the road — a plain brief (the facts, the intended reader, the register laws) hands the drafting to a fresh writer session with no package rules loaded; the rules-loaded session briefs, reviews, and lands, and never writes the prose itself; binds new and in-edit text only; a blanket rewrite of settled text is refused; never the marinated session drafting a page a human will read | INV-84 | string | `test_clean_writer_law` (red proven against the pre-fold text — no INV-84, no rule 21 there, 2026-07-10) | BUILT |
| M-234 | The convergence principle stated once (INV-98, row 218): every piece of work names its goal up front as an artifact the work can be held against (frozen norm, exemplar bank, failing test, written acceptance); a paraphrase cannot serve as the goal; every iteration measures distance to the goal itself and a proxy never replaces the goal; the distance only shrinks and reached levels lock by a mechanism; a divergent stretch is named and bounded by its convergence point — in BOTH homes (spec clause + base rule 22, the rule citing the playbook chapter); never the rule restated in full inside a working skill, never a goal accepted as a paraphrase | INV-98 | string | `test_goal_is_an_artifact` + `test_distance_to_the_goal_itself` + `test_levels_lock_and_divergence_bounded` + `test_spec_anchor_index_and_playbook_cite` (red proven against the pre-delta tree, 2026-07-11) | BUILT |
| M-244 | One canonical state directory, worktree isolation by default on overlap (INV-105, row 227): the canonical state directory is named `.live-spec` once and no second directory competes; a near-miss directory (`.livespec`, `.live_spec`, `livespec/`) found at attach or resume is a red finding retired to the attic under a manifest line (INV-7); worktree isolation is the default when two lanes' write-sets overlap, the later lane building in its own isolated copy and integrating under the pen (INV-11, T-18, INV-39); the law lives in three homes: the spec clause, base rule 7 (the fence), and the adoption sweep in adopt/ADOPT.md; never a look-alike left standing beside `.live-spec`, never a shared file one lane holds open written by another | INV-105 | string | `test_canonical_name_in_both_attach_and_spec` + `test_lookalike_retired_to_attic` + `test_worktree_default_in_both_fence_homes` + `test_spec_anchor_and_index` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-240 | A landing closes the checkpoints it shipped (INV-107, row 226): a landing that ships a checkpoint's items flips that checkpoint to its closed state in the same landing, so a returning session never reopens finished work; a checkpoint whose items all live in git history is stale by definition and reads as a resume defect; the closing sweep rides the NEXT_STEPS replacement, a checkpoint left reading "not started" after its items shipped fails the landing — in BOTH homes (spec breakpoint clause + base rule 6); never a checkpoint left reading "not started" after its items shipped, never a returning session redoing work already in git history | INV-107 | string | `test_closing_half_in_both_homes` + `test_stale_checkpoint_is_a_defect_in_both_homes` + `test_spec_anchor_and_index` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-246 | A behavioural rule that breaks mid-turn twice earns a live channel (INV-108, row 256): a standing behavioural rule whose normative home is a once-read file (loader, profile, skill text), on its second mid-turn break, earns a live channel that moment — an every-prompt hook line reminding at the decision point, or a mechanical after-the-fact check that turns the suite red — with the pick recorded where the rule lives; the once-read homes stay the normative homes and the live channel only carries the rule; kin of rule 19's second-occurrence law (a channel chosen instead of an owner row); the 1.1.0 audit's once-read walk is the law's first sweep; in BOTH homes (spec clause + base rule 23); never a twice-broken behavioural rule left on prose alone, never the live channel promoted over the rule's once-read home | INV-108 | string | `test_law_in_both_homes` + `test_worked_proof_in_both_homes` + `test_spec_anchor_and_index` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-256 | Every session carries a stable start-minted identity and the pen tie-break orders on it (INV-117, row 277): a session mints one identity before its first act and records it in its `.live-spec/` checkpoint, unchanged for its life — the harness session id where present, else start-time + worktree path + nonce (INV-105 already separates worktrees); the parallel-lanes concurrent-claim tie-break [INV-2, INV-11] orders on this identity, the row→in-work flip recording the claimant's identity so a peer computes the same order, so exactly one session backs off; the inbox source-mark's session token is a projection of this identity; homes — the spec clause and base-rulebook's fence rule; never a tie-break resting on a token only inbox-collision sessions own | INV-117 | string | `test_session_identity_minted_at_start_phrase` + `test_tiebreak_orders_on_session_identity` + `test_inbox_token_is_projection_phrase` + `test_spec_anchor` + `test_spec_anchor_and_index` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-257 | The worker-death verdict requires a stale heartbeat as its third signal (INV-76, row 278): a worker touches its checkpoint file on a fixed interval (~60 s [default]) as a bare heartbeat independent of its work output, and a resuming session declares the worker dead only when all three checks read quiet together — the write-set still over ~30 s [default], the heartbeat stale (untouched past ~2 min [default]), and the probe unanswered (~2 min [default]); life on any one check keeps the worker live and its files claimed, so a compute-bound run that writes no file for minutes is never mistaken for dead and no second writer is spawned onto its tree; never a busy worker declared dead on a quiet write-set and an unanswered probe alone | INV-76 | string | `test_worker_death_requires_stale_heartbeat` | BUILT |
| M-276 | The process stations are kind-abstract, and a project's founding declares its concrete layers and proof kinds (INV-135, row 292 — P0k of the fourteen-principle architect draft): the entry impact read, the footprint categories (INV-128), and the test ladder are kind-abstract stations the pack states once, each project kind filling them with its own concrete decomposition — the three footprints hold past code but the layers and proofs are the project's own; `project.kind` (INV-36) recorded at founding gains a `project.layers` line (concrete footprint categories) and a `project.proofs` line (concrete test-ladder rungs) in the host profile; a founding check reds a kind recorded with no declared layers and no declared proofs, red-proven against three real hosts as fixtures — a code/music kind (track-coach), a photo kind (tlvphotos), a prose kind (the promotion campaign) — plus a kind-only profile that goes red; ARCHITECTURE.md carries the per-kind footprint-and-proof table, spec-author and test-author read the declared layers and proofs instead of assuming code; homes base rule 24 + the founding clause + the host-profile record + the ARCHITECTURE per-kind table + spec-author/test-author + the founding check; never a project.kind recorded with neither layers nor proofs, never the code ladder assumed for a non-code kind | INV-135 | string | `test_three_kind_fixtures_pass` + `test_kind_without_layers_or_proofs_goes_red` + `test_missing_only_proofs_goes_red` + `test_live_host_profile_is_complete` + `test_base_rulebook_states_layers_and_proofs` + `test_spec_clause_and_index` + `test_architecture_has_per_kind_footprint_and_proof_table` + `test_spec_author_and_test_author_read_declared_layers` + `test_adopt_founding_prompts_layers_and_proofs` (the founding check red-proven against a kind-only fixture, 2026-07-12) | BUILT |
| M-278 | A project kind also carries its own DESIGN PRINCIPLES, and the verify pass runs them (INV-136, row 298): beside its concrete layers and proofs (INV-135), a project kind names a set of checkable design rules of the family of cross-surface policy uniformity (INV-125) and paired-transition symmetry (INV-126); the pack ships a per-kind starter set homed in the ARCHITECTURE per-kind design-principles table, a founding that records a VISUAL kind declares them in the host profile on a `project.design-principles` line, and a founding check reds a visual kind recorded with none (red-proven against a visual kind-only fixture) while a non-visual kind with none passes; the frontend starter set gathers the visitor walk, the feel pass scaled to a whole site, and motion/scroll feel plus its founding design principle — interactive controls that belong to different layers occupy separate screen space (a non-interactive element may overlap freely; the rule binds the clickable regions); the verify feel pass reads the declared design principles; spec-author reads them and ADOPT records them; the interactive-overlap rule's pixel/DOM projection runs in the ADOPTING project's own suite, live-spec shipping the law and the starter set only; never a visual kind recorded with no design principles, never the pixel assertion faked in live-spec's own suite | INV-136 | string | `test_visual_kind_without_design_principles_goes_red` + `test_visual_kind_with_design_principles_passes` + `test_nonvisual_kind_without_design_principles_passes` + `test_founding_check_has_teeth` + `test_ui_lookalike_kinds_are_not_misclassified_visual` + `test_real_visual_kinds_are_caught` + `test_live_host_profile_is_not_falsely_reddened` + `test_spec_clause_and_index` + `test_architecture_has_per_kind_design_principles_table` + `test_interactive_overlap_has_one_full_home_the_spec` + `test_verify_feel_pass_reads_design_principles` + `test_spec_author_reads_declared_design_principles` + `test_adopt_founding_prompts_design_principles` + `test_adopting_project_pixel_projection_specified` (the founding check red-proven against a visual kind-only fixture, the visual detector word-boundaried so a 'ui'-lookalike kind is not misclassified, 2026-07-13) | BUILT |
| M-281 | The frontend kind carries a legibility floor beside the interactive-overlap rule: min contrast ratio + min text size, checked at the verify feel pass (computed styles) and the pre-show gate (scripts/preshow-legibility-lint.py on the styled file), the pack shipping the law + default numbers + script and leaving the browser-computed assertion to the adopting project (INV-139, legibility-floor wish); never styled text shown to a human under the contrast or size floor without a block. | INV-139 | string | `test_red_fixture_flagged` + `test_green_fixture_passes` + `test_contrast_math` + `test_spec_clause_and_index` + `test_design_principle_and_preshow_wired` + `test_matrix_row` (red proven against pre-delta tree, 2026-07-13) | BUILT |
| M-285 | The orchestrator decides what it can decide, and surfaces only what it cannot (INV-143, base rule 27): the seat decides a mechanical step, a value a proven artifact already determines (INV-121), or a sensible default it can pick and name (INV-70), and reports the choice; it surfaces a decision to the human only where it genuinely cannot be made without them — a taste call, a trade-off no artifact settles, a change to the definition of correct; it never parks derivable work on the human's queue to avoid deciding (INV-4), the posture holding on every session including one resumed from its files after a memory wipe (INV-48); homes base rulebook rule 27 + the delegation clause + the Formal-index row; never derivable work parked on the human's queue to dodge a decision, never the posture dropped on a wipe-resumed session | INV-143 | string | `test_base_rule_states_the_default_action_posture` + `test_spec_invariant_143_present_and_indexed` + `test_architecture_owns_143` + `test_matrix_row_for_143` (red proven against the pre-delta tree, 2026-07-14) | BUILT |
| M-287 | A periodic full audit catches the drift no lint names (INV-145, base rule 28): two layers guard the living documents — the continuous lints (the register lint INV-83, the provenance-narrative arm, and their kin) run on every push and hold each KNOWN drift class the moment it reappears, and a full audit runs on a landing-count cadence (every ten landings since the last full audit, default, host-settable on the human's word INV-70) running the milestone gate's whole-read (M-1: the full spec and architecture re-prove INV-116, the design review INV-141, the doc-compaction sweep INV-115) even where no milestone falls due, so an UNKNOWN drift class accumulated between milestones is caught by a fresh whole-read; the count is read from the landing history (INV-107) and a milestone gate resets the counter; an audit is adversarial by nature — a whole-read that sets out to break, refute, and find holes (INV-46); homes base-rulebook rule 28 + the rhythm clause + the Formal-index row; never an unknown drift class left to accumulate unread between milestones, never the cadence counter unreset by a milestone that already ran the whole-read | INV-145 | string | `test_base_rule_states_periodic_full_audit` + `test_spec_invariant_145_present_and_indexed` + `test_architecture_owns_145` + `test_matrix_row_for_145` (red proven against the pre-delta tree, 2026-07-14) | BUILT |
| M-297 | A deferral must justify itself, or the item is the seat's to do (INV-152, base rule 29, companion of the request-layer classifier): a work item carrying a needs-the-human's-word marker (a held queue row, a NEXT_STEPS line, an open setup-script decision) is re-tested by derivability every time it is touched — an answer pinning to an existing artifact (a base rule, a spec sentence, the architecture, an approved prototype, an already-answered decision INV-59) makes the item the seat's (do it, cite the artifact, drop the marker INV-121/INV-143), an answer needing a fact no artifact holds (a taste, a policy, an act irreversible outside git INV-17) makes it the human's and the marker stands; writing such a marker requires naming that human-only fact, a marker that cannot name it defaulting to the seat's and a marker that cannot say why the item is the human's being itself the finding (the same shape as a request matching no kind INV-151); homes base rulebook rule 29 + the deferral clause + the Formal-index row + the mechanical net `guardrails/check-deferral-marker.py` + the chat-law delivery line; never a held item that cannot name its human-only fact parked on the human's queue, never a needs-his-word marker written without naming the fact, never a claim of mechanical enforcement beyond what the commit gate actually wires | INV-152 | string | `test_deferral_clause_stands` + `test_lives_in_the_base_rulebook` + `test_base_description_counts_the_rule` + `test_inv152_index_and_ownership` (red proven against the pre-delta tree, 2026-07-14); the mechanical arm `guardrails/check-deferral-marker.py` with `test_bare_marker_is_flagged` / `test_wrapped_signal_is_flagged` / `test_wrapped_reason_passes` / `test_far_negation_passes` / `test_signal_in_fenced_block_is_skipped` (the folded-item scan) and `test_real_repo_tree_is_clean` the CI backstop on the repo's own files (2026-07-15) | BUILT |
| M-302 | The deferral re-test's delivery arm: `hooks/chat-law-hook.sh` exists, is executable, and its output carries the deferral law — before parking a work item or opening an AskUserQuestion, re-test by derivability and name the human-only fact (taste, policy, or an act irreversible outside git), an unnamed marker or question being itself the finding; the line names `guardrails/check-deferral-marker.py` as the mechanical arm and pins to INV-152 / INV-28; delivered every prompt as a reminder that cannot block, since chat cannot be machine-gated; never a claim that the reminder itself blocks a commit | INV-152 | string | `test_output_carries_the_deferral_law` (with `test_script_exists_and_executable` — the hook run by deed) | BUILT |
| M-312 | The ship-the-shape / host-owns-the-instance split has one stated home (INV-163, ROADMAP 332): the split is minted once — the discriminator (can the pack ship a single identical body every host runs?) and both poles (centralize to one pack home; ship the shape and the host owns the instance) — and every site cites it in place of re-deriving the contrast (INV-125/136/139 the ship-side, INV-158 the centralize-side, E-26 the removal scanner, plus the sixth in ARCHITECTURE.md); the split binds forward (INV-159) and its authoring home is spec-author's pole-declaration duty; never a site re-spelling the split in its own words, never a host runtime change since the formalization is docs-only | INV-163 | string | `test_pack_to_host_split_stated_once` + `test_split_binds_forward_off_the_stated_law` + `test_every_ship_shape_site_cites_the_root` + `test_no_site_re_derives_the_split_in_its_own_words` + `test_architecture_cites_the_root_not_a_re_derivation` + `test_pole_declaration_duty_homed_in_spec_author` (red proven against the pre-delta tree — INV-163 absent, 2026-07-15) | BUILT |
| M-352 | The agent/skill boundary, stated once (E-31): an agent is a project window carrying its own tree, its own queue, its own gates, its own contracts, a standing mission, and a card of its own, each outliving any one conversation — the tree sits on disk, the queue holds rows nobody's session owns, the mission stands until the owner changes it — and one window serves one agent, the law the pair's two repos already hold [INV-86]; a skill is a capability any window loads, holding no tree, no mission, and no queue of its own, working inside the conversation that loaded it and leaving nothing standing when that conversation ends; homes the spec's two clauses + their index rows; never a skill credited with durable state it does not carry, never an agent's standing mission treated as one session's property | E-31 | string | `test_agent_is_a_project_window` + `test_skill_dies_with_the_session` + `test_e31_index_and_ownership` (red proven against the pre-delta tree, 2026-07-17) | BUILT |
| M-353 | The grain question follows from the boundary (INV-182): durable state, a standing mission, and a zone of its own make a capability an agent, and a capability living wholly inside a session is a skill; a real case sitting on the line is placed by the owner's word [T-22], taste being the human-only fact this deferral names [INV-152]; homes the grain clause + its index row; never a grain call made on the owner's behalf, never a capability sorted by its size while its durable state and its zone go unread | INV-182 | string | `test_grain_boundary_names_its_three_marks` + `test_grain_on_the_line_is_the_owners_word` + `test_inv182_index_and_ownership` (red proven against the pre-delta tree, 2026-07-17) | BUILT |
| M-354 | The two-channel law (INV-183): exactly two channels carry everything that passes between two agents — the receiver's inbox carries a one-shot request to change something [E-11], and a published contract carries a recurring read, versioned and read on the reader's own clock [E-33]; no third channel exists, and a reply rides the inbox channel in the other direction so the count of two holds [INV-192]; two agents sharing one machine changes the transport's speed and leaves the contract itself untouched, and a remote agent reaches the other through git alone [INV-112]; the base rulebook carries the law in the phrase "exactly two channels"; never a third channel opened between two agents, never a reply counted as a channel of its own | INV-183 | string | `test_exactly_two_channels_in_the_spec` + `test_no_third_channel_exists` + `test_base_rulebook_carries_the_two_channel_law` + `test_inv183_index_and_ownership` (red proven against the pre-delta tree, 2026-07-17) | BUILT |
| M-355 | Data never travels as a message (INV-188): asking an agent for numbers costs the neighbour a session and yields a snapshot nobody versioned or stamped, and the road for it already exists — a consumer wanting data reads the published contract, and a consumer wanting a field the contract lacks holds a request about the contract's shape, which the earned message governs [INV-189]; this is the noise the channel exists to remove; homes the clause + its index row; never a data request sent as a message while a contract publishes the field, never a snapshot passed hand-to-hand in place of a versioned, stamped artifact | INV-188 | string | `test_data_never_travels_as_a_message` + `test_missing_field_routes_to_the_earned_message` + `test_inv188_index_and_ownership` (red proven against the pre-delta tree, 2026-07-17) | BUILT |
| M-356 | The earned message names the sender's own blocked work IN the message (INV-189): a real row, a real failing step, a real thing the sender cannot finish while the receiver's zone stands as it does, and a message that cannot name such work is never sent; the births are a closed set of two — blocked by the receiver's zone as it stands, or lived a fault in it and carrying the evidence lived — and a candidate matching neither has no birth; the zone's owner is presumed competent and informed, so nothing that owner's own instruments already see earns a file, which is what keeps the second birth narrow; curiosity, tidiness, and a neighbour-might-want-to-know are the noise this law removes; the base rulebook carries the law in the phrase "names the sender's own blocked work", and `guardrails/check-earned-message.py` is its mechanical arm, exercised against a real fixture inbox deposit and judged by its exit code both ways — a deposit marked an agent's with a named `Blocked:` line exits 0, the same deposit with the field absent exits 1; the gate nets a forgotten field and the receiving sweep still reads whether the named work is real [INV-150, INV-153]; never an unearned message deposited on curiosity or tidiness, never the gate's verdict assumed while the script goes unrun | INV-189 | string | `test_earned_message_names_its_block` + `test_two_births_are_a_closed_set` + `test_owner_presumed_competent_and_informed` + `test_base_rulebook_carries_the_earned_message_law` + `test_gate_passes_a_message_naming_its_blocked_work` + `test_gate_reds_a_message_naming_no_blocked_work` + `test_inv189_index_and_ownership` (the spec and rulebook arms red proven against the pre-delta tree, 2026-07-17; the two gate arms run the real script against a real fixture deposit and read its exit code) | BUILT |
| M-357 | The referral's direction (INV-190): a question from another agent's zone is referred, and the referral travels back to whoever asked — a human asker is answered in chat in plain words, the answer being the other agent's so ask that agent, nothing sent anywhere and the referral costing one sentence; an agent asker is answered along the reply road as the terminal state of the message it sent, declined and naming the zone that owns the question [INV-192]; the zone's owner receives nothing from a referral, the referring agent decides nothing on the owner's behalf, and it carries the question to the human as its own question on no occasion; the base rulebook carries the law in the phrase "travels back to whoever asked"; born 2026-07-17 of a live-spec session about to deposit into a neighbour's inbox a question no work of its own stood on, travelling forward to the zone's owner with tidiness the only thing pushing it; never a neighbour's question forwarded to its zone's owner, never a referral carried onward as the referrer's own question | INV-190 | string | `test_referral_travels_back_to_the_asker` + `test_zone_owner_receives_nothing_from_a_referral` + `test_base_rulebook_carries_the_referral_direction` + `test_inv190_index_and_ownership` (red proven against the pre-delta tree, 2026-07-17) | BUILT |
| M-358 | A question that pins to no artifact, and on which no work of the sender's stands, is dropped, and holding it was itself the defect (INV-191): the routing principle carried across agents [INV-153] — every incoming thing routes to the home whose declared sentence governs it, such a question has no home to route to and no work standing on it, so the finding is the question itself and the holding is what the finding names; the deferral test's re-read applies at every touch [INV-152]; homes the drop clause + its index row; never a homeless question parked as a work item, never such a question carried to the human as the agent's own | INV-191 | string | `test_homeless_question_is_dropped` + `test_the_holding_is_itself_the_finding` + `test_inv191_index_and_ownership` (red proven against the pre-delta tree, 2026-07-17) | BUILT |
| M-359 | An agent-initiated message is a proposal until the owner ratifies it (INV-193): it carries the sending agent's judgment and carries nothing else, standing as a proposal in the receiver's queue; an owner-initiated message is the one kind that carries the owner's authority; relaying changes a message's carrier and leaves its authority exactly where it started, so an agent never launders its own proposal into an instruction and self-certification stays closed off [INV-94]; the base rulebook carries the law in the phrase "stays a proposal until the owner ratifies"; never an agent's proposal laundered into an instruction by relaying it, never a ratification inferred from a message's passage | INV-193 | string | `test_agent_message_is_a_proposal_until_ratified` + `test_relaying_leaves_authority_where_it_started` + `test_base_rulebook_carries_the_ratification_law` + `test_inv193_index_and_ownership` (red proven against the pre-delta tree, 2026-07-17) | BUILT |
| M-360 | Non-duplication (INV-194): a capability another agent's zone owns is taken through one of the two channels — an agent needing it sends a message or reads a contract [INV-183]; a local copy of a neighbour's capability is the violation the cards exist to prevent, since the copy drifts from its original the day after it is made and the two owners then answer one question two ways [E-32]; the base rulebook carries the law in the phrase "a local copy of a neighbour's capability"; never a neighbour's capability re-implemented at home while a channel reaches the original, never two owners left answering one question two ways | INV-194 | string | `test_capability_taken_through_one_of_the_two_channels` + `test_local_copy_is_the_violation_the_cards_prevent` + `test_base_rulebook_carries_the_non_duplication_law` + `test_inv194_index_and_ownership` (red proven against the pre-delta tree, 2026-07-17) | BUILT |
| M-369 | An agent recognises a neighbour's zone on its own, and the owner's word is an acknowledgement (INV-195, born 2026-07-17 on the owner's count of how often he had said to put a thing in the pack's inbox to an agent that had already lived the fault and held the evidence): meeting a fault or a lack in something another zone owns — a rule of the method, a shape a neighbour ships, a field a contract lacks — the agent scans for cards, finds the owner, and takes the channel that fits INV-183; the owner naming the road afterwards carries no fact the agent lacked; the recognition is bounded by the message's own two births INV-189; homes the earned-message scenario + base rule 31 + the Formal-index row; never an agent waiting to be told before it routes a fault it lived, never the owner standing as the router between two agents, never a recognition that outruns the two births into curiosity | INV-195 | string | `test_agent_recognises_a_neighbours_zone_itself` (red proven against the pre-delta tree, 2026-07-17) | BUILT |
| M-370 | One question crosses between two agents twice, and the third crossing goes to the owner (INV-196, the audit's finding: every hop of a refer-and-re-send loop passes its own law while the exchange manufactures traffic): the bound is two, counted by the exchange's identifier INV-192; a third crossing is named in the sender's own status report as a zone question the two agents could not settle INV-27; the human-decision withdrawal loop already carries this shape, where a genuine taste call could withdraw without end and the bound is two INV-130; homes the earned-message scenario + base rule 31 + the Formal-index row; never an unbounded exchange of individually-legal messages, never a count reopened by rewording the question, never a third crossing sent to the neighbour instead of the owner | INV-196 | string | `test_one_question_crosses_twice_then_goes_to_the_owner` (red proven against the pre-delta tree, 2026-07-17) | BUILT |
| M-371 | A concern no agent's zone owns goes to the pack as the default owner, and the work never stalls on ownership (INV-197, the owner's word 2026-07-17 through the promoter's inbox deposit, which lifted the multi-agent stage one level: that stage answers how two KNOWN agents talk, this answers who owns a concern nobody owns): a homeless question is dropped INV-191, and a homeless concern is real work whose owning zone does not exist yet — cross-project measurement, a shape every agent needs and none ships — so the agent takes it to the pack's inbox E-32 and the pack answers who owns it (an existing agent, a new agent the owner ratifies T-22, or a skill INV-182); zones may overlap and no disjointness is forced, each card recording what its own agent claims; an agent meeting an unowned concern does the reasonable thing now in whatever tree can hold it, marks that work provisional, and the re-home lands later as ordinary pipeline work in each tree; homes the earned-message scenario + base rule 31 + the Formal-index row; never a real concern dropped for having no owner, never an agent stalling its work until ownership is settled, never a forced carve of disjoint zones | INV-197 | string | `test_an_unowned_concern_goes_to_the_pack` + `test_work_never_stalls_on_ownership` (red proven against the pre-delta tree, 2026-07-17) | BUILT |

### [node: spec-author]

| ID | Fact (from spec) | Spec ref | Test level | Owning test | Status |
|---|---|---|---|---|---|
| M-013 | The spec is the living truth, use-case-first, one surface = one name; never two synced copies | E-4 | string | prover FULL pass at milestones (M-1) | TODO |
| M-148 | A conditionally-entered face (first visit, empty state, onboarding, one-time banner) states its deliberate re-entry path or names the one-way as a decision; trigger wording ("only on first visit/run", "until dismissed") owes its return sentence; the fit walk's journey lens carries the question, the prover's stress list the entry-symmetry lens (wiring); never a get without a set unless the one-way is written by name | INV-50 | string | `test_entry_symmetry` | BUILT |
| M-321 | A re-enterable surface states the state a re-entry opens in (INV-167): where it lands focused or positioned, and whether entry resets its internal state or resumes the state a prior visit left; a spec that pins the open ceremony, exit, variants, and guards while the entry position and reset-or-resume semantics stay blank is a finding; entry symmetry (INV-50) tests that a re-entry path exists, this tests the state it opens in; never a re-enterable surface whose entry state stays unstated passing the prover, never the lens read as a duplicate of entry symmetry | INV-167 | string | `test_spec_states_the_entry_state_lens` + `test_spec_distinguishes_it_from_entry_symmetry` + `test_prover_skill_carries_the_lens` + `test_index_row_present` | BUILT |
| M-322 | Every stated transition carries a payload lens (INV-168): the parameters a person perceives across a transition are enumerated — focus and selection landing, scroll or playback position, sound continuing, a timer running, a shown value fresh or stale; a parameter the spec leaves blank is answered by the platform default alone, and the motion-parity lens (INV-165) and the entry-state lens (INV-167) are instances this parent lens generalizes; never a transition whose payload parameters stay wholly unstated passing the prover, never the lens read as a restatement of the topology lenses it sits beside | INV-168 | string | `test_spec_states_the_transition_payload_lens` + `test_prover_skill_carries_the_lens` + `test_spec_names_the_instances` + `test_index_row_present` | BUILT |
| M-097 | spec-author's template pointer resolves for a standalone install: the skill names the pack repo as the templates' home; never an in-skill template copy (a copy forks the truth, D-4) | E-4 | string | `test_standalone_template_pointers` | BUILT |
| M-014 | Every stateful surface composes across the canonical axes + the provenance axis at adoption; never only its own axis | C-1 | string | prover FULL pass at milestones (M-1) | TODO |
| M-072 | A feature-doored spec-delta walks the standard-facet sweep (phone/narrow · touch-vs-hover · empty/error/loading · accessibility · performance envelope); the canonical list has ONE home (spec-author); a mid-work re-door re-runs the sweep; a fenced prototype is never swept | T-13 | string | `test_spec_states_facet_sweep`, `test_skills_carry_facet_sweep` | BUILT |
| M-077 | Every feature's delta closes with a non-goals sentence, ALWAYS written ("nothing deliberately left out this time" is valid); a scope-narrowing non-goal rides the batched report; never a missing sentence passing as fine | INV-20 | string | `test_spec_states_intake_trio`, `test_skills_carry_intake_trio` | BUILT |
| M-078 | Every feature states one success measure, decided or `[default]`-tagged (provenance only — no derived test row while the reading machinery is [target]); binds forward, adopted features owe theirs at first touch (A-3); never a measure claimed as machine-checked before the machinery lands | INV-21 | string | `test_spec_states_intake_trio`, `test_skills_carry_intake_trio` | BUILT |
| M-074 | A feature-delta touching a live surface opens with regression fences BEFORE the facet sweep; each fence cites the clause it guards; a fact is fenced xor re-authored; a prototype fences nothing; never a live surface touched without its fences authored first | T-14 | string | `test_spec_states_regression_fences`, `test_skills_carry_regression_fences` | BUILT |
| M-075 | A fence earns NO new matrix row — it discharges through the cited clause's never-side; an unwritten or unverified promise reconciles first (A-3) and becomes its own fact; fences named by anchor in the wish's row | INV-19 | string | `test_spec_states_regression_fences`, `test_skills_carry_regression_fences` | BUILT |
| M-073 | Every facet ends as a spec sentence — decided, or `[default]`-tagged + TOLD on the landing report's defaults list as a plain-words tradeoff, never confirmed (INV-31); adopted/promoted surfaces reconcile defaults from shipped truth; never a facet silently absent | INV-18 | string | `test_spec_states_facet_sweep`, `test_skills_carry_facet_sweep` | BUILT |
| M-268 | Each person-facing scenario states how it is ENTERED and how it EXITS (INV-127, row 192): a scenario is a flow with edges — it states how the walk arrives (from which prior scenario or state, with what already true — the preconditions assumed) and how it leaves (to where the person lands, what it leaves true for the next scenario — the postcondition); the per-operation precondition/postcondition lenses lifted to the SCENARIO level, kin of the entry-symmetry lens (INV-50) and the runtime view's flow walks (INV-74); spec-author states the entry/exit duty per flow, product-prover carries the scenario-level lens (an unstated edge is a finding, the blank-answer class of INV-72), binds forward (INV-15 — a new scenario states its edges, an existing gap is flagged not blocked), a trivially-none edge stated as such in one clause; never a scenario shipped with a silent entry or exit where one is real, never the backlog of older scenarios' edges blocking a lane | INV-127 | string | `test_spec_clause_stands` + `test_spec_lifts_pre_post_to_scenario_level` + `test_formal_index_row` + `test_spec_author_carries_the_entry_exit_duty` + `test_prover_carries_the_scenario_level_lens` + `test_matrix_row_covers_the_entry_exit_law` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-267 | Both directions of a paired state change get the same craft or a stated reason they do not (INV-126, paired-transition-symmetry wish): when a surface has a pair of opposite state changes (open/close, enter/exit, expand/collapse, show/hide), a transition crafted for one direction is a decision about the pair, so the other direction is stated too — the default is symmetry (the exit mirrors the enter unless a reason is written), a shorter or deliberately-instant exit being a valid STATED answer; it rides the standard-facet sweep as its own facet (the canonical list's home is spec-author), and because motion feel is the human's own gate (INV-30) an undecidable pair is surfaced to him; the prover flags a pair with one direction's transition described and the opposite unstated (the blank-answer class of INV-72); the temporal twin of INV-125; homes the composition clause + spec-author facet list + product-prover paired-transition check; never a crafted-in/instant-out pair shipped without a stated decision, never the exit left silent when the enter has a transition | INV-126 | string | `test_spec_clause_stands` + `test_spec_names_default_and_the_human_gate` + `test_formal_index_row` + `test_spec_author_carries_the_facet` + `test_prover_carries_the_paired_transition_check` + `test_matrix_row_covers_the_paired_transition_law` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-280 | The spec-author facet sweep carries edge completeness and the product-prover carries its lens: both ends of a threshold-gated transition and the three states (pending/arrived/failed, visible pending) of an async slot, each written as a decided or [default]-tagged spec sentence, and the prover sweeps every threshold-gated transition and async slot for an unnamed edge (a blank edge being a finding of the unwritten-seam class INV-72) (INV-138, edge-completeness wish); homes the composition clause + spec-author facet list + product-prover edge-completeness lens; never a threshold-gated transition or async slot whose edge is left unwritten, never a gated behaviour passing prove with an unnamed edge. | INV-138 | string | `test_spec_clause_stands` + `test_spec_names_both_faces` + `test_formal_index_row` + `test_spec_author_carries_the_facet` + `test_prover_carries_the_edge_completeness_lens` + `test_matrix_row_covers_edge_completeness` (red proven against pre-delta tree, 2026-07-13) | BUILT |
| M-114 | The fit walk: a feature-doored wish is interrogated for product fit at intake, kind-scaled (product: journey/return/cross-entry/implied state/feel/invited-next; infra: flows/data lifecycle/failure paths; skill: trigger/correction/not-fire); trivially-closable holes are closed by the walker AND written how; only genuine taste calls go out, batched; never a fit question left unwalked, never the person interrogated | INV-29 | string | `test_fit_walk_law` | BUILT |
| M-237 | Declared cross-cutting laws (INV-101, row 223): the spec keeps its cross-cutting laws in one declared-laws home; every new surface's section states its line against each declared law (the clause or a dated exemption) before the prover reads it (spec-author habit); the prover's station enumerates every surface and transition per declared law, a missing clause ranking as a broken invariant; the pack's own list declared (register + clock-honesty + no-self-certification, two dated exemptions); never a declared law checked only where the author remembered, never an exemption without its date | INV-101 | string | `test_the_home_and_the_packs_own_list` + `test_the_prover_station` + `test_the_author_habit` + `test_spec_anchor_and_index` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-258 | Shipped product docs state each requirement impersonally (INV-118, row 274): the rule, the actor as a role (the user, the producer, the target user), and the reason; the rationale stays and the personal attribution drops; a dated decision keeps the date and drops the name; personal attribution and candid process voice live only in the local-only diaries (JOURNAL/NEXT_STEPS), which no publish ships; the law lives in three homes — the spec Publishing clause, spec-author guidance, and the publish floor; the pack adopted the impersonal voice for its own shipped docs 2026-07-12 (the former self-exemption retired, attributions moved to JOURNAL, row 279); never a shipped requirement that reads as one person's word instead of a rule with a role and a reason | INV-118 | string | `test_impersonal_rule_in_all_three_homes` + `test_diaries_hold_attribution` + `test_spec_anchor_and_index` | BUILT |
| M-292 | Every declared cross-cutting law names its enforcing net (INV-150, the property→review routing law, tlvphotos openable-face miss 2026-07-14): a law names one of three nets — a mechanical gate (a named guardrail script or a dedicated test, deterministic, blocking in CI), the prover's judgment station (a violation pins to a stated sentence and the walk blocks), or the design review's recommendation (the deciding fact lives only in the human's intent, soft, never blocking); the net is recorded beside the law in the declared-laws home (INV-101) and the station demands one per law, a netless law ranking a broken invariant; the pack's own three laws each name a mechanical gate (register → check-shipped-language.sh + test_preshow_register_lint.py, clock-honesty → check-future-times.sh, no-self-certification → test_no_self_certification.py); declaration is the promotion lever moving a property from the design review to the prover, the architecture every-fact-owned-once check the backstop; homes the routing clause + the INV-101 field and station + spec-author's habit + the design-reviewer routing-split + the architecture backstop; never a declared law with no named net, never a property claimed by more than one net or dropped by any | INV-150 | string | `test_each_law_names_its_net` + `test_net_routing_law_stands` + `test_prover_station_demands_net` + `test_spec_author_writes_the_net` + `test_spec_index_and_ownership` + `test_cross_sibling_routing_split` + `test_pack_declared_laws_each_name_a_net` (red proven against the pre-delta tree, 2026-07-14) | BUILT |
| M-294 | The paired-transition law's second half, reversibility of the means (INV-126, tlvphotos openable-face miss 2026-07-14): where a surface opens by a continuous, reversible gesture (a pinch, a drag, a lift), the same gesture reversed stands among its ways to close, or a decided sentence states why it is absent; silence blocks as the blank-answer class (INV-72), the rightness of the deciding sentence staying the human's gate (INV-30); the continuity half shares the transition's craft across the two directions, this half carries the gesture's inverse across them; homes the composition clause + spec-author facet + product-prover paired-transition lens; never a reversible opening gesture with no inverse close and no deciding sentence, never the reason judged from the text instead of surfaced to the human | INV-126 | string | `test_reversibility_of_means_half` (red proven against the pre-delta tree, 2026-07-14) | BUILT |
| M-341 | The paired-transition law's magnitude sub-question (INV-126, tlvphotos inspect-zoom incident 2026-07-16, row 363): where the paired open and close ride a continuous, reversible quantity (a pinch span, a drag distance, a wheel accumulation), the spec states whether the inverse demands the same magnitude as the forward move — symmetric or a named deliberate asymmetry — decided or `[default]`-tagged (INV-31), the rightness of a named asymmetry staying the human's gate (INV-30); the sub-question stands in all three homes (the composition clause, the product-prover lens, the spec-author facet entry with its incident key); never a stated pair riding a continuous quantity passing the lens with only existence asked, never the asymmetry's rightness judged from the text | INV-126 | string | `test_magnitude_sub_question` (red proven against HEAD — the sub-question absent in all three homes) | BUILT |
| M-349 | The standard-facet sweep carries one band-general viewport facet (INV-18, tlvphotos caption-over-picture landscape overlap 2026-07-16, row 367; merged from the old width-only phone facet and the orientation / short-viewport facet, v2.4.0): the viewport bands run width and height alike — narrow, wide, short, tall, and the bands a future device adds — so every layout-bearing feature ends the sweep with a decided or `[default]` sentence per band its layout law names or excludes, and a law scoped to one band answers for the others, the landscape incident its key per the curated-list law; the facet stands in both homes — the spec facet list and the spec-author canonical list with its incident key — and the prover holds it with the viewport-quantifier lens (INV-138); never a layout-bearing feature swept by one fixed band with the other bands left blank, never the facet joined without its named incident | INV-18 | string | `test_orientation_short_viewport_facet` (red proven against HEAD — the facet absent from both homes) | BUILT |
| M-350 | Every layout guarantee states its viewport quantifier (INV-138, same tlvphotos landscape incident 2026-07-16, row 368): every layout guarantee names its viewport quantifier, holding on every viewport or naming the band it is scoped to, and a band-scoped guarantee draws the standing question about the other bands, a true-as-written but band-scoped guarantee the class this catches; the lens grows INV-138 (the range-and-lifecycle member of the composition-lens family, the viewport a ranged and banded quantity) instead of a new law, standing in the INV-138 clause and its index row, the product-prover edge-completeness lens, and the design review's group pass (the viewport bands a same-kind group routed through the similarity divergence, INV-150); never a layout guarantee scoped to one band with the other bands left silent, never the quantifier judged only from a text that reads clean because the claim is true as written | INV-138 | string | `test_viewport_quantifier_lens` (red proven against HEAD — the lens absent from the clause, the index row, the prover lens, and the design review) | BUILT |
| M-351 | The standing sub-domain question lifts to the general range law (INV-138, the owner's word 2026-07-17 reading the 2.4.0 report, row 369): the "answers for the rest" duty shipped worded for layout guarantees over viewport bands, and a guarantee scoped to any other named sub-domain — a user state, a network condition, a locale — is the same hole class the incident taught, so the general duty leads the law (a guarantee scoped to a named part of its domain draws the standing question about the remainder, each remaining part decided or `[default]`-tagged) with the viewport bands kept as the worked instance; the general duty stands in the INV-138 clause and its Formal-index row, the product-prover edge-completeness lens leads with the named-part ask and names viewport its worked case, and the design review's standing named-part lens and the spec-author viewport facet cite it as their group-pass and author-side instances; never the duty worded as a literal for one quantity alone, never a guarantee scoped to a named part with the remainder left silent | INV-138 | string | `test_general_sub_domain_duty` (red proven against HEAD — the general duty absent from the clause, the index row, and the prover lens, only the viewport-band wording present) | BUILT |
| M-361 | The published contract is a spec surface in the producer's own spec (E-33): it is written, proven, and tested where the producer's other surfaces are, and it earns its feature coverage there like any of them [INV-73]; its machine-readable artifact lives at the path the producer's card names [E-32], states the contract version it was generated under, and states the moment it was generated [INV-24], so a reader tells the artifact's shape and its age from the artifact itself with no second document to consult [E-14]; the law stands in the documents today, which is what this row's `string` level asserts, and the artifact machinery rides an owned queue row — a producer's real published artifact ships when a host declares its first contract (ROADMAP row 385, [target]), this pack carrying no data producer for a test to exercise; never a contract specified outside the producer's own spec, never an artifact whose version or generation stamp needs a second document to read | E-33 | string | `test_contract_is_a_spec_surface` + `test_artifact_carries_its_version_and_stamp` + `test_e33_index_and_ownership` (red proven against the pre-delta tree, 2026-07-17) | BUILT |
| M-362 | A contract publishes nothing by default (INV-185): every field leaves the producer's tree on the owner's explicit permission, recorded in the producer's tree with its date and its author [INV-24], a permission being an act with an author and a date; an agent reasoning its way to a permission from how a neighbour's product happens to be built has reasoned from nothing, since the way a system is built grants no permission, and a field with no recorded permission stays home; credentials never cross a channel at all under any permission, the published artifact being the only data path between two agents [INV-183]; product data placed in a card is a contract field taking this permission road whatever file it sits in [INV-184]; the base rulebook carries the law in the phrase "publishes nothing by default"; the law stands in the documents today, which is what this row's `string` level asserts, and its mechanical arm rides an owned queue row — the producer-side permission gate reddening a published field with no recorded permission [INV-150] ships when a host declares its first contract (ROADMAP row 385, [target]), this pack carrying no data producer for a test to exercise; never a field published on a permission reasoned from a product's construction, never a credential crossing a channel, never product data escaping the permission road by sitting in a card | INV-185 | string | `test_contract_default_deny` + `test_construction_grants_no_permission` + `test_credentials_never_cross_a_channel` + `test_base_rulebook_carries_the_default_deny_law` + `test_inv185_index_and_ownership` (red proven against the pre-delta tree, 2026-07-17) | BUILT |
| M-363 | The producer's form and its clock (INV-186): every contract field names four things — what it means, the window it is measured over, how it is aggregated, and the source it derives from — and a field missing any of the four is an incomplete surface the prover reads as one [INV-101]; the producer declares ONE number, its cadence — how often it regenerates — and holds to it on its own clock whatever its consumers are doing, a deploy refreshing the artifact as a bonus while triggering it on no occasion, since a contract triggered by a human's build goes stale the day the building stops; the cadence is a budget, so it names its watcher and the producer's own check reds when the scheduled regeneration did not run [INV-41]; the law stands in the documents today, which is what this row's `string` level asserts, and its mechanical arm rides an owned queue row — the cadence watcher ships when a host declares its first contract (ROADMAP row 385, [target]), this pack carrying no data producer for a test to exercise; never a contract field shipped missing one of its four namings, never a deploy standing as the contract's trigger, never the cadence budget left without its red | INV-186 | string | `test_every_contract_field_names_its_four_things` + `test_producer_declares_its_cadence_and_its_watcher` + `test_a_deploy_never_triggers_the_contract` + `test_inv186_index_and_ownership` (red proven against the pre-delta tree, 2026-07-17) | BUILT |
| M-364 | The consumer's read (INV-187): the consumer declares ONE number, its staleness bound — how old an artifact may be and still carry this consumer's analysis — and it is a budget too, so the consumer's freshness check reds past that bound before any analysis runs [INV-41]; the consumer pins the contract version it was written against, reads the artifact read-only (over the filesystem when co-located, over git when remote [INV-112]), checks the generation stamp against its bound before any analysis, names stale data aloud and stops there — the same honest failure a missing reach earns [INV-67] — and carries a compatibility test that reds when its pinned version and the artifact's version diverge; the cadence and the staleness bound are two numbers set independently and neither side reads the other's, so one daily contract is honest input at a thirty-hour bound and stale at a six-hour one, both readings correct [INV-186]; the law stands in the documents today, which is what this row's `string` level asserts, and its mechanical arms ride an owned queue row — the consumer-side freshness check and the compatibility test ship when a host declares its first contract (ROADMAP row 385, [target]), this pack carrying no data producer for a test to exercise; never an analysis run against an artifact past its stated bound, never a consumer writing in a producer's tree, never one side's number read off the other's | INV-187 | string | `test_consumer_declares_its_staleness_bound` + `test_consumer_pins_a_version_and_reads_read_only` + `test_cadence_and_bound_are_two_independent_numbers` + `test_inv187_index_and_ownership` (red proven against the pre-delta tree, 2026-07-17) | BUILT |

### [node: product-prover]

| ID | Fact (from spec) | Spec ref | Test level | Owning test | Status |
|---|---|---|---|---|---|
| M-160 | Process bookkeeping scales to the delta: the pre-push re-check keeps rigor (previous records · delta walked · verdict) and scales FORM — small deltas (skill/prose/infra, no new surface/structure) ride a three-line short-form record, surface/structural deltas the full walk; claims batch per lane, journal + resume once per batch; never the safety net scaled (law text, red-first, delta prove, gates), never a surface delta on the short form | INV-61 | string | `test_process_cost_scales` (red proven against HEAD's shipped state) | BUILT |
| M-015 | Every live-spec push is preceded, same session, by fence + a whole-spec re-check recorded in `docs/prover/` — and the record is for the PUSHED STATE: the newest record commit is never older than the last PRODUCT_SPEC.md commit (row 61); never a push without its record, never a today-dated-but-stale record passing. One diff-scoped carve-out (row 269, INV-112): a push whose diff against its base is exactly one NEW file under inbox/ owes no fresh record — the CI script (`guardrails/check-prover-record.sh`) and the spec (M-6) carry the same carve-out, so an inbox deposit on a recordless day stays green while any larger diff rides the full gate | M-6 | string | `guardrails/pre-push` gate a: `test_real_repo_passes`, `test_missing_record_fails`, `test_stale_record_fails`, `test_record_with_spec_same_commit_passes`, `test_inbox_only_push_carve_out_needs_no_record`, `test_diff_beyond_one_inbox_file_still_needs_a_record` (carve-out red-proven 2026-07-12) | BUILT |
| M-179 | The prover actively hunts the unwritten seam: the shipped product-prover skill carries a stress-lens that, for each stateful surface, derives the reachable situations itself — every axis it passes through while already shown (view/mode/tier/viewport/reopen) and every other co-present surface (siblings on its screen, one step before/after in the flow, stateful or not) — and flags any with a blank answer as a finding of the E-14 class; the shipped spec-author skill carries the matching "every other live surface" axis so the author writes the missing sentence as a C-1 composition invariant, `[default]`-tagged like the facet sweep; never the prover inventing the answer, never asking the human (silence stays consent, INV-31) | INV-72 | string | `test_prover_hunts_unwritten_seam` | BUILT |
| M-266 | A cross-surface policy is stated at the surface-CLASS level and held uniform across its siblings (INV-125, cross-surface-uniformity wish): when a decision governs an interaction KIND living on several sibling surfaces (a gesture policy, an affordance, an input-to-action mapping), the spec clause names the class and enumerates the surfaces it governs, and a policy written for one surface while siblings exist is a spec defect; the shipped product-prover carries the cross-surface-policy lens (enumerate the surfaces of that kind from the surface registry, flag any the clause does not cover — the check the owner asked the prover to write for itself, the finding class of INV-72), and build-pipeline's completeness guardrail family holds it mechanically for a rendered product (the policy asserted across every registered sibling root, red until all covered); the preventive twin of the class hunt (INV-124); homes the composition clause + product-prover lens + build-pipeline completeness; never a policy shipped for one surface while a sibling of the same kind keeps the default, never the DOM-wide assertion assumed for the pack itself (a skill with no DOM ships the rule + lens only) | INV-125 | string | `test_spec_clause_stands` + `test_spec_names_the_class_rule_and_the_two_holds` + `test_formal_index_row` + `test_prover_carries_the_cross_surface_lens` + `test_build_pipeline_completeness_holds_uniformity` + `test_matrix_row_covers_the_uniformity_law` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-293 | The sharpened cross-surface trigger fires on a kind-general rule worded inside one member's own section (INV-125, tlvphotos openable-face miss 2026-07-14): a sentence stating a principle for a whole kind (a way in and out, a gesture, a treatment) homed on one surface while siblings of that kind exist is the same defect in prose form, recognizable before the kind is declared; the finding demands lift-to-class enumerating the members or scope-by-decision to the one member; the prose-law form a declared-class enumeration alone would miss, since that enumeration presupposes the kind is already declared; homes the composition clause + product-prover cross-surface-policy lens; never a class-general rule left scoped to one member while siblings exist, never the sentence form passing prove because no class was declared | INV-125 | string | `test_sentence_form_trigger` (red proven against the pre-delta tree, 2026-07-14) | BUILT |
| M-190 | The re-prove exemption is lens-aware: a prover record opens naming the prover version that ran, and adoption's skip-the-re-prove exemption holds only when that version matches the installed prover — a prover that grew a lens re-arms the full pass; never a stale green surviving a lens the spec was never held to (the 2026-07-09 composition-slip mechanism, pinned) | M-6 | string | `test_prove_exemption_is_lens_aware` (red proven against HEAD — no version condition there) | BUILT |
| M-253 | A restructure/migration merge gate judges the delta (INV-114, row 258): three parts — load-bearing token identity old-versus-new modulo the per-chunk named deltas plus the punctuation-multiset check (INV-111), the full suite green on the merged tree (INV-39), and a full prover pass on both sides whose blocking set is delta-scoped (an unmatched token, a red suite, a new-side finding absent on the old side, an unnamed meaning change); pre-existing findings equal on both sides route to queue rows in the same landing and never block; and a session that sharpens a human's spoken bar beyond his words says the sharpened form back and marks it as its own interpretation; the law lives in three homes — the spec clause, product-prover's restructure-merge gate, and build-pipeline's restructure door; never a merge held on debts it did not create, never a sharpened bar applied as the human's own | INV-114 | string | `test_merge_gate_judges_the_delta_in_all_homes` + `test_preexisting_findings_route_not_block_in_all_homes` + `test_say_the_bar_back_duty_in_all_homes` + `test_spec_anchor_and_index` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-282 | The product-prover labels each finding a defect (a stated invariant violated / a false claim — blocks and folds at the push gate) or a recommendation (nothing stated broken, a consistency or quality gain — queues for a taste call, optional now/later grade), kind the sole verdict axis with the tag `kind · label` and no severity token, the kind derivable from whether a broken invariant stands behind it (INV-140, finding-kind wish); never a findings list returned undifferentiated, leaving the human to sort defect from recommendation by hand. | INV-140 | string | `test_prover_tag_carries_kind` + `test_prover_defines_defect_and_recommendation` + `test_severity_axis_retired_from_prover` + `test_push_gate_folds_on_kind` + `test_spec_clause_stands` + `test_formal_index_row` + `test_matrix_row_covers_finding_kind` (severity-collapse red proven 2026-07-14; original red vs pre-delta tree 2026-07-13) | BUILT |
| M-324 | A surface add re-verifies the document's quantified claims (INV-170): CROSS-LINK carries one mandatory whole-document step, sweeping for enumerations and universal quantifiers ("every", "only", "all", "exactly", explicit member lists) and re-verifying each against the surface set including the newcomer; a member list that now excludes the newcomer, an "only" ranging wider, or a no-longer-terminal edge is a finding at the add; never a class enumeration going stale unflagged through the very mode that grew the set | INV-170 | string | `test_spec_states_the_quantifier_reverify` + `test_spec_names_the_staleness_vector` + `test_prover_skill_crosslink_carries_the_step` + `test_index_row_present` | BUILT |
| M-325 | The audit's point fixes hold in the prover's text (INV-140, INV-126 kin): the surface-authority fallback is a stated assumption, never silence; 3d probes overlapping-data agreement; the paired-transition lens splits the declared one-sided pair (defect, blocks) from the never-declared grouping (design review, recommends) and lets an open motion question ride without holding the push (homed in the lens since the M-338 restructure); FEATURE-FIT scopes its consistency exclusion to pre-existing clauses and cites pinned clauses on shipped systems; the FULL closing summary reports the accumulated [default] count; never the clear-evidence gate self-disarming on the forgotten registry, never a delta contradiction passing as out-of-scope | INV-140 | string | `test_surface_authority_fallback_is_a_stated_assumption` + `test_3d_probes_overlapping_data_agreement` + `test_kind_block_names_motion_question_gate_semantics` + `test_feature_fit_consistency_scope_rewritten` + `test_feature_fit_verdicts_cite_pinned_clauses_on_shipped_systems` + `test_full_pass_reports_default_accretion` | BUILT |
| M-326 | A full prover pass owes a coverage record (INV-171): the stress lenses split into mandatory sweeps — declared laws, edge-condition completeness, uniformity, the lifecycle sweep gathered under the transition-payload parent (INV-168) with its six named sub-questions keeping their anchors, unwritten seams — each owing one verdict line, hit / clean / N/A-with-reason, rendered as a surface × sweep table where the classic coverage tables go N/A; the imaginative probes stay discretionary; never a skipped sweep indistinguishable from a clean one, never the record shape minted outside the review-record class (INV-156) | INV-171 | string | `test_spec_states_the_coverage_record_law` + `test_skill_splits_sweeps_from_probes` + `test_each_sweep_owes_a_verdict_line` + `test_surface_by_sweep_table_replaces_the_na_collapse` + `test_lifecycle_gathers_under_the_payload_parent` | BUILT |
| M-338 | The prover doc's reader-facing homes (outside adversarial review, 2026-07-16): the frontmatter description carries only the autoload trigger (no sibling-pass matter, no anchor codes, the answered question kept), the prover/design-reviewer boundary is homed in "When NOT to use" with the modes paragraph pointing at it, and the paired-transition kind-split lives in its lens while the KIND block stays general; NEVER the sibling pass's vocabulary back in the trigger, NEVER the family split leaking back into KIND | INV-140 | string | `test_description_carries_only_the_trigger` + `test_boundary_homed_in_when_not_to_use` + `test_kind_block_stays_general_split_lives_in_lens` (red proven against the pre-restructure file, all three) | BUILT |
### [node: build-pipeline]

| ID | Fact (from spec) | Spec ref | Test level | Owning test | Status |
|---|---|---|---|---|---|
| M-227 | Derivation rule: a norm-pointered clause (INV-43 `norm:` pointer) owes a norm-conformance matrix row — render structure vs the frozen norm (sections + row names present, no invented formats) at DOM-text or above, the look staying the human's feel-gate call; the build half (norm-open + plan-vs-prototype diff line) cited together at review; never a render inventing its own structure shipped green, never the conformance claim delegated to a pixel row | INV-43 | string | `test_derivation_owes_the_conformance_row` + `test_never_side_names_the_bounce` (red proven against the pre-law skill, 2026-07-10; M-211 is the rule's first live instance) | BUILT |
| M-306 | The forward-binding law has one stated home (INV-159): a duty binds forward from the first landing that touches its surface after the clause exists, never retroactively across what already landed, and the law is stated once in words with every forward-binding cite (formerly INV-15 / T-16-kin / A-3 / INV-21-kin) repointed at INV-159; never a "binds forward" cite resolving to a root whose own text is silent on the law | INV-159 | string | `test_forward_binding_law_stated_once` + `test_forward_binding_cites_are_repointed_off_the_silent_roots` (red proven against the pre-delta tree, 2026-07-15) | BUILT |
| M-076 | Scope, never time: a too-big wish is cut (fewer surfaces, plainer defaults) or split into stages; a time budget/estimate is never an input; the proposal proceeds on the recommended option, surfaced in the batched report; never a silent cut, never lane order moved, never the safety net touched | T-15 | string | `test_spec_states_intake_trio`, `test_skills_carry_intake_trio` | BUILT |
| M-098 | build-pipeline's template pointers (architecture + matrix steps) resolve for a standalone install: the pack repo named as their home; never an in-skill template copy | E-14 | string | `test_standalone_template_pointers` | BUILT |
| M-100 | A second bug in the same area within ~30 days re-doors to FEATURE — the area is missing an invariant; the journal grep is the detector; never a second patch on the same spot inside the window | T-9 | string | `test_gap4_recurring_bug_escalates` | BUILT |
| M-101 | The CHANGELOG speaks to the user (what changed for them + one real example); internal names/ids/rows stay in the journal; and no doc pins a drifting version number in prose — the version's one home is pointed at, never copied; never a user-facing entry in mechanism words | T-6 | string | `test_gaps5_8_docs_discipline` | BUILT |
| M-016 | A wish is one request in plain words, any size, any moment; never lost for being informal | E-2 | string | process — inbox + intake discipline; milestone re-listing (M-1) | TODO |
| M-017 | The wish walks arrived → classified → spec-delta → validated → queued → in-work → landed; never skipping the spec-delta or validation step | T-1..T-6 | string | the E-6 host-facing gates (rows 55+) (bounds check: behaviour traces to spec) | TODO |
| M-018 | A wish may exit declined / deferred / superseded — the row stays in the table; never deleted | T-8 | string | queue archive rule + milestone audit | TODO |
| M-094 | Declining an absorber lists the rows superseded into it; each listed row is declined by name or returned to the queue; never a superseded wish dying by pointer | T-8 | string | `test_spec_states_decline_absorbed` (clause in SPEC + template; the behavioural side audits at M-1 — no declined absorber exists yet to check) | BUILT |
| M-019 | A bug preempts: every rolling wish parks with its own checkpoint (at most one parked per lane), nothing red is committed, parked wishes resume in landing order; never a bug itself interrupted | T-9 | string | process — next real preemption journals it; the E-6 host-facing gates (rows 55+) | TODO |
| M-020 | Priority bends the lane visibly: critical heads the line, a quick win may bubble once, then the queue head; never a silent jump and never starvation | T-11 | string | queue audit at milestones (M-1) | TODO |
| M-021 | No wish is ever lost: the row exists the minute it is spoken; never an intake that leaves no trace | INV-1 | string | milestone re-listing of gates + inbox files (M-1) | TODO |
| M-136 | The architecture owes numbers: measurable quality budgets plus each budget's instrumentation home (numbers measured and human-readable), the project's KIND proposing the dimensions (product: paint/interaction times; backend: latency/throughput/errors; CLI/pipeline: run time, per-unit cost; skill pack: eval pass rate, suite time; prose: what honestly has a number) with a no-honest-number quality said by name; each budget asserted by a matrix-row acceptance; spec-author's performance facet ends as a budget sentence and build-pipeline's architecture step demands the kind-derived budget line; never a surface passing derivation with no budgets and no instrumentation home, never a vanity metric standing in for an unmeasurable quality, never a budget living only as prose hope | INV-41 | string | `test_architecture_owes_budgets` | BUILT |
| M-347 | Every stated quality budget owes a watcher (INV-41, the 2.3.0 design review finding 2, row 365): the budget law states that each budget names the mechanical check that reds past the stated number, or carries a decided sentence naming why it is read by eye, and a budget with neither is a derivation defect; the duty stands in all five homes — the INV-41 clause and its index row, the architecture template's budget table (a Watcher column), the prover's architecture lens (the budget bullet), the pipeline's budget instruction, and the pack's own ARCHITECTURE.md budget table (a filled Watcher cell per row); never a stated budget in a governed architecture with no named watcher and no decided eye-read sentence, never the template's budget table shipping without the watcher column | INV-41 | string | `test_budgets_owe_watchers` (red proven against HEAD — the watcher duty absent in the clause, the index row, the template table, the prover lens, and the pack's own budget table) | BUILT |
| M-140 | An approved prototype is the norm, four arms in four homes: spec-author states the pointer format (`norm: <path>` at the clause's line end, approval freezing the artifact into `docs/norms/` with a dated provenance line — a frozen copy, independent of the live prototype home); build-pipeline's code step OPENS a norm-pointered surface's artifact before building and the landing records a one-line plan-vs-prototype diff, the verify feel bar reading the same pointer; build-pipeline's door step honours a row-recorded mockup-first entry condition, cancelled only by the human naming it; product-prover carries the norm lens (prototype-born clause without pointer / text contradicting its own artifact = finding); never a general "go build" cancelling a named entry condition, never a norm-pointered surface built without the artifact opened, never the pointer format living in a second home | INV-43 | string | `test_prototype_norm_pointer` | BUILT |
| M-144 | Verify's adversarial option: a fresh-context checker briefed with the landing's SPEC sentences + artifact paths (never the worker's summary), hypothesis "tasks completed, goal missed", ladder exists → substantive (stub-grep list in step 8) → wired → flows; MANDATORY when the change is HIGH-STAKES (delta surface-sized · OR it edits the method itself — a rule whose MEANING changed) AND its only review is the author's own (no independent read = a differently-contexted head briefed from primary sources on the goal-missed hypothesis; a same-context prover pass never counts, delegation never makes it independent); one fresh checker per landing batch covers the batch (INV-61 scales form never freshness); kind-scaled for skill/prose; the checker under the worker contract, verdict in the landing report; never the checker briefed with the worker's summary (spec sentences + artifact paths only), never findings closed with a nod | INV-46 | string | `test_adversarial_verify_option` | BUILT |
| M-175 | The routing rule: a unit of work's tier is PROPOSED by its step and kind; size alone never decides it — a judgment step (spec/prove/architecture/matrix-level/triage/taste) proposes the senior and is never routed down, a no-decision one-shot proposes haiku, a self-contained multi-step brief proposes sonnet; the economy rung moves the threshold (full = map as written, lean = airtight brief one tier cheaper, tight = cheapest sufficient always); the proposal is ADVISORY — the senior may override per wish with the override logged (proposed → chosen → why) on the checkpoint and landing report; never a mechanical gate the senior cannot overrule, never a silent tier change, never a judgment step routed to a worker, never a fourth tier invented | INV-69 | string | `test_routing_rule` | BUILT |
| M-176 | A tunable parameter is set by the agent to a sensible default and TOLD in the landing report with its `[default]` tag, never asked — carries the taste-told law (INV-31) to numeric/config knobs, kin to the economy ladder (T-19); the agent moves every task it can and reserves questions for the genuinely undecidable (INV-4); where the human GRANTS it, the agent pushes to prod on its own certification when the work is sound (M-6, INV-9); never a stall on a knob it can reasonably set, never a re-ask after telling | INV-70 | string | `test_parameter_default` | BUILT |
| M-151 | A brief editing existing files is born from reading them in full: three recorded lines per file (current state · what changes · what must survive), every step back-referencing its spec sentence, every technical claim citing a source; never a brief written from memory of a file | INV-53 | string | `test_brief_trio_laws` | BUILT |
| M-152 | The worker HALT list is closed and short: ambiguous requirement · two consecutive unexplained failures · missing config/dependency · acceptance impossible as briefed — stop WITH evidence, otherwise run to completion; never an open-ended "ask if unsure", never a silent stop outside the list | INV-54 | string | `test_brief_trio_laws` | BUILT |
| M-153 | A brief is sized: text ~300 lines, ~8 files to edit [default], splits above either; paths, never inlined file bodies; never a brief inlining a file body | INV-55 | string | `test_brief_trio_laws` | BUILT |
| M-161 | Taste-heavy deliverables build smallest-first: the cheapest judgeable sample gets the human's word before the full build spends; never five full packs before one judged paragraph | INV-62 | string | `test_sample_first_and_source_reopen` (red proven against HEAD) | BUILT |
| M-162 | A rejected artifact reopens its SOURCE: correct the spec clause/card/brief first, rebuild from it; never line-patching rejected output against an unchanged source | INV-63 | string | `test_sample_first_and_source_reopen` | BUILT |
| M-023 | Every landing cites its wish row in commit or journal; never an unexplained change | INV-3 | string | pre-push hook (the E-6 host-facing gates (rows 55+)) | TODO |
| M-024 | A pending human question never stops the lane — work proceeds on the recommended option, marked in the row | INV-4 | string | queue audit at milestones (M-1) | TODO |
| M-262 | A proven artifact settles a fork before the human hears it (row 270): before surfacing a design choice, a session checks whether the architecture / spec / invariants already determine it — deriving the requirement and citing the section, offering no fork; a fork reaches the human only for a genuinely-open call; the read-the-doc twin of ask-never-guess, in the base rulebook beside it and the spec; never a fork offered on a question a proven document already answers | INV-121 | string | `test_base_rule_carries_the_twin` + `test_spec_clause_stands` + `test_formal_index_row` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-025 | Ambiguous size, priority, or work-kind is asked at intake, never guessed; the class column speaks the four-word vocabulary + priority marks only | INV-12 | string | `test_roadmap_class_vocabulary` | BUILT |
| M-026 | ARCHITECTURE.md exists, every node pinned to a real file:line, proven before the matrix derives; never an unproven architecture under a matrix | E-14 | string | `test_architecture_owns_every_anchor_once`, `test_architecture_no_orphan_nodes` | BUILT |
| M-027 | The matrix is DERIVED node × fact with the coverage validation walked; never just filled | E-15 | string | `test_matrix_covers_every_anchor`, `test_matrix_blocks_match_architecture_nodes` | BUILT |
| M-028 | No wish lands whose facts lack an owning node and a right-level matrix row (binds from this landing, never retroactively) | INV-15 | string | `test_matrix_covers_every_anchor` (re-runs at every commit) | BUILT |
| M-029 | The milestone gate runs: full re-prove + matrix audit + composition check + compaction (docs AND the test suite — a duplicate/superseded test dies only with its rows shown still covered) + gate/inbox re-listing + index re-check + the loader-stays-thin walk (line count stated; a line failing the must-hold-before-pack-loads test migrates to its real home); never a MINOR without it | M-1 | string | clause presence: `test_m1_names_loader_thin_item`; the walk itself: next MINOR walks it; journal records the walk (first walk journaled 2026-07-05, session 9: 16 non-empty lines, all pass) | TODO |
| M-128 | The milestone gate also re-walks the pack's skills through the standard skill-making skill (skill-creator's format/frontmatter/description-triggering lens — evals test behaviour, this lens tests the craft of the skill file); findings folded or rejected with a written reason in a dated record; a skill newly joining the pack walks it at birth; never a milestone without the re-walk item, never a walk whose findings vanish unrecorded | M-1 | string | clause presence: `test_m1_names_skill_creator_rewalk`; the walk itself: dated record in docs/audit/ (first full walk 2026-07-06, session 17) | BUILT |
| M-067 | The door is named before any code: every wish states size · priority · door · work-kind in ONE intake line; never code before the door is said | T-12 | string | `test_spec_states_door_procedure`, `test_working_skills_carry_the_door` | BUILT |
| M-068 | Feature tripwires are hard and ordered (surface/state/interaction/[target]/unbacked behaviour ⇒ feature); the verdict outranks a casual label, a re-doored wish takes no preemption, the door re-fires mid-work; never a wish hand-built past the pipeline for sounding small | INV-16 | string | `test_spec_states_door_procedure`, `test_base_rules_door_and_prototype` | BUILT |
| M-084 | The work-kind is named at intake: product · infra · skill · prose, one kind per wish, called from what the wish PRODUCES; a host with ONE usual kind may record a profile default; the vocabulary is curated — a fifth joins only with a named mis-served wish; never a guessed kind (uncallable ⇒ asked, like size) | T-16 | string | `test_spec_states_work_kind`, `test_skills_carry_work_kind` | BUILT |
| M-108 | One wish = one user story: a multi-story wish splits at intake, each story its own row citing the one spoken wish it came from; sub-behaviours (hover face, phone face, backpointer) are the story's acceptance only; unclear count asked at intake; never two stories fused into one new row, never a split that loses the origin | T-17 | string | `test_one_story_close_whole` | BUILT |
| M-109 | A row closes only whole: a multi-leg row's Done-when enumerates per-leg acceptance; no landing report closes a row with an unmet leg (half-done is a status); the resume file's LIVE-STATE restates an open leg at every supersession; never "COMPLETE" beside an unmet enumerated leg, never a leg compressed out of the resume file | INV-26 | string | `test_one_story_close_whole` | BUILT |
| M-085 | The kind scales each running step's FORM (per-kind table's one home: build-pipeline SKILL.md); at landing every door-granted step has APPLIED or STOOD DOWN by name in the report; an unresolved kind scales nothing down; never a silent skip, and never the safety net (door law, mandatory sentences, ask-at-intake) touched by a kind | INV-22 | string | `test_spec_states_work_kind`, `test_skills_carry_work_kind` | BUILT |
| M-115 | Product-kind verify includes the VISITOR WALK (first visit · return · cross-entry · from-any-point navigation · exits) and the FEEL pass (motion quality, affordance craft) against the approved prototype's bar, in the medium's own form (a text product walks its reading path; it carries no animations to walk); findings become rows or red; never "renders and clicks" alone passing a product-kind verify, never findings kept as vibes | INV-30 | string | `test_visitor_walk_feel_pass` | BUILT |
| M-116 | A taste choice made without asking is TOLD at landing: the report names each open `[default]` in plain words with an example and a tweakable mark; no confirmation requested, silence is consent, never re-asked — the person asks when they want a change; never a choice accumulating untold, never a forced confirmation loop | INV-31 | string | `test_default_expiry_law` | BUILT |
| M-120 | Every pipeline step is worked in its craft's mindset — the step→craft ladder (product manager at spec · formal reviewer at both prove steps · architect at architecture · QA automation at matrix and tests · senior developer at code · the visitor's own eyes at verify · release hand at commit & show) lives in ONE home, build-pipeline's step list, and artifacts are judged by their craft's standards; never a second full ladder statement elsewhere, never a generalist head excused by habit | INV-33 | string | `test_craft_ladder` | BUILT |
| M-126 | Every wish is placed on the feature map at intake: the placement spoken with the echo and written in the row's `map:` note — changes feature X · new feature · restructure; the map is the spec's scenarios + the architecture's nodes, no third document; a restructure verdict queues its own row and re-carves only through the architecture step's re-prove; never a silent placement, never a re-carve in passing | INV-37 | string | `test_feature_map_placement` | BUILT |
| M-182 | The runtime view: for every flow the spec promises, the architecture walks the running product — the serving node per step, what crosses each hop (citing the seam by name; payload/format stay the seam table's fact), where the flow can fail; one short walk per flow; a flow that cannot be walked end to end is a finding; scales by kind (per-kind flow unit named in the template: product = visitor scenario, CLI = one invocation per command, skill pack = a wish through the skills, book = one sentence); never a runtime view restating what the seam table owns | INV-74 | string | `test_spec_mandates_runtime_and_placement_views` + `test_template_carries_the_views` (red proven pre-code: template had no view sections) | BUILT |
| M-183 | The placement view: every node states its place (build-time · CDN static · client browser · edge worker · external service) plus the load-bearing technology where one exists, first-class (a node-table column or its own table); readable at a glance; scales by kind — a book's one sentence satisfies it; never placement buried in prose only | INV-75 | string | `test_spec_mandates_runtime_and_placement_views` + `test_template_carries_the_views` | BUILT |
| M-193 | The architecture reads as an LLD reader expects: the doc OPENS with the shape at a glance (tiers first, then nodes, then flows through the tiers); every runtime-view failure point carries its fallback ("if X fails, Y happens"); the placement table is framed as THE tiers-and-technology table; the crosswalk names the BMAD/Kiro counterparts; never a failure point without its what-happens-then, never a doc that starts the reader inside a node table | INV-74, INV-75 | string | `test_lld_reading_order_and_fallbacks` (red proven against HEAD — no shape-at-a-glance, no fallback column there) | BUILT |
| M-195 | Accepted work reaches the remote by rule: same-or-better + every reached gate green (verdict from the suite log) ⇒ push, never parked locally; remote discovered from the tree first; ONE contextual question only when no remote exists; every push re-walks the README against the pushed truth (crisp and current); never a routine push waiting on a spoken word, never a push past a stale README, never the human's personally named milestone gates moved | INV-82 | string | `test_push_to_remote_law` (red proven against HEAD — no INV-82 there) | BUILT |
| M-184 | The architecture lens checks SIX things and says so in all three homes (SPEC's architecture-step prose, product-prover's Phase 0 lens, build-pipeline step 4): fact ownership · node backing · named seams · budgets + instrumentation home · runtime view per promised flow · placement per node, each at the project's kind scale; never a home still speaking the three-item lens (the tlvphoto validation skipped budgets and views because the lens never asked) | INV-74, INV-75 | string | `test_architecture_lens_is_six_items` (red proven pre-code: prover + pipeline still spoke three items) | BUILT |
| M-214 | Every frozen norm in docs/norms/ carries a content fingerprint in scripts/norms-manifest.json and matches it; a norm file absent from the manifest, or drifted from its recorded fingerprint, is red — a norm changes only as a deliberate manifest update named in its landing; never a silently edited norm (the convergence locks, row 217) | INV-43 | string | `test_norm_fingerprints` (red proven by the mechanical protocol — a manifest hash flipped, the row failed, restored) | BUILT |
| M-235 | Every skill-kind landing's verify walks the installed skill-creator's review of the touched skill (INV-99, row 219): format, frontmatter, description-triggering (does the skill load when it should), evals where applicable; each finding folded or rejected by name in the landing record; the work-kind classifier is the trigger; the milestone gate's whole-pack walk is the catch-up for earlier skills; never the walk skipped on a skill-kind landing by mood, never a finding dropped without its named fold-or-reject | INV-99 | string | `test_walk_in_both_homes` + `test_findings_fold_by_name` + `test_classifier_is_the_trigger` + `test_spec_anchor_and_index` (red proven against the pre-delta tree, 2026-07-11) | BUILT |
| M-243 | The chat-law hook reminds the routing law (row 253, INV-69; his 2026-07-12 word — the orchestrator duty binds the seat, whatever model leads): the hook's output carries the routing line — the orchestrator seat orchestrates, briefs, and accepts; work routes to the cheapest sufficient tier (opus judgment, sonnet mechanical, haiku one-shot); workers locate their own anchors; the law's homes stay base rule 5 + the routing rule; never the hook promoted to the law's home, never the routing duty read as one model's own | INV-69 | string | `test_output_carries_the_routing_law` (red proven against the pre-edit hook, 2026-07-12) | BUILT |
| M-241 | Every landed queue row carries its delegation accounting and a suite check reads the queue (INV-103, row 254): a landed row's status cell records what went to a worker with a rough saving or a stood-down line naming why the senior kept the work; a suite check reads ROADMAP.md and a landed row without the line goes red; binds forward from its landing date (a row landed 2026-07-12 or later carries the line, earlier rows stay as they landed); the duty binds the orchestrator seat regardless of model; never a forward-landed row whose status cell omits its delegation accounting | INV-103 | string | `test_law_in_both_homes` + `test_spec_anchor_and_index` + `test_every_forward_landed_row_carries_the_line` (red proven against the pre-delta tree; the ROADMAP scan red-proven by the mechanical protocol — a planted landed-today row without the line failed the run, removed) — 2026-07-12 | BUILT |
| M-279 | The orchestrator read-discipline law (base rule 25, SPEC INV-137) is stated across its homes — the base rule, the spec clause + Formal-index row, and build-pipeline's delegation-accounting echo naming reads dispatched — and enshrined by a standing test; never silently dropped from any one of its homes without the suite going red | INV-137 | string | `test_base_rule_states_read_discipline` + `test_spec_invariant_137_present_and_indexed` + `test_delegation_accounting_names_reads` + `test_architecture_owns_137` + `test_matrix_row_for_137` (red proven before the law landed, 2026-07-13) | BUILT |
| M-239 | The bug door and the skip door carry one added door-step tripwire (INV-104, row 225): does this edit touch a spec-backed literal or clause (version string, pinned count, named vocabulary, promised wording)? a yes binds the docs-travel-with-the-change rule and the red-first small-fix path into one duty — the docs and the test land in the same session as the fix; the tripwire reads the edit's content, so a one-word change to a spec-cited literal owes the same duty as a full feature; never a spec-cited literal edited without its same-session docs and test, never the tripwire waived for a small diff | INV-104 | string | `test_tripwire_in_both_homes` + `test_binds_docs_and_test_same_session` + `test_reads_content_not_diff_size` + `test_spec_anchor_and_index` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-245 | The push walk reads the remote gate's verdict and a red CI is the session's own immediate bug (INV-106, row 228): a push does not end at the push, and the push step reads the remote gate's own verdict (the CI run the push triggered, one `gh run` read, minutes, no human wait); a red verdict is the pushing session's own immediate bug, preempting by the bug lane (INV-2), fixed and re-pushed the same session before anything else, so the human never meets the red first in a GitHub email; a slow gate is watched to its verdict on the detached-work cadence (INV-35); the law lives in two homes: the spec's push clause and build-pipeline step 9; never a push left standing on an unread verdict, never a red run the human meets first in his mailbox | INV-106 | string | `test_verdict_read_in_both_homes` + `test_red_is_immediate_bug_in_both_homes` + `test_spec_anchor_and_index` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-252 | A deliberate architecture redesign re-shapes and re-proves ARCHITECTURE.md in the same movement (INV-113, row 257): when structure is deliberately redesigned — layers restacked, a surface's ownership moved, nodes merged or split — the architecture document is re-shaped to the new form and re-proven with the architecture lens in that movement, not only re-pinned; the pins-only path is scoped to a boundary shift that leaves the document's shape standing, since after a real redesign the old shape itself lies and fresh pins on a stale shape are a defect; the re-carve routing (INV-37, E-14) carries the row and this states what the row owes the document; the law lives in three homes — the spec's architecture-doc clause, build-pipeline's architecture step, and the refactor line; never a real redesign shipped with only its pins updated, never a boundary shift dragged into a full re-shape it did not need | INV-113 | string | `test_redesign_owes_rework_in_both_homes` + `test_pins_only_scoped_to_boundary_shift_in_both_homes` + `test_spec_anchor_and_index` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-263 | Every new or carved architecture node passes a three-question fitness test at its birth (INV-122, row 260a): can it be tested alone · does a real second place need it · can it and its neighbour be worked in parallel without queuing on shared files — three yes make it right, a single no is a flag to answer (name the plan or fold), two or more no make it premature (folds back into its caller); first home build-pipeline's architecture step, second home product-prover's speculative-node flag (one caller + no promised second = flagged); never a carve shipped that fails two of the three, never the flag skipped on a one-caller node | INV-122 | string | `test_build_pipeline_carries_the_three_questions` + `test_prover_flags_the_speculative_node` + `test_spec_clause_and_index` + `test_formal_index_row` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-264 | Compaction is a scheduled station for CODE as well as docs (INV-123, row 260b): beyond doc-compaction (INV-115) the station widens to code — duplicate logic merges, dead weight leaves with its listing (INV-109), a ripened abstraction is extracted only through the three-question fitness gate (INV-122); two triggers — the milestone gate and the second occurrence of the same problem (base rule 19), which opens the duplication's own compaction row that moment (not an instant in-place fix), the row landing through the ordinary pipeline (one row's delta INV-39, never blocking its lane INV-56); each pass locks its reached level with a test or lint where newly reached else the existing green suite (rows 216-218, INV-98); homes the milestone-rhythm section + build-pipeline's before-a-MINOR gate; never a code-compaction pass that skips the fitness gate or leaves its reached level unlocked | INV-123 | string | `test_spec_clause_and_index` + `test_spec_names_second_trigger_and_gate` + `test_build_pipeline_minor_gate_carries_code_compaction` + `test_formal_index_row` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-313 | Compaction runs every pass, held by a gate, not saved for the milestone (INV-164, base rule 30, the 2.0 method rule): the doc- and code-compaction stations (INV-115, INV-123) run at every push, above the milestone whole-read (INV-145) that once held them alone; every push is held to the reached-clean floor by mechanical gates — the register lint at 0 errors, the redundancy gate at 0 open pairs, the debt cap that only ratchets down (`scripts/spec-debt-cap.json`, INV-98) — asserted against the live document, so no bloat accumulates between milestones; the deeper method rule reaching every project through the base rulebook — any quality a machine can verify is shipped as a runnable gate a host attaches (INV-97), held by no pass's attention, since a quality left to attention is a defect of the method; the fix for the spec bloating when compaction ran milestone-only (2026-07-15); homes the rhythm compaction clause + base rule 30 + build-pipeline's gates section; never a machine-verifiable quality left to a pass's attention, never a compaction deferred to the milestone gate | INV-164 | string | `test_machine_verifiable_is_a_gate_not_a_habit` + `test_compaction_is_continuous` + `test_index_row_present` + `test_base_rulebook_carries_the_principle` + `test_build_pipeline_carries_compaction_every_pass` + `test_live_spec_sits_at_the_clean_floor` (the live ratchet lock) | BUILT |
| M-320 | The style lint names two tiers (INV-166): the universal language laws (scissors, negation-opener, machine-jargon) bind every host's gate whatever its register; the pack-register tier (caps-shout, second-person, reassurance, future-narration) is the pack's own reference-documentation taste a host adopts on its word; `--tier universal` runs the universal tier as errors and leaves the register tier advisory, `--tier full` (alias `--gate`) runs the union with output unchanged, and the default output is unchanged; the split is declared in docs/spec-style.md; never a host forced onto the pack's register to enforce the universal floor, never the `--gate` output altered by the tier split | INV-166 | string | `test_scissors_reds_under_tier_universal` + `test_universal_leaves_caps_advisory` + `test_full_promotes_caps_to_error` + `test_gate_and_tier_full_agree` + `test_bogus_tier_exits_nonzero_with_message` + `test_live_spec_sits_at_the_clean_floor` | BUILT |
| M-265 | A confirmed bug drives a class hunt before it closes (INV-124, class-hunt wish): four moves before the bug door calls a fix done — (1) name the defect abstractly and actively SEARCH every surface for un-seen siblings, fixing all in the same change (the active face of base rule 14, going to FIND the class not yet seen); (2) an architecture check — a structural cause updates ARCHITECTURE.md; (3) a spec check — a spec silent on the broken behaviour is the real defect, fixed first so the prover can flag it (INV-15), then the code lands under it; (4) escalate to the human when the class boundary needs his read (INV-4); the product-prover carries the class lens for the same three questions; homes the F-bug spec clause + build-pipeline bug entry + product-prover class lens + base rule 14; never a point fix that leaves the siblings standing called a landing (INV-26), never the architecture and spec checks skipped on a confirmed bug | INV-124 | string | `test_base_rule_14_goes_and_finds_the_class` + `test_spec_clause_stands` + `test_spec_names_the_four_moves` + `test_formal_index_row` + `test_build_pipeline_bug_entry_drives_the_hunt` + `test_prover_carries_the_class_lens` + `test_matrix_row_covers_the_class_hunt` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-269 | Every request enters through a three-source impact read, the footprint decides the route (INV-128, row 259 — the entry station P1-P6 of the fourteen-principle architect draft, recorded live 2026-07-12): beside the door (T-12) and work-kind (T-16), the FOOTPRINT is read from the spec (what behaviour changes) + architecture (which module owns it) + code (what gets touched), producing one named footprint — presentation-only / single-module / cross-cutting — spoken in the capture echo and written in the row's footprint note; the footprint decides the road (presentation → light road; single-module → the matrix step against the module's interface; cross-cutting → the full pipeline), the footprint not the size picking the road; a source disagreement is a finding routed to its owner (bug row / spec fix / restructure row INV-37), never a silent pick; the three-source read is the verdict derive-before-fork (INV-121) rests on; the footprint re-classifies mid-work (mirroring the door re-fire); the station carries the boundary-health law (a right boundary keeps a typical request in one node, repeated cross-cuts signalling a boundary move through the architecture step INV-37); homes the intake clause + build-pipeline step zero + ARCHITECTURE boundary-health + product-prover three-source lens + communicator capture echo; never a request routed without its footprint read, never a cross-cutting change slipped onto the light road | INV-128 | string | `test_spec_clause_stands` + `test_spec_names_three_footprints_and_the_route` + `test_spec_cites_derive_before_fork` + `test_spec_carries_boundary_health` + `test_formal_index_row` + `test_build_pipeline_reads_the_footprint` + `test_architecture_states_boundary_health` + `test_prover_carries_three_source_lens` + `test_communicator_echo_carries_footprint` + `test_matrix_row_covers_the_entry_station` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-270 | Deferred rows are revisited at every queue-take, not only at milestones (INV-129, row 282 — prover finding F3, 2026-07-12): a deferred row's revisit trigger (T-8) can be time-bound (before the next release, when the campaign ships) and so come true and lapse between two milestone gates, making the milestone re-scan (M-1) too rare a reader; at every queue-take the session also re-scans each deferred row's trigger against the current moment, a fired trigger returning its row to the runnable head (INV-49) right then; the two cadences read the same triggers by the same rule so a deferred wish never waits on a trigger nobody reads (INV-1) whichever comes first, the trigger vocabulary staying free-form since a reader now runs at queue cadence; homes the queue-take clause + build-pipeline's queue-take walk; never a deferred trigger read only at the milestone while a queue-take passes it by, never a fired trigger left parked until the next gate | INV-129 | string | `test_spec_clause_stands` + `test_spec_states_both_cadences` + `test_formal_index_row` + `test_build_pipeline_carries_the_queue_take_rescan` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-274 | A critical non-bug heads the queue but never preempts a rolling lane, and the bound is echoed at intake (INV-133, row 284 — prover finding F5, 2026-07-12): "critical" is defined in bug terms but liftable onto any door (T-11), so a critical non-bug could head the queue and then wait for the current lane while a human who said "critical" for a live break expected bug-like preemption; preemption belongs to the bug door alone (T-9), so a critical non-bug lands as soon as the current lane reaches its checkpoint (ahead of everything else waiting) but never stops the rolling lane, a genuine live break being a bug; the capture echo (INV-27) says the bound back when a wish is marked critical on a non-bug door, so the human hears the difference and can re-door it a bug; the human's word still owns priority (INV-9), the clause never refusing the mark; homes the priority clause + communicator's capture echo; never a critical non-bug silently expected to preempt, never the queue-head-only bound left unsaid at intake | INV-133 | string | `test_spec_states_the_bound_unambiguously` + `test_spec_states_the_intake_echo` + `test_formal_index_row` + `test_communicator_echo_carries_the_bound` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-275 | A landed feature-or-refactor row carries its footprint note, and a suite check reads the queue (INV-134, row 291 — the mechanical follow-on INV-128 deferred): the footprint the intake read named (presentation-only / single-module / cross-cutting, INV-128) is written in the landing row's `footprint:` note beside door/kind/map, and a suite check reads ROADMAP.md, reddening a landed feature-or-refactor row without a footprint note; this is the mechanical floor under the footprint read, built to the same shape the delegation-accounting check (INV-103) gives the routing rule; binds forward from the footprint read's own landing (2026-07-12 ~17:01, when INV-128 landed), rows landed before it staying as they landed; the ROADMAP row template and the capture echo carry the footprint field; homes the enforcement clause + the ROADMAP template field + the footprint-note suite check; a cutoff-day feature/refactor row with no `~HH:MM` stamp is placed by its landing ORDER (a pinned genuinely-pre-law set), not by the absent time — a forward no-time row is required (fail-closed), closing the missing-time escape (row 297); never a forward-landed feature/refactor row shipped without its footprint note, never the bare word "footprint" in prose mistaken for the note, never a forward no-time cutoff-day row escaping on its missing time | INV-134 | string | `test_law_in_the_spec` + `test_spec_index_row` + `test_roadmap_template_carries_the_footprint_field` + `test_capture_echo_carries_the_footprint_field` + `test_every_forward_landed_feature_row_carries_the_note` + `test_missing_time_cutoff_day_row_does_not_escape` (the scan red-proven by stripping a footprint note from a checked row, 2026-07-12; the missing-time escape red-proven against the escape-open `_required`) | BUILT |
| M-254 | The full pass states the compaction discipline in the M-1 doc-compaction step: compact means no redundant information (a fact lives once, in one home, with pointers), a pass removes only redundancy and keeps anything whose removal changes meaning or a reader's understanding, a redundant-looking second statement can be a fact's sole home (per-item judgment, kin to INV-39 and the traceability-read index rows), a pass removing substance accounts for each removal [INV-109], restructure only for a faster reading shape and only via the content-preserving layout vehicle [INV-111]; never a mechanical strip that drops a fact's only home | INV-115 | string | `test_fact_lives_once_phrase` + `test_removal_keeps_meaning_phrase` + `test_per_item_judgment_phrase` + `test_spec_anchor_and_index` (red proven against HEAD) | BUILT |
| M-255 | The full pass proves the architecture beside the spec (INV-116, row 271): every milestone gate [M-1] and every push gate [M-6] runs the product-prover over ARCHITECTURE.md as well as PRODUCT_SPEC.md, the architecture pass recorded in docs/prover/ beside the spec's and carrying the spec's freshness rule — a record predating the last ARCHITECTURE.md change is stale; owner build-pipeline, executed by product-prover; never a milestone or push gate proving the spec alone while the architecture goes unchecked | INV-116 | string | `test_full_architecture_reprove_phrase` + `test_both_documents_named_phrase` + `test_spec_anchor` + `test_spec_anchor_and_index` (string clauses) + `test_stale_when_architecture_changed_after_record` (guardrail red-proven) | BUILT |
| M-277 | No pack skill states a superseded size/time numeric delegation trigger (INV-69, row 295 — the surviving arm of the row 262 delegation consolidation): base rule 5 and the routing rule [INV-69] settle the trigger as judgment-vs-mechanical with size a weak hint only, never the decider, but a divergent operative trigger ("delegate when ≥1 holds — >3 files touched/read ... a script/suite runs >~30s ...") survived in build-pipeline's junior-delegation passage, contradicting base rule 5 and INV-69; the passage now states delegation only as the judgment-vs-mechanical test and points at base rule 5 / INV-69 as its home, and a suite check scans every `skills/**/SKILL.md` for the superseded shape — a `>` comparison threshold on a file count or a running time ('>3 files', '>~30s'), or the 'N files touched/read' file-count phrasing — keying on the threshold SHAPE so a demoted-proxy sentence ("size is a weak hint only") and benign prose ("scan in 30 seconds", "~8 files to edit") never trip it; never a pack skill carrying a size/time numeric delegation trigger | INV-69 | string | `test_no_pack_skill_states_a_size_or_time_delegation_trigger` + `test_scan_has_teeth` (red-proven against the surviving build-pipeline trigger, 2026-07-12) | BUILT |
| M-286 | When the product and the spec diverge, the spec is the definition of correct, and changing it is a decision (INV-144): a divergence defaults to a possible error in the product checked against the spec, first named — what the spec states, what the product does, why they differ — and routed to the home that owns it (INV-37); a product that is wrong is fixed to the spec (INV-124), a spec silent where the product is correct is completed to state the guarantee with a test pinning it, a spec that conflicts with a correct product is changed only when the spec is confirmed the error and the human has understood the divergence and given their word (INV-4, INV-9); homes the reconciliation clause beside the three-source read + product-prover's divergence-finding pointer + the Formal-index row; never the spec silently rewritten to match the product, never a spec change landed without the human's ratified word | INV-144 | string | `test_spec_states_the_definition_of_correct` + `test_prover_carries_the_divergence_pointer` + `test_architecture_owns_144` + `test_matrix_row_for_144` (red proven against the pre-delta tree, 2026-07-14) | BUILT |
| M-296 | The request-layer classifier: a closed door set entered at the highest document its change reaches (INV-151, companion of the property→review routing INV-150): a request enters at the highest document in the derivation chain (spec → architecture → matrix → code → docs) whose sentences must change to satisfy it, the settings ladder standing beside the chain for a pure value; the door set is CLOSED — product behaviour → the spec, a technically-phrased request → the architecture with the spec-motion tripwire firing AT INTAKE (a surface/state/unbacked-behaviour trip lifts it to the spec at the door, outranking the label INV-16, re-firing mid-work), a defect → the matrix with a red-on-bug test, docs-only → its light path, a tiny reversible edit → the skip shortcut owing the spec-backed-literal tripwire INV-104, a settings/parameter value → the settings ladder, an outsider's request → one inbox wish INV-17, a method/skill change → the same criterion with work-kind skill (no separate meta-layer set), a see/try → a labelled sketch, research/a doc question → no layer, a hand-back → feedback-intake T-20; a request matching no kind is one plain question INV-4, the closed set making no-gap-between-layers checkable through the applied-or-stood-down-by-name landing contract INV-22; homes the entry-layer clause + build-pipeline's door step + the Formal-index row; never a request routed by a guess when it matches no kind, never the closed set silently bypassed by a hand-built path | INV-151 | string | `test_entry_layer_criterion_stands` + `test_one_plain_question_fallback` + `test_closed_set_at_the_build_pipeline_door` + `test_intake_moment_back_check` + `test_inv151_index_and_ownership` (red proven against the pre-delta tree, 2026-07-14) | BUILT |
| M-298 | The unification, stated once: request, property, work item, and the earned message are one routing principle (INV-153): the request classifier INV-151, the property net INV-150, the deferral test INV-152, and the earned message INV-189/INV-191 are one principle — every incoming thing routes to the home whose declared sentence governs it, and a thing that pins to no home is itself the finding (a request matching no kind → one plain question INV-4, a netless declared law → a broken invariant INV-101, a held work item that cannot name its human-only fact → the seat INV-143, a question no work of the sender's stands on → dropped, the holding being the finding INV-191); the first three route a thing arriving inside one project and the fourth routes a thing leaving one project for another, manufactured traffic being a homeless item with a stamp on it; declaration is the lever in all four (declaring the blocked work promotes a message from a wish to a thing a gate can read); they stay separate controls under one principle verified each landing at no new machinery — the classifier by the applied-or-stood-down contract INV-22, the property net by the declared-laws station INV-101, the deferral by the derive-before-defer posture INV-143, the earned message by the receiving sweep's gate on the named blocked work INV-189; the count word is a spec-backed literal travelling with the control set (INV-104), swept across its homes (the spec's clause, its index row, build-pipeline's door step); never a control merged into another's station, never an incoming thing left homeless by silence, never a stale count left standing in one home after the set grew | INV-153 | string | `test_unification_clause_stands` + `test_names_all_four_controls` + `test_count_word_tracks_the_control_set` + `test_the_count_agrees_across_its_homes` + `test_inv153_index_and_ownership` (red proven against the pre-delta tree, 2026-07-14; the four-control growth red-proven 2026-07-17 on the count literal) | BUILT |
| M-368 | The birth walk (T-22): a capability no agent's zone owns, or one that has outgrown its host, lets any agent propose a new agent — the proposal naming the capability, the zone the new agent would own, and the contracts it would publish — and it stands as a proposal until the owner ratifies the birth, like every agent-initiated proposal [INV-193]; ratifying the founding and declaring the agent are two acts on two objects — the owner's word authorizes the founding, since a new agent is a new tree, queue, gates, and a standing cost he carries, and making a new zone sits inside no agent's zone [INV-10], while the founded agent's own hand writes its card and every scan finds it from that moment [E-32]; the founding is a landing, so the new tree's journal carries it with its date and the wish row it cites [INV-3, INV-24]; a card the owner never authorized appears in the same scan that finds every other, which is the road a false declaration travels to the human, and no gate reds on it today [INV-150]; the contract outlives the migration — when a capability moves from its old host to a new agent the artifact's address and its version may both move while the consumer's pin survives, the consumer reading its pinned version until it chooses to move [INV-187], and the new owner publishing at the address its own card names, so a migration that breaks a consumer's pin has broken the contract rather than moved it; the skill-or-agent grain is taste, the human-only fact this deferral names [INV-152], so the owner's word settles it and the proposing agent's own journal records the call with its date [E-31, INV-182]; never a birth inferred rather than ratified, never a migration that breaks a consumer's pin, never a grain call made on the owner's behalf | T-22 | string | `test_agent_birth_walk` + `test_ratification_authorizes_the_founding_and_the_agent_declares_it` + `test_contract_outlives_the_migration` + `test_grain_is_the_owners_call_recorded_with_its_date` + `test_t22_index_and_ownership` (red proven against the pre-delta tree, 2026-07-17) | BUILT |

### [node: parallel-lanes]

| ID | Fact (from spec) | Spec ref | Test level | Owning test | Status |
|---|---|---|---|---|---|
| M-022 | One landing at a time per repo under one pen; in-work rows never exceed T-18's lane cap (three [default] — the number's ONE home is T-18) and claiming stays an atomic committed flip; never a fourth row in-work unasked, never two foreign sessions sharing a repo's pen | INV-2 | string | `test_roadmap_in_work_cap` | BUILT |
| M-129 | Parallel lanes: one session may roll up to three INDEPENDENT build lanes without asking (T-18's [default] cap, his 2026-07-06 word; a fourth only on the human's asked word) — opening narrated, every train on the departures board, a waiting lane naming whom it waits behind; penless stages overlap (later trains' code and tests each in its own isolated tree only; disjoint-file workers within one lane; read-only analysis free); pen-stages serialize (every shared doc — spec, architecture, matrix, queue, journal, resume file — plus integration and row close), a pen-stage never cut mid-edit; never a fourth build lane unasked, never parallel lanes across sessions, never mid-milestone, never a spec-delta proven against another lane's half-written draft | T-18 | string | `test_parallel_lanes_law` | BUILT |
| M-130 | Landing purity under several trains: a landing commit carries exactly one row's delta and its gate (full suite + guardrails) runs on a tree holding nothing of any other lane's unfinished work; after a landing the waiting lanes re-check the fence and re-run their gates on the new truth — landed-first wins; never half of another train riding a landing, never a later lane landing unre-verified | INV-39 | string | `test_landing_purity` | BUILT |
| M-147 | Lanes are picked by a dependency graph at queue-take (edge = shared surface / spec section / skill file / doc region), opened on a pairwise-independent set up to the T-18 cap, integration-only collisions pre-rolling with the landing order DECLARED at claim; tiny rows ride serial (parallel pays only when build stages dominate) and the choice is narrated on the board; never lanes picked by mood, never an undeclared integration order, never the cap read as counting one lane's serial rows | INV-49 | string | `test_lanes_by_graph` | BUILT |
| M-242 | The drafter-applier pipeline is the standard colliding-rows form (INV-49, row 255): when queued rows collide on shared files landings serialize under the pen and the penless DRAFT stage overlaps the current landing — a drafter worker at the judgment tier returns the next row's exact edit strings with self-verified needles while the applier lands the current row; landing order stays declared and numbers follow it; before handoff the drafter walks a standing self-verify list against the suite's cross-reference laws (index density · owning node in ARCHITECTURE.md's owns-list · matrix-row-under-owner), so the draft catches these suite-enforced cross-references itself rather than costing the applier a red-first STOP (row 263); born live 2026-07-12, the night the law batch serialized on the spec/matrix/version chain and still moved at two rows an hour; never a drafter deciding what the applier lands, never an undeclared landing order | INV-49 | string | `test_form_named_in_the_pipeline` + `test_born_live_and_cited` + `test_standing_self_verify_list` (all red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-272 | A mid-work re-door rebuilds the parallel-lanes independence graph (INV-131, row 286 — prover finding F7, 2026-07-12): a lane re-doored to feature mid-work (INV-16) can create a surface overlapping a rolling sibling, but the independence graph (INV-49) was not rebuilt, so the departures board kept asserting independence after it stopped being true; the same mid-work re-check now re-runs the independence edges against every rolling lane, a new edge pulling the re-doored lane back to serial with a board line, so the board never asserts a stale independence after the ground moved; the integration re-fence (INV-39) still catches the collision at landing, so this closes the observability gap rather than adding a net; the senior still judges independence and says it aloud, the re-door being when that judgement is owed again; homes the mid-work re-door clause + build-pipeline's door re-fire; never a re-doored lane left on the board as independent after it created a colliding surface, never the board asserting an independence the ground no longer supports | INV-131 | string | `test_spec_clause_stands` + `test_spec_states_the_rebuild_and_the_board_line` + `test_formal_index_row` + `test_build_pipeline_carries_the_rebuild` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-372 | The lane branch and its walk (E-34, T-23, row 386): a lane's isolated copy is a git worktree holding a branch of its own, so the mechanism T-18/INV-105 named only as a word now has a name — `lane/<row>-<slug>`, so `git branch --list 'lane/*'` reads the open lanes off the machine itself; the branch is cut from the claim commit and that claim commits to MAIN under the pen, because INV-2 orders two claims by git ancestry and a claim on a lane's own branch would sit outside the ordering, leaving two sessions each reading itself as first [INV-2, INV-117]; a worker lane takes its worktree today with no permission of any kind, the Agent tool's `isolation: "worktree"` option carrying no gate; the walk runs claim on main → branch cut into its own worktree → code and tests built there penless → integration takes the pen → teardown, a parked lane keeping both with the board saying which [T-9, INV-27]; never an isolated copy that stays a word while the fallback is the shared tree INV-105 forbids, never a claim committed where INV-2's ancestry cannot read it | E-34, T-23, T-18 | string | `test_spec_names_the_worktree_mechanism_and_the_branch_name` + `test_spec_grants_a_worker_lane_its_worktree_with_no_gate` + `test_the_claim_commits_to_main_and_the_spec_states_why` + `test_every_new_anchor_carries_an_index_row` (red proven against the pre-delta tree, 2026-07-17) | BUILT |
| M-373 | The pen moves main and the lane's branch is penless, and the pen keeps the shared documents (INV-198, row 386): the pen's existing integration clause read at the branch grain makes holding the pen the sole right to move main, while a lane commits to its own branch as often as it likes and that traffic is penless, nothing another lane reads having moved [T-18]; the documents stay under the pen even though branches remove the textual collision, because the collision the pen prevents was never textual — two lanes drafting deltas on two branches would each prove against a spec the other is about to move, and the second to land would carry a proof of a document that no longer exists, since a rebase re-runs the tests and no suite reads a proof; so the shared tree stays clean of every lane's unfinished work, turning INV-39's precondition from a discipline into a structure; never a lane's document delta proven against a spec another lane is about to move, never a branch read as buying back the pen | INV-198, T-18 | string | `test_the_pen_moves_main_and_the_lane_branch_is_penless` + `test_the_pen_keeps_the_documents_and_the_spec_states_why` (red proven against the pre-delta tree, 2026-07-17) | BUILT |
| M-374 | Git itself refuses another worktree's move of a checked-out main, BY DEED (INV-198, row 386): the road's central claim is that the pen's new clause needs no gate the pack writes, because while the primary tree holds main every other worktree's `checkout main`, `branch -f main`, and `push . HEAD:main` is refused by git BY NAME, and `--force` does not buy the push refusal off; this is the one road fact no document can hold up — the spec's sentence would read the same on the day git stopped refusing — so it is proven on a real two-worktree repo built per run in the temp home under the suite's own prefix [INV-100], with the machine's global and system git config switched off so the run reads git's defaults rather than this machine's habits [INV-155]; the probe asserts it IS two trees with one holding main before any assertion reads it, so a probe that failed to build reds instead of handing every row below a vacuous pass (row 384); never a refusal claimed from the spec's own sentence, never a guarantee certified by a string test that would stay green after the tool changed | INV-198, T-18 | browser-computed | `test_checkout_of_a_checked_out_main_is_refused_by_name` + `test_force_update_of_a_checked_out_main_is_refused_by_name` + `test_push_to_a_checked_out_main_is_refused_by_name` + `test_the_push_refusal_is_not_bought_off_with_force` (red proven by mutation 2026-07-17: with the primary tree stepped off main — the guarantee's own precondition removed — all four go red) | BUILT |
| M-375 | The refusal's boundary, BY DEED — where the tool's guarantee stops (INV-198, row 386): the guarantee rests on the primary tree HOLDING main, which is exactly why INV-198 marks the config-health assertion of that condition [target] — with no tree on main, `branch -f main` from a lane simply succeeds; and git guards the three porcelain roads while leaving named doors open, each verified rather than assumed — `git update-ref refs/heads/main` carries no worktree check at all and moves a checked-out main silently, leaving the primary tree's index inconsistent (the very damage the push refusal names in its own error text), `git checkout --ignore-other-worktrees` is a documented one-flag override, and the push refusal is the `receive.denyCurrentBranch` config DEFAULT rather than a law, vanishing under `=ignore`; pinning the boundary beside the guarantee is what keeps the road from reading wider than the tool it rests on, and each of these rows going red would be good news, meaning git closed a door and the law can grow; never a guarantee stated wider than the deed supports, never the config-health condition left as prose because the happy path passed | INV-198, T-18 | browser-computed | `test_the_refusals_lapse_when_no_tree_holds_main` + `test_update_ref_walks_past_every_refusal` + `test_update_ref_leaves_the_primary_tree_inconsistent` + `test_the_checkout_refusal_carries_a_documented_override_flag` + `test_the_push_refusal_is_a_config_default_rather_than_a_law` (red proven by mutation 2026-07-17: each mutation removes the door the test claims to observe — restore the precondition, drop the override flag, unset the config — and the row goes red) | BUILT |
| M-376 | A lane lands by fast-forward from a rebased branch, and teardown removes what it made (INV-199, row 386): at integration the lane takes the pen, rebases onto main's tip, runs the landing gate on the rebased tree, and fast-forwards main; rebase and fast-forward are derived rather than chosen — a merge commit's second parent breaks INV-39's one-row landing commit and a non-linear main breaks INV-2's ancestry order; the gate's first check is that the branch's merge-base with main equals main's tip, so a lane that has not rebased reds BEFORE the gate runs and the gate never reads a stale tree, and landed-first wins exactly as INV-39 states, every still-rolling lane rebasing onto the new truth and re-gating; teardown removes a landed lane's branch and worktree, is refused on a worktree holding uncommitted work (that refusal being a finding, since it protects a parked lane's work), and a lane worktree or a `lane/*` branch with no open row reds in the config-health gate [target], the prover's station being the net meanwhile [INV-150]; never a landing gate run on a tree that has not rebased, never a lane's worktree left standing behind a closed row | INV-199, INV-39 | string | `test_the_landing_is_rebase_then_gate_then_fast_forward` + `test_teardown_and_the_stale_lane_check` (red proven against the pre-delta tree, 2026-07-17) | BUILT |
| M-377 | The landing's two mechanisms, BY DEED (INV-199, row 386): the merge-base predicate the gate's first check runs — `merge-base(main, HEAD) == rev-parse(main)` — really does separate a stale lane from a rebased one, proven on a real repo across all three moments (cut from main's tip: equal; main moves under it: unequal; rebased: equal again), so the check reds a stale tree rather than passing it to the gate; and a rebased lane's fast-forward really does leave main's tip carrying exactly ONE parent, which is what makes INV-39's one-row landing commit structural under branches rather than a discipline; a predicate asserted from the spec's prose would prove only that the spec states a formula, never that the formula discriminates; never a merge-base rule trusted from prose, never a landing shape claimed without the parent count read off the real commit | INV-199, INV-39 | browser-computed | `test_merge_base_check_separates_a_stale_lane_from_a_rebased_one` + `test_a_rebased_lane_fast_forwards_main_with_no_merge_commit` (red proven by mutation 2026-07-17: drop the rebase and the stale lane reads as rebased; land with `--no-ff` and the parent count goes to two) | BUILT |
| M-378 | Lane conflict — the nets that exist and the residual named out loud (INV-200, row 386): git halts the rebase on a textual conflict so the tool is that net, the lane resolving it in its own worktree with its gate re-running from the top on the resolved tree; a semantic conflict is the one surviving a clean textual merge and it meets the two nets the road actually holds — every document delta stays under the pen so two lanes' documents never diverge to reconcile [INV-198], and the full suite on the rebased tree reads two lanes' diverging code; a semantic conflict surviving a green suite on the rebased tree is a fact no test covers, which is a matrix gap routed to the matrix's own home rather than answered with a net invented here [INV-73, E-15]; naming the residual is what keeps the road honest, since the section claims no net it does not hold; never a net claimed that the method does not have (the ordinary landing gate runs no prover pass), never a semantic residual papered over instead of routed | INV-200, INV-2 | string | `test_the_semantic_residual_is_named_rather_than_papered_over` (red proven against the pre-delta tree, 2026-07-17) | BUILT |
| M-379 | Git halts a rebase on a textual conflict, BY DEED (INV-200, row 386): the road names the tool as its own conflict net, so the net is exercised rather than quoted — two lanes edit one line, the rebase halts with a CONFLICT and a non-zero exit, and the landing cannot proceed; never a tool's net asserted from the sentence that claims it | INV-200, INV-2 | browser-computed | `test_git_halts_a_rebase_on_a_textual_conflict` (red proven by mutation 2026-07-17: point the lane's edit at a different file and the rebase sails through, reding the row) | BUILT |
| M-380 | Two worktrees share one ref store, so a foreign session's claim needs no fetch, BY DEED (T-18, INV-198, row 386): the lanes law does not fire across sessions, and that sentence scopes the cap, the board, and the independence judgment while the pen's own arbitration fires across sessions and always did — INV-2's re-check names a foreign session's committed in-work row and INV-117 breaks a genuine tie; that arbitration only works if a peer can READ the foreign claim, so the sharing is proven on a real repo rather than assumed: a claim commit made on main in the primary tree is readable from the lane worktree with no fetch of any kind, by rev-parse and by log; this is the live case rather than a hypothetical, several windows sharing this repo today; never a cross-session claim assumed readable, never "not across sessions" read as "nothing governs two sessions on one repo" | T-18, INV-198 | browser-computed | `test_two_worktrees_share_one_ref_store_so_a_foreign_claim_needs_no_fetch` (red proven by mutation 2026-07-17: build the lane as a CLONE instead of a worktree — its own ref store makes the foreign claim need a fetch and the row goes red) | BUILT |
| M-382 | Teardown's refusal protects a parked lane's work, BY DEED (INV-199, row 386): the road states that teardown is refused on a worktree holding uncommitted work and that the refusal is a FINDING rather than an error, since a parked lane's unfinished work is the thing it protects — so the refusal is exercised rather than trusted: `git worktree remove` on a lane holding uncommitted work is refused by name and the tree survives, and an UNTRACKED file alone is enough to trigger it, which is what makes it a real guard on a lane whose work never reached the index; the boundary sits beside the guarantee as everywhere else on this road — `--force` walks past the refusal and erases the tree, so the protection is a default rather than a law, and a teardown step that reaches for `--force` to get past a stuck lane destroys exactly the work the refusal exists to save; never a teardown refusal trusted from the prose that claims it, never a parked lane's unfinished work erased by a forced teardown | INV-199, INV-39 | browser-computed | `test_teardown_is_refused_on_a_worktree_holding_uncommitted_work` + `test_the_teardown_refusal_carries_a_force_override` (red proven by mutation 2026-07-17: leave the lane clean and the refusal has nothing to protect; drop `--force` and the override reds) | BUILT |
| M-381 | The law's default and the tool's gate agree through one vendored line (INV-201, row 386): INV-105 fires worktree isolation on a machine-readable condition — two lanes' write-sets overlap — while the `EnterWorktree` tool fires only on a human's spoken word or a project instruction, and it lists work on a feature (INV-105's own case) among the cases it declines, so today NEITHER fires and the fallback is the shared tree the law forbids; the tool's own contract names the road out, accepting a project instruction as authorization equal to a human's word, so adoption vendors one line into the host's project instructions and that line CITES INV-105's condition rather than restating it, which keeps the condition's one home [INV-101] — a restatement would give the condition a second home in every adopted host and the two would drift the first time INV-105 is reworded, across N hosts at once with no gate reading them; a machine-wide instruction line would reach every project on the machine including trees that never adopted the pack and whose owners never spoke, so the line is scoped to the host it governs and versioned in that host's own tree, the catch-up walk carrying it to a host adopted before this law [A-11, INV-159]; each host's line records that host owner's word, live-spec's riding the owner's 2026-07-17 request; a worker lane needs no line at all, its isolation option carrying no gate, so the vendored line matters only when the session itself moves into a lane; the adoption gate reds a host without the line [target], the prover's station being its net meanwhile [INV-150]; never INV-105's condition given a second home in every host, never a machine-wide line spoken for owners who never spoke | INV-201, INV-105 | string | `test_the_vendored_line_cites_inv105_rather_than_restating_it` (red proven against the pre-delta tree, 2026-07-17) | BUILT |

### [node: publish]

| ID | Fact (from spec) | Spec ref | Test level | Owning test | Status |
|---|---|---|---|---|---|
| M-089 | Publishing owes the artifact's kind its checklist — the per-kind table's ONE home is the publish skill (floor: what/who/how-to-start, claims true today, license explicit; skill → install+commands+when-NOT; tool → real runs; visual product → fresh screenshots; prose → reading path); never a deposit past the checklist | E-20 | string | `test_publish_skill_carries_checklist` | BUILT |
| M-141 | A version push re-opens the shopfront: publish's fire-list includes any push shipping a new version; the walk checks README claims against the pushed truth + kind-owed visuals (skill pack: diagrams; visual product: fresh screenshots; tool: example runs); the commit-and-show step points at the walk and the landing report carries its outcome ("shopfront checked — current" when untouched); never a version push past a stale claim, never the checklist's home moving out of the publish skill | INV-44 | string | `test_shopfront_fresh_at_push` | BUILT |
| M-090 | Publish targets are PLUGINS embedding their steps (GitHub · plugin directory · design project), never removing the kind's owed minimum; the walk runs BEFORE the human's gate and never sends anything itself; never a publish bypassing base rules 12/17 or a host push gate (M-6) | E-20 | string | `test_publish_skill_carries_checklist`; behaviour: first real use = our own next public push, journaled | BUILT |
| M-225 | Everything built with the method says so: the standard attribution line ("made with live-spec" + the pack repo link + the pack version the project runs, read from the attach record) stated in the publish skill's shared floor and the duty in the spec clause; the line is an OFFER, never a gate — the walk says once when absent and proposes, the owner decides, a declined offer closed and never re-asked; on the pack's own standalone mirrors the sync script stamps the line onto README.md and SKILL.md from the live VERSION file; never the wording duplicated into a second home, never a blocked publication over the line, never a re-ask, never a hand-written mirror footer (an invented number, wiped by the next rsync) | INV-96 | string | `test_standard_line_stated_in_both_homes` + `test_publish_walk_offers_the_line` + `test_declined_offer_never_reasked` (red proven 2026-07-10; softened same day on his correction) + `test_sync_script_builds_the_line_from_the_live_version` + `test_sync_script_stamps_both_landing_files` (red proven 2026-07-11, row 246) + `test_script_wording_locksteps_with_the_publish_floor` (the row-246 prover pass's drift guard) | BUILT |
| M-339 | Every standalone mirror carries a generated release-history section (INV-181, row 360): one line per shipped version — version · date · one story line — harvested by sync-mirrors.sh from the pack's own git history (oldest commit per version wins; the story cuts at the first colon or period outside parentheses and sheds trailing parentheticals), stamped fresh into the mirror's README.md ahead of the attribution line at every sync, the assembled README scanned for stray Cyrillic and owner names before the push (the shipped-language machine's reach, INV-120), with JOURNAL.md linked as the full story's home; the section is printable by `--print-release-history` for verification; NEVER a hand-kept history on a mirror, NEVER the section on SKILL.md, NEVER a follow-up commit's subject shown as a release's story | INV-181 | string | `test_print_mode_emits_real_history` + `test_loop_stamps_before_attribution` (red proven — the print mode absent at HEAD) | BUILT |
| M-340 | A mirror's generated blocks are one declared kind with three members (INV-181; the 2.2.0 design review's one question, landed on the owner's word 2026-07-16): the read-only banner at the README's top, the release-history section, and the attribution line — each declared in the spec and each pinned by a test, so no generated block on a public surface drifts with no net; NEVER a sync-written block on a mirror that no clause names | INV-181 | string | `test_banner_is_the_kinds_third_declared_member` (red proven on the spec arm — the kind unnamed before the clause) | BUILT |
| M-171 | The shipped plugin metadata matches the pack's truth: both `.claude-plugin` JSONs parse and carry a non-empty description, plugin.json's version equals the VERSION file, and a description naming ANY pack skill names them ALL; never a validator-flagged gap, never a partial skill list (the row-167 drift class), never a stale version pin | INV-44, INV-66 | string | `test_metadata_described_and_current`, `test_descriptions_never_carry_a_partial_skill_list` | BUILT |
| M-259 | The instance→engine boundary crosses clean — provenance and naming (INV-119, row 276): a generalized engine's spec records how each behaviour landed in its own code — a reconciliation log headed "how each behaviour landed in code", each entry citing the engine's own public commit ("landed in engine commit <hash>"), one intro sentence stating the normal intake path (proven first on a live instance, then generalized), no private-instance hash standing as engine provenance; a mechanism carries a neutral internal name and a visible instance label is marked as instance-supplied copy [INV-79], the neutral term staying the one name [E-4]; the publish gate checks a generalized package for the two leaks — a foreign provenance hash (mechanical) and an instance locale label as a mechanism name (tripwire + advisory) [E-20]; three homes — the spec F-pair clause, spec-author's boundary section, the publish generalized-engine check; never a generalized spec citing a private-instance hash, never an instance's UI label standing as the engine's mechanism name | INV-119 | string | `test_reconciliation_phrase_in_spec` + `test_engine_commit_phrase_in_spec` + `test_proven_first_phrase_in_spec` + `test_reconciliation_phrase_in_spec_author` + `test_engine_commit_phrase_in_spec_author` + `test_proven_first_phrase_in_spec_author` + `test_reconciliation_phrase_in_publish` + `test_engine_commit_phrase_in_publish` + `test_proven_first_phrase_in_publish` + `test_spec_anchor` + `test_spec_anchor_and_index` | BUILT |

### [node: skill-evals]

| ID | Fact (from spec) | Spec ref | Test level | Owning test | Status |
|---|---|---|---|---|---|
| M-086 | Every working skill owns an eval file: scenario · criteria · dated bare-run record (red PROVEN, never asserted) · re-run instructions; the required set derives from skills/ itself, so a fifth working skill is red until its eval exists; never a skill without its eval | E-19 | string | `test_skill_evals_present` | BUILT |
| M-087 | The eval method states its honest boundary (bare = bare-of-the-SKILL, the machine loader still feeds method) and the authoring rule (the scenario speaks like the human — no enumerated facet hints); run records are dated and append-only; evals re-run at milestones (M-1 list) and at behaviour-changing skill landings; never a contaminated red sold as clean | E-19 | string | `test_eval_readme_states_honest_boundary`; re-run discipline: milestone audit (M-1) | BUILT |

### [node: communicator]

| ID | Fact (from spec) | Spec ref | Test level | Owning test | Status |
|---|---|---|---|---|---|
| M-170 | The showing channel matches the session's seat: local seat opens the rendered window, remote seat sends the same content through its own channel (artifact page / chat), seat detected and the pick said; never a local path handed to a remote reader | INV-67 | string | `test_showing_seat` (red proven against HEAD — the law absent there) | BUILT |
| M-030 | A landing is reported in one plain-language line (position · what landed · what remains); never a report that only points at internal rows | T-7 | string | process — communicator rules 8/9; milestone audit | TODO |
| M-092 | Batched questions arrive as ONE decision page (one card per pick, recommendation named, free-form room), answers archived in docs/decisions/ and harvested the same session; never a serialized chat questionnaire, never an answer left un-harvested | E-22 | string | `test_spec_names_decision_page` (SPEC clause + rule-10 mechanics in the shipped communicator SKILL.md) | BUILT |
| M-099 | Communicator's trigger is narrowed: decisions, landing/milestone reports, problems needing the human's word — with a stated NOT-side (mid-work status lines, internal notes, plain factual answers); never a description that fires on every passing report | T-7 | string | `test_communicator_trigger_narrowed` | BUILT |
| M-107 | A done-claim ("did we do X?") is answered as an evidence walk: each claim pinned to a checkable artifact walked NOW (adoption/prover record, suite run, commit, matrix row), verified vs asserted said apart, and the answer names the method version from the host's installed set — or says plainly that none exists; never a narrative answer standing alone, never "done by live-spec" without its version, never an invented version for an unadopted host | INV-25 | string | `test_done_claim_evidence_walk` | BUILT |
| M-111 | The capture echo: the moment a wish is intaken the human hears ONE plain sentence — what was heard, the door called, the name the work will answer to, its row number; a silent arrival (inbox file, harvest) echoes in the next report, never as an interruption; never a wish intaken without its echo, never the echo replacing the written row | INV-27 | string | `test_capture_echo_and_board` | BUILT |
| M-112 | The departures board: every status report names each in-flight feature by its echo-name with its pipeline station (all nine steps: spec → prove → architecture → prove architecture → matrix → test → code → verify → commit & show, plus the terminal landed), the station vocabulary being the pipeline's own step names, one station per step; never progress reported as prose archaeology, never a station name drifting from the pipeline's step names, never a pipeline step left without its own station | INV-27 | string | `test_capture_echo_and_board` | BUILT |
| M-113 | The outcome does the talking: echo-names are short descriptive phrases in the product's words (never a private metaphor); a human-facing report/board line opens with what changed for the reader; spec codes, row/session numbers, and coined names only TRAIL in parentheses; one fact = one standalone sentence; never a line led by a nickname the reader never chose to learn, never riddle-compression | INV-28 | string | `test_outcome_leads_law` | BUILT |
| M-117 | A decision card opens with what the choice CHANGES for the person under each option, in the product's words; mechanism only after, only if it helps; never a card answerable only by understanding the mechanism | INV-32 | string | `test_decision_card_consequences` | BUILT |
| M-121 | The bookkeeping NEVER-list: rule 8 names the tokens that are never message content (a test count, a suite size, a version string, a check tally) with the translation the message says instead ("tested clean", "saved") and a worked before/after example; the carve-out stands — a direct question about the number, or the done-claim evidence walk (INV-25), gets the number as the answer; never bookkeeping doing the talking in a human-facing message, never the carve-out lost | INV-28 | string | `test_bookkeeping_never_list` | BUILT |
| M-133 | The chat laws' mechanical voice: `hooks/chat-law-hook.sh` exists, is executable, and its one line carries both laws' teeth — "plain product words" doing the talking with codes only allowed to "trail in parentheses", and narration naming the wish and station, digesting station ends, owing a heartbeat past ~10 minutes; `scripts/install-session-hooks.sh` covers BOTH session hooks (clock + laws, UserPromptSubmit wiring) and runs by the agent's own hand, said aloud in the report (trust.self-install, his 2026-07-11 grant; the human's hand stays equally legal); the skills stay the laws' homes; never the hook line legislating law the skills don't hold, never a SILENT install — every install is named in the session's report | INV-28 | string | `test_output_carries_both_laws` (with `test_script_exists_and_executable` + `test_installer_covers_both_hooks`; red-proven on the absent scripts first) | BUILT |
| M-233 | The chat-law hook reminds the no-scissors law (row 249): the hook's output gains the say-what-it-IS line — each thing named by its own positive sentence, the contrast frame (an em-dash or comma leading into a denied neighbour) banned in every text, the law's home named (personal profile language.no-scissors); the register self-scan checklist already carries the same line (communicator's scissors scan, checklist item 9); never the hook promoted to the law's home, never the reminder line itself using the frame it bans | INV-28 | string | `test_output_carries_the_no_scissors_law` (red proven against the pre-edit hook, 2026-07-11) | BUILT |
| M-177 | The pre-show lint's mechanical arm: `scripts/preshow-lint.py` reads a human-facing artifact's prose and flags any line that OPENS with an internal handle (a spec code, a row or session number) so it is rewritten to lead with the reader's outcome before the human sees it — a warning to clear, never a silent rewrite, scanning the shown surface only (the spec's own trailing anchors stay legal); the pre-report walk runs it | INV-28 | string | `test_leading_handle_goes_red` (with `test_outcome_led_and_trailing_anchor_pass`) | BUILT |
| M-178 | Where we are now is answerable in any seat: a short NOW (work in hand + station) and NEXT status kept current in the CHAT (the one surface every seat shows, INV-67), refreshed at each station change with a heartbeat on a long stretch (INV-35); never relies on the harness task list or spinner (browser-absent, local-only and intermittent); the harness list when shown is kept plain-worded (INV-28), a rendered status page is the local seat's optional view; binds for every project live-spec runs | INV-71 | string | `test_live_status` | BUILT |
| M-122 | The pre-report walk: communicator states the walked step — before any movement-end or milestone report goes to the human, the rules are re-read and the draft passes phrase by phrase through "does this sentence stand for a reader outside the pack" (a pack surface named is explained in the reader's words or dropped; quiet trailing anchors stay legal); never a movement-end report sent unwalked, never the walk becoming a confirmation request | INV-34 | string | `test_pre_report_walk` | BUILT |
| M-124 | Working narration: while work runs, beats are narrated as they happen — a pipeline station passed, a load-bearing find, a change of direction — one-two plain sentences in the roadmap's terms, the reports' voice, the mechanical grind quiet; the three teeth stand in both homes (SPEC INV-35 + communicator rule 13): IDENTITY — every beat names the wish and station in hand (the work's own name outside the pipeline) and whether it mends or builds; DIGEST — a station's completion is a beat whose line digests what the station produced in the work's own words, a worker-closed station becoming the senior's beat when its result lands; HEARTBEAT — a beatless stretch past ~10 minutes [default] gets a line naming what grinds; a narration line stays chat, carrying no report status — it walks no pre-report walk, asks no questions, and the plain-language + bookkeeping laws still bind it; never a beat-worth stretch left silent between echo and report, never a station closed silently, never a digest pasting the artifact or speaking in counters, never narration replacing the milestone report or triggering the walk | INV-35 | string | `test_working_narration` | BUILT |
| M-138 | The heartbeat's offline-window face: when a coming stretch needs nothing from the human — a local suite run, a worker batch, a long render — narration says BEFORE it starts that he may step away, an honest range for how long (unknown said as unknown, never a guess dressed as a promise), and what he is needed for at its end; the needed-again moment is its own beat — a chat line awaiting his return, never a summons; beats keep landing inside the window (a read, never a dismissal), its questions batch to its end, an off-range end says itself (overrun, done sooner, blocked on his word alone); the clause stands in both homes (SPEC INV-35 + communicator rule 13's heartbeat tooth); never an offline sentence when the very next beat needs the human, never the superseded row-138 fence sentence left standing in either home | INV-35 | string | `test_offline_window` | BUILT |
| M-139 | His word on a shown artifact is read as meant, in both homes (SPEC INV-42 + communicator): a phrasing the human killed in a review round stays killed in every later draft of that artifact — the kill-list written where the artifact's project keeps its records (journal or notes file), never only in session memory; a vivid phrase of his is adopted only as meant — mockery of a bad draft is not guidance, intent read from context or asked (INV-4); never a cut phrasing resurfacing in a later draft, never a wipe resurrecting a cut, never his sarcasm earnestly baked in as prescription | INV-42 | string | `test_his_word_read_right` | BUILT |
| M-156 | The stretch's end is unmissable: one short final line LAST (what closed · what's next · what's needed · when the agent wakes), the long report above it, a page deliverable repeating its passport; the line must be delivered, not merely exist somewhere in the output; never a stretch ending in a wall above tool noise, never a silent sleep | INV-57 | string | `test_stretch_end_unmissable` | BUILT |
| M-157 | Approved text is frozen: a revision applies exactly the named correction, never a fresh rewrite around it; churn of approved material = a defect; never a banned pattern smuggled in by a rewrite of settled text | INV-58 | string | `test_promoter_harvest_trio` | BUILT |
| M-158 | No question asked twice: recorded answers searched before any ask; an already-answered question = a defect; dialogues converge — answered closes forever + harvested same session, named problems return solved with evidence, round N+1 only new; never a re-ask a record already answers | INV-59 | string | `test_promoter_harvest_trio` | BUILT |
| M-159 | A taste ask carries a mined proposal (exemplars/options with citations + a chosen recommendation); never an empty-handed taste question offloading the mining to the human | INV-60 | string | `test_promoter_harvest_trio` | BUILT |
| M-163 | A review surface carries per-claim provenance (artifact · his recorded word · the agent's inference, inferences loudest) and is commentable with answer capture (the JSON law extended from decision pages); never a read-only wall, never an unmarked inference | INV-64 | string | `test_review_provenance_commentable` (red proven against HEAD) | BUILT |
| M-149 | Anything handed/opened to the human leads with its passport: the project's NAME in the visible content (never only the URL) + the read contract ("needs your word: what, by when" or "just an update, no action"); the announcing chat line carries the same two facts; never an anonymous page opened at him, never an artifact silent about whether it wants him | INV-51 | string | `test_artifact_passport` | BUILT |
| M-150 | During an away-stretch (the offline window is the trigger) no browser window opens mid-stretch: artifacts accumulate on ONE page, opened once at the stretch's end; mid-stretch re-open only as the same page refreshed in place; precedence stated: INV-52 governs WHEN, the show rule HOW, the passport WHAT; never a window per artifact at an away human | INV-52 | string | `test_windows_accumulate` | BUILT |
| M-127 | The feature map on demand: on the human's ask ("show me all the features" and kin) the WHOLE map is read at ask-time off the spec's scenario sections, the current-vs-target header (statuses at the [target] tag's own granularity — a mixed scenario reads "shipped, with promised parts", never one blanket status), and the queue's open rows (stations for in-flight, queued NEW-verdict wishes included); answer lines obey the line law; chat by default, rendered page on the human's word; a host with nothing to read is answered honestly (bootstrap/adoption pointer); never a third document created or cached, never fired uninvited (routine reports keep the board's in-flight scope), never a queued wish missing from the map | INV-38 | string | `test_feature_map_on_demand` | BUILT |

| M-137 | The session's task list is a language-law surface: task subjects and spinner lines speak plain product ENGLISH (the docs language) — understandable at a glance, what is being done for which feature — with codes, row numbers, and internal step names only trailing; never a subject that is a bare code chain, never task titles in jargon the human must decode | INV-28 | string | `test_task_list_plain_words` | BUILT |
| M-192 | The pre-ask scan covers questions wherever they ride (report tail, decision page, lone ask): the outside-reader phrase-by-phrase read plus the FIRST gate "can I decide or verify this myself?" — a failing question is work done instead of asked, a surviving one carries its recommendation; never a jargon question offloading a decidable call onto the human | INV-81 | string | `test_pre_ask_scan_covers_questions` (red proven against HEAD — no INV-81 there) | BUILT |
| M-196 | The register lint's mechanical arm: `scripts/preshow-register-lint.py` reds on each caught leak class — English coined collocations shown raw ("the wish door", "full rigor" / "work lean", tonight's onboarding abstraction), Russian calques of pack terms («швы с соседями» · «работники в поле» · «смерть счёта» · «стыки поверхностей»), the profile's known-bad list, transliterated pack terms («пайплайн» · «воркер») — and greens on the nine accepted reader docs, proving each pattern is the specific coinage while the ordinary constituent words ("station", "door", "lean") stay legal; never a pattern firing on an ordinary industry word, never a fixture leak passing <!-- user-language --> | INV-83 | string | `test_lint_reds_on_each_fixture_leak` (with `test_full_rigor_and_work_lean_both_caught_on_one_line`, `test_lint_greens_on_accepted_reader_docs`; red proven on tonight's real leak fixtures, 2026-07-10) | BUILT |
| M-197 | The register lint is wired as a BLOCK: communicator's pre-show walk names `preshow-register-lint.py` as a blocking step (a red stands the showing down, unlike the leading-handle arm's warning), and the spec carries the grows-by-one duty — each leak past the lint becomes a pattern the same day with its source and date, plus the named residual (the clean-reader check owns novel abstractions); never the lint as advisory, never a leak without its same-day pattern | INV-83 | string | `test_communicator_names_the_lint_as_a_blocking_preshow_step` (red proven against the pre-fold walk — the wiring red, 2026-07-10) with `test_spec_carries_grows_by_one_duty` | BUILT |
| M-215 | The register lint's pattern set only grows: the live pattern count stays at or above the floor pinned in scripts/register-lint-floor.json, and the floor itself never lowers; never a quietly removed pattern (the convergence locks, row 217) | INV-83 | string | `test_register_lint_pattern_floor` (red proven by the mechanical protocol — the floor raised past the count, the row failed, restored) | BUILT |
| M-216 | The prose-debt caps ratchet downward only: the recorded cap values stay at or below the reached values pinned in the owning test itself, so raising a cap is a visible test edit; never a quiet cap raise (the convergence locks, row 217) | INV-83 | string | `test_debt_cap_only_downward` (red proven by the mechanical protocol — a cap raised, the row failed, restored) | BUILT |
| M-222 | Every ask hears an honest time range at its capture echo (unknown said as unknown, never a guess dressed as a promise); long work explained up front in plain steps; a long stretch occasionally says roughly how much remains; the landing report states the estimate beside the actual — in BOTH homes (spec clause + communicator echo/heartbeat/report rules); never an estimate said once and never settled, never a range invented with no observed shape behind it | INV-93 | string | `test_estimate_at_the_echo` + `test_landing_settles_estimate_against_actual` (red proven against the pre-law homes, 2026-07-10) | BUILT |
| M-232 | Detached-work visibility (INV-35 tightened, row 248): any operation expected to run past ~2 minutes detached (a background command or a delegated worker the chat does not stream) opens with a START line (what runs, where its log lives, an honest range), keeps a beat every ~2 minutes or at each stage [default], and closes with a DONE digest; the trap named — detached work writes only to its log and shows in no agent panel, so silence reads as lost; the mechanism stays free, visibility is the requirement; never a multi-minute detached run left silent, never the harness's own panel trusted as the visibility channel, never a waiting timer narrated as a beat of work | INV-35 | string | `test_cadence_in_both_homes` + `test_the_trap_is_named` + `test_mechanism_free_visibility_required` + `test_spec_index_row_carries_the_cadence` (red proven against the pre-delta tree, 2026-07-11) | BUILT |
| M-223 | No line certifies its own sincerity: the law stands in the spec and the communicator's never-list, the register lint carries the caught-phrase family (say-so-plainly, honest-treatment, unsoftened, «из честного», «честно говоря») with the pattern floor raised to 22, and the pack's own README reads clean of the class; never a shown artifact praising its own honesty, never a caught phrase left out of the lint <!-- user-language --> | INV-94 | string | `test_law_in_both_homes` + `test_readme_swept_of_the_class` (red proven against the pre-law homes, 2026-07-10) | BUILT |
| M-224 | One spoken leave-word winds the session down to a shutdown-safe stop: workers halted or run to their landing (an unhaltable one recorded, its death-with-sleep said aloud), every lane at its checkpoint, red never committed (failing test tops the resume file), green committed under standing gates, resume file says what resumes where; first beat gives minutes-to-safe, last is ONE closing line — safe to power off + what resumes where — said only when every point above holds, in all three homes (spec clause + communicator leave-word rule + base checkpoint rule 6); never the closing line before the walk holds, never a leave guessed from silence | INV-95 | string | `test_leave_word_reaches_a_safe_stop` + `test_closing_line_says_safe_and_what_resumes` + `test_never_said_early` (red proven against the pre-law homes, 2026-07-10) | BUILT |
| M-247 | A rewrite or restyle that removes substance names every removal in its landing report (INV-109, row 233): a rewrite or restyle that removes a section, an argument, a rationale, or a worked example lists every removal in its landing report, one line of judgment each — the fact kept and where, the owner killed it by name, or the rewriter proposes dropping and asks; a removal the rewriter cannot justify becomes a question before the report closes; never a silent cut of substance; the rule scopes to substance and leaves line-level wording free; the rule's one home is communicator rule 6 (its landing report owns the accounting), stated in full in the spec clause; build-pipeline's docs-only door points at that home rather than restating it (R6 compaction, 2026-07-14); never a silent cut of substance, never a line-level tightening dragged into the accounting | INV-109 | string | `test_removal_rule_in_both_full_homes` + `test_scope_carveout_in_both_full_homes` + `test_build_pipeline_points_at_the_one_home` + `test_spec_anchor_and_index` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-250 | The README states the feels boundary as the method's own position (row 242, rides INV-84/INV-83, no new invariant minted): one clean-writer paragraph appended to the README's "Why live-spec, when BMAD…" critique block, before "## Known issues" — a spec owns what a project can write down and test; feel belongs to the owner's eye; the method answers taste by routing (the feel pass at verify, the visitor walk, the list of taste defaults), never dressing a taste call up as a test; the photo-portfolio project stays cited as the case that taught the boundary; this pin exists because gate h's `check_tests_present.py` (INV-97, this repo attached as its own first host) reds on any README.md diff with no tests/ change, regardless of whether the row mints a law; never a user-facing README claim landed with no owning test | INV-84, INV-83 | string | `test_stance_paragraph_present` + `test_stance_paragraph_before_known_issues` (red proven against the pre-delta README, 2026-07-12) | BUILT |
| M-271 | A withdrawn decision converges — after two withdrawals the recommended option is taken as a surfaced [default] (INV-130, row 285 — prover finding F6, 2026-07-12): an answered question closes forever (INV-59) but a withdrawal re-asks in plainer terms with no cap of its own (INV-9), so a genuine taste call could loop unbounded; the bound is two — on the second withdrawal of the same decision the session takes the recommendation and surfaces it as a [default] on the landing report, silence staying consent from there and never re-asked (INV-31), the same convergence an answered question already has; a later real change of mind rides the ordinary channel as a new wish, never a reopening of the closed decision; homes the decision-page clause + communicator's rule 10; never a withdrawn decision re-asked past the second withdrawal, never the converged default demanded back as a confirmation | INV-130 | string | `test_spec_clause_stands` + `test_spec_states_the_bound_and_its_kin` + `test_formal_index_row` + `test_communicator_rule_carries_the_bound` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-334 | A remote seat shows textually by construction (INV-67 sharpened, row 350): inline markdown or a host-rendered artifact page; `open` and other local-display verbs are the local seat's arms, never attempted remotely; the seat is re-detected after any move between machines; never a teleported session showing into nowhere on its old channel | INV-67 | string | `test_remote_show_is_textual_by_construction` + `test_seat_is_redetected_after_a_move` | BUILT |


### [node: templates]

| ID | Fact (from spec) | Spec ref | Test level | Owning test | Status |
|---|---|---|---|---|---|
| M-031 | The queue is one table: wish · class · status · acceptance, one row per wish; never a second scale or format | E-3 | string | `test_roadmap_class_vocabulary` (columns + vocabulary) | BUILT |
| M-032 | The matrix shape: inventory + node blocks + coverage validation; never a bare row bucket | E-5 | string | `test_matrix_blocks_match_architecture_nodes` | BUILT |
| M-033 | Every matrix row states the DO and the NEVER side; never a row without its regression fence | INV-6 | string | `test_matrix_rows_have_level_and_negative_side` + `test_gap10_step5_both_sides` (the derivation step TEACHES it, row 12 gap 10) | BUILT |
| M-194 | The architecture template carries the field-norm pieces adopted 2026-07-09 (sources checked on his ask — BMAD/Kiro/arc42/C4/Google DD): a Decisions section as ONE pointer table to where decisions live (never a second home), the seam row naming where a crossing's schema lives, and the placement view saying where secrets live + which tier holds non-client verdicts; the pack's own doc models all three; never a decisions section holding the decisions themselves | E-3, INV-75 | string | `test_field_norm_pieces` (red proven against HEAD — none of the three there) | BUILT |
| M-034 | Bootstrap = VCS gate FIRST → copy the six templates + the suite scaffold (which DEFINES green for landing #1: docs present, headers filled, no surviving placeholder) → hooks offered as at adoption → first wish through the pipeline; never landing before the gate, never a bootstrap without a runnable suite | B-1 | string | `test_templates_ship` + `test_scaffold_bootstrap_runs` (simulated bootstrap BY DEED: filled → green, placeholder → red) + `test_spec_states_bootstrap_order` | BUILT |
| M-103 | The problem-ledger template ships the shape: signature + dated occurrences + the full status vocabulary (WATCHED · OWNED · AGREED NON-PROBLEM · SOLVED) + a dated ARCHIVED tail; never a shape missing a status or the archive home | E-24 | string | `test_problems_template_shape` | BUILT |
| M-164 | The kill-list's mechanical face ships: the template (dated literals, appended, never removed; scanner note) + the guardrails scanner guidance; never a cut living only in memory, never a removed kill-line | E-26 | string | `test_kill_list_mechanical` (red proven against HEAD — template absent there) | BUILT |
| M-146 | The resume file is a digest with a hard cap: the whole file ≤ 100 lines [default], the check red-proven on a synthetic bloated file and green on the pack's own NEXT_STEPS; the template states the cap; open legs restate as one terse line each (INV-26 by form); never a cap satisfied by dropping an open leg; the number's normative home is the law, template and check restating it | INV-48 | string | `test_resume_digest_cap` + `test_resume_cap_catches_synthetic_bloat` (tests/test_resume_digest.py) | BUILT |

### [node: attach]

| ID | Fact (from spec) | Spec ref | Test level | Owning test | Status |
|---|---|---|---|---|---|
| M-079 | Founding questions asked or profile-read at bootstrap AND owed at adopt orient (A-1 pointer); personal-vs-reusable first; never a founding answer inferred from examples | B-2 | string | `test_spec_states_founding_and_designsync` | BUILT |
| M-165 | Onboarding at setup: SPEC states the who-am-I-working-with step (B-3: profile found ⇒ loaded and said; absent ⇒ offer from the template, every line on the human's word), the base rulebook and ADOPT carry it, the profile template ships with placeholders marked; never a profile line written without the human's word, never a worker running the step | B-3 | string | `test_onboarding_step` (red proven against HEAD — clause and template absent there) | BUILT |
| M-035 | The host owns its docs and a `.live-spec/` folder (profile, checkpoints, installed versions); never pack state scattered into host folders | E-1 | string | `test_host_profile_recorded_override` (dogfood instance) | BUILT |
| M-036 | The attic archives superseded host files with a manifest line; never deletion | E-9 | string | `test_adopt_phases_cite_spec` (procedure text; behavior = next adopt run) | BUILT |
| M-037 | No adopt or rework run deletes a host file; authored content never bypasses the attic | INV-7 | string | next real adopt run journals it; the E-6 host-facing gates (rows 55+) long-term | TODO |
| M-038 | No landing into an unversioned host: git + (remote exists OR explicitly declined, recorded); never a mere recommendation | INV-8 | string | next bootstrap/adopt run journals the gate outcome | TODO |
| M-039 | Adoption codes name meanings; sequence lives elsewhere. The VCS gate runs FIRST; never an irreversible touch before it | A-0 | string | `test_adopt_phases_cite_spec` | BUILT |
| M-040 | Orient: every existing document is read before anything is touched; never a blank-slate assumption | A-1 | string | `test_adopt_phases_cite_spec` | BUILT |
| M-041 | Inventory: code, surfaces (seeding the registry), docs — with owners; never an unlisted surface | A-2 | string | `test_adopt_phases_cite_spec` | BUILT |
| M-042 | Existing docs re-engineer into live-spec shapes, claims marked unverified until reconciled; never trusted unreconciled | A-3 | string | `test_adopt_phases_cite_spec` | BUILT |
| M-043 | Superseded files move to the attic during adoption; never silently replaced | A-4 | string | `test_adopt_phases_cite_spec` | BUILT |
| M-044 | The version-control gate: init, .gitignore, pristine baseline, remote settled as a named deliverable, closed only on completed evidence; never on a mere recommendation | A-5 | string | `test_adopt_phases_cite_spec` | BUILT |
| M-045 | On any version change the agent re-reads the changed SKILL.md + journals old → new; re-stats at every breakpoint; never coasts on a stale copy | A-7 | string | `test_adopt_phases_cite_spec` (text; behavior audits at M-1) | BUILT |
| M-091 | install.sh copies every pack skill into the skills home, idempotently — an existing copy is backed up with a timestamp before overwrite; never a skill deleted, never a second run destroying the first | E-21 | string | `test_install_sh_installs_and_backs_up` (a REAL run against a temp home, twice) | BUILT |
| M-118 | install.sh backs up a replaced skill OUTSIDE the live skills directory (an attic folder beside it), so the harness never lists stale duplicates as loadable skills; never a backup landing inside ~/.claude/skills | E-21 | string | `test_install_backup_home` | BUILT |
| M-131 | The pack update check (a REAL run against fixture files): a newer remote yields the spoken proposal — both versions, the what-changed pointer, the install.sh/pull road, the PROPOSAL-ONLY sentence; an equal or OLDER remote reads "up to date" (forward only, never a downgrade); an unreadable remote reads one honest skip line NAMING the address, stamp left unwritten; a same-day stamp skips quietly; never an install performed, never a block on no network | E-25 | string | `test_pack_update_check` (REAL script runs against temp stamp/remote/installed files) | BUILT |
| M-046 | Adopt working artifacts live tracked in `.live-spec/adopt/`; never scattered into the host's own folders | A-8 | string | `test_adopt_phases_cite_spec` | BUILT |
| M-047 | A cruft sweep is offered, listed, human-gated, regenerable-only; never silent and never authored content | A-9 | string | `test_adopt_phases_cite_spec` | BUILT |
| M-071 | Adoption gives every unbacked live surface a human verdict — promote / quarantine / attic; never an unbacked surface silently registered as product | A-10 | string | `test_adopt_phases_cite_spec` (A-10 citation + verdict wording) | BUILT |
| M-125 | The project knows its own kind: `project.kind` asked at founding and at adoption's orient — always the human's answer, never profile-seeded — recorded in the host profile, seeding project-wide defaults without overriding an explicit line, updated on the human's word the moment evolution is noticed, journaled; never inferred from examples, never a founding fossil an audit has to catch | INV-36 | string | `test_project_kind` | BUILT |
| M-200 | The engine/instance split is proposed at founding and at adoption's orient when the reusable answer lands on a content-carrying product — proposed, never imposed; the human's word decides, both outcomes recorded (declined = a one-line reuse note, the offer returns only when the product outgrows one home); taken = engine repo (public, generic fixtures, content contract) + instance home (content, corrections, private fragments); never a split imposed, never the founding questions fused | INV-85 | string | `test_pair_split_proposal` (red proven against the pre-fold text — no INV-85 there, 2026-07-10) | BUILT |
| M-201 | Leading a pair: each repo a full host with no third document across the seam; one spec each (the engine cites no instance content, the instance cites contract-entry handles only); two queues with a both-shaped wish split at intake, one row per queue citing the spoken wish; the instance's inbox as the human's front door; lessons travel only through the inbox door under write-ownership; one window per repo of the pair, unsure means ask; never a window writing the pair's other tree beyond one inbox file | INV-86 | string | `test_pair_leadership_law` (red proven against the pre-fold text, 2026-07-10) | BUILT |
| M-217 | The catch-up walk has one named home: MIGRATION.md opens with the routing (a never-adopted host goes to adoption, an already-adopted host goes to this walk) and carries the four phases in order — orient on the delta, plan behind the owner's gate (the plan in `.live-spec/adopt/`, the owner's word before any file moves), execute preserving facts (baseline commit + checkpoint), verify and re-record; pair routing (own repo's walk + one inbox wish for the other half; an under-attached half runs the full adoption first) and machine-level once-per-machine steps each with an already-done check; the spec section and the guide tell one story; never a guide step that moves files ahead of the gate | A-11 | string | `test_catchup_walk` (red proven against the pre-rewrite MIGRATION.md, 2026-07-10) | BUILT |
| M-218 | Every catch-up step is safe on a half-done state: the guide states the precondition read from the tree, the skip-as-done branch, and the both-forms-present merge — file by file, identical drops the old to the attic, profile lines reconcile up the settings ladder (the personal-profile write re-reads before appending), every other differing file rides the plan to the gate; the installed-set record re-reads from the version lines of the skills on disk; never a bare `git mv` with no target-exists branch, never the old directory nested inside the new, never the new form overwritten by the old | INV-89 | string | `test_catchup_walk` (red proven against the pre-rewrite MIGRATION.md, 2026-07-10) | BUILT |
| M-219 | The catch-up preserves facts and re-homes them: the guide states the no-blanket-rewrite law (settled prose rewritten only on the owner's rejection or a shape that cannot hold it); the canonical document set lives ONCE in adopt/ADOPT.md and adoption.md + MIGRATION.md point at it; a host keeps its own doc names via the `spec.file` host-profile line (a rename only offered) and the base's package-defaults table carries the `spec.file` row; stray root checkpoints re-home to `.live-spec/checkpoints/`; never a second canonical-set list in another guide | INV-90 | string | `test_catchup_walk` (red proven against the pre-rewrite guides, 2026-07-10) | BUILT |
| M-220 | A release that owes hosts actions ships one dated, versioned MIGRATION.md chapter; the catch-up walk is version-chained — the ordered chapter chain from the host's recorded version to the current one, oldest first, one plan for the whole chain; a record with no readable version starts the chain at the earliest chapter (the half-done-state law makes over-application harmless); never a host-visible change released with no chapter and no changelog line, never a chain walked newest-first, never a versionless record stalling the walk | INV-91 | string | `test_catchup_walk` incl. `test_versionless_record_starts_at_earliest_chapter` (both red proven, 2026-07-10) | BUILT |
| M-221 | The walk's self-test: a pre-walk inventory (document fingerprints, the host spec's anchor multiset, the suite's verdict) recorded beside the plan, re-recorded after execute, compared — every difference accounted for by a plan item, the suite at least as green as before; the baseline commit is the named restore point and the plan states the one restore command; never an unplanned difference passing verify silently, never a walk with no named restore point | INV-92 | string | `test_catchup_walk` (red proven against the pre-rewrite MIGRATION.md, 2026-07-10) | BUILT |
| M-248 | The catch-up routing carries its version-delta discriminator (INV-110, row 239): the walk fires only when the host's recorded package version is behind the current package VERSION; a docs restructure carrying no version delta is the host's own queue row through its pipeline, whatever wording the ask used; the trigger wordings are examples under the version test and a wording never decides the routing; the law lives in two homes — the spec's F-catchup skill-behaviour paragraph and MIGRATION's When-to-run section; never the heavy catch-up walk fired on a same-version docs-layout ask, never a trigger wording routing the ask on its own | INV-110 | string | `test_discriminator_in_both_homes` + `test_trigger_wordings_read_as_examples` + `test_spec_anchor_and_index` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-249 | The light vehicle for an owner-scoped docs-layout pass is sanctioned (INV-111, row 240): a host restructuring its own live-spec documents with no package-version delta (INV-110) runs one named shape — the owner's decisions locked in a checkpoint before any file moves (INV-107), the work built on a clean pushed base with a one-command restore, content proven by a word-token multiset check AND a punctuation multiset check, one journal chapter landed; the law lives in two homes — the spec's F-catchup vehicle clause and build-pipeline's docs-only/restructure door; never a host layout pass improvised without the vehicle, never content proven by a word-token check alone | INV-111 | string | `test_vehicle_in_both_homes` + `test_vehicle_owes_the_full_shape` + `test_spec_anchor_and_index` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-327 | Adoption wires the ratchet gates in one pass, seeded at the host's current size (INV-172): `adopt/install-ratchet.sh` vendors the lint/redundancy/freeze scripts with a source-pin manifest (pack version + sha256), writes the host's debt caps at the measured counts (green at once), generates a guard test that reds when the debt grows or the cap is raised, and wires the push gate; never a host told to re-implement the gates from prose, never a seeded gate red on day one | INV-172 | string | `test_ratchet_install_seeds_at_current_size` + `test_ratchet_lock_reds_on_growth` + `test_ratchet_lock_reds_on_cap_raise` | BUILT |
| M-332 | The update check reads the host's vendored pins (INV-177): an old manifest pin draws the re-install proposal naming the stale vendored files (read against the local pack, no remote per-file diff), a current pin stays silent, a host with no manifest gets the plain proposal; the installer pins the scaffold kit's files into the same manifest where present; never an install or re-seed without the human's word | INV-177 | string | `test_old_pin_proposes_reinstall_and_names_stale_files` + `test_current_pin_stays_silent_on_vendored_files` + `test_no_manifest_keeps_the_plain_proposal` + `test_installer_pins_scaffold_files_when_present` + `test_spec_block_and_index_row` | BUILT |
| M-333 | Version is one fact (INV-178): every skill's frontmatter version and every in-text base-version reference equals the root VERSION, stamped by `scripts/stamp-versions.py` at every bump; never a hand-rolled per-skill number drifting while the suite stays green | INV-178 | string | `test_every_skill_frontmatter_version_equals_pack_version` + `test_every_base_reference_equals_pack_version` + `test_spec_states_the_law` | BUILT |
| M-336 | The pack's authored artifacts and their installed copies are one declared class with net-parity (INV-180): four members — vendored kit scripts (INV-172/177), installed hooks and gates (INV-173/175), stamped version copies (INV-178), installed skills (A-7/M-7/E-23/E-25) — each named, the parity stated (each member names the mechanical net that tells its running copy stale), the installed-skills member named the class's acknowledged weakest (held by discipline where its three siblings hold by a machine); the class binds forward [INV-159]; never a new installable artifact with no stated staleness net to write against | INV-180 | string | `test_installed_copy_class_declared` + `test_installed_copy_class_enumerates_every_member` + `test_installed_copy_class_states_parity_and_binds_forward` | BUILT |

### [node: inbox]

| ID | Fact (from spec) | Spec ref | Test level | Owning test | Status |
|---|---|---|---|---|---|
| M-048 | An outside wish = one NEW committed file, named by the law; never an edit to an existing file | E-11 | string | `test_inbox_states_write_rule` | BUILT |
| M-049 | A live-spec session sweeps the inbox FIRST and harvests each file into the home its route owns — a wish into a queue row, feedback by the routing law (T-20); never an item durably recorded but operationally invisible | T-10 | string | milestone re-listing of unharvested files (M-1); route homes asserted by `test_feedback_never_lost_in_both_homes` | TODO |
| M-050 | Only a session assigned to live-spec writes this repo; an outsider never writes spec/queue/journal/skills — the inbox file is the whole exception | INV-10 | string | `test_inbox_states_write_rule` | BUILT |
| M-251 | The inbox door has a remote arm (INV-112, row 247): a seat that reaches a repo only through git deposits one new file in inbox/, committed touching inbox/ only with the source named, then pushed, under a per-repo grant recorded in the host profile like the push grant [INV-82] (the owner links the Claude environment to GitHub once, grants each repo once); a seat with no grant fails honestly — it names the grant it lacks and hands the owner the one action, the honest failure of a window that never opened [INV-67], never silent; the law lives in three prose homes — the spec inbox-law clause, inbox/README.md, and adopt/ADOPT.md's remote-settle step; the field beat — one real remote deposit — waits on a real browser cloud session and is not self-certified on a local run [INV-94] | INV-112 | string | `test_remote_arm_in_all_prose_homes` + `test_honest_failure_in_all_prose_homes` + `test_deposit_stays_inbox_only` + `test_spec_anchor_and_index` (red proven against the pre-delta tree, 2026-07-12) | BUILT |
| M-288 | The inbox door has a stranger arm (INV-146, row 261): a contributor with no push rights and no per-repo grant who can open a GitHub Issue or Discussion (a read-only collaborator on a private repo, anyone on a public one) submits by a templated Issue/Discussion requesting a source; the monitor bridges each open un-surfaced item into one new inbox/ file naming the source Issue + its source and COMMITS it (touching inbox/ only, source in message, the committed-file law [E-11, INV-112]), so from that file on it is an ordinary inbox wish under the git-atomic harvest already proven [T-10, INV-11]; the wish never touches the queue or repo directly; a missing source rides the wordless-item ask [E-11]; the re-surface has a named actor — the monitor reads the item's activity generation from its non-marker comments and re-surfaces one whose generation is newer [INV-138, M-295]; both channels served (Issue over `gh issue`, Discussion over GraphQL — the read path verified by deed on the live repo, the live Discussion write round-trip a field beat that waits on a real run, not self-certified [INV-94]); the law lives in its prose homes — the spec inbox-law clause + index, inbox/README.md's stranger-arm section — and the templated door ships (`.github/ISSUE_TEMPLATE/wish.yml`, `.github/DISCUSSION_TEMPLATE/wish.yml`); NEVER scoped to a public repo alone (a private read-only collaborator is covered), NEVER a sourceless wish lost (asked, not dropped), NEVER the stranger writing the queue | INV-146 | string | `test_stranger_arm_in_prose_homes` + `test_wish_issue_template_requests_source` + `test_stranger_arm_spec_anchor_and_index` (red proven against the pre-delta tree) | BUILT |
| M-289 | The monitor's law (INV-147, row 261): `scripts/stranger-wish-monitor.py` surfaces each open un-surfaced stranger item into a committed inbox file exactly once — its surfacing decision is a pure function of (open items, the generation each was last surfaced at, existing inbox files) so a crash between committing and recording still surfaces once; single-instance per host by a lock (a second run stands down, a stale lock stolen by age); a deposit that does not complete is logged and retried, never counted done [INV-67]; it holds no verdict, wish-vs-not staying the inbox sweep's call [T-20], a no-wish item closed with a recorded note; a run that cannot reach the repo fails honestly — names the reach it lacks, logs, retries, drops no wish silently [INV-67]; the schedule is required where the door is open (liveness rests on it); it surfaces each item once WITHOUT re-triggering on its own marker — the activity generation is read from non-marker comments, never the raw updatedAt GitHub bumps strictly past the monitor's own marker createdAt, so the monitor's own claim and confirm never read back as fresh activity [INV-146, INV-138, M-295]; a channel the repo lacks (Discussions off) degrades to empty rather than felling the run [INV-67]; NEVER a monitor that decides a route or opens a queue row (the verdict stays the sweep's [T-20]), NEVER a wish silently dropped on an unreachable repo, NEVER a half-done deposit counted finished, NEVER a self-re-surface loop where the monitor's own marker reads as fresh activity (the daily-cron amplifier), NEVER the whole run felled because one channel is disabled | INV-147 | string | `test_monitor_surfaces_each_item_once` + `test_monitor_idempotent_on_already_surfaced` + `test_monitor_single_instance_lock` + `test_monitor_honest_failure_on_unreachable` + `test_monitor_does_not_retrigger_on_own_marker` + `test_fetch_discussions_degrades_when_disabled` (red proven — script absent at HEAD) | BUILT |
| M-337 | The stranger arm answers a captured wish on its source too, extending M-289's no-wish close (INV-147, row 261): a surfaced item the sweep judges a wish is answered on its source as well as harvested — at harvest the sweep posts the capture echo (heard · door · name · row [INV-27]) as a comment on the source Issue, and the Issue is closed once its row reaches a terminal exit [INV-59]; both writes ride the monitor's existing Issue/Discussion channel, asking no new grant [INV-146, INV-148]; NEVER a captured stranger wish left open and silent on its source, NEVER the stranger answered only on a no-wish rejection, NEVER a new grant invented for the echo | INV-147 | string | `test_stranger_yes_wish_echoes_and_closes_source` | BUILT |
| M-290 | The package repo's concrete monitor schedule (INV-148): the scheduled Action `.github/workflows/stranger-monitor.yml` ships and wakes the monitor — it triggers on a daily `schedule` cron AND on `workflow_dispatch` (a hand-run for verify), is a single instance by a `concurrency` group whose in-progress run is not cancelled (a second run waits, the CI form of the per-host lock), runs `scripts/stranger-wish-monitor.py`, and grants the token `contents: write` + `issues: write` + `discussions: write` (contents to push the inbox commit, issues/discussions to record the marker comment [INV-146]); the workflow pushes any inbox commit the monitor made; NEVER a schedule that can cancel a mid-flight sibling run (`cancel-in-progress: true` would let two overlap and race the push), NEVER a monitor arm without the discussions write it needs to record a Discussion's marker, NEVER a workflow that runs the monitor without pushing what it committed (a bridged wish stuck unpushed is operationally invisible) | INV-148 | string | `test_monitor_schedule_workflow_ships` + `test_monitor_schedule_is_single_instance` + `test_monitor_schedule_grants_write_scopes` + `test_monitor_schedule_runs_and_pushes` (red proven — workflow absent at HEAD) | BUILT |
| M-291 | The cross-host coordinator (INV-149): where two hosts' monitors watch one repo they converge on a single surfacing over the source Issue/Discussion they share [INV-146]. A host posts a claim comment carrying its host identity [INV-117] under a hidden claim marker and deposits ONLY when its own claim wins the shared comment log — the winner a pure function of the claims: the earliest by creation time, the lower host identity breaking a tie, every host computing the same winner; a losing host stands down (deposits nothing) and retries next run. The claim is the per-host lock [INV-147] lifted onto the repo and stolen by age at the SAME stale bound: a claim older than that bound with no surfacing recorded behind it is abandoned, so a surviving host wins past a dead winner and surfaces the wish itself [INV-1]. The claim marker is a DISTINCT string from the surfaced-generation marker, so a claim comment never reads back as a recorded surfacing and never perturbs the re-surface generation [INV-146]. The claim rides the monitor's existing Issue/Discussion write, asking no new grant [INV-148]; NEVER two hosts each depositing the same wish (both compute one winner from the shared log), NEVER a dead winner swallowing the wish (its stale claim is stolen and the survivor surfaces), NEVER the door blinded (no claim suppresses a surfacing beyond its own staleness), NEVER a claim comment mistaken for the surfaced-generation record (distinct markers), NEVER a losing host's trailing claim looping a re-surface every run — a claim is a monitor marker, so the activity generation (read from the item's non-marker comments [INV-146, M-295]) never counts it and it reads as no new activity, with a newest-of-any-marker ceiling standing beside as a belt-and-suspenders baseline, while a genuine stranger comment is a non-marker comment that does re-surface [INV-138] | INV-149 | string | `test_claim_winner_earliest_then_host_tiebreak` + `test_claim_winner_identical_across_hosts` + `test_stale_claim_stolen_by_age` + `test_claim_loser_stands_down_and_deposits_nothing` + `test_claim_marker_never_reads_as_surfaced` + `test_parse_claims_reads_host_and_time` + `test_marker_ceiling_counts_claims_and_confirms` + `test_trailing_claim_does_not_reloop_surfacing` (red proven — claim arbitration absent at HEAD) | BUILT |
| M-295 | The monitor's activity signal excludes its own writes (INV-146/INV-147/INV-149, a live round-trip on the package repo's Discussion #1, 2026-07-14): GitHub advances an item's updatedAt to a moment STRICTLY LATER than the createdAt of the comment that caused the bump, so the monitor's own claim + confirm marker comments push updatedAt a hair past their own createdAt; feeding that raw updatedAt as the activity generation, the next run read it as fresh outside activity, cleared the marker ceiling, and deposited a SECOND inbox file — a self-triggered re-surface loop firing every run on any open item. The fix reads the activity generation from the item's non-marker comments (`_activity_gen_from_comments`), so the monitor's own markers never advance it however far they push updatedAt, and a run settles at exactly-once, while a genuine third-party comment (a non-marker comment) still advances it and re-surfaces once. BOTH channels shared the defect (Issue and Discussion both fed raw updatedAt) and both are fixed by the same helper; the spec's claim that "the item's generation after the marker equals the recorded value" was the wrong premise (the createdAt-vs-updatedAt skew) and was corrected. A bare edit/reopen with no accompanying comment is surfaced on its next comment — data-safe (no wish lost, the reopened item stays visible on GitHub), narrower than the pre-fix looping updatedAt read; NEVER the monitor's own claim/confirm read as fresh activity, NEVER a duplicate inbox file per run on an open item, NEVER one channel fixed while the other loops | INV-146 | string | `test_own_marker_updatedat_bump_does_not_reloop_discussion` + `test_own_marker_updatedat_bump_does_not_reloop_issue` + `test_activity_gen_excludes_the_monitors_own_markers` (red proven against the pre-fix tree — both channels deposited a second file, 2026-07-14) | BUILT |
| M-329 | The inbox's local co-located arm (INV-174): a same-filesystem depositor writes its one new inbox file and STOPS — no staging, no commit, no push; the assigned session's sweep commits the harvest; never a co-located neighbour's git act on the shared index read as the allowed deposit shape | INV-174 | string | `test_spec_states_the_local_arm` + `test_local_deposit_never_stages` + `test_inbox_readme_carries_the_split` | BUILT |
| M-367 | The message's lifecycle (INV-192): a message carries a stable identifier its reply can name — a projection of the sender's session identity [INV-117], the same scheme the inbox's source mark already uses rather than a second one, riding inside the file — and a reply names the message by that identifier after the file has left the inbox and become a row in the receiver's queue [E-11]; a reply travels the SENDER's own inbox as one new file, by the arms that carry every inbox item [INV-10, INV-174, INV-112], inheriting its passage from the message it answers and owing no blocked work of its own, since the message it discharges already named the blocked work that earned the exchange [INV-189]; that road keeps the channel count at two, the inbox running in the other direction [INV-183]; every message states its need-by and reaches a terminal state — delivered, declined, or escalated past its stated need-by — so no message dies waiting on a window that never opens [INV-1, INV-67], an escalation surfacing in the sender's own status report as blocked work aged past its need-by for the human to read [INV-27], and an agent wakes a dormant window on no occasion; never a message left without a terminal state, never a reply asked for blocked work of its own, never a dormant window woken by an agent | INV-192 | string | `test_message_carries_a_stable_identifier` + `test_reply_travels_the_senders_own_inbox` + `test_reply_owes_no_blocked_work_of_its_own` + `test_every_message_reaches_a_terminal_state` + `test_no_agent_wakes_a_dormant_window` + `test_inv192_index_and_ownership` (red proven against the pre-delta tree, 2026-07-17) | BUILT |

### [node: host-contract]

| ID | Fact (from spec) | Spec ref | Test level | Owning test | Status |
|---|---|---|---|---|---|
| M-051 | The host profile narrows the human's contract for one project, every line a recorded override; never a silent divergence | E-8 | string | `test_host_profile_recorded_override` | BUILT |
| M-065 | The personal layer has ONE home (the profile); the global instruction file is a thin loader carrying only the pointer + bootstrap lines (their one home; the profile never restates them); a session line is never written by the agent; the migration fork never writes a foreign repo | E-16 | string | milestone audit (M-1) — loader live since row 52; asserted at audits | TODO |
| M-365 | The card and the scan are the lookup (E-32): a tree carrying a card is an agent, and writing the card is the declaration; the card lives in each agent's own tree at `.live-spec/agent.md` and names five things — the agent's name, its standing mission, the zones it owns, the contracts it publishes each with the path its artifact lives at, and its inbox address; discovery is a scan reading two globs under each root — the parent of the reader's own tree, plus any root the personal profile names [E-16]; an agent scans for cards and reads the owning card before it acts on anything that might not be its own, the read running first because every law keys on its answer: which zone owns the thing, whether a contract already publishes it, and where a message would go [INV-184]; who does what is a lookup, so tribal memory answers nothing here and an agent that finds no card asks one plain question [INV-4]; a scan finding only the reader's own card answers that the reader is the only declared agent; never a lookup answered from tribal memory, never a mission invented for a tree whose card is absent | E-32 | string | `test_card_and_scan_law` + `test_card_names_its_five_fields` + `test_the_read_runs_before_the_acting` + `test_e32_index_and_ownership` (red proven against the pre-delta tree, 2026-07-17) | BUILT |
| M-366 | The declaration law (INV-184): a tree's presence grants nothing and writing a card grants everything, the card being the agent's own deliberate act in the tree it owns; no file outside any tree describes any agent, so no shared mutable file exists for two windows to race [INV-10, INV-11] — write-ownership is the reason, a single shared file listing every agent's description being precisely the file two windows would race to edit, and this design has no such file to protect; discovery is a live scan bounded to two globs per root with no branch descended, so a Library tree or a cloud-synced folder costs one listing, and no index caches it, since a cache would be that shared mutable file and would answer from yesterday while a scan reads the machine itself; write-ownership grants the card, so it needs no permission act and default-deny meets no exception [INV-185], while product data placed in a card stays a contract field on INV-185's road; a tree carrying no `.live-spec/agent.md` is flagged where its siblings are flagged — at the founding questions and at adoption's orient, beside the project kind [INV-36] and the declared layers [INV-135] — by a mechanical gate that reads the tree, writing the card at its catch-up walk and binding forward [INV-150, INV-159]; the card arm is exercised against this pack's own real `.live-spec/agent.md`, read from disk with its five fields checked; never an agent's description parked in a shared mutable file, never a cached index standing in for a live scan, never a card-less tree passing the founding record as complete | INV-184 | string | `test_pack_card_exists_and_names_its_five_fields` + `test_no_file_outside_any_tree_describes_any_agent` + `test_write_ownership_grants_the_card` + `test_cardless_tree_is_flagged_beside_its_siblings` + `test_inv184_index_and_ownership` (the spec arms red proven against the pre-delta tree, 2026-07-17; the card arm reads the pack's own shipped card on disk) | BUILT |

### [node: package-docs]

| ID | Fact (from spec) | Spec ref | Test level | Owning test | Status |
|---|---|---|---|---|---|
| M-052 | Shipped vs target is marked honestly; every [target] machine is owned by a STILL-OPEN queue row (declared map, self-closing both directions); a [target] node names its missing pin, a fully-pinned node carries no tag; never a claimed-shipped target, never a target orphaned by its row landing or vanishing | S-0 | string | `test_targets_owned_by_open_rows`, `test_target_nodes_pin_honesty` (row 64 mechanized what prover F1 caught by hand) | BUILT |
| M-053 | The queue and the spec carry dated versions; never an undated truth | M-3 | string | `test_roadmap_header_dated` | BUILT |
| M-096 | Dev-machine skill sync goes through the named script: installed copies overwritten from the repo source, every version change REPORTED old → new (the A-7 trigger line); idempotent — a fresh dest fills, an unchanged one says so; never a silent hand-copy as the sync path | E-23 | string | `test_sync_skills_script` (a REAL run against a temp dest, twice) | BUILT |
| M-054 | live-spec eats its own cooking — this repo works by its own spec and queue; the pack's own push gates run mechanically on the installed hooks; never a claim of mechanical enforcement beyond what is actually wired | M-4 | string | `test_hooks_and_scripts_exist_and_executable` (+ `guardrails/install.sh` run for real, journaled) | BUILT |
| M-143 | A stranger's first minute is clean: `python3 -m pytest` from the repo root collects and runs the real suite (pytest.ini pins testpaths to tests/) — the scaffold template is never collected, never executed, and stays a template; never a shipped file whose NAME makes a standard tool trip in the repo's first minute | M-4 | string | `test_pytest_collects_clean_from_root` (real `pytest --collect-only` run from ROOT; red-proven on the pre-fix tree) | BUILT |
| M-055 | Attic layout choice stays open with a named revisit trigger; never silently resolved | D-1 | string | `test_spec_decide_markers_match_open` | BUILT |
| M-056 | Tier-routing override is DECIDED (row 56): the router proposes, the senior may override per wish, the override is logged; the D-2 line reads decided in Open decisions, never both Decided and ⟨DECIDE⟩-open; never silently resolved, never mechanically fixed | D-2 | string | `test_spec_decide_markers_match_open` | BUILT |
| M-057 | Snapshot retention choice stays open (closes via row 55); never silently resolved | D-3 | string | `test_spec_decide_markers_match_open` | BUILT |
| M-058 | D-4 records its decided state (package-is-source, row 51 executes); never re-litigated silently | D-4 | string | `test_spec_decide_markers_match_open` | BUILT |
| M-059 | D-5 records its decided state (all-into-profile, rows 52–54 execute); never re-litigated silently | D-5 | string | `test_spec_decide_markers_match_open` | BUILT |
| M-202 | Pair-queue shape stays open: a stitched reading view or strictly two separate queues; recommended two plain queues until real friction earns a view; never silently resolved | D-6 | string | `test_spec_decide_markers_match_open` | BUILT |
| M-203 | Pair-spec citation depth stays open: the instance cites engine contract-entry handles and nothing deeper, recommended; never silently resolved | D-7 | string | `test_spec_decide_markers_match_open` | BUILT |
| M-105 | The pack keeps its own problem ledger (dogfood — E-24's shape applied to itself): `.live-spec/PROBLEMS.md` exists, every entry carries a signature, ≥1 date, and a legal status; never an empty ledger claiming entries, never an entry without date or status | M-4 | string | `test_pack_own_ledger` | BUILT |
| M-185 | live-spec models its own law (dogfood): ARCHITECTURE.md carries the Runtime view (every feature in the coverage table walks there), the Placement view (the five places, incl. the installed-skills dir and the agent session), and the Quality budgets (numbers + instrumentation homes, suite wall-time and evals among them); never a mandate the pack's own doc skips | M-4, INV-74, INV-75, INV-41 | string | `test_own_architecture_carries_views_and_budgets` | BUILT |
| M-199 | A rendered documentation page resolves its cross-links: a relative `.md` link to a sibling doc opens the rendered `.html` neighbour (path kept, extension swapped), a link's `#anchor` is preserved and slugified, every rendered heading carries a GitHub-style id (lowercase, spaces→hyphens, punctuation stripped, repeats deduped -1/-2) so those anchors land, and an external http(s)/mailto link plus a non-`.md` path stay untouched; a bare in-page `#anchor` is slugified onto its heading id; never a rendered page shipping a raw `.md` href for a relative target, never an anchor that resolves to nothing | M-4 | string | `test_relative_md_link_becomes_html_neighbour`, `test_link_with_anchor_keeps_the_anchor`, `test_headings_carry_github_style_ids`, `test_external_links_stay_untouched`, `test_bare_in_page_anchor_stays_and_lands` (render-link fixtures — five string-level assertions against real `render-doc.py` output — a/b/c/e red-proven against HEAD before the fix: `href="pipeline.html"` absent, anchors unresolved, no heading ids; d — external links — already green, so proven not to regress) | BUILT |
| M-204 | The Formal index of PRODUCT_SPEC.md holds its own shape, mechanized (the 2026-07-10 manual audit made standing): every expanded index code is anchored at least once as a trailing bracket in the body prose AND every body bracket anchor is listed in the index (anchors may share a bracket `[T-14, INV-19]`; `[feature: F-x]` tags belong to the separate coverage layer and are excluded; `[default]`/`[target]`/`[HH:MM]` carry no PREFIX-NUMBER so they never count, while a real code sharing their bracket still does); per-prefix numbering carries no duplicate and no gap beyond the pinned empty set (a new hole fails); every code cited inside an index summary resolves to an existing index code; summary-drift stays the prover's call, named as out of scope; never an orphan anchor, a dangling cross-reference, or a silent numbering hole | M-4 | string | `test_index_and_body_anchor_symmetry`, `test_index_numbering_contiguous`, `test_index_cross_references_resolve`, `test_the_checks_catch_seeded_defects` (the fourth seeds three defects — a removed index row, a duplicated code, a dangling cross-reference — into a copy of the real spec and asserts each check fires, so the red-proof stands forever) | BUILT |

### [node: guardrails [target]]

| ID | Fact (from spec) | Spec ref | Test level | Owning test | Status |
|---|---|---|---|---|---|
| M-168 | Pack-list parity: every list of the pack's skills (SPEC working-skills sentence · the skills' closing lists · README table) names the same complete set, checked by a pure function over the real files; never a list missing a skill (the checker proven to catch the historic four-skill communicator footer) | INV-66 | string | `test_real_repo_lists_complete` + `test_stripped_copy_goes_red` (the never side runs the checker on the pre-fix footer) | BUILT |
| M-082 | Architecture pins are names first, the :line a cache (E-14); the drift gate (g) turns file-missing/beyond-EOF RED and reports label drift (strict mode blocks); never a rotten pin trusted silently | E-6 | string | `test_real_repo_passes` (gate g), `test_missing_file_fails`, `test_label_drift_strict_fails` | BUILT |
| M-083 | The surface registry's PREFERRED form is executable (a declared map inside a completeness-gate test, red both directions); the .md is the doc-only fallback; adoption never asks an executable registry to step back into a document | E-10 | string | clause presence: `test_spec_states_registry_and_pins`; machine rides row 55 | BUILT |
| M-081 | Every shipped skill loads: frontmatter parses, name = folder, description + version present, a "when NOT to use" section scopes it; a broken skill turns the push gate RED; never an unloadable or unscoped skill shipped | E-6 | string | `test_real_repo_passes` (gate f), `test_broken_skill_fails`, `test_missing_skills_dir_fails` | BUILT |
| M-060 | The guardrails run on the pre-push hook: completeness, tests-present, behaviour-traces-to-spec, declared-scope diff; never a red push | E-6 | string | first slice BUILT (pack gates: prover record · green suite · anchor ownership · matrix coverage — `guardrails/pre-push` + `test_guardrails.py`); the four host-facing checks await the surface registry + snapshot (rows 55+) | TODO |
| M-226 | The four host checks' shipping contract (INV-97): generic runnable code under scaffold/guardrails/, ONE host config (paths, globs, registry, render command), gate-contract output, honest failure (missing config = red attach-me; dead path = red; waivers DECLARED in config), adopt-walk attachment with the pack repo as first host, measured acceptance (~15-min config-only attach, red-first on a planted defect per check); never a silent pass on an undeclared gap, never the contract's wording forked from the code's behaviour, never the pack's own attach leaving surface_discovery_pattern null (the external push-probe's hole, 2026-07-10), never the attach walk hiding the config step (it OPENS with it, the pre-config red named as by-design, the no-config fix line pointing at it — row 251) | INV-97 | string | clause presence: `test_contract_in_the_spec` + `test_waivers_are_declared` (red proven against the pre-delta tree, 2026-07-10); function rows M-228..M-231; own-attach lock: `test_own_attach_arms_the_discovery_pattern` (red proven on the nulled config, 2026-07-11); walk opening: `test_walk_opens_with_the_config_step` (red proven against the pre-edit walk, 2026-07-11) | BUILT |
| M-228 | check_completeness.py: every registered surface's needle present AND non-empty in the rendered content; with a discovery pattern, every discovered id registered (self-closing); never a registered-but-absent, registered-but-empty, or rendered-but-unregistered surface green | INV-97 | string | `test_completeness_green` + `test_registered_but_absent` + `test_registered_but_empty` + `test_rendered_but_unregistered` (red proven before the code existed, 2026-07-10, worker checkpoint) | BUILT |
| M-229 | check_tests_present.py: a diff touching user-facing globs with no test change is red, naming the offenders; never a user-facing change pushed testless | INV-97 | string | `test_change_without_test_red` + `test_change_with_test_green` + `test_non_user_facing_change_green` (red-first, same run) | BUILT |
| M-230 | check_traces_to_spec.py: every registry surface cites >=1 anchor and every cited anchor lives in the spec; never an unanchored surface or a dead anchor green | INV-97 | string | `test_traces_green` + `test_unanchored_surface` + `test_dead_anchor` (red-first, same run) | BUILT |
| M-231 | check_conflicts.py: duplicate anchors, invariant-without-matrix-row, resolved-but-live markers, surface-named-twice all red; honest ladder shared by all four (no-config = attach-me red, dead path = red, declared waiver visibly exits 0); never a silent pass | INV-97 | string | `test_conflicts_green` + `test_duplicate_anchor` + `test_invariant_without_row` + `test_resolved_but_live` + `test_surface_named_twice` + `test_missing_config_is_red_with_attach_me_line` + `test_dead_path_is_red` + `test_waived_check_is_visible_and_green` (red-first, same run) | BUILT |
| M-061 | The surface registry is self-closing: a rendered-but-unregistered surface is RED; never a trusted hand-list | E-10 | string | rides row 55 (registry executable form travels with the snapshot/guardrails family; the stale row-3 cite caught by row 64's mechanization — row 3 landed its pack slice and archived) | TODO |
| M-062 | A host may mirror the same checks in CI — one source of truth, CI never redefines them | M-5 | string | lands at row 14 | TODO |
| M-070 | The prototype fence is mechanical: a prod file referencing anything inside a prototype home turns the push gate RED; never a green push with such a reference | INV-17 | string | `test_prod_reference_fails`, `test_real_repo_passes`, `test_narrative_mention_passes` | BUILT |
| M-142 | The push gate scales to the diff's reach: a declared reach map (guardrails/check-push-reach.sh) decides gate b's scope mechanically from the diff's file list — prose-only diff (README/OVERVIEW/MIGRATION/LICENSE/docs research-reports-audit-decisions) stands the suite down by name, an infra-class diff rides the scoped middle road (M-344, row 362), ANY tested document (SPEC, matrix, architecture, queue, skills — or anything new/unmapped) runs it whole, empty diff and missing base fall to full by construction; the cheap gates never scope; never a fast path for a tested document, never the script deciding by judgment | INV-45 | string | `TestGateReachMap` (the script RUN by deed on fixture file-lists — red proven bare) + `test_push_gate_reach_law` | BUILT |
| M-344 | The reach map's scoped middle road (INV-45, row 362): a diff confined to the infra classes (guardrails/, scaffold/guardrails/, templates/, scripts/, hooks/, adopt/, guardrails.config.json, tests/test_*.py) exits scoped and lists the test files that name its changed files (one referrer level deep) plus the traceability net, the picked set and its reason logged; a changed infra file that no test names falls to full, a mixed diff falls to full; never a scoped run dropping the traceability net, never a file outside the declared classes riding the scoped road | INV-45 | string | `test_scoped_reach_guardrails_diff_exits_scoped` + `test_scoped_reach_unnamed_file_falls_full` + `test_scoped_reach_mixed_diff_falls_full` (red proven against HEAD — the script has no scoped verdict) | BUILT |
| M-345 | The gate-machinery meta-test rides the same reach (INV-45, row 362): the suite-in-suite scratch runs (TestGateB_Tests) fire when the diff against the push base touches a gate-machinery file (guardrails/, scaffold/guardrails/, guardrails.config.json, .github/workflows/, tests/test_guardrails.py) and skip under their own named reason otherwise; an empty or unreadable diff runs them, conservative by construction; never the meta-test silently skipped on a gate-machinery diff, never a skip without its named reason | INV-45 | string | `test_meta_reach_fires_on_gate_machinery_diff` + `test_meta_reach_skips_off_class_with_named_reason` + `test_meta_reach_conservative_on_empty_diff` (red proven against HEAD — no reach classifier in the meta-test) | BUILT |
| M-346 | The suite wall-time budget's mechanical net (INV-41, INV-164, row 361): after a green full run, `guardrails/check-suite-budget.sh` reads the run log's own tail duration line and the architecture's stated budget row and reds past the budget naming both the measured and the budgeted figure; a log with no readable duration line, or an architecture with no readable budget row, reds conservative; scoped and scratch runs stand the check down by name; never a budget claim no machine reads against the real number, never a budget red hiding either figure | INV-45 | string | `test_slow_log_reds_naming_both_numbers` + `test_fast_log_passes` + `test_unreadable_log_reds` (red proven on a synthetic slow log against HEAD — the check absent) | BUILT |
| M-348 | The scoped road's by-name discovery is guarded by a suite-hygiene net (INV-45, defends INV-40's reach-identity claim, row 366): a test that reads an infra directory (guardrails/, scaffold/guardrails/, templates/, scripts/, hooks/, adopt/) by os.walk/glob/Path.glob/rglob/listdir/iterdir/scandir rather than naming its files' basenames is invisible to check-push-reach.sh's basename grep, so every such enumerating test must appear in the pinned ALWAYS_SCOPED block the script and the net read from one home (test_traceability.py pinned inside the block as the integrity rider); a test enumerating only non-infra dirs (skills/, docs/) stays free; ALWAYS_SCOPED carries the integrity rider and stands open for the first enumerating-infra test. Never an enumerating-infra test outside the scoped set, never a second copy of the infra-dir list. | INV-45 | string | `test_enumerating_infra_tests_are_pinned` + `test_synthetic_enumerating_infra_test_reds_unpinned` (red proven against HEAD — the script carried no ALWAYS_SCOPED block, docs/prover/red-proof-2026-07-16-row366.txt) | BUILT |
| M-145 | Gate hygiene: every BLOCKING gate on red emits one typed failure line {severity, code, message, fix} beside its human lines; every check declares blocking or advisory (advisory never flips the exit); artifact-rebuilding scripts validate all outputs before writing any; home guardrails README, applied by deed to ≥1 shipped gate (the prototype fence); the reach decider exempt by name (a deliberate verdict); never a blocking red without its typed line on the converted gates | INV-47 | string | `TestGateHygieneContract` (`test_readme_states_contract` + `test_prototype_fence_emits_typed_failure` — the converted gate's real FAIL output parsed as JSON) | BUILT |
| M-154 | The CI mirror is live and honest: `.github/workflows/gates.yml` runs the SAME gate scripts as the local pre-push (a second net; full set — the reach map stays a local optimization; fetch-depth 0 + author-day TZ so gate (a) reads real history and the real day), INCLUDING the same test RUNNER — both sides run `python3 -m pytest`, so the local net can never under-collect what CI runs (the `unittest discover` gate false-greened on plain-function pytest-style tests until this was pinned); the guardrails README carries the host guidance (copy, swap the test command on BOTH sides in lockstep, never redefine a check); never CI redefining a check, never the local gate on a weaker runner than CI, never the second net scoped by reach, never a false red on a machine-local pin in CI (noted + skipped there; strict locally) | M-5 | string | `TestCIMirror` (`test_workflow_ships_and_mirrors_the_gates` + `test_readme_carries_the_mirror_guidance` + `test_local_gate_uses_the_same_runner_as_ci` + `test_machine_local_pins_skip_in_ci_only`, red proven bare + by the first live run) + the real run watched on the landing push | BUILT |
| M-106 | Time is read off the clock: no repo file NAME, journal entry heading, or ledger date sits later than the current clock — a future-dated stamp turns the suite (and so the push walk) RED; prose quoting a past incident's wrong date stays legal; never an invented tomorrow shipped | INV-24 | string | `test_no_future_dated_stamps` (red proven by deed on a synthetic future-named file) | BUILT |
| M-110 | INV-24's second arm at pre-commit: an ADDED line carrying the ADJACENT stamp shape (today's date [~]time) with a time later than the commit moment goes red (commit clock the reference; CHECK_TODAY/CHECK_NOW only for tests); a past time today, any time on another day's date, and a line mixing today's date with quoted other-moment times (F9, the fence's own first live run) all stay green; never a same-day future stamp reaching a commit, never the fence blocking a quoted past | INV-24 | string | `test_future_time_today_goes_red` (with `test_past_time_today_stays_green` + `test_other_day_time_stays_green` — the script run by deed on a staged synthetic repo) | BUILT |
| M-123 | The chat face of the clock law — no fence can reach chat, so the arm is stated as law where the exchange shapes live (the sentence is carried by communicator rule 7): a human-facing timestamp — the [HH:MM] a reply leads with, any moment spoken to the human — is read off the clock at write time, never continued or extrapolated from an earlier stamp; quoting a past moment's recorded time stays legal; never an extrapolated stamp presented as the current time | INV-24 | string | `test_chat_timestamp_at_write_time` | BUILT |
| M-132 | The chat clock's mechanical hand: `hooks/clock-hook.sh` exists, is executable, and when run emits the CURRENT machine time (HH:MM within a minute of now, plus today's date) together with the read-off-this-clock instruction — the output names "[HH:MM]" and says "never a continued or extrapolated stamp"; installed per machine as a prompt hook (a copy under ~/.claude/hooks + a UserPromptSubmit entry in the host's settings); never an invented or cached time in its output, never a silent install on a machine the human didn't put it on | INV-24 | string | `test_output_carries_current_machine_time` (with `test_script_exists_and_executable` + `test_output_carries_the_law` — the script run by deed; red-proven on the absent script first) | BUILT |
| M-180 | The feature-coverage trace ships: live-spec's primary unit is its person-facing scenario, so each such heading carries an inline `[feature: F-x]` tag and ARCHITECTURE.md's Feature coverage table maps every unit to implementer node(s) + a real test; a per-project-type unit layer ABOVE the anchor matrix, its format's home the spec-author skill; never a feature layer that forks a second machine (it reuses anchor-ownership), never a promised scenario without its tag | E-29 | string | `test_every_scenario_carries_its_feature_tag` + `test_spec_and_spec_author_carry_the_format` (red proven against HEAD — no tags, no E-29, no coverage table there) | BUILT |
| M-181 | The feature-coverage check is two-way and has teeth: every tagged unit resolves to a real implementer node and a real test, every promised scenario carries its tag, and the pure checker is proven to catch a dropped tag, an orphan coverage row, a fake node and a fake test name; never a green push with a broken feature trace, never a one-directional check that misses an orphan row | INV-73 | string | `test_feature_coverage_two_way` + `test_stripped_coverage_goes_red` (the never side runs the checker on a deliberately broken table) | BUILT |
| M-260 | The shipped-language gate refuses Cyrillic and owner-name in the shipped set, file:line (INV-120, row 275): guardrails/check-shipped-language.sh (engine scripts/check-shipped-language.py) scans the shipped set — tracked text files minus the local-only diaries and fixture homes — and exits 1 reporting each offence as file:line, sparing deliberate user-language regions (a user-tagged fenced block or an inline marker) and allowlisted program-data and authorship-byline files; the machine covers the two mechanical offences and leaves a coined metaphor to the register lint; proven on fixtures and wired into the pack's own pre-push (gate i) and CI mirror 2026-07-12 (row 279), the gate's own machinery and the prototype/ sketch home excluded from the scan; never a shipped requirement carrying an untranslated owner-language string or a personal name slipping out uncaught | INV-120 | string | `test_offence_fixture_fails_with_file_line` + `test_clean_fixture_passes` + `test_deliberate_user_language_region_is_spared` + `test_gate_wired_into_pre_push_and_ci` + `test_gate_green_on_the_swept_tree` | BUILT |
| M-273 | The reverse direction of the feature-coverage check has teeth through a heading convention (INV-132, row 283 — prover finding F4, 2026-07-12): INV-73's "every scenario carries its tag" was not mechanically enforceable, since the checker could not tell an untagged NEW scenario from a legitimately-untagged machinery/rules/reference section; now every H3 heading in PRODUCT_SPEC.md carries either its `[feature: F-x]` tag or the explicit `[not a scenario]` marker, so an H3 carrying neither is unambiguously red — a forgotten scenario tag can no longer ship uncovered; the convention lives on H3 headings, sub-parts nesting under an already-tagged-or-marked heading; homes the feature-coverage clause + spec-author; the check reads the shipped spec, the red-proof seeds an untagged H3 into a copy; the recognizer accepts a numeric feature id (`F-12`) and a multi-feature heading (`[feature: F-a, F-b]`) as validly tagged while a truly untagged/unmarked heading stays red (INV-132 mechanism hardened, row 296); never an untagged, unmarked H3 shipping green, never a new machinery section passing without its marker, never a valid numeric-id or multi-feature tag reddened | INV-132 | string | `test_every_h3_is_tagged_or_marked` + `test_untagged_h3_goes_red` + `test_numeric_and_multi_feature_headings_are_accepted` + `test_spec_states_heading_convention` + `test_formal_index_row` + `test_spec_author_carries_heading_convention` (red proven against the pre-delta tree — 10 unmarked H3s, 2026-07-12; the numeric/multi cases red-proven against the lowercase-only recognizer) | BUILT |
| M-328 | Pack hooks live as source in `hooks/` and install via `scripts/install-pack-hooks.sh` (INV-173): the canonical scan hook carries the universal English contrast patterns only, reads the personal overlay `~/.claude/hooks/scissors-personal.json` when present, and skips quoted demonstrations (guillemets, quotes, backticks, code fences); never a live hook whose only copy is the installed one, never a pattern-demo flagged as a violation | INV-173 | string | `test_canonical_hook_exists_with_universal_patterns_only` + `test_hook_skips_quoted_demos` + `test_hook_reads_personal_overlay` + `test_installer_dry_run_touches_nothing` | BUILT |
| M-330 | The installed gate is the source gate (INV-175): `guardrails/check-config-health.sh` reds a missing or byte-drifted installed hook naming the fix, runs inside the suite (self-healing under a stale push gate) and as push-gate m, skips a CI checkout by name; the commit fence stops a file both staged and carrying unstaged edits; never the installed copy silently under-running the source's gate list | INV-175 | string | `test_this_repo_installed_hooks_match_source` + `test_missing_installed_hook_reds` + `test_drifted_installed_hook_reds` + `test_matching_hooks_pass` + `test_ci_checkout_skips_by_name` + `test_staged_file_with_unstaged_edits_blocks` + `test_clean_staged_commit_passes` | BUILT |
| M-331 | A law extracted into a gate scans the whole tracked tree, retroactive by construction (INV-176): pre-gate debt is found the day the gate lands, an oversized backlog is absorbed by cap seeding (INV-172), and adoption + the catch-up walk run the current gate set backward over the host's tree; never a delta-only scan letting old debt hide until the next touch | INV-176 | string | `test_spec_states_the_retroactive_gate_law` + `test_gate_contract_carries_the_clause` + `test_adoption_runs_gates_backward` | BUILT |

### [node: snapshot [target]]

| ID | Fact (from spec) | Spec ref | Test level | Owning test | Status |
|---|---|---|---|---|---|
| M-063 | The snapshot is the last accepted baseline; it advances only at *landed* and only for DECLARED surfaces; never an undeclared advance | E-7 | string | machine lands at row 55 | TODO |
| M-064 | Adoption saves a baseline snapshot of current artifacts as the first diff base; never a first landing diffed against nothing | A-6 | string | lands at row 55 (with the machinery) | TODO |
| M-169 | The snapshot DESIGN is decided and stated: home `.live-spec/snapshot/` with a per-surface manifest (what · landing · hash), advance at *landed* for declared surfaces only, last-only retention with git history as the archive, heavy surfaces hash-only; never a second archive mechanism, never a baseline advanced for an undeclared surface | E-7 | string | `test_snapshot_design` (red proven against HEAD — design facts absent there) | BUILT |

### [node: design-sync]

| ID | Fact (from spec) | Spec ref | Test level | Owning test | Status |
|---|---|---|---|---|---|
| M-080 | Design-sync: optional machine — declared-scope footprint, supplements the render (which stays the gate authority), human-gated publish; never a sync without the human's word, never on without a recorded profile line | E-18 | string | machine's first real run = row 93's remainder; clause presence: `test_spec_states_founding_and_designsync` | TODO |
| M-088 | Design-sync WIRING is live: the `design-sync` switch off-by-default in base defaults, the channel line in communicator (cards after the gate, render stays authority) and in the pipeline's commit-and-show step; never a wired line contradicting the machine's [target] honesty | E-18 | string | `test_designsync_wiring` | BUILT |

---

### [node: test-author]

| ID | Fact (from spec) | Spec ref | Test level | Owning test | Status |
|---|---|---|---|---|---|
| M-167 | The test method's one home ships: the test-author skill (level ladder · real-artifact assertions · red-first proof · pinned skip-set · traceability standing) wired from build-pipeline steps 5–6 and listed in every pack list (SPEC · base footer · communicator footer · README); never a second home for the method, never an unlisted skill | E-27 | string | `test_test_author_skill` (red proven against HEAD — wiring absent there) | BUILT |
| M-187 | The real-device boundary is named honestly: test-author's level ladder states that touch physics, scroll snapping, and background throttling live past a desktop headless browser, and mandates the real-device walk row — a matrix row the suite can never green, owed to the human's hands before ship, stating what the hands check and on which device class; never a green desktop run claiming such a fact | INV-77 | string | `test_real_device_boundary` (red proven against HEAD — no INV-77, no walk row there) | BUILT |
| M-188 | A geometry fact asserts relative, wide, and long: the element's center within ε of the viewport's center, at ≥ 2 viewport sizes, after N consecutive steps so cumulative drift shows; never an absolute one-viewport one-step assertion standing in for a centering fact | INV-78 | string | `test_relative_geometry_assertions` (red proven against HEAD — no INV-78 there) | BUILT |
| M-189 | An engine extracted from an instance tests on its own generic fixtures: engine-shaped ids/content model in the engine's suite (donor data legal only as an extra), and every donor constant found at extraction becomes a named content-contract entry with a works-without-it test — both halves present (test-author fixtures + spec-author contract); never an engine suite running only on donor-shaped data | INV-79 | string | `test_engine_generic_fixtures` (red proven against HEAD — no INV-79 there) | BUILT |
| M-205 | The small-fix path is stated in the method's home: red before code stays the default order at every size; on a tiny reversible edit inside the pipeline's skip boundary a one-batch fix+test is legal only when the batch closes with the mechanical red proof (restore the pre-change file, run the suite, watch the new rows fail, restore the fix) named in the landing record — and build-pipeline's skip sentence points at the path; never a batch with no recorded red run, never a session re-deciding the order under pressure | E-27 | string | `test_small_fix_red_path` (red proven against HEAD — the path absent there) | BUILT |
| M-191 | The suite's own plumbing must not lie, three legs stated: skip helper imports at module load (an unrunnable skip path is red); an engine/instance shim owes a re-export completeness test; a background/delegated run's verdict is the suite log's own tail line, never the wrapper's exit code; never a silent pass born of broken skip plumbing | INV-80 | string | `test_suite_plumbing_must_not_lie` (red proven against HEAD — no INV-80 there) | BUILT |
| M-301 | green means deterministic and a flaky owned test is a defect fixed at its root (INV-155): the spec states the removable-in-owned-code seam question, the two nets, and the marked quarantine; build-pipeline step-8's green definition and test-author both carry "green means deterministic"; the retry/rerun-plugin mask is banned by a mechanical guardrail; never a retry, rerun-until-green, or "it passed this time" masking an owned flake | INV-155 | string | tests/test_flaky_test_is_a_defect.py + test_no_retry_plugin (red proven against pre-delta tree 2026-07-15) | BUILT |
| M-304 | the canonical harness template launches muted (--mute-audio), reaps the whole process group on teardown, and bounds each command with a per-command deadline; teardown also runs on atexit and SIGINT/SIGTERM while leaving a host's SIG_IGN disposition intact, and a launch-time sweep reaps a prior killed run's leaked profile dir + process group (owner-pid dead) across every temp workspace the harness could use, and reaps an OLD ownerless dir by age (the system temp is not self-purging) while leaving a young ownerless dir and a live concurrent run alone, and surfaces a temp glut loudly; it ships a by-deed post-run orphan net (orphan_guard/surviving_orphans) scoped to this process's own launches so a concurrent sibling process never false-reds it; test-author states the rule; never a shipped harness template that plays sound, leaks orphans (even on a run killed before teardown), lets its temp litter accumulate unswept, overrides a host's ignored signal, or reads its own slowness as a red | INV-157 | string | test_template_launches_muted + test_template_reaps_process_group + test_template_bounds_commands_with_a_deadline + test_template_sweeps_stale_profiles_at_launch + test_template_teardown_on_signal_and_atexit + test_template_is_generic_core_no_project_methods + test_test_author_states_the_harness_rule + test_sweep_covers_every_temp_workspace + test_teardown_leaves_a_hosts_ignored_signal_intact + test_harness_ships_by_deed_orphan_check_helper + test_orphan_net_scopes_to_this_process_own_launches + test_sweep_reaps_old_ownerless_litter_by_age + test_harness_warns_on_a_temp_glut + test_spec_states_the_temp_is_not_self_purging + test_sweep_reaps_only_the_provably_not_in_use + test_sweep_never_reaps_the_current_run_exclude (the last two are by-deed: the sweep DELETES dirs, so young-survives / old-reaped / live-owner-untouched / excluded-untouched is proven by running it, prover F1/F2) | BUILT |
| M-315 | INV-157's third net ships: `guardrails/check-muted-launch.sh` scans the repo's tracked scripts; never a repo with the browser-driving law stated but no scanning guardrail | INV-157 | string | test_guardrail_ships + test_guardrail_passes_the_clean_repo | BUILT |
| M-316 | A tracked script that launches a real headless browser (`--headless` / `--remote-debugging-port` / `puppeteer.launch` / `chromium.launch`) with no `mute-audio` anywhere in the file reds the guardrail by name (INV-157); never a script that drives a headless browser unmuted while the suite stays green | INV-157 | string | test_reds_on_an_unmuted_headless_launch (red proven against HEAD — guardrail absent) | BUILT |
| M-317 | A tracked script that launches a headless browser WITH `--mute-audio` passes the guardrail; never a false-flag on a properly muted launch | INV-157 | string | test_allows_a_muted_launch | BUILT |
| M-318 | A tracked script with no browser-launch signal at all passes the guardrail; never a false-flag on a file that drives no browser | INV-157 | string | test_allows_a_file_with_no_launch | BUILT |
| M-319 | The guardrail checks the whole FILE, not one line: a multi-line arg list whose `--headless` and `--remote-debugging-port` sit on separate lines from each other, with no `mute-audio` anywhere in the file, still reds; the same multi-line shape WITH `--mute-audio` on its own line passes; never a per-line check that a wrapped arg list slips past | INV-157 | string | test_catches_a_multiline_arg_list_without_mute + test_allows_multiline_args_with_mute (red proven against HEAD — guardrail absent) | BUILT |
| M-342 | The harness template prefers chrome-headless-shell (INV-157, tlvphotos frame-death incident 2026-07-16, row 364): the template's binary resolution picks the newest chrome-headless-shell install first (`~/.cache/puppeteer/chrome-headless-shell/...`), falls back to Chrome for Testing and then a system Chrome, and drops `--headless=new` when the shell is the pick (headless by construction); never the shell launched with `--headless=new`, never Chrome for Testing preferred over a present shell | INV-157 | string | `test_template_prefers_headless_shell` + `test_shell_pick_drops_headless_new` (red proven against HEAD — no shell preference in the template) | BUILT |
| M-343 | The harness template probes the browser at launch (INV-157, row 364; the loopback leg from the 2.3.0 design review's held ask, folded same session): before any suite is trusted, one throwaway page is served from the loopback address, first loaded and then awaited for a single requestAnimationFrame, each leg under its own bounded ~2 s, and a browser that stalls the load or produces no frame fails loudly under the probe's own name, the message naming which leg failed and the shell install command; never a stalling or frame-dead browser bleeding false reds through paint-waiting rows, never the probe's failure shaped like a product red | INV-157 | string | `test_template_probes_for_a_frame_at_launch` + `test_probe_failure_names_itself` + `test_probe_covers_the_local_load_stall` (red proven against HEAD — no probe in the template; the loopback leg red-proven against the pre-fold tree) | BUILT |
| M-305 | the browser test harness has one canonical home shipped once by the pack as a template, cited by INV-157/158 and the spec; never the harness core forked into divergent copies inside the pack | INV-158 | string | test_template_exists + test_template_docstring_cites_invariants + test_spec_states_harness_invariants | BUILT |
| M-311 | A cleanup targets only what the run owns, never a shared resource in current use (INV-162): a kill uses a recorded PID / process group or an install path, never a broad name pattern (`pkill chrome`, `chrome_crashpad_handler`) that can match the human's own program; the harness reaps only its own group and sweeps only a dead-owner dir; a guardrail reds a forbidden broad kill in a tracked script and passes a path-scoped one; never a cleanup that kills the user's real browser or a live sibling's resource | INV-162 | string | test_guardrail_ships + test_guardrail_passes_the_clean_repo + test_guardrail_reds_on_a_broad_kill + test_guardrail_catches_every_evasion + test_guardrail_allows_the_safe_forms + test_guardrail_allows_a_path_scoped_kill + test_spec_states_the_cleanup_ownership_law + test_worker_briefing_carries_the_constraint (red proven against HEAD — guardrail absent) | BUILT |
| M-307 | The suite-honesty / test-infrastructure invariants are one declared class with net-parity (INV-160): the nine members (INV-77/78/79/80/100/102/155/157/158) are enumerated and the parity stated — each names the net that reddens a run on its own violation, a member naming none a prover-blockable class defect [INV-125]; the class binds forward [INV-159]; never a new suite-honesty invariant with no stated parity to write against | INV-160 | string | test_infra_family_declared_a_class + test_infra_class_enumerates_every_member + test_infra_class_states_net_parity_and_binds_forward (red proven against the pre-delta tree, 2026-07-15) | BUILT |
| M-236 | Tests clean up after themselves and are born in a temp home (INV-100, row 222): every test removes what it creates (temp files, fixtures, processes, mutated shared state), a suite run leaves the machine as it found it, a leak is a defect of the test; placement — files born in the system temp home / gitignored state dir, user-visible folders never a test's workspace, a headless browser's download dir pointed at the temp home; the pack's own instantiation: prefixed temp artifacts (livespec-test-) + the session before/after temp-home diff (tests/conftest.py) + the card tests' leak swept; never a suite run that drops files where a human sees them, never a leak waved off as harmless | INV-100 | string | `test_cleanup_half_in_both_homes` + `test_placement_half_in_both_homes` + `test_template_carries_the_checklist_item` + `test_the_leak_check_stands` + `test_spec_anchor_and_index` (red proven against the pre-delta tree) + the leak check itself red-proven by the mechanical protocol (a planted livespec-test- file, the session diff failed, removed) — 2026-07-12 | BUILT |
| M-238 | A test's expected value derives independently of the code under test (INV-102, row 224): a test compares the code's output against an expected value, and that value comes from an independent source — a hand-computed constant, an independent derivation, or a recorded real output reviewed by a human; when the expected value is the code's own formula the assertion only ever proves the code equal to itself; boundary — a round-trip or property test over the outputs is legal because it asserts an invariant rather than a recomputed value; never recompute the code's own formula and assert the result as the expected value, never dress a mirror assertion as a real check | INV-102 | string | `test_independence_stated_in_both_homes` + `test_legal_sources_in_both_homes` + `test_boundary_stated_in_spec` + `test_spec_anchor_and_index` (red proven against the pre-delta tree, 2026-07-12) | BUILT |

### [node: feedback-intake]

| ID | Fact (from spec) | Spec ref | Test level | Owning test | Status |
|---|---|---|---|---|---|
| M-172 | The feedback-intake skill ships: SKILL.md loads (frontmatter name + version), states the three channels (spoken/typed · comment on something shown · dropped file via the inbox door) and the ledger's home and line shape (when · who/channel · concerns · plain words · route); never a skill file without the channels or the ledger shape | E-28 | string | `test_feedback_intake_ships` (red proven against HEAD — file absent there) | BUILT |
| M-173 | The routing table names every route WITH its home (wish→queue row · fix→commit+journal · answer→archive+harvested row · field evidence→ledger line · workshop noise→problem ledger) and both fire sides (fires on receipt and at inbox sweep; never on the agent's own output, never opening a queue row on its own judgment); never a route without a named home | T-20 | string | `test_feedback_routes_have_homes` | BUILT |
| M-174 | The never-lost law agrees across its three surfaces: SPEC's INV-68 index line, the skill's own text, and inbox/README.md all speak route-homes (same session · one echo per item · re-mention appends its date · only the assigned session writes the ledger, outsiders via inbox); never a surface still speaking the old wishes-only harvest | INV-68 | string | `test_feedback_never_lost_in_both_homes` | BUILT |

### [node: feedback-collector]

| ID | Fact (from spec) | Spec ref | Test level | Owning test | Status |
|---|---|---|---|---|---|
| M-308 | The feedback-collector ships and fires only rarely: SKILL.md loads (name + version 1.0.0), states a rare strong-moment offer that drafts an upstream note, and is OFF by default (the `feedback-upstream` flag), reading nothing until a host opts in; never a skill file missing the offer or the off-by-default flag, never firing on the agent's own output or below the unmistakable bar | E-30 | string | `test_feedback_collector_ships` + `test_off_by_default_and_positive_consent` (red proven against HEAD — the skill absent there) | BUILT |
| M-309 | The upstream note is distilled, non-public, deposited, and never sent: on a positive yes the pack drafts a self-contained note that carries its own context and deposits it in the gitignored `outbox/`, opening no network connection and no public request; never a raw transcript or script inside the note, never a tracked or pushed note | T-21 | string | `test_never_sends_only_deposits` (asserts both the deposit-not-send and the gitignored outbox; red proven against HEAD) | BUILT |
| M-310 | Consent is positive word and the arm is fenced: an explicit yes every time (the deliberate opposite of silence-is-consent [INV-31]), distinct from feedback-intake (the inverse arrow [T-20]) and from the measurement family (no scoring), the flag a marked row in the settings catalog; never a silent send, never sentiment scoring, never a behaviour with no spec anchor | INV-161 | string | `test_off_by_default_and_positive_consent` + `test_distinct_from_intake_and_measurement` + `test_flag_in_settings_catalog` + `test_spec_states_the_third_arrow` (red proven against HEAD) | BUILT |
| M-335 | The upstream note is anonymized in the draft the user reads at consent (INV-179): host entities become neutral role words, pack-public names stay, the approved note is exactly the travelling note; never a note masked after the yes, never an internal name leaving in a note | INV-179 | string | `test_collector_masks_host_entities` + `test_masking_precedes_consent` + `test_spec_states_the_law` | BUILT |

### [node: onboarding-card]

| ID | Fact (from spec) | Spec ref | Test level | Owning test | Status |
|---|---|---|---|---|---|
| M-206 | The rendered card carries every card-visible package-defaults table row, and every card row traces to a marked table row or a recorded profile line (completeness both ways, run on this repo's real base table and profiles); never a silently dropped card-visible row, never an orphan card row, and the render completes within the 1 s budget | INV-87 | DOM-text | `test_onboarding_card_completeness` (red proven against HEAD — the renderer absent there) | BUILT |
| M-207 | The card's fixed copy states rules: rendered with a fixture profile, the language row carries the conversation-follows-you rule and the docs-language value from the ladder, and a personal value (name, language) appears only in the reader's-own slot labelled changeable; never a personal value baked into fixed copy (rendered with a value-free fixture, no personal value remains) | INV-88 | DOM-text | `test_onboarding_card_copy_rules` (red proven against HEAD — the renderer absent there) | BUILT |
| M-208 | A missing personal profile renders the card on package defaults with a plain absence notice naming the founding offer; never a crash, never a normal-looking card that hides the absence | INV-87 | DOM-text | `test_onboarding_card_missing_profile` (red proven against HEAD — the renderer absent there) | BUILT |
| M-209 | A malformed package-defaults table row fails the render loudly (nonzero exit naming the row); never a silently dropped row | INV-87 | string | `test_onboarding_card_malformed_row` (red proven against HEAD — the renderer absent there) | BUILT |
| M-211 | The rendered card matches the frozen norm structurally: every norm section heading and every norm row name appears in the render, and no invented value formula appears ("yours today" is the caught sample); never a render whose sections or row names drift from the approved page | INV-88 | DOM-text | `test_onboarding_card_norm_conformance` (red proven against the bounced renderer) | BUILT |
| M-212 | A multi-line keyed host-profile entry renders whole: its wrapped lines join into one logical line, its value reaches its card row (a card-visible key like `project.kind` shows the recorded value), and no raw markdown (a leading bullet-and-backtick, a dangling backtick) leaks into the card; never a truncated first-line render | INV-87 | DOM-text | `test_onboarding_card_multiline_host_lines` (red proven against the bounced renderer) | BUILT |
| M-213 | The card survives real-shaped and hostile inputs: markup inside a recorded value is escaped (never raw injection); a missing HOST profile renders with the project-rules part present and no crash; nested sub-bullets and wrapped lines (the live profiles' shape) leak no raw markdown; the template personal profile renders clean on a fresh project; never a crash or an injection on any of these | INV-87 | DOM-text | `test_onboarding_card_robustness` (red proven by the mechanical protocol — renderer removed, rows fail, renderer restored; the v2 renderer already carried these guards) | BUILT |
| M-210 | The trigger wiring stands: the adoption guide carries the setup-end card line, communicator carries the standing-question line, and the spec's norm pointer resolves to the frozen file in docs/norms/; never a promised trigger with no home sentence | INV-87 | string | `test_onboarding_card_wiring` (red proven against HEAD — the wiring absent there) | BUILT |

### [node: design-reviewer]

| ID | Fact (from spec) | Spec ref | Test level | Owning test | Status |
|---|---|---|---|---|---|
| M-283 | The design-review pass ships in `skills/design-reviewer/SKILL.md`: it reads a proven spec after the prover, builds its own element inventory below the page-level surface list (photo, caption, control, slot) that is never written into the surface registry [E-10], describes each element by the person's action words, proposes same-kind groups, checks behaviour parity, and names two concrete objects with their spec sentences per finding; it runs full at every full prover pass and scoped at every surface add, standing down at intake and the push gate; every finding is a recommendation or an ask and never a blocking defect, so it never holds a landing; each run writes a dated `docs/design-review/YYYY-MM-DD.md` record; a confirmed grouping lands as a class sentence [INV-125], a confirmed difference as a decided sentence [INV-59]; never an auto-generated rival registry, never a finding that blocks a landing (INV-141, design-review wish) | INV-141 | string | `test_skill_ships` + `test_similarity_lens_ships` + `test_inventory_never_a_rival_registry` + `test_never_blocks` + `test_record_discipline` + `test_spec_clauses_stand` + `test_formal_index_rows` + `test_architecture_node_and_seams` + `test_matrix_rows_cite_the_node` (red proven against the pre-delta tree, 2026-07-14) | BUILT |
| M-284 | Every design-review finding carries a confidence read of confident or likely: confident when the grouping and divergence stand on the spec text alone (written as a recommendation, queues, never blocks), likely when the deciding fact is the human's intent; a likely finding fires as one ask only when the strong-signal bar holds (shared role in one plain sentence, a whole behaviour one member lacks rather than a parameter, no spec sentence already deciding it), shown as two objects each with its spec sentence plus a recommended default on the batched-question path [INV-30], at most three per pass strongest first, a signal below the bar staying silent; an unanswered ask is held on the dated record and not re-raised on its own until answered; the channel carries exactly one producer for now, the same-kind divergence; never an ask below the strong-signal bar, never more than three asks per pass, never an unanswered ask re-firing as noise each pass (INV-142, design-review wish) | INV-142 | string | `test_confidence_read_two_values` + `test_echo_bar_and_cap` + `test_unanswered_held` + `test_spec_clauses_stand` + `test_formal_index_rows` + `test_matrix_rows_cite_the_node` (red proven against the pre-delta tree, 2026-07-14) | BUILT |
| M-299 | the prover/design-review loop is bounded and never holds a landing (INV-154): the spec states a round (one prover re-read + one design-review re-read), three named resting states (converge / wait / stand down) with a standing recommendation not holding the loop open, a waiting round not counting toward the cap, the cap at three progressing rounds (host-settable) counted by the design review, and that surfacing at the cap holds no landing (the landing proceeds, unsettled groupings recorded); build-pipeline step-2 states a confirmed grouping re-enters the prove step; never a silent spin, never a design-review finding blocking a landing | INV-154 | string | test_fixed_point_loop_bounded_and_nonblocking (red proven against pre-delta tree 2026-07-15) | BUILT |
| M-300 | the design review ships as a first-class member of the pack's shopfront: skills/design-reviewer/ carries its own README (why / what a finding looks like / when it fires / install) and LICENSE like product-prover, and product-prover's README presents the design review as its younger sibling that ships in the same pack (the cross-reference names design-reviewer); never a shipped skill with no README, never the design review invisible from the prover's shopfront | INV-141 | string | test_public_skills_ship_readme_license_and_crossref (red proven against pre-crossref tree 2026-07-15) | BUILT |
| M-314 | A gesture/overlay/motion spec triggers the design review's standing motion-parity lens (INV-165): on top of the bottom-up similarity lens (INV-141), a spec that ships a gesture, motion, or opening/closing layer runs a standing lens by construction naming three same-kind groups — entry mirrors exit (close is open reversed), every object type behaves alike (each lands on its own on-screen rectangle via a per-element source-rect read rather than a per-type special case), every position behaves alike (same gesture, same type, different slot behaves the same); each a recommendation or question, never a blocker, holding once declared a class sentence (INV-125); the second producer of the design review's echo channel; homes the design-review clause + the design-reviewer skill; never a gesture/overlay spec whose entry/exit, object-type, or position parity goes unchecked by construction | INV-165 | string | `test_spec_clause_names_the_three_groups` + `test_spec_anchor_and_index_row` + `test_design_reviewer_skill_carries_the_lens` + `test_architecture_owns_165_under_design_reviewer` + `test_matrix_row_present` | BUILT |
| M-323 | A feature delta adding a second member of a kind an existing surface already has draws the scoped design review at intake (INV-169): FEATURE-FIT asks the second-sibling question by construction, a yes draws the scoped pass over the delta's elements against the existing inventory, a no is recorded as a lens verdict; the intake stand-down holds only for a delta with no such sibling; never a second sibling entering, shipping, and diverging with no design pass until the next FULL | INV-169 | string | `test_spec_states_the_second_sibling_law` + `test_spec_scopes_the_stand_down` + `test_prover_skill_asks_the_question_at_intake` + `test_design_reviewer_carries_the_exception` + `test_index_row_present` | BUILT |
| M-303 | Every review pass records its outcome of one class (INV-156): three write a dated file under the pass's own home (prover `docs/prover/`, design review `docs/design-review/`, audit `docs/audit/`), opening by naming the skill and version, a per-finding disposition column, a same-day `-suffix`; members — the prover record and the design-review record as full siblings (the design review alone holding an ask across passes INV-142), and the audit record (the periodic adversarial audit + its whole-pack skill-creator craft walk INV-145 (M-128)) carrying folded / rejected(+why) for a craft walk and MET / OWED / FLAG for a milestone read, no held-ask; the verify-by-deed audit the one named difference — its verdict and the per-landing skill-creator review it runs INV-99 land in the landing record's accounting rather than a dated class file INV-46; the class binds forward, a new pass a member or a named difference, pre-declaration records unreshaped; never a review pass whose record shape no reader can predict, never verify silently promoted to a dated class file | INV-156 | string | `test_review_record_class_declared` + `test_names_every_member` + `test_states_verify_difference` + `test_formal_index_and_ownership` (red proven against the pre-delta tree, 2026-07-15) | BUILT |

---

## Coverage validation — walked 2026-07-05 at derivation; re-walked mechanically by `tests/test_traceability.py` at every run

- [x] Every spec anchor (invariant / state / transition) appears in ≥ 1 row — the count is recomputed mechanically at every run by `test_matrix_covers_every_anchor` (`spec_index_anchors()` expands the Formal index's ranges; 170 unique anchors from 164 index rows at the 2026-07-10 audit), so this line never pins a number that can drift.
- [x] Every architecture node has ≥ 1 block, and its negative-side rows exist — 18/18 blocks (counted as the `### [node: ...]` headings under Matrix rows, via `matrix_blocks()`), every row carries a NEVER clause (`test_matrix_rows_have_level_and_negative_side`).
- [x] Every artifact-inventory entry is asserted shipped-and-non-empty (`test_artifact_inventory`) — the text-product rendered level (prover F6).
- [x] Every visibility / layout / colour / interaction fact sits at level ≥ `browser-computed` — vacuously true: the pack ships no browser surface (prover F6); the clause re-arms the day one exists.
- [x] No row cites a spec anchor or node that no longer exists — mechanized in `test_matrix_covers_every_anchor` (stale refs fail the suite; retirement, never deletion).

A fact with no row, or a row at a too-weak level, is a **derivation defect** — fix it here, before it is
a production bug.

---

*Add rows as the spec grows. Retire rows (mark RETIRED, do not delete) when a feature is removed — so the removal is auditable.*
