# Pending inbox sweep — ready-to-apply draft

Prepared by an Opus analyst, 2026-07-12. This file is a draft only. It writes nothing to ROADMAP.md,
the inbox files, or the spec — another session holds the pen on ROADMAP.md right now. Apply it when the
pen is free, from a session assigned to live-spec.

Three wish files sit in `inbox/`:

- `inbox/2026-07-11-from-tlvphotos-architecture-rework-owes-document-rework.md`
- `inbox/2026-07-12-from-tlvphotos-restructure-merge-gate-needs-a-stated-law.md`
- `inbox/2026-07-12-from-alexander-design-principles-impact-analysis-and-compaction.md`

## Verdicts

**Wish 1 (architecture rework owes document rework) — NEW ROW 257.**
The pack already owns the re-carve mechanism: a restructure verdict opens its own row and re-divides only
through the architecture stage and its re-proof (SPEC line 141, line 634, INV-37, E-14). What the wish
names is genuinely uncovered — the refactor door's "pins update when boundaries move" path is weaker than
a real redesign owes, because after a restacking the old document's own structure lies even with fresh
pins. The new row states the law and scopes the pins-only path to a boundary shift that leaves the shape
standing. It leans on INV-37/E-14 as the mechanism, so it opens a NEW ROW rather than folding in.

**Wish 2 (restructure/migration merge-gate law) — NEW ROW 258.**
Rows 204 and 240 already own part of this. Row 204 makes the token-identity plus punctuation-multiset plus
full-prover-pass equivalence proof a durable script; row 240 names the docs-layout vehicle whose owed
checks include the same word-token and punctuation multiset. Neither states the MERGE-GATE law: that the
blocking set is delta-scoped, that pre-existing findings equal on both sides route to queue rows and never
block, and that a session sharpening a human's spoken bar must say the sharpened form back and mark it as
its own interpretation. The spec carries no equivalence-merge-gate clause today (grep found none). So the
new row is scoped to the uncovered part and cites rows 204/240 as the proof mechanism it builds on.

**Wish 3 (design principles: impact analysis + compaction, Alexander live 2026-07-12 ~02:04) — TWO NEW
ROWS, 259 and 260.**
The wish carries six formulated principles; they split into two separable stories plus one already-covered
citation.

- Principles 1-3 (entry impact analysis, footprint decides the route, module-boundary health) → NEW ROW
  259, sized **surface** — it changes how every request enters the pipeline. Overlaps resolved: the door
  (T-12) and kind (T-16) triage and the map placement (INV-37) already read the spec and the architecture
  at intake; the wish adds the CODE read, the footprint named aloud before work starts, and the
  footprint-decides-weight law, which today lives only as the general reach and scaling principles
  (INV-45, INV-61) with no entry station. Those stand as fences and kin; the row builds the station on
  them. Principle 3's boundary-move signal rides this row, since the station is what observes the repeated
  cross-cuts; the move itself executes through the restructure re-carve (INV-37/E-14) and candidate row
  257's document-rework law.
- Principles 4-5 (abstraction fitness test, scheduled compaction) → NEW ROW 260. Milestone DOC compaction
  already exists (M-1; SPEC line 699 sweeps spec/matrix/queue/skills/ledger plus the test suite; INV-13
  prunes restatements) — that half stands as a fence, never rebuilt. The uncovered part: CODE compaction,
  abstraction extraction gated by the three-question fitness test (testable alone · a second place needs
  it · parallel-workable), the second-occurrence trigger (base rule 19 kin), and the fitness test's second
  home at the architecture step where abstractions are born. The wish itself names INV-109 as covering the
  removal listing — cited, never restated.
- Principle 6 (a reached level locks) → ALREADY COVERED by the convergence family: row 218 put the
  principle in the base rulebook (reached levels lock by mechanism), row 217 audits every lock. Row 260
  cites the lock duty per compaction pass; no new law opens for it.

Highest existing row = 256. Next free numbers = 257, 258, 259, 260.

## Drafted rows (paste into the ROADMAP table, exact format)

| 257 | **A deliberate architecture redesign re-shapes and re-proves ARCHITECTURE.md in the same movement** (tlvphotos inbox 2026-07-11 ~23:11, Alexander's word: a second-finger bug — a moving finger drags the underlying room layer into view in the polaroid side room, the resting-finger fix having opened the window for it — led him to order a rethink of the UI-layer architecture plus a spec update, and he asked whether the pack already forces the document rework in that case; the honest answer was only partly): the pack states two narrower things today — build-pipeline step 3 updates ARCHITECTURE.md from the proven spec, and the refactor line updates the doc's pins when a refactor moves node boundaries; both fall short of a real redesign, because after a restacking the old document's own structure lies even with fresh pins; state the law — when structure is deliberately redesigned (layers restacked, surface ownership moved, nodes merged or split) the architecture document is re-shaped to the new form and re-proven with the architecture lens in the same movement, and the pins-only path is scoped to a boundary shift that leaves the shape standing | small | queued 2026-07-11 ~23:11 from inbox (door: feature; kind: skill; map: build-pipeline's architecture step + the refactor door line + the re-carve law INV-37/E-14 at the architecture-doc section, no new surface; kin: rows 204/240 same homelessness family, the restructure re-carve clause; born-of: the tlvphotos second-finger redesign, from the tlvphotos window) | Done when: the redesign-owes-rework sentence stands in its homes red-proven, the pins-only path scoped to boundary shifts, the suite covers the clause, and the first real architecture redesign re-shapes and re-proves its document |
| 258 | **The restructure/migration merge gate gets a stated law** (tlvphotos inbox 2026-07-12 ~01:28, Alexander's word «надо переписать правила … пойми откуда пришло»): the pack states no law today for what a restructure merge's equivalence proof consists of or how prover findings route, so each session invents its own bar; tonight that gap produced a wrong one — the orchestrator over-sharpened his spoken «prover finds nothing both sides» into «any finding parks the merge», and parked a strictly-improving merge on the OLD side's pre-existing clarity debts, which he corrected live: the gate judges the DELTA; state the law in three parts — (1) load-bearing token identity old-vs-new modulo the per-chunk named deltas plus the punctuation-multiset check, (2) the full suite green on the merged tree, (3) a full prover pass on both sides whose BLOCKING set is delta-scoped (an unmatched token, a red suite, a new-side finding absent on the old side, an unnamed meaning change); pre-existing findings equal on both sides route to queue rows in the same landing and never block; and a session that sharpens a human's spoken bar beyond his words says the sharpened form back and marks it as its own interpretation | small | queued 2026-07-12 ~01:28 from inbox (door: feature; kind: skill; map: a merge-gate law across the product-prover walk + build-pipeline's docs-only/restructure door, no new surface; kin: row 204 owns the token-plus-punctuation verifier this builds on, row 240 owns the docs-layout vehicle's multiset check, INV-39 the landing gate; born-of: the equivalence-gate-judges-the-delta correction, from the tlvphotos window) | Done when: the three-part gate stands red-proven, pre-existing-equal findings route to queue rows and never block, the say-the-bar-back-and-mark-interpretation duty stands in its home, the suite covers the clause, and the first real restructure merge is gated by it |
| 259 | **Impact analysis stands at every request's entry — the footprint decides the route** (Alexander live 2026-07-12 ~02:04, via inbox/2026-07-12-from-alexander-design-principles-impact-analysis-and-compaction.md, his word: every incoming request gets an entry analysis reading the spec AND the architecture AND the code together; presentation-only, one-backend-module, and cross-cutting changes are three kinds of work; formulate the proper design principles): today's intake reads the spec and the architecture — the door (T-12), the kind (T-16), the map placement (INV-37) — and the reach/scaling principles (INV-45, INV-61) size the process after the fact; the wish adds the third read and the station — every request (feature, bug, wish) is triaged at entry against spec (what behavior changes), architecture (which modules own it), and code (what actually gets touched), the footprint named aloud before any work starts; the footprint decides the route in both directions — presentation-only takes the light road, a change inside one module goes through that module's interface and tests, a cross-cutting change always opens the full pipeline; and the station carries the boundary-health law — a right boundary means an edit inside a module leaves its neighbors untouched, so a typical request lands in one module, and requests repeatedly cutting across several modules are the signal to move a boundary (the move executes as a restructure row through INV-37/E-14 and row 257's document-rework law) | surface | queued 2026-07-12 ~02:04 from inbox, his live word (door: feature; kind: skill; map: changes the pipeline's intake — build-pipeline's door/kind/map triage widened with the footprint read, spec-author's echo carries it, the whole pipeline is the surface; kin: T-12/T-16/INV-37 the existing triage as fences, INV-45/INV-61 the scaling family, rows 253/254 the routing family, row 257 the boundary-move executor; born-of: the six-principle wish, principles 1-3) | Done when: the entry station stands in its homes red-proven (the three-source read, the spoken footprint, the footprint-to-route table, the boundary-health signal), the facet/fit sweeps stay whole as fences, one real request of each footprint class walks its route, and the suite covers the clauses |
| 260 | **Compaction is a scheduled station for code as well as docs, and an abstraction proves itself by three questions** (same wish and word, 2026-07-12 ~02:04: do not forget periodic compaction of docs and code — finding the right abstractions so everything is more testable, more reusable, and workable in parallel): milestone DOC compaction already stands (M-1's audit sweeps spec/matrix/queue/skills/ledger and the test suite; INV-13 prunes restatements; INV-109 lists every removal of substance) — that half is a fence; the uncovered part — the station widens to CODE (duplicates merge, dead weight leaves with its listing, ripened abstractions are extracted), gains the second trigger (the second occurrence of the same problem, base rule 19's moment), and every extraction is gated by the three-question fitness test: can it be tested alone · does a second place need it · can two people work it and its neighbor in parallel without queuing on shared files — three yes answers make it right, two no answers make it premature; the fitness test's second home is the architecture step, where new abstractions are born; each pass ends with the reached level locked by a test or a lint (the convergence law, base rule from row 218, cited never restated; row 217's audit covers the lock) | small | queued 2026-07-12 ~02:04 from inbox, his live word (door: feature; kind: skill; map: the milestone rhythm's compaction section + the architecture step's abstraction guidance, no new surface; kin: M-1/INV-13/INV-109 the doc half as fences, base rule 19 the second-occurrence trigger, rows 216-218 the lock family; born-of: the six-principle wish, principles 4-6, principle 6 already covered by row 218) | Done when: the code-compaction clause and the two triggers stand in the milestone rhythm red-proven, the fitness test stands in both homes, the first real pass runs at the 1.1.0 audit with its levels locked, and the suite covers the clauses |

## Mechanical apply steps

1. **Insert the four rows into ROADMAP.md.** Anchor = the current last table row (row 256), which begins:
   `| 256 | **A repeatedly-broken behavioural rule earns a live channel**`
   and ends with `... suite 400) | Done when: ... suite green — MET (both homes red-proven; the audit item cites the law; suite 400) |`.
   Insert the four drafted rows on new lines immediately AFTER that full line, in order 257, 258, 259, 260.

2. **Retire each wish file from inbox/.** The inbox protocol (inbox/README.md) says the harvest removes the
   file in the same commit; git history keeps it. Established precedent (rows 47 and 109) moves the file to
   `docs/queue-archive/` with a pointer, which keeps the trail visible. Recommended, matching precedent:
   - `git mv inbox/2026-07-11-from-tlvphotos-architecture-rework-owes-document-rework.md docs/queue-archive/2026-07-11-from-tlvphotos-architecture-rework-owes-document-rework.md`
   - `git mv inbox/2026-07-12-from-tlvphotos-restructure-merge-gate-needs-a-stated-law.md docs/queue-archive/2026-07-12-from-tlvphotos-restructure-merge-gate-needs-a-stated-law.md`
   - `git mv inbox/2026-07-12-from-alexander-design-principles-impact-analysis-and-compaction.md docs/queue-archive/2026-07-12-from-alexander-design-principles-impact-analysis-and-compaction.md`
   - Append one pointer line to the top of each archived file: `> Harvested into ROADMAP row 257 (2026-07-12).`, `> Harvested into ROADMAP row 258 (2026-07-12).`, and `> Harvested into ROADMAP rows 259-260 (2026-07-12).` respectively.
   (If the project prefers the literal README wording instead, `git rm` the files in the harvest commit and skip the archive move — history still holds them.)

3. **These rows are queued, not landed.** They enter the serial queue behind the current work; no pipeline
   steps run in this sweep. Execution is each row's own later landing.

## Commit message suggestion

```
Sweep inbox: three wishes into ROADMAP rows 257-260

- Row 257: a deliberate architecture redesign re-shapes and re-proves ARCHITECTURE.md in the same movement (tlvphotos 2026-07-11)
- Row 258: the restructure/migration merge gate gets a stated law (tlvphotos 2026-07-12)
- Row 259: impact analysis stands at every request's entry, the footprint decides the route (Alexander live, 2026-07-12)
- Row 260: compaction becomes a scheduled station for code as well as docs, with the abstraction fitness test (Alexander live, 2026-07-12)

All three wish files moved to docs/queue-archive/ with a pointer to their rows.
```

---

**APPLIED + CLOSED at landing 2026-07-12 ~02:25** (.live-spec/checkpoints/inbox-sweep-worker.md). Rows
257-260 pasted verbatim into ROADMAP.md after row 256; rows 259/260 additionally carry the
orchestrator-ordered working-material clause pointing at
`.live-spec/checkpoints/pending-design-principles-architect-draft.md (P0-P12) + the wish file's two
addenda`. All three wish files retired to docs/queue-archive/ with pointer lines; wish 3 had gained two
ADDENDUM blocks live and was untracked in git at apply time (never committed) — moved whole via plain
`mv` instead of `git mv`, content unchanged. Full suite green: 411 passed. No version bump (queue
bookkeeping, precedent da6e9e2).
