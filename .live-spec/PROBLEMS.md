# live-spec — Problem ledger (`.live-spec/PROBLEMS.md`; SPEC E-24, INV-23)

The pack's own workshop ledger (dogfood; opened at the row-100 landing, 2026-07-05). The walk: grep for
the signature; not listed → one WATCHED line, keep working — never a silent retry. Listed → the second
occurrence gets an owner that moment (a queue row, or the human's dated AGREED NON-PROBLEM). A third
unowned recurrence goes to the queue as a METHOD defect. Owned entries only append dates; the landing
that closes the row flips them to SOLVED; the milestone compaction archives below.

| Signature | Occurrences (dates) | Status | Notes |
|---|---|---|---|
| zsh eats a bare `===` echo separator | 2026-07-05 (session 7), 2026-07-05 (session 9), 2026-07-05 ~23:55 (session 11, fired once more before the verdict was read), 2026-07-06 ~10:15 (session 12 — fired again mid-work; the hand typed it a fourth time) | SOLVED 2026-07-06 ~11:17 [his word escalated to full delegation («сам разберись, мне всё равно, можешь менять — но backward compatible»); with that word given, the classifier let the agent install its own fence: a PreToolUse hook (`~/.claude/hooks/block-triple-equals.sh` via `scripts/install-separator-fence.sh`) now DENIES any command carrying a bare `===` — backward compatible by design: quoted "===" is legal zsh and passes, heredoc bodies (file content) are excluded from the scan, both covered by the installer's self-tests; PROVEN BY DEED: `echo === proof` blocked live at 11:16] | shell glob behaviour, not ours to fix; standing workaround: separators are `---`, never bare `===` |
| invented-time stamps: files/entries dated tomorrow | 2026-07-05 (rows 95–97 intake), 2026-07-05 (session 8 re-sweep), 2026-07-05 ~23:15 (inbox file + tlvphoto prover record both dated 07-06) | SOLVED 2026-07-05 (row 103 landed: `test_no_future_dated_stamps`, red proven on a synthetic future file) | hand-swept twice, recurred anyway → mechanical owner landed the same night |

| invented-time stamps, TIME variant: same-day stamps written ahead of the wall clock | 2026-07-05 (session 8: "~23:55"/"00:05" written at 22:40 — JOURNAL:853), 2026-07-05 23:43 (session 10: "~23:50"/"~23:58"/"00:02" written at ~23:35–23:43; caught in-session, corrected against git), 2026-07-06 ~00:11 (session 11: queue/landing stamps "00:15"/"00:30"/"00:40" written at ~00:05–00:11 — elapsed time GUESSED again; caught at the journal step's `date` call, corrected; third occurrence on the owned entry — row 104 should bubble as a quick win), 2026-07-06 ~01:32 (session 11 again: rows 110–115 stamped "~01:40", committed 01:28:57 — fourth same-night occurrence, caught by self-check against git while row 104's fence was BUILDING) | SOLVED 2026-07-06 (row 104 landed: guardrails/check-future-times.sh in pre-commit, red proven by deed — a staged "23:59" stamp blocked at 01:36) | the date fence (row 103) can't see same-day times; owner: pre-commit check — an ADDED line stamping today with a time later than the commit clock goes red (not racy at commit time) |
| invented-time stamps, CHAT variant: a reply's leading [HH:MM] typed from feel, not the clock | 2026-07-05 23:47 (session 11 opener stamped [01:47]; caught at the first `date` call, corrected aloud), 2026-07-06 ~13:13 (session 13: mid-session stamps ran up to ~7 min fast; a queue stamp born from one corrected against `date` before commit — second occurrence), 2026-07-06 ~13:22 (session 13 again: the row-128 queue stamp written 4 min ahead, caught at the next `date`; recurrence on the owned entry — dates appended, row 127 unchanged), 2026-07-06 13:47 (session 14 — the row-127 landing session itself: mid-session leads drifted up to ~9 min ahead, caught at the eval-brief clock read and owned aloud in the same reply, before the shipped sentence was a day old), 2026-07-06 14:13 (session 15 — the day the clause was one session old: three consecutive leads extrapolated to 14:16/14:17/14:18 while the clock read ≤14:13; caught at the journal step's `date` call, owned aloud, the row-132 intake stamp corrected; recurrence on the owned entry — the shipped sentence alone does not stop the hand, the `date`-before-any-stamp habit remains the law's hands), 2026-07-06 14:18 (session 15 SECOND catch, minutes after the first: leads 14:19–14:22 written while the clock read ≤14:18 — drift resumes the moment the clock isn't re-read; sharpened habit: a reply that leads with [HH:MM] runs `date` at its OWN start, every reply; if the drift recurs next session the entry re-opens as a METHOD defect row — candidate owner: a hook that injects the clock into the reply), 2026-07-06 15:50 (session 16 — the named next-session recurrence: leads extrapolated up to ~6 min ahead, 15:52–15:56 written while the wall clock read 15:50; caught at the worker-brief clock read, owned aloud; per the 14:18 note the entry RE-OPENS as a METHOD defect — row 134 owns the mechanical hand), 2026-07-06 20:38 (session 19 — the hand's own landing session: chat leads ran ~4 min ahead of the 20:38 clock and the row-139 landing stamps 20:42/20:43 were written at 20:38 — BLOCKED by the commit fence, the file face doing its job; corrected against `date`; the hook was built this same session, install awaits his hand), 2026-07-06 21:16 (session 20 — the row-142 landing stamps "~21:19" written at 21:16, BLOCKED by the commit fence, ninth catch; corrected against `date`; row 134’s hook install still awaits his one command), 2026-07-06 21:45 (session 20 second catch — a journal heading stamped one minute ahead, BLOCKED by the fence again, tenth; the hook install remains the open fix), 2026-07-06 21:56 (session 20 third catch, eleventh total — a resume-file stamp one minute ahead, BLOCKED by the fence), 2026-07-06 22:10 (session 21 — the hook's FIRST live session: the prompt-time lead was right off the injected clock, but MID-TURN the hand drifted again — record stamps and chat leads ran up to 2 min ahead of the 22:10 clock; caught at the journal step's own `date`; the hook covers the prompt moment, mid-turn stamps stay on the date-before-any-stamp habit + the commit fence — row 134's zero-drift field leg NOT yet met, twelfth catch; and AGAIN at 22:13 the same session, minutes after the correction — a queue stamp born one minute ahead, caught at the intake's own `date`: the hand drifts the moment the clock isn't read, the per-stamp `date` habit is the only mid-turn hand), 2026-07-06 22:54 (session 22 — mid-turn record stamps "~22:55"/"~22:58" written while the wall clock read ≤22:54; caught at the pre-landing `date` and corrected before commit, the fence never fired — thirteenth catch; row 134's zero-drift field leg stays open), 2026-07-06 23:06 (session 22 second catch, fourteenth total — a journal heading stamped one minute ahead, caught at the same reply's `date` before commit), 2026-07-06 23:07 (session 22 third catch, fifteenth total — "~23:08" staged while the commit clock read 23:07, BLOCKED by the commit fence live; corrected and recommitted), 2026-07-06 23:25 (session 22 fourth catch, sixteenth total — a journal heading one minute ahead written in the same command that had JUST printed the clock; caught at the next read, corrected before commit), 2026-07-06 23:37 (session 22 fifth catch, seventeenth total — a queue stamp one minute ahead, BLOCKED by the commit fence live), 2026-07-06 23:39 (session 22 sixth catch, eighteenth total — three lane-claim stamps one minute ahead, BLOCKED by the fence again; six catches ONE session: the mid-turn hand is beyond habit, row 134's mechanical leg is the only cure), 2026-07-06 23:57 (session 22 seventh catch, nineteenth total — a pushgate record heading one minute ahead, BLOCKED by the fence), 2026-07-07 00:09 (session 22 eighth catch, twentieth total — a journal heading one minute ahead in the same command that printed the clock; corrected at the next read), 2026-07-07 00:11 (session 22 ninth catch, twenty-first total — three claim stamps one minute ahead, BLOCKED by the fence), 2026-07-07 00:14 (session 22 tenth catch, twenty-second total — landing stamps two minutes ahead, caught at the pre-commit `date`; ten catches one session), 2026-07-07 00:18–00:36 (session 22, ONE batch append per the new INV-56 law — its first deed: four more fence/self catches serviced silently, no ceremony, work uninterrupted) | RE-OPENED as METHOD defect 2026-07-06 15:50 (row 134 — a hook injects the clock, the lead stamp stops being handwritten); previously SOLVED 2026-07-06 (row 127 landed: the read-at-write-time sentence in communicator rule 7, suite row `test_chat_timestamp_at_write_time`; no fence can reach chat — the law now lives where the exchange shapes live, and the walk's own habit line stands: `date` before any stamp) | rows 103/104 fence files and commits — neither sees chat; the habit alone did not hold, so the rule moved into the pack |
| a needled verbatim phrase straddles a hand-wrapped prose line: a test's exact-substring check fails though the words are all there (whitespace-collapse hides it only when the READER collapses; plain `in` on raw file text does not) | 2026-07-06 23:47 (session 22 — lane-B worker's README edit: "before writing any" split across a wrap; worker kept the phrase contiguous and flagged the trap) | WATCHED | authoring rule of thumb: keep a test-needled phrase on one source line, or collapse whitespace before the assert (the traceability tests' `re.sub` idiom) |
| push-gate prover-freshness test red mid-batch: `test_real_repo_passes` fails while a SPEC-touching commit sits ahead of the newest committed prover record | 2026-07-06 ~12:50 (session 13 — row-123 worker's full-suite run; the worker isolated it to bare HEAD and HALTed by contract), 2026-07-12 ~00:02 (session 36 — the midnight rollover flavour: the row-222 worker's baseline run went red the minute the date changed, no record dated the NEW day existed yet; resolved by the landing's own same-commit record) | WATCHED, recommended AGREED NON-PROBLEM — the ask rides the morning report | by design, not a defect: the test IS the prover-before-push tooth [M-6]; a batch of landings closes with the full prover pass + record commit, which turns it green before any push; recommendation if it fires again: his dated AGREED NON-PROBLEM |
| GitHub Actions annotation: Node.js 20 deprecated, actions/checkout@v4 + actions/setup-python@v5 forced onto Node 24 on the runners | 2026-07-11 23:10 (session 36 — the row-248 push's green CI run, annotation only, nothing failed) | WATCHED | fix when it bites or at the next workflow touch: bump the two actions to their Node-24 releases in .github/workflows/gates.yml |

## ARCHIVED (moved here at each milestone compaction, dated)

| Signature | Occurrences (dates) | Status | Notes |
|---|---|---|---|
