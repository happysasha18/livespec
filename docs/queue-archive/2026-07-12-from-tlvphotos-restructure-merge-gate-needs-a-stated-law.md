> Harvested into ROADMAP row 258 (2026-07-12).

# Wish: the pack owes a stated law for restructure/migration MERGE gates

**The item.** When a document restructure (a plain-language rewrite, a big reflow) is gated for
merging back into main, the pack today states no law for what the equivalence proof consists of or
how prover findings route. Each session invents its own bar. Tonight (2026-07-12, tlvphotos) that
gap produced a wrong bar: the orchestrator over-sharpened the owner's spoken «prover finds nothing
both sides» into «any finding parks the merge», and parked a strictly-improving merge on the OLD
side's pre-existing clarity debts. The owner corrected it live (~01:28): the gate judges the DELTA.

**The law proposed.** A restructure merge gate has three parts: (1) load-bearing token identity
old-vs-new modulo the per-chunk NAMED deliberate deltas, plus the punctuation-multiset check;
(2) the full suite green on the merged tree; (3) a full prover pass on both sides where the BLOCKING
set is delta-scoped — an unmatched token, a red suite, a new-side finding absent on the old side, an
unnamed meaning change. Pre-existing findings equal on both sides route to queue rows in the same
landing and never block. A session that sharpens a human's spoken bar beyond his words must say the
sharpened form back AND mark it as its own interpretation.

**Who throws it.** The tlvphotos Mac window, 2026-07-12 ~01:28, on Alexander's explicit word
(«надо переписать правила … пойми откуда пришло»).
