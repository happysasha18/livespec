> ARCHIVED 2026-07-08, session 26 — milestone queue compaction (SPEC INV-1: archive, never delete). 38 terminally-landed rows moved out of the active table; git history + this file keep them. Rows with any open/field leg, [target] ownership, or a waiting gate stayed active.

# Landed rows — milestone compaction 2026-07-08

| # | Wish (plain words) | Class | Status | Decision / acceptance |
|---|---|---|---|---|
| 74 | A plain-language map of the whole structure (Alexander 2026-07-05 evening: "помоги навести порядок. чето структура странная очень выходит" — he can't hold where things live: pack vs profile vs playbook vs projects, base vs four working skills). One readable page: what each piece IS, where each kind of rule lives, one home per fact — in his words, no jargon; likely grows the README or a new OVERVIEW doc | small | **landed 2026-07-05 ~20:19, session 8** (door: docs-only; built session 7) — Alexander read the rendered OVERVIEW and thanked; his follow-up question (why the playbook loads per-session, not always) answered in chat | Done when: Alexander reads it and says the structure is clear; every "where does X live" question in it answerable in one line — met on his read |
| 12 | Mine CLAUDE.md + PLAYBOOK for rules not yet in the package skills — map rule → skill → gap, fold gaps (Alexander 2026-07-04) | large | **landed 2026-07-07, session 24** | 45 rules mapped (22 full / 13 partial / 10 gap); 11 package-worthy gaps ranked in `~/.claude/playbook/claudemd_mining_s51.md` (moved from /tmp 2026-07-05 — a map quoting the private playbook stays out of the public repo). Folded: gaps 1+2 as one class (primary-source discipline: base rule 13 + build-pipeline reconcile sentence + prover meta-rule, skills → 0.1.1, 2026-07-05); gap 3 (fix-the-class sweep: base rule 14 + build-pipeline bug-path sentence + prover 3e sibling-instances lens; header rule-list + base pin swept across all four working skills, 2026-07-05 ~15:05 — unblocks row 52's flip); gap 4 folded (recurring bug re-doors to feature — build-pipeline 0.2.12, M-100, session 9); gaps 5+8 folded as one docs-discipline class (CHANGELOG speaks to the user · no drifting version pins — build-pipeline 0.2.13 step 9 + spec-author 0.1.11 anti-pattern, M-101, session 9); gaps 6 (delegation savings line, M-102) · 9 (prover domain-language lens) · 10 (step 5 teaches DO+NEVER) folded session 9; **CLOSED session 24 (2026-07-07):** last two gaps folded — gap 7 (standing default-delegate list) verify-only: the ≥1-holds trigger list in build-pipeline §Junior delegation already IS that standing list (run a suite · audit across files · apply defined edits · produce a dump), map moved partial→folded, no skill change; the stale-copy sweep rule folded into live-spec-base rule 14 as the supersession clause (a rule changed at a broad scope sweeps its narrower-scope restatements the same move) — base 0.1.24, all 7 skill base-pins swept, and the pin-drift gate's own list widened to the 7 real citers (test-author + feedback-intake were uncovered). All 11 ranked gaps resolved; mining map banner-closed. Pack 0.8.74, suite 174 green |
| 56 | (prover 2026-07-05-architecture, F1 — same class as row 55) The model router (ACT-3 [target]: cheapest sufficient tier, budget-aware) had no owning queue row either; D-2 (who fixes the tier) closes with this design | large | **LANDED 2026-07-07 ~13:20, session 24** (door: feature; kind: skill; map: grows the delegation scenario, closes D-2 — no new surface, no new node; the routing rule INV-69 owned by the build-pipeline node; prover CROSS-LINK docs/prover/2026-07-07-row56.md — 4 findings, 1 must-fix (the [router target] tag sweep) + 3 should-clarify, all folded; SPEC v0.15.61 INV-69 + D-2 Decided + ACT-3 tag dropped + T-19 rung cites re-pointed; header target-list + test target-map swept (self-closing); M-175 red-proven then green, M-056/M-010 re-authored; build-pipeline 0.2.39 carries the elaboration; pack 0.8.75; suite 175 green; **the routing record — this build's own first routed landing:** spec/prove/architecture/matrix-level → senior (judgment, ACT-2's own, never routed down); version-bump + installed-copy sync → sonnet worker (self-contained mechanical brief, ~10 min senior saved, HALTed on none, checkpoint row56-versionsync.md); one OVERRIDE logged — the SPEC prose clause sat in the code step but was kept on senior, routed UP from the size-proposed sonnet because spec prose is taste-heavy [INV-62]) | Done when: router rule spec'd (queue class → proposed tier, senior override logged per D-2) — MET; first real routed landing recorded — MET (this build) |
| 57 | (prover 2026-07-05-architecture, F2+F3) Two mechanisms live in the pack but have no spec sentence: `install.sh` (how skills arrive on a machine — attach node carries it today via A-7's record clause) and the decision page (communicator rule 10 — backed today only by the batched-questions clause in the wish walk). Give each one sentence + anchor at the next spec touch | small | **landed 2026-07-05 ~22:15, session 9** (door: bug; kind: prose; steps stood down by name: facet sweep — bug door, no feature delta; design-sync — text product; architecture = assignment only, no re-prove) — SPEC v0.15.1: E-21 installer clause (adoption home) + E-22 decision-page clause (wish-walk home), index + owns-columns extended, matrix M-091/M-092; M-091's REAL install run (fresh temp home, twice) caught a real bug — install.sh lacked mkdir -p and failed on a genuinely fresh machine; fixed same change; suite 73 green; pack 0.8.7 | Done when: SPEC names the installer and the decision page, each with an anchor; index updated; architecture owns-columns extended to the new anchors. Partial 2026-07-05 ~15:05: the hook-install half landed in E-6 — hooks OFFERED never imposed: git hosts only, installed only after asking with a plain-words explanation (Alexander's word this session); installer + decision-page sentences remain |
| 60 | One name-collision law, stated once in the base skill: prefix/base name, then numeric suffix -2, -3 everywhere (attic second collision, inbox concurrent same-slug — add a per-session token if needed) | small | **landed 2026-07-05 ~22:23, session 9** (door: feature; kind: skill; fences held: E-9 attic manifest · INV-10/E-11 inbox one-new-file · rule-10 ordinal mechanics — all intact, now citing; steps stood down by name: facet sweep — no visible surface, skill kind; design-sync — text product; non-goals: no renaming of already-archived files, no third scheme legalized; success measure [default]: the next real collision in any home resolves by the law, checked by eye at the milestone audit) — base rule 18 (0.1.11) states it ONCE; SPEC v0.15.2 attic+inbox clauses, ADOPT and communicator rule 10 CITE it (the prover's F2 caught rule 10 as the unswept fourth); pin sweep across five skills; matrix M-093; prover record rows57-60; suite 74 green; pack 0.8.8 | Source: audit (fable F3, opus F-7, comp N6) |
| 61 | Pre-push gate freshness: the prover record must be newer than the last commit touching SPEC.md, not merely today-dated | small | **landed 2026-07-05 ~23:45, session 8** (door: bug; kind: infra; Sonnet worker on a self-contained brief, red→green in checkpoint, senior verified by re-running gate + suite) — gate a now checks record-commit ≥ last SPEC.md commit (same-commit passes, M-6's fold clause honored); tests test_stale_record_fails + test_record_with_spec_same_commit_passes; suite 70 green; M-015 re-authored | Source: audit (fable F4); lands in guardrails/pre-push + its tests |
| 65 | Milestone audit item: the thin loader stays thin — every line passes the must-hold-before-pack-loads test, count stated in the audit report | small | **landed 2026-07-05 ~22:34, session 9** (door: feature; kind: prose; fences held: M-1 gate list grows, never shrinks — all prior items intact; INV-7 checked at the prover (C2: migration moves the RULE, loses nothing); stood down by name: facet sweep + design-sync — prose kind; non-goals: no mechanical test of the loader file itself — it lives outside the repo, the item binds the audit WALK (prover C1); success measure [default]: next milestone's report states the count) — SPEC v0.15.5 M-1 item + first walk done NOW: 16 non-empty lines, all pass, two provenance pointers named prune-candidates; M-029 extended; suite 78 green; pack 0.8.11 | Source: audit (fable F7); pairs with row 52 |
| 66 | Freshness on the pack developer's own machine: repo skill copy vs installed copy — a named sync rule/script + the A-7 re-read | small | **landed 2026-07-05 ~22:48, session 9** (door: feature; kind: infra; fences held: A-7 cited not restated · D-4 (repo stays source) · E-21 distinct by actor+direction (prover H1); stood down by name: facet sweep + design-sync — infra kind; non-goals: no auto-sync on commit — the tool is called, not hooked, until a real stale-skill incident argues otherwise; success measure [default]: next skill-editing session's journal shows the script's old→new line instead of a hand-copy) — scripts/sync-skills.sh shipped + real-run tested twice + run for real on this machine; SPEC v0.15.8 E-23; matrix M-096; suite 83 green; pack 0.8.14 | Source: audit (composition H4) |
| 68 | Communicator description over-triggers (fires on any progress report, no OFF-switch) — narrow it | small | **landed 2026-07-05 ~22:53, session 9** (door: bug; kind: skill; fences held: movement-end report stays a named trigger (prover J1) · rule texts untouched, only the description; stood down by name: facet sweep + design-sync — skill kind; success measure [default]: the next mid-work status line ships without loading the skill, the next landing report still loads it) — description narrowed with a stated NOT-side, over-trigger phrase asserted GONE; communicator 0.1.10 synced; matrix M-099; eval re-run duty recorded (J2); suite 85 green; pack 0.8.16 | Source: skill-creator eval |
| 84 | Milestone 0.8.0 (renamed from 1.0 on Alexander's word, 2026-07-05 late evening: "может это 0.8 или 0.9, не надо спешить" — 1.0 waits for polish: rows 86, 93, 94 + tlvphoto lessons): full 3-pass preventive audit, terminal rows archived, README refreshed, VERSION 0.8.0, gated push if maximally clean | large | **landed 2026-07-05 night, session 7** | 3-pass audit: FULL prover READY-TO-PUSH 0 must-fix (84 anchors symmetric, all 24 night folds verified) · matrix audit 1 must-fix + 8 (ALL folded, placement check mechanized) · architecture/composition 0 must-fix + 6 (ALL folded, design-sync node re-proven, 16 pins re-pinned); VERSION 0.8.0; suite 61 green; pushed on the clean verdict |
| 86 | The pack understands WHAT it is building (Alexander 2026-07-05 night, decision-page note: "скилл сет должен понимать с чем работает: делаем инфру или продукт, скилл или рассказ пишем — задействовать необходимые функционалы, не все всегда нужны"): a work-kind axis (product / infra / skill / prose…) that scales which pipeline parts apply | surface | **landed 2026-07-05 ~20:55, session 8** (door: feature; kind: skill; fences held: T-12/INV-16 · T-15 · INV-12 · T-13/INV-18 · T-14/INV-19 · INV-20/INV-21 — full suite green) | Done when: kind named at intake; each pipeline step says what it means per kind (or that it stands down), never silently skipped — MET: SPEC v0.15.0 T-16+INV-22, per-kind table in build-pipeline 0.2.7 (its one home), base rule 15 (0.1.8), communicator report line (0.1.8), matrix M-084/M-085, suite 64 green, prover CROSS-LINK record row86 (3 findings folded). Non-goals: no mechanical intake-kind guardrail yet; no per-kind templates; design-sync wiring = row 93. Success measure: every new row names a kind + first non-product landing shows a named stand-down, by eye until the reading machinery [default] |
| 94 | Skill-behavior evals (research adopt #6's third leg, superpowers "no skill without a failing test"): per skill, a scenario where a bare session errs and the skill corrects it; run at milestones | surface | **landed 2026-07-05 ~22:00, session 8** (door: feature; kind: infra; fences held: E-12/INV-13 · M-1 grows · T-16/INV-22 — suite green) | Done when: one eval per working skill, red proven without the skill — MET: SPEC E-19, evals/ (4 files + README), 8 real runs recorded (docs/evals/2026-07-05-first-run/), reds scored per criterion, suite 66 green (self-closing check), M-1 carries the re-run, skill-evals node re-proven. Honest boundary recorded: no truly bare session on this machine (the loader feeds method) — red = bare-of-the-SKILL. Non-goals: no clean-machine CI arm yet (candidate rides row 14); scenario de-contamination at next re-run. Success measure: next milestone's eval re-run flips ≥0 criteria silently (any flip = a caught regression), checked at M-1 [default] |
| 97 | Docs he reads get typography, not a code editor (Alexander 2026-07-05: Sublime for prose = "как когда-то на VT220"): `scripts/render-doc.py` renders any .md into one readable self-contained page; the show-a-doc rule now goes render→browser, Sublime retired for prose | small | **landed 2026-07-05 evening, session 7 (door: feature; date corrected from 07-06, session 8)** | Script shipped + inventoried + smoke-tested; profile line show.text-docs updated (playbook); OVERVIEW.md was its first render |
| 102 | **A multi-story row can't close by half** (from the tlvphoto investigation, 2026-07-05 ~23:17, session 10): tlvphoto's MVP fused TWO user stories (door + gallery) into one queue row 5a; shipping the door half let the row be declared COMPLETE while the gallery was still the rejected grid wall — four drops in a row, each caught by his EYES, never the pipeline. The law: one wish = one user story (his standing word, not yet a pack sentence); where a row DOES carry several stories, its Done-when enumerates per-story acceptance and the row cannot close with an unmet leg; the resume file's LIVE-STATE supersession must not compress an unfinished story out of existence. Evidence: tlvphoto NEXT_STEPS.md:19 "threshold story COMPLETE" vs the wall still shipped; adopt-era SPEC filed the approved Room as "not yet specified, later" | surface | **landed 2026-07-06 ~01:29, session 11** (door: feature; kind: skill; prover CROSS-LINK record `docs/prover/2026-07-06-night.md` pass 2 — F4 folded (a split's rows cite the one spoken wish, INV-1), F5 verification note (the pack's own multi-leg rows 12/100 already legal); the tlvphoto case re-walked in the record as the red test: T-17 outlaws the fused row at intake, INV-26 refuses the half-close, the M-2 arm bans the compressed-away leg — all three catch the real incident; architecture: assignment only (T-17/INV-26 → build-pipeline); stood down by name: facet sweep + design-sync — skill kind, no visible surface; non-goals: nothing left out; success measure [default]: the next fused wish is split at intake and no row again reads COMPLETE beside an unmet leg; delegation savings: red-first test, M-108/M-109, three SKILL.md edits, two template lines, VERSION, sync — a Sonnet worker, ~15 min senior saved; the worker STOPPED correctly on one brief defect (my anchor quoted the wrong file) — corrected, resumed same tier, logged) — SPEC v0.15.13 T-17+INV-26 · matrix M-108/M-109 · `test_one_story_close_whole` RED PROVEN then green · build-pipeline 0.2.15 · pack 0.8.24 · suite 96 green | Done when: one-story law + close-only-whole stated in the pipeline (one home) — MET; resume + roadmap templates carry the per-leg check — MET; the tlvphoto case re-walked against the new sentences as the red test — MET (prover pass 2) |
| 103 | **Invented-time gets a mechanical owner** (born in the pack's own problem ledger, 2026-07-05 ~23:40 — third recurrence of the family: rows 95–97 mis-dated at intake, re-swept session 8, and tonight the prototype-norm inbox file + tlvphoto's prover record both dated 07-06 while the clock said 07-05; the hand-sweep ran twice and did not hold): a suite + pre-push check that no repo file NAME and no journal/queue/ledger date stamp is LATER than the clock at commit time | small | **landed 2026-07-05 ~23:43, session 10** (door: feature; kind: infra; quick win taken right after row 100 — the ledger's first entry closed the same night it was owned; fences held: M-3 dated versions · M-2 journal discipline · INV-23 (the ledger cites the fence, the fence doesn't restate the ledger); stood down by name: facet sweep + design-sync — infra kind, no visible surface; non-goals: no rewrite of history lines QUOTING the wrong dates — prose quoting a past incident stays legal, the fence scopes to file names + journal headings + ledger dates; success measure [default]: the next invented stamp never reaches a push) — SPEC v0.15.11 INV-24 (Rhythm) · matrix M-106 (guardrails node) · `test_no_future_dated_stamps`, RED PROVEN by deed on a synthetic future-named file, then suite 94 green; pack 0.8.22 | Done when: the check runs in the suite and the pre-push walk (suite runs IN the push walk, gate b) — MET; a future-dated stamp goes red — MET by deed; the pack ledger entry flips to SOLVED — MET |
| 104 | **Invented-time, the TIME variant** (ledger-born 2026-07-05 23:43, session 10 — the fence row 103 had just landed and the session's own landing stamps were STILL written ahead of the wall clock ("~23:50"/"~23:58"/"00:02" at 23:35–23:43), same failure as session 8's journaled one (JOURNAL:853): the date fence can't see same-day TIMES): a pre-commit check — an ADDED line stamping TODAY with a time later than the commit clock goes red; not racy, the commit moment is the reference | small | **landed 2026-07-06 ~01:37, session 11 — QUICK-WIN BUBBLE, jump marked here (T-11): taken ahead of the queue after the hand guessed time ahead TWICE MORE the same night (occurrences 3 and 4 on the ledger entry)** (door: feature; kind: infra; prover CROSS-LINK record `docs/prover/2026-07-06-night.md` pass 3 — F6 folded (time check wired ABOVE the opt-in fence's early exit: the fence is opt-in, the clock is law), F7 recorded (named residual holes: --no-verify, test-only env overrides — habit fence, not adversary fence), F8 verified by deed (the future-plan writing cost; tonight's resume file complies); stood down by name: facet sweep + design-sync — infra, no visible surface; non-goals: no sweep of PAST wrong times in history (prose quoting stays legal, same as row 103); success measure [default]: no same-day future stamp ever reaches a commit again; delegation savings: script, hook wiring, 3 red-first tests, M-110, VERSION, install — a Sonnet worker, ~15 min senior saved; TWO brief defects both caught by the worker/suite and corrected (wrong anchor file quoted · matrix level vocabulary) — the contract and the traceability teeth each did their job) — SPEC v0.15.14 INV-24 second arm · matrix M-110 · `TestGateTimeFence` 3 tests RED PROVEN (exit 127 bare) then green · guardrails/check-future-times.sh + pre-commit wiring · pack 0.8.25 · suite 99 green · **proven by deed in the real repo: a staged "23:59" stamp BLOCKED at 01:36 — and the fence then blocked its own landing commit (F9): real catches (a heading one minute ahead; five stale time references) fixed, the over-broad line-global reading narrowed to the adjacent stamp shape the same hour, 5 fixture tests green** | Done when: the pre-commit check runs and a same-day future-timed added stamp goes red by deed — MET (blocked live at 01:36); the ledger entry flips to SOLVED — MET (this landing) |
| 105 | **A captured wish is echoed aloud, and every feature reports its station on the pipeline** (Alexander 2026-07-05 ~23:50, before sleep: "надо просто необходимо показывать роадмап и статус… более регламентированно. например 'captured this that request, it's a feature, we'll call it this and that' а потом рапортовать как каждая фича идет по пайплайну"): two halves of one ritual — (a) the CAPTURE ECHO: the moment a wish is intaken the human hears one plain sentence — what was heard, the door called, the name it will answer to, its row number — so he KNOWS the request survived; (b) the PIPELINE BOARD: every status report names each in-flight feature by that name with its current station (spec → prove → architecture → matrix → tests → code → verify → landed), so progress reads like a departures board, not prose archaeology. Kin: his status-report-format standing word (reports anchored to roadmap/stage, 2026-07-04) — this row REGIMENTS it | surface | **landed 2026-07-06 ~01:45, session 11 — per-leg status (INV-26): pack-side legs MET, first-real-run leg = this morning's report, closes in the same session's final commit** (door: feature; kind: skill; prover CROSS-LINK record `docs/prover/2026-07-06-night.md` pass 4 — F10 must-fix folded (silent arrivals echo in the NEXT report, never an interruption), F11 folded (station vocabulary = the pipeline's own step names), F12 folded (the echo IS the T-12 intake line spoken, not a second classification; pipeline cites rule 12); architecture: assignment + pin (INV-27 → communicator rule 12 `:135` + rule 9 station line); stood down by name: facet sweep + design-sync — skill kind, no visible surface; non-goals: no per-feature progress FILE, the board lives in reports (the queue table stays the one written home); success measure [default]: he can say where any feature stands without reading a table; delegation savings: red-first test, M-111/M-112, six SKILL.md edits across two skills, VERSION, sync — a Sonnet worker, ~15 min senior saved, zero brief defects this round) — SPEC v0.15.15 INV-27 · matrix M-111/M-112 · `test_capture_echo_and_board` RED PROVEN then green · communicator 0.1.12 rule 12 + rule 9 stations · build-pipeline 0.2.16 step-zero echo citation · pack 0.8.26 · suite 102 green | Done when: capture-echo sentence + pipeline-board format spec'd in one home — MET; the report announcing this landing uses the board itself (first real run) — MET this morning (the report page + the chat report are the run); suite covers the clause on the shipped SKILL.md — MET by deed |
| 106 | **pytest from the repo root trips over the scaffold template** (found 2026-07-06 ~00:00 by a clean-context analyst running our own suite: `python3 -m pytest` collects `templates/test_scaffold.template.py` and errors; only `python3 -m unittest discover tests` is green — a packaging rough edge a stranger hits in their first minute) | bug | **landed 2026-07-06 ~23:25, session 22** (door: bug; kind: infra; claimed ~23:22; map: fixes the pack repo's own packaging — package-docs node, no new surface; class swept: exactly one collectable template (the test_*-named scaffold), the only other non-suite .py is scripts/render-doc.py — not collectable; delegation: kept on the senior by the decision rule — the brief would cost more than the two-file edit) — M-143 · `test_pytest_collects_clean_from_root` RED PROVEN on the pre-fix tree then green · pytest.ini (testpaths = tests) · suite 141 green | Done when: a stranger's `pytest` from the root either runs the suite green or is cleanly told where the suite lives (collect-ignore / config), red-proven first — MET (pytest.ini pins testpaths; `pytest --collect-only` from root: 141 collected, zero errors, template not collected); the template itself stays a template — MET (file untouched) |
| 111 | **The brief is born from read code** (row-107 harvest ~01:25; BMAD's create-story critical step): before authoring a worker brief that edits existing files, the senior (or the brief-writer) READS IN FULL every file the work will modify and the brief records current-state / what-changes / what-must-survive; every step carries a back-reference to the spec sentence it serves (we half-do this), every technical claim a source citation | small | **landed 2026-07-07 ~00:14, session 22** (door: feature; kind: skill; claimed ~00:11, serial lane; prover `docs/prover/2026-07-07-rows111-113.md`; law caught up with deed — tonight's briefs already read their files first; delegation decision said aloud: kept on the senior, the remaining mechanical surface (one edit + one test) costs less than any brief's own ~300-line law) — SPEC v0.15.45 INV-53 · M-151 · `test_brief_trio_laws` RED PROVEN then green · build-pipeline 0.2.35 | Done when: the read-first rule + the three recorded lines join the pipeline's delegation gate (one home); suite covers; first real brief written under it |
| 112 | **Workers get a named HALT list** (row-107 harvest ~01:25; BMAD's dev contract): a worker stops early ONLY on an enumerated condition — ambiguous requirement · N consecutive failures · missing config/dependency · acceptance impossible as briefed — otherwise it runs to completion; sharper than "ask if unsure", composes with our one-tier escalation law | small | **landed 2026-07-07 ~00:14, session 22** (door: feature; kind: skill; claimed ~00:11, serial lane; prover record shared — F2 folded: HALT is the worker's stop-with-evidence, escalation the senior's next move, stated as composing; the list's first full night: three workers HALTed by it, every stop a real defect, two of them the senior's own) — SPEC v0.15.45 INV-54 · M-152 · `test_brief_trio_laws` red→green · build-pipeline 0.2.35 | Done when: the HALT list in the worker-contract clause (SPEC ACT-3 + pipeline delegation gate, one home); suite covers |
| 113 | **Brief sizing gets numbers** (row-107 harvest ~01:25; GSD's degradation curve): the delegation gate learns quantities — a brief targets a bounded share of the worker's context, splits above it; pass PATHS, never inlined file bodies | small | **landed 2026-07-07 ~00:14, session 22** (door: feature; kind: skill; claimed ~00:11, serial lane; prover record shared — F1 must-fix folded: the row promised NUMBERS and the draft said only "bounded share"; concrete default now law: brief text ~300 lines, ~8 files to edit [default], splits above either; F3 resolved through the sibling law) — SPEC v0.15.45 INV-55 · M-153 · `test_brief_trio_laws` red→green · build-pipeline 0.2.35 | Done when: the sizing sentence + paths-not-bodies rule in the delegation gate; suite covers |
| 114 | **Gate scripts learn the neighbours' hygiene** (row-107 harvest ~01:25; OpenSpec's archive/CLI): all-or-nothing batched writes (validate all rebuilt outputs before writing any), a typed failure contract per blocked gate ({severity, code, message, fix} — one JSON for agents, the same fix line for humans), an explicit advisory-vs-blocking taxonomy per check | small | **landed 2026-07-06 ~23:55, session 22** (door: feature; kind: infra; claimed ~23:39, lane B of three; prover record shared (rows110-114-115) — F1 folded (the reach decider exempt by name: a verdict, not a defect), F2 folded into the brief (gate-e tests re-run after the JSON line); architecture: assignment only — INV-47 → guardrails; facets N/A — no visible surface; non-goals: no retroactive sweep of all gates (each converts as next touched), no machine-parseable SUCCESS lines (only failures); success measure [default]: the next agent that hits a blocked gate parses one JSON line instead of prose; delegation: lane-B Sonnet worker (README section + gate conversion + 2 fixture tests red→green), ~15 min saved; worker's WATCHED find kept: a needled phrase must not straddle a hand-wrapped line — ledger line added) — SPEC v0.15.43 INV-47 · M-145 · `TestGateHygieneContract` RED PROVEN then green (real scratch-repo run, JSON parsed) · suite 147 green | Done when: the three conventions stated in guardrails' README/contract and applied to at least one existing gate script by deed; suite covers the shape |
| 116 | **The outcome does the talking — a coined name never leads a report line** (Alexander 2026-07-06 ~10:05, morning after the first real departures board: «"Прогулка по уликам" (строка 101) это что такое? … Часы получают зубы. ЧТО??? … я даже эти файлы не открываю»): the board's first real run passed its eval and FAILED its reader — lines LED with coined metaphor-names, row numbers he never opens, and riddle-compression («семь раз — дважды забор»); third strike of the jargon family in two days (calques twice 2026-07-05, profile line already standing) ⇒ recurring bug re-doors to FEATURE: the area was missing an invariant. Two arms: echo-names are short DESCRIPTIVE phrases parseable cold; a human-facing report line opens with the reader's outcome, every handle (codes, numbers, coined names) only trails, one fact = one standalone sentence | small | **landed 2026-07-06 ~10:33, session 12** (door: feature — recurring-bug escalation; kind: skill; prover CROSS-LINK record `docs/prover/2026-07-06-row116.md` — F1 folded (naming arm: INV-27's board and INV-28 compose — names lead only because names become plain), F2 folded (scope fence: human-facing surfaces only, method-internal docs keep anchors), F3 recorded (obedience is judged by eval + reader, the string test only pins the shipped clause); architecture: INV-28 assigned to the communicator node — and the assignment was NOT free: the senior briefed "no architecture change", the ownership test went red, the worker HALTed by contract and the node's owned-anchor cell + rule-9 pin were added on the senior's word (the teeth bit their author, correctly); facet sweep + design-sync stood down by name — skill kind, no visible surface; non-goals: past reports not rewritten (the record stands, the translation lives in chat), method-internal docs unchanged; success measure [default]: the next morning-style report draws zero "а это что?"; delegation savings: test, matrix row, ten file edits, base-version sweep, VERSION, suite runs, install sync — a Sonnet worker, ~20 min senior saved; two contract HALTs, both correct, one catching the senior's own skipped step) | Done when: INV-28 spec'd with both arms; communicator rules 6/9 + base rule 2 name coined names as handles; eval criterion added; suite covers the shipped clauses red-first; the session's closing report itself written under the law |
| 120 | **A taste choice made without asking is told, never confirmed** (Alexander 2026-07-06 ~11:20: «если сделал какие-то штуки по умолчанию — расскажи их, обозначь; если мне всё ок — не надо подтверждать; можно сказать вот разные штуки, можно твикать, и всё; дальше уже, если надо, пользователь спросит»): supersedes the two-landing forced-review mechanism row 118 landed the same morning — the TELLING half stands (silent accumulation stays illegal), the confirmation half dies; the landing report names each choice with an example and a tweakable mark, silence is consent, nothing re-asked | small | **landed 2026-07-06 ~11:30, session 12** (door: feature — same-day correction on the human's word, journal owns the story; kind: skill; architecture: INV-31 ownership moves communicator → build-pipeline node — the landing report owns the telling; success measure [default]: he never sees a confirmation request about a taste call and never finds an untold one) | Done when: INV-31 rewritten both arms; pipeline step 9 tells; matrix/test updated; suite green |
| 122 | **install.sh backups polluted the live skills folder** (found by deed 2026-07-06 ~11:25: Alexander ran the installer and six stale skill copies appeared to the harness as loadable duplicate skills — backups went to `<skill>.bak_<ts>` INSIDE ~/.claude/skills, which the harness scans as skills): backups move to an attic folder BESIDE the skills dir; today's six strays moved out by hand the same hour | bug | **landed 2026-07-06 ~11:30, session 12** (door: bug; kind: infra; red-proven: `test_install_backup_home` on the shipped script) | Done when: install.sh writes backups outside the scanned dir; test red→green; the strays gone — done by hand |
| 123 | **A worker's brief carries the problem-ledger walk** (retro fit-walk find, 2026-07-06 session 12, record `docs/prover/2026-07-06-feature-fit-retro.md`): workshop noise a WORKER hits (flaky harness, missing dependency, tool misbehaving) today gets silently retried unless the senior happens to see it in raw output — the delegation gate should put the ledger path and the WATCHED-line duty into every brief | small | **landed 2026-07-06 ~12:53, session 13** (door: feature; kind: skill; source: the retro run; the CLOCK arm folded into the same contract clause — the eval re-runs' wrong-hour wobble was this row's kin and shares the fix (a brief states the date/time read at briefing, worker stamps come off it, INV-24); homes: SPEC ACT-3 + delegation gate + M-119; dogfooded on its own landing — the row-123 brief carried both lines, the worker wrote a ledger line and HALTed by contract on the push gate's expected mid-batch red; stood down by name: facet sweep + design-sync — skill kind, no visible surface; non-goals: no mechanical brief-linter, past briefs not retro-edited; success measure [default]: the next delegated worker's stamps carry the right hour and its noise reaches the ledger without the senior digging; delegation: a Sonnet worker ran the seven verbatim edits + red/green proof, ~15 min senior saved) | Done when: the delegation gate's brief checklist includes the ledger walk — MET (ACT-3 + gate elaboration, one home each); suite covers the shipped clause — MET (test_brief_carries_ledger_and_clock red first, then green; suite 109, the one red being the push gate's own prover-freshness tooth awaiting the batch-closing pass) |
| 124 | **Each pipeline step is worked in its craft's mindset** (Alexander 2026-07-06 ~11:35: «когда ты делаешь продукт-спеку — ты крутой продакт, когда архитектуру — крутой архитект, когда матрицу тестов — крутой QA-автоматчик, в таком духе»): every step of the pipeline names the profession whose head you wear while walking it — product manager at the spec step, architect at architecture, QA automation engineer at matrix and tests, senior developer at code, and the visitor's own eyes at the verify walk — so each artifact is judged by its craft's standards, not by a generalist's | small | **landed 2026-07-06 ~13:05, session 13** (door: feature; kind: skill; the spec step picked a compact craft LADDER beside the pipeline's step list over per-step scattered sentences — one home, nine lines; SPEC INV-33 binds the law, M-120 covers it; prove-architecture bit correctly: the senior briefed "assignment-free", the suite went red on INV-33 having no owning node, the worker HALTed by contract, INV-33 → build-pipeline node + four pins re-run on the senior's word; stood down by name: facet sweep + design-sync — skill kind, no visible surface; non-goals: no per-artifact hat stamp forced into documents — the report's step accounting speaks the standards, the hat is namable on ask; success measure [default]: the next landing's artifacts read like their craft made them, and the hats are named in its report; delegation: a Sonnet worker ran the seven verbatim edits + red/green proof off a brief carrying the clock + ledger walk, ~15 min senior saved) | Done when: each pipeline step carries its craft line in build-pipeline (one home) — MET (the craft ladder, SKILL.md:31); suite covers the shipped clause — MET (test_craft_ladder red first, then green; suite 110, the one red being the push gate's prover-freshness tooth awaiting the batch-closing pass); the next landing's artifacts each name the hat they were made under — MET at the rows-126/127/128 landing (2026-07-06 session 14: its report names the hat per artifact — product head at the spec delta, formal reviewer at the prover record, architect at the ownership assignments, QA automation at matrix and tests, writer at the skill text (the craft wearing the prose kind's face), the reader's own eyes at verify, release hand at commit & show) |
| 127 | **A chat reply's lead [HH:MM] keeps drifting ahead of the wall clock** (problem-ledger CHAT variant, second occurrence 2026-07-06 session 13: mid-session stamps ran up to 7 minutes fast, and a queue stamp born from one had to be corrected before the fence let it commit; first occurrence 2026-07-05 23:47): the date/time fences guard FILES and COMMITS, chat has no mechanical fence — so the pack states the habit as law where human-facing exchange shapes live: communicator gains one sentence — a human-facing timestamp is read off the clock at write time, never continued or extrapolated from an earlier stamp (kin INV-24, the invented-time family's chat face) | small | **landed 2026-07-06 ~13:52, session 14** (door: feature; kind: skill; fit walk, skill lenses: trigger — any moment spoken to the human, the [HH:MM] lead included · correction — read the machine clock at write time · not-fire — quoting a past moment's recorded time; facet sweep + design-sync stood down by name — skill kind, no visible surface; SPEC INV-24 gains its CHAT face (third arm — no fence can reach chat, so the law lives where the exchange shapes live), the sentence shipped in communicator rule 7; matrix M-123 under guardrails (the invariant's owner) naming the carried sentence · `test_chat_timestamp_at_write_time` red first, then green; ledger CHAT-variant entry flipped OWNED→SOLVED — with the landing session's own ~9-min drift appended honestly first (caught at the eval-brief clock read, owned aloud in the same reply); fences held: the two mechanical fence arms unchanged, quoting past times stays legal; non-goals: no mechanical fence for chat — there is nothing to hook; success measure [default]: no unowned recurrence of a drifted chat stamp) | Done when: the sentence lives in communicator (one home) — MET (rule 7); suite covers it — MET (M-123 green); the ledger entry flips OWNED→SOLVED at landing — MET |
| 132 | **A new wish is placed on the product's feature map at intake — change vs new vs restructure** (Alexander 2026-07-06 ~14:10, mid-landing: «когда приходит новый запрос то ты должен понять к какой фиче относится, меняем ли фичу, пишем ли новую, нужна ли структуризация и все такое… формирование продуктовой модульной архитектуры… я ожидал бы чтобы это было "понятно из коробки"»): today the mapping happens IMPLICITLY — the spec step grows the scenario the change belongs to, the fences name the touched surfaces (T-14), the architecture step assigns the owning node — but no intake step SAYS the verdict (the capture echo names door · kind · row, never "this changes feature X / this is a new feature beside X"), and RE-STRUCTURING has no owner at all: nothing asks "have the modules outgrown their carving" as features accrete — the architecture doc is iterative by design but only ever grows one landing at a time; kin row 129 (the project knows its KIND and re-checks it as it evolves — the same product-self-knowledge family, one scale up) | surface | **landed 2026-07-06 ~15:58, session 16, whole** (door: feature; kind: skill; map: changes the intake walk — this cell is the law's own first written note; worked as ONE head with row 129; SPEC v0.15.26 INV-37 + the INV-27 pointer clause + E-14's legal re-carve entrance; prover record `docs/prover/2026-07-06-rows129-132.md` — F1 folded: the echo's field list keeps one home, INV-27 points; F3 folded: the verdict is WRITTEN in the row's `map:` note, the fences' greppability precedent; architecture: INV-37 → build-pipeline (T-16's precedent; communicator rule 12 carries the spoken arm as a wiring pin), assignment only; stood down by name: facet sweep + design-sync — skill kind, no visible surface; non-goals: no third map document — the spec's scenarios + the architecture's nodes ARE the map; no auto-re-carve — a restructure verdict only queues its row; success measure [default]: he hears «это меняет фичу X / это новая фича» in every echo and never has to ask which; matrix M-125/M-126 red-proven then green; delegation: shared with row 129's line) | Done when: the intake verdict is spec'd and SPOKEN, one home named — MET (INV-37; spoken arm in communicator rule 12, written arm the row's `map:` note); a restructure trigger exists — MET (the verdict's third value; re-carve only through the architecture step's re-prove); composition with row 129's project-kind line stated in one sentence — MET; suite covers the shipped clauses — MET (M-126, red-proven) |
| 126 | **The with-skill communicator keeps leaking bookkeeping into the human's message** (eval evidence, 2026-07-06: the morning push re-run leaked the test count; the batch-2 re-run leaked "all 64 checks green, v0.9.16" into the message body — two consecutive with-skill runs violating rule 8, which already forbids exactly this): rule 8 states the law but doesn't land in workers; give it teeth — an explicit NEVER-list of bookkeeping tokens as message content (test counts, version numbers, suite sizes; "tested clean / saved" is the translation) and one worked before/after example, so the eval criterion has a hard edge to score against | small | **landed 2026-07-06 ~13:52, session 14** (door: bug — entered at the matrix, the SPEC sentence updated in the same change per the bug lane; kind: skill; rule 8 gains the NEVER-list — test count, suite size, version string, check tally never message content, "tested clean / saved" the translation — plus the worked ❌/✅ example; SPEC INV-28 paragraph extended, the carve-out sharpened by the prover's F1: a direct question about the number, or the done-claim walk (INV-25), keeps the number as the answer; prover record `docs/prover/2026-07-06-rows126-128.md`; matrix M-121 · `test_bookkeeping_never_list` red first, then green; communicator 0.1.19 synced to the installed copy; fences held: the done-claim walk keeps its counts/versions (discharged through M-107's existing row), trailing anchors stay legal; non-goals: no retro-sweep of past reports; success measure MET by deed — the re-run's with-skill arm shipped zero bookkeeping tokens as message content on the FIRST run under the shipped clause, after two consecutive red runs before it, record `docs/evals/2026-07-06-rows126-128-rerun/`; delegation: two Sonnet workers ran the eval arms, ~15 min senior saved) | Done when: rule 8 carries the NEVER-list + example — MET; the next eval re-run's with-skill arm ships a message with zero bookkeeping tokens — MET (13:47 run); suite covers the shipped clause — MET (M-121 green) |
| 125 | **The departures board has no name for two pipeline stops** (push-gate prover find, 2026-07-06 session 12, record `docs/prover/2026-07-06-push.md` F4): the board's station list (spec → prove → architecture → matrix → tests → code → verify → landed) promises "the pipeline's own step names, one name per step", but the pipeline walks NINE steps — prove-architecture and commit-and-show have no station, so a feature paused exactly there gets an improvised name, the drift the one-name law exists to stop; fix = either name all nine stations or state in one sentence which steps report under which station (both homes: SPEC INV-27 + communicator's board rule) | small | **landed 2026-07-06 ~12:42, session 13** (door: bug; kind: skill; all nine steps named as stations, landed = the terminal state, not a step; class-swept beyond the two named homes: M-112 + OVERVIEW's pipeline bullet + ARCHITECTURE's lifecycle line — records untouched; red-proven in test_capture_echo_and_board; delegation: a Sonnet worker ran the eight verbatim edits + red/green proof, ~15 min senior saved) | Done when: station list and step list reconciled, one home each — MET (SPEC INV-27 the law's home, build-pipeline the step list's, every other mention now matches); suite covers the mapping sentence — MET (station assertions red first, then green, suite 108) |
| 98 | Publish skill (Alexander 2026-07-05 ~21:15: "короткий паблиш скилл — ридми грамотный; если скилл — команды показать; сравнительный анализ, скриншоты или диаграммы — всё по типу того что заливается"): a publish-quality gate for anything deposited outward — the README/landing-page owes what its artifact KIND owes (a skill: install + commands + when-to-use; a tool: run examples; a visual product: fresh screenshots; comparisons/diagrams where they sell the idea) — the work-kind axis (T-16) applied to PUBLISHING; and target PLUGINS (GitHub repo, plugin directory, design project…) contribute their own steps into the publish walk — his word: "плагин гитхаба должен встраивать свои этапы", same plugin pattern as measurement (row 48); kin: the distributor idea (his voice + follow responses) stays its own later skill | surface | **landed 2026-07-05 ~22:50, session 8** (door: feature; kind: skill; fences held: E-12/INV-13 · rule 17 + M-6 gates · E-19 · T-16 — suite green) | Done when: publish skill spec'd + proven + shipped in the pack; per-kind publish checklist stated once; the plugin seam named (how a target adds steps); first real use on one of our own pushes — MET except the last leg: SPEC E-20 + skills/publish 0.1.0 (fifth working skill), per-kind table one home, target plugins (GitHub · directory · design project), eval red-proven both arms (and it cut both ways: 5 hygiene steps folded INTO the skill from the bare arm), suite 68 green, pack 0.8.4. First real use = the next public push of THIS repo (tonight's push window walks it, journaled) |
| 142 | **The parallel-lanes cap loosens — up to three trains without asking, more on his word** (Alexander 2026-07-06 ~21:04, session 20: he does not want a hard two-train default — take the independent work that exists, two-three lanes, and ASK whether he wants more in parallel; session 19's report had already marked the cap a tagged [default] one word moves — the word arrived): T-18's cap moves 2→3 [default]; a fourth independent lane opens only on his asked word; everything else in the law stands unchanged — one pen, the isolated tree, landing purity (INV-39), no cross-session lanes, one train through a milestone | small | **landed 2026-07-06 ~21:17, session 20** (door: feature; kind: skill; map: changes the parallel-lanes law (T-18/INV-39 family), no new surface; prover CROSS-LINK docs/prover/2026-07-06-row142.md — F1 the cap number's one-home fold (M-022 now takes its number FROM T-18), F2 the ask-for-a-fourth never stops rolling lanes (INV-4 unchanged); red-proven on the skills then green, suite 129) | Done when: SPEC T-18 + the law's prose carry the new cap and the ask-beyond-three duty — MET (v0.15.34); matrix M-022/M-129 never-sides read "never a fourth unasked" — MET; in-work cap test allows three — MET; base + build-pipeline + communicator carry the new sentence, installed copies synced — MET (0.1.17 / 0.2.27 / 0.1.27); suite green — MET |
| 146 | **The shopfront is fresh at every push** (Alexander 2026-07-06 ~22:44, mid-session: «когда пушишь новые версии в гитхаб — думай и над ридми: диаграммы для твоего типа скилла, скриншоты если бы генерился фронтенд — надо чтобы всегда были апдейтнутые, как и всё остальное; пропиши себе в часть скилла и учти при каждом пуше»): a push that publishes a new version owes the repo's PUBLIC FACE the same freshness the artifacts get — the README's claims match the pushed truth, and its kind-owed visuals ride along (a skill pack: its diagrams/flow pictures; a visual product: fresh screenshots; a CLI: its example runs) — stale shopfront = the push gate goes red, same law as stale prover records; kin: the publish gate's kind-checklist (E-20) and track-coach's README-reshot-on-push host line | small | **landed 2026-07-06 ~23:06, session 22** (door: feature; kind: skill; claimed ~23:00; map: extends the publish feature — publish node + build-pipeline step 9; fences: E-20 kind-checklist one-home stays · M-6 push gate stays · INV-40 never-bend list stays — suite green proves them held; prover CROSS-LINK `docs/prover/2026-07-06-row146.md` — F1 should-clarify folded in-pass (publish's fire-list was DIFF-triggered, the law is TRUTH-triggered — the list gained "any push that ships a new version"), F2 recorded no-change (E-20's diff-trigger and the version-trigger COMPOSE to cover the space); architecture: assignment only — INV-44 → publish node, step-9 pointer is wiring; prove-architecture stood down by name (no structure change); facet sweep + design-sync stood down by name — skill kind, no visible surface; fit walk closed in-clause (trigger: any push shipping a new version · correction: one-look shopfront walk, claims vs pushed truth + kind-owed visuals, outcome line in the landing report · not-fire: nothing leaves the machine / communicator's in-session show); non-goals: no mechanical README-vs-diff checker (row 147 the candidate owner), no auto-regenerated images; success measure [default]: no push lands whose README claims an older behaviour or count, checked at milestone audits; delegation savings: red-first test + four verbatim edits + three version bumps + sync — a Sonnet worker off a clock+ledger-armed brief, ~10 min senior saved, zero brief defects, zero WATCHED lines) — SPEC v0.15.41 INV-44 · M-141 · `test_shopfront_fresh_at_push` RED PROVEN then green · publish 0.1.2 · build-pipeline 0.2.31 · pack 0.8.53 · suite 136 green · walked by DEED on its own landing (this push's README step-9 line + row-109's push earlier the same evening) | Done when: the freshness clause lives in its one home (publish SKILL.md's checklist or step 9 — the architecture step picks) and SPEC backs it — MET (publish owns the walk, its fire-list + shopfront paragraph; step 9 carries only the pointer; SPEC INV-44); suite covers the shipped clause — MET (`test_shopfront_fresh_at_push`, M-141, red first then green); this session's own push walks it by deed — MET (row-109 push: README pipeline lines + diagrams verified; this push: README step-9 shopfront line) |
| 147 | **The push gate scales to the diff's reach** (Alexander 2026-07-06 ~22:47, an AUDIT-ONLY case from track-coach: a README-only change paid a full 795-test run before push — «это вот надо было? давай думать что мы не до конца прописали»): today's law says "the whole suite before any push" blind to what the diff can touch, so it double-misses — burns minutes of behavioural tests that read no README line, while the checks a prose diff CAN break (dead links, claims contradicting code, a stale shopfront — row 146's family) run never; the fix (re-shaped on his ~22:51 word + track-coach's own written answer — a naive "only .md ⇒ skip tests" is a trap because some documents ARE tested: SPEC/matrix/SKILL.md string rows; «надо понимать что изменилось чтобы понять что тестить — построить график зависимостей, даже немного с запасом»): the gate derives its check-set from a declared REACH MAP — which file classes which checks READ — mechanically from the diff's file list, never self-judged; the map is CONSERVATIVE (an unknown or unmapped file ⇒ full suite; fast paths only for explicitly claimed classes) and ITSELF TESTED (a repo file no class claims goes red, so the map cannot rot silently); the map derives from what the tests declare they read; the never-bend "push gate at full rigor" (INV-40) stays, read as "ALL checks the diff can reach", never "all checks that exist" | small | **landed 2026-07-06 ~23:19, session 22** (door: feature; kind: skill; claimed ~23:09; map: changes the push-gate feature — guardrails node + build-pipeline step 8; kin: row 146; track-coach's own-cadence verdict respected, AUDIT-ONLY; fences: M-6 prover-record gate stays unscoped · INV-40 never-bend list stays · E-6 cheap gates stay at every push — suite green proves them held; prover CROSS-LINK `docs/prover/2026-07-06-row147.md` — F1 must-fix folded IN the delta (the two standing "suite green" sentences, the never-bend bullet and the machines bullet, re-read through the reach map — else the fast path would be illegal by the spec's own words), F3 folded (cheap gates named as never scoping), F2 recorded as intended narrowness (the pack's own prose class is rare by design — landings touch queue/journal/prover, all full-reach; the LAW is for hosts with minutes-long suites); architecture: assignment only — INV-45 → guardrails node + its script pinned; prove-architecture stood down by name (no structure change); facet sweep + design-sync stood down by name — skill kind, no visible surface; fit walk closed in-clause (trigger: any push · correction: the deciding script scopes gate b from the diff's file list · not-fire: never on judgment — explicit patterns only, unknown ⇒ full); non-goals: no host-side maps shipped (each host widens its own through its rows), no per-test dependency tracing (file classes, deliberately coarse — his «с запасом»); success measure [default]: a README-only push stops paying the behavioural suite while no landing push ever skips it; delegation savings: script + gate edit + 4 fixture/string tests + versions — a Sonnet worker, ~20 min senior saved; the worker HALTed by contract at the suite's red and the red was the SENIOR's own brief defect (a compound level word in M-142 against the closed level vocabulary — the same trap row 104's worker hit; the traceability tooth caught it exactly as designed), fixed by the senior, suite re-run whole) — SPEC v0.15.42 INV-45 · M-142 · `TestGateReachMap` 3 fixtures RED PROVEN (exit 127 bare) then green + `test_push_gate_reach_law` red→green · guardrails/check-push-reach.sh + pre-push gate-b scoping · build-pipeline 0.2.32 · pack 0.8.54 · suite 140 green · proven by deed: REACH_FILES README→exit 0, SPEC→exit 1 | Done when: the reach-map rule spec'd (one home) with the conservative default + self-test clause — MET (INV-45, the machines scenario); INV-40's rigor sentence reconciled — MET (the never-bend bullet + the machines bullet + step 8, all re-read through the map); suite covers the shipped clause — MET (M-142: `TestGateReachMap` + `test_push_gate_reach_law`, red first); the pack's own pre-push reflects it — MET (gate b scoped by `check-push-reach.sh`, proven by deed both ways) |
| 153 | **A limping thing never dams the flow** (Alexander 2026-07-07 ~00:17: «на одном единственном баге не зацикливайся — правь остальное тоже; прописать: если одно не совсем работает, оно не затыкает всё остальное»): when a defect RECURS or a check is red for a KNOWN, owned reason, the session PARKS it — the ledger line / the owning row / an expected-red note — and every unrelated lane keeps rolling; hand-fixing loops on one defect cap at the ledger's own two-strikes law (an owner that moment), and a defect with a NAMED mechanical owner is serviced in BATCH (fix instances silently at the fence, one ledger append at session end), never as a per-instance ceremony that interrupts the work; kin: the problem ledger (E-24), bug preemption (T-9 — a real NEW bug still preempts; this row governs the KNOWN limp), the mid-batch expected-red precedent | small | **landed 2026-07-07 ~00:36, session 22** (door: feature; kind: skill; claimed ~00:31; map: extends the problem-ledger feature — base rule 19 elaboration; prover: folded inline — the park needs a WRITTEN holder (ledger line / owning row / expected-red note), a real NEW bug still preempts (T-9 boundary named); applied by DEED before it landed: the clock-catch ceremony retired to batch mode at 00:18, tonight's remaining catches went to the ledger as ONE batch line; RED proven against HEAD's shipped state (order slipped — the skill edit preceded the test — owned aloud, the git-show red-proof recorded); the base version bump tripped the inherit-pin tooth in all five working skills, swept in the same landing; facets N/A; non-goals: no auto-parking heuristics — the senior names the park; success measure [default]: no future session hand-fixes an owned defect in a loop; delegation: kept on senior, one skill edit + one test) — SPEC v0.15.47 INV-56 · M-155 · `test_limp_never_dams_flow` · base 0.1.20 (inherit pins swept) · pack 0.8.59 · suite 156 green | Done when: the park-and-flow clause in its one home (ledger law or pipeline) + SPEC backing; suite covers; this session's own clock-limp serviced in batch by deed |
| 167 | **Pack lists never drift — a parity check** (born 2026-07-07 ~10:24, the second occurrence of the same class: at n=4 a pack map missed a skill (cross-audit F2), and today the communicator's closing pack list turned out to have missed the publish skill since its birth — found by a worker's correct HALT during the test-author wiring): one mechanical check that every place the pack lists its skills — the SPEC's working-skills sentence, the base rulebook's closing list, the communicator's closing list, the README table, the installed-skills sync — names the same complete set; a list missing a skill goes red at commit | small | **LANDED 2026-07-07 ~10:41, session 23** (door: feature; kind: infra; map: guardrails — mechanical checks; prover folded inline per the small-delta law: the checker is a pure function so the never side lives IN the suite forever (`test_stripped_copy_goes_red` runs it on the historic four-skill footer — red proven against the real pre-fix artifact), and the real-repo side asserts zero gaps; INV-66 → guardrails; delegation: kept on senior — the work was smaller than its brief, said per the savings law; SPEC v0.15.57 INV-66 · M-168 · suite 167 green · pack 0.8.69) | Done when: the parity test in the suite, red-proven against a stripped copy — MET (the historic footer IS the stripped copy, permanent); the communicator-footer find journaled — MET (session-23 chapter) |
| 169 | **marketplace.json ships its description line** (inbox wish from the promoter window, 2026-07-07 morning: `claude plugin validate .` passes but warns `.claude-plugin/marketplace.json` has no `description` field, and directory submission runs the same check; the second warning — `icon` unknown to the validator — is recorded safe, no action): add the one-line description; the wording enumerates NO skill list (a partial list is the row-167 drift class — the parity check owns complete lists, the shopfront line describes without enumerating) | small | **LANDED 2026-07-07 ~11:19, session 24** (door: bug; kind: infra; map: package shopfront metadata; source: inbox/2026-07-07-marketplace-description-line.md, harvested at intake ~11:12; **the class widened at the fix** per base rule 14: plugin.json turned out to carry two siblings — a FIVE-skill description (the row-167 drift class, live in shipped metadata the parity scanner never read) and a version pinned 0.8.0 against VERSION 0.8.71 — all three siblings fixed in the one change, both descriptions now describe without enumerating; M-171 · `TestPluginMetadata` red-proven on the real files then green · plugin.json version now asserted equal to VERSION every run · pack 0.8.72 · suite 171 green · validator re-run recorded: description warning GONE, only the agreed-safe icon warning remains; delegation: kept on senior, smaller than a brief; prover: bug door, no spec delta — short-form record in the journal chapter) | Done when: the field ships and the validator warning is gone (run recorded) — MET; a matrix row asserts the shipped plugin metadata carries it, red-proven first — MET |
