# Queue archive — the 1.0.0 milestone compaction (2026-07-10)

Rows 173–187, all landed (terminal exits), moved here whole at the 1.0.0 release per the
standing milestone note; never edited, never lost (INV-1). The active queue stays in ROADMAP.md.

| # | Wish | Size | Status | Done when |
|---|---|---|---|---|
| 180 | **The architecture doc owes a runtime view and a placement view** (Alexander 2026-07-09 evening, his verdict on the read-only tlvphoto derivation — REOPENS RUN item 5): per product-spec flow, the architecture shows the walk through the nodes — which node serves each step, what data crosses each hop, where the flow can fail; and per node, WHERE it runs (build-time · CDN static · client browser · edge worker · external service) with the load-bearing technology choice, first-class (a table/column, never buried prose); both views scale by `project.kind` (a book has no placement view and says so in one line). The validation re-runs read-only over tlvphoto WITH the two views AND the already-mandated quality budgets + spec-fact ownership table the first run skipped; the real ARCHITECTURE.md there is the bar — every section it needed and the method missed is a named hole, closed or deferred by name. The clean-agent + lint prose gate guards ARCHITECTURE prose the same as PRODUCT_SPEC prose | surface | **LANDED 2026-07-09 ~22:48, session 30** (door: feature; kind: skill; wish: `docs/wishes/2026-07-09-architecture-runtime-placement-views.md`; map: grows the architecture step — INV-74/75 beside INV-41, no new node; SPEC v0.16.4 + the six-check lens in three homes (spec prose, product-prover Phase 0, build-pipeline step 4) + template gains Runtime/Placement/Budgets/Feature-coverage sections + live-spec's own ARCHITECTURE v0.3.0 models all three; prover CROSS-LINK `docs/prover/2026-07-09-row180.md`, F1-F5 folded; M-182..M-185 red-proven (`TestArchitectureViews`); validation re-run over tlvphoto read-only: prose lint 0 errors, diff vs its real LLD leaves NO unnamed hole (one deferred by name: the at-a-glance diagram stays optional); adoption of the two views handed to tlvphoto's window as one inbox wish; versions: product-prover 0.1.15 · build-pipeline 0.2.43 · spec-author 0.1.23 · pack 0.9.6) | Done when: the template + build-pipeline step 3 mandate both views scaled by kind, red-proven — MET; the re-derivation carries views + budgets + fact-ownership — MET; the diff against tlvphoto's real LLD leaves no unnamed hole — MET; tlvphoto untouched (one inbox wish only, the sanctioned channel) — MET |
| 181 | **A background worker outlives a memory wipe — the resume protocol must know it** (tlvphoto window 2026-07-09, inbox wish on Alexander's word): after a /clear or handoff a spawned worker may still be writing the shared tree; `ps` and the harness task list are NOT proof of death (a background agent has no distinct OS process; a prior context's agent is invisible to the new session). The reliable liveness checks: file mtimes/md5 sampled over a few seconds, and a message to the recorded agentId (a live worker replies). The handoff note that records a worker must state HOW to verify liveness and reconnect, and never frame its output as finished while it may run. The fence extends: no second worker onto a shared tree until the first confirms halted by its own reply; prefer halting workers before a wipe so the next session starts single-writer; a handoff says when a worker dies with the window/sleep | surface | **LANDED 2026-07-09 ~22:54, session 30** (door: feature; kind: skill; wish: `docs/wishes/2026-07-09-tlvphoto-worker-liveness-across-clear.md`; evidence: the two-writer race on tlvphoto 2026-07-09, caught by his eye; map: grows the Rhythm/resume scenario — INV-76 beside M-2/INV-48, no new node; SPEC v0.16.5 + base rules 6 (handoff duty) and 7 (prior-context worker = foreign writer until verified) · prover CROSS-LINK `docs/prover/2026-07-09-row181.md`, F1-F3 folded, F4 verified no-change · M-186 red-proven (`test_worker_liveness_protocol`) · defaults told per INV-70: ~30 s file-time watch, ~2 min reply window · base 0.1.26, inherit pins re-synced in all 7 working skills · pack 0.9.7) | Done when: the checkpoint/fence rules + the pipeline's resume walk carry the protocol (detect → verify → reconnect-or-declare-dead → only then spawn), red-proven — MET; the handoff/NEXT_STEPS duty names liveness verification — MET |
| 182 | **The test ladder names its device-fidelity boundary honestly** (tlvphoto week, escaped-bug cluster 1: iOS momentum swipe flew through works; a backgrounded phone throttled a JS failsafe into a black screen — desktop headless Chromium cannot see either): behaviours living in touch physics, scroll snapping, background throttling get a "real-device walk" matrix row the suite CANNOT green, owed before ship — kin of the motion-feel gate (feel is his call, never ship off green) | small | **LANDED 2026-07-09 ~22:58, session 30** (door: feature; kind: skill; wish: `docs/wishes/2026-07-09-test-method-lessons-tlvphoto-week.md` §1; map: grows the test method — INV-77 in SPEC v0.16.6 + test-author's level-ladder boundary paragraph; M-187 red-proven against HEAD, `test_real_device_boundary`) | Done when: test-author's level ladder states the boundary + the real-device row form, red-proven by a string test — MET |
| 183 | **Geometry facts assert viewport-relative, multi-viewport, multi-step** (tlvphoto week, cluster 2: absolute-shift centering drifted further off-center each image; a one-swipe one-viewport assertion passes forever): a centering/positioning fact asserts relative geometry (|center(work) − center(viewport)| ≤ ε), at ≥2 viewport sizes, after N consecutive steps so cumulative drift shows | small | **LANDED 2026-07-09 ~23:01, session 30** (door: feature; kind: skill; wish: same file §2; map: grows the test method — INV-78 in SPEC v0.16.7 + test-author's assertion-form bullet; M-188 red-proven against HEAD, `test_relative_geometry_assertions`) | Done when: test-author states the geometry-assertion form with the three axes (relative · multi-viewport · multi-step), red-proven — MET |
| 184 | **An engine extracted from an instance tests on its OWN generic fixtures** (tlvphoto week, cluster 3: the donor's digits-only id regex rejected the engine's own slug ids — every story call failed; the donor wordmark stayed hardcoded): the engine's suite runs on engine-generic fixtures, never only donor data; every donor constant the extraction finds becomes a named content-contract entry with a test that the engine works without it | small | **LANDED 2026-07-09 ~23:07, session 30** (door: feature; kind: skill; wish: same file §3; map: grows the test method + the authoring method — INV-79 in SPEC v0.16.8, test-author's fixtures bullet + spec-author's content-contract section; M-189 red-proven against HEAD, `test_engine_generic_fixtures`) | Done when: both skills state the extraction law (generic fixtures + content-contract per donor constant), red-proven — MET |
| 185 | **Verify the prover's stateful-surface sweep fires on fresh and adopted specs** (tlvphoto week, cluster 4: finale kept the previous caption, quiz chip lingered, door re-faded — the composition law names exactly this, a prover lesson was already filed, and an 844-line freshly-authored spec still slipped the compositions): enforcement, the law exists — find why the sweep did not fire and pin it | small | **LANDED 2026-07-09 ~23:09, session 30** (door: bug — the method's own gate misfired; kind: skill; wish: same file §4; miss mechanism NAMED from primary sources: the composition lens INV-72 landed 2026-07-09 morning, AFTER the week's specs were proven, and adoption's skip-the-re-prove exemption was lens-blind; fix: ADOPT.md exemption requires the same prover version + every prover record opens naming the version that ran; M-190 red-proven against HEAD, `test_prove_exemption_is_lens_aware`; the sweep itself already rides both prover modes, M-179 pins it) | Done when: the prover's walk names the composition sweep as unconditional for stateful surfaces (fresh or adopted) — already pinned by M-179; the miss mechanism is named — MET; a check pins it — MET |
| 186 | **Suite plumbing must not lie: skip paths execute, shims re-export completely** (tlvphoto week, cluster 5: a `skip()` NameError hid on the no-Pillow path; a missing shim re-export kept a suite silently red; a worker's "green" was the wrapper's exit code): the pinned-skip-set check also FAILS on a skip path that cannot execute (import the skip helper at module load); an engine/instance shim owes a re-export completeness test; gate-on-suite-LOG already stands | small | **LANDED 2026-07-09 ~23:13, session 30** (door: feature; kind: skill; wish: same file §5; map: grows the test method — INV-80 in SPEC v0.16.9 + test-author's plumbing bullet, three legs incl. gate-on-suite-LOG for background/delegated runs; M-191 red-proven against HEAD, `test_suite_plumbing_must_not_lie`) | Done when: test-author states both duties, red-proven — MET (plus the log-tail leg, same class) |
| 187 | **The pre-ask self-scan covers questions, never only reports** (tlvphoto week, same evening: a session asked him to decide a client-asset sync in jargon he could not parse — a sync the agent could have just done; both rules exist, the failure is live-chat enforcement): a question to the human passes the same scan as a report — plain words, every term grounded, and first "can I decide or verify this myself?" (ask-never-guess's second half) | small | **LANDED 2026-07-09 ~23:16, session 30** (door: feature; kind: skill; wish: same file, closing note; map: grows the report/ask scenario — INV-81 beside INV-34/INV-60, one home communicator; SPEC v0.16.10 + communicator 0.1.41 walk extension (the first gate: can I decide this myself?); prover CROSS-LINK `docs/prover/2026-07-09-row187.md`, F1-F3 folded; M-192 red-proven against HEAD, `test_pre_ask_scan_covers_questions`) | Done when: communicator's pre-show/pre-ask walk names questions explicitly, red-proven by a string test — MET |
| 173 | Deferred rows need an evaluation point: the milestone gate (M-1) re-scans every deferred row's revisit trigger; a fired trigger returns the row to the runnable queue (else a fired trigger goes unnoticed — tension with INV-1) | small | landed 2026-07-09 s29; was (FULL re-prove F5) | Done when: SPEC names where deferred triggers are evaluated + a test asserts a fired trigger surfaces the row. Detail: `docs/prover/2026-07-09-full-reprove-session29-body.md` F5 |
| 174 | A feature parked by a bug re-fences and re-proves its delta against the committed truth on resume, the way INV-39's later lane does (T-9 currently resumes "in original order" with no re-fence — can integrate a delta proven against stale law) | small | landed 2026-07-09 s29; was (F6) | Done when: T-9's resume step states the re-fence/re-prove, pointing at INV-39. Detail: prover record F6 |
| 175 | Define the transition for a critical bug arriving during a RUNNING milestone (T-18 covers opening lanes mid-milestone, not bug preemption of one already running) | small | landed 2026-07-09 s29; was (F7) | Done when: SPEC states whether a milestone is one indivisible pen-stage a bug preempts only at completion, or has named checkpoints. Detail: prover record F7 |
| 176 | Name the milestone-quiesce state distinctly from bug-"parked" (T-18 reuses T-9's bug-only parked state without a defined milestone-hold transition/resume) | small | landed 2026-07-09 s29; was (F8) | Done when: SPEC names the milestone-hold state + its resume rule. Detail: prover record F8 |
| 177 | Concurrent lane-claim back-off (two sessions, one repo) names no tie-breaker — specify the total order "later claimant" reads so exactly one backs off (else mutual back-off / double-claim reachable) | small | landed 2026-07-09 s29; was (F9) | Done when: SPEC states the ordering key (commit ancestry, or the inbox session token). Detail: prover record F9 |
| 178 | `tight`-rung batched-gate recovery: state the revert/re-apply path on a batch-end red so HEAD is never left red across a breakpoint (bisect names the culprit but not the fix action) | small | landed 2026-07-09 s29; was (F10) | Done when: SPEC states the tight-rung rollback. Detail: prover record F10 |
| 179 | ARCHITECTURE.md:33 — disambiguate the INV-67 / clock-arm wiring note so the prose no longer reads as the guardrails node owning INV-67 (mechanically clean, traceability parses it to communicator; prose-only) | bug | landed 2026-07-09 s29; was (F4) | docs-only; edit carefully to keep the green owns-every-anchor-once check. Detail: prover record F4 |
