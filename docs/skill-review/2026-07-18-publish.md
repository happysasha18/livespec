# Skill review — publish (the release-note offers section)

SKILL-REVIEW

Skill: publish

Date: 2026-07-18
Reviewer: skill-creator (Anthropic)

Verdict: passes; the edit is reviewed and sound. The publish skill's GitHub-repo target gains an
optional offers section on the release-note step, phrased as a recorded offer-or-none decision with a
named checker. It states cleanly, sits in the right plugin section, and cites invariants that resolve.
No must-fix. No description frontmatter changed, so no triggering surface moved.

## What changed

Commit 7242339 (ROADMAP rows 402 and 409) added to the GitHub-repo target step in "Targets are plugins".
The release-note item now carries an optional offers section: appealing next steps the reader may take,
phrased as choices on his own clock, with the walk owing a recorded offer-or-none decision rather than a
silent skip. The walk offers a next step in a `<!-- release-note:offers -->` region or records
`<!-- release-note:no-offer -->`, and `guardrails/check-release-note.py` reds a note that leaves the
decision unrecorded. It cites SPEC INV-228 and INV-205.

## Findings

- **The edit states clearly and keeps the plugin-list structure (sound).** The offers detail rides the
  existing "release notes for a tagged release" bullet in the GitHub-repo target. The `·` separators around
  the insertion stay intact, so the item still reads as one entry in the target's middle-dot list, and the
  following "repo description + topics set" item is unbroken. The step reads as a self-contained duty in the
  same voice as its siblings.

- **The cited invariants resolve and match the edit (sound).** INV-228 (PRODUCT_SPEC.md line 1867) states
  the release-note offers-section law word-for-word as the skill describes it — the optional section, the
  recorded offer-or-none decision, the two markers, and the checker riding the suite rather than the push
  chain. INV-205 (line 1859) is the touchpoint frame the edit names as its parent. Both citations are
  accurate, and `guardrails/check-release-note.py` exists on disk. The commit that landed this also carried
  the parked-question default (INV-229), but that leg landed on the waiting board, not in this skill, so the
  skill's own citations stay confined to the offers section it actually states.

- **Consistent with the skill's existing offer stance (sound).** The publish skill already treats the
  "made with live-spec" attribution line as an offer rather than a gate. The new offers section is a
  different object with a different mechanism — a recorded decision a checker reads, versus a proposed line
  the owner accepts or declines — and the two do not collide: each governs its own surface, and the offers
  section is correctly placed inside the GitHub-repo plugin section rather than the shared floor, so one home
  per fact holds and the targets-are-plugins structure stays clean.

- **Description and triggering (none).** The diff touches only body prose; the `description` frontmatter is
  unchanged. The trigger surface is untouched and no eval drift is expected.

- **No coined names (sound).** "offers section", "offer-or-none decision", and the literal HTML-comment
  markers name their mechanisms in plain words; no metaphor or invented term entered.

- **Version stamp:** no skill-version bump this landing (the pack stays 2.6.3); the change is body law under
  the pack's existing version, and this record ships after the reviewed edit in the same push range.

- **List-length observation (folded, no change needed).** The release-note item is now noticeably longer
  than its `·`-separated siblings in the GitHub-repo target, which leans on the middle-dot list. The extra
  length carries a legitimately richer step (the optional section, the recorded decision, the checker and its
  suite placement), and breaking it out would scatter one target's steps, so it stays as one entry. Worth a
  note if the item grows again, but nothing to fix here.
