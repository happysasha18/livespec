# Skill review — INV-237 wiring (build-pipeline, product-prover, live-spec-base)

SKILL-REVIEW

Skill: build-pipeline
Skill: product-prover
Skill: live-spec-base

Date: 2026-07-18
Reviewer: skill-creator (Anthropic) — run from a clean context per the very rule under review (INV-237)

Verdict: passes; the wiring is sound and reviewed from a clean context. Three skills gained the INV-237
carry — build-pipeline's verify station states that the authoring seat never certifies its own work
adversarially and a release's adversarial pass is authored by a fresh seat; product-prover's persist
step states that a release's adversarial pass runs from a clean context and a newly added lens is
self-applied to its own body before release; live-spec-base gains rule 33 stating the law once. No
description frontmatter changed on any of the three, so no triggering surface moved.

## What changed

The INV-237 landing (2.8.0 MINOR) added the clean-context-review law's wiring: build-pipeline SKILL.md
(a paragraph in step 8's verify/audit section), product-prover SKILL.md (a sentence in the
persist-findings meta rule), and live-spec-base SKILL.md (rule 33 plus the rule-count claim in the
description raised to "thirty-three rules"). No version-frontmatter-only change; the bodies changed.

## Findings

- **Duplication with INV-46 handled by citation, not restatement (sound).** The clean-context reviewer
  flagged that carrier one first re-described INV-46's "differently-contexted head" definition; the SPEC
  clause and its wiring were tightened to POINT at the freshness INV-46 defines and to state only the new
  binding to the release-gate re-prove [INV-116], the locus INV-46's verify-station scope never reached.
  One home per fact holds.
- **Ownership labeling corrected (sound).** The homes sentence read "carried into build-pipeline as
  wiring" while ARCHITECTURE.md assigns INV-237's ownership to the build-pipeline node; it now reads
  "owned by build-pipeline whose verify station is its operative home, carried into product-prover as
  wiring", matching the architecture. Single-node ownership holds (checked against the INV-164 precedent).
- **Rule count in sync (sound).** live-spec-base's description claims "thirty-three rules in the body";
  the numbered rules count to exactly 33, rule 33 being INV-237. The test asserts the claim.
- **Wiring text matches the spec and the tests (sound).** The build-pipeline and product-prover
  paragraphs say what INV-237 claims they say, verified verbatim against the SPEC clause and the M-419
  test assertions.
- **Description and triggering (none).** No `description` frontmatter changed on any of the three skills.
- **Version stamp.** This record ships in the same commit as the 2.8.0 stamp and the reviewed edits, so
  gate s reads it fresh against each skill's change.
