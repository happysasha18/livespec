# Skill review — spec-author (skill-creator review at the 4.0.0 landing)

`SKILL-REVIEW`

Skill: spec-author
Date: 2026-07-23
Reviewer: skill-creator review at the 4.0.0 landing, applied over the register rewrite and the
format content update.

Verdict: passes — trigger and boundaries read as the strongest in the pack. Meaning is preserved and
every INV/T/E anchor is intact across both passes: the register rewrite (22 scissors, 8 inflation,
4 metaphor fixes over 4 cold-read rounds) and the format content update that rewrote the 13 passages
still teaching the old use-case-first, hand-kept-index shape into the requirements genre (a closed
glossary, Context/User Story/named-case criteria, a generated code-to-location table), adding the
comprehension-gate and change-record sections. One test pin was forced by the migration itself
(a scenario heading test) and is recorded, not silently changed. The frontmatter trigger phrases are
byte-identical.

Open items: R1 — the comprehension-gate section duplicated text-audit's cold-reader loop instead of
pointing at it — was fixed the same day (repointed to a pointer sentence). R2 stays a queue row: the
676-line body is the one meaningfully over the ~500-line ideal; moving a section to references/ is
low priority, not blocking.
