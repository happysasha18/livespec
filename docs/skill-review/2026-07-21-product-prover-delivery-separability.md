# Skill review — product-prover + spec-author (new lens: delivery separability along a declared axis)

`SKILL-REVIEW`

Skill: product-prover (lens body + dual-discovery habit), spec-author (authoring duty)
Date: 2026-07-21
Reviewer: skill-creator review method applied against the diff from a fresh read (live-spec-base v3.5.0)

Verdict: passes — an additive change on both skills, no frontmatter or description touched, so triggering is unaffected; the additions sit in the right homes and match the surrounding register.

## What changed

**product-prover** gains one lens bullet in the Phase-3 stress-lens list, inserted right after the INV-49 false-serialization lens (its nearest concurrency/composition-plan sibling): "Delivery separability along a declared axis." The bullet mirrors the INV-49 lens's own shape — a two-sided read (a monolith with a named reason is a settled answer; a monolith naming neither reason nor owed road is the finding), the explicit "senior read, never a gate" stance keyed to INV-214, and the anchor trailing in brackets. The same bullet carries the dual-discovery habit as its second half: for a lens the list applies, ask whether that lens's dual bites — held as a habit, stated to never demand every lens ship a partner, with the collapse cases named (an invariant's dual is its progress measure, already the liveness reading). The habit's home is the skill, not a hard invariant, which is correct: it is guidance for how the prover imagines, not a checkable property.

**spec-author** gains one sentence extending the composition-axes duty (INV-244's read): where an owed axis adds runtime code, the delta states how that axis is delivered — a monolith with a named reason, or a delivery road a later row lands. It sits directly after the "reads these declared axes from the kind" sentence, the natural place, and reads by the prover's new lens.

## Checks walked

- **Description / triggering:** unchanged on both skills — no frontmatter edit. The lens is reached through the existing review flow, not a new trigger, so the description need not grow. Correct.
- **Home fit:** the lens body belongs in product-prover (it is a review lens); the law and the authoring duty belong to spec-author (INV-244's owner). The split matches the INV-49 precedent (lens carried by product-prover, invariant owned by the law's home node). Correct.
- **Register:** the additions obey the pack's documentation register — plain words, positive sentences, no contrast-frame scissors, no leading internal code. The register lint passes at zero errors on the shipped tree (the "AI" caps token was reworded to "an assistant capability" during the build).
- **Redundancy:** the lens does not restate the INV-49 lens above it (that one reads a concurrency plan's edges; this reads a spec's delivered artifact) nor duplicate INV-244's coverage-gap read (that reads behaviour along an axis; this reads the delivered artifact along the same axis). The dual framing is the distinguishing obligation.
- **Length / scannability:** the bullet is one paragraph, in line with the INV-49 lens bullet's length; it does not bloat the list.

## Note

The adversarial correctness of the lens (does it overreach, is "adds runtime code" a crisp trigger, is it genuinely distinct from INV-244) is judged by the fresh-context prover audit recorded under docs/prover/2026-07-21, not by this skill review; this review covers skill quality and home fit.
