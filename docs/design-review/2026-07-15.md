# Design review — live-spec, 2026-07-15 (MINOR-gate 1.5.0, review-pipeline family)

Clock 08:37 15.07.2026. No skill loaded. Reads only PRODUCT_SPEC.md (whole) + ARCHITECTURE.md.
Every finding below is a recommendation or a question — never a defect, never a block.

**Framing (себе).** The standard INV-141 design review *stands down* on live-spec, because a skill
pack has no elements a person acts on (recorded 2026-07-14). This pass is the task-directed same-kind
parity review over the spec's *behaving clauses* — its invariants, review passes, and pipeline steps
treated as the "things that behave" — with the FOCUS on the just-changed review-pipeline family
(INV-140 prover labels findings · INV-141 design review · INV-142 confidence read + held ask · INV-154
the bounded loop). Today's prover pass (`docs/prover/2026-07-15-inv154-fixed-point-loop.md`) already
folded seven *within-clause* soundness findings on INV-154; none of them is a sibling-parity finding,
so the ground below is fresh.

## Where the family already behaves alike (no finding)

Across the four siblings, three obligations are carried uniformly, and the parity is clean:

- **Never-side / holds-no-landing.** INV-140 blocks-or-queues; INV-141 "produces no blocking defects…
  can never hold a landing"; INV-142 "holds no landing"; INV-154 "surfacing at the cap holds no
  landing." Uniform.
- **Record discipline.** INV-140 a record column; INV-141 a dated record with a per-finding outcome
  column; INV-142 the held ask on the dated record; INV-154 the resting states + unsettled groupings
  on the dated record. Uniform.
- **Net (INV-150).** Each locates itself in the three-net taxonomy: INV-140 is the prover station,
  INV-141 *is* the design-review net, INV-142 rides it, and INV-154 names "the design review [as]
  the net that counts its own progressing rounds and stops at the cap [INV-150]." Uniform.

One difference that is **plain and accounted-for, so it is left silent as a finding**: INV-140 findings
carry no confidence read while INV-142 findings do (confident/likely). This is right by net-kind — a
prover finding pins to a stated sentence (deterministic), a design-review finding's deciding fact may
live only in the human's intent — so the asymmetry is intended, not a divergence.

## Findings

| # | Two objects | Shared role (one sentence) | The divergence | Confidence | Outcome |
|---|---|---|---|---|---|
| 1 | **INV-154** (the bounded loop, PRODUCT_SPEC.md:442) · **INV-141** (the design review, :438) | Both are the design-review pass over a spec, run per project kind, whose result must not read as coverage it did not actually give. | INV-141 explicitly *stands down by name in the record on a kind with no elements a person acts on, rather than running vacuously and reading as coverage it did not give.* INV-154 carries no such case: its two named resting states are CONVERGES (a round surfaces no grouping/difference) and WAITS (only unanswered questions). A round whose design-review re-read **stood down** (a UI-less kind — live-spec itself, a backend service) surfaces no grouping and no difference, so it falls into **CONVERGES by the literal definition** — the loop records "converged / design settled" on round 1 when no element inventory was ever examined. The exact "reading as coverage it did not give" that INV-141 guards against, reintroduced by the loop that is *built from* the design review. | confident → recommendation | |
| 2 | **INV-29** fit-walk forward-binding (`…owes its walk at the first landing that touches it, never retroactively en masse [INV-21 kin]`, :424) · **INV-41 / INV-75 / INV-127** forward-binding (each `…binds from the first landing that touches the surface after the clause exists, never retroactively [INV-15]`) | Both state the one recurring "a duty binds forward from the first landing that touches the surface, never retroactively en masse" behaviour and cite a root for it. | The identical forward-binding behaviour is anchored to **two different roots**: INV-29 (and INV-21's own text) treat INV-21 as the kin-root; INV-41, INV-75, INV-127 treat INV-15 as the root. Same law, two cited homes — a reader tracing "who owns forward-binding" lands in two places. | confident → recommendation | |
| 3 | **The design-review record** (ARCHITECTURE seam "design review → record": dated record, per-finding outcome column, `-suffix` arms a same-day second run, *mirroring the prover record*) · **The skill-creator craft walk's record** (M-1 / INV-99: "Fold or reject each finding, with a written reason, in a dated record") | Both are a review pass that writes a dated record listing each finding with its per-finding disposition (fold / reject / outcome). | The prover record and the design-review record are *declared* mirror siblings (the seam says so, and both carry the same-day `-suffix` and outcome-column shape). The skill-creator craft walk — and, arguably, the verify audit [INV-46] whose findings become "rows or red" — is the same kind of pass but is **not declared a member of that record family**: its record's location, its same-day-second-run convention, and its outcome-column shape are left unstated to follow the one shape. Either it should join the declared review-record class, or a sentence should say it deliberately differs. | likely → question | |

## The one question (finding 3), both objects in hand

Should the **skill-creator craft walk's dated fold/reject record** (M-1 / INV-99) be declared a member
of the same review-record class as the **prover record and the design-review record** — inheriting the
same per-finding outcome-column shape and the same same-day `-suffix` convention the design-review
record already mirrors from the prover record — or is its record deliberately a different shape?
Recommended default: declare the review-record shape once and name skill-creator's record (and the
verify audit's) as members, so all review passes report their findings in one readable shape.

## Strongest divergence (two sentences)

INV-141 stands down *by name in the record* on a kind with no elements a person acts on, precisely so a
non-run is not read as coverage it did not give; INV-154 — the loop built directly on that same design
review — offers only CONVERGES and WAITS as resting states, and a round whose design-review re-read
stood down surfaces no grouping and so is swept into CONVERGES. On a UI-less kind the loop therefore
records "design settled / converged" on the first round when the design review never examined a single
element, reintroducing the very coverage-illusion INV-141 exists to prevent.

## Tally

Recommendations: 2 (findings 1, 2). Questions: 1 (finding 3). No blocking defects; the landing is not held.

## Outcomes — senior fold, 08:47 15.07.2026

- **Finding 1 (stand-down parity) — FOLDED into INV-154.** The loop now has THREE resting states: CONVERGES, WAITS, and STANDS DOWN (the design review did not run on a kind with no element a person acts on, recorded by name so the rest is not read as coverage it never gave). This closes the coverage-illusion on a UI-less kind, live-spec itself included. Rippled to the design-reviewer skill, the ARCHITECTURE loop seam and node, M-299, and the guard test. The whole-spec prover pass found the same gap from the other side (its finding 1, the confident-recommendation case), folded in the same rewrite.
- **Finding 2 (forward-binding cited to two roots) — QUEUED, ROADMAP 322.** A whole-spec one-home hygiene item, pre-existing and outside this delta; non-blocking.
- **Finding 3 (declare the review-record class) — QUEUED, ROADMAP 323, at its recommended default.** Declare the review-record shape once and name the skill-creator and verify-audit records as members. Taken at the default rather than surfaced to the human, since it is a one-home hygiene recommendation the design review never blocks on.
