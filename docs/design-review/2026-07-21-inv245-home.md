# Design review — INV-245 project-name arm: the gate-family HOME question (2026-07-21)

Fresh-seat design review (a seat other than the lane's author), recommendations and questions only,
never a block. Question: is the project-name check correctly homed as an ARM on the existing
shipped-language gate (gate i, INV-120), or should it be a standalone sibling gate — and is its
scope-tension handling consistent with its sibling gates?

## Verdict

- **HOME — arm-on-gate-i is right, confirmed.** Every pre-push letter a–z is spoken for
  (`gate-red-proofs.json`: proofs a,b,d–z, covered c), so a standalone gate would need a 27th letter
  the alphabet-keyed meta-guards (INV-210 CI-mirror, INV-212 every-gate-can-fail, the proof registry
  itself) are not built to carry. The check is offence arm (3) inside `scan_file`, sharing the file
  walk, the waiver shape, the glob helper, and the allowlist file — one script, one wrapper, one
  pre-push line, one CI step, one proof entry, one unified verdict. No redundant second gate is
  created. It follows the pack's own established house style — a distinct invariant riding an existing
  gate's letter with no new letter: INV-229 on gate q, INV-216 and INV-243 on gate m.

- **Letter-proof coverage — no hole.** Gate i's known-red proof exercises the Cyrillic/owner path, not
  the project path; this is correct under INV-212 (the requirement is that the gate can red, verified by
  structure). The project arm's own reddening is proven by the M-430 suite tests riding gate b, exactly
  as the INV-229/216/243 arms are covered.

- **INV-120 vs INV-245 — a real distinct invariant, correctly not folded.** The two arms differ on both
  axes that would bind them into one invariant: scope (INV-120 is the whole shipped set; INV-245 is core
  specs only — a README naming a project is fine and must not red) and principle (INV-120 is the
  impersonal-voice rule composing with INV-118; INV-245 is core-spec self-containment — no sibling-project
  coupling, no dated history the diaries own). Sharing a mechanism is not sharing an identity.

## Actionable finding (folded)

- **The matrix permit was stated as unconditional; it is emergent and date-conditional.** The siblings
  spare by declaration (every allowed thing has a named home); the arm's "permit the fixture-ledger kind
  names / test-function substrings" falls out of the dated-window rule plus the word boundary, so a
  fixture name that ever lands beside a date would red. FOLDED: the INV-245 clause and the M-430 row now
  state the permit is conditional on distance and name the existing `project_name_waivers` escape (the
  dated-allowlist discipline the sibling offences already keep). No code change was required — the waiver
  channel already exists with the sibling shape.

No blocking findings (a design review never blocks).
