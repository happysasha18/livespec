# Prover record — 2026-07-05 (~22:15), row 93 pack-side CROSS-LINK (design-sync wiring)

Mode: **CROSS-LINK**. Delta: E-18 re-worded to the honest wired-vs-target split; the `design-sync`
switch lands off-by-default in the base defaults table; channel lines land in communicator rule 5 and
build-pipeline step 9. The machine itself (first real sync on a visual host, through the human's gate)
stays [target] and is the row's named remainder. Previous records: zero unfolded.

## Seams checked

- E-18 × E-13 — the switch is a NAMED package-default setting with a home in the base table; a host
  turning it on writes a recorded profile line (INV-14 untouched).
- E-18 × base rule 17 / ACT-1 — every sync passes the human's publish gate; the wiring lines say so in
  both places they could fire (communicator, pipeline step 9) — no line lets a sync bypass the gate.
- E-18 × T-7/communicator rule 5 — the design project is the team-review channel and NEVER a substitute
  for the in-session show; the render stays the landing authority (stated in both wiring lines).
- E-18 × T-16/INV-22 — applicability reads off the work-kind: product-kind on a visual host; every
  other kind stands it down by name. The pack itself (text) never syncs — mechanically derivable now.
- E-18 × S-0 — the [target] tag now binds at the honest granularity: "[target: the machine; the wiring
  is live]"; the header's target list still names the design-sync MACHINE. No over-claiming: no wiring
  line asserts a sync ever ran.

## Findings

| # | Finding | Tag | Status |
|---|---|---|---|
| F1 | The S-0 header's target list says "the optional design-sync machine [E-18]" — still accurate after the split (the MACHINE is what remains target); checked, no edit needed. | worth-considering · consistency | no change needed |
| F2 | The row's DONE-WHEN cannot close from a text-only window (needs a visual host + the human's gate) — the row must carry its remainder explicitly so the queue never shows a half-landed surface as landed. | should-clarify · missing-outcome-check (postcondition) | **FOLDED** — row 93 status names the pack-side half landed and the remainder (real run + gate) still open |

## Verdict

Wiring proceeds; row 93 stays OPEN with a named remainder. Suite green (67) including the new
`test_designsync_wiring`.
