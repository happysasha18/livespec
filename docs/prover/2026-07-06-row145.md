# Prover record — 2026-07-06 ~22:22, row 145 (his word read as meant), CROSS-LINK

Scope: the INV-42 delta (SPEC v0.15.39 — scenario + invariant row) against its named neighbours:
ask-never-guess (INV-4), silence-is-consent (INV-31), the no-disclaimers rule, nothing-unasked in the
build (INV-16 family), the host-profile AUDIT-ONLY line (the promoter core rerouted by his word).

Opening assessment: the re-scope is the right carve — the pack keeps the two general clauses (cuts
stay cut; sarcasm is not instruction), the confident-specialist voice core goes to the promoter's own
skill on his placement word, and the two bans the pack already holds are cross-linked, never restated
(one home per fact). One real hole found and folded in-pass. Ready to build.

| # | Finding | Severity · category | Status |
|---|---|---|---|
| F1 | The kill-list had no HOME: "the writer keeps the kill-list" lived nowhere named, so it dies with session memory — the exact failure the wish reported (a cut word resurfacing four times in one document) would recur across a wipe. | should-clarify · missing-rule (invariant) | FOLDED — the kill-list is written where the artifact's project keeps its records (journal or the artifact's own notes file), never only in session memory; both homes carry it |

Cross-links that hold without change: INV-4 — the sarcasm clause cites it (unclear intent is read from
context or ASKED, never assumed); INV-31 — no collision: that law governs MY defaults being told, this
one governs HIS cuts being honoured; a kill-list entry is his veto's memory, not a re-askable default;
the no-disclaimers rule + silence-is-consent — the original wish's bans 3 and 5 already live in the
pack; the delta cross-links instead of restating (one home per fact, base rule 4); AUDIT-ONLY — the
verdict on the voice core is recorded in row 145 for the promoter window to read; this window touches
nothing there. Sibling sweep: no other pack surface speaks of review-round cuts — the clause is new,
no duplicate to reconcile.

Findings: one, folded in-pass.
