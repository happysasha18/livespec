# Prover short-form record — row 214 landing (2026-07-10 ~03:18, session 32)

Short-form per INV-61 (skill kind, no new surface, no structure change).

- Previous records clean: `2026-07-10-row201.md` (bp trim, 277 green).
- The delta in one line: the small-fix path is now written where the red-first law lives — test-author's "Red first, proven" bullet states that red before code stays the default at every size, and that a one-batch fix+test on a tiny reversible edit inside the pipeline's skip boundary is legal only when the batch closes with the mechanical red proof (restore the pre-change file via `git show HEAD:<file>`, run the suite, watch the new rows fail, restore the fix) named in the landing record; build-pipeline's skip sentence points at the path. Composition checked against INV-61's never-scaled list: the red RUN stays mandatory either way — the path reorders, never removes, the proof. Answering the tlvphoto wish's (a)/(b) fork: (b), because the law's own words already accept a red run "against the pre-change state", the field session's recovered proof was exactly this protocol, and a written escape valve with a mandatory mechanical proof holds better under time pressure than an order that already decayed once.
- Verdict: test_small_fix_red_path proven RED against the pre-change tree (AssertionError on the missing path, log read directly), then 278 green after the edits; matrix M-205 pins the test; test-author 0.1.3, bp 0.2.47, installed copies synced. Safe to land.
