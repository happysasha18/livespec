# Prover record — row 194: accepted work reaches the remote (INV-82)

Mode: CROSS-LINK (one clause against the push/trust/shopfront neighbours). Date: 2026-07-10 ~00:14,
session 30. Prover skill version: 0.1.15. Spec under review: PRODUCT_SPEC.md v0.16.13.

F1 — the law must not swallow his personally named gates (the 1.0 "go"): folded — the clause and
step 9 both carry "the human's personally named gates still wait for his word"; a general grant
never cancels a named condition (the mockup-first precedent). `must-fix · consistency` — folded.

F2 — the remote question must obey inspect-first (the onboarding consensus): folded — the remote is
discovered from the tree; the ONE question fires only where no remote exists, contextually at the
first push moment, recorded in the host profile. `should-clarify · precondition` — folded.

F3 — README-every-push vs the shopfront law (INV-44, version pushes): one law, two cadences — folded
by citation: the clause names INV-44 as the walk and adds the every-push cadence for remoted hosts;
the publish skill's checklist stays the walk's one home. His 00:14 word: README crisp and current,
mandatory. `should-clarify · one-home-per-fact` — folded.

Red proof: no INV-82 at HEAD. All folded; nothing rejected.
