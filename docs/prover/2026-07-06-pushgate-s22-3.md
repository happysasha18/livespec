# Prover record — 2026-07-06 ~23:20, push-gate re-check, session 22 (row 147 landing)

Previous records: `2026-07-06-pushgate-s22-2.md` (clean) and `2026-07-06-row147.md` (F1+F3 folded in
the delta, F2 recorded as intended narrowness) — no unfolded must-fix rows ride in.

Scope of the delta since the row-146 push: SPEC v0.15.42 (INV-45 in the machines scenario; the
never-bend push-gate bullet and the guardrails-machine bullet re-read through the reach map; index
row) · ARCHITECTURE.md (guardrails node owns INV-45, script pinned) · TEST_MATRIX.md (M-142, level
"string" after the senior's own vocabulary defect was caught by the traceability tooth and fixed) ·
guardrails/check-push-reach.sh NEW + pre-push gate-b scoping · build-pipeline 0.2.32 (step 8 re-read)
· TestGateReachMap (3 fixtures, red-proven bare) + test_push_gate_reach_law (red→green) · suite 140
green · pack 0.8.54.

Walk: the owns-once parse stays clean (INV-45 a token in exactly one node's column); the index rows
match the prose; the sibling sweep for "whole suite"/"full suite" found no stale sentence left (the
lean rung's "full suite still at every landing gate" is a LANDING sentence, untouched by push reach —
recorded in the row147 record). Shopfront walk for this push (INV-44): README's pipeline section
already carries the reach sentence's family via step 8's re-read? — checked: README step 8 line says
"Verify by deed. Run it and see the result. Green = zero failures AND the skip-set is exactly the
expected list" — no "whole suite before any push" claim lives in README, nothing stale; diagrams
unchanged — current. No ⟨DECIDE⟩ touched; D-1/D-2/D-3 ride unchanged.

Verdict: no unfolded rows; the delta coheres; push may proceed.
