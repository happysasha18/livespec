# Prover record — 2026-07-06 ~23:10, row 147 (the push gate scales to the diff's reach), CROSS-LINK

Scope: the INV-45 delta (SPEC v0.15.42 — one paragraph in "The machines that hold the bounds", the
INV-40 push-gate bullet re-read, the guardrails-machine bullet's suite line, + index row) against its
named neighbours: the push gate (M-6), the never-bend list (INV-40), the guardrails machine (E-6), the
economy ladder (T-19), the shopfront law (INV-44, kin), the prover-freshness gate.

Opening assessment: the law lands with its two old contradictions reconciled IN the same delta — the
never-bend bullet and the machines bullet both said "suite green" unconditionally; both now read
through the reach map. The map's three teeth (explicit, conservative, self-tested) are exactly what
keeps the fast path from becoming the hole the host audit warned about. Ready to build.

| # | Finding | Severity · category | Status |
|---|---|---|---|
| F1 | Two standing sentences contradicted the new law at authoring time: INV-40's "suite green at HEAD" and E-6's "the suite is green" — left unreconciled, a session reading either would run (or demand) the ritual full suite and call the reach map illegal. | must-fix · direct-contradiction (contradiction) | FOLDED in the same delta — both bullets now read through the reach map (INV-40: "every check the diff can REACH green at HEAD"; E-6: "its RUN scoped by the diff's reach"); build-pipeline step 8's "run the whole suite before any push" gets the same re-read at the code step |
| F2 | The pack's own prose-only class is narrow to the point of rarity: landings always touch ROADMAP/JOURNAL/docs/prover (all full-reach), so the fast path fires only on pure README/OVERVIEW/research pushes. | worth-considering · over-specific (abstraction) | RECORDED, no change — honest and intended: the pack's suite is 18 s, the LAW is for hosts with minutes-long suites; the pack ships the working shape, a host widens its own map through its own rows |
| F3 | check-prover-record must never scope with the diff (a prose push still changes what a reader meets; the record is about the PUSH, not the code). | should-clarify · missing-rule (invariant) | FOLDED — the clause names the cheap gates (prover record, ownership, coverage, loadability, prototype fence) as never scoping, running at every push |

Cross-links that hold without change: T-19 tight rung's "a push still requires the full gate green at
HEAD [M-6]" — "full gate" now resolves through INV-40's re-read sentence, one home, no second edit
needed; INV-44 — the shopfront walk is push-triggered, not suite-triggered, so it fires regardless of
the reach class (a prose-only push is exactly a shopfront-heavy push — the two laws compose, the cheap
walk covering precisely the diff class whose suite stands down); row 55's host-facing gates stay
[target] untouched. Sibling sweep: grep for "whole suite"/"full suite" across SPEC and skills found
the two folded bullets + build-pipeline step 8 (in the delta's worker edits) + T-19's lean rung ("full
suite still at every landing gate" — a LANDING gate sentence, not a push sentence; landings run the
suite as part of verify-by-deed, untouched by push reach) — no other stale sentence rides.

Findings: three — two folded in-pass, one recorded as intended narrowness.
