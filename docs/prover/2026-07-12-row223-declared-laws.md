# Prover — row 223, declared cross-cutting laws (2026-07-12, short form per INV-61 + the station's first run)

Surface-class row; the delta itself is small (three homes, no new visible surface; architecture =
anchor assignment INV-101 → spec-author + a wiring mention on the prover node), so the record
pairs the short form with the station's first-run table.

- **Previous records:** 2026-07-12-row222-test-hygiene clean (0 must-fix); no unfolded rows.
- **The delta in one line:** the spec keeps its cross-cutting laws in ONE declared-laws home;
  every new surface's section states its line against each declared law (the clause or a dated
  exemption) before the prover reads it (spec-author's habit); the prover's walk gains the
  matching station — per declared law, enumerate every surface and transition, demand the clause
  or exemption per item, a missing clause ranking as a broken invariant (INV-101, clause + index;
  prover lens before entry-symmetry; spec-author paragraph after the curated-list law; M-237,
  four string tests red-proven). The pack's own list declared: register + clock-honesty +
  no-self-certification, with dated exemptions for measurement (deferred family, rows 47-49/96)
  and accessibility (GitHub's renderer). A lens grew, so the next FULL pass is re-armed by the
  prover's own record law — the 1.1.0 audit runs FULL anyway.
- **Verdict:** ready to ship — 0 must-fix, 1 observation recorded below.

## The station's first run (the row's field leg) — clean

Run by a fresh reviewer over the pack's own spec, 2026-07-12 ~00:21 (the run's landing read off the wall clock): **11 items** (4 registered
shopfront surfaces + 5 spec-named exchange surfaces + 2 named transitions) × **3 declared laws**
= 33 cells. **31 covered by clause · 0 by exemption · 2 n/a** (clock-law cells on the decision
page and the settings card, which carry no dated line) · **0 MISSING**. The two declared
exemptions checked as dated. Full table in the run agent's report, digested here:

- Register: every item covered — the rendered-surface lint [INV-83], the clean-writer road
  [INV-84], and the chat-law enumerations [INV-28, INV-34, INV-35] between them reach all 11.
- Clock-honesty: chat-facing items covered by the chat face [INV-24, INV-93]; the four shopfront
  pages and the push transition ride the stale-claim net at push [INV-44].
- No-self-certification: universal — the clause binds every surface by its own text [INV-94].

**Observation (should-clarify, recorded):** the clock law's fences are homed to machinery (file
names, journal headings, ledger dates) and the chat face; shopfront PROSE dates ("July-2026
survey") sit outside its literal reach and are guarded by the stale-claim net at push [INV-44].
Today's shopfront dates are all legal past references. Worth one homing sentence in the INV-24
clause naming that seam; queued as part of the batch's bookkeeping rather than a must-fix.
