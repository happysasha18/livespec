# Prover — row 239, the catch-up routing's version-delta discriminator (2026-07-12, short form per INV-61)

Small delta (kind: skill; architecture = anchor assignment INV-110 → attach).

- **Previous records:** 2026-07-12-row233-no-silent-drop clean (0 must-fix); no unfolded rows.
- **The delta in one line:** the catch-up routing states its discriminator in one sentence — the
  walk fires only when the host's recorded package version is behind the current package VERSION;
  a docs restructure that carries no version delta is the host's own queue row through its
  pipeline, whatever wording the ask used; the trigger wordings are examples under this test, and a
  wording never decides the routing — spec clause (INV-110 + index row), MIGRATION.md's "When to
  run this" home, M-248, three string tests red-proven against the pre-delta tree. Composition
  read: no clash with the existing trigger-wordings list (MIGRATION.md line 9) — that list stays as
  illustrative examples, now explicitly demoted to "examples under this test" rather than the
  routing's own truth; composes with row 240's vehicle clause, which the new sentence's own carve-out
  ("the host's own queue row through its pipeline") now names as the destination for a no-delta ask.
  Born of the track-coach audit (2026-07-10 ~14:19): the trigger wordings alone would have justified
  the heavy catch-up walk for a pure docs-layout pass carrying no pack-version delta.
- **Renumber note:** the draft was originally numbered for a slot ahead of row 233's own reservation;
  the orchestrator's landing-time call on row 233 (codes consume in landing order) already walked
  this row's numbers down to INV-110/M-248 before this landing began — confirmed free in the live
  tree at apply time, sequential after row 233's INV-109/M-247, no gap opened.
- **Verdict:** ready to ship — 0 must-fix, 0 should-clarify.
