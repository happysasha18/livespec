# Prover record — INV-177, the vendored-pins arm of the update check (2026-07-16, FEATURE-FIT)

Prover skill version: 1.1.4. Mode: FEATURE-FIT on one delta (row 348).

## The delta
INV-177 — the daily update check (E-25) reads the ratchet manifest's pack-version pin (INV-172),
covering both installable kits, and proposes the re-install naming the vendored files that
differ from the local pack's current copies. Proposal only; re-seed explicit.

## Fit walk
- **Arrival** — the arm rides the existing daily check; no new trigger surface. HOLDS.
- **Exits** — old pin → proposal with stale files named; current pin → silent on vendored files;
  no manifest → the plain proposal unchanged. All three tested. HOLDS.
- **Contradiction check** — E-25 forbids a per-skill REMOTE diff; the per-file list reads the
  LOCAL pack checkout, and the clause states that; the first draft repeated E-25's own phrase and
  the redundancy gate caught it (fixed same landing — the compaction gate doing its job). HOLDS.
- **Composition with INV-172** — the manifest is written by the installer, read by the watcher;
  the scaffold pins are informational (the installer never vendors scaffold files). Stated. HOLDS.

## Disposition
| Finding | Kind | Disposition |
|---|---|---|
| task 6 (upstream update-watcher) | feature | landed — INV-177, M-332, the watcher arm |
| design-review 2026-07-16 rec (manifest over both kits) | recommendation | folded — scaffold pins join the manifest |

Verdict: the delta FITS; no open questions ride.
