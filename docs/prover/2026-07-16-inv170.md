# Prover record — INV-170, the CROSS-LINK quantifier re-verify (2026-07-16, FEATURE-FIT)

Prover skill version: 1.1.4 (pre-fold; this record reviews the fold that lands INV-170).
Mode: FEATURE-FIT on one delta — the quantifier re-verify. The independent deep pass that
motivated it is the Fable adversarial audit, `docs/audit/2026-07-16-prover-fable.md` (finding D3);
this record checks the fit of the fold that answers it.

## The delta
INV-170 — "A surface add re-verifies the document's quantified claims against the grown surface
set." CROSS-LINK keeps its seam scope but gains one mandatory whole-document step: sweep for
enumerations and universal quantifiers and re-verify each against the set including the newcomer.

## Fit walk
- **Arrival** — the step runs inside CROSS-LINK, which fires on every surface add (the modes
  section); the mode text now names the step mandatory. HOLDS.
- **Every next-step** — a falsified sentence is a finding at the add, taking the ordinary
  defect/recommendation kind rules (INV-140); a clean sweep adds nothing. Both exits stated. HOLDS.
- **Scope seam** — the step is whole-document while the mode is seam-scoped; the mode text states
  the carve-out explicitly ("Skip the whole-doc property sweep, with one mandatory whole-document
  step kept"), so the two sentences cannot be read as contradicting. HOLDS.
- **Composition with INV-125** — the uniformity lens demands member enumerations; this law keeps
  those enumerations current. The two clauses cite each other; no circularity, since one demands
  the form and the other maintains it. HOLDS.
- **Cost bound** — the sweep is mechanical (grep-shaped over quantifier words and member lists),
  so it does not re-open the FULL-versus-CROSS-LINK cost trade the mode exists for. HOLDS.

## Disposition
| Finding | Kind | Disposition |
|---|---|---|
| Fable audit D3 (CROSS-LINK never re-checks old quantified claims) | defect | folded — INV-170, M-324, the mode text |

Verdict: the delta FITS; no open questions ride. The batch-close FULL pass re-reads the whole spec
including this fold.
