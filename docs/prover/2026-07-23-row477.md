# Prover record — row 477 (TEST_MATRIX.md gets its format), CROSS-LINK, 2026-07-23

Prover skill: product-prover, live-spec pack v4.0.1 (base pin `live-spec-base` v4.0.1). Mode: CROSS-LINK
— the new surface (the test-matrix format member + `docs/test-matrix-format.md`) proven against the
family's own laws and the named existing seams (test-author's law, the current `TEST_MATRIX.md`, the
gate family, the traceability suite). The whole-document quantifier re-verify (INV-170) is the mandatory
whole-doc step; it is recorded **partially blocked** below because the spec body it must sweep is absent
from the working tree (F1).

Previous record checked: `docs/prover/2026-07-23-rows461-464-shortform.md` — states F9–F15 folded by rows
461–462 and F1–F8 folded at 4.0.0, "no other unfolded rows stand." No unfolded prior row touches this
delta.

Verdict (initial pass): blocked on F1. Verdict (after restore and re-read, same date): **lands-with-fixes**
— the spec half was re-read on the restored body (6902 lines, index gate green 358/358), F1–F8 folded as
verified below, and the re-read added F9–F13.

| # | Kind / severity | Folded / rejected (+why) |
|---|-----|--------------------------|
| F1 | defect / must-fix | folded — body restored from HEAD + delta re-spliced; root cause fixed (build-index.py refuses `-o` onto its input, red-proven, row 478); verified: R283–285 + glossary terms + R282 Context present, index green 358/358 |
| F2 | defect / must-fix at conversion | folded — verified at docs/test-matrix-format.md "Conversion seams": reader rebuilt to five cells, anchors from trailing brackets, repointed same delivery |
| F3 | defect / must-fix at conversion | folded — verified: statuses lowercase as a declared content-preservation delta, suite assertions repointed same delivery |
| F4 | recommendation | folded — verified: the lint's home is `test_matrix_rows_have_level_and_negative_side`, extended (row naming + reach line), no new script; supersedes stated |
| F5 | recommendation | folded — verified: retirement fans out to four named homes incl. the `guardrails/pre-push` caller; two further homes found at re-read → F12 |
| F6 | recommendation | folded — verified: builder reads several codes per row and expands `T-1..T-7` ranges; R284.1 carries the plural "anchors" |
| F7 | recommendation | folded — verified: never side found by the literal word *never*, named as the suite's existing convention |
| F8 | recommendation | folded — coordinator reports the non-goals sentence now closes ROADMAP row 477's Done-when cell |
| F9 | defect / should-fix | folded 2026-07-23 — R283.4 says level ladder; four glossary entries added (artifact inventory, matrix-reference gate, never side, node block); the format doc renamed both mentions |
| F10 | defect / should-fix | folded 2026-07-23 — R124.2 sharpened with the era clause: at the conversion delivery the row lint and the matrix-reference gate stand as the derivation's mechanical close |
| F11 | defect / should-fix | folded 2026-07-23 — R285.2 stops at the red trigger; new R285.3 states the green pass with the reach line; the retirement criterion renumbered to R285.4 |
| F12 | recommendation / should-fix | folded 2026-07-23 — the retirement fan-out grew to six homes: test-author SKILL.md and the matrix template joined the four |
| F13 | note | folded 2026-07-23 — the reach line prints through the suite's terminal summary, stated in the format doc |

## F1 — The prove subject is absent: PRODUCT_SPEC.md's requirements body has been removed in the working tree

> `git diff PRODUCT_SPEC.md` = **−6468 lines, +5 lines**; the file is now only the generated
> `## Reference` table (Code | Location), 360 lines. The +5 lines are the three new index rows
> (`INV-272 | R283.1..R283.7`, `INV-273 | R284.1..R284.5`, `INV-274 | R285.1..R285.3`) and the amended
> `INV-269` row. — working tree `PRODUCT_SPEC.md`, lines 1–360, all table.

The requirement text for R283–285, the four new glossary entries (`test-matrix format`, `matrix row`,
`matrix Reference`, `matrix row lint`), and the sharpened R282 Context exist in **no** file: not in the
working tree, not in `HEAD:PRODUCT_SPEC.md` (6823 lines, `grep R283` = nothing), not in the four agent
worktrees (all pre-format, ~2445 lines, no R283), not in `.live-spec/checkpoints/`, not in the
`.spec-freeze` snapshot. `grep "matrix Reference"` and `grep "matrix row lint"` across the repo hit only
`docs/test-matrix-format.md`. The index rows referencing R283–285 prove a body carrying those criteria
existed when `build-index.py` last ran, and was then overwritten — the generated Reference replaced the
whole document rather than its own trailing section.

Consequence: the CROSS-LINK spec-seam checks the task asks for — glossary closure of the four new terms,
the sharpened R282 Context read against R277–282, R283–285's criteria read against test-author's law and
the current matrix — have nothing to read. The quantifier re-verify (INV-170) cannot sweep the spec's
enumerations for a sentence the matrix-as-second-family-member falsifies (for example any R277–282
sentence that names the spec as the format family's sole member). A prover cannot certify prose that is
not present; per the primary-sources rule, this is reported, not worked around.

Restore the working tree before the spec half can be proven: recover `PRODUCT_SPEC.md`'s body (HEAD body
plus the R283–285 / glossary / R282 draft the index was built from), regenerate the `## Reference`
section **into** the document (not over it), and re-request the CROSS-LINK pass on the restored file.
This reads as a mid-pipeline or concurrent-edit artifact (the fence sits at `2c0db46` "row 477 taken
in-work", one worktree is locked), which points at a `build-index.py -o` invocation that targeted
`PRODUCT_SPEC.md` itself instead of a side file, or an editor that saved the index over the body — worth
confirming so the same step does not clobber the body again after restore.

`defect · missing-scenario (state-space)`

## F2 — The promised row shape (trailing anchor, no Spec-ref column) breaks the traceability suite's matrix reader

> "The spec anchor the row derives from trails at the sentence's end, the family's criterion form … the
> Reference the matrix generates reads the row's coverage from that trailing anchor." — `docs/test-matrix-format.md`,
> "The matrix row is one criterion". And: "Restating them on the row would give one fact a second home,
> so the row stands down." — same doc, "Node blocks stand as the case grouping".

The current matrix row is six columns — `| ID | Fact (from spec) | Spec ref | Test level | Owning test |
Status |` (`TEST_MATRIX.md:138`), and `tests/test_traceability.py::matrix_blocks()` hard-requires
`len(cells) == 6` and reads every row's anchors from **`cells[2]`, the separate Spec-ref column**
(`tests/test_traceability.py:75-82`). The new format moves the anchor into the criterion sentence and,
by its own one-home law, retires the duplicate Spec-ref column — a five-cell row. Under a five-cell row
`matrix_blocks()` silently matches nothing, so every matrix test that reads it — `test_matrix_covers_every_anchor`,
`test_matrix_rows_sit_under_their_owning_node`, `test_matrix_rows_have_level_and_negative_side`,
`test_matrix_built_rows_name_real_tests`, `test_matrix_blocks_match_architecture_nodes` — reads an empty
set and either fails ("index anchors with no matrix row") or passes vacuously. The format doc names the
new Reference gate and row lint as arriving "in that same delivery" but is **silent on rebuilding the
matrix reader in `test_traceability.py`** to the new row shape. The row-445 gates-plan
(`prototype/2026-07-22-spec-format/delta/gates-plan.md:87`) explicitly listed the matrix checks as
"coexisting unchanged … this migration does not touch", so nothing upstream has repointed this reader.

Add to `docs/test-matrix-format.md` (the conversion section) the seam sentence: the conversion delivery
rebuilds `tests/test_traceability.py`'s matrix reader to parse the five-cell row and take each row's
parent anchor from the trailing bracket of the criterion, so the coverage/placement/real-test checks
keep reading. Mark it must-hold at the conversion, since the full suite must stay green on the merged
tree (INV-39).

`defect · boundary-issue (composition)`

## F3 — Status casing: the inherited no-caps law collides with the current UPPERCASE status vocabulary and the suite's assertions

> "the criterion form, so … no word stands in all capitals outside a code anchor" — `docs/test-matrix-format.md`,
> "What the matrix inherits from the family". And: "the status — one of *built*, *todo*, or *retired*"
> — same doc, "The matrix row is one criterion".

The format doc inherits the family's no-all-caps law and states the status values in lowercase italic
(*built* / *todo* / *retired*). The current matrix uses **UPPERCASE** throughout — the header "Status
vocabulary: BUILT · TODO · RETIRED" (`TEST_MATRIX.md:41`) and every one of 410 BUILT / 35 TODO row cells
— and `tests/test_traceability.py` asserts `row["status"].startswith(("BUILT","TODO","RETIRED"))`
(`:246`) and `row["status"].startswith("BUILT")` (`:259`). The conversion must lowercase every status
cell (else the inherited no-caps lint reds them) **and** repoint those two uppercase assertions in the
same delivery, or the family law the format adopts contradicts both the shipped matrix and its own suite.
The format doc does not name this seam.

State in the conversion section that status labels lowercase to *built/todo/retired* under the inherited
no-caps law, and that the two `test_traceability.py` status assertions repoint to the lowercase
vocabulary in the same delivery.

`defect · internal-conflict (consistency)`

## F4 — The new row lint duplicates the two facts the traceability suite already holds; one-home not reconciled

> "The lint reads every body row and reds a row that pins no level from the declared ladder, or that
> states no never side." — `docs/test-matrix-format.md`, "The row lint".

`tests/test_traceability.py::test_matrix_rows_have_level_and_negative_side` (`:235-249`) already asserts
exactly these two facts: `row["level"]` in `{string, DOM-text, browser-computed, pixel}` and `"never" in
row["fact"].lower()`. The format doc retires the hand-walked checkbox and moves the two facts to a new
mechanical "matrix row lint", but says nothing about the existing pytest that mechanically holds the same
two facts today — leaving two mechanical homes for one pair of facts (INV-101 one-home tension). Either
the row lint supersedes that pytest method (say so, and retire the method in the same delivery) or they
coexist (say why — for example the lint runs at every push while the pytest runs in the suite), but the
relationship must be stated, not left for the reader to reconcile.

Add one sentence to "The row lint" naming whether the lint replaces `test_matrix_rows_have_level_and_negative_side`
or coexists with it, and why.

`recommendation · boundary-issue (composition)`

## F5 — Retiring the checkbox gate is under-scoped: it has a caller and a document section the doc does not account for

> "the checkbox gate that read that checklist retires with it." — `docs/test-matrix-format.md`, "The row lint".

`guardrails/check-matrix-coverage.sh` reads the `## Coverage validation` section (`TEST_MATRIX.md:698`)
and is itself invoked by `tests/test_guardrails.py`. Retiring the gate therefore also owes: removal or
repoint of the `test_guardrails.py` invocation, a decision on the `## Coverage validation` section the
gate parsed (removed, or kept as prose with no gate), and — since the row-445 gates-plan filed this gate
under "coexisting unchanged" precisely because that migration did not touch the matrix
(`prototype/2026-07-22-spec-format/delta/gates-plan.md:87`) — an explicit re-classification for row 477.
None of these three are named in the format doc.

In the conversion section, list the retirement's fan-out: the `test_guardrails.py` caller, the
`## Coverage validation` section's fate, and the re-classification of the gate the row-445 plan pinned as
untouched.

`recommendation · hard-to-operate (ops-ux)`

## F6 — The Reference builder/gate sibling contract is coherent, but "the row's trailing anchor" understates compound and range anchors

The three faults the format doc's Reference gate holds (committed-differs-from-fresh-build; anchor on a
body row absent from the Reference; anchor in the Reference carried by no body row) mirror
`guardrails/check-index-generated.py`'s three faults (drift; body code missing from index; index code
carried by no body criterion) one-to-one — a clean, consistent sibling contract, and a genuine strength
of the design. One gap: the doc says the builder reads "each row's trailing anchor" (singular), but the
matrix's parent anchors include **compound** brackets (`[T-14, INV-19, INV-6]`, seen throughout the spec
criterion form) and **range** anchors (`T-1..T-7`, which `test_traceability.py::expand()` fans out at
`:17-23`). A builder that reads one token per row will under-cover a multi-anchor row's parents.

State in "The generated Reference" that a row's trailing bracket may carry several parent anchors and a
range form, and that the builder fans both out — mirroring `expand()` — before mapping anchor → rows.

`recommendation · over-general (abstraction)`

## F7 — The row lint's "never side" detection is unspecified; the suite's literal-"never" convention should be named

> "reds a row that … states no never side." — `docs/test-matrix-format.md`, "The row lint".

A mechanical lint cannot judge a regression fence semantically. The existing suite detects it by the
literal token — `"never" in row["fact"].lower()` (`test_traceability.py:244`) — which has trained all
410 rows to contain the word "never". If the new row lint detects the never side by any other rule, a
fence phrased "must not" or "rejects" without the literal "never" would red where the suite passes today,
and the two disagree. The doc leaves the detection rule blank.

State that the row lint detects the never side by the literal marker the suite already uses, so lint and
suite agree; if a richer detection is intended, name it and repoint the suite in the same delivery.

`recommendation · missing-outcome-check (postcondition)`

## F8 — ROADMAP row 477: the fence is stated, the success measure is derivable, an explicit non-goals sentence is absent

> "Done when the format definition stands in the family's docs and is proven; TEST_MATRIX.md reads in the
> new format with content preservation proven (word-token + punctuation multisets modulo named deltas);
> the generated Reference section's gate reds a hand edit (red-proven); the row lint reds a fixture row
> missing its level or never side (red-proven); and lints + cold reader reach two consecutive clean
> reads" — `ROADMAP.md:365`, row 477 Done-when.

Regression fence: **stated** — "content preservation proven (word-token + punctuation multisets modulo
named deltas)" is the restructure-safety fence (INV-111), correct for a content-preserving conversion.
Single success measure: **derivable** — "TEST_MATRIX.md reads in the new format with content preservation
proven" is the core measure, though it is not labelled as the one measure among the five Done-when
clauses. Non-goals: **absent** as an explicit sentence; the nearest derivation is the "content-preserving
vehicle" clause (implying the conversion changes no fact the matrix asserts) plus the scope to the matrix
member alone (implying the other family docs are out of scope). Per the movement-delta rule the non-goals
should be one stated sentence, not left to inference.

Add a non-goals sentence to row 477, e.g. "Non-goals: no change to what any matrix row asserts (content
preserving); no touch to the other family documents or to the spec's own format." Optionally mark which
Done-when clause is the single success measure.

`recommendation · missing-rule (invariant)`

## Mandatory step kept in CROSS-LINK

**Quantifier re-verify (INV-170): was PARTIALLY BLOCKED at the initial pass** (the spec body absent,
F1); the readable slice — `docs/spec-format.md` and `docs/test-matrix-format.md:3` — was clean. **Run to
completion at the re-read** on the restored body: see the Addendum below — R280.4 and the sharpened R282
enumeration are clean against the newcomer; the one hit is R124.2, filed as F10.

## Addendum — the spec-half re-read on the restored body (same date)

The body was restored (6902 lines; R283 at line 6469, R284 at 6498, R285 at 6522; the four glossary
entries at lines 129–131 and 224; the sharpened R282 Context at line 6456 now enumerating the
matrix-reference gate and the matrix row lint among the family's gates). The checks F1 blocked now ran;
the folds F2–F7 were each verified in `docs/test-matrix-format.md` and none contradicts the spec delta
text — R283.3/283.4/283.6 match the five-cell row, the lowercase statuses, and the stood-down
Context/User Story; R284.1's plural "trailing spec anchors" matches the builder's several-codes read.
One benign ripple noted without a finding: R285's Context says the lint runs "at every push" while the
format doc says "at every suite run" — compatible, since the suite runs at push.

**Quantifier re-verify (INV-170): COMPLETE.** R280.4 scopes the ratchet to `PRODUCT_SPEC.md` by name
and leaves TEST_MATRIX.md on the flat bound — clean against the newcomer. R282's family enumeration was
sharpened to include both new gates — clean. R277–279 and R281 scope to the spec document or state
member-neutral family law — clean. One hit: R124.2, filed as F10.

## F9 — One thing, two names: the ladder is "level ladder" in the glossary and "proof ladder" in the delta; and the delta's other new nouns lack glossary entries

> "a pinned test level drawn from the project's declared proof ladder" — R283.4 (PRODUCT_SPEC.md:6485);
> versus "**level ladder** — the ordered set of test levels a matrix row pins to" — glossary,
> PRODUCT_SPEC.md:124. The format doc also says "proof ladder" (docs/test-matrix-format.md:37).

The family's own law 2 (one name per thing, R277's Context: "every domain noun carries one glossary
entry under one name") is broken by the delta's ladder name, and "proof ladder" has no glossary entry.
Class sweep of the delta's other introduced nouns with no glossary entry: **node block** (R283.5),
**artifact inventory** (R283.2 and inside the test-matrix-format entry itself), **never side** (R285,
matrix-row-lint entry), **matrix-reference gate** (R284.2–5, R282 Context; the glossary defines the
sibling "delta classifier", so gate names are glossary-worthy by existing practice). One fix: rename
"proof ladder" to "level ladder" in R283.4 and the format doc, and add the four missing entries (or fold
"never side" into the matrix-row entry where it is already described).

`defect · internal-conflict (consistency)`

## F10 — R124.2 still demands closing the derivation with the coverage validation the delta retires

> "The system *shall* close the derivation with the coverage validation, confirming every spec anchor
> owns at least one row …" — R124.2 (PRODUCT_SPEC.md:2694, [E-15, INV-6]); versus "*when* the conversion
> delivery lands, the checkbox gate that read the coverage-validation checklist *shall* retire" —
> R285.3 (PRODUCT_SPEC.md:6537).

At the conversion delivery the `## Coverage validation` section and its gate retire (R285.3 + the format
doc's four-home fan-out), and R124.2's demanded closing artifact disappears while the criterion still
stands — a maintainer deriving a matrix after conversion is ordered to close with a checklist that no
longer exists. The facts survive (moved to the Reference gate and the row lint), so the fix is a declared
*sharpen* of R124.2 in the conversion delivery, naming the row lint and the matrix-reference gate as the
mechanical close of the derivation. The delta creates this timed conflict and should name the sharpen
now.

`defect · internal-conflict (consistency)`

## F11 — R285.2 hangs the reach-on-green obligation on a trigger under which there is no green line

> "*if* a body row states no never side, *then* the row lint *shall* red, naming the row, and *shall*
> state its reach on the green line." — R285.2 (PRODUCT_SPEC.md:6533).

Under the stated trigger the lint reds, so no green line exists to carry the reach; the second response
cannot follow from its own trigger. A reader building the lint from this criterion finds no criterion
stating the pass behaviour at all. Split it: R285.2 keeps the red-on-missing-never-side response, and a
new criterion states "*when* the row lint passes, it *shall* state its reach on the green line
[INV-274] [INV-269]" — the shape R282.1 gives the family.

`defect · undefined-path (transitions)`

## F12 — The retirement fan-out misses two family homes: test-author's step 8 and the matrix template

> "Close by walking the coverage checklist (the template ships it)" — skills/test-author/SKILL.md:58;
> and the template's retired shape: the six-column header `| ID | Fact (from spec) | Spec ref | … |`
> (templates/TEST_MATRIX.template.md:36) plus its `## Coverage validation` checklist section (:43–54).

The verified F5 fold names four homes (gate script, pre-push caller, test_guardrails.py, the matrix's
own section) but the skill that authors matrices still commands the hand-walk, and the template ships
the six-column row and the checklist to every new host — at conversion both teach the retired shape.
Add both to the conversion delivery's fan-out: repoint test-author step 8 to the row lint + Reference
gate close, and re-shape the template's row header and close (or state a decided sentence naming the
template's lag as deliberate for hosts not yet converted).

`recommendation · boundary-issue (composition)`

## F13 — The lint's pytest home versus the family's printed green line: name the mechanism

> "the lint holds them at every suite run … its home is the suite check … extended … it gains … the
> reach line on green" — docs/test-matrix-format.md, "The row lint" (the verified F4 fold); versus
> R282.1: the family's gates "print a green line" on pass.

A passing pytest method's stdout is captured in a quiet run, so the reach line a test prints on green is
invisible in the run a maintainer actually reads. Implementable (a terminal-summary hook, or printing
from the suite's tail), but the doc should name the mechanism in one sentence so the family promise
R282.1 makes is discharged rather than assumed.

`recommendation · unenforceable-promise (discharge)`

## What is working

- The family-inheritance framing is right: `docs/test-matrix-format.md` states only what is particular to
  the matrix and defers the shared laws to `docs/spec-format.md` by reference, so one law keeps one home.
- The Reference builder/gate sibling contract mirrors the spec's index builder/gate fault-for-fault (F6),
  a consistent and checkable design.
- The artifact-inventory requirement (every received file owns a row that asserts it at the rendered
  level) closes the ship-broken-while-green gap by construction, and matches the existing
  `test_artifact_inventory` that already parses that table.
