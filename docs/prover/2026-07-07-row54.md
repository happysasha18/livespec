# Prover record — 2026-07-07 ~08:19+, row 54 (onboarding step), CROSS-LINK mode, session 23

Previous record: pushgate-s23-2 (clean) — nothing unfolded rides.

Delta under review: SPEC B-3 (the pack learns WHO it works with at setup) + ADOPT orient insert +
base-rulebook insert + `templates/profile.template.md` (new) + matrix M-165 + `test_onboarding_step`.
Kind: skill — no visible surface, facets N/A by kind; the step's one face is an ask exchange, governed
by the existing decision-card laws.

| # | Finding | Verdict |
|---|---------|---------|
| F1 | Order collision with B-2: founding answers resolve "asked or profile-read", so the profile must be found BEFORE any founding question resolves — a step running after B-2 would read nothing | folded: B-3 opens with "before any founding question resolves"; ADOPT names it the first breath of orient |
| F2 | INV-9 hazard: a template shipping plausible example values could pass for the human's word after a wipe | folded: every placeholder marked as a placeholder; the template's own notes say each line traces to the human's word; test asserts the marking |
| F3 | Worker seam: a worker session must never run the step (it cannot hear the human; its settings come from the brief, ACT-3) | folded: the stand-down named in all three homes (SPEC, base, ADOPT) |
| F4 | Loading an old profile under a newer pack: unknown lines | folded by citation: E-13's ignored-ALOUD law reused, no second mechanism |
| F5 | Done-when leg 3 ("first real run recorded") cannot land from the package window | recorded: field leg stays OPEN per INV-26 — the row does not close; the landing report names legs met vs open |

Open ⟨DECIDE⟩ touched by this delta: none (D-1/D-2/D-3 untouched).
Verdict: fold-complete; proceed to architecture assignment and the matrix.
