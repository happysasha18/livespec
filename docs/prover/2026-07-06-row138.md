# Prover record — 2026-07-06, row 138 (offline window), CROSS-LINK

Scope: the offline-window delta (SPEC v0.15.38 — INV-35 scenario + invariant row) against its named
neighbours: the three narration teeth (INV-35), narration-asks-nothing (INV-31), the echo (INV-27),
the bookkeeping law (INV-28), ask/batch (INV-4), the heartbeat ~10-min default. Previous session-20
records checked: no unfolded rows.

Opening assessment: the delta lands as a second, forward-looking face of the existing heartbeat tooth
rather than a fourth tooth — the right shape: the three-teeth structure (and its suite pin) stays
intact, and the fence sentence row 139 left for this territory is exactly the socket being filled.
Honest-range language ("unknown said as unknown, never a guess dressed as a promise") discharges the
roadmap row's own honesty clause. Two seams needed folding; both folded in-pass. Ready to build.

| # | Finding | Severity · category | Status |
|---|---|---|---|
| F1 | The window's end was one-sided: only OVERRUN was spoken, yet a window also ends early — work done sooner, or blocked on a decision only the human can make. He returns on the spoken range and finds the work stopped twenty minutes ago with no line saying why. | should-clarify · undefined-path (transitions) | FOLDED — "a window that ends off its spoken range says so — overrun, done sooner, or blocked on his word alone" (scenario + invariant row) |
| F2 | "When he IS needed again, that too is a beat" read as a promise to REACH him — but a chat line does not summon an absent human; the law would promise what the medium cannot deliver. | should-clarify · unenforceable-promise (discharge) | FOLDED — "the needed-again beat is a chat line awaiting his return, never a summons (the machinery of reaching an absent human stays outside this law)" |
| F3 | Class sweep (sibling instances): the superseded fence sentence "…is the offline-window face — its own promised law (queue row 138), not this rule" lives in TWO more homes — communicator SKILL.md in the repo and its installed copy. Left standing they would contradict the live law. | note · consistency | COVERED — the code station replaces both copies in the same landing; suite string-row asserts the shipped clause |

Cross-links that hold without change: INV-31 — the offline sentence is a statement, batched questions
ride INV-4's existing law, narration still asks nothing; INV-27 — the echo is untouched; INV-28 — a
time range here is content, not bookkeeping (the never-list names test counts / suite sizes / version
strings, not durations); heartbeat default — beats, including heartbeats, keep landing inside the
window ("a read, never a dismissal"), so the two faces of the tooth compose.

⟨DECIDE⟩ open elsewhere: D-1/D-2/D-3 — none touched by this delta.
