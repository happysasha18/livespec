# Prover record — 2026-07-06, rows 129+132 (CROSS-LINK), session 16

Mode: CROSS-LINK on SPEC v0.15.26 delta — INV-36 (the project knows its own kind) + INV-37 (every
wish placed on the feature map at intake) — against the seams they compose with: INV-27 (capture
echo), T-16 (per-wish work-kind + host default), B-2 (founding questions), E-13 (settings ladder),
E-14 (architecture stays iterative), T-9/T-11 (lane order), T-14 (fences precedent for greppability).

Previous record checked: `2026-07-06-row131.md` — no unfolded rows carried over (F4 there was a
deliberate rejection, reason recorded).

## Opening assessment

The delta makes an implicit mapping explicit and gives it a voice — low structural risk because the
map deliberately reuses two existing surfaces (spec scenarios, architecture nodes) instead of opening
a third document, and the restructure verdict correctly refuses to re-carve outside a landing. The
seams that needed attention were all of the "two homes for one enumeration" family. Ready to build
after the four folds below, all folded in-pass.

## Findings

F1 — The echo's field list would have lived in two places

> "one plain sentence — what was heard, the door called, the name the work will answer to, its row
> number" — Throwing a wish / INV-27

A reader (or a worker briefed) off INV-27 alone builds a four-field echo and the placement arm
silently drops — exactly the enumeration drift INV-28 already fought once. INV-27 must point at the
fifth part's own home.

Folded in-pass: INV-27 paragraph + its index row now carry one pointer clause to INV-37; the law
itself stays in INV-37 (one home). Code step must sweep EVERY echo enumeration (communicator rule 12,
build-pipeline step zero, M-111/M-112 pinned strings) — the sibling-instances lens, handed to the
implementation brief.

`must-fix · boundary-issue (composition)` — **FOLDED**

F2 — Two host-profile lines could fight over the intake default

> "seeds project-wide defaults (the usual work-kind…)" — Bootstrap / INV-36 (as first drafted)

A host carrying both `project.kind: fullstack app` and an explicit `work-kind.host-default: skill`
had no stated winner; the classifier would guess, which INV-12 forbids.

Folded in-pass: INV-36 now states precedence — the seed proposes, a written line disposes; the
project kind never silently overrides an explicit profile line.

`must-fix · internal-conflict (consistency)` — **FOLDED**

F3 — A spoken-only placement verdict evaporates

> "the placement is spoken, out of the box" — Throwing a wish / INV-37 (as first drafted)

Once the echo scrolls away, nobody can grep which feature a landed row belonged to; the fences set
the precedent that intake verdicts live in the row.

Folded in-pass: INV-37 now requires the row's `map:` note (changes X · new · restructure) beside the
spoken echo.

`should-clarify · hard-to-monitor (observability)` — **FOLDED**

F4 — B-2's profile-seeding arm cannot answer a host question

> "A founding answer resolves like any setting [E-13]: the human's profile answers when a line covers
> it" — Bootstrap / B-2

personal-vs-reusable can be profile-seeded (Alexander's standing "reusable" line proves it);
`project.kind` never can — no personal line says what a HOST is. Left unstated, a session could
"seed" a kind from the personal profile and skip the ask.

Folded in-pass: INV-36 states the ask is always the human's, asked at founding or orient every time;
its index row says "always asked, never profile-seeded".

`should-clarify · missing-prerequisite (precondition)` — **FOLDED**

## Verified seams (no finding)

- Silent arrivals: the placement rides the echo wherever the echo lawfully happens (next report for
  inbox/harvest wishes) — INV-37 inherits INV-27's mechanics by composition, no restatement needed.
- Lane order: a restructure row is a normal queued row (T-11); placement never grants preemption —
  queue-cutting stays with the bug door (T-9, INV-16).
- Door consistency: "refactor when only structure moves, feature when behaviour moves too" matches
  the door procedure's own definitions (INV-16 arms 1 and 3).
- E-14 composition: the re-carve pointer adds no second law — re-prove-on-structure-change was
  already E-14's own sentence; the pointer only names the new legal entrance.
- Forward binding: both laws bind forward (INV-36's no-retro-ask, T-16 precedent cited); no landed
  row owes retro placement.

## Summary

| # | Finding | Severity | Status |
|---|---|---|---|
| F1 | echo enumeration needs the INV-37 pointer | must-fix | folded in-pass |
| F2 | project.kind vs work-kind.host-default precedence | must-fix | folded in-pass |
| F3 | placement must be written in the row, not only spoken | should-clarify | folded in-pass |
| F4 | project.kind is never profile-seeded | should-clarify | folded in-pass |

Open ⟨DECIDE⟩ count untouched by this delta: D-1 / D-2 / D-3 remain open, none touched by these
surfaces.

Verdict: ready to build.
