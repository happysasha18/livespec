# Prover cross-link — SPEC humanize: "Throwing a wish" CHUNK 5 (2026-07-07, session 25)

CHUNK 5 = seven bold-leads: A review surface shows its sources and takes his pen · His word on a shown
artifact is read as meant · Approved text is frozen · The kill-list has a mechanical face · No question
is asked twice · A taste ask carries the agent's own researched proposal · Priority bends the lane order.

Gates (whole section after splice): 157/157 tested phrases section-scoped; code multiset identical to
baseline (165 occurrences, zero diffs); full suite 175 green. Chunk 5 carries 15 tested phrases and
13 code occurrences, all preserved.

Two case-sensitivity slips caught+fixed (the whole-section verify flagged them, suite red before commit):
"a question a record already answers is a DEFECT" and "round N+1 carries only new material" were both
capitalized at a sentence start; reworded to keep them lowercase. No commit happened until green.

## Facts carried (all KEPT)
- Review surface [INV-64]: per-claim provenance (read from artifact / his word / inference), inferences
  flagged LOUDEST; COMMENTABLE, never a read-only wall; saved-answers law [INV-32] extends to review pages.
- His word read as meant, cuts hold [INV-42]: killed phrasing stays killed in every later draft, kill-list
  written where the project keeps records, never only in session memory; a vivid phrase adopted only as
  meant (mockery of a bad draft, not guidance), intent read or asked [INV-4]; two bans cross-linked
  (no-disclaimers; silence is consent [INV-31]).
- Approved text is frozen [INV-58]: a revision applies only the named correction, never a fresh rewrite
  around it; churn is a defect kin of a resurfaced cut [INV-42].
- Kill-list mechanical face [E-26]: dated-literals template + scanner test; a killed literal reappearing
  turns the suite red; "this is its teeth".
- No question asked twice [INV-59]: search the recorded word first; a question a record already answers is
  a DEFECT; exchanges converge — an answered question closes forever, a named problem returns solved,
  round N+1 carries only new material.
- Taste ask [INV-60]: never arrives empty-handed; mine first, ask with a recommendation; sharpens the
  recommended-option law [INV-4].
- Priority bends the lane order [T-11]: critical bug preempts, critical priority heads the queue, only the
  bug door preempts the in-work lane [T-9]; quick-win bubble-up with anti-starvation; inbox arrival is the
  harvest moment, ties by queue row order.
- Three Russian quotes rendered as plain-English paraphrases (promoter case); no new quotation introduced.

Verdict for chunk 5: **CLEAN** (after two caught-and-fixed casing slips). Whole-section gates green.
