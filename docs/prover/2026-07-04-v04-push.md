# Prover pass — SPEC v0.4 (use-case-first restructure), push gate (2026-07-04, third pass this date)

Mode: FULL (whole spec), run as the push gate (M-6) before pushing v0.4 + the day's queued commits.
Context: v0.4 is a pure RESTRUCTURE of v0.3 (queue row 22, Alexander's OK on the sample shape) — scenarios
lead, codes trail as anchors, a formal index closes the document. The v0.3 content was FULL-proven twice
today (2026-07-04.md, 2026-07-04-v03-push.md); this pass therefore probes what a restructure can break:
loss, drift between prose and index, and reading order — not the already-proven semantics.

Triage: PROCEED. Mechanical guard ran first: the anchor SET of v0.4 is byte-identical to v0.3
(49 anchors, grep-extract + diff — recorded in the session transcript). No rule lost or added silently;
two deliberate additions this landing (M-2 self-compaction wording per Alexander; A-7 breakpoint re-stat,
row 25) are queue-cited, not silent.

## Opening assessment

The restructure holds. Every v0.3 semantic clause was located in v0.4 during the pass (spot-walk of all
11 invariants, 10 transitions incl. the T-1..T-7 range, 11 entities, adoption A-0..A-7, milestones
M-1..M-6): meanings intact, several now clearer in scenario voice. The two real risks of the new shape —
a section landing in the wrong reading position, and the index becoming a second truth — both surfaced
and both folded. Ready to push.

## Findings

F1 — Package-internal governance interrupted the product story
> "## The package repo: who may write…" originally placed between "When a bug cuts the line" and "Starting a new project"
A first-time reader (the README invites the public in) met write-ownership/fence rules for livespec's OWN
repo before learning how to bootstrap or adopt a project — internal kitchen ahead of the product.
Fold: section moved after "The machines that hold the bounds", next to the rhythm/self-application
material where M-4 lives.
`should-clarify · confusing-for-users (cognitive-load)` → **FOLDED** (section order)

F2 — The formal index is a second home for meanings and can drift
> "| INV-1 | no wish is ever lost | Throwing a wish |" — Formal index
The index's one-liners paraphrase the prose. Six months of landings edit the prose and forget the table —
exactly the two-synced-copies failure Alexander rejected, smuggled back in as a table.
Fold: index header states it is "only the map"; M-1 milestone now re-checks the index against the prose
(a derived map, never a second truth).
`should-clarify · internal-conflict (consistency)` → **FOLDED** (index note + M-1)

F3 — D-1 revisit trigger was stale after the first real adopt run
> v0.3: "revisit at the first real adopt run" — Open decisions, D-1
The first real run happened today (tlvphoto) without a D-1 revisit; the trigger as written had already
expired. Rewritten to "next real adopt run" — a factual refresh, surfaced here rather than buried.
`worth-considering · missing-outcome-check (postcondition)` → **FOLDED** (D-1 wording)

F4 — README described only the formal frame of spec-author's output
> "Write or grow SPEC.md: entities, states, transitions…" — README, pipeline step 1
True but now incomplete: the document SHAPE (use-case-first, anchors trailing, index) is a product claim
of the package and was stated nowhere public. One sentence added to README step 1 naming livespec's own
SPEC.md as the reference shape. Full propagation to the template + spec-author skill is row 23, strictly
its own landing.
`worth-considering · over-general (abstraction)` → **FOLDED** (README, one sentence)

## Closing

Anchor guard: identical sets (49). All four findings folded in-session (the gate's own folds ship with
this record per M-6). Nothing new for the queue beyond already-filed rows 19–21, 23, 24. Readiness:
ready to push.
