# Prover record — 2026-07-06, row 139 (CROSS-LINK), SPEC v0.15.31 delta

Scope: the INV-35 working-narration paragraph's three new teeth (identity / digest / heartbeat) +
the grown Formal-index INV-35 row. Seams walked: INV-27 (echo), INV-28 (bookkeeping never content),
INV-31 (no questions), INV-34 (pre-report walk scope), row 138's promised offline-window law,
T-18/INV-39 (two-lane narration), INV-24 chat face (stamps on beats).

Previous record: docs/prover/2026-07-06-row136.md — no unfolded rows ride.

## Opening assessment

The delta is small and lands where the law already lives; the three teeth sharpen an existing
invariant rather than adding a surface, so most seams close on sentences the paragraph already
carries ("asks nothing", "every law of human-facing lines still binds", the explicit row-138 fence).
The bookkeeping seam — the riskiest one, since "digest the test station" naively invites test-count
dumps — is closed in the delta's own words ("in the work's own words", "token and test counts stay
bookkeeping"). Two genuine under-specifications and one over-specificity found; all three are
one-phrase folds. Ready to build after the folds.

## Findings

| # | Finding | Severity · tag | Status |
|---|---|---|---|
| F1 | Two-lane composition: a station completed by a lane-B WORKER produces no beat by itself — the worker never speaks to the human; unstated, the digest law silently under-delivers exactly when T-18 runs two trains. Fold: the delegated station's completion is the senior's beat to say when its result lands. | should-clarify · unclear-owner (actors) | FOLDED — sentence added to the DIGEST tooth |
| F2 | "When a stretch runs long" has no threshold — unenforceable as written, and the human's own complaint named the scale (half an hour). Fold: a [default] mark — a beatless stretch past ~10 minutes owes its heartbeat; told at landing as a tweakable default (INV-31), never asked. | should-clarify · missing-rule (invariant) | FOLDED — [default] threshold added to the HEARTBEAT tooth |
| F3 | Identity tooth says "which pipeline station" — over-specific for work with no stations (research, docs-only, harvest): read literally, those beats can't comply. Fold: outside the pipeline the work's own name serves. | worth-considering · over-specific (abstraction) | FOLDED — parenthetical added to the IDENTITY tooth |
| F4 | Working well, noted: identity-per-beat is exactly what makes T-18's two-train chat readable — interleaved beats disambiguate by naming their lane's wish; no change needed. | — | — |

## Coverage notes

CRUD / authorization tables: N/A — the surface is a chat exchange shape, no persistent entities, no
roles. Invariant coverage: the three teeth all live inside INV-35's one home; no second home created
(index row restates, which is the index's job). No acknowledged gaps (no TBD in the delta).
