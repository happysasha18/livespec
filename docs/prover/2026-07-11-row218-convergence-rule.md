# Prover — row 218, the convergence principle enters the base rulebook (2026-07-11, short form per INV-61)

Small delta (kind: skill, no new surface; the architecture touch is an anchor assignment, no
structure change) — the three-line short form.

- **Previous records:** 2026-07-11-row249-hook-no-scissors clean (0 must-fix); no unfolded rows in
  any earlier record.
- **The delta in one line:** the principle stated once — every piece of work names its goal up front
  as an artifact the work can be held against (frozen norm, exemplar bank, failing test, written
  acceptance); a paraphrase cannot serve as the goal; every iteration measures distance to the goal
  itself and a proxy never replaces the goal; the distance only shrinks and reached levels lock by a
  mechanism; divergence only as a named, bounded stretch — in the spec (INV-98, "What holds the
  bounds", where the machines it commands live) and as base rule 22 (citing the playbook chapter and
  the rows-216/217 teeth), the playbook chapter pushed to its own private repo the same hour.
  Composition read: rule 16's labelled prototype IS the named divergent stretch (cited in-rule);
  rules 11/13 (verify by deed, primary sources) are the proxy ban's kin, none weakened; the
  stated-once law holds (working skills reference, none restates); INV-98 owned by the base-rulebook
  node, M-234 sits under that node's block; the seven working skills' base pins moved in lockstep to
  v1.0.2 (the trace suite enforced both placements — two briefs' holes it caught are in the landing
  accounting).
- **Verdict:** ready to ship — 0 must-fix, 0 should-clarify.
