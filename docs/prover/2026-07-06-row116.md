# Prover record — row 116 (outcome-leads law, INV-28), CROSS-LINK pass

Date: 2026-07-06 ~10:25, session 12. Scope: the new INV-28 clause against its named neighbours —
INV-27 (echo + departures board), base rule 2 (plain words / no calques), communicator rules 6/8/9,
profile `language.no-calques`, E-22 (uninformed click). Mode: CROSS-LINK (one new clause on an
existing surface; FULL pass not owed — no MINOR gate here; the next push's full pass covers it).

| # | Finding | Severity | Folded / rejected |
|---|---|---|---|
| F1 | INV-28's line-shape arm ("the outcome leads, names trail") collides with INV-27's board law ("every in-flight feature is named BY ITS NAME with its station") when the echo-name itself is a private metaphor — the board would either lead with a riddle or bury its own naming law | must-fix | FOLDED: INV-28 gains a NAMING arm — an echo-name is a short DESCRIPTIVE phrase in the product's words, parseable cold; with names plain, the board leading with a name IS leading with the meaning; the two invariants compose instead of colliding |
| F2 | Unfenced scope would outlaw internal anchors everywhere — but the pack's own English method docs (SPEC, ROADMAP, matrix, prover records) are OPERATOR-facing: their anchors are the mechanism | must-fix | FOLDED: the law names its surface — HUMAN-FACING report surfaces (chat reports, report pages, decision pages, the capture echo); method-internal docs keep their anchors; stated as a non-goal in the row |
| F3 | "A name the reader never chose to learn" is judgment, not mechanics — the string test can only assert the clause SHIPPED, not that future reports obey it | worth-considering | RECORDED: the eval's with-skill arm scores it at the next re-run (criterion added); the real gate is the next report's reader — same honesty boundary as all guardrails (structure mechanical, meaning by prover/human) |
| F4 | Checked E-22, decision-page rule 10, no-calques: no contradiction — no-calques governs terms CROSSING the language split; INV-28 governs coinage in ANY language (the failing nicknames were native Russian) | none | — |

Regression fences (T-14): INV-27's echo sentence and station vocabulary untouched — discharge through
existing rows M-111/M-112 (their never-sides already pin the board's shape).

Non-goals: no rewrite of already-read past reports (the record stands; the translation lives in chat
and the journal); no change to method-internal docs' anchor style. Success measure [default]: the next
morning-style report draws zero "а это что?" from its reader.

Open ⟨DECIDE⟩ touched by this surface: none (D-1/D-2/D-3 untouched).
