# Prover record — 2026-07-07 ~09:32, row 165 (skill discovery), CROSS-LINK mode, session 23

Previous records: pushgate-s23-3 (clean), row54 (fold-complete) — nothing unfolded rides.

Delta under review: SPEC INV-65 (search for an existing skill at setup and at struggle, with the
borrowing practice) + base rule 20 + ADOPT setup line + matrix M-166 + `test_skill_discovery`.
Kind: skill — no visible surface, facets N/A by kind; the law's one face is a proposal exchange,
governed by the existing ask-with-recommendation laws.

| # | Finding | Verdict |
|---|---------|---------|
| F1 | Overlap risk with the researched-proposal law (a taste ask arrives carrying the agent's own proposal): both make the agent search before asking | folded by kin-statement: that law shapes HOW an ask arrives; this one names WHEN a skill search fires (setup, second-occurrence struggle) — cross-cited, no second home |
| F2 | INV-9 seam: adopting a skill changes how the pack works for the human — must stay proposal-only | folded: "the human's word picks" in the clause and the rule |
| F3 | Catalog reach varies by machine/session (no network, no marketplace) | folded: the clause says "the catalogs it can reach"; an unreachable catalog is simply absent from the scan, and the proposal names what was scanned |
| F4 | Borrowing practice could drift into republishing licensed text | folded: the practice closes the clause — invoke as shipped · paraphrase + named credit · verbatim only under license, notice kept, unlicensed never republished |
| F5 | Done-when's field leg (first real struggle run) cannot land from this window | recorded: field leg stays OPEN per the whole-row law — the row does not close |

Open ⟨DECIDE⟩ touched by this delta: none.
Verdict: fold-complete; proceed to the matrix and the build.
