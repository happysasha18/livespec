# Prover pass — SPEC v0.14.0, FULL milestone/push gate for the 0.8.0 milestone (2026-07-05, v14-push)

Mode: **FULL** (whole spec, every phase, all NINE stress lenses). This is the M-6 push-gate record for the
live-spec **0.8.0** milestone AND the FULL re-prove that **discharges the accumulated MINOR-under-CROSS-LINK
debt** carried by the four deltas landed since the last FULL pass:

- **-facets** (T-13/INV-18) — debt noted at `2026-07-05-facets.md` F4, itself already discharged by the
  v0.11.0 FULL pass (`2026-07-05-v11-push.md`); relisted here for the audit trail.
- **-fences** (T-14/INV-19, 0.11.1 → 0.12.0) — `2026-07-05-fences.md`, 8 findings all folded, FULL owed.
- **-intake-trio** (T-15/INV-20/INV-21, 0.12.0 → 0.13.0) — `2026-07-05-intake-trio.md`, 9 findings all
  folded, FULL owed.
- **-founding-designsync** (B-2/E-18, 0.13.0 → 0.14.0) — `2026-07-05-founding-designsync.md`, 7 findings all
  folded, FULL owed.

**This pass is the FULL discharge for all of the above.** Per each record's process note, the CROSS-LINK
passes proved their own delta against the existing surfaces but could NOT see the deltas' INTERACTIONS WITH
EACH OTHER (they landed in sequence, each blind to the ones not yet written or not in its own scope). That
gap is this pass's primary target, alongside the whole-doc property sweep the MINOR gate requires.

## Phase 0 — Triage

**TRIAGE: PROCEED.** A method spec with full operational content — entities (wish, queue row, spec-delta,
matrix, the machines), a lifecycle with transitions (arrived → classified → drafted → validated → queued →
in-work → landed / declined / deferred / superseded), actors (human, senior, workers), and stated
invariants. Analyzable.

**Conditional-on-pins note (Phase 0 rule).** The header claims some surfaces shipped ("the five skills … the
first guardrails slice … installed and tested"). Those shipped claims were reconciled against code at
`2026-07-05-architecture.md` and are unchanged by tonight's four deltas (all four are METHOD rules, none
flips a shipped/[target] tag except E-18, correctly parked [target]). Findings below rest on the spec's own
prose for method rules; the shipped-state claims remain conditional on the architecture doc's pins staying
current, as at every prior push.

## Opening assessment

The spec is trying to state a complete, memory-wipe-durable working discipline: wishes enter a proven
pipeline, machines hold the bounds, the human is interrupted only for genuinely-human calls. It is in strong
shape. The four tonight deltas compose **cleanly and in the right order**: for a feature touching a live
surface the spec-delta now has a coherent fixed shape — it OPENS with regression fences (T-14, before the
sweep), MIDDLE walks the facet sweep authoring new visible surfaces (T-13/INV-18), and CLOSES with non-goals
+ a success measure (INV-20/INV-21) — with appetite (T-15) tuning richness at intake. All four correctly
route their human-facing output to the ONE existing batched report rather than inventing a channel, and all
four carry the prototype carve-out and the adoption/first-touch scoping. Every one of the 24 folds from the
four CROSS-LINK records reads as folded in the current prose (verified below).

The one thing that needs attention is a single cross-delta seam the sequential CROSS-LINK passes structurally
could not catch: **T-15's "what a trim may cut" enumeration protects only the facet sentences, but the same
feature spec-delta now carries three OTHER mandatory-sentence obligations** (fences, non-goals, success
measure) that a "half-day" appetite reader could think fair game to skip — fences most dangerously, since
they are the regression-safety net, not a richness dial. That is F1 below, a should-clarify, not a blocker.

Overall confidence: **ready to build / ready to push.** Zero must-fix. The delta interactions hold; the
holes are one enumeration-scope clarification and two minor ops/observability notes.

## Phase 1 — The model (unchanged since v0.11.0; delta-touched parts re-checked)

Entities and lifecycle are as proven at v0.11.0 and reconfirmed here. The delta-touched region is the
**feature spec-delta** sub-entity. Its composed contents, after four deltas:

- Regression fences (T-14) — one sentence per neighbouring live promise, citing its clause; opens the delta.
- Facet sentences (T-13/INV-18) — one per standard facet of each new VISIBLE surface (or the explicit "no
  visible surface — facets N/A" for a headless feature).
- Non-goals (INV-20) — always one sentence ("nothing left out this time" is valid).
- Success measure (INV-21) — always one, decided or `[default]` (provenance-only tag, no matrix row yet).

Actors unchanged: human (taste/gates/founding answers), senior (judgment/deltas), workers (mechanical).
B-2 adds no actor — it constrains WHEN the first wish may walk. E-18 adds a machine, not an actor.

**What I assumed.** I read the feature-spec-delta ordering as fences → facets → non-goals/measure (T-14
says fences precede the sweep; INV-20/21 say the pair "closes" the delta) — this ordering is implied across
three clauses, never stated in one place (Phase 4 note). I treated the pack itself (a text product) as the
running host for this gate, so the visual-only machines (E-7 snapshot, E-18 design-sync) are [target] and
never fire here. I took "every feature" in INV-20/INV-21 as already scoped by their own bind-forward clause
(F7 fold in -intake-trio), so no retroactivity finding recurs.

## Phase 2 — Structural issues in the model

No new structural defect. The four deltas add rules to an existing entity (the feature spec-delta) without
introducing a new unowned state or a competing actor. The one structural observation is that the feature
spec-delta's full required contents are now distributed across five clauses (T-13, T-14, INV-18, INV-20,
INV-21) with no single assembly point — carried to Phase 4 as an ops-ux note (F3), not a model defect, since
the assembled checklist's proper home is the spec-author skill, not this use-case-first spec.

## Phase 3 — Property analysis (whole-doc sweep + cross-delta interactions)

The whole-doc safety/liveness/consistency sweep reproduces no regression against the v0.11.0 FULL baseline:
the lane-token atomicity (INV-2/INV-11), bug-preempt/park/resume ordering (T-9/T-11), the door tripwire
ordering (INV-16), the settings ladder (E-13/INV-14), and the attic/no-delete law (INV-7) all still hold and
are untouched by tonight's deltas. The live findings are the cross-delta INTERACTION seams — the material
the sequential CROSS-LINK passes could not see — plus the ops notes.

| # | Finding (one line) | Severity | Outcome |
|---|---|---|---|
| F1 | **Appetite's "what a trim may cut" protects only the facet sentences — it is silent on the THREE other mandatory-sentence obligations the same feature spec-delta now carries (fences, non-goals, success measure).** T-15 (L68–70): "What a trim may cut: which surfaces are in, and how considered the defaults are … — never whether a **kept surface's facets get their sentences**." This enumeration was written (intake-trio F9) when facets were the only silence-not-an-option class in a feature delta. Tonight T-14 makes **fences** mandatory ("one sentence per neighbouring promise that must stay true"), and INV-20/INV-21 make **non-goals + success measure** mandatory ("both are ALWAYS written … silence is not a legal state"). A senior drafting a "half-day" wish reads "scope bends to fit the appetite" and sees explicit protection ONLY for facets — so a touched surface's regression **fences** (the safety net, not a richness dial) or the closing pair could be read as trimmable to hit budget. The result: a live-surface-touching feature ships under-fenced, and "fixed one thing, quietly broke the neighbour" (the exact failure T-14 exists to stop) returns — introduced by an appetite trim T-15 appears to license. This is the base-rule-14 sibling sweep the intake-trio F9 fold left half-done: it protected the facet class, not the mandatory-sentence CLASS. Fix: generalize T-15's protected clause from "a kept surface's facets get their sentences" to the whole class — "never any mandatory-sentence obligation: a kept surface's facets, its neighbour **fences** [T-14], the feature's **non-goals** and **success measure** [INV-20/INV-21] — appetite tunes only which surfaces are IN and how considered a DEFAULT is, never whether a required sentence is written." | should-clarify · over-specific (abstraction) | **FOLDED**: T-15 protected clause generalized to the whole mandatory-sentence class (fences, facets, non-goals, success measure) — appetite dials richness, never the safety net |
| F2 | **The batched-report load doubles as a silent-decision surface for four decision classes, but nothing states its completeness obligation as one rule.** The decide-time batched report now carries: genuinely-human questions (INV-4), taken facet defaults as tradeoffs (INV-18), appetite trims (T-15), scope-narrowing non-goals (INV-20), and success-measure quantification questions (INV-21) — five clauses each independently say "rides the same batched report." The routing is consistent (no channel drift — a genuine strength), but no single clause states the report's **postcondition**: that it is COMPLETE — every non-wish choice of every class present, so "no silent micro-decisions" (INV-5) is a checkable property of the report, not an aggregate a reader must reconstruct from five homes. Today a report missing (say) an appetite trim is only caught by cross-reading T-15; nothing makes report-completeness one assertion. Fix: add one sentence at INV-5 (the class home): "the batched report is COMPLETE by construction — every choice not in your wish, of every class (facet defaults [INV-18], appetite trims [T-15], scope-narrowing non-goals [INV-20], measure questions [INV-21]), appears in it; a class present in the delta but absent from the report is the silent micro-decision this rule forbids." | should-clarify · missing-outcome-check (postcondition) | **FOLDED**: INV-5 states the batched report's postcondition — a decision absent from the report is silent by definition |
| F3 | **A feature spec-delta's full required contents are stated in five separate clauses with no single assembly point — a senior can omit one obligation without any clause catching it.** After tonight, a feature-doored delta touching a live surface must contain: fences (T-14), facet sentences (T-13/INV-18), non-goals (INV-20), success measure (INV-21) — spread across five anchors, ordered only implicitly (T-14 "before the facet sweep"; INV-20/21 "closes with"). A senior assembling a delta from memory can drop the success measure or the non-goals and no clause in the reading path flags the omission at authoring time (only a later whole-spec prover pass would). This is not a spec DEFECT (the use-case-first spec deliberately teaches through scenarios, not a template), but the assembled ordered checklist has no named home. Fix: name the home in one half-sentence — "the assembled order and completeness of a feature spec-delta (fences → facet sentences → non-goals → success measure) is the spec-author skill's checklist; each clause here binds its own piece" — so the composed template is owned once (INV-13), where the author will actually reach for it. | worth-considering · hard-to-operate (ops-ux) | **FOLDED**: spec-author gains "The feature delta, assembled" — one ordered home for the delta's mandatory parts (INV-13 held: it cites, never restates) |

**Interaction seams checked and found CLEAN (no finding — recorded so the audit shows they were walked):**

- **T-15 (appetite trim) × T-14 (fence/re-author split).** An appetite trim that drops a surface from the
  change could, in principle, move that surface from "re-authored" to "fenced" (it now must STAY true rather
  than change). No defect: intake is sequential — appetite trims scope at intake, THEN the spec-delta is
  drafted and computes the fenced-vs-changed split (T-14) over the already-trimmed scope. No recomputation
  race; the split naturally operates on final scope.
- **INV-20 (non-goals) × T-14 (fences).** A non-goal ("not touching the catalog") and a fence ("catalog
  still opens on click") can name the same neighbour without conflict: the non-goal declares out-of-scope,
  the fence guards its survival against change elsewhere — complementary registers, never a contradiction.
- **INV-21 (success-measure `[default]` = provenance only, no row) × E-15 coverage ("every spec anchor
  appears in ≥1 row").** No collision: E-15's anchor-coverage walk applies to the METHOD anchors in the
  pack's own matrix (row 50), not to a per-feature success measure; the F6 fold already scoped the reused
  tag. Reconfirmed against the closing prose.
- **B-2 (founding blocks the first WISH) × T-10 (inbox swept as FIRST act).** No deadlock: T-10 HARVESTS
  inbox files into rows (durable capture); B-2 blocks the first wish from WALKING until the founding answer
  is asked or profile-read. Harvest and block operate on different transitions (row-creation vs lane-walk).
- **E-18 (design-sync syncs the DECLARED components) × E-7 (snapshot advances by DECLARED scope) × T-14
  (fenced vs changed).** Aligned by the F4 fold: design-sync reuses E-7's declared-scope set; fenced
  neighbours are NOT declared (their baseline stays — the asymmetry that catches unasked change), so
  design-sync correctly shows only changed/declared components. Consistent.

### Fold verification — all 24 CROSS-LINK folds read as folded

Every fold from the four delta records was re-checked against the CURRENT prose; all present and correctly
worded:

- **-fences (8/8):** F1 fence earns NO new row, discharges through cited clause's never-side (L148–149) ·
  F2 order pinned, fenced-xor-re-authored split (L145, L151–153) · F3 fences ride the queue row by anchor,
  LANDING line stays one line (L156–158) · F4 unwritten promise reconciles from shipped truth like A-3,
  own row, surfaced (L153–156) · F5 adoption-born unverified neighbour reconciled before fencing (L156) ·
  F6 bug/refactor inherit only the catching (L158–160) · F7 prototype fences nothing (L160) · F8 fences
  greppable by anchor, completeness guardrail rides E-10 (L157–158).
- **-intake-trio (9/9):** F1 trim proceeds on recommended, surfaced, re-widen=new wish, no park (L65–70) ·
  F2 appetite = optional rider, T-12 enumeration updated (L62–64, L83–85) · F3 bends scope not order,
  half-day ≠ quick win (L64–66) · F4 non-goals always written, "nothing left out" valid (L163–166) · F5
  scope-narrowing non-goal rides batched report (L166–167) · F6 success `[default]` = provenance only, no
  row (L169–172) · F7 both bind forward, adopted owes at first touch (L174–176) · F8 prototype writes
  neither (L176) · F9 trim cuts surfaces + default consideredness, never facet sentences (L68–70).
- **-founding-designsync (7/7):** F1 design-sync SUPPLEMENTS the render, render stays authority (L468–471)
  · F2 switch + off-default land with the machine, toggle-on = recorded profile line (L471–472) · F3
  dedicated queue row 93 (L472) · F4 footprint = declared-scope (E-7) (L469–470) · F5 founding = deliberate
  STRENGTHENING, blocks first wish (L249–252) · F6 resolves like a setting (E-13), profile line SEEDS,
  said aloud (L244–247) · F7 A-1 back-pointer to founding questions (L256–258).

### Coverage tables

- **CRUD:** N/A — the pack has no user-mutated persistent domain entity; its "records" (spec, queue rows,
  journal) are append/attic-governed, covered by INV-1/INV-7, not a CRUD grid.
- **Authorization:** N/A single-owner discipline product — write-ownership is one rule (INV-10) proven, not
  a role matrix.
- **Invariants per delta-touched state (feature spec-delta):** fences state — DO cite an existing clause,
  NEVER earn a new row (INV-19/INV-6): stated. Facet state — DO end as a sentence, NEVER silent (INV-18):
  stated. Non-goal state — DO always write one, NEVER a missing sentence (INV-20): stated. Success-measure
  state — DO always write one, NEVER derive a matrix row while reading machinery is [target] (INV-21):
  stated. **Missing:** the appetite-trim interaction with the fences/non-goals/measure invariants (F1) —
  the "NEVER trimmed by appetite" side is stated for facets only.

## Phase 3.5 — Acknowledged gaps

The spec's explicit open items are the ⟨DECIDE⟩ rows D-1 (attic layout), D-2 (tier-routing override), D-3
(snapshot retention) — each carries a current pick and a named revisit trigger, so none is an unmanaged
hole. D-4/D-5 are recorded as DECIDED with execution rows (51, 52–54). No new acknowledged gap introduced by
tonight's deltas; E-18's own [target] legs (design-sync wiring, row 93) and INV-21's reading machinery are
owned [target] clauses, not open decisions.

`acknowledged · (no new open items tonight)`

## Phase 4 — Human and operational factors

F3 above is the main ops-ux note (no single assembly point for a feature delta's contents). One further
observation, no finding: the decide-time batched report is dense by DESIGN (five decision classes in one
batch) — this is deliberate (INV-18's "never one ping per facet") and the density is the intended cost of
not interrupting per-decision; F2 asks only that its completeness be one stated property, not that it be
split. Performance/scale of the spec artifact itself (a ~640-line method doc) is within easy reading
budget. Security/privacy: out of scope for a local method pack — a named skip, not a blind spot.

## Phase 5 — Closing summary

1. **Top fixes before push:** none are must-fix. F1 (generalize appetite's trim-protection from facets to
   the whole mandatory-sentence class — the sharpest cross-delta seam), then F2 (state batched-report
   completeness as one property), then F3 (name the assembled-delta checklist's home in spec-author).
2. **Properties worth stating explicitly:** "Appetite tunes which surfaces are IN and how considered a
   DEFAULT is — never whether any required sentence (facet, fence, non-goal, success measure) is written."
   · "The batched report is complete by construction: every non-wish choice of every class appears in it."
3. **Open questions needing the human:** none from this pass — F1–F3 are all resolvable by inspection/wording.

**Overall readiness: ready to push.** Zero must-fix; the four deltas compose cleanly and in a coherent
order, all 24 folds hold, and the three findings are one enumeration-scope generalization and two ops
clarifications.

## Mechanical guard (senior-run, exact counts)

Formal-index ↔ prose anchor symmetry, EXACT both directions: **84 distinct anchors** in the index set, the
**same 84** in the prose; **index-not-prose = ∅, prose-not-index = ∅**. Index table has **78 row
definitions** expanding to 84 (the single `T-1..T-7` range row contributes +6); **zero duplicate index-row
definitions**. Header reads **v0.14.0**. Index one-liners track their home sections (spot-checked against
the four new/changed anchors T-14, T-15, INV-19, INV-20, INV-21, B-2, E-18 — all match).

## Verdict

**READY-TO-PUSH · 0 must-fix · 3 should-clarify/worth-considering (F1 should-clarify, F2 should-clarify, F3
worth-considering) · all PENDING for the senior.** This FULL pass DISCHARGES the accumulated
MINOR-under-CROSS-LINK FULL debt owed by -fences (0.12), -intake-trio (0.13), and -founding-designsync (0.14)
— the whole-spec re-prove those records deferred to the milestone/push gate lands here. Per M-6, only
must-fix folds before the push; there are none, so the three findings become queue rows (or same-sitting
folds at the senior's discretion — a fold produced by this gate's own pass ships with this record and does
not re-trigger the gate, provided it stays LOCAL to the sections its finding named).

All three findings folded same sitting by the senior (Fable). Matrix-audit pass 2 findings folded in the same milestone sitting (record: docs/audit/2026-07-05-night/matrix-audit.md).
