# Prover record — 2026-07-05 ~23:32, session 10: CROSS-LINK, row 100 (the problem ledger, E-24 + INV-23)

Mode: CROSS-LINK — the new section "When the workshop itself misbehaves" against its named seams:
T-9 bug lane · recurring-bug re-door (pipeline) · INV-9 · INV-4/E-22 · E-8 · E-11/INV-10 · ACT-3 ·
INV-11 · M-1 compaction. Spec under review: v0.15.10 draft.

Opening: the section does one thing — turns the silent retry into a written line with an ownership law
at the second occurrence — and names its seams explicitly. Strong: the third-recurrence law as a
method-defect detector; grep-able status vocabulary. Needing attention before build: the entry's life
AFTER ownership, and the archive home.

| # | Finding | Severity · category | Folded? |
|---|---|---|---|
| F1 | A recurrence on an entry that ALREADY has an owner (OWNED / AGREED NON-PROBLEM) was an undefined path — append? re-raise? | should-clarify · undefined-path (transitions) | FOLDED — "appends its date and changes nothing else; re-raising an agreed non-problem is the human's move, the date list is what he re-raises from" |
| F2 | SOLVED had no owning actor/step — OWNED entries would rot until an audit | should-clarify · unclear-owner (actors) | FOLDED — the landing that closes the owning row flips the entry to SOLVED in the same session |
| F3 | The compaction's archive HOME was unnamed (queue has docs/queue-archive/; the ledger had nothing) | should-clarify · missing-outcome-check (postcondition) | FOLDED — dated ARCHIVED tail of the same file; one file stays the one home |
| F4 | Signature drift: one problem under two wordings = two WATCHED lines the second-occurrence law never fires on | worth-considering · missing-rule (invariant) | FOLDED — duplicates merge into one entry at the milestone compaction |
| F5 | "Sessions write the ledger" read as forbidding a worker whose BRIEF names the file — friction with ACT-3 | worth-considering · internal-conflict (consistency) | FOLDED — "unless its brief names the ledger among its files (the brief stays the write-ownership law)" |

Seams checked clean, no finding: T-9 (the ledger never takes the lane; a product defect goes to the bug
lane — the boundary case "flaky test of our own suite" resolves correctly through the OWNED path, since
an owner may be a queue row either way) · recurring-bug re-door (distinct by what broke: product vs
workshop — both laws state the contrast) · INV-9 (agreed non-problem is the human's word alone) ·
INV-4/E-22 (recommended owner written now, ask rides the batched page; a veto declines the row through
T-8's normal exits) · E-8 (ledger git-tracked; only checkpoints ignored — matches the existing clause
verbatim) · E-11/INV-10 (the method-defect escape from a host window is the standard one-inbox-file
exception) · INV-11 (shared under the concurrent-edit fence like any doc) · M-1 (ledger joined the
compaction list in the same edit; the suite-compaction clause landed separately tonight, v0.15.9).

Open ⟨DECIDE⟩ touched by this surface: none. Unfolded must-fix: none. Verdict: section ready for the
architecture step.
