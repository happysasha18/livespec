# Prover short-form — row 232, the time-estimates law (2026-07-10 ~13:43)

One-hop extension of the reporting family, short-form per the cadence for a small prose law.

The new clause (INV-93) composes with three standing laws and contradicts none: the capture echo
(INV-27) gains a rider, not a second echo; the honest-range wording is INV-35's own (the offline
window), cited rather than restated, so the no-guess-dressed-as-a-promise rule keeps one home; the
landing report (INV-28) gains one field — estimate beside the actual — and the never-list is
untouched (a duration is the asked substance here, the carve-out already written). Cross-link lens
walked: INV-93 cites INV-27/INV-35; the Formal index row lands beside the family; the skill's three
touched rules (12, 13, 8) each trail the anchor. Edge named and folded: a direct command with no
row still hears a range when it holds the session past a beat — otherwise every ad-hoc ask would
escape the law. No new entity, no state, no seam; matrix M-222 owns the fact at string level,
red-proven against the pre-law homes.
