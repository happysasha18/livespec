# Prover — row 258, the restructure/migration merge gate judges the delta (2026-07-12, short form per INV-61)

Small delta (kind: skill; architecture = new invariant, owner node product-prover).

- **Previous records:** 2026-07-12-row257-architecture-rework.md clean (0 must-fix); no unfolded rows.
- **The delta in one line:** a restructure or migration merge gate judges the delta, in three parts —
  load-bearing token identity old-versus-new modulo the per-chunk named deltas plus the
  punctuation-multiset check (INV-111); the full suite green on the merged tree (INV-39); and a full
  prover pass on both sides whose blocking set is delta-scoped (an unmatched token, a red suite, a
  new-side finding absent on the old side, an unnamed meaning change); pre-existing findings equal on
  both sides route to queue rows in the same landing and never block; and a session that sharpens a
  human's spoken bar beyond his words says the sharpened form back and marks it as its own
  interpretation — spec clause (INV-114 + index), product-prover's restructure-merge gate paragraph,
  build-pipeline's restructure-door bullet, M-253, three string tests plus the spec-anchor/index test,
  red-proven against the pre-delta tree. Composition read: cites INV-111 (token+punctuation multiset)
  and INV-39 (landing gate) as kin, never restated — this law states the composition and the routing.
  Born of the tlvphotos correction (2026-07-12 ~01:28): a strictly-improving restructure merge was
  parked on the OLD side's pre-existing clarity debts because a spoken «prover finds nothing both
  sides» was over-sharpened into «any finding parks the merge», corrected live.
- **Architecture gap, same shape as row 257, headed off proactively:** the draft's DELTA section names
  its owner node explicitly ("Owner node = product-prover"), but none of its edit blocks touch
  ARCHITECTURE.md. Per the orchestrator's instruction to check for this before starting the lane, and
  since the owner was unambiguous (unlike a case that would need a STOP), `INV-114` was added to
  ARCHITECTURE.md:42's `product-prover` node Owns column, with a `:168` pin, before the first full-suite
  run — no unpredicted red this lane. Suite: 422 passed on the first full run.
- **Verdict:** ready to ship — 0 must-fix, 0 should-clarify.
