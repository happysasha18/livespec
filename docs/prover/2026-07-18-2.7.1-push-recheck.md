# Push re-check — 2.7.1 patch (2026-07-18)

Prover skill version: 2.7.1.

Short-form re-check before pushing the 2.7.1 patch. This certifies the pushed state holds together
and is push-ready. The patch changes no spec content: PRODUCT_SPEC.md is identical to the 2.7.0
release-proven state except for the version stamp in its title line (`(v2.7.1, …)`), written by
`scripts/stamp-versions.py` as a copy of the one VERSION fact (INV-178). The behavioural change this
patch carries is a readability restructure of the product-prover skill, reviewed from a clean context
and recorded under gate s.

## Commits (origin/main 2f48d11..HEAD)
- `b6eee11` product-prover 2.7.1 content: the clean-context skill-creator review folds (the orphaned
  magnitude fragment dropped, the README lifecycle-sweep bullet restored), the two gate-g pin
  reconciliations, and the skill-review record.
- `bf3e9dd` v2.7.1 version stamp across the skills, plugin.json, and the spec title.
- this record's commit — the prover push-recheck, the ARCHITECTURE register fix (a de-hyphenated pin
  label, no caps token), and the content-pin test reconciled to the folded skill phrasing.

## Verdict
The spec content is unchanged since the 2.7.0 release pass (`docs/prover/2026-07-18-release-2.7.0.md`)
save the title version stamp; no invariant, transition, or Formal-index row moved. The Formal index is
contiguous through INV-236. The full suite is green at the pushed state. The gate chain runs a–z; the
meta-gate reports every gate carries a known-red proof; the CI mirror carries every local gate; gate z
confirms the four working documents sit within their bounds; gate s confirms product-prover carries a
fresh clean-context review record. VERSION stamped to 2.7.1 across every copy of the one fact.
Push-ready.
