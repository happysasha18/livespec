# Prover record — ROADMAP rows 390 + 392 (the growth/grooming family): doc rotation, INV-209

Date: 2026-07-18. Mode: CROSS-LINK + architecture lens, SHORT FORM (small delta, infra kind —
SPEC INV-61). The delta adds one invariant (INV-209), one gate (`guardrails/check-doc-rotation.py`,
gate t), and one mechanism (`scripts/rotate-doc.py`); no new node (the guardrails node grows), no new
user-visible surface.

## What landed

The grooming word Alexander gave 2026-07-17 ~18:25 — the pack's append-only working documents have
grown until a guard's scan and a grep run slow, so these documents are split and rotated, a done row
marked done and the closed portion rolling into a dated archive with a manifest line (base rule 10), the
bound governing the live file while the archive keeps everything. This word sits in both row 390 and
row 392, which is why the two rows fold together here: the rotation mechanism is the shared resolution of
the grooming clause both carry.

## Cross-link seams checked

| seam | resolution |
|---|---|
| INV-209 ↔ base rule 10 (attic + manifest line) | INV-209 IS base rule 10 applied to a document's own closed portion; the manifest line is the base-rule-10 manifest, checked by the gate's no-manifest red condition. Consistent. |
| INV-209 ↔ INV-1 (closed rows live in dated archives under `docs/queue-archive/`) | the rotation archive lands under the same `docs/queue-archive/` home INV-1 already declares; the mechanism realises INV-1's promise the live table had stopped keeping (130 closed rows still sat in the live queue). No conflict — same home, the rotation is the act INV-1 named. |
| INV-209 ↔ INV-206 (the waiting-list board bounds what is SHOWN, moves the rest to the attic with a manifest line) | same shape: bound the shown set, keep everything, manifest the move. INV-209 is the docs-side sibling; the clause cites INV-206 as kin. |
| INV-209 ↔ INV-204 (cleanup-notice structural scan) | the gate is a structural scan of the same family — it reads the manifest's promises against the archives, and the judgment of which rows are ripe stays the author's own. Cited as kin. |
| INV-209 ↔ INV-98 / debt-cap ratchet | the existing caps bound style errors, redundancy pairs, and suite wall-time; INV-209 bounds the live doc's own size by rotation. Complementary, no overlap — no existing cap measured document bytes. |
| gate k (freeze) ↔ ROADMAP rotation | gate k deliberately excludes ROADMAP.md (a living queue whose rows are added and archived by design), so rotating ROADMAP rows trips no freeze; the spec/architecture/matrix edits re-froze in the same movement. Consistent. |

## Architecture lens

- Every spec fact owned by one node: INV-209 → guardrails node (owns-list + two pins,
  `guardrails/check-doc-rotation.py:1`, `scripts/rotate-doc.py:1`). No orphan, no speculative node.
- Seam named: the gate reads the manifest (a marker-keyed block in the live doc) against the archives
  under `docs/queue-archive/` — the format owned by the mechanism that writes it.
- Watcher present: the invariant is asserted by M-390 and the gate, wired into both nets (pre-push gate t
  and the CI mirror), not left to a pass's attention (base rule 30 / INV-164).

## Findings

| # | finding | folded / rejected |
|---|---|---|
| 1 | The DONE convention first read the whole status cell for the closed/open signal, so a landed row whose delegation prose merely mentioned `[target]` or `WAITING` (row 14, row 55's history) could be misread. | FOLDED — the mechanism now reads only the status headline (the first `**bold**` span), where a row states its OWN closure or open legs; a later mention deep in the prose describes history. Red-proven against rows 55 and 93 (both HALT) and confirmed rows 14/27/33/… rotate. |
| 2 | A literal `\|` inside a backticked token (`\| n \|`) in the matrix and index table cells broke the naive table parser, hiding M-390's INV-209 reference from the coverage check. | FOLDED — the table cells now say "its own numbered table line" with no literal pipe; the prose clause keeps its illustrative form. |

## Design review

Stands down by name: infra kind, no user-facing surface, and the same-kind grouping is already declared —
INV-209 is stated as a structural-scan sibling of the waiting-list [INV-206] and cleanup-notice [INV-204]
nets, so there is no undeclared same-kind group for the design reviewer to surface.

## Open DECIDEs touched

None. The cadence ("roughly one to two months, or once a file holds enough closed material") is stated as
the owner's word, not a knob resolved here.

## Scope honesty (the folding rows' other legs)

This landing builds the grooming/rotation leg that rows 390 and 392 share. It does NOT build row 390's
node-growth-law leg (the ratcheted nodes-per-file counter, the prover's seventh lens, the design-review
split-proposal shape) or row 392's growable-artifact-bound leg (the four docs each declaring a byte or
ratchet bound with its watcher). Those legs stay OPEN in the two rows' landed cells, in the row-247/257
partial-landing form.
