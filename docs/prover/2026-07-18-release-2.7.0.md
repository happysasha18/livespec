# Release prover record — the 2.7.0 MINOR release (2026-07-18)

- Pass run by: **product-prover 2.7.0** (the pack version; the bump 2.6.3 → 2.7.0 lands at this gate,
  VERSION and every stamped copy read 2.7.0, per INV-178's one-fact rule)
- Date: 2026-07-18
- Form: **RELEASE-LEVEL re-check** — the fuller form the host profile owes before a push, sized to a MINOR
  release (a large delta: 34 ROADMAP rows, 39 new invariants INV-198…INV-236). This record certifies the
  whole 2.7.0 holds as an integrated release; the per-row structural proofs live in this movement's own
  prover records (referenced below), each already folded to zero must-fix.
- Movement boundary: since **v2.6.0** (commit 3774685). The tree carries the integrated result on `main`
  at HEAD 5860cbf, VERSION bumped to 2.7.0 this session.

## Release-tier verdict (rule 32 / SPEC INV-217) — applied to this release

**MINOR.** The trailing question INV-217 asks — what must a host that vendored 2.6.3 do to take 2.7.0 —
is answered "re-run the catch-up walk." The movement grows what a host may adopt (39 new invariants, 12
new push gates o…z, the register judge, doc rotation, the far tier, the touchpoint frame + waiting list +
read-back, the lane-open act, worker-teardown reap, reach-classes config, versioned founding questions)
in a backward-compatible way; nothing a host already carries is reworded in a breaking way, no surface a
host depends on is renamed or removed, and no earlier adoption step changes. The three borderline cases,
judged honestly:

- **The register judge replacing the literal-list lint (416/418; INV-83's growth duty retracted).** Not a
  breaking reword. The literal-pattern list stays the deterministic gate; the judge is opt-in
  (`PRESHOW_REGISTER_JUDGE`, off by default). INV-83's retracted growth duty is a duty REMOVED — a host
  that vendored it drops it and does nothing forced. Adopted by re-running the walk with the reworded
  INV-83. **MINOR.**
- **The reach classes moving to config (380; INV-224).** Same script, same gate letter, same verdicts —
  the class lists move from the script body to `guardrails.config.json` with the pack's values as the
  default, and the default reproduces every existing reach verdict (a missing/empty config can only
  over-run scope, never false-green it). A host adopts via its own declared layers; nothing it carries
  breaks. **MINOR.**
- **The founding-question versioning (394; INV-227).** This IS the catch-up walk's own designed
  mechanism (Phase 1 step 5): the walk reads the host's `founding.set-version` and rides each question
  added since — the agent card among them for an older host — surfaced for the owner and answered on no
  one's behalf. Backward-compatible by construction. **MINOR.**

No change in this movement forces host action beyond the normal re-vendor + re-run-the-walk + re-seed-caps
catch-up. The dated 2.7.0 MIGRATION.md chapter is written (grouping the eleven adopt-clusters), as the
minor tier's "re-run the walk" instruction — not because a migration is forced.

## The release holds — the certification checks

1. **Formal index contiguous through the latest.** PRODUCT_SPEC.md carries 236 distinct Formal-index
   rows, INV-1…INV-236, **zero gaps** (verified: `seq 1 236` minus the present set is empty). The
   movement's INV-198…INV-236 are all present and indexed. Gate x (index-prose) rides the same set —
   every code carried in its home prose.

2. **Every gate carries a known-red proof (gate w).** `check-every-gate-can-fail.py` → OK, **26 gates
   checked**, each with a known-red proof or a declared covered ride. No gate in the chain is a check
   that looks at nothing (INV-212).

3. **The CI mirror carries every gate (gate u).** `check-ci-mirror.sh` → OK: every local pre-push gate is
   mirrored in `.github/workflows/gates.yml` or is one of the four declared carve-outs (c folded into the
   suite; k reads a gitignored local baseline; m/v read the personal layer absent on a CI checkout), each
   carve-out stating why it stays out of CI (INV-210). Gate v (judges-listed) confirms every wired chat
   judge is referenced in the installed settings (INV-211).

4. **The suite is green.** `python3 -m pytest -q tests` in the foreground, its own tail line the verdict
   (INV-80): **1578 passed in 195.01s, exit 0** — no failures, no errors. Against the ≤360s budget, green
   with margin.

5. **The movement's high-stakes landings each passed adversarial review with defects fixed.** The three
   the release rests hardest on — each carried a "Corrections after adversarial review" section in its own
   record, and each closed to zero unfolded must-fix:

   - **The register judge (rows 416/418).** `docs/prover/2026-07-17-rows416-418-register-judge.md`,
     Corrections section: an adversarial review found six defects, two defeating the row's own claim (the
     chat judge delivered no verdict — a wait-on-non-child race destroyed every one; the whole-turn gather
     read the wrong slice). All six fixed on top, each red-proven first: the race (one subshell owns write
     + rename under nohup), the turn boundary (skip tool_result user records), quote validation (min-length
     floor + verbatim-span recovery, both directions), INV-94's residual growth command retracted, a
     missing-installed-hook green skip closed to drift, and the owner's personal register bank replaced by
     synthetic stand-ins in the shipped test.

   - **The cleanup inversions (row 417).** `docs/prover/2026-07-17-row417-cleanup-notice-and-inversions.md`,
     Corrections section: the verdict's claim that the four guard inversions "each keep their gate's prior
     catch" was falsified by an adversarial review of the four guards — four confirmed defects, each
     red-proven against the committed tree then fixed and greened: the broad-kill resolver's blind spots
     (ps|grep|xargs kill, full-path pkill, cross-line taint) restored so it catches strictly more; the
     shared-install-path exemption removed (it re-blessed a cross-run kill the INV-162 spec forbids); the
     cleanup-notice blindness to Python `.terminate()`/`.kill()` closed; and the deferral marker's
     coincidental-reason-word evasion closed by the soft/core split. The corpus fixture — which had itself
     failed its narrowing-guard job — now pins every newly-caught form.

   - **The read-back (row 415).** `docs/prover/2026-07-17-row415-authority-anchor.md`, Corrections section:
     an adversarial review demolished the first build and was right. The first sweep's "zero genuine
     unanchored attributions" was FALSE — the founding fabrication stood LIVE at ROADMAP.md:291, passed by
     a row-level anchor shortcut; and the gate spared exactly the surfaces the incident used (the resume
     file and roadmap). Fixed: the fabrication struck and row 386's cell rewritten in the pack's own voice;
     the gate now hard-blocks the decision records AND reaches the resume file and roadmap as an advisory
     report feeding the read-back; four record-surface evasions and one false positive closed red-first.
     The honest layering the first build inverted is restored — the read-back is the load-bearing defence a
     dated fabrication no gate can catch, the gate the cheap first pass.

## Verdict

**The 2.7.0 release HOLDS. Ship the MINOR bump.** The Formal index is contiguous INV-1…INV-236 with zero
gaps; every one of the 26 push gates carries a known-red proof (gate w) and is mirrored in CI or a
declared carve-out (gate u); the suite is 1578 green, exit 0; and the movement's three high-stakes
landings each passed adversarial review with every found defect fixed red-first and folded to zero
must-fix. The release-tier call is MINOR per INV-217 — a host takes 2.7.0 by re-running its catch-up walk,
rewriting nothing it carries — and the dated 2.7.0 MIGRATION.md chapter records exactly that. No blocking
finding. The whole-gate re-run and the re-freeze (gate k) close the landing.
