# INV-163 audit — the ship-the-shape / host-owns-the-instance split minted as one root

Ran by: product-prover lens + adversarial fresh-reviewer pass (opening hypothesis: *the change was applied but the goal was missed — the formalization is incomplete, inconsistent, or wrong*).
Date: 2026-07-15 (Wed Jul 15 21:56 IDT).
Target: ROADMAP row 332 — minting the pack↔host body-placement split as one invariant INV-163 that every prior site cites, cut (per the brief) as the 2.0.0 queue-complete milestone, framed as a non-breaking docs formalization with no host action.
Primary sources read directly: `PRODUCT_SPEC.md` (INV-163 prose L1759 + index row L2031; E-26 L267/—; INV-158 L657/L2026; INV-136 L1170/L1849; INV-139 L1172/L1850; INV-125 L1751/L2016; INV-159 L343/L2027), `skills/live-spec-base/SKILL.md` (base rule 4, L42), `skills/product-prover/SKILL.md` (lens list L181–L322), `ARCHITECTURE.md`, `MIGRATION.md`, `ROADMAP.md`, `VERSION`.

## Verdict up front

The spec-level goal is **MET with one defect to fold**. INV-163 is minted with a correct discriminator and both poles; all five prior sites cite `[INV-163]` instead of re-deriving the contrast; the class-completeness sweep finds **no un-repointed sixth site**; poles are correctly assigned; prose and index mirror each other. The single real defect is a **false home**: INV-163 (prose and index) claims "product-prover's placement lens" as a home, and no such lens exists in product-prover. Two release-level gaps sit outside the spec (VERSION not bumped, no MIGRATION 2.0.0 chapter, ROADMAP row 332 not closed) — relevant to the "cut as 2.0.0" framing, not to the correctness of INV-163 itself.

## Findings

| id | finding | verdict | ground (why) | folded/open |
|----|---------|---------|--------------|-------------|
| F1 | INV-163's `homes —` line (prose L1759 and index row L2031) names **"product-prover's placement lens"** as a home. No such lens exists. product-prover's only "placement" is the **architecture-lens "placement view"** [INV-75] at `skills/product-prover/SKILL.md:181` — "the placement view says where every node **runs**" (runtime deployment: which machine/process), a different axis from INV-163's authoring-home question (does a capability's body live in the pack or in each host). No lens checks which pole a new host-specific capability takes. | DEFECT | Per product-prover's own KIND rule a defect is "a claim the spec makes is false." The homes line asserts a home that is not there. This also leaves INV-163's forward-binding duty ("a new host-specific capability states which pole it takes from its first landing", L1759) with **no enforcing net** — the exact bare-binds-forward gap INV-159/INV-160 treat as a finding. Contrast the sibling homes that are real: INV-125's "cross-surface-policy lens" (SKILL.md:281) and INV-136's "interactive-overlap lens" (SKILL.md:283) both exist; INV-163's does not. The name "placement lens" also collides with the existing "placement view" [INV-75], misdirecting a reader to a runtime-location check that does not assess pole assignment. | OPEN |
| F2 | Discriminator + both poles + escape + forward-binding all present and correct. Prose: "can the pack ship a single identical body that every host runs?" → yes centralizes [INV-158], no ships the shape [INV-125, INV-136, INV-139, E-26]; "A host whose instance grows a genuinely generic seam lifts that seam to the pack"; "binds forward [INV-159]". | no finding (completeness confirmed) | INV-163 states the discriminator once, names the centralize pole with its one member and the ship-shape pole with its four, carries the generic-seam escape and the forward-binding clause. Nothing load-bearing missing except F1's false home. | n/a |
| F3 | All five sites CITE `[INV-163]` rather than re-derive. E-26 L267 "the ship-the-shape pole of the pack-to-host split [INV-163]"; INV-158 L657 "the centralize pole of the pack-to-host split [INV-163]"; INV-136 L1170 "the ship-the-shape pole of the pack-to-host split [INV-163]"; INV-139 L1172 "the ship-the-shape pole of the pack-to-host split [INV-163] since live-spec has no UI of its own"; INV-125 L1751 "the ship-the-shape pole of the pack-to-host split [INV-163]". No site still spells out "the discriminator is…" in its own words. | no finding (goal met) | Each after-text names its pole and defers to INV-163 for the contrast. The prior sites retain their own concrete instance (the harness, the DOM assertion, the pixel projection, the greps) — correct: the instance is theirs, the split is INV-163's. | n/a |
| F4 | Class-completeness sweep: no sixth site re-derives the split. Grepped the whole spec for `ships the shape`, `ship-the-shape`, `centraliz`, `owns the instance`, `no DOM of its own`, `no UI`, `leaves the … assertion/projection/greps`, `the products it serves`, `adopting project`, `each host owns`, `pack-to-host`, `pack↔host`. Every hit falls on one of the 5 sites or INV-163 itself. L659 (INV-160 suite-honesty) mentions INV-158's net but does not re-derive the split. Other `leaves`/`ships` hits (L930 feedback flag, L1054, L1996 INV-100, L2003) are unrelated. | no finding (class complete) | The hardest check passes: the split now has exactly one root and five citers, zero orphan re-derivations. | n/a |
| F5 | Pole assignment correct. INV-158 = CENTRALIZE (harness = one identical body). INV-125 / INV-136 / INV-139 / E-26 = SHIP-THE-SHAPE (DOM assertion / pixel projection / pixel projection / host-owned greps — each host-specific). INV-163's index row lists the ship-shape sites as `[INV-125, INV-136, INV-139, E-26]` and the centralize as INV-158 — matches. | no finding | The discriminator ("names a host's own surfaces, holds a host's own data, or reads a host's own artifacts") sorts each site correctly. No mis-assignment. | n/a |
| F6 | Prose↔index consistency. INV-163 index row L2031 mirrors the prose L1759: same discriminator question, same yes/no poles, same site citations, same forward-binding, same homes line (including the same false "product-prover's placement lens"). The five repointed index rows (L267 E-26 area, L2026 INV-158, L1849 INV-136, L1850 INV-139, L2016 INV-125) each carry the `[INV-163]` cite matching their prose. | no finding (mirror faithful) | The index is a faithful mirror — including faithfully mirroring F1's error, so fixing F1 must touch both L1759 and L2031. | n/a |
| F7 | INV-159 forward-binding membership is bidirectionally consistent: both the prose member list (L343) and the index row (L2027) include INV-163 in `[INV-103, INV-134, INV-156, INV-160, INV-163]`. | no finding | The forward-binding wiring is complete on the INV-159 side; the gap is only that INV-163's claimed enforcer (F1) does not exist. | n/a |
| F8 | Non-breaking / no host action. `ARCHITECTURE.md` has zero INV-163 references and gains no node/seam/budget. No new guardrail script or template ships. The five sites already behaved this way (hosts already hold their own DOM assertions, pixel rows, and greps). The only new obligation is an **authoring** duty (a new host-specific capability declares its pole, via INV-159) borne by whoever writes the pack spec — not a host runtime action. | RECOMMENDATION (confirms non-breaking, flags a missing chapter) | Non-breaking = CONFIRMED. But the brief frames this as "the 2.0.0 migration chapter" and there is **no such chapter**: `MIGRATION.md` has no 2.0.0 entry and no INV-163 mention. If shipped as 2.0.0, a one-line migration note stating "docs formalization, no host action" is owed. | OPEN |
| F9 | Release-state mismatch vs the "cut as 2.0.0, queue-complete milestone" framing. `VERSION` still reads **1.10.1** (not 2.0.0). ROADMAP row 332 still reads "**idea — not a build until it earns one**" / "Consider minting…", Done-when "either the split is minted as one invariant every site cites, or a dated note agrees…" — the minting has landed in the spec but the row is not marked Done. | RECOMMENDATION | The spec formalization landed ahead of the release bookkeeping. Close row 332 (its "minted as one invariant every site cites" branch is now satisfied), bump VERSION, and add the MIGRATION chapter if 2.0.0 is the intended cut. Not a spec defect. | OPEN |

## What is done well

The discriminator is stated once and cleanly — one question ("can the pack ship a single identical body every host runs?") does all the sorting, and the generic-seam escape ("the boundary moves toward centralization as a body proves uniform") gives the rule a direction rather than a frozen line. The repointing is disciplined: every site keeps its own concrete instance and defers only the contrast, which is exactly the one-home shape base rule 4 wants. The class-completeness sweep is clean — this was the real risk (a sixth site quietly re-deriving) and it does not occur.

## Bottom line

Goal (one root, every site cites it): **MET** at the spec level — 1 defect (F1, the false "product-prover's placement lens" home) to fold, plus two release-bookkeeping recommendations (F8 missing migration chapter, F9 version/roadmap not closed). Fold F1 by either creating the placement lens in product-prover (the enforcer INV-163's forward-binding clause presumes) or rewording the homes line to name a home that exists — and mirror the fix in both L1759 and L2031.

---

## Disposition (folded by the orchestrator, 2026-07-15)

- **F1 — FOLDED.** The `homes —` line in both the INV-163 prose clause and its Formal-index row no longer names a "product-prover placement lens". It now names **spec-author's pole-declaration duty for a new host-specific capability**, a real home: spec-author gains a section ("Declare the pole when a capability could live in the pack or in each host", SPEC INV-163) making the authoring duty concrete, and the duty binds forward through INV-159. The pole of a new host-specific capability is an authoring judgment, so its home is the author rather than a mechanical prover lens.
- **F8 / F9 — release bookkeeping, addressed at the commit station.** The 2.0.0 version bump, the MIGRATION.md 2.0.0 chapter (host action: none), and closing ROADMAP row 332 land at build-pipeline step 9, after the suite is green. Correctly sequenced behind the spec/architecture/test stations.
- **Design review (INV-141):** stands down by name — the split's ship-side sites are now a declared class citing one root [INV-163], and the audit confirmed the grouping is complete and the poles consistent, so no undeclared same-kind grouping remains for the design review to propose.
