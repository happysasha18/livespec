# Prover — row 248, detached-work visibility (2026-07-11, short form per INV-61)

Small delta (kind: skill, no new surface, no structure change) — the three-line short form.

- **Previous records:** 2026-07-11-row246-mirror-attribution clean (0 must-fix, F1/F2 folded same
  session); 2026-07-10-external-push-probe fully folded at row 251 — no unfolded rows anywhere.
- **The delta in one line:** INV-35's heartbeat gains the detached cadence — any operation expected
  to run past ~2 minutes detached (a background command or a delegated worker the chat does not
  stream) opens with a START line (what runs, where its log lives, an honest range [INV-93]), keeps
  a beat every ~2 minutes or at each stage [default], and closes with a DONE digest; the trap named
  (detached work writes only to its log and shows in no agent panel, so silence reads as lost); the
  mechanism stays free by the owner's word — in both homes (spec prose + index row, communicator
  rule 13), M-232, four string tests red-proven then green. Composition read: the offline window
  keeps its beats-during clause (a detached run inside a window still beats); INV-52 stays whole
  (beats are chat lines, no mid-stretch window opens); the per-command-quiet clause holds (a waiting
  timer earns no beat); INV-93/95 untouched, their tests green.
- **Verdict:** ready to ship — 0 must-fix, 0 should-clarify.
