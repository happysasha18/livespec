# Prover cross-link — SPEC humanize: "Throwing a wish" CHUNK 7 (2026-07-07, session 25)

CHUNK 7 = four bold-leads: A feature is specified past what you know to ask · Every facet ends as a spec
sentence · A feature is interrogated for how it fits the product · A face you can enter once owes a way back.

Gates (whole section after splice): 157/157 tested phrases section-scoped; code multiset identical to
baseline (165 occurrences, zero diffs); full suite 175 green. Chunk 7 carries 18 tested phrases and ~20
code occurrences, all preserved (incl. the literal `[default]` tag kept in the prose about writing it).

## Facts carried (all KEPT)
- A feature is specified past what you know to ask [T-13]: the standard-facets sweep pulled into a bulleted
  list (phone or narrow window / hover-only needs a touch answer / empty, error, and loading / accessibility
  / performance envelope / visual hierarchy / two windows at once / missing source); list curated by a named
  real incident; no-visible-surface sentence; a re-doored wish walks the sweep before work resumes [INV-16];
  A fenced prototype is NOT swept [E-17].
- Every facet ends as a spec sentence [INV-18]: decided vs defaulted (the literal `[default]` tag, matrix
  row either way [E-15]); told on the defaults list, silence is consent [INV-31]; a facet with no sentence
  is a prover-flagged defect (the Room's hole); adopted surfaces reconciled like any re-engineered claim
  [A-10] [A-3]; sweep AUTHORS the facet sentences, axes compose [C-1].
- A feature is interrogated for how it fits [INV-29]: a small prover on the wish itself; the fit walk
  kind-scaled (product journey / infra flows / skill trigger-correction-when-not); interrogates the feature
  not the person; trivially-closable holes closed and written (his 2026-07-06 word paraphrased); only taste
  calls batched [INV-4, INV-18] (Alexander's don't-torment-the-user word paraphrased); FEATURE-FIT prover
  mode; lens lists curated [T-13]; binds forward [INV-21 kin].
- A face you can enter once owes a way back [INV-50]: a one-way face is a dead end the state lenses miss
  (tlvphoto's door, his get/set word paraphrased); every conditionally-entered face states its deliberate
  RE-ENTRY path or the one-way as a named decision; trigger wording ("until dismissed" etc.) owes a return
  sentence; entry-symmetry lens extends the fit walk's journey lens [INV-29].
- Three Russian quotes rendered as plain-English paraphrases with attributions; no new quotation introduced.

Verdict for chunk 7: **CLEAN.** Whole-section gates green.
