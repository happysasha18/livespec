# Prover cross-link — SPEC humanize batch: "The rhythm: breakpoints, milestones, pushes" (2026-07-07, session 25)

Register rewrite of the rhythm-rules section (a bulleted list of nine rules, several very dense). Gates
green: 16/16 tested phrases re-matched section-scoped; bracket-token multiset identical to baseline —
this includes the THREE literal inline tokens that are prose, not trailing anchors (`[HH:MM]`, `[~]` in
`date [~]time`, `[-suffix]` in `YYYY-MM-DD[-suffix].md`), all kept in place; full suite 175 green. Clean
on first splice. Outer bulleted list kept; each bold lead kept.

## Facts carried (all KEPT, bullet by bullet)
- Safe breakpoint: NEXT_STEPS live-state replaced not stacked + dated JOURNAL + committed ⇒ zero-loss wipe;
  journal is the durable net; agent compacts and SAYS so; full clear is the human's; re-check freshness on
  return [A-7] [M-2].
- Resume digest cap: read in one minute; 100-line cap [default], suite owns the number (red on bloat,
  red-proven synthetic); cap + restate-every-open-leg [INV-26] resolve by FORM not dropping; compaction
  moves prose home, never drops a leg [INV-48].
- Milestone MINOR gate: pulled into a sub-list — spec re-prove; matrix audit ([E-15] re-walked);
  surface-composition; evals re-run [E-19]; skills re-walked through the standard skill-making skill
  (craft lens, findings folded or rejected with a written reason in a dated record, a joining skill walks
  it at birth); doc COMPACTION [E-24] + suite sweep, queue ARCHIVES not deletes [INV-1]; re-list open gates
  + unharvested inbox/; formal index vs prose; derived headers re-pinned + proven; the thin loader stays
  thin [E-16] with the "must this hold BEFORE any pack file loads?" test and states the line count, a
  failing rule migrates to its real home [M-1].
- Documents versioned [M-3]. Time-off-the-clock [INV-24]: clock at write time, git arbiter; mechanical date
  fence (future-dated stamp red, quoting a past wrong date legal); TIME arm at commit (adjacent `date [~]time`
  shape); CHAT face — the [HH:MM] lead read AT WRITE TIME, never continued or extrapolated; `scripts/clock-hook.sh`
  injects the wall clock; origin notes kept (seven minutes fast; six catches). Versions have named homes:
  `VERSION`, SKILL.md `metadata:`, `.live-spec/`; freshness [A-7] compares version to version [M-7].
- CI mirror [M-5]: same scripts, one source of truth, second net runs the FULL set, reach map [INV-45] a
  local optimization not a CI shortcut; `.github/workflows/gates.yml`; ROADMAP row 14.
- Push gate for live-spec [M-6]: concurrent-edit fence [INV-11] + whole-spec re-check to docs/prover/
  (name `YYYY-MM-DD[-suffix].md`), must-fix folds before push, gate-own folds don't re-trigger, no record
  ⇒ push shouldn't have happened, folds stay LOCAL.
- Process bookkeeping scales to the delta [INV-61]: eighteen-landings measurement; his 2026-07-07 question
  rendered in plain English (Russian dropped); reach idea on PROCESS keeps RIGOR, scales FORM; SHORT-FORM
  record of three lines pulled into a sub-list; batch per lane; journal + resume once per BATCH never per
  tiny row; the irreducible named — quality itself, never scaled.

## Wording changes worth naming (meaning intact)
- The milestone "+"-pile and the short-form record pulled into sub-lists; long clock-fence bullet split
  into four short paragraphs.
- The Russian quote rendered as plain English with its date (2026-07-07); no new quotation.
- "a future-dated stamp is red, not a style nit" → "turns the suite red as a real defect" (positive).

Verdict: **CLEAN.** Every fact and token carried; 16/16 tested phrases section-scoped; token multiset
identical incl. the three inline literals; suite 175 green. No must-fix.
