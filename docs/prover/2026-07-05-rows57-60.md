# Prover record — 2026-07-05, session 9: rows 57 + 60 (SPEC v0.15.1 → v0.15.2), CROSS-LINK mode

Scope: the two spec deltas of this session so far — E-21 (installer) + E-22 (decision page) from row 57,
and the one-collision-law citations (base rule 18) from row 60 — checked against their named neighbour
clauses. Mode: CROSS-LINK (surface adds + a shared-rule add; no structural rewrite — FULL not owed).
Previous record (`2026-07-05-row99…v15d`, session 8) had zero unfolded rows.

| # | Finding | Severity | Folded / rejected |
|---|---|---|---|
| F1 | E-22 restates rule 10's archive step (docs/decisions/) — potential second home | should-clarify | REJECTED as defect: SPEC states the CONTRACT (answers archived + harvested), rule 10 owns the MECHANICS (filename law, ordinals, JSON round-trip) — the split is stated inside E-22 itself, same pattern as E-20/publish |
| F2 | communicator rule 10 speaks the ordinal mechanic without citing the new one-collision law — the class (four collision-speaking surfaces) was only ¾ swept | must-fix | FOLDED: rule-18 citation added to rule 10 (base rule 14: sweep look-alikes) |
| F3 | install.sh backup naming (`.bak_<timestamp>`) is arguably its own scheme beside rule 18 | should-clarify | RECORDED, not folded: a backup name is not collision RESOLUTION — the timestamp IS its semantic mark; same-second double-run merge is a real but tiny hole, noted for the next installer touch |
| F4 | prover-record naming (`YYYY-MM-DD[-suffix].md`, SPEC M-6 area) resolves collisions by semantic suffix and never needs the ordinal | should-clarify | RECORDED: conforms to the law's first move; no citation added — the law binds pack-wide via base rule 18, citations were added only where a HALF-form previously lived (attic, inbox, ADOPT, rule 10) |
| F5 | E-21 says "every pack skill" — verified against install.sh (`skills/*/`: base + 5 working skills, all copied); fresh-machine promise now mechanically tested (M-091 real run) | — | verified, no action |

Open ⟨DECIDE⟩ untouched by these deltas: D-1 (attic layout), D-2 (tier routing), D-3 (snapshot
retention) — none of the changed clauses depend on them (E-9's collision sentence works under both D-1
layouts).

Verdict: with F2 folded, both deltas are consistent with their neighbours; no unfolded must-fix rows.

## Addendum, same session (~22:26): row 63 delta (SPEC v0.15.3), CROSS-LINK

Scope: the decline-lists-absorbed extension of T-8, against its neighbours.

| # | Finding | Severity | Folded / rejected |
|---|---|---|---|
| A1 | The clause and INV-1 ("no wish is ever lost") could be read as two homes for one rule | should-clarify | REJECTED: INV-1 is the invariant, the T-8 extension is the TRANSITION that enforces it at decline time — the clause cites INV-1 explicitly, standard invariant↔transition pairing |
| A2 | The queue archive (INV-1/M-1) moves terminal rows out — an absorbed row's pointer may point into the ARCHIVE when its absorber declines later | should-clarify | RECORDED: the archive is append-only and readable; the decline walk reads its absorber's row wherever it lives; no wording change needed now, the milestone audit watches the first real case |
| A3 | The ROADMAP template spoke the three exits without the new rule — half-form risk (base rule 14) | must-fix | FOLDED: template extended, cites SPEC T-8 |

Verdict: consistent; no unfolded must-fix rows.

## Addendum 2, same session (~22:30): row 64 delta (SPEC v0.15.4), CROSS-LINK

Scope: S-0's mechanization note + the E-10 index [target] mark, against S-0's own granularity rule.

| # | Finding | Severity | Folded / rejected |
|---|---|---|---|
| B1 | E-10's index line lacked the [target] mark its own prose carries (line 541) — exactly the drift class S-0 warns about | must-fix | FOLDED: index line marked; the new test holds both directions from now on |
| B2 | Matrix M-061 cited LANDED row 3 as the registry machine's future owner — a target pointing at a closed row | must-fix | FOLDED: re-owned to row 55 (registry executable form rides the snapshot/guardrails family, per the 0.8.6 resume file) |
| B3 | The declared map (test) vs S-0's "owned by a ROADMAP row" — the map is a THIRD home for ownership? | should-clarify | REJECTED: the map IS the mechanization S-0 now names ("the suite holds the map"), same pattern as the surface registry's executable form (E-10) — the test is the machine, not a second statement |
| B4 | INV-17's [target] legs mapped to row 55 — verified against archived row 3's remainder clause ("host-facing checks ride E-6 [target] with rows 55+") | — | verified, no action |

Verdict: consistent; no unfolded must-fix rows. Red proven for the new tests against the pre-fix SPEC
(git-stash run: map↔index mismatch fails as promised).

## Addendum 3, same session (~22:33): row 65 delta (SPEC v0.15.5), CROSS-LINK

Scope: the loader-stays-thin item added to M-1's gate list, against E-16 and M-1's neighbours.

| # | Finding | Severity | Folded / rejected |
|---|---|---|---|
| C1 | The item reads a file OUTSIDE the repo (the human's global instruction file) — can a public pack's audit bind a private machine file? | should-clarify | RECORDED: the item binds the AUDIT WALK (a session on the human's machine), not the suite — deliberately process-level, like the composition check; the clause cites E-16 (the loader IS a spec'd entity: host-contract node pins it) |
| C2 | "migrates to its real home" could collide with INV-7 (attic, never delete) | REJECTED as defect: the loader is the human's authored file only in form — migration means the RULE moves to profile/pack, the loader line is removed on the human's own machine with the change journaled; nothing is lost, the rule lives on in its home (same move row 52 already performed once, on Alexander's OK) |
| C3 | First walk owed — an audit item landed without ever running is a vow, not a gate | must-fix | FOLDED: walked NOW (2026-07-05, session 9): 16 non-empty lines; verdict — every line passes (window law + ask-before-infer must precede any pack read; profile pointer + pack pointer are the bootstrap itself; migration-map/attic pointers are one-line provenance, arguably prunable at the next milestone but each answers "where did my rules go" before the pack loads). Count + verdict journaled |

Verdict: consistent; no unfolded must-fix rows.

## Addendum 4, same session (~22:39): row 59 delta (SPEC v0.15.6), CROSS-LINK

Scope: the worker contract at ACT-3, against INV-10 (write-ownership), INV-11 (fence), E-13 (ladder),
D-2 (tier routing).

| # | Finding | Severity | Folded / rejected |
|---|---|---|---|
| D1 | "fence-benign for sibling files" could be read as weakening INV-11 | REJECTED as defect: it SHARPENS it — the fence's alarm was always about FOREIGN sessions (INV-11's own wording: a parallel session's edit); a senior's own briefed workers are one session's hands, and the clause pins seam ownership on that senior |
| D2 | Escalation ladder (haiku → sonnet → senior) vs D-2 (who fixes the tier) — does the contract pre-empt the open decision? | REJECTED: D-2 asks who picks the STARTING tier; the contract governs what happens on FAILURE — one rung up, logged. Compatible with either D-2 outcome; noted in D-2's eventual design (row 56) |
| D3 | Eval re-run duty (E-19): build-pipeline's behaviour text changed | should-clarify | RECORDED: the skill's eval scenario exercises door/intake behaviour, not delegation mechanics — no criterion can flip on this delta; the standing M-1 re-run covers it, and the next eval authoring round should ADD a delegation criterion (noted in evals/build-pipeline.md's Re-run section owner: M-1) |

Verdict: consistent; no unfolded must-fix rows.

## Addendum 5, ~22:44: row 62 delta (SPEC v0.15.7), CROSS-LINK

Scope: the bootstrap-order rewrite of B-1, against INV-8, A-0/A-5 (adoption's gate-first), E-6 (hooks
offered), E-15 (what a suite owes).

| # | Finding | Severity | Folded / rejected |
|---|---|---|---|
| E1 | B-1's old order (templates → gate) contradicted adoption's gate-FIRST (A-0) — the exact asymmetry the audit named | must-fix | FOLDED: gate first, with the reason stated ("a gate cannot protect files older than itself") |
| E2 | "Green for landing #1" was undefined — the landing rule (green suite) was vacuously satisfiable on a testless bootstrap | must-fix | FOLDED: the scaffold DEFINES it (docs present, headers filled, checklist in place, one live-state block); floor-not-ceiling stated |
| E3 | The scaffold is a new shipped artifact — inventory + a BY-DEED test owed (M-091's lesson: script promises are behaviours) | must-fix | FOLDED: inventory row + test_scaffold_bootstrap_runs simulates a real bootstrap BOTH ways (filled → green, placeholder → red) |
| E4 | Hooks-at-bootstrap could become a second home of E-6's offered-never-imposed | REJECTED as defect: B-1 CITES E-6, states only the bootstrap timing — same cite pattern as the session's other landings |

Verdict: consistent; no unfolded must-fix rows.

## Addendum 6, ~22:48: row 66 delta (SPEC v0.15.8), CROSS-LINK

Scope: E-23 (dev-machine skill sync) against A-7 (re-read), D-4 (package-is-source), E-21 (installer),
M-4 (dogfood).

| # | Finding | Severity | Folded / rejected |
|---|---|---|---|
| H1 | E-23 vs E-21: two copy tools — one mechanism twice? | REJECTED as defect: different actors and directions — install.sh is the HOST's arrival door (backup-then-overwrite, fresh machines), sync-skills.sh is the DEVELOPER's freshness tool (version-diff report for A-7); the spec states each in its own scenario; install.sh backups are pointless on the dev machine where git IS the backup |
| H2 | The session's earlier hand-copies (rows 60/59 landings) are exactly the anti-pattern E-23 retires | must-fix | FOLDED: real run of the script executed on this machine after landing (report: everything fresh — the hand-copies happened to be complete); from this landing on, the script is the only sync path |
| H3 | mtime-based re-copy (same version, newer file) could re-copy on git checkout churn | should-clarify | RECORDED: deliberate — a same-version edit (defect fix without a bump) must still sync; a spurious re-copy is harmless and reported |

Verdict: consistent; no unfolded must-fix rows.
