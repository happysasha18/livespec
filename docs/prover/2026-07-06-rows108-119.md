# Prover record — rows 108/117/118/119 (the product-fit family), CROSS-LINK pass

Date: 2026-07-06 ~10:52, session 12. Scope: four new clauses (INV-29 fit walk at intake · INV-30
visitor walk + feel pass at verify · INV-31 default expiry · INV-32 consequence-first decision cards)
against their seams: T-13/INV-18 (facet sweep), INV-4/INV-12 (batching, ask-at-intake), INV-22 (kind
scales form), E-22 (decision page), E-17 (prototype bar), INV-27/28 (echo/board), prover modes
(FULL/CROSS-LINK). Mode: CROSS-LINK (four clauses on existing surfaces; the FULL pass rides the push
gate as always). Provenance: Alexander 2026-07-06 morning — «мне надо чтобы ты весь тул проапдейтил»,
«чисто прувер такой», «прувер может валидировать что угодно», «если прувер дырку нашёл — можно
пользователя спросить, или закрыть и написать как закрыл, если тривиально», «не надо мучать
пользователя вопросами как на пытках»; plus the tlvphoto window's assessment (its inbox note) as the
evidence base.

| # | Finding | Severity | Folded / rejected |
|---|---|---|---|
| F1 | INV-29's walker could read as a questionnaire aimed at the human — contradicting INV-4/INV-12 and his explicit word | must-fix | FOLDED into the clause's own law: the walk interrogates the FEATURE, not the person — derive from the existing spec + shipped truth, close trivially-closable holes yourself AND WRITE HOW (his word), `[default]`-tag the rest, batch only genuine taste calls (same law as INV-18) |
| F2 | Duplicate-home risk: journey lens lists could end up stated in SPEC, spec-author, prover, and pipeline (INV-13 violation) | must-fix | FOLDED: lens lists live ONCE in spec-author's sweep section (like the facet list, curated with incidents); SPEC binds THAT the walk runs; prover names the mode; pipeline step 1 points |
| F3 | INV-30 vs INV-22: the verify step's per-kind FORM has one home (build-pipeline's table); a second full statement in SPEC would fork it | must-fix | FOLDED: SPEC clause binds THAT a product-kind verify includes the visitor walk + feel pass; the walk's checklist lives in the step-8 product cell |
| F4 | INV-31 liveness: who carries the default count across sessions? A counter file would be a new state home | must-fix | FOLDED: the landing report restates the OPEN defaults list with age-in-landings every time (the resume file's INV-26 discipline, same shape); a list dropped between reports is a named defect; no new state file |
| F5 | INV-31 mechanics are prose-level (no guardrail counts `[default]` age yet) — could read as enforced when it is not | worth-considering | RECORDED honestly in the row: suite covers clause presence; a mechanical age-checker is a candidate wish, not implied |
| F6 | INV-32 vs communicator rule 1 (ask by showing): a consequence may be SHOWN rather than told | none | Checked — compose cleanly; the card leads with the consequence in words or in a shown before/after; no conflict |
| F7 | FEATURE-FIT mode naming: one-name law across prover modes (FULL · CROSS-LINK · FEATURE-FIT); the mode must not silently replace CROSS-LINK on surface adds | must-fix | FOLDED: FEATURE-FIT is the intake-time walk of ONE feature's journey seams; CROSS-LINK remains the surface-add pass; both named in the modes list, each with its trigger |
| F8 | Retroactivity: do already-landed features owe a retro fit walk? | none | Bound forward like INV-20/21: an adopted/landed feature owes its walk at the first landing that touches it — stated in the clause |

Regression fences (T-14): INV-18's facet law, E-22's page mechanics, INV-27/28's echo/board/line laws
all untouched — discharged through their existing rows (M-96-family, M-111/112/113 never-sides).

Non-goals: no retro-walk en masse; tlvphoto's own five fixes stay in their window (its inbox already
has them); no mechanical default-age guardrail this landing (F5). Success measures [default]: 108 —
the next product-kind wish in an adopted host ships with journey sentences and he never has to ask
«а отсюда куда?»; 117 — nothing green-but-cheap reaches his eye before the walk names it; 118 — no
default older than two landings anywhere; 119 — he answers every decision card without asking what
it means.

Open ⟨DECIDE⟩ touched: none (D-1/D-2/D-3 untouched).
