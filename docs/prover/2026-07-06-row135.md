# Prover record — 2026-07-06, row 135 (parallel lanes), CROSS-LINK

Mode: CROSS-LINK — the new law (T-18 parallel lanes · INV-39 landing purity · re-authored INV-2 pen ·
ACT-3 isolated-tree line) walked against its seams: INV-2 claim/back-off, ACT-3, INV-11, INV-27 board,
T-9 bug preemption, M-1 milestone gate, INV-26 row closing, E-22 batched decision page.

Previous record checked: `2026-07-06-row130.md` — no unfolded rows (13 findings: 7 folded, 4 rejected
with reasons, 2 re-queued as row 137). `2026-07-06-row133.md` — clean (both folded in-pass).

Opening assessment: the pen model is sound — one write-token over the shared truth keeps every existing
safety property (atomic claim, foreign back-off, landing purity) while lifting the long mechanical
stages into parallel. The two real defects were seam contradictions with T-9 and with INV-39's tree
purity; both fold cleanly. Ready after folds.

## Findings

F1 — Bug preemption promises "at most one wish is ever parked"; two rolling lanes park two
> "so at most one wish is ever parked. [T-9]" — When a bug cuts the line
A bug arriving while two trains roll must park BOTH lanes (the bug takes the pen and the senior's
hand); the reader of T-9 would conclude the second lane somehow keeps running, or that opening it was
illegal. Fold: T-9 states per-lane parking — at most one parked wish PER LANE, both resume in landing
order once no bug waits.
`must-fix · direct-contradiction (contradiction) — FOLDED in-pass`

F2 — The board's waiting-for-pen face was designed but never written
> "both trains appear on the departures board [INV-27]" — Throwing a wish / T-18
The wish explicitly asks how the board reports two moving trains; a lane blocked at integration shows
the same station line as a rolling one, and the human cannot tell waiting from working. Fold: T-18
adds — a lane waiting for the pen SAYS so on its board line and names the row it waits behind.
`must-fix · hard-to-monitor (observability) — FOLDED in-pass`

F3 — A second lane's disjoint-file workers in the shared tree break INV-39's landing purity
> "code and tests written in an isolated copy of the tree or on brief-named disjoint files" — T-18
If lane B's workers write real files in the shared tree, lane A's landing gate runs on a tree holding
B's unfinished work — exactly what INV-39 forbids; A's suite can go red on B's half-done red-first
test. Fold: the SECOND train's writes live in its isolated tree only; the disjoint-file road stays
legal WITHIN one lane (a lane's own workers, landing with that lane's commit).
`must-fix · internal-conflict (consistency) — FOLDED in-pass`

F4 — "Release point" used but never defined
> "it takes the pen and the senior's hand at the next release point" — T-18
An undefined moment invites the wrong read (interrupt mid-edit of the spec). Fold: reworded — the bug
takes the pen at the end of the current pen-stage; a pen-stage is never cut mid-edit.
`should-clarify · undefined-path (transitions) — FOLDED in-pass`

F5 — The pen's document list reads as exhaustive; the journal and resume file are shared truth too
> "edits to the shared documents (the spec, the architecture doc, the matrix, the queue)" — T-18/INV-2
Two lanes writing JOURNAL.md or NEXT_STEPS.md concurrently is the same collision the pen exists to
prevent. Fold: the list names the class — every document both lanes share, the journal and resume file
included.
`should-clarify · over-specific (abstraction) — FOLDED in-pass`

F6 — Milestone gate covered one direction only
> "a milestone's whole-spec operations run with one train only — no second lane opens mid-gate" — T-18
The converse was unstated: a milestone arriving while two trains roll. Fold: the milestone opens only
once a single train rolls (the other lands or parks first).
`worth-considering · missing-prerequisite (precondition) — FOLDED in-pass`

F7 — "Lane token" vocabulary survives one spot after the pen re-author
> "the re-doored wish KEEPS the lane token" — re-door paragraph (~line 232)
The re-authored INV-2 dropped "lane token" for "pen"; the dangling old term is a second name for one
concept (one-surface-one-name). Sibling sweep run: no other occurrence. Fold: reworded to "keeps its
lane".
`should-clarify · naming (consistency) — FOLDED in-pass`

## Seams checked clean
- INV-2 atomic claim + foreign back-off: preserved verbatim in the re-author; cross-session law whole.
- INV-26/M-2: two open lanes at compaction are two restated LIVE-STATE items — existing law already
  plural; no edit.
- E-22: cross-lane questions on one page, cards naming rows — written in T-18; mechanics stay in
  communicator rule 10.
- ACT-3 sibling-worker fence-benignity across lanes: one senior briefs all hands and owns the seams —
  existing sentence already carries it.
- Liveness (a lane starved of the pen): impossible under one senior sequencing pen-stages; the pen is
  judgment-scheduled, not contended.
- Line 202 ("only the bug DOOR preempts the in-work lane") reads correctly under multiple lanes.

⟨DECIDE⟩ still open elsewhere: D-1 / D-2 / D-3 — none touched by this delta.

| # | finding | severity | status |
|---|---------|----------|--------|
| F1 | T-9 parked-count contradiction | must-fix | folded in-pass |
| F2 | waiting-for-pen board line missing | must-fix | folded in-pass |
| F3 | cross-lane shared-tree writes vs INV-39 | must-fix | folded in-pass |
| F4 | "release point" undefined | should-clarify | folded in-pass |
| F5 | pen's doc list read as exhaustive | should-clarify | folded in-pass |
| F6 | milestone-opens-while-two-roll unstated | worth-considering | folded in-pass |
| F7 | dangling "lane token" | should-clarify | folded in-pass |
