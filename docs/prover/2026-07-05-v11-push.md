# Prover pass — SPEC v0.11.0, push gate for pack 0.5.2 ("Row 72 lands") (2026-07-05, session 7)

Mode: **FULL** (whole spec, every phase, all NINE stress lenses). Run as the M-6 push gate for pack
version 0.5.2. This is also the FULL discharge owed by the facet-sweep CROSS-LINK pass
(`2026-07-05-facets.md`, F4): a MINOR bump (0.10.1 → 0.11.0) requires a FULL re-prove before the push, and
this pass supplies it.

**Prior context.** v0.10.1 passed a FULL push-gate pass earlier today (`2026-07-05-v10-push.md`, zero
must-fix, six clarification-grade findings all FOLDED same sitting, nothing owed to the queue). The only
delta since is the facet-sweep clause pair **T-13 / INV-18**, CROSS-LINK-proven in
`2026-07-05-facets.md` — 1 must-fix + 6 lesser, **all 7 folded same sitting**, nothing owed to the queue.
This pass re-verified every one of those 7 folds reads as folded in the current prose (F1 → INV-18's literal
`[default]` tag; F2 → sweep pinned to the spec-delta step, no "intake" wording left; F3 → mid-work re-door
walks the sweep before resuming; F5 → sweep AUTHORS / C-1 COMPOSES split named; F6 → adopted/promoted
surface reconciles from shipped truth; F7 → fenced prototype not swept). **F4 discharges here.** No prior
pass left any unfolded row to carry forward.

**Verdict: ready-to-push, zero must-fix.** One should-clarify surfaced by the FULL sweep (F1 below) — a
state-space edge the facet delta opens; per M-6 only must-fix folds before the push, so it ships as a queue
row, not a blocker. No regression: the v0.11.0 prose reverses no clause proven at v0.9.1, v0.10.1, or in the
facet CROSS-LINK; the delta paragraphs compose cleanly with T-12/INV-16 (door law), INV-4/INV-5 (batched,
no silent decisions), E-15/INV-15 (matrix rows), A-3/A-10/C-1 (adoption + provenance), and E-17/INV-17
(prototype fence).

**Mechanical guard (senior-run).** Formal-index ↔ prose anchor symmetry EXACT both directions: **71 index
rows** (T-1..T-7 collapsed to one range row) expand to **77 distinct anchors**; the prose carries the **same
77**; prose-not-index and index-not-prose are BOTH empty. **Zero duplicate anchors** (raw index rows and
expanded set both collision-free). Index one-liners match their home sections. Header reads v0.11.0.

| # | Finding (one line) | Severity | Outcome |
|---|---|---|---|
| F1 | The facet sweep assumes a **visible surface**, but INV-16 fires the feature door on "new persistent state appears" even with **no new surface** (e.g. "cache the last query"). T-13/INV-18 then demand a spec sentence per facet — phone layout, touch-vs-hover, empty/error/loading — for a feature that has nothing to lay out. The senior drafting that spec-delta either writes meaningless facet sentences or the prover flags "missing facet" as a defect on a headless feature. Fix: state the sweep is scoped to the feature's VISIBLE surfaces; a feature adding no new visible surface satisfies INV-18's "silence is not an option" with ONE explicit line ("no visible surface — facets N/A"), not a defaulted sentence per facet. | should-clarify · missing-scenario (state-space) | **FOLDED**: sweep scoped to VISIBLE surfaces; a headless feature satisfies INV-18 with one explicit "no visible surface — facets N/A" sentence (T-13 reworded) → v0.11.1 |

Nothing must-fix. F1 becomes a queue row (headless-feature facet scope). Anchor set unchanged. This record
ships with the 0.5.2 push.

The single should-clarify FOLDED same sitting by the senior -> SPEC v0.11.1; anchor set unchanged (no new anchor). Nothing left for the queue from this pass.
