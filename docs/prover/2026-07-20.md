# Prover record — 2026-07-20

## Prior record check
Opened docs/prover/2026-07-19.md (comms build, on branch wip/comms-naming-424): its unfolded rows (N1–N4)
belong to the parked comms feature, out of scope for today's slice. Nothing on main's prover trail is
unfolded for the surfaces this delta touches.

## Under review
The hedge-gate delta (INV-238, M-420, ROADMAP 429): a new Stop-hook literal-scan machine that reds a
reply carrying an offering-hedge frame. Authored by a worker seat; proven here by a separate seat
(INV-237). Mode: CROSS-LINK — the new machine's seams against the named existing scan gates.

## Checks and findings

| # | Check | Verdict |
|---|---|---|
| 1 | Every spec fact owns a node | PASS — INV-238 added to the `guardrails [target]` node, pin `hooks/hedge-scan.py:1` [target]. |
| 2 | No node without spec backing | PASS — no new node; a fact added to the existing scan-gate node. |
| 3 | Every seam names what crosses it | PASS — stdin JSON payload in (transcript_path → last assistant turn), `{"decision":"block"}` JSON out, mirroring the scissors and answer-first seams. |
| 4 | Quality budget + instrumentation home | PASS — runs/fires read by the net-meter [INV-202]; success measure is the fixture suite (every hedge fixture reds, every legitimate-question fixture green). |
| 5 | Runtime view walks the flow | PASS — reply emitted → Stop hook fires → hedge-scan reads the transcript → a surviving pattern match blocks the stop with a rewrite instruction, one turn later (the same one-turn floor INV-203/INV-220 carry). |
| 6 | Placement stated | PASS — runs client-side at the Stop event on the author's machine, like its siblings. |

Cross-link seams named and routed to the code step as wiring (not spec gaps):
- config-health parity [INV-175, INV-180] — the new installed copy must be covered; the code step adds it to whatever roster the parity check reads.
- judge-hooks.json [INV-211, gate v] — `hedge-scan` must be listed under `wired: Stop` AND wired into `~/.claude/settings.json` hooks.Stop, or gate v/m reds. The code step does both.
- net-meter [INV-202] — the live hook is wrapped by `hook-meter.py` in settings, like scissors.

Blocking findings: none. Recommendations: none.

## Design review (surface-add cadence, INV-141)
The undeclared same-kind group is the Stop-hook literal-scan gates (scissors INV-173, answer-first
INV-220, hedge INV-238). Behaviour parity holds by construction: one mechanism (transcript read →
pattern match → block-with-rewrite, always exit 0), one seam shape, net-meter coverage, config-health
parity, judge-hooks classification. INV-238 joins the declared class with parity; no new class clause is
owed, no divergence surfaced.

## Language-of-patterns note (author's seat interpretation, not the human's word)
English hedge frames ship universal in the pack (inline PATTERNS). Russian hedge frames ride the
personal overlay `~/.claude/hooks/hedge-personal.json`, the same universal-versus-personal split the
scissors scan carries [INV-173, INV-203] — the pack ships nobody's personal-language rules. Derived from
the scissors precedent, cited, not a taste fork.
