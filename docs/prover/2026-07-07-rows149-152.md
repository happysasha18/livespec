# Prover record — 2026-07-07 ~00:01, rows 149 + 150 + 151 + 152 (one serial lane), CROSS-LINK ×4

Previous records: pushgate-s22-5 (clean), rows110-114-115 (folded/recorded) — nothing unfolded rides.

## Row 149 — lanes picked by a graph (INV-49, beside T-18/INV-39)
Opening assessment: the law writes down what tonight's deed already proved both ways (three medium
rows parallel; five tiny rows serial). Ready.
| # | Finding | Severity · category | Status |
|---|---|---|---|
| F1 | Four rows sit in-work at once for THIS lane while T-18's cap says three — a reader could call the queue illegal. | should-clarify · internal-conflict (consistency) | FOLDED — INV-49's sentence: rows joined by edges "serialize inside one lane"; the cap counts LANES, not the serial rows riding one; the claim line names the one-lane declaration; M-022's row wording already keys the cap to T-18 |
| F2 | "Tiny" is judgment. | worth-considering · over-general (abstraction) | RECORDED — deliberately the senior's call, said aloud on the board (the law makes the SAYING mandatory, not the measure); a numeric threshold would be a vanity metric (INV-41 kin) |

## Row 150 — entry symmetry (INV-50, beside INV-29)
Opening assessment: the tlvphoto miss re-told as law; the lens mirrors it in the prover. Ready.
| # | Finding | Severity · category | Status |
|---|---|---|---|
| F1 | Two homes risk: the face-law (spec content) vs the lens (prover) could drift apart. | should-clarify · boundary-issue (composition) | FOLDED — SPEC owns the LAW (INV-50); the prover's lens paragraph cites it and adds only the trigger patterns; spec-author's journey lens gains one clause POINTING at the law, never restating it |

## Row 151 — the artifact passport (INV-51) · Row 152 — windows accumulate (INV-52)
Opening assessment: two communicator rules born of four of his messages inside one hour, both already
walked by deed on the live decisions page (passport header ~23:35; no new windows since ~23:37).
Ready.
| # | Finding | Severity · category | Status |
|---|---|---|---|
| F1 | The passport vs the show rule (open in a NEW window) and INV-52 (no windows mid-stretch) could read as three contradicting commands. | must-fix · internal-conflict (consistency) | FOLDED — precedence stated in the communicator rule: INV-52 governs WHEN (accumulate during an away-stretch, one opening at its end), the show rule governs HOW (a new window at that opening), INV-51 governs WHAT the artifact leads with (its passport) — three axes of one act, no collision |
| F2 | "Away-stretch" needs its trigger: who says the human is away? | should-clarify · missing-prerequisite (precondition) | FOLDED — the offline window (INV-35) IS the trigger: the stretch between an announced "you may step away" and its needed-again beat; outside such a window the normal show rule stands |

Cross-links that hold: INV-27/INV-28 (board lines already govern lane narration; INV-49 cites);
INV-39 (landing purity untouched — the graph only PICKS, order still declared); INV-32/rule 10 (the
decision page keeps its own JSON law; the passport rides ON it, both live on the same page tonight);
E-17/prototype (none touched). Sibling sweep: no other clause speaks of window-opening cadence; the
render/show line in the profile stays the HOW (his personal tuning), the pack law the WHEN.

Findings: six — five folded in-pass, one recorded as the senior's said-aloud call.
