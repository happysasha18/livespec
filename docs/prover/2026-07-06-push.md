# Prover record — FULL pass over SPEC v0.15.18 → v0.15.19 (push gate M-6)

Date: 2026-07-06 ~11:57, session 12. Scope: the WHOLE spec as it stands before the 0.8.30 push —
required by the host push gate (full re-check before EVERY push) and due anyway: the morning landed
seven rows (105 echo/board · 116 outcome-leads · 108/117/118/119 product-fit family · 120/121/122
same-hour corrections) since the last FULL record (2026-07-05-v15d-push.md). Previous records checked
first: rows108-119 and feature-fit-retro carry no unfolded must-fix rows.

Opening assessment: the spec is coherent and buildable; the morning's four new invariants (INV-29..32)
composed cleanly with the older law in the CROSS-LINK pass, and this FULL walk confirms it — the holes
found are seams where OLDER text still speaks the pre-INV-31 language, plus one spec-lags-test gap.
All must-fixes folded below, locally, same session.

| # | Finding | Severity | Folded / rejected |
|---|---|---|---|
| F1 | CLASS: the taken-default example still ASKS confirmation ("…stacks into one column — ok?") and routes defaults through the QUESTION batch — contradicting INV-31 (told, never confirmed; silence is consent) written the same morning. Four sibling instances swept: SPEC INV-18 clause · spec-author facet section · communicator rule 10 · build-pipeline step 1 ("reported…, batched") | must-fix (internal-conflict / consistency) | FOLDED in all four homes: a taken default is TOLD on the landing report's defaults list ("…— tweakable"), never a confirmation request; the question batch keeps only the genuinely open picks. Matrix M-073 re-worded to match. Suite needles unaffected, 108 green |
| F2 | Spec lags a red-proven test: matrix M-118 + `test_install_backup_home` enforce "backups land OUTSIDE the live skills dir" (row 122, the bug he hit by deed), but E-21's prose never states it — the living truth missing the exact promise the fix made | must-fix (missing-rule / invariant; S-0 honesty direction) | FOLDED into E-21: one sentence — the backup lands in an attic folder BESIDE the skills home, never inside it, so the agent never scans a stale copy as a live skill |
| F3 | `test_default_expiry_law`'s docstring still asserts the ABOLISHED expiry law ("may not outlive two landings unreviewed") while its body checks the new told-never-confirmed law — a false claim in code | should-clarify (hard-to-operate / ops-ux) | FOLDED (trivial): docstring now states the rewritten law; function name kept — it is the matrix pin, renaming is churn without meaning gain |
| F4 | INV-27 promises "the station vocabulary is the pipeline's own step names, one name per step" — but the pipeline walks NINE steps and the board lists EIGHT stations: prove-architecture and commit-and-show have no station, so a feature paused there gets an improvised name (the exact drift the one-name law stops) | should-clarify (internal-conflict, mild) | QUEUED: row 125 (both homes: SPEC INV-27 + communicator's board rule) |
| F5 | TEST_MATRIX and ARCHITECTURE headers still pin SPEC v0.7.x | none (known rhythm) | REJECTED as a gate finding: header re-pinning is the milestone's own step (M-1), not the push gate's; noted so the next milestone doesn't miss it |

Coverage sanity walked: every INV-27..32 clause has its matrix row (M-111..M-118) and a green test;
anchor ownership and matrix-coverage boxes ride the suite (108 green after folds); [target] map intact
(S-0 test family). CRUD/authorization tables: N/A for this product — a method pack with no
user-mutated persistent entities beyond its own documents, single-human, no roles.

Acknowledged gaps: D-1/D-2/D-3 stand open (untouched by this pass); README research paragraph awaits
his word and does NOT ride this push.

Folds from this pass stay local to the sections their findings name (INV-18 sentence, E-21 sentence,
four skill/matrix/test echoes of F1, one docstring) — no wider reach, gate not re-triggered.
Versions after folds: SPEC v0.15.19 · spec-author 0.1.13 · communicator 0.1.17 · build-pipeline 0.2.19
· pack 0.8.30.

Verdict: ready to push once the eval re-runs (E-19) and the publish walk (E-20) complete.
