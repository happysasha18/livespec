# Prover pass — SPEC v0.10.0 delta (rows 70–71: the door law + the prototype law), CROSS-LINK (2026-07-05, session 6)

Mode: CROSS-LINK (the new surface's seams against the named existing surfaces), per the pipeline's step-2
rule for a surface add. Run on an Opus worker; senior (Fable) triage + folds. Delta under review: the
"door is named before any code" paragraph (T-12, INV-16), the section "A prototype is not the product"
(E-17, INV-17), and the Machines live-guardrails line — the Room-incident landing.

Verdict as returned: must-fix-first, 4 blocking (F1, F3, F4, F5 — all on the exact seam the delta exists
to close). All 10 findings FOLDED same sitting; fold wording is the senior's.

| # | Finding (one line) | Severity · class | Outcome |
|---|---|---|---|
| F1 | The door is a second per-row scale, which INV-12 forbids; both scales contain "bug" | must-fix · internal-conflict | **FOLDED**: two named axes (size · priority · door) in ONE intake line; size-bug IS door-bug, one call stated once; INV-12 reworded to "never a second SIZE scale" |
| F2 | Door-naming has no position in the wish walk T-1..T-7; intake vs landing report lines conflated | should-clarify · undefined-path | **FOLDED**: walk now reads "classified by size, priority, and door — stated back in one INTAKE line"; closing line named the LANDING line |
| F3 | Only the feature door had criteria; bug/refactor/docs-only/skip undefined | must-fix · undefined-path | **FOLDED**: ordered procedure (tripwires first → feature; wrong shipped behaviour → bug; identical behaviour, structure moves → refactor; prose only → docs-only; the narrow all-hold boundary → skip) |
| F4 | A tripwire-firing wish the human calls "bug" is unresolved — a queue-cutting exploit via T-9 | must-fix · internal-conflict | **FOLDED**: tripwire verdict outranks a casual label, re-door said in the intake line; queue-cutting belongs ONLY to the bug door; the human's word can raise priority, no word skips the spec step |
| F5 | Door law and prototype law both claim the casual "покажи" with no boundary and no mid-build re-check | must-fix · boundary-issue | **FOLDED**: SEE/TRY with no commitment → labelled sketch, no lane no spec; HAVE in the product → feature door; unclear ⇒ one plain question; plus the mid-work re-fire — work STOPS the moment it is about to create a surface/state its door doesn't grant |
| F6 | Adoption meets a pre-existing unlabelled prototype in prod: no rule | should-clarify · missing-scenario | **FOLDED**: A-10 — every unbacked live surface gets a human verdict at orient: promote / quarantine / attic |
| F7 | "Visible PROTOTYPE label" defined only for visual artifacts | should-clarify · missing-rule | **FOLDED**: label per artifact kind (screen banner · `_prototype: true` field/header · first-line CLI banner · name/header marker) |
| F8 | Who may CREATE a prototype unstated; collides with INV-10/ACT-3 | should-clarify · unclear-owner | **FOLDED**: opening a prototype home = repo write under INV-10, judgment act of the assigned senior; outsiders → inbox wish; workers never on their own brief |
| F9 | "Honesty holds in BOTH directions" stated present-tense while 2 of 3 legs are [target] | should-clarify · unenforceable-promise | **FOLDED**: qualified — fence leg live today, completeness + behaviour-traces [target]; both directions hold when all three land; index one-liner matched |
| F10 | The BINDING "not yet specified" tripwire has no canonical machine-checkable form | should-clarify · unenforceable-promise | **FOLDED**: the existing [target] tag IS the canonical form (one token, one meaning); plain-prose cousins still bind, spec-author emits the tag |

Formal-index check (worker): T-12, INV-16, E-17, INV-17 one-liners match prose — no drift; A-10 and the
INV-17 qualifier added with the folds, index updated in the same edit.

Open ⟨DECIDE⟩ count after this pass: unchanged (D-1, D-2, D-3 remain; none touched by this delta).
Readiness: fold-and-proceed — pipeline continues to architecture / matrix / tests / code.
