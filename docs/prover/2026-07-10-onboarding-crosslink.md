# Prover record — CROSS-LINK on F-onboarding (2026-07-10 morning, session 32)

**Mode:** CROSS-LINK (new surface's seams against the named existing surfaces). **Prover:** a fresh
opus session with the product-prover skill loaded (spawn-per-item; the senior folded and gated).
**Delta:** PRODUCT_SPEC v0.16.22, new scenario "Meeting your settings" [feature: F-onboarding],
anchors INV-87/INV-88, norm docs/norms/onboarding-card-2026-07-10.html.

Seams walked: B-2/B-3/INV-36/INV-85 (founding), A-1 (orient), E-13/INV-14/INV-9 (settings ladder),
INV-31 (defaults told), INV-67 (seat channel), INV-83 (pre-show lint), INV-84 (clean writer),
INV-43 (norm), E-29/INV-73 (feature coverage), T-19 (economy), E-14 (one home). Verdict at review:
structurally sound; one must-fix and two render-contract seams to reconcile before build.

| id | finding (one line) | severity | disposition |
|---|---|---|---|
| PF-1 | "every table row appears" collides with the curated approved norm (workshop rows would clutter a reader's page) | must-fix | FOLDED same hour — the base table gained a per-row Card mark (visible/internal, base 0.1.31 with a legend line); INV-87 reworded to card-visible completeness + host lines in the project-rules part |
| PF-2 | fixed copy's "always in good English" overclaims against the overridable `language.docs` rung | should-clarify | FOLDED — "always" dropped; the fixed copy states the rule frame, the value comes from the settings ("the project's recorded documentation language, good English out of the box") |
| PF-3 | "its current value" contradicted the norm's rule-only personal rows | should-clarify | FOLDED — a value prints where one is recorded, labelled the reader's own; an unrecorded row shows the rule alone (exactly the norm's no-profile look) |
| PF-4 | the card's showing named no register-lint gate | should-clarify | FOLDED — one sentence added: the showing passes the pre-show register lint on fixed copy and rendered values alike [INV-83] |
| PF-5 | F-onboarding had no owning node, script, or coverage row yet | should-clarify | DISCHARGED by the pipeline's own next stations — node onboarding-card + three seams + coverage row + runtime flow + placement + budget landed in ARCHITECTURE.md; matrix M-206..M-210; tests proven red before code |
| PF-6 | a future table row's card copy had no named author | worth-considering | FOLDED — one sentence added: a new row's card rule-copy is drafted on the clean-writer road before it first renders [INV-84] |

All folds drafted by a clean writer (INV-84) and landed the same hour; the base-table Card column and
pins are the senior's mechanical hand. Architecture delta after the folds walks the six checks: every
new fact owned (INV-87/88 → onboarding-card), no unbacked node (the node maps to the shipped script and
frozen norm this landing builds), seams name their crossings and format owners, the budget has its
instrumentation home (the render script's run + matrix row), the runtime view walks the flow with both
failure branches, the placement view says where it runs (the agent session on the host machine, output
in the browser).

**Verdict: proceed to build; the render contract is reconciled.**
