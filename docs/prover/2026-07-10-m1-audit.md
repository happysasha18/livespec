# M-1 pre-1.0 audit — five angles, one record (2026-07-10 morning, session 32)

The milestone gate before 1.0.0, run as five parallel read-only reviewers (each a fresh opus session;
the senior folded and gated). The owner's two additions to the classic three passes stand: the
convergence-locks pass (his 2026-07-10 ~10:24 word, row 217) and the skill-creator sweep (his ~10:29
word, row 219's catch-up). Full per-angle reports live beside this session's checkpoint
(.live-spec/checkpoints/m1-final-audit.md names the five paths).

## Verdicts by angle

| Angle | Verdict | Findings → disposition |
|---|---|---|
| 1. Whole-spec FULL prover pass (v0.16.22, 1758 lines; index both ways; ⟨DECIDE⟩ and [target] live-checked; prior records reconciled — no unfolded must-fix rides) | READY FOR 1.0 — 0 must-fix, 2 should-clarify, 4 worth-considering | MF-1 (the card's third failure branch) FOLDED — the blocked state joined the card's state enumeration + the architecture flow row; MF-5/F4 (dead ≠ done) FOLDED — one sentence in INV-76 body + index; MF-2/3/6 ride as queue rows; MF-5's F3 half stays row 197 |
| 2. Matrix audit (170/170 anchors covered · all 213 rows carry the never side · 0 ghost tests · 0 dishonest levels · suite 286 green, 0 skips) | PASS — 2 cosmetic | D1 (a pinned count drifting in prose) FOLDED — the line now defers to the mechanical recount; D2 (budget honesty) FOLDED — the renderer reports its own render time, the test asserts < 1000 ms from that instrumentation |
| 3. Surface composition (14 surfaces, 27 pairs) | mature surfaces 14/14 clean; the card owed its axis sweep | F2 FOLDED — four composition sentences landed in the card's section (read-only concurrency · render-moment truth + fresh re-ask [default] · independent co-presence · unattended stretch); F1/F3-F5 are its instances, closed by the same sentences |
| 4. Convergence locks (13 locks enumerated) | 8 held by test, 5 attention-held → now 11/13 | F1 (norm fingerprints) FIXED — scripts/norms-manifest.json + test, red-proven mechanically; F2 (lint pattern floor) FIXED — scripts/register-lint-floor.json + test, red-proven; F3 (debt-cap ratchet) FIXED — the caps pinned in the owning test, red-proven; F4 (anchor guard only-on-invoke) + F5 (local hook bypass; CI backstop needs branch protection — the owner's one GitHub click) → post-1.0 row |
| 5. Skill-creator sweep (8 skills) | ALL SOUND | three polish edits + one reference check → the post-1.0 law batch; two divergences from skill-creator's model examined and kept deliberately (load-by-reference for the base; event-triggering for feedback-intake) |

## The gate

Spec v0.16.23 (the three folds landed through the clean-writer road), matrix + architecture updated in
the same change, three new lock tests (M-214..216) each proven red by the mechanical protocol. Suite
re-run after all folds — the tail line read directly. No must-fix remains open; every should-clarify is
folded or owned by a named queue row. Rows 173-187 archive at the milestone per the standing note.

**Verdict: the pack is ready for 1.0.0. The bump and the release wait on Alexander's explicit go — his
named gate, never the agent's.**
