# Prover record — INV-169, the second-sibling intake question (2026-07-16, FEATURE-FIT)

Prover skill version: 1.1.4 (pre-fold; this record reviews the fold that lands INV-169).
Mode: FEATURE-FIT on one delta — the second-sibling law. The independent deep pass that motivated
it is the Fable adversarial audit, `docs/audit/2026-07-16-prover-fable.md` (finding D2); this
record checks the fit of the fold that answers it.

## The delta
INV-169 — "A feature delta that adds a second member of an existing kind draws the scoped design
review at intake." FEATURE-FIT gains one standing question (is anything in this delta a second
member of a kind an existing surface already has?); a yes draws the scoped design review, and
INV-141's intake stand-down is re-scoped to hold only for a sibling-free delta.

## Fit walk (journey seams of the changed law)
- **Arrival** — the question fires inside FEATURE-FIT, which runs with the spec step at intake
  (INV-29); backed by the amended mode text. HOLDS.
- **Every next-step** — a yes hands the delta's elements to the scoped design review against the
  existing inventory (the same pass a surface add draws, INV-141); a no lands as a lens verdict in
  the FEATURE-FIT record (INV-29's verdict-per-lens form). Both exits stated. HOLDS.
- **Return visit** — a later pass re-reading the record sees the second-sibling verdict beside the
  other lens verdicts; no new record class is minted (INV-156 untouched). HOLDS.
- **Cross-entry** — a surface add already draws the scoped review by its own keying; the two
  triggers compose without double-running, since intake and surface-add are distinct stations.
  HOLDS.
- **Implied neighbour state** — the design review's own loop and cap (INV-154) govern what a drawn
  scoped pass does next; nothing here changes the loop. HOLDS.
- **Contradiction check** — INV-141's old sentence ("stands down at feature intake and at the push
  gate") would contradict the new law; it was re-scoped in the same landing. The design-reviewer
  skill's keying bullet likewise. Swept: no other clause states the intake stand-down
  unconditionally (`grep "stands down"` over the spec and both skills). HOLDS.

## Disposition
| Finding | Kind | Disposition |
|---|---|---|
| Fable audit D2 (second-sibling blind window at intake) | defect | folded — INV-169, M-323, both skills |

Verdict: the delta FITS; no open questions ride. The batch-close FULL pass re-reads the whole spec
including this fold.
