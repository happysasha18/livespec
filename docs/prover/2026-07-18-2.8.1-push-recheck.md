# Push re-check — 2.8.1 patch (2026-07-18)

Prover skill version: 2.8.0 (the release stamps the pack to 2.8.1).

A PATCH by INV-217: the register judge now holds base rule 2's already-stated law — "never open a line
with a code" — on the chat surface. No new law and no new capability a host adopts; the host takes it and
does nothing. The judge's universal law body gains a second class (a bare internal code opening a chat
sentence, the codes trailing as quiet anchors instead), and the chat-law assembly renumbers the joined
universal and personal laws into one sequence so the judge cites a law by a number no overlay base can
collide with. INV-203's clause and Formal-index row are updated to name the universal law as plural
(scissors plus the bare-code class); the mechanism is `hooks/register_judge_core.py` and
`hooks/register-judge.py`.

## Commits (origin/main 06662fb..HEAD)
- this record's commit — the judge's bare-code universal law and the renumber step, the INV-203 clause
  and row update, tests/test_register_judge.py's five new cases, the 2.8.1 stamp, and this record.

## Verdict
The spec content change is confined to INV-203's clause and row (the universal law is now plural) — no
other invariant, transition, or Formal-index row moved, and the index stays contiguous through INV-237.
The register-judge suite is green (36 cases), the new cases proving the universal law carries the
bare-code class, that renumbering yields a single 1..N sequence with no duplicate, and that a bare-code
lead reds while a trailing anchor passes. The installed judge copies were re-synced
(`scripts/install-pack-hooks.sh`), so config-health reads no drift. The spec and architecture register
lints report zero errors. Push-ready.
