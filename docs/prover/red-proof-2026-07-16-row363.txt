Row 363 — paired-transition lens gains the magnitude sub-question (INV-126, matrix row M-341)
================================================================================================

Red-first proof, run against HEAD (commit 8bbf160, "Sweep inbox: two tlvphotos wishes into rows
363-364 (magnitude lens; headless-shell + frame probe)") before any of this row's own edits landed
in the working tree.

Method: `git worktree add "$TMPDIR/ls-head-B" HEAD`, copy the row's updated
tests/test_paired_transition.py (with the new test_magnitude_sub_question) into the worktree, run
the suite there. HEAD carries none of this row's changes — not the senior's PRODUCT_SPEC.md
magnitude clause, not the skills/product-prover/SKILL.md or skills/spec-author/SKILL.md prose —
so the new test reds on its first assertion (the spec arm), before it can even reach the prover-
and spec-author-SKILL.md arms.

Command:
  cd "$TMPDIR/ls-head-B" && python3 -m pytest -q tests/test_paired_transition.py

Result: 1 failed, 7 passed in 0.37s

Failing test: TestPairedTransition::test_magnitude_sub_question

    def test_magnitude_sub_question(self):
        spec = read_flat("PRODUCT_SPEC.md")
        for needle in ("same magnitude as the forward move", "a named deliberate asymmetry"):
>           self.assertIn(needle, spec, needle)
E           AssertionError: 'same magnitude as the forward move' not found in '...' : same magnitude as the forward move

    tests/test_paired_transition.py:74: AssertionError

All 7 other tests in the file (pre-existing INV-126 coverage) pass unchanged at HEAD — only the
new magnitude-sub-question test is red, and it reds for the expected reason: the spec's magnitude
clause is absent at HEAD.

Cleanup: `git worktree remove --force "$TMPDIR/ls-head-B"` run after capture.
