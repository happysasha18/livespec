# Prover record — 2026-07-06 ~21:44, rows 143 + 144 + the row-140 amendment (CROSS-LINK), SPEC v0.15.36

Scope: three deltas from Alexander's one correction message (~21:33): (1) row 143 — the architecture
step owes measurable quality budgets + an instrumentation home (INV-41); (2) row 144 — the session's
task list speaks plain product ENGLISH, his correction of the session's first Russian reading of the
wish; (3) row 140 amendment — the economy rung is asked (or the default told) at a project's setup,
alongside the kind question.

Previous record: docs/prover/2026-07-06-pushgate-s20.md — no unfolded rows ride.

## Findings

| # | Finding | Severity · tag | Status |
|---|---|---|---|
| F1 | INV-41's defect clause could read RETROACTIVE — every existing budgetless architecture (every current host) instantly defective. | must-fix · composition (INV-15 seam) | FOLDED — the clause now binds from the first landing that touches the surface after it exists, never retroactively, mirroring INV-15's own bring-up rule |
| F2 | The task-list rule's language could collide with `language.chat` (chat runs Russian for this human; the session first read the wish as "rename to Russian" and he corrected it). | must-fix · internal-conflict | FOLDED — the rule names the DOCS language explicitly: the task list is a work artifact, not chat; subjects follow `language.docs` (English), understandable at a glance, codes only trailing |
| F3 | The setup-phase ask could contradict INV-36's "never profile-seeded" strictness for `project.kind`. | should-clarify · composition | CLARIFIED — the rung is NOT kind: a personal profile MAY hold a standing budget preference (it describes the human, not the host), so "asked — or the standing default told" is the right split; only the kind's ask is always-live |
| F4 | Working well, noted: the field evidence for INV-41's shape already exists — the gallery host landed performance budgets + a timings export in its own window the same evening (its commit c93d2cd), before the pack clause shipped; the clause turns that one-off into law. | — | — |

## Coverage notes

Seams walked: E-14 (architecture step — duty added, iterative-doc law untouched), E-15 (reuses the
"derivation defect" vocabulary), INV-15 (bring-up mirror, F1), INV-28/rule 6 (the task list joins the
named language-law surfaces), INV-36 (F3), T-19/INV-9 (the setup ask keeps the word-only law — asking
IS the human's word arriving). Open ⟨DECIDE⟩s untouched: D-1/D-2/D-3.
