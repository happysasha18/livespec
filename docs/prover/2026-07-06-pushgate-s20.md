# Prover record — 2026-07-06 ~21:27, push-gate re-check (session 20, SPEC v0.15.35)

Scope: the whole spec as it stands before the session-20 push (M-6). Delta since the last pushed
state (v0.15.33): row 142 (parallel-lanes cap 2→3, T-18 reworded, INV-39 generalized) and row 140
(the economy ladder — new section, T-19/INV-40). Both carry same-day CROSS-LINK records
(`2026-07-06-row142.md`, `2026-07-06-row140.md`) with all must-fixes folded; no unfolded rows ride.

## The walk

- **Changed-law composition against the whole spec:** T-18's new cap read against INV-2 (queue cap now
  POINTS at T-18 — one home), M-1 (milestone single-train, unchanged), T-9 (bug parks all lanes,
  generalized wording), INV-27/INV-35 (board holds N lines), ACT-3 (per-train isolated trees). The
  economy ladder read against M-6 (push gate unbendable — stated in the rung AND the never-bend
  list), INV-39 (batch keeps one-delta commits), E-13 (host line outlives any rung — this repo's own
  full-prover-before-push survives `tight`), INV-9 (word-only setting), INV-5/INV-31 (named sheds),
  T-15/T-16 (safety net's third non-toucher). No contradiction found.
- **Index vs prose:** T-19/INV-40 added to the Formal index with owning section named; the suite's
  mechanical walk (anchors owned once by an architecture node; every invariant in ≥1 matrix row;
  matrix rows under their owning node) ran green at 130 — it CAUGHT two derivation defects mid-landing
  (no architecture owner; wrong matrix block) which were fixed before this record, evidence the
  mechanical arm of this gate is live, not ceremonial.
- **Stale-phrase sweep:** grep over SPEC + skills for the old cap's phrasings ("two trains", "max
  two", "second lane", "two-lane") — zero hits outside history-quoting contexts (journal, ledger,
  queue rows' provenance, this records dir).
- **Open ⟨DECIDE⟩s:** D-1/D-2/D-3 — untouched by both deltas (D-2 cited by the ladder, not resolved).

## Findings

| # | Finding | Severity · tag | Status |
|---|---|---|---|
| F1 | None new beyond the two same-day CROSS-LINK records; their folds are local to the sections their findings named (T-18 law prose, M-022; the ladder's own section). | — | gate passes |

Push may proceed; this record ships with it.
