# product-prover — CROSS-LINK pass: adoption fixes (rows 19–21), 2026-07-05 (M-6 gate for this push)

Mode: CROSS-LINK — three same-surface additions to the adoption scenario (SPEC A-5 reworded, A-8 and A-9
added; ADOPT.md phases 0/0.5/1/2/6 updated; INV-8's bootstrap sentence lifted to the same rule as the
same defect class). Seams checked against the existing spec; full-pass baseline is this morning's
2026-07-05.md record.

## Seams checked

1. **A-9 (cruft sweep) × INV-7 (never delete a host file)** — the direct collision the wish invites.
   Resolved in wording, not buried: INV-7 now reads "never bends for anything authored", A-9 is the ONE
   gated exception (regenerable junk only · listed with counts/sizes · human's explicit OK · authored
   content always via attic). Index line for INV-7 updated to match ("authored host files") — caught as
   drift during this pass, folded with the record. No residual contradiction found.
2. **A-5 remote-or-declined × INV-8 (no landing into an unversioned host)** — consistent: both now state
   the same closed set {remote exists | explicitly declined, recorded}; the bootstrap sentence carried the
   same recommendation-only weakness and was fixed as the same class. Outcome's home is named (the run's
   journal entry). No open edge: a declined remote still satisfies the gate, and the decline is a recorded
   human decision (INV-9-compatible).
3. **A-8 (adopt artifacts tracked in `.livespec/adopt/`) × ACT-3 (checkpoints gitignored in
   `.livespec/checkpoints/`)** — two different lifetimes deliberately split: transient worker checkpoints
   stay gitignored; adoption artifacts are the run's audit trail and are tracked. The pick "tracked" is
   the recommended option recorded in row 20 (Alexander may flip it); no rule elsewhere forces either.
4. **ADOPT.md ↔ SPEC drift sweep** — grep for the old artifact paths (`data/adopt_*`) and the old
   "recommend a remote" wording across all .md: clean (only row 19's historical wish text, which is
   queue history and stays).
5. **Anchor-set guard** — deliberate ADD of exactly {A-8, A-9}, both indexed; no other anchor changed.

## Findings

None beyond the INV-7 index-line drift (folded with this record, see seam 1). 0 must-fix · 0 queued.

Readiness: ready to push.
