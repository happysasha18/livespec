# Prover record — FULL pass, 2.1.0 MINOR gate

- Pass run by: **product-prover 2.0.0** (the pack version, per INV-178's one-fact rule)
- Date: 2026-07-16
- Mode: **FULL** over `PRODUCT_SPEC.md` (v2.0.0, 2083 lines, read whole) + the **architecture lens** over `ARCHITECTURE.md`
- Quantifier re-verify (INV-170) run over the whole document against today's ten additions (INV-169 … INV-179), per the gate brief
- Previous records checked: every 2026-07-15/16 record in `docs/prover/` closes folded or landed; no unfolded defect rides in from a prior pass
- First live run of the INV-171 duty: the mandatory-sweep verdict table is below, one verdict per sweep

## Triage

TRIAGE: PROCEED — a full product spec with states, transitions, actors, and invariants, paired with an architecture doc carrying node pins; both analyzable. The doc claims a shipped system, and the architecture's pins are present; pins were spot-resolved by name (the drift check runs non-strict and reports one label), so findings below are unconditional except where a pin note is itself the finding.

## Opening assessment

This spec states a method-pack whose product is its own working method: wishes become rows, rows walk a nine-step pipeline, and machines hold the bounds. The two strongest things in this revision: the ten invariants landed today compose almost seamlessly with the older laws — every new anchor is owned by exactly one node, every named net resolves to a real file on disk (all fourteen scripts and tests cited by INV-101/150/152/157/160/162/170/171 exist), and the forward-binding cites all point at INV-159 as the law demands. And the INV-171 duty itself is well-formed: the five mandatory sweeps in the skill match the five the spec names, verbatim in scope.

The two things that need attention: the document has accumulated a small class of **stale enumerations and counts** — claims of "exactly one", "nine", "eight", and one member list that were true when written and were falsified by later member additions that no sweep re-verified (the quantifier re-verify fires only on surface adds, and today's additions were lens and law adds); one of these is a live direct contradiction between INV-142 and INV-165. And the hooks-home law (INV-173) states one canonical home the spec's own pins contradict. All defects fold in one sitting; nothing structural is wrong. Confidence: needs one folding iteration, then ready for the 2.1.0 bump.

## Phase 1 — The model

Entities and relationships:
- **Wish / queue row**: one wish = one row (T-17); a row carries size, priority, door, work-kind, footprint, map verdict, fences, acceptance; closes only by named exit (landed / declined / deferred / superseded); archives, never deletes [INV-1].
- **Documents**: PRODUCT_SPEC.md (owned facts, anchors), ARCHITECTURE.md (nodes owning facts, seams, views, budgets), TEST_MATRIX.md (node × fact rows), JOURNAL, NEXT_STEPS (≤100 lines), ROADMAP, FEEDBACK.md, PROBLEMS.md, MIGRATION.md.
- **Sessions and workers**: assigned session (pen holder), foreign sessions (inbox-only), tiered workers (haiku/sonnet/senior) with checkpoints and heartbeats; each session carries a minted identity [INV-117].
- **Review passes**: prover, design review, periodic audit, verify-by-deed — one record class [INV-156]; prover ↔ design-review loop capped at three progressing rounds [INV-154].
- **Skills**: one base + nine working skills on disk (ten directories under `skills/`), versions stamped from the root VERSION [INV-178].
- **Inbox items**: local, co-located, remote (granted), and stranger arms; the monitor bridges strangers; cross-host claim arbitration [INV-146..149, INV-174].

States and transitions (the load-bearing lifecycles):
1. Row: arrived → queued → in-work → landed | declined | deferred(trigger) | superseded; parked (bug preemption) and held-for-milestone are named side-states with checkpointed entry and ordered resume [T-9, T-18].
2. Decision: asked (batched page) → answered (closed forever) | withdrawn (re-asked plainer, bound two, then converges to surfaced [default]) [INV-9, INV-59, INV-130].
3. Ledger entry: WATCHED → OWNED | AGREED NON-PROBLEM → SOLVED; archives at compaction [E-24].
4. Prover finding: found → folded | rejected(+why); defect blocks, recommendation queues [INV-140, M-6].
5. Lane pen: claim (committed flip) → hold → pass at pen-stage boundary; tie-break by ancestry then identity [INV-2, INV-11, INV-117].

Actors: the human (taste, gates, wording — ACT-1), the senior agent (judgment — ACT-2), tiered workers (mechanical — ACT-3), the stranger-wish monitor (automated, surfaces only), the guardrail scripts and CI (mechanical gates), a remote seat and a stranger (deposit-only).

Composition: the pack (rules, templates, shipped bodies) versus each host (instances, profiles, registries) split by INV-163's two poles; within the repo, nodes per the architecture table, seams named.

### What I assumed

- I read the M-6 push-gate "full walk" on a surface-sized delta as the **CROSS-LINK** mode (the modes section routes surface adds there), so the INV-171 surface × sweep table is owed by **FULL** passes — milestone gates, the ten-landing audit, and standalone whole-spec reviews — not by every push. If the intent is that a full-walk M-6 record also owes the table, that sentence is missing and should be written.
- Surface-authority probe: the authoritative surfaces the doc names (Formal index for anchors, feature-coverage table for scenarios, the [target] surface registry for host surfaces) are each kept complete by a named check; I found no operation that creates an object of a registered category without registering it. Assumption line: I found no other authoritative surface named in these docs; if one exists in the product, today's additions do not register with it.
- Skill files outside the two reviewed documents were read only as shipped-truth evidence where the documents make claims about them (INV-66, INV-13, INV-178).

## Phase 2 — Structural issues

F1 — Two clauses openly disagree on how many producers the design review's echo channel has

> "This channel carries exactly one producer for now, the same-kind divergence from the similarity check, and a later producer would earn its own clause." — INV-142, "Every design review finding carries a confidence read…"
> against: "It is the second producer of the design review's echo channel, beside the similarity divergence." — INV-165 (body and Formal-index row both)

A design reviewer running the echo channel by INV-142's letter treats a motion-parity divergence as out of channel — the "exactly one producer" sentence licenses dropping it — while INV-165 mandates it rides; two sessions reading different clauses will file the same finding into different paths, and one of them will be wrong by the spec's own text. INV-165 satisfied INV-142's "a later producer would earn its own clause" arm, but the "exactly one" sentence was never retired.

Rewrite the INV-142 sentence to: "This channel carries two producers — the same-kind divergence from the similarity check and the motion-parity lens [INV-165] — and a later producer earns its own clause."

`defect · direct-contradiction (contradiction)`

F2 — A class of stale enumerations and counts, falsified by member additions no sweep re-verified

> "The test-infrastructure family — INV-77, INV-78, INV-79, INV-80, INV-100, INV-102, INV-155, INV-157, INV-158 — shares one role…" — INV-160, "From the spec to the tests"
> "live-spec is a skill pack: nine skills (text a model reads)…" — ARCHITECTURE.md, "The shape at a glance"
> "read after the prover's pass at every full pass and, scoped, at every surface add (INV-141)" — ARCHITECTURE.md, seam "spec → design review"

One class, four instances found by the whole-doc quantifier sweep (F1 above is its fifth and sharpest sample):
(a) **INV-160's member list omits INV-162.** INV-162 (cleanup ownership, landed 2026-07-15 after INV-160) fits the class's own role sentence — "leaves the machine worse than it found it" is the killed-browser incident verbatim — names three nets, and sits on the same test-author node as the other members; the declared family enumeration never grew. Per INV-160's own standing ("the same standing an under-enumerated review-record member has"), either the list adds INV-162 or a sentence states its deliberate exclusion.
(b) **ARCHITECTURE claims "nine skills" while ten ship** (`skills/` holds nine working skills plus the base). The count went stale at the feedback-collector or design-reviewer add.
(c) **The "spec → design review" seam row omits INV-169's intake trigger.** The scoped design review now also fires at feature intake on a second-sibling yes; the seam names only full passes and surface adds, so a reader of the architecture will miss a real crossing the spec mandates.
(d) Shipped-truth evidence beyond the two documents, same class: `skills/live-spec-base/SKILL.md:8` is titled "one rulebook, **eight** working skills" while its own description enumerates nine — INV-66's mechanical net reads lists, not counts, so the count claim escaped it.

Any reader deriving behaviour from a member list or count — which node owns a new suite-honesty net, whether the design review is due at an intake, how many skills the roster check must cover — computes from a set that is missing members, and the miss is invisible because each stale sentence sits far from the add that falsified it.

Fold each instance: grow INV-160's list (or state the exclusion), fix the ARCHITECTURE count and seam row, fix the base skill's title. The class-level prevention is R1 below.

`defect · stale-enumeration (consistency)`

## Phase 3 — Property analysis

F3 — The hooks one-home law contradicts the spec's own hook pins

> "A live-channel hook [INV-108] the pack relies on lives as source in the pack's `hooks/` home and reaches a machine through an installer…" — INV-173, "Composing across axes"
> against: "`scripts/clock-hook.sh`, wired as a prompt hook" — INV-24; "A prompt hook, `scripts/chat-law-hook.sh`, installed beside the clock's hand" — INV-28

On disk, `hooks/` holds one file (`scissors-scan.py`) while the two prompt hooks the spec relies on by name live in `scripts/`. A session executing INV-173's letter — "a hook living only in an installed location has no home to update from, and that is a defect of this law" — reads the canonical home as `hooks/`, finds the clock and chat-law hooks outside it, and cannot tell whether they are violations to migrate, personal-layer members (they are not — INV-24 names the clock hook the law's mechanical hand), or a scoped exception nobody wrote. Two sessions will resolve it two ways.

Either move the two prompt hooks under `hooks/` and repoint the INV-24/INV-28 pins (a migration row), or add one scope sentence to INV-173 naming which hooks the `hooks/` home governs and where the prompt hooks' source home is. State the choice; do not leave both readings live.

`defect · internal-conflict (consistency)`

F4 — Two currency claims in ARCHITECTURE.md are stale against the tree

> "Last reconciled with the spec: 2026-07-15." — ARCHITECTURE.md header
> "`templates/headless_harness.py:1` (the canonical hardened + muted harness template — [target], lands with row 327's code step)" — ARCHITECTURE.md, test-author node row

The doc was edited 2026-07-16 (git: today's INV-179 assignment) and owns all ten of today's anchors, yet the header claims a 07-15 reconcile — a later session running the freshness check reads the doc as one day staler than it is. And the harness template's pin still carries "[target], lands with row 327's code step" while row 327 landed 2026-07-15 and the 816-line template ships with the mute flag in it — a [target] mark on shipped code is the honesty inversion S-0 exists to prevent, in the architecture's own pin cell.

Bump the reconcile date whenever anchors are assigned (or derive the line from git), and strike the `[target]` note from the harness pin.

`defect · stale-claim (observability)`

R1 — Widen the quantifier re-verify's trigger past surface adds

> "A surface add re-verifies the document's quantified claims against the grown surface set." — INV-170

Every instance in F1/F2 was created by a **member** add (a lens, a law, a skill, a class member) — none by a surface add — so INV-170's sweep, scoped as written, could not have caught any of them, and the next member add will mint the next stale count. Two options: (a) co-trigger the quantifier re-verify on INV-169's second-sibling yes and on any delta that adds a member to a declared kind or named family — preferred, it reuses the intake question that already detects the moment; (b) extend the INV-66 roster check to numeric count claims ("N skills", "N working skills") — cheap, mechanical, but covers only the roster instance. Do (a) as the spec sentence; (b) rides along as its mechanical floor for the roster case.

`recommendation · now · missing-rule (invariant)`

R2 — The monitor lock's stale bound has no stated value in its own clause

> "a lock a hard-killed run left behind is stolen by age, so the door is never blinded permanently" — INV-147, "The monitor's own law"

The age is stated nowhere on INV-147; the only value in the document is an aside inside INV-149 ("the stale bound is an hour where a contended claim is seconds old"). An implementer of the monitor on a new host must reverse-engineer the bound from a different invariant's parenthetical — exactly the one-point-named, interval-silent shape the edge-condition sweep exists to catch, and against the pack's own knob convention [INV-70] the bound carries no `[default]` tag. Write the bound on INV-147 as a tagged default ("stolen past ~1 hour [default]") and let INV-149 keep citing it.

`recommendation · now · missing-rule (precondition)`

R3 — Refresh the drifted pin caches

> "The `:line` beside it is a convenience cache and can lag, so the name is resolved and a drift check re-greps it." — "ARCHITECTURE.md describes how the product is built"

Names resolve (the doc's own law is met), but several cached lines have lagged well past their targets — spec-author's spine pin points at `:109` while the spine heading sits at `:123`, and `guardrails/check-pin-drift.sh` itself reports the `scripts/check-shipped-language.py` engine label unfound within ±25 lines. A reader trusting a cached number lands mid-paragraph in the wrong rule. Re-run the pin refresh over the node table and fold the drift check's one reported label.

`recommendation · later · stale-claim (observability)`

R4 — The FEATURE-FIT record cites a home nowhere named

> "A no is recorded as a lens verdict in the FEATURE-FIT record like any other [INV-29]" — INV-169

INV-156 enumerates the record class's members (prover, design review, audit, verify) and the prover's home `docs/prover/`; the FEATURE-FIT record is presumably the prover's record in one of its modes, but no sentence says so, and INV-156's class binds new passes to state their record against it. One sentence in INV-29 or INV-156 — "a FEATURE-FIT record is the prover's record, landing in `docs/prover/` in the class shape" — closes it.

`recommendation · later · missing-rule (invariant)`

### Coverage tables (INV-171 / INV-135 replacement)

The three classic tables each collapse for this kind, one line why:
- **CRUD per entity** — N/A as a grid: the pack's entities are rows, ledger lines, and records whose create/read/update paths are the pipeline itself and whose delete is banned by construction (archive/attic, never delete [INV-1, INV-7]); a grid would restate the prose.
- **Invariants per state** — carried by the Formal index and the matrix's node × fact rows [E-15]; a second table would be a drift copy [INV-13].
- **Authorization per action** — N/A: no user roles exist; authority is the fixed ACT-1/2/3 split plus write-ownership [INV-10], each already stated as law.

Per INV-171, the surface × sweep verdict table stands in their place:

| Surface | Declared laws (INV-101) | Edge completeness (INV-138) | Uniformity (INV-125) | Lifecycle (INV-168 parent) | Unwritten seams (INV-72) |
|---|---|---|---|---|---|
| Queue & rows (ROADMAP) | clean | clean (deferred-trigger cadence both-ended, INV-129) | clean | clean (exits, park, resume all stated) | clean |
| Decision page + batched questions | clean | clean (withdrawal bound = two, both sides stated) | clean | clean (answered/withdrawn/converged; late word = new wish) | clean |
| Chat surfaces (echo, narration, board, reports) | clean (register law + nets) | clean (heartbeat ~10 min / ~2 min detached, tagged) | clean | N/A — stateless lines; no enter/leave/return to walk | clean |
| Settings card | clean | clean | clean | clean (render-moment truth; re-ask re-renders; stated) | clean |
| Feature-map answer | clean | clean | clean | N/A — read-only answer, no state | clean |
| Inbox (4 arms) + stranger monitor | clean | **hit — R2** (lock stale bound unhomed) | clean (all arms one committed-file law) | clean (surfaced-once, generation re-surface stated) | clean |
| Feedback ledger + outbox / collector | clean | clean | clean (sixth line-kind declared beside the five routes) | clean (outbox notes cleared on delivery — stated) | clean |
| Problem ledger | clean | clean (two-strikes / third-recurrence both ends) | clean (product-vs-workshop seam decided) | clean (WATCHED→OWNED→SOLVED, archival stated) | clean |
| Prototype home + norms | clean | clean | clean | clean (promotion path, frozen norm provenance) | clean |
| Review records (prover / design review / audit) | clean | clean | **hit — R4** (FEATURE-FIT record home unnamed) | clean (held asks carried, -suffix collision law) | clean |
| Resume file + checkpoints | clean | clean (100-line cap, both sides) | clean | clean (INV-107 closing sweep) | clean |
| Push/CI gates + hooks | clean (three laws, nets on disk, verified) | clean | **hit — F3** (hooks home vs pins) | clean (INV-175 config-health self-heal) | clean |
| Suite & harness (test infrastructure) | clean | clean (age bounds young/old both stated) | **hit — F2a** (INV-160 list omits INV-162) | clean | clean |
| Adoption / catch-up / bootstrap walks | clean | clean (half-done-state law both-ended) | clean | clean (resumable checkpointed walk, restore point) | clean |
| ARCHITECTURE.md (nodes, seams, views) | clean | clean | **hit — F2b, F2c, F4** (count, seam trigger, currency claims) | N/A — a document, not an entered surface | **hit — F2c** (the intake crossing the seam table never names) |

A missing verdict line reads as a skipped sweep, so for the record: all five sweeps ran; the imaginative probes ran without owed verdicts and produced F1 (quantifier sweep per the gate brief), F3 (class lens on hooks), and the assumption lines above.

## Phase 3.5 — Acknowledged gaps

The document flags its open items honestly; commentary on each, no new discoveries here:

1. **D-1 attic layout, D-6 pair queues, D-7 pair-spec citation depth** — three ⟨DECIDE⟩ marks with recommendations already attached; each waits on a real triggering event (next adopt run, real pair friction). Second-order: D-7's recommended answer is already load-bearing in INV-86's text ("cites the engine only by its contract entries' handles (D-7)") — when D-7 is decided, sweep that sentence so a reversal doesn't leave it stale. `acknowledged · missing-rule (invariant)`
2. **The [target] machines** — host-facing guardrails + surface registry [E-6, E-10], snapshot [E-7], design-sync [E-18], measurement family (rows 47-49/96). Each carries its owned row and the S-0 suite tie; consistent. `acknowledged · missing-scenario (state-space)`

## Phase 4 — Human and operational factors

- **Domain language**: strong — the spec speaks in product words with anchors trailing; the two register lints plus the legibility floor guard the shown surfaces. No leaked internal identifier found on a promised visible string.
- **Observability**: strong — the record class, the departures board, delegation accounting, and the Now/Next status make state answerable in any seat. F4 is the one observability defect (stale currency claims in the architecture header and a pin cell).
- **Cognitive load**: the mode split (FULL / CROSS-LINK / FEATURE-FIT) now carries different record duties per mode; the "What I assumed" ambiguity about which passes owe the table is the one place a reviewer must remember an unwritten rule. Covered by the assumption; one sentence in INV-171 naming the owing passes would retire it.
- **Performance/scale budgets**: stated with instrumentation homes (suite ≤60 s, card ≤1 s, resume ≤100 lines); the spec's own size is held by the ratchet gates [INV-164]. Clean.
- **Security/privacy**: the no-secrets line is a named skip in the placement view; INV-179 closes the upstream-note leak class at the consent moment. Clean.

## Phase 5 — Closing summary

**Top 3 to fix before the 2.1.0 bump:**
1. F1 — retire INV-142's "exactly one producer" sentence (direct contradiction with INV-165).
2. F2 — fold the four stale-enumeration instances (INV-160's list, ARCHITECTURE's count and seam row, the base skill's title).
3. F3 — decide the hooks' home: migrate the prompt hooks or scope INV-173, one written choice.

**Properties to state explicitly (paste-ready):**
- "The design review's echo channel carries two producers — the similarity divergence and the motion-parity lens — and a later producer earns its own clause."
- "The quantifier re-verify also fires on any delta that adds a member to a declared kind or named family, the same moment the second-sibling question answers yes."
- "The monitor's lock and claim are stolen past ~1 hour [default]."
- "A FEATURE-FIT record is the prover's record, landing in docs/prover/ in the record class's shape."

**Open questions for the author** — none; every finding resolves by inspection, the folds are the author's ordinary work.

**Recommendations queued for a taste call:** R1 (widen the quantifier re-verify trigger — graded now), R2 (home the stale bound — now), R3 (pin cache refresh — later), R4 (FEATURE-FIT record home — later).

**[default] count (FULL-pass duty, this skill's Phase 5 item 5):** 36 lines in PRODUCT_SPEC.md carry a live `[default]` tag (51 lines mention the literal; 15 are backticked or in-prose mentions of the tag itself, excluded). Oldest five, by git blame, for a taste call:
1. 2026-07-06 — the parallel-lane cap: "at most three build lanes roll at once without asking [default]" (T-18).
2. 2026-07-06 — the resume-file cap: "whole file ≤ 100 lines [default]" (INV-48).
3. 2026-07-07 — the brief bound: "~300 lines, ~8 files to edit [default]" (INV-55).
4. 2026-07-07 — feedback section: "Facets otherwise N/A [default]" (F-feedback).
5. 2026-07-07 — problem-ledger success measure: "checked at the milestone audit [default]" (F-problem-ledger).

**Findings disposition** (the orchestrator folds; left open per the gate brief):

| Finding | Kind | Disposition |
|---|---|---|
| F1 — INV-142 vs INV-165 producer count | defect | open |
| F2 — stale enumerations/counts class (4 instances: INV-160 list · ARCH "nine skills" · ARCH design-review seam · base title "eight") | defect | open |
| F3 — INV-173 hooks home vs INV-24/INV-28 pins | defect | open |
| F4 — ARCHITECTURE currency claims (reconcile date · harness [target] mark) | defect | open |
| R1 — widen quantifier re-verify trigger to member adds | recommendation · now | open |
| R2 — state the lock stale bound as a tagged default on INV-147 | recommendation · now | open |
| R3 — refresh drifted pin caches | recommendation · later | open |
| R4 — name the FEATURE-FIT record's home | recommendation · later | open |

**Architecture lens verdict** (six checks): every spec fact owned by exactly one node including all ten of today's anchors — clean; no node without spec backing, no new node born this delta (fitness test N/A) — clean; seams named — one hit (F2c, the intake trigger missing from the design-review seam); budgets stated with instrumentation homes — clean; runtime view walks all twelve promised flows, matching the feature-coverage table one-to-one — clean; placement view complete with the secrets line — clean, save the F4 currency claims.

**Readiness:** needs one folding iteration — four defects, all local one-sentence-to-one-cell folds with no structural change; fold them and the spec and architecture are ready for the 2.1.0 bump.

## Disposition addendum (the orchestrator's fold, same day)
| Finding | Disposition |
|---|---|
| F1 (INV-142 producer count vs INV-165) | folded — two producers named, each under its clause |
| F2a (INV-160 list omits INV-162) | folded — member added |
| F2b (ARCHITECTURE "nine skills") | folded — ten |
| F2c (spec → design review seam omits INV-169) | folded — intake trigger named on the seam row |
| F2d (base title "eight working skills") | folded — nine |
| F3 (hooks one-home vs scripts/ session hooks) | folded at the root — clock-hook.sh and chat-law-hook.sh moved to hooks/, installer + pins + tests + allowlist repointed, config-health covers all three installed hooks |
| F4 (stale header date; harness [target]) | folded — 2026-07-16; [target] dropped |
| R1 (widen INV-170 to member adds) | folded now — the step fires on every member add; the 2.1.0 gate's four falsified sentences cited as evidence |
| R2 (stolen-by-age value unstated on INV-147) | folded now — about an hour [default], aligned with INV-149 |
| R3 (refresh drifted pin caches) | queued — row 356 |
| R4 (name the FEATURE-FIT record home) | queued — row 356 |
