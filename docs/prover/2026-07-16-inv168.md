# Prover record — INV-168, the transition-payload lens (2026-07-16, CROSS-LINK)

Mode: CROSS-LINK on one added invariant. The independent deep pass that motivated it is the Fable
adversarial audit, `docs/audit/2026-07-16-prover-fable.md` (its finding D1); this record checks the
seams of the fold that answers it.

## The delta
INV-168 — "Every stated transition carries a payload lens: name what the person perceives across it."
The prover verifies the state graph's topology (a way in, out, and back exist); INV-168 adds the payload
question — for every stated transition, enumerate the parameters a person perceives across it and demand
the spec name each, since a blank parameter is answered by the platform default alone. It is the parent
of INV-165 (motion on the exit) and INV-167 (internal state on re-entry).

## Seam checks

| # | Seam | Verdict |
|---|------|---------|
| 1 | INV-168 vs INV-165 / INV-167 — do the children stay consistent as instances? | HOLDS. Both are named as instances the parent generalizes; neither is contradicted, and each keeps its own clause. The parent adds the general question; the children remain the two payload parameters already written. No rewrite of INV-165/167 is owed. |
| 2 | INV-168 vs the topology lenses INV-50 / INV-72 / INV-127 — overlap or gap? | HOLDS. The topology lenses ask whether a path/exit/scenario-edge EXISTS; INV-168 asks what the existing transition CARRIES. Complementary, stated as "beside topology". No double-coverage of one fact. |
| 3 | INV-168 vs the class-uniformity lens INV-125 | HOLDS. A payload parameter, once the human decides it, becomes a spec sentence the ordinary state-coverage and INV-125 hold; INV-168 produces the finding, INV-125 holds the decided policy. |
| 4 | Does INV-168 re-open the one-invariant-per-bug loop it names? | HOLDS — and this is the point. The seven cases Fable listed (focus after close, unsaved input across interruption, sound under a covering, back-step restoration, timer across suspend, derived-display refresh, global-setting re-sampling) are ENUMERATED AS EXAMPLES of the one lens, not minted as seven invariants. The parent's payload enumeration is what catches them, so no new INV per case. |
| 5 | Scope honesty | HOLDS. Each unstated parameter is a finding raised as an open question; the timing/feel choice is deferred to the human (INV-30), never baked. The lens produces findings, never a blocking auto-decision. |

## ⟨DECIDE⟩ / human calls
None owed by the invariant. Per-transition, which payload parameters matter and whether a default is
acceptable is the author's to write and the human's to judge (INV-30) — the invariant defers rather than
bakes.

## Ownership / homes
INV-168 owned by spec-author (the author writes the payload sentence), carried by the prover — consistent
with its instances INV-50 / INV-167. One clause, one matrix row (M-322), one owning node.

## Verdict: HOLDS
The fold answers Fable D1 with the parent lens rather than another per-bug invariant. The remaining audit
findings (D2–D5 method defects, R1–R5 recommendations) are queued as their own folds, not part of this
delta.
