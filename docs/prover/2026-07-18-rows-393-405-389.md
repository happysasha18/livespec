# Prover record — 2026-07-18 — rows 393, 405, 389 (one movement)

CROSS-LINK pass over the three deltas plus the architecture lens on each new node-owned invariant.
Previous record (docs/prover/2026-07-18-*.md batch) carried no unfolded rows into this one.

## Scope

Three small, largely independent rows landed in one movement, sharing only the four docs
(PRODUCT_SPEC.md, ARCHITECTURE.md, TEST_MATRIX.md, ROADMAP.md), edited serially under one pen. No new
pre-push gate — all three ride the suite / process-space / queue-take habits, so the gate chain
(gates.yml, gate-red-proofs.json, pre-push letters a–y) is untouched. VERSION held at 2.6.3.

## Row 393 — INV-230 — the worker-teardown reap + idle-output detection

- **Cross-link.** INV-230 grows the runaway-child class INV-213. INV-213's notice-first mechanism
  (`guardrails/check-runaway-child.py`, report-only) stands untouched — the reap is a separate module
  `guardrails/reap_owned_group.py`, so INV-213's "ends no process" promise is not weakened. Seam to
  INV-162 (owned-identity) and INV-204 (cleanup-notice shape) is by reuse, not re-mint.
- **Safety finding (the row's own gate).** The reap must be provably scoped to the run's own group.
  Verified: `reap_owned_group` accepts a numeric process group only, refuses any group absent from the
  run's owned set and any non-numeric target (raising `UnownedReapRefused`, ending nothing), and reaps
  by `os.killpg` on the owned group. Red-proven by `test_reap_refuses_an_unowned_group_and_ends_nothing`
  and `test_reap_refuses_a_non_numeric_target`, and by the row-417 broad-kill gate j passing on the
  module (`test_reap_module_passes_the_broad_kill_gate`). A bare-name reap is not expressible through
  this door. VERDICT: reap is scoped to the run's own group — the SAFETY condition holds, the reap
  ships (not report-only).
- **Architecture lens.** INV-230 owned by exactly one node (guardrails), pinned to
  `guardrails/reap_owned_group.py`. The ACT-3 idle-habit clause is base-rulebook's worker contract,
  carried on the guardrails node's INV-230 entry as wiring with its owner named. No orphan node, no
  unowned fact.

## Row 405 — INV-231 — the listener tripwire (deferred, field-gated)

- **Cross-link.** INV-231 is an instance of INV-129 (deferred-row revisit trigger re-scanned every
  queue-take), distinct from `far` INV-222 (which carries no trigger). It watches the transport law
  INV-183. The check `guardrails/check-listener-tripwire.py` fires on a non-empty socket field and
  stays silent on an empty/absent one, red-proven both ways.
- **Field-gating finding.** The firing must not be simulated as real. Verified: the check reads a real
  `claude agents --json` / session registry (or a `LIVE_SPEC_AGENTS_JSON` fixture for tests), and
  today's listenerless harness leaves it quiet. The row stays DEFERRED with its mechanical trigger; the
  build legs land, the firing leg stays deferred (the rows 247/385 pattern). No fired state is recorded
  as real.
- **Architecture lens.** INV-231 owned by the guardrails node, pinned to the check. Rides the suite and
  the queue-take scan, not the push chain — no gate letter (the far-tier precedent, INV-222/INV-83).

## Row 389 — INV-232 — the remote-seat read arm (LAW arm; real read field-gated)

- **Defect confirmed and folded.** The audit's finding 8: INV-187 sent a remote consumer to read "over
  git when remote" citing INV-112, whose home defines only a PUSH grant. INV-232 grows the read arm: the
  read grant recorded in the host profile beside the push grant (INV-82) as `trust.read-grant`, and the
  honest grantless failure naming the grant it lacks (INV-67). INV-187's read bullet now cross-cites
  INV-232.
- **Honest-failure finding.** Red-proven: `scripts/read-grant.py`'s `check_read_grant` proceeds on a
  recorded read grant and raises `ReadGrantMissing` (naming `trust.read-grant` + the one action,
  `scripts/read-grant-ask.md`) on a grantless seat; a grant for another repo does not satisfy this repo.
- **Field-gating.** The real cross-machine read is not attempted — field-gated on the promoter↔site pair
  over private tlvphotos (rows 385/247). Only the law arm and its honest failure land.
- **Architecture lens.** INV-232 owned by the inbox node (beside INV-112's grant model), pinned to
  `scripts/read-grant.py` and `scripts/read-grant-ask.md`. No orphan node, no unowned fact.

## Open ⟨DECIDE⟩ touched

None — no ⟨DECIDE⟩ marker sits on the surfaces these three deltas change.

## Findings folded

| # | Finding | Fold |
|---|---|---|
| 1 | 393: reap could weaken INV-213's ends-nothing promise if co-located | Folded — reap is a separate module; INV-213 mechanism untouched, its test green |
| 2 | 393: reap must be provably scoped to the run's own group (SAFETY) | Folded — numeric-group-only + owned-set membership, red-proven + broad-kill gate j passes |
| 3 | 405: firing must not be simulated as real | Folded — field-gated, build legs land / firing leg stays deferred |
| 4 | 389: INV-187 cited a push-only grant for a read | Folded — INV-232 read arm + INV-187 cross-ref |

All folded. No unfolded rows carried forward.
