# Prover record — 2026-07-06, row 133 (CROSS-LINK), session 17

Mode: CROSS-LINK on SPEC delta — INV-38 (the feature map readable on demand, new scenario "Asking what
the product does") — against the seams it composes with: INV-27 (departures board scope), INV-37
(intake placement / `map:` notes), INV-28 (line law), INV-34 (pre-report walk scope), E-14 (map = spec
scenarios + architecture nodes, no third document), S-0 (the [target] tag's granularity and its
mechanized row-map), the show rule (rendered page), INV-31 (no confirmation pings).

Previous record checked: `2026-07-06-rows129-132.md` — no unfolded rows carried over (all four folded
in-pass, verified in the summary table).

## Opening assessment

A read-only transparency surface with no persistence and no new document — structurally the cheapest
kind of feature, and the delta correctly reaffirms the no-third-document law instead of amending it.
The two real seams were both of the "which source is authoritative at which granularity" family:
statuses against S-0's clause-level tag, and features that exist only as queue rows. Both folded
in-pass. Ready to build.

## Findings

F1 — A feature's status was promised at the wrong granularity

> "the header's current-vs-target paragraph splits shipped from promised" — Asking what the product
> does (as first drafted)

The [target] tag binds at the clause it is written on, never a parent section (S-0). A scenario
holding both shipped law and promised parts (e.g. "The machines that hold the bounds") cannot carry
one blanket status, so the success measure's section-to-section diff was undefined for mixed sections
— the reader would see either an over-claim ("shipped") or a false hold ("target") on a half-real
feature.

Folded in-pass: the law now reads statuses at the tag's own granularity — a mixed scenario reads
"shipped, with promised parts (named)"; the success measure diffs at that granularity; the index row
carries the clause.

`must-fix · over-general (abstraction)` — **FOLDED**

F2 — A feature living only in the queue was invisible to the map

> "the spec's scenario sections name the features" — Asking what the product does (as first drafted)

A wish placed with a NEW verdict at intake (INV-37) has a `map:` note but no scenario section until
its spec step runs. Read off scenarios alone, the on-demand map loses exactly the features the human
most recently asked for — he asks «покажи все фичи» the day after tossing a wish and the wish is
missing, which reads as a lost wish (the fear INV-27's echo exists to kill).

Folded in-pass: the queue's open rows now contribute both stations for in-flight features AND
queued NEW-verdict wishes, shown as queued; the success measure covers them.

`should-clarify · missing-scenario (state-space)` — **FOLDED**

## Verified seams (no finding)

- Board scope: INV-27's report-time board stays in-flight-only; INV-38 fires only on ask — the two
  scopes are disjoint by construction and each law names the other's.
- No third document: the answer is derived at ask-time, nothing cached, nothing written — E-14
  reaffirmed, not amended; no competing authoritative surface is created (surface-authority lens).
- Line law: each answer line obeys INV-28 by explicit citation; a status mark trails like any handle.
- Pre-report walk: the map answer is an answer to a direct ask, not a movement-end report — INV-34's
  walk scopes stay untouched; INV-28 still binds (stated in the law).
- Empty host: the nothing-to-read answer is specified (bootstrap/adoption pointer), no invented list —
  the bounds/absence lens closes clean.
- Persistence/versions: no state persists; N/A by construction.
- Rendered page: rides the existing show rule on the human's word, `[default]` chat — no new render
  machinery promised.

## Summary

| # | Finding | Severity | Status |
|---|---|---|---|
| F1 | statuses bind at the [target] tag's granularity, not the section's | must-fix | folded in-pass |
| F2 | queued NEW-verdict wishes appear on the map from their rows | should-clarify | folded in-pass |

Open ⟨DECIDE⟩ count untouched by this delta: D-1 / D-2 / D-3 remain open, none touched by this surface.

Verdict: ready to build.
