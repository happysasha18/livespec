# Prover record — FULL pass, pre-MINOR gate 2.1.1 → 2.2.0

- Pass run by: **product-prover 2.1.1** (the pack version, per INV-178's one-fact rule; read from `VERSION`)
- Date: 2026-07-16
- Mode: **FULL** over `PRODUCT_SPEC.md` (v2.1.1, 2089 lines, read whole) + the **architecture lens** over `ARCHITECTURE.md`
- Delta since the morning's 2.1.0 full pass, weighted but not scoping the read: the `skills/product-prover/SKILL.md` restructure (M-338 — boundary/description/KIND homes), the new INV-181 clause beside INV-96 (grown mid-pass by three sentences: the script named, the README-only reason, the three-member generated-blocks kind), matrix rows M-339/M-340, the extractor fix in `tests/test_traceability.py` (row 359)
- Previous records checked: the 2.1.0 full pass closes fully folded; its two queued recommendations (R3 pin refresh, R4 FEATURE-FIT record home) landed at 2.1.1 (row 356); the 2.1.1 prepush's one must-fix (INV-147's actor sentence) reads folded in the current text — the sweeping session posts the echo, the landing session closes the Issue, each under its own auth. No unfolded defect rides in.
- Deed checks run this pass: `guardrails/check-pin-drift.sh` (52 pins strict-clean); `tests/test_traceability.py` (170 green, the row-359 extractor fix red-proven by its own regression test); `tests/test_mirror_release_history.py` read claim-vs-deed against `scripts/sync-mirrors.sh`; the live mirror `happysasha18/product-prover` README read over `gh api` (evidence for F3); a per-skill diff of `skills/*/SKILL.md` against the installed copies under `~/.claude/skills/` (evidence for F2); the full suite run riding in the background, its log's own tail line the verdict [INV-80].

## Triage

TRIAGE: PROCEED — a full product spec with states, transitions, actors, and invariants, paired with an architecture doc whose header reconcile date is current (2026-07-16) and whose 52 node pins pass the strict drift check; the doc claims a shipped system and the pins stand, so findings are unconditional.

## Opening assessment

This revision's delta is small and well-made: INV-181 composes cleanly with INV-96 (one new anchor, owned by the publish node, matrix-covered by M-339/M-340, its machine `scripts/sync-mirrors.sh` real and its tests pinning exactly the behaviours the clause promises, the paren-aware story cut included), and the prover skill's own restructure moved three reader-facing homes without changing any rule's meaning, each home pinned by a red-proven test. Two things need attention before the bump. First, a process-integrity break: the three-member generated-blocks kind in INV-181 was landed `[default]`-tagged while the design review's own record holds that exact question open as an unanswered ask — INV-142 states in so many words that this channel's class sentence lands only on the human's word, never on the recommended default. Second, three shipped-truth claims fail by deed: the installed prover skill copy predates today's restructure (E-23's same-session sync did not run), queue row 360 records a verification on a live mirror that provably has not happened, and the architecture's suite wall-time budget (≤ 60 s) reads 302 s off its own instrumentation line with no row that could have seen the drift. All four fold in one sitting; nothing structural is wrong. Confidence: needs one folding iteration, then ready for the 2.2.0 bump.

## Phase 1 — The model

The entity, state, actor, and composition model is unchanged from the 2.1.0 full pass this morning (`docs/prover/2026-07-16-full-2p1p0.md`, Phase 1), with one addition:

- **Standalone mirror (new surface facts):** a mirror is rebuilt from the pack folder by `scripts/sync-mirrors.sh` at every sync (`rsync --delete`, so nothing hand-kept survives); the sync writes three generated blocks — the read-only banner at the README's top, the release-history section (one line per shipped version, harvested oldest-commit-per-version from the pack's git log), and the attribution line, which stays last [INV-96, INV-181]. History lands on README only; SKILL.md stays the machine surface. The three blocks are declared one kind, each pinned by a test [INV-181, M-340].
- Lifecycle: mirror content has no entered/left state — it is regenerated whole at each sync; staleness between syncs is by construction and bounded by the next push's sync.

### What I assumed

- I read the M-340/INV-181 kind sentence's provenance from the artifacts alone: the design-review record (`docs/design-review/2026-07-16-2p2p0.md`) holds the banner-membership question **asked/held (loop WAITS)**, and no answer is recorded in FEEDBACK.md, `docs/decisions/`, or the queue — so I read the landed `[default]` kind sentence as unratified. If the human's word landed in chat and simply was not recorded, F1's fold is the recording, not a retraction.
- The mirror's between-sync staleness (the live mirror today carries neither the history section nor the current attribution wording) I read as legal by construction — the sync rides the push. Only the row's claim of a completed verification (F3) is the finding, not the staleness itself.
- Carried forward unchanged: the FULL-pass table duty reading, and the surface-authority assumption lines, both from the 2.1.0 record.

## Phase 2 — Structural issues

F1 — A design-review class sentence landed on the spec by default while its ask still waits, which INV-142 forbids in so many words

> "A mirror's generated blocks form one declared kind with three members — … — each declared and each pinned by a test [default]." — INV-181, "Publishing"
> against: "That default is not applied to the spec the way the pack's usual proceed-on-recommended arm would [INV-4]: the class sentence lands only on the human's word [INV-141]" — INV-142, "Every design review finding carries a confidence read…"
> and: "Disposition: **held** — asked … the loop **WAITS** — on the banner-membership ask (finding 1)" — `docs/design-review/2026-07-16-2p2p0.md`

The design review raised the banner's kind-membership as a likely question to the human (its record's finding 1), and INV-142/INV-154 say only a human-accepted declaration lands the class sentence — yet commit `9e727ab` landed the declaration `[default]`-tagged, citing that very review's group A (M-340's own text names it). A reader of the record believes the question is open; a reader of the spec believes the kind is settled; and the next design-review pass, reading held asks per its re-fire rule, will find its open question already answered in the spec by nobody. Whichever way it resolves, one of the two artifacts is wrong today.

Fold one of two ways: (a) bring the banner question to the human on the ordinary batched path, and on his word re-tag the kind sentence decided (drop `[default]`) and close the record's held ask with the answer; or (b) if the sentence must stand now, restate it as the prover-side completeness fact it can honestly be (three blocks exist and each is pinned by a test — a deed claim needing no taste call) and leave the kind-membership word — is the banner *of the family* or hygiene-by-intent — to the held ask. (a) is cleaner and costs one card on the existing page.

`defect · direct-contradiction (contradiction)`

## Phase 3 — Property analysis

F2 — The installed prover skill is a day behind its source: E-23's same-session sync did not run after the restructure

> "A session that edits a skill syncs the installed copy the same session, through the named tool `scripts/sync-skills.sh`." — "The package repo", E-23
> Deed: `diff skills/product-prover/SKILL.md ~/.claude/skills/product-prover/SKILL.md` → differs; the installed copy lacks the M-338 restructure (the design-reviewer boundary paragraph, the KIND split's new home), while all nine other skills read SAME.

The M-338 restructure landed in commits `a01f6a6`/`179d2d7` today, but the copy every session actually loads still carries the pre-restructure text — so the very homes M-338 red-proved (description-as-trigger, boundary in "When NOT to use") are not what the next prover pass runs by, and INV-180's "acknowledged weakest, held by discipline" member is in live violation the same day the class was declared. The fix is the named one-command tool; run it before the bump so the 2.2.0 gate's own prover loads the text it certifies.

`defect · stale-claim (observability)`

F3 — A queue row records a verification that has not happened: "MET (verified on the live mirror after the 2.2.0 push)" with no 2.2.0 push in existence

> "Done when: a mirror repo shows each shipped version with one line of story, generated by the sync rather than hand-kept — MET (verified on the live mirror after the 2.2.0 push)" — ROADMAP.md, row 360
> Deed: `VERSION` reads 2.1.1 and this gate precedes the bump; the live mirror's README (read now over `gh api`) contains no "## Release history" section and no current attribution line.

A done-claim is answered by walking the evidence fresh [INV-25], and this cell asserts a walk that can only happen after a future event — the exact narrative-vs-verified inversion the law exists to prevent. A later session answering "did row 360 verify on the live mirror?" reads MET and asserts a falsehood. The generation itself is honestly verified (the `--print-release-history` tests and the acceptance fold are real); only the live-mirror half is anticipation written as fact.

Reword the cell to the truth: "MET on the generator (print-mode tests + acceptance); live-mirror verify owed at the 2.2.0 push's sync" — and at that push, walk it and write the date. Never a MET on an event that has not occurred.

`defect · missing-outcome-check (postcondition)`

F4 — The suite wall-time budget is violated five-fold by its own instrumentation line, and no matrix row can see the number

> "full suite wall-time | ≤ 60 s on the dev machine [default] | the pytest tail line in the suite log" — ARCHITECTURE.md, "Quality budgets"
> Deed: this pass's full run's tail line reads "1 failed, 940 passed in 302.26s (0:05:02)".

The budget's own instrumentation home reads 302 s against a stated 60 s — the suite has grown past its number (941 tests, many spawning subprocess gates) and nothing noticed, because the only test near the budget (M-185) asserts the architecture *carries* the budget, not that the number holds: exactly INV-41's "a budget counts only once a matrix row at the right level can see it — a hope in prose does not," violated by the pack's own doc. A session trusting the stated budget schedules a "quick" gate run and waits five minutes with no beat owed.

Two folds together: re-tune the number to the measured truth (it is `[default]`-tagged and the agent's to set and tell — propose ~6 min, or split a fast lane and budget both), and give the budget the seeing row INV-41 demands — a check that reads the suite log's tail line and reds when the wall time exceeds the stated budget, so the next growth is caught at the gate rather than by a reviewer's stopwatch.

`defect · unenforceable-promise (discharge)`

R1 — "The sync script" names two different machines across three clauses

> "written by the sync script (`scripts/stamp-versions.py`) at every bump" — INV-178, "Composing across axes"
> against: "the sync script stamps the line onto each mirror's README.md and SKILL.md from the live VERSION file at every sync" — INV-96, "Publishing" (script unnamed)

INV-181 now names its machine ("The mirror sync script (`scripts/sync-mirrors.sh`)"), but INV-96 still says bare "the sync script" for the same program, while INV-178 uses the identical phrase for `stamp-versions.py` — one name, two machines, and a reader wiring the attribution stamp can land on the wrong script. Name `scripts/sync-mirrors.sh` in INV-96 (or adopt "the mirror sync script" there), matching INV-181's wording. (The design review's naming note routed this here.)

`recommendation · now · confusing-for-users (cognitive-load)`

R2 — The mirror's generated release-history lines reach a public README without the shipped-language machine ever reading them

> "A shipped artifact carries no Cyrillic outside a user-language string the program deliberately emits… the publish gate holds this with a machine (`guardrails/check-shipped-language.sh`)" — INV-120, "Publishing"
> and: "The sync script harvests those lines from the pack's own git history and writes them fresh at every sync" — INV-181

The machine scans the tracked tree, but the history section is assembled at sync time from commit subjects and lands on a public mirror README that is not this repo's tree — so a Cyrillic or personal-name commit subject would ship to the shopfront past every net (today's subjects are English and the print-mode test pins several, so nothing is red now; the gap is the path, not the content). One line in `sync-mirrors.sh` runs the existing checker over the assembled README before push, or the declared-laws home records a dated exemption naming the commit-subject discipline as the carrier.

`recommendation · now · missing-rule (invariant)`

R3 — The publish node's pins do not carry INV-181's artifacts

> "publish … E-20, INV-44, INV-96, INV-119, INV-181 | `skills/publish/SKILL.md:1` (frontmatter + when it fires), the kind-checklist table and target-plugin sections in the same file" — ARCHITECTURE.md, node table
> Deed: `skills/publish/SKILL.md` contains no mention of the release history, INV-181, or `sync-mirrors.sh`.

The node owns the anchor but its pinned file carries none of the fact; the fact's real carriers — `scripts/sync-mirrors.sh` and `tests/test_mirror_release_history.py` — are unpinned, so a reader resolving INV-181 to its owning place lands in a file that never mentions it. Add a pin (`scripts/sync-mirrors.sh:210`, the `stamp_release_history` home) to the publish node's row; INV-96's mechanical case earns the same pin in passing.

`recommendation · later · hard-to-operate (ops-ux)`

R4 — M-339's fact cell under-describes the story cut its own test pins

> "the story cuts at its first colon and drops a trailing parenthetical" — TEST_MATRIX.md, M-339
> against the deed: the shipped cutter is paren-aware and cuts at the first colon **or period outside parentheses** (row 360's own landing note; `test_print_mode_emits_real_history` pins the "(his word" non-fragment).

A later fix re-implementing "first colon" as written re-introduces the paren-blind cut the acceptance already caught and folded. Align the cell's wording to the paren-aware rule.

`recommendation · later · over-general (abstraction)`

### Quantifier re-verify (INV-170, member-add trigger)

INV-181 is a member add twice over — a new anchor on the publish node and a new declared kind. Swept the enumerations it could falsify: the publish node's anchor list grew correctly; the Formal index carries INV-181 once; "ten skills" (ARCHITECTURE) and "nine working skills" (base title) still match the ten directories on disk; the three-member kind sentence matches the script's three stamp sites by deed (`banner_for`, `stamp_release_history`, `stamp_attribution`); INV-96's "the one mechanical case" is not falsified (INV-181 rides the same case). Clean beyond the F1 provenance issue.

### Coverage tables (INV-171 / INV-135 replacement)

The three classic tables collapse for this kind exactly as the 2.1.0 record states (entities archive rather than delete; invariants-per-state live in the Formal index and matrix; no user roles exist) — one line each there, unchanged. Per INV-171, the surface × sweep verdict table stands in their place:

| Surface | Declared laws (INV-101) | Edge completeness (INV-138) | Uniformity (INV-125) | Lifecycle (INV-168 parent) | Unwritten seams (INV-72) |
|---|---|---|---|---|---|
| Queue & rows (ROADMAP) | clean | clean | clean | clean | **hit — F3** (a Done-when cell claims a future event's verification as walked) |
| Decision page + batched questions | clean | clean | clean | clean | **hit — F1** (a design-review ask's answer landed in the spec while the ask still waits) |
| Chat surfaces (echo, narration, board, reports) | clean | clean | clean | N/A — stateless lines | clean |
| Settings card | clean | clean | clean | clean | clean |
| Feature-map answer | clean | clean | clean | N/A — read-only answer | clean |
| Inbox (4 arms) + stranger monitor | clean (actors pinned at 2.1.1) | clean (lock stale bound ~1 h [default], both cadences) | clean | clean | clean |
| Feedback ledger + outbox / collector | clean | clean | clean | clean | clean |
| Problem ledger | clean | clean | clean | clean | clean |
| Prototype home + norms | clean | clean | clean | clean | clean |
| Review records (prover / design review / audit) | clean | clean | clean (FEATURE-FIT home named since 2.1.1) | clean (held-ask carry read against the 2p2p0 record) | clean |
| Resume file + checkpoints | clean | clean | clean | clean | clean |
| Push/CI gates + hooks | clean (three laws, nets on disk) | clean | clean (hooks home settled at 2.1.0's F3 fold) | clean (INV-175 self-heal) | clean |
| Suite & harness (test infrastructure) | clean | clean | clean (INV-160 lists ten members incl. INV-162) | clean | clean |
| Installed skills (E-23 / INV-180 member) | clean | clean | clean | **hit — F2** (the running copy predates today's edit; the same-session sync did not run) | clean |
| Standalone mirrors (README shopfront) — new surface | **hit — R2** (register law's machine never reads the generated lines) | clean (staleness between syncs bounded by the next push's sync, stated by construction) | **hit — R1, R4** (one name two machines; the cut rule under-stated) | N/A — regenerated whole each sync, no entered state | clean (SKILL.md/README split now reasoned in-text) |
| Adoption / catch-up / bootstrap walks | clean | clean | clean | clean | clean |
| ARCHITECTURE.md (nodes, seams, views) | clean | clean | **hit — R3** (publish pins carry none of INV-181's artifacts) | N/A — a document | clean |

All five mandatory sweeps ran; the imaginative probes ran without owed verdicts and produced F1 (three-source disagreement between the spec, the design-review record, and the commit history), F2/F3 (surface-authority and dependency-reality reads against the machine and the live mirror), F4 (the budget read against its own instrumentation line, under the architecture lens), and R2 (the class lens on INV-120's scan boundary).

## Phase 3.5 — Acknowledged gaps

1. **D-1 attic layout, D-6 pair queues, D-7 pair-spec citation depth** — unchanged from the 2.1.0 record; each waits on its real triggering event, recommendations attached. `acknowledged · missing-rule (invariant)`
2. **The [target] machines** — host-facing guardrails + surface registry [E-6, E-10], snapshot [E-7], design-sync [E-18], the measurement family (rows 47-49/96). Each carries its owned row and the S-0 suite tie; consistent. `acknowledged · missing-scenario (state-space)`
3. **The banner-membership ask** — the design review's held question (its record, finding 1) is the document set's one live open ask; F1 above is about its answer's provenance, not the question itself, which stays the human's. `acknowledged · missing-rule (invariant)`

## Phase 4 — Human and operational factors

- **Domain language**: strong; the delta's new prose (INV-181, the restructured skill homes) speaks product words with anchors trailing. No leaked internal identifier on a promised visible string.
- **Observability**: F2 and F3 are the two observability breaks — a running copy that lies about which text governs, and a queue cell that lies about what was verified. Both are one-command or one-sentence folds.
- **Cognitive load**: R1's one-name-two-machines is the one place a reader must already know which script is meant.
- **Performance/scale budgets**: stated with instrumentation homes, but the full-suite wall-time budget is falsified by this pass's own run (F4: 302 s against a stated 60 s, with no row that can see the number).
- **Security/privacy**: the no-secrets line stands; R2 is the one privacy-adjacent path (a commit subject shipping unscanned to a public shopfront).

## Phase 5 — Closing summary

**Top fixes before the 2.2.0 bump (all four defects fold in one sitting):**
1. F1 — settle the kind sentence's provenance: the human's word (re-tag decided, close the held ask) or restate as a deed-only fact; never a design-review class sentence standing on `[default]`.
2. F2 — run `scripts/sync-skills.sh` so the installed prover carries the restructure the gate certifies.
3. F3 — reword row 360's Done-when to the truth today; walk the live-mirror verify at the actual 2.2.0 push and date it.
4. F4 — re-tune the suite wall-time budget to the measured truth and add the seeing row that reads the log's tail line.

**Properties to state explicitly (paste-ready):**
- "A design-review class sentence never lands `[default]`-tagged; it lands decided, on the human's word, or the question stays held on the record." (Already derivable from INV-142; one sentence in INV-141 or INV-125 would make the landing rule readable where the landing happens.)
- "The sync runs the shipped-language check over each assembled mirror README before pushing it." (R2's fold, one line.)
- "INV-96's machine is `scripts/sync-mirrors.sh`, named where INV-181 names it." (R1's fold.)

**Open questions for the author** — none new; the banner-membership question is already asked on the design-review record and stays the human's. Everything else resolves by inspection.

**Recommendations queued for a taste call:** R1 (name the mirror sync script in INV-96 — now), R2 (scan or exempt the generated mirror lines — now), R3 (pin INV-181's artifacts on the publish node — later), R4 (align M-339's cut wording — later).

**[default] count (FULL-pass duty):** 38 live `[default]` lines in PRODUCT_SPEC.md (53 lines mention the literal; 15 are backticked mentions of the tag itself, excluded); 36 at the 2.1.0 pass this morning, the two newcomers both INV-181's (the README surface pick, and the kind sentence F1 challenges). Oldest five unchanged from the 2.1.0 record, restated for the taste call:
1. 2026-07-06 — the parallel-lane cap: "at most three build lanes roll at once without asking [default]" (T-18).
2. 2026-07-06 — the resume-file cap: "whole file ≤ 100 lines [default]" (INV-48).
3. 2026-07-07 — the brief bound: "~300 lines, ~8 files to edit [default]" (INV-55).
4. 2026-07-07 — feedback section: "Facets otherwise N/A [default]" (F-feedback).
5. 2026-07-07 — problem-ledger success measure: "checked at the milestone audit [default]" (F-problem-ledger).

**Findings disposition** (the orchestrator folds):

| Finding | Kind | Disposition |
|---|---|---|
| F1 — kind sentence landed [default] against INV-142's own-word rule, ask still held | defect | open |
| F2 — installed prover copy stale (E-23 sync not run after M-338) | defect | open |
| F3 — row 360 claims a live-mirror verify that has not happened (INV-25) | defect | open |
| F4 — suite wall-time 302 s against a stated ≤ 60 s budget, no seeing row (INV-41) | defect | open |
| R1 — "the sync script" names two machines; name sync-mirrors.sh in INV-96 | recommendation · now | open |
| R2 — generated mirror lines bypass the shipped-language machine | recommendation · now | open |
| R3 — publish node pins carry none of INV-181's artifacts | recommendation · later | open |
| R4 — M-339 under-states the paren-aware story cut | recommendation · later | open |

**Architecture lens verdict (six checks):** every spec fact owned by exactly one node, INV-181 included, mechanically re-verified (traceability 170 green) — clean; no node without spec backing, no new node this delta (fitness test N/A) — clean; seams named, the mirror sync riding the existing publish → human's-gate seam — clean, with R3 the one pin-quality note; budgets stated with instrumentation homes — **one hit (F4)**: the suite wall-time number is falsified by its own instrumentation line and lacks a seeing row; runtime view walks all twelve promised flows — clean; placement view complete with the secrets line — clean. The header's reconcile date is current (2026-07-16) and the 52 pins are strict-clean. This pass's row in the ARCHITECTURE Prover-record table is the orchestrator's append at the fold, with the record name here.

**Suite note:** the full run's tail line reads "1 failed, 940 passed in 302.26s". The one red is gate A (`test_real_repo_passes`): the newest *committed* prover record predates the spec's last commit (`9e727ab`) — the gate doing its job, and this record is the cure; it greens when the orchestrator commits this file with the folds. The 302 s wall time is F4's evidence. Traceability (170 green), pin drift (52 strict-clean), and the mirror/prover-homes tests ran green in the foreground.

**Readiness:** needs one folding iteration — four defects, each a one-command, one-sentence, or one-number fold with no structural change; fold them (F1 needing the human's word or a restatement, F4 a re-tune plus its seeing row) and the spec and architecture are ready for the 2.2.0 bump.

## Disposition addendum (the orchestrator's fold, same day)
| Finding | Disposition |
|---|---|
| F1 (class sentence landed [default] past INV-142) | folded — the kind sentence, M-340, and the banner test reverted; the ask rides to the owner in the session report |
| F2 (installed prover copy stale) | folded — scripts/sync-skills.sh run, installed copy diff-verified current |
| F3 (row 360 claimed an unrun verification) | folded — done-when reworded to the locally-proven truth; MET flips on the live-mirror walk at the push |
| F4 (suite budget falsified, unwatched) | folded (claim) — budget reset to ≤ 180 s with the measured figure; the mechanical net queued as row 361 |
| R1 (name the mirror sync script in INV-96) | folded — the clause names scripts/sync-mirrors.sh |
| R2 (generated lines bypass the shipped-language machine) | folded — check_mirror_language() scans the assembled README before any commit, M-339 cell states it |
| R3 (publish node pins no INV-181 carrier) | folded — scripts/sync-mirrors.sh:1 pinned, 53 pins clean |
| R4 (M-339 cell lags the paren-aware cutter) | folded — cell aligned |
