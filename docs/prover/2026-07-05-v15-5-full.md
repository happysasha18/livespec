# Prover record — 2026-07-05 late night, session 9: FULL pass over SPEC v0.15.5 (push gate, M-6)

Scope: the WHOLE spec as it stands after the five Stage-A landings (rows 57, 60, 63, 64, 65 — SPEC
v0.15.0 → v0.15.5). All phases: entities, states, transitions, invariants, safety/liveness, composition,
plus the architecture and matrix seams. Mechanical halves verified by the suite (78 green: index
uniqueness, single ownership, full matrix coverage, target ownership both directions, pin existence).
Per-delta CROSS-LINK findings live in `2026-07-05-rows57-60.md` (+3 addenda) — zero unfolded must-fix.

| # | Finding | Severity | Folded / rejected |
|---|---|---|---|
| G1 | E-21/E-22/T-8-ext/M-1-item cross-read against their sections' neighbours (adoption walk, wish walk, exits, rhythm) — no contradiction found; each new clause cites the invariant it serves (INV-1, INV-13, E-16) rather than restating it | — | verified |
| G2 | D-1's "current pick" wording (source-dir prefix on collision) predates rule 18 but names the same first move — the ordinal second move applies on top; no rewording owed until D-1 itself closes | should-clarify | RECORDED: revisit rides D-1's own revisit trigger (next real adopt run) |
| G3 | The five landings added 6 new tests and 5 matrix rows; matrix header re-pinned only to v0.15.1 (row-57 note) while the spec is at v0.15.5 | should-clarify | RECORDED: header re-pin is an M-1 milestone item by its own text ("headers re-pin at each milestone"); per-landing pinning is not owed — no drift, the suite checks content not header prose |
| G4 | Composition: the new E-22 (decision page) under the concurrency axis — two windows, two pages, same day: covered by rule 10's ordinal + rule 18 citation | — | verified |

Verdict: READY TO PUSH — zero unfolded must-fix rows across this record and the session's CROSS-LINK
records. Open ⟨DECIDE⟩: D-1, D-2, D-3 (all untouched tonight, none blocking).
