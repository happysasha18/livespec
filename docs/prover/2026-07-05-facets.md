# Prover pass — facet-sweep delta (T-13 + INV-18), v0.10.1 → v0.11.0 (2026-07-05, session 7)

Mode: **CROSS-LINK** on the new "A feature is specified past what you know to ask" delta — clauses **T-13**
(feature-door spec step sweeps the standard facets: phone/narrow layout · touch-vs-hover · empty/error/loading ·
accessibility · performance envelope; facet list's normative home = spec-author skill) and **INV-18** (every
facet specified or defaulted-and-reported, batched, never silently absent; composes with C-1), plus the two
Formal-index rows and the header bump to v0.11.0. Seams checked against T-12/INV-16 (door law, mid-work
re-door), INV-4 (batched, non-blocking), INV-5 (no silent micro-decisions), INV-12 (ask-at-intake), C-1 (axes),
E-14/E-15/INV-15 (owning node + matrix row), E-17/INV-17 (prototype), A-10/A-3/C-1 (adoption + provenance),
M-6 (push gate), and the Formal index (anchor↔prose symmetry).

**Prior-record check (task item 6).** `2026-07-05-v10-push.md` shows all six findings **FOLDED** same sitting;
its closing line reads "Nothing left for the queue from this pass." **Zero unfolded rows carried forward** —
nothing owed from that pass.

**Mechanical guard.** Delta anchor↔prose symmetry exact: T-13 (prose L106 / index L515) and INV-18 (prose L115 /
index L533) each appear once per direction, no duplicate anchors; index one-liners match the prose. Header
bumped to v0.11.0. No regression: the new paragraphs do not reverse any v0.9.1- or v0.10.1-proven clause; the
seams below are gaps, not contradictions of proven material.

**Worker verdict: 1 must-fix · 4 should-clarify · 2 worth-considering.** Per M-6 the must-fix (F1) folds before
the push, or the delta does not ship.

| # | Finding (one line) | Severity | Outcome |
|---|---|---|---|
| F1 | A **defaulted-and-reported** facet has no stated durable home: the two-state framing writes a spec sentence only for **specified**, so a later prover reading SPEC.md cannot tell a once-reported default from a silently-absent facet (INV-18's enforcer is blind to its own middle state), and with no spec anchor E-15 grants the facet no browser-level matrix row → the defaulted phone layout ships unverified. Collides with INV-5 ("recorded in the spec AND surfaced"). Fix: state that defaulted-and-reported ALSO writes a spec sentence tagged as a default the human may revisit — the two states then differ only in provenance (human-chosen vs default-taken), both durable, both prover- and matrix-visible. | must-fix · unenforceable-promise (discharge) | **FOLDED**: every facet ends as a SPEC SENTENCE; a defaulted one carries the literal `[default]` tag at line end — prover tells default from hole, matrix derives its row either way (INV-18 reworded) |
| F2 | The two clauses name **different stages** for the same sweep: T-13 says "the **spec step** walks a fixed sweep," INV-18 says "the sweep fires earlier, at **intake**." The walk (T-1..T-7) makes intake and the spec-delta step distinct; INV-18's own "batched with the walk's other questions [INV-4]" points to the validation stage, not intake. Pin the sweep to ONE stage (recommend the spec-delta step, matching T-13 and the batched-question home) and reword INV-18's "at intake" to "before the surface is composed across axes [C-1]." | should-clarify · internal-conflict (consistency) | **FOLDED**: pinned to the spec step — "drafting its spec-delta walks a fixed sweep" (T-13); INV-18 now says the sweep authors sentences "when the feature is first specified", no intake wording left |
| F3 | **Mid-work re-door to feature** (INV-16: keeps the lane token, "re-enters the walk in place") does not state that the facet sweep runs when the walk already passed its spec step. This is the exact case T-13 targets ("it sounded like loading until the surface existed") — a surface recognized late would skip phone/touch/a11y if "in place" means resuming past the spec step. State that a re-door to feature re-runs the spec step's facet sweep for the newly-recognized surface. | should-clarify · undefined-path (transitions) | **FOLDED**: T-13 states a mid-work re-door to feature walks the sweep before work resumes — the late-recognized surface is the one nobody looked at |
| F4 | The bump is **MINOR** (0.10.1 → 0.11.0) but this pass is **CROSS-LINK**; the prover skill requires a **FULL** re-prove before any MINOR (0.x.0) bump (and M-1 makes the milestone gate a full re-prove). Either bump PATCH (0.10.2) for a two-clause add, or run/own a FULL pass before the M-6 push. | should-clarify · process gate (composition) | **FOLDED as process**: v0.11.0 stands; the M-6 push gate for this landing runs FULL (whole spec, all lenses) — satisfies the MINOR-gate rule, verdict recorded in the push-gate record |
| F5 | The **phone-layout facet** (T-13) and the **viewport axis** (C-1) are the same dimension named in two homes; INV-18 separates them only by timing, not by ownership. Without naming the overlap, an author may specify phone layout twice (sweep sentence + axis composition) — a one-home-per-fact drift. State that the sweep AUTHORS the facet's spec sentence and C-1 only COMPOSES/tests it (viewport, touch↔input, persistence overlap the sweep's facets). | worth-considering · boundary-issue (composition) | **FOLDED**: INV-18 names the split — the sweep AUTHORS the facet sentences at first specification; C-1 axes COMPOSE and test them once the surface exists |
| F6 | An **adopted surface promoted at A-10** "enters at the spec step as a feature [INV-16]" → gets the sweep, but T-13/INV-18 default facets as if greenfield; on an existing surface the sweep must reconcile each facet FROM shipped behaviour (A-3, C-1 provenance axis), else it writes "phone → one column" as the chosen default while the shipped gallery already does two columns → spec-vs-shipped drift (the reverse-verify failure). State that on a promoted/adopted surface the sweep's facet answers are reconciled-from-shipped, not defaulted. | worth-considering · missing-scenario (state-space) | **FOLDED**: on an adopted/promoted surface a default is read from the shipped truth and reconciled like any re-engineered claim (A-3), never invented greenfield (INV-18) |
| F7 | The **prototype boundary** is only inferable: the sweep gates on "door says feature," so an un-promoted SEE/TRY sketch (E-17, no lane, no spec) correctly gets NO sweep, and a promoted prototype ("enters at the spec step [INV-17]") DOES — but neither is stated. Add one clause: the facet sweep does not fire for a fenced prototype and fires on promotion, so a throwaway sketch is never forced to answer phone/a11y/perf. | worth-considering · missing-scenario (state-space) | **FOLDED**: T-13 states a fenced prototype is not swept (a sketch has no facets to promise, E-17); the sweep fires when promotion makes it a feature |

All seven findings folded same sitting by the senior (Fable) -> SPEC v0.11.0 final wording; F4 discharges at the push gate (FULL pass owed before the M-6 push of this landing). Nothing left for the queue from this pass.
