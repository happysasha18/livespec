# Prover record — 2026-07-05 (~22:45), row 98 CROSS-LINK + architecture lens (publish skill)

Mode: **CROSS-LINK** (new surface: E-20 + the fifth working skill) + **architecture lens** (node ADD).
Previous records: zero unfolded.

## Seams checked

- E-20 × T-16 — one axis, read twice: intake keys the pipeline's form, publish keys the deposit's
  checklist; no second kind vocabulary appeared (checked by grep — the four words only).
- E-20 × base rules 12/17 / ACT-1 / M-6 — the checklist runs BEFORE every gate and the skill sends
  nothing itself; no clause lets publish authorize a push (stated in SPEC, SKILL.md, and the seam row).
- E-20 × INV-22 — the publish walk's result rides the landing report; a stood-down publish step is
  named like any other.
- E-20 × E-18 — the design project is one publish TARGET; its own law (declared scope, human gate,
  render authority) is referenced, not restated (INV-13 held).
- E-20 × E-12/INV-13 — pack grew to five working skills: enumeration sweep run (SPEC ×3, base header +
  description + closing line, OVERVIEW heading + bullet, README heading + table, ARCHITECTURE seams ×2)
  — every count updated; "the pack's fifth skill" wording for the base replaced by count-free phrasing
  so the sentence can't rot again.
- E-20 × E-19 — the eval law caught the new skill MECHANICALLY: `test_skill_evals_present` went red the
  moment skills/publish/ appeared without evals/publish.md — the self-closing promise verified by deed,
  same evening it shipped.

## Findings

| # | Finding | Tag | Status |
|---|---|---|---|
| F1 | **The skill's first draft lacked public-repo hygiene the bare eval arm knew:** secrets/history sweep, fixture copyright, dependency-license compatibility, fresh-clone check, name collision. An eval that can only flatter the skill is not an eval — this one cut the other way on its first run. | must-fix · missing-rule (invariant) | **FOLDED** — floor gains the nothing-secret-or-unshareable sweep; GitHub target gains license-compat + name-collision + fresh-clone steps; provenance noted in the skill |
| F2 | A count baked into prose ("the pack's fifth skill") rots at every addition — the same class as "two axes" caught at the v15 gate. | should-clarify · over-specific (abstraction) | **FOLDED** — base described count-free where possible; remaining counts swept this landing |
| F3 | The publish walk needs its first REAL use to earn trust (a plan-shaped eval probes ordering, not deed). | worth-considering · discharge | recorded — first real use = this repo's own next public push, journaled (M-090 names it) |

## Architecture lens (node add)

New node **publish** owns E-20 alone; ownership 1:1 preserved (suite-checked); node backed by spec +
shipped files; new seam "publish → the human's gate" names what crosses and who owns which side.

## Verdict

Landed pending the FULL push-gate pass that precedes the next push (M-6), as always.
