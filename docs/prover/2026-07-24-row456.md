# Prover + audit record — ROADMAP row 456, architecture-format member (v4.3.0, 2026-07-24)

The row-456 landing was worked the night of 2026-07-23 and the fresh-context MINOR gate concluded past midnight; this push-gate record is dated 2026-07-24, the push day.

Fresh-context MINOR-gate pass on the architecture-format conversion: a delta-focused prover with the
architecture lens over the whole spec and the converted `ARCHITECTURE.md`, an adversarial fresh-eyes audit
of the shipped landing, and the design-review parity check folded into the audit. Both reviewers were
seats other than the one that authored the change (SPEC INV-237). Every finding below was folded before
the landing committed.

## Prover findings

| # | severity | finding | resolution |
|---|---|---|---|
| 1 | BLOCKING | `guardrails/check-pin-drift.sh` — a consumer R291.1 names ("the pin-drift check") — still read the raw node shape by slicing `## Nodes`..`## Seams` with `sed`, so INV-280's one-reader discipline was false on arrival; and the enforcing test scanned only `.py`, so the gate's reach could not see the shell consumer. | FOLDED. `guardrails/archformat.py` gained a `--pins` mode emitting `path⇥line⇥label`; `check-pin-drift.sh` now drives off it (121 pins checked, gate green). `test_no_consumer_parses_the_raw_node_table_by_hand` widened to reach `.sh` and the `sed` range idiom `/## Nodes/,/## Seams/`. |
| 2 | SHOULD-FIX | R290.4 ("the no-history law shall thereafter reach this document") was mechanized only on pin labels and the record relocation; a date or `row N landed` note added to an owns, notes, or responsibility field reddened nothing. | FOLDED. Added `test_no_node_field_carries_history` scanning every node's responsibility/owns/notes for a calendar date, a session number, or a landed-row note (pin paths and roadmap-row pointers excluded). It caught two real leaks — "row 417 landed" in a guardrails owns cell and a "2026-07-17" date in a guardrails notes cell — both dropped. |
| 3 | RECOMMENDATION | R290.1's "at most one parenthetical sentence" has no length/count gate; a few owns cells carry multi-clause parentheticals mixing wiring and rationale. | Noted, not acted on this landing — one parenthetical group per anchor holds; a count/length cap would rework several cells for a soft-consistency gain. Candidate for the row-483 doc-cleanup row. |

## Audit findings

| # | severity | finding | resolution |
|---|---|---|---|
| 1 | BLOCKING | Three prose pointers in the "Decisions — where they live" section still said the prover record was "below" / "this table", where the relocated table no longer exists — a gate green (heading absent) over stale prose that fails the cold-reader clause. | FOLDED. The three sentences repoint to `docs/prover/architecture-prover-record.md`. |
| 2 | SHOULD-FIX | The `design-sync` node's wiring pins carried no `:line`, so the one reader counted its pins as zero — a node whose declared pins are invisible to the reader that INV-280 makes canonical. | FOLDED. The three wiring pins gained `:1` anchors, so the reader now reads them. |
| 3 | RECOMMENDATION | The shipped `docs/architecture-format.md` still self-labelled "(draft for ROADMAP row 456)". | FOLDED. The draft label removed. |

## Design-review parity

The three architecture-format invariants (INV-278/279/280) behave in parallel with the matrix member
(INV-272) and the queue members (INV-275/276/277): the same inheritance-by-reference to
`docs/spec-format.md`, the same one-delivery arming, the same reader discipline. The one divergence —
INV-280 (one reader) has no matrix or queue analog — is principled, deriving from a real defect the
conversion retired (two consumers parsed the node table by hand). No same-kind grouping was left
undeclared; the design review folds into this record and lands no separate finding.

## What is sound (verified against primary sources)

The three new requirements pass the family's own format gate (291 requirements / 1420 criteria); the six
relocated rules carry keyword form and trailing anchors and are absent from `ARCHITECTURE.md`; the
conversion holds (22 node sections, every anchor owned exactly once, the matrix node set equal both ways,
the seams/runtime/placement/per-kind/budget sections intact); the format gates have teeth (`archformat`
refuses the retired table shape, red-provable). Suite green modulo the resume-file cap (the close step).
