# Prover — row 254, the delegation line's mechanical check (2026-07-12, short form per INV-61)

Small delta (kind: infra; architecture = anchor assignment INV-103 → build-pipeline).

- **Previous records:** 2026-07-12-row253-routing-reminder clean (0 must-fix); no unfolded rows.
- **The delta in one line:** every landed queue row's status cell carries its delegation accounting
  (what went to a worker with a rough saving, or a stood-down line naming why the senior kept it);
  a suite check reads the queue and a forward-landed row without the line goes red (binds from
  2026-07-12; stock rows stay); the duty binds the orchestrator seat regardless of model — clause
  beside the routing law (INV-103, prose + index, "Who decides what"), the pipeline's
  delegation-saving paragraph gains the teeth sentence, M-241; the scanner red-proven by the
  mechanical protocol (a planted landed-today row without the line failed the run, removed, the
  file restored byte-identical). Composition read: INV-69's routing proposal law untouched (the
  check reads the OUTCOME, the proposal stays advisory); the row-253 reminder is the
  decision-moment half, this check the after-the-fact half — the pair is the rows-253/254 cure the
  row-256 law will generalize.
- **Verdict:** ready to ship — 0 must-fix, 0 should-clarify.
