# Prover record — INV-139, the legibility floor (gap-2)

**Prover skill version:** product-prover, referencing live-spec-base **v1.0.9** (skill file carries no own version stamp; base-rulebook pin).
**Date:** 2026-07-13 · **Mode:** CROSS-LINK (single added clause, INV-136 family) · **Reviewer:** fresh-context, not an author of the delta.
**Delta under review:** PRODUCT_SPEC.md INV-139 legibility-floor clause + Formal-index row; ARCHITECTURE frontend design-principles row (line 189) + base-rulebook node ownership (line 43); communicator pre-show walk step 5; build-pipeline verify feel pass cite; `scripts/preshow-legibility-lint.py`; TEST_MATRIX M-281.

**Opening hypothesis (adversarial):** "the legibility floor duplicates the register lint, its numbers are unjustified, the two-homes wiring is incomplete, or the goal was missed."
**Verdict:** hypothesis largely REJECTED. The floor is a genuine sibling, the numbers are justified and attributed as host-settable, the wiring is complete and green. ONE real gap: the spec ships "large text ≥ 3:1" without ever defining, in the law, what size counts as "large" — the script encodes it (WCAG 24px / 18.66px bold) but the spec's own numbers list is incomplete. Sound to land once that one line is folded.

## What I verified (primary sources, read directly)

- Script runs: flagged `#999` on `#fff` at 2.8:1 and a 9px block; correctly PASSED white-on-black 30px (large-text 3:1 branch). Static-file read, WCAG luminance math correct.
- M-281 tests: `tests/test_legibility_floor.py` — 6 passed (red_fixture, green_fixture, contrast_math, spec_clause_and_index, design_principle_and_preshow_wired, matrix_row).
- All named homes present: spec clause (1128), Formal-index row (1780), ARCHITECTURE row (189), base-rulebook node owns INV-139 (43), communicator step 5 (465, a BLOCK), build-pipeline verify pass cites INV-139 (175), script exists, matrix row M-281.

## Findings

| id | finding | severity | folded / rejected |
|---|---|---|---|
| F1 | "Large text" size boundary is undefined in the spec's law. The clause + index ship the numbers 4.5:1 / 3:1 / 12px but never say what SIZE makes text "large" so the 3:1 floor applies instead of 4.5:1. The script encodes 24px / 18.66px-bold (WCAG); the spec does not. An adopting project building its browser-computed row from the spec numbers alone could pick a wrong threshold (treat 16px as large), ship text at ~3.5:1 that passes its own suite yet sits under the real floor — the exact regression the floor exists to stop. | should-clarify (leaning must-fold; the fix is one parenthetical and the numbers list is explicitly "the law the pack ships") | **must-fold (orchestrator to apply).** In PRODUCT_SPEC.md line 1128 change `large text at least 3 to 1` → `large text (font size at least 24 pixels, or 18.66 pixels when bold) at least 3 to 1`. In the Formal-index row (line 1780) change `large text ≥ 3:1` → `large text (≥ 24px, or ≥ 18.66px bold) ≥ 3:1`. Matches the script's `LARGE_PX` / `LARGE_PX_BOLD` and WCAG. |
| F2 | Register-lint duplication (the opening hypothesis's first horn). | — | **REJECTED.** Genuine sibling with a distinct obligation, cleanly stated: register guards the words are the product's own plain language, legibility guards the same words can be read. "The two guards where text meets the eye" is a real, non-overlapping distinction — one reads the pattern set, the other computes contrast/size. No shared mechanism, no shared failure. |
| F3 | Numbers unjustified / asserted as hard law (second horn). | — | **REJECTED.** 4.5:1 and 3:1 are WCAG AA; 12px is a pack default (WCAG has no absolute size floor, correctly a pack choice, not a false WCAG citation). All three are attributed as host-settable defaults — "a host may set its own on its word" — which is the INV-70 default-parametrization law, not a hard assertion. Correct. |
| F4 | Two-homes wiring incomplete (third horn). | — | **REJECTED.** Both homes wired and green: verify feel pass (build-pipeline line 175, cites INV-139, covered by "runs each declared design principle in the medium's own form") and the pre-show gate (communicator step 5, a BLOCK). Plus ARCHITECTURE row, owning node (base-rulebook), the script, matrix row M-281 with 6 passing tests. No home named-but-unwired. |
| F5 | ships-law / adopting-ships-pixel split; static-floor honesty (fourth horn / goal). | — | **REJECTED as a must-fix; kept as should-clarify note below.** The split is applied correctly and matches INV-136/INV-125 verbatim ("the split the interactive-overlap rule takes since live-spec has no UI"). The script docstring is fully honest about being "a PRAGMATIC STATIC FLOOR, not a browser" (no cascade, specificity, media queries, gradients, JS styles; skips rather than guesses). The goal — a readable floor at the two moments text meets the eye — is met. |
| F6 | Register violations in the clause (scissors / coined metaphor / calque / traceability). | — | **REJECTED.** No banned «X — not Y» scissors: the summary line is a parallel ("one that the words are the product's own, one that they can be read"), not a negation-contrast. No coined metaphor — "legibility floor", "two guards", "where text meets the eye" are ordinary words. Fully traceable: Formal-index row 1780, matrix row M-281, owning node base-rulebook. |

## Should-clarify notes (not blocking)

1. **(F5 spillover) The spec clause could label the script a static floor explicitly.** The docstring does this honestly, and the clause conveys the distinction implicitly (pre-show reads "the styled file", the authoritative browser-computed assertion lives in the adopting suite). Optional: add "(a static read of declared CSS, not a full browser cascade)" after the script name at line 1128, so a spec-only reader doesn't over-trust the pre-show gate. Not required — the docstring owns the honest can/cannot and the clause already routes the authoritative check to the browser.

2. **build-pipeline verify pass names only interactive-overlap by name.** Line 175 spells out INV-136's rule explicitly and folds legibility into the generic "reads the kind's declared design principles and runs each in the medium's own form" (INV-139 cited in the code list). Correct and sufficient, but naming the legibility floor alongside would make the verify step self-documenting.

3. **JOURNAL / version row at landing.** INV-136 earned an ARCHITECTURE JOURNAL row (v1.1.17, line 251). INV-139 has no JOURNAL row of its own yet and the pack version stamp for it is unclear (JOURNAL top is v1.1.18 for the mirror-sync change). Ensure the landing adds an ARCHITECTURE JOURNAL row for the legibility floor and the matching version bump — an at-landing mechanic, outside the law's soundness, flagged so it is not dropped.
