# Prover — row 255, the drafter-applier form enters the pipeline (2026-07-12, short form per INV-61)

Small delta (kind: skill; no spec edit — the form elaborates the trains law's pre-roll, INV-49 stays
the anchor; no architecture change).

- **Previous records:** 2026-07-12-row254-delegation-check clean (0 must-fix); no unfolded rows.
- **The delta in one line:** the trains law's pre-roll paragraph names the standard colliding-rows
  form — the penless DRAFT stage overlaps the current landing (a drafter worker at the judgment tier
  returns the next row's exact edit strings with self-verified needles) while the applier lands the
  current row under the pen; landing order stays declared, numbers follow it — M-242, two string
  tests red-proven; born live tonight, the night the law batch serialized on the spec/matrix/version
  chain and still moved at two rows an hour. Composition read: the pen discipline and the
  one-row-one-commit law untouched (the draft stage writes no repo files); the drafter decides
  nothing (the never side), the senior's brief and acceptance stay the seat's work [INV-69].
- **Verdict:** ready to ship — 0 must-fix, 0 should-clarify.
