# Push re-check — 2.8.2 patch (2026-07-19)

Prover skill version: 2.8.1 (the release stamps the pack to 2.8.2). This record is authored from a clean
context — a fresh session that did not write the change — so the release's adversarial pass meets INV-237
and base rule 33 rather than the authoring seat certifying its own work.

A PATCH by INV-217: the register judge now holds two more universal classes on both surfaces. No new law
a host adopts and no new capability the host wires — the host takes the pack and does nothing; the change
is in the shipped mechanism. The chat law's universal body gains LAW 3 (grading importance or quality
without a concrete fact) and LAW 4 (a coined or jargon term shown with no plain gloss on first use); the
document law gains the same grading class as its LAW 2 and folds the unglossed-term class into its LAW 1.
Both are stated as classes a model reads, not literal lists, so the judge catches the class where a
pattern list trails one escape behind the next word. INV-203's clause and Formal-index row are updated to
name the universal law as four classes and to cite the size-grading law it now binds [INV-221]. The
worked instance (2026-07-19): a session flagged one graded result yet let a graded thought and a graded
rhyme pass and let an unglossed coined term reach the reader — each a narrow instance of the class the
class-reading law now owns.

## Adversarial read (set on breaking it)
- **Renumbering after two new universal laws.** `renumber_laws` renumbers the joined universal + personal
  text into one unbroken 1..N sequence regardless of each block's own base numbers, so the personal
  overlay's literal "LAW 2" base does not collide — universal(4) + personal(2) renumbers to 1..6. The
  test asserts both the joined 1..6 and the universal-alone 1..4. No duplicate number for the judge to
  mis-cite.
- **Shipped-language gate on the new Russian program strings.** The two new detector-vocabulary strings
  («сильная мысль», «развилки») are added to `scripts/shipped-language-allowlist.json` as cyrillic
  waivers of the same self-exclusion class as «X, а не Y», so gate i does not red on the source naming the
  frames it forbids.
- **Spec confinement.** The spec content change is confined to INV-203's clause and its Formal-index row;
  no other invariant, transition, or index row moved, and the index stays contiguous.

## Commits (origin/main 0873bc7..HEAD)
- this record's commit — the judge's two new universal classes on both surfaces (grading-worth,
  unglossed-term), the INV-203 clause and row update, `scripts/shipped-language-allowlist.json`'s two new
  waivers, `tests/test_register_judge.py`'s four new cases and the updated renumber assertion, the 2.8.2
  stamp, and this record.

## Verdict
The register-judge suite is green (40 cases), the new cases proving the universal law carries the
grading-worth and unglossed-term classes and that the judge reds each handed the model's verdict, and the
renumber test proving universal(4) + personal(2) yields a single 1..6 sequence. The installed judge
copies were re-synced, so config-health reads no drift. The spec register lint reports zero errors.
Push-ready.
