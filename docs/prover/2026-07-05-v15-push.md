# Prover record — 2026-07-05 evening, v15 push gate (FULL re-check before push, M-6)

Mode: **FULL** (push gate — host contract: whole-spec re-check before EVERY push). Spec under review:
SPEC.md v0.15.0 as committed in b3c9756 + this gate's own local folds. Delta since the last clean FULL
(`2026-07-05-v14-push.md`, READY-TO-PUSH, 0 must-fix): the row-86 work-kind landing (T-16, INV-22,
CROSS-LINK record `2026-07-05-row86.md`, 3 findings folded) and queue maintenance (date sweep, rows
74/86 closed). Previous records checked: **zero unfolded rows** in both v14-push and row86.

## Opening assessment

One surface landed since the last clean FULL pass, itself CROSS-LINK-proven with its folds verified in
the prose. This gate's pass therefore concentrated on what a CROSS-LINK cannot see: whole-document
consistency of the enumerations the new axis touches, template/spec agreement, and the forward-binding
of the new law against the EXISTING queue. Three findings, all folded within this gate (M-6 locality
held: T-12 wording, the queue template's header, one T-16 clause). Suite 64 green, anchor symmetry and
matrix coverage mechanized and green. **Verdict: READY TO PUSH.**

## What the pass walked

- **Model (Phase 1):** unchanged since v14 except the intake line — now size(+appetite) · priority ·
  door · work-kind; entities/states/actors as proven at v14. The kind is an attribute of the wish row,
  not a stateful surface — no new composition axis (C-1 untouched; the kind classifies WISHES, the
  canonical axes compose SURFACES — checked as distinct, no drift).
- **Enumeration sweep (rule 14):** every place the intake line or its axes are enumerated — SPEC walk
  summary (T-1..T-7 line), T-12, INV-12, base rule 15, build-pipeline step zero, ROADMAP.md header,
  ROADMAP.template.md header. All now speak the same three-axes-plus-kind language; the template was the
  one missed sibling (F2 below).
- **Safety/liveness spot-walk on the new law:** no dead-end (uncallable kind → asked, lane moves on
  INV-12/INV-4; unresolved kind scales nothing down); no over-claiming (S-0: the axis ships as text law
  + skill table + string tests — true); the safety-net clause keeps INV-22 from ever weakening T-12/
  T-14/INV-18/INV-20/INV-21 (consistency checked pairwise).
- **Persistence/versions lens:** the queue IS persisted state — rows queued before T-16 carry no kind;
  without a forward-binding clause every old row becomes a spec violation at the next audit (F3, folded).
- **Index symmetry + matrix coverage:** mechanized (`test_traceability`), 64 green, T-16/INV-22 rows
  M-084/M-085 present, M-067/M-025 re-authored to current truth.
- **⟨DECIDE⟩ inventory:** 3 live, unchanged, none touched by this landing — D-1 (attic layout), D-2
  (tier routing), D-3 (snapshot retention).

## Findings (all from this gate's own pass; folds local to the named sections)

| # | Finding | Tag | Status |
|---|---|---|---|
| F1 | T-12's "The two axes share one word" became ambiguous the moment the line holds three axes — a reader can't tell WHICH two share "bug". | should-clarify · over-general (abstraction) | **FOLDED** — "The size and door axes share one word deliberately" |
| F2 | ROADMAP.template.md (the shape every bootstrapped host copies) teaches size/priority/door but not the work-kind — a host bootstrapped tomorrow would miss the axis its spec binds; the exact sibling of the ROADMAP.md header fix, caught by the rule-14 sweep. | must-fix · missing-scenario (state-space) | **FOLDED** — template header gains the work-kind sentence |
| F3 | T-16 lacked forward-binding: every row queued before the law carries no kind, so the next queue audit would flag the whole existing queue as violating "kind named at intake". INV-20/21 solved the same problem with "binds forward"; T-16 needed its own. | must-fix · missing-rule (invariant) | **FOLDED** — T-16: binds forward; an old row names its kind when it next moves (in-work claim = its intake for this axis) |

## Acknowledged / worth-considering (recorded, not folded — each would widen the landing)

- Adoption could ASK the host's usual kind at orient (a candidate line for row 54's onboarding step or
  the next real adopt run) — today the profile default is optional and that is lawful.
- A mechanical guardrail leg "every new queue row names a kind" is a natural future tooth (named
  non-goal of row 86; rides the host-facing guardrails family, E-6).

## Verdict

**READY TO PUSH.** Zero unfolded findings across this record and both predecessors; suite 64 green /
0 skips; folds local; the pushed state is the state reviewed.
