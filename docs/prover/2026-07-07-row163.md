# Prover record — 2026-07-07 ~10:19, row 163 (test-author skill), CROSS-LINK + architecture lens, session 23

Previous records: pushgate-s23-5 (clean), row165 (fold-complete) — nothing unfolded rides.

Delta under review: the new **test-author** skill (skills/test-author/SKILL.md, 0.1.0) + its wiring —
SPEC E-27 + the working-skills count five → six in every list · build-pipeline steps 5–6 invoke lines ·
README table row · matrix node block + row · `test_test_author_skill`. Kind: skill — no visible
surface, facets N/A.

## Architecture lens (node ADD = structure change — the lens runs with the landing)

- E-27 owned by exactly one node (test-author); the node backed by a spec fact — holds.
- No orphan node: test-author pins to its shipped SKILL.md — holds.
- Seams: the derivation seam named (build-pipeline · test-author; what crosses: proven spec +
  architecture in, TEST_MATRIX.md + owning tests out; format owner: test-author). The two
  working-skills seam cells recounted five → six — holds.
- The one-reviewer rule (cross-audit D): no second review skill created; product-prover untouched —
  holds.

## Delta findings

| # | Finding | Verdict |
|---|---------|---------|
| F1 | Method text would live in TWO homes (build-pipeline steps 5–6 keep their full prose AND the skill states it) — the drift the extraction exists to kill | folded: the pipeline's steps keep ORDER and gates and now open with "invoke test-author"; the ladder/derivation details stay stated once in the skill; the pipeline's step text stays as its own summary the way steps 1–2 summarize their skills |
| F2 | Discoverability at 7 skills (cross-audit F12): author-verb collision "write tests for X" | folded: the boundary sentence rides the skill's description — routes to test-author only when a proven spec/matrix exists, else build-pipeline first |
| F3 | Every pack list must gain the skill the SAME commit (the n=4 failure precedent) | folded: SPEC list · base footer · communicator footer · README table · this doc's seams — all in the one landing; the loadability gate re-counts automatically |
| F4 | Borrowing: the method was proven on track-coach — provenance owed | folded: the skill's credits line names the repo (MIT) and the paraphrase practice (INV-65's own law, landed this morning) |
| F5 | Done-when's field leg (first real use on a host) cannot land from this window | recorded: field leg stays OPEN per the whole-row law |

Skill-creator walk at birth (row-130 clause), abbreviated to its checklist: frontmatter valid
(name · description · version) — holds; description carries triggers AND the NOT side — holds; size
~110 lines, under the 500 ideal — holds; when-NOT section present — holds. Dated audit note:
docs/audit/2026-07-07-test-author-birth-walk.md (written with this landing).

Open ⟨DECIDE⟩ touched: none.
Verdict: fold-complete; proceed to the matrix, the test, and the build.
