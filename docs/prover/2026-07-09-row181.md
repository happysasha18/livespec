# Prover record — row 181: a background worker outlives a memory wipe (INV-76)

Mode: CROSS-LINK (one new clause in Rhythm against the fence/worker/resume neighbours). Date:
2026-07-09 evening, session 30. Spec under review: PRODUCT_SPEC.md v0.16.5. Previous record
(`2026-07-09-row180.md`): all findings folded, nothing carried.

## Opening assessment

The clause teaches the resume walk what the field just proved: a worker survives the wipe, the process
list and the harness task list both lie about it, and the next session's first duty on a recorded
worker is a liveness verdict by deed. The evidence is a real two-writer race (tlvphoto, 2026-07-09).
The seams that could leak are the same-session courtesy, the write-grant's home, and the undefined
verdict thresholds; all three closed in the drafted text. Buildable.

## Findings (each checked against the drafted clause)

F1 — The same-session courtesy could swallow the race

> "Same-session sibling-worker files are fence-benign" — Delegation and workers [ACT-3]

A resuming session could read the recorded worker as its "own briefed hand" and skip the fence — the
exact mistake that raced two writers. The clause must draw the boundary by context, never by ownership
feeling.

Resolution: the clause states it — a worker from a PRIOR context is a foreign writer until verified;
fence-benign never crosses a wipe.

`must-fix · boundary-issue (composition)` — **folded** (in the clause as drafted).

F2 — A verdict with no thresholds is a mood

> "watch the write-set's file times over a short window" — Rhythm, the new clause

Two sessions would pick two different watch windows and one of them declares a live worker dead again.

Resolution: both numbers are stated and `[default]`-tagged (~30 s watch; ~2 min reply), tunable on the
human's word, told at landing [INV-70].

`should-clarify · missing-rule (invariant)` — **folded** (both defaults in the clause and the index).

F3 — The recorded id needs one home

> "the handoff note that records a worker" — Rhythm, the new clause

An id in chat memory dies with the wipe; an id in two homes drifts.

Resolution: the note records the id and points at the worker's own checkpoint file — the checkpoint
stays the one detailed home [ACT-3]; the note is the pointer a cold session reads first.

`should-clarify · unclear-owner (actors)` — **folded** (the clause parenthesizes the pointer).

F4 — Sibling sweep: the "safe to wipe" sentence

> "Session memory can then be wiped with zero loss" — Breakpoints, resume, and milestones [M-2]

The breakpoint offer is the sentence the field failure rode out on ("safe to wipe, the worker will be
recognized"). The offer must stay true beside the new clause: with a live worker recorded, "zero loss"
holds only if the note carries the protocol.

Resolution: no text change needed — the new clause sits in the same section and conditions the offer;
the base rule 6 elaboration says "prefer halting workers before a wipe" in the same breath as the
checkpoint duty. Checked, no contradiction.

`worth-considering · consistency` — **no change needed**, verified side by side.

## Coverage note

CRUD/authorization: N/A (method prose). Entry-symmetry/persistence lenses: the clause IS the
persistence lens applied to workers — state written by an older context meeting a new session; no
further findings.

## Closing

All folded in the drafted clause; nothing rejected. Ready to build.
