# Prover cross-link — SPEC humanize: "Throwing a wish" CHUNK 3 (2026-07-07, session 25)

CHUNK 3 = two bold-leads: The report law is walked, not remembered · Work is narrated while it runs (the
third voice). Correct boundary this time: report-law lead → just before the passport lead.

Gates (whole section after splice): 157/157 tested phrases section-scoped; code multiset identical to
baseline (165 occurrences / 86 groups, zero diffs); full suite 175 green. Chunk 3 carries 21 tested
phrases and ~9 code occurrences, all preserved (incl. "beatless stretch past ~10 minutes owes its
heartbeat [default]" with its embedded code).

## Facts carried (all KEPT)
- Report law walked, not remembered [INV-34]: session-13 bounced report; chat has no suite so enforcement
  is a STEP — re-read communicator rules, pass the draft phrase by phrase through "does this sentence stand
  for a reader who does not live inside the pack"; acceptance belongs to the reader (the "what is this?"
  signal).
- Work narrated while it runs [INV-35]: the third voice between the echo [INV-27] and the report [INV-28];
  beats said as they happen; narration marks beats, never a per-command commentary; three teeth pulled into
  a sub-list — IDENTITY (every narration beat names the work it belongs to), DIGEST (a station's completion
  is itself a beat by law; a station a delegated worker closed becomes the senior's beat), HEARTBEAT (a
  beatless stretch past ~10 minutes owes its heartbeat [default]); the OFFLINE WINDOW (may step away, an
  honest range, never a guess dressed as a promise; questions batch to its end [INV-4]; overrun/done
  sooner/blocked said; a chat line awaiting his return, never a summons; no offline sentence fires when the
  very next beat needs the human); the trail is time accounting, token and test counts stay bookkeeping
  [INV-28]; a narration line is chat, not a report — no pre-report walk [INV-34], asks nothing [INV-31].
- Five Russian quotes rendered as plain-English paraphrases with dates; no new quotation introduced.

Verdict for chunk 3: **CLEAN.** Whole-section gates green.
