# Prover record — the banner joins the declared kind (2026-07-16, short form)

Prover skill version: 2.2.0 (pack version, per INV-178). Mode: short-form re-check on a one-sentence spec delta.

## The delta
One sentence in the INV-181 paragraph, landed on the owner's word (~20:04): a mirror's generated blocks are one declared kind with three members — banner, release history, attribution — each pinned by a test. This answers the 2.2.0 design review's one held question; M-340 + `test_banner_is_the_kinds_third_declared_member` (spec arm red before the clause).

## Walk
- **INV-142 conformance** — the class sentence lands on the human's word, given at ~20:04; the design-review record's question flips to answered, the loop rests CONVERGED. HOLDS.
- **One home** — the kind is stated once, in INV-181's paragraph; M-340 restates only as a matrix fact. No new anchor minted. HOLDS.
- **No behaviour change** — the banner itself was already written by the sync; the delta declares and pins it. Suite covers via the new test. HOLDS.

Verdict: READY TO PUSH (one-sentence decided delta on top of the full-2p2p0 record).
