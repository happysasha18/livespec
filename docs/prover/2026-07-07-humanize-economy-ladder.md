# Prover cross-link — SPEC humanize batch: "When money or time run short (the economy ladder)" (2026-07-07, session 25)

Register rewrite of one scenario section. Gates green: 8/8 tested phrases re-matched section-scoped;
bracket-code multiset identical to baseline (16 distinct groups, incl. [INV-69]×3, [INV-39]×2, [M-6]×2,
[T-19]×2, [default]×2); full suite 175 green.

## Facts carried (old → new, all KEPT)
- Rigor costs money and time (suite runs, prover passes, senior-model hours); pack spends full rigor
  today; section names what a tight budget may LEGALLY shed [T-19].
- `budget.pressure` (package default `full`) moves only on your word, like proactivity and trust
  [E-13, INV-9]; agent may PROPOSE a rung, never set one.
- At SETUP the economy rung is asked, or the standing default told, alongside `project.kind` [INV-36].
- Every shed taken is SAID in the landing report; a silent economy is the micro-decision the report
  prevents [INV-5].
- Three rungs kept as a list with every fact: **full [default]** (full suite / prover cadence / router
  [INV-69]); **lean** (scoped mid-work runs, full suite at landing + push, CROSS-LINK prover, deferred
  FULL pass as a dated debt line in its queue row, cheaper tier on airtight brief [INV-69]); **tight**
  (batched landing gates, each commit exactly one row's delta [INV-39], a red at batch end bisects by
  landing order, a push still requires the full gate green at HEAD [M-6], cheapest sufficient tier
  [INV-69]).
- Never-bend list [INV-40] kept intact: door law + tripwires [T-12, INV-16]; red-before-fix; human's
  gates [INV-9]; landing report with taken-defaults AND named sheds [INV-5, INV-31]; landing purity
  [INV-39]; push gate at full rigor, reach map reading [INV-45] + host cadence [M-6]; safety net,
  poverty its third non-toucher [T-15, T-16]; narration whole at every rung [INV-35].
- Host line outlives any rung: a tighter pinned cadence holds even under `tight` [E-13]; non-goals (no
  numeric budgets/meters, no auto rung-switching); success measure [default]: first budget-named
  session names its rung and sheds aloud [T-19].

## Wording changes worth naming (meaning intact)
- "Rigor costs — suite runs…" → "Rigor costs money and time: suite runs…" (no dash-contrast).
- Long compound sentences split into one-idea sentences; the rung bodies split into short sentences
  inside each bullet. Both lists kept as lists.
- Case-sensitivity watched: "a red at batch end bisects by landing order" and "a push still requires the
  full gate green at HEAD" kept lowercase at their tested casing.

Verdict: **CLEAN.** Every fact and code carried; 8/8 tested phrases section-scoped; 16-group code
multiset identical; suite 175 green. No must-fix.
