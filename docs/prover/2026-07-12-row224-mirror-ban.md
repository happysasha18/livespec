# Prover — row 224, the mirror-assertion ban (2026-07-12, short form per INV-61)

Small delta (kind: skill, no new surface; architecture = anchor assignment INV-102 → test-author).

- **Previous records:** 2026-07-12-row223-declared-laws clean (0 must-fix, 1 observation recorded);
  no unfolded rows.
- **The delta in one line:** a test's expected value derives independently of the code under test —
  recomputing the code's own formula as the expected value makes the assertion a mirror that only
  proves the code equal to itself; three legal sources (a hand-computed constant, an independent
  derivation, a recorded real output reviewed by a human); the honest boundary stated — a round-trip
  or property test over outputs is legal, the ban strikes only same-formula expected values — in the
  spec (INV-102, clause + index) and test-author's writing rules, M-238, four string tests
  red-proven. Composition read: the red-first law and the hygiene rule sit beside it untouched; the
  real-artifact rule [E-15 family] is the ban's kin (both keep the test's truth outside the code);
  the mechanical red-proof path for one-batch fixes stays legal — it restores the pre-change file,
  which is an independent source by construction.
- **Verdict:** ready to ship — 0 must-fix, 0 should-clarify.

Delta drafted by an opus worker from the row text + house exemplars (self-verified needles),
applied by a sonnet worker (zero halts, zero brief defects) — the drafter-applier form's second
run tonight.
