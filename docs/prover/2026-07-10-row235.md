# Prover short-form — row 235, the leave-command (2026-07-10 ~14:55)

One-hop extension of the reporting + rhythm families, short-form per the cadence for a small
skill-kind law.

The new clause (INV-95) composes with four standing laws and contradicts none: the worker-liveness
law (INV-76) already owns the sleeping-machine truth and the handoff discipline — the leave walk
CITES it for the unhaltable-worker path rather than restating the checks, so the liveness protocol
keeps one home; base checkpoint rule 6 already owns red-never-committed and the-checkpoint-IS-the-
red-test — the walk fires that rule for everything open at once, adding no second checkpoint law;
the minutes-to-safe first beat and the standing remaining-minutes habit are INV-93/INV-35's own
wording, cited; the commit at the safe stop runs under the standing gates (INV-80/82), the walk
granting no new push right. Edges named and folded: the closing line is fenced to "only when every
point above holds" (the never side — an early «можно выключать» is the defect the law exists to
kill); a worker that cannot halt in time is recorded per INV-76 and its death-with-sleep said
aloud, so the human closes the lid informed; non-goals written into the clause — no guessing a
leave from silence, the command closes nothing itself. No new entity, no state, no seam; the
architecture step is pure assignment (INV-95 → communicator node), no structure change, so the
architecture prove stands down by name. Matrix M-224 owns the fact at string level across all
three homes, red-proven against the pre-law homes (5 fails → 311/311 green).
