# Prover record — 2026-07-05 ~23:23, session 10: focused delta check, SPEC v0.15.8 → v0.15.9

One clause changed: M-1's compaction leg now names the TEST SUITE alongside spec/matrix/queue/skills —
"a duplicate or superseded test is deleted only when the matrix audit shows its rows still covered by a
live test; nothing grows unboundedly, docs or suite". Triggered by Alexander's question tonight (as we
add, do we also clean?) — the hole was real: the old list never said the suite itself.

Delta walk (entities/invariants the clause touches):
- **INV-1 (queue compaction archives, never deletes)** — untouched and still cited in the same
  parenthetical; the deletion license is for TESTS only, queue rows keep the archive law. No conflict.
- **Matrix-row permanence (RETIRED, never deleted — matrix status vocabulary)** — consistent: the dying
  test's rows are NOT deleted, they re-point to the surviving live test; the clause's own condition
  ("rows still covered by a live test") forces exactly that re-pointing.
- **Removal law (base rule: tombstone + RETIRED rows + owning tests deleted)** — composes, no overlap:
  removal deletes tests whose FEATURE died; compaction deletes tests whose DUPLICATE lives. Different
  triggers, same safety (coverage shown before deletion).
- **Safety direction** — the license is guarded by the matrix audit that runs in the SAME M-1 gate; a
  test can only die at a milestone, with coverage proven, never mid-landing. Liveness: suite size now
  has a shrinking force, answering the "nothing grows unboundedly" promise for the suite too.
- **One home** — the rule lives once, in SPEC M-1; TEST_MATRIX M-029 is the derived row text (mirrors,
  does not restate normatively). No skill text change needed: build-pipeline's removal sentence is the
  other law, correctly separate.

Also this session (queue/docs only, no further SPEC deltas): rows 101–102 intaken (done-claims
evidence walk + versioned answer; multi-story rows can't close by half), the serious-talk item dropped
on his word, tlvphoto investigation folded (one wish file to its inbox — INV-10's one-new-file
exception), the mis-dated prototype-norm inbox file corrected against the clock. Suite 90 green / 0
skips after all edits.

Verdict: READY TO PUSH (v0.15.9 covered by this delta record on top of 2026-07-05-v15-8-full.md).
