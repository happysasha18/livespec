# Prover record — row 350, the seat law sharpened (2026-07-16, FEATURE-FIT)

Prover skill version: 2.0.0 (pack version, per INV-178). Mode: FEATURE-FIT on one delta.

## The delta
INV-67 sharpened: a remote seat's show is textual by construction (inline markdown or a
host-rendered artifact page); local-display verbs stay the local seat's arms; the seat is
re-read after any move between machines.

## Fit walk
- **Arrival** — the sharpened sentences ride the existing seat-detection moment (before the
  first show) and add the re-detect trigger (any move between machines). HOLDS.
- **Exits** — local seat: unchanged (profile show line). Remote seat: textual channel. Moved
  session: re-detect, then the right arm. All stated. HOLDS.
- **Contradiction check** — the profile's `show.artifacts` line names itself the LOCAL seat's
  arm already; no clause tells a remote seat to open a window. Swept `open -na` mentions: all
  scoped local. HOLDS.
- **Size gate** — the communicator body sits under its 500-line ideal after compaction of the
  new bullet (the size test red caught the first draft; compacted, meaning preserved). HOLDS.

## Disposition
| Finding | Kind | Disposition |
|---|---|---|
| task 4 (remote/teleport shows must be textual, no browser) | feature | landed — INV-67 sharpened, M-334 |

Verdict: the delta FITS; no open questions ride.
