# Prover record — 2026-07-07, row 56 (CROSS-LINK: the routing rule, SPEC INV-69, closes D-2)

Mode: CROSS-LINK — the new routing-rule clause's seams against the delegation neighbours it touches:
the senior's judgment ownership (ACT-2), the worker contract and its failed-acceptance escalation
(ACT-3), the economy ladder (T-19), the tier-routing decision (D-2), the adversarial checker (INV-46),
and bookkeeping-scales (INV-61). Previous record check: 2026-07-07-row47.md — 5 findings all folded,
one open field leg riding row 96 as designed; nothing unfolded.

Opening assessment: the rule is sound — it keys the proposal on the STEP and kind (not the row's size
alone, which is the right correction: a large row still holds mechanical sub-briefs, a small one is
often all-judgment), makes the economy rung the only budget knob (no numeric meters), and closes D-2
on the advisory pick already recorded. One seam was blurred in the first draft: the assignment-time
override read as if it were the same event as ACT-3's runtime escalation. Foldable in place; folded.

| # | Finding | Severity · tag | Folded / rejected |
|---|---|---|---|
| F1 | Two "logged tier change" events sit one paragraph apart and read as one: INV-69's OVERRIDE (the senior picks a tier other than the proposal, BEFORE the work) and ACT-3's ESCALATION (a result failing its brief's acceptance bumps one tier, AFTER a miss). Both say "logged"; a reader conflates them and a landing report can't tell which line it owes. Consequence: the override log and the escalation log blur into one, and "proposed → chosen → why" gets written for a runtime escalation it was never meant to describe. Action: name the distinction inline — assignment-time senior choice vs runtime failed-acceptance bump, both logged as different lines. | should-clarify · boundary-issue (composition) | FOLDED — INV-69 gains the parenthetical distinguishing it from ACT-3's escalation |
| F2 | T-19's three rungs previously cited [D-2] for the cheaper-tier shed; with D-2 decided, a live [D-2] cite at the rung would read as "still open" where the mechanism now has a home. Action: re-point the rung cites to [INV-69] (the routing rule owns the mechanism; D-2 remains the decision record in Open decisions). | should-clarify · reference integrity | FOLDED — the full/lean/tight rung lines now cite [INV-69]; D-2's own line reads decided |
| F3 | S-0's target map binds every [target]-marked index fact to a still-open row; ACT-3 carried "[router target]" owned by row 56, and the suite's TARGET_ROW_OWNERS maps ACT-3→56. Landing row 56 without dropping the tag turns the suite red (a target owned by a terminal row). Action: drop "[router target]" from ACT-3's index fact and the header's target list, remove ACT-3 from the target map — the self-closing design the target test intends. | must-fix · reference integrity | FOLDED — index fact + header list + test map all swept; test_targets_owned_by_open_rows green |
| F4 | A stale note: matrix M-010 (ACT-3 checkpoint fact, TODO) read "router design (row 56) makes it mechanical" — but D-2 decides the router stays ADVISORY, so the note would claim the opposite of what landed. Action: rewrite the note to the decided reality. | should-clarify · stale-claim | FOLDED — M-010 note now reads "routing decided advisory not mechanical (row 56, INV-69)" |

Open ⟨DECIDE⟩ count touched by this delta: D-2 was the one, now Decided. Remaining open elsewhere: D-1
(attic layout) — untouched, not in this delta's sections.

## Architecture lens (no structure change — assignment only)

- No new node: INV-69 is added to the existing **build-pipeline** node's owned anchors (same family as
  the delegation-mechanic invariants it lives with — INV-46/53/54/55/62/63); its normative home is the
  SPEC delegation scenario, elaborated in the build-pipeline SKILL. Owned exactly once (grep of the
  Nodes table run at the landing: INV-69 appears in build-pipeline only).
- Spec-backed: the clause is INV-69's charter; no speculative structure, no new pin (the build-pipeline
  SKILL pins already cover the gates/delegation region the elaboration sits in).
- Seam unchanged: ACT-3 (base-rulebook) still owns the worker contract; INV-69 (build-pipeline) owns
  the tier PROPOSAL that precedes it — the seam is "proposed tier → the contract binds the chosen tier",
  named in the prose, no format crosses that both nodes must agree on beyond the tier name.

Verdict: CROSS-LINK clean after the fold — 1 must-fix (F3) folded, 3 should-clarify folded. Ready to
land. First routed landing recorded in the landing report (this build's own routing).
