# Prover pass — 2026-07-05, 15:20 — settings scopes + thin loader (rows 52–53)

Mode: CROSS-LINK (new/changed paragraphs of SPEC v0.8.0 — E-13 four nested scopes, INV-14 session
sentence, new E-16 — checked against the whole spec, all sections in view). Previous record
(2026-07-05-iterativity.md) had no unfolded rows.

Opening assessment: the scope rewrite is a clean generalization — the old three-step ladder survives as
the resolution order, scopes gain nesting and inheritance, and the session scope finally names what was
always practiced (the human's live word). The loader paragraph correctly routes the migration through
scopes rather than files. Two seams needed a sentence each (session lines vs the zero-loss breakpoint
promise; who may write a foreign host's profile during the fork) and one home-of-fact ambiguity (the
loader's bootstrap lines vs "profile is the ONE home"). All three folded same pass. Ready to build.

| # | Finding | Severity · category | Folded? |
|---|---|---|---|
| F1 | A session override lives "only in your spoken word", but the breakpoint rule promises the session memory can be wiped "with zero loss" — a live "today answer in English" would not survive the wipe, and nothing said which promise wins. Consequence: the human sets a session line, a self-compaction or wipe happens mid-sitting, the agent silently reverts to profile behavior and the human repeats themselves. Action: state that an announced self-compaction carries live session lines forward in its summary, and that a full wipe ends the sitting — session lines die with it by design (the wipe is the human's own move). | should-clarify · missing-scenario (state-space, persistence axis) | folded — sentence added to the INV-14 paragraph |
| F2 | The migration fork sends "a project line → that project's host profile", but a foreign project's repo is exactly what this session must never write (write-ownership; the one-window rule exists because of a real near-miss). Consequence: a live-spec session executing row 52 writes track-coach's `.live-spec/profile.md` in track-coach's repo — the class of accident INV-10 forbids. Action: the fork WRITES only what the running session owns; a project line becomes a written migration note the project's OWN session lands at its next update. | must-fix · unclear-owner (actors / composition) | folded — sentence added to E-16 |
| F3 | E-16 says the profile is "the ONE home of the personal layer", then puts bootstrap lines in the loader — two homes for personal lines unless the exception is named. Consequence: the same rule (e.g. which-project disambiguation) gets stated in both loader and profile, the copies drift, and resolution between them is undefined. Action: name the loader as those lines' one home; the profile never restates them. | should-clarify · boundary-issue (consistency with INV-13) | folded — clause added to E-16 |

Coverage notes (cross-link scope): session scope × concurrency (two sessions, one host) is handled by the
nesting sentence (a host holds every session — each session its own scope); inheritance handles an absent
or partial profile (no override → broader scope stands); promotion of a session line is journaled under
the existing no-silent-override rule. No further seams found against wish-lifecycle, adoption, machines,
package-repo, or rhythm sections.

Open ⟨DECIDE⟩ count after this pass: 3 (D-1 attic layout, D-2 tier routing, D-3 snapshot retention) —
none touched by the changed surfaces. D-4, D-5 stand decided.
