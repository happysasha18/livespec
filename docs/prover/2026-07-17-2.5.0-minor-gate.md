# Prover record — FULL pass, pre-MINOR gate 2.4.0 → 2.5.0

- Pass run by: **product-prover 2.4.0** (the pack version; read from `VERSION` = 2.4.0, the bump to 2.5.0 lands at this gate, and the skill frontmatter = 2.4.0 — they agree, per INV-178's one-fact rule)
- Date: 2026-07-17
- Mode: **FULL** over `PRODUCT_SPEC.md` (2091 lines, read whole) + the **architecture lens** over `ARCHITECTURE.md` (six checks)
- Delta since the 2.4.0 full pass — one ROADMAP row landed in the working tree (uncommitted), weighted but not scoping the read:
  - **369** — the standing sub-domain question lifts to the general range law. Within INV-138 the general duty now LEADS the sub-domain portion of the clause ("A guarantee scoped to a named part of its domain owes the same completeness across the whole domain…", line 1779), with the viewport-band case demoted to its worked instance; the Formal-index row (line 2071) follows the same order; the product-prover edge-completeness lens leads with "the named-part ask" (viewport its worked case, SKILL.md ~269); the design-reviewer band lens cites itself as the group-pass instance; the spec-author viewport facet cites itself as the author-side instance. M-351 / `test_general_sub_domain_duty`. Owner's word 2026-07-17: the "answers for the rest" duty shipped worded for one quantity in 2.4.0, and a guarantee scoped to any other named sub-domain (a user state, a network condition, a locale) is the same hole class — the law must not read as a literal for one quantity.
- Previous record checked (opening duty): the 2.4.0 gate record (`docs/prover/2026-07-17-2.4.0-minor-gate.md`) closes **fully folded** — its disposition addendum (00:17) folds R1 and decides R2. No unfolded row rides forward.
- Deed checks run this pass: `guardrails/check-pin-drift.sh` (**54 pins, strict-clean**); the full suite `python3 -m pytest -q tests` run in the foreground, its own tail line the verdict [INV-80] — **965 passed, 2 skipped, exit 0** (the two skips are the reach-scoped suite-in-suite meta-tests, `test_guardrails.py:289/299`, skipped because the diff touches no gate-machinery file, INV-45/M-345 — a legitimate reach skip, not a swallowed failure); M-351 read under its `### [node: spec-author]` heading (INV-138's owner) so the traceability net holds; `git diff --stat` to bound the delta to its declared homes; the general-duty needles read in all four homes (spec clause, Formal-index row, prover lens, matrix row) and the viewport needles from row 368 read intact.

## Triage

TRIAGE: PROCEED — a full product spec with states, transitions, actors, and invariants, paired with an architecture doc whose reconcile date is current (2026-07-16) and whose 54 node pins pass the strict drift check. The doc claims a shipped system; the pins stand and the suite is green, so findings are unconditional. No worker code is in flight — the one row's test code has landed and the suite is fully green.

## Opening assessment

This is a small, well-shaped document delta, and the pass holds green end to end. Row 369 lifts one wording within one existing invariant: INV-138's viewport-specific sub-domain sentence becomes the general sub-domain duty — a guarantee scoped to a named part of its domain owes completeness across the whole domain — with the viewport bands kept as the worked instance and the 2026-07-16 caption incident parenthetical intact. The lift lands in exactly four homes (the spec clause, its Formal-index row, the product-prover edge-completeness lens, and — as citing instances — the design-reviewer band lens and the spec-author viewport facet), adds no invariant, no node, no seam, and no matrix law beyond M-351 anchoring the existing INV-138. `git diff --stat` bounds the change to those homes plus the ROADMAP/NEXT_STEPS/matrix housekeeping and the one new test. The three instance citations agree: all four homes carry the same core sentence ("a guarantee scoped to a named part of its domain draws the standing question about the remainder") and all name the viewport as the worked/group-pass/author-side instance of the same duty — no home claims the general law owes something the others don't.

The generalization is the owner's own word, examples and all (user state, network condition, locale), so its breadth is ratified rather than an agent's over-reach. The one thing I probed hardest — whether the broadened "any named sub-case" now lets a prover flag every conditional sentence as sub-domain-scoped — is honestly bounded: the finding fires only "while the remainder stays silent" and only "until the domain is covered," and a conditional whose complement is governed elsewhere or is unreachable draws no finding. The block/recommend split with the design review is preserved verbatim across all three skills. I found no blocking defect. One low recommendation only: the clause's bold lead header now under-describes its broadened body (R1), a structural-clarity point that is largely pre-existing from row 368 and non-blocking.

Confidence: **ready for the 2.5.0 bump.** Nothing structural is wrong; the one recommendation queues for a taste call and does not hold the merge.

## Phase 1 — The model

The entity, state, actor, and composition model is unchanged from the 2.4.0 full pass (`docs/prover/2026-07-17-2.4.0-minor-gate.md`, Phase 1, which itself carries the 2.2.0 model forward). The delta touches one law and adds no new entity, node, or seam:

- **Range-and-lifecycle law (INV-138)** — the viewport-quantifier ask that landed at row 368 is generalized to its parent duty: a guarantee scoped to any named part of its domain (a band of a ranged quantity, a user state, a network condition, a locale, any named sub-case) draws the standing question about the remainder, each remaining part decided or `[default]`-tagged until the domain is covered. The viewport bands become the worked instance of this general duty rather than the whole of it. The clause's other two duties — the both-ends-of-the-range duty and the async-slot pending/arrived/failed duty — stand unchanged beside it, and the composition-lens family line still reads `[INV-125, INV-126, INV-136]`.

### What I assumed

- I read the general duty's breadth as ratified by the owner: ROADMAP 369 and M-351 both quote his 2026-07-17 word naming user state, network condition, and locale as the same hole class, so the generalization is his decision, not a derived over-reach. The prover's job here is to check the wording bounds the ask honestly, not to relitigate whether to generalize.
- I read live-spec's own spec as having **no layout-bearing surface** (the architecture states "live-spec itself has no UI"), so INV-138's own worked instance — layout guarantees across viewport bands — has no in-document instance to sweep here; the lens lands for the *host* projects the pack serves. The generalized duty is proven by its homes' presence, the four-home agreement, and its test, not by a sub-domain-scoped guarantee inside this spec.
- Carried forward unchanged: the FULL-pass table duty, and the surface-authority assumption lines from the 2.1.0–2.4.0 records (no authoritative surface is named for a category this delta touches without registering with it).

## Phase 2 — Structural issues

No structural finding. The delta adds no node, seam, or actor; `git diff --stat` bounds it to INV-138's four homes plus the ROADMAP/matrix/NEXT_STEPS housekeeping and the one new test. Node ownership holds: M-351 anchors INV-138 and sits under `### [node: spec-author]` (INV-138's owning node), so the traceability net (`test_matrix_rows_sit_under_their_owning_node`) stays green. No new anchor is introduced that would want a node.

## Phase 3 — Property analysis

The one substantive probe this delta invites — is the generalized "named part of its domain" bounded enough to be enforceable, or does the mandatory edge-completeness sweep now license flagging every conditional sentence? — comes out clean. I record the reasoning rather than a finding, and file one low structural recommendation (R1).

**The over-breadth probe — clean.** The lifted duty is bounded by three devices, all present in every home:

1. *Silence is the trigger.* The finding fires only on "a guarantee that speaks for one part while the remainder stays silent" (spec clause) / "a guarantee true as written over one part while the remainder stays silent" (prover lens). A conditional whose complement is answered by another clause draws no finding.
2. *Coverage discharges it.* "each remaining part gets its own decided or `[default]`-tagged sentence until the domain is covered" — the duty is satisfied by coverage anywhere in the spec, not by a remainder sentence bolted onto each conditional.
3. *The examples are total, reachable domains.* Band of a ranged quantity, user state, network condition, locale — each is a domain every real user lands somewhere in, so a silent remainder is a genuinely reachable, undecided situation (the viewport case's property: a surface always renders *something*, so silence produces a concrete undecided artifact). The catch-all "any named sub-case of the domain it governs" ties the sub-case to the guarantee's own governed domain, and the silence-plus-reachability bound keeps it from firing on a deliberately-scoped guarantee whose complement is either governed or not reachable.

On top of the clause's own bounds, the prover skill's standing meta-rule caps over-firing directly ("A lens that prompts no real concern produces no finding. Do not invent issues to satisfy a lens."). So the sweep does not flag every conditional; it flags a guarantee scoped to one reachable part of a domain whose other reachable parts nobody answered. The breadth is the owner's word, and the enforcement bound is honest.

**Composition with the clause's neighbours — clean.**

- *The both-ends duty.* The general sub-domain duty and the both-ends-of-the-range duty are complementary framings, not rivals: both-ends asks that a single gate name what holds below and above its threshold; the sub-domain duty asks that a guarantee scoped to one named region of a domain draw its complementary regions. A "band of a ranged quantity" is explicitly listed under the sub-domain duty, so for a ranged domain both can apply; they produce the same kind of finding (a silent reachable region) and never contradict. The both-ends sentence stands intact in the clause ("both ends of the live range").
- *The async-slot faces.* The pending/arrived/failed duty stands unchanged as its own sentence before the general duty. An async slot's three faces are the lifecycle of one slot, not a domain partition, so the general sub-domain duty correctly does not reach or subsume it — no collision. The sentence reads intact ("pending, arrived, and failed").
- *The unwritten-seam class [INV-72].* The general duty ties itself to INV-72 explicitly ("the same unwritten-seam class"), exactly as the both-ends case and the viewport case already do. One root, consistently cited across all three duties in the clause.

**The three instance citations agree on what the general law owes.** All four homes carry the same core sentence and name the viewport as the worked instance of the same duty:

- Spec INV-138 clause (line 1779): "A guarantee scoped to a named part of its domain owes the same completeness across the whole domain… draws the standing question about the remainder, and each remaining part gets its own decided or `[default]`-tagged sentence until the domain is covered. The layout guarantee over the viewport is the worked instance…"
- Formal-index row (line 2071): "a guarantee scoped to a named part of its domain draws the standing question about the remainder, each remaining part decided or `[default]`-tagged, the layout guarantee over the viewport bands its worked instance…" — same order, general duty leading.
- product-prover edge-completeness lens (SKILL.md ~269): "**And the named-part ask:** a guarantee scoped to a named part of its domain draws the standing question about the remainder… Its worked instance is the viewport…"
- design-reviewer band lens: "It is the group-pass instance of the general sub-domain duty the range law carries — a guarantee scoped to a named part of its domain draws the standing question about the remainder — with the viewport bands its worked case…"
- spec-author viewport facet: "the author-side of the viewport-quantifier lens the prover holds, itself the worked instance of the range law's general sub-domain duty (a guarantee scoped to a named part of its domain answers for the remainder, a user state or a locale drawing the same sweep on its own parts)…"

The block/recommend split is preserved verbatim in all three skills: the design review reaches an *undeclared* same-kind group and recommends (never blocks); the prover holds a *declared* part-uniform guarantee via INV-72 and blocks. No citation claims the general law owes a stronger or weaker duty than the others. The author-side (facet list) correctly stays incident-curated at viewport only, while the prover-side lens carries the general ask — the right division: the facet list grows by incident (T-13), the prover lens generalizes; spec-author's user-state/locale mention is an illustrative parenthetical, not a new facet the list must back.

R1 — The clause's bold lead header now under-describes its broadened body

> "**A gated behaviour names every side of its gate.**" — PRODUCT_SPEC.md, INV-138 clause header (line 1779)

The clause's bold lead names the both-ends-of-a-gate duty, but the clause's center of gravity is now the general sub-domain duty — broader than "a gate's sides" (a user state or a locale is not a gate). A reader indexing the composition section by its bold headers to find "where does the spec state that a locale-scoped or user-state-scoped guarantee owes its remainder?" would not land on "names every side of its gate." This is a discoverability/consistency cost, not a correctness or completeness gap — the law is fully stated in the body, and the Formal-index row and M-351 both carry the general wording as anchors. It is largely pre-existing: at 2.4.0 the header already carried the viewport generalization under the same "gate" lead; row 369 widens the mismatch rather than creating it. Fold, if taken: broaden the bold lead to name the general duty (e.g. lead with "A guarantee names every part of its domain" and keep the gate's-two-sides case inside), or add a second bold lead sentence for the sub-domain duty. Preference: broaden the lead — the clause's dominant duty deserves the header a scanner reads first. Non-blocking; queues for a taste call.

`recommendation · later · confusing-for-users (cognitive-load)`

### Quantifier re-verify (INV-170)

Swept the enumerations the delta could falsify:

- **The composition-lens family membership** (INV-138: "the range-and-lifecycle member of the composition-lens family [INV-125, INV-126, INV-136]"): row 369 grew INV-138's own clause wording, added no family member, and the sibling list is unchanged (verified `[INV-125, INV-126, INV-136]` reads intact). Clean.
- **The design review's "two producers" line** (design-reviewer SKILL.md line 85: "This channel carries two producers — the same-kind divergence from the similarity lens, and the motion-parity divergence…"): row 369 edited the band lens paragraph above it but did not touch the producer count; the pinned two-producer line reads intact. The generalization routes through the *existing* similarity divergence, minting no third producer. Clean.
- **The viewport needles from row 368** ("every layout guarantee names its viewport quantifier", "on every viewport", "the band it is scoped to", "the other bands", "the short-viewport band"): all read intact in the spec clause, the index row, and the prover lens — the lift demoted them to the worked instance without deleting any. Clean.
- **The architecture lens "six checks"** enumeration (product-prover ARCHITECTURE-lens block): row 369 touches no architecture check; the count stands. Clean.
- **M-350 vs M-351** (both anchor INV-138): M-350 describes the viewport-quantifier lens specifically, M-351 the general lift; they coexist as distinct matrix rows over one invariant, no member-list contradiction. Clean.

No enumeration is falsified by this delta.

### Coverage tables (INV-171)

The three classic tables collapse for this kind exactly as the prior records state (entities archive rather than delete; invariants-per-state live in the Formal index and matrix; no user roles). Per INV-171 the surface × sweep verdict table stands in their place. All five mandatory sweeps ran; every cell is a real verdict.

| Surface | Declared laws (INV-101) | Edge completeness (INV-138) | Uniformity (INV-125) | Lifecycle (INV-168 parent) | Unwritten seams (INV-72) |
|---|---|---|---|---|---|
| Composition laws (INV-138 general sub-domain duty) | clean | **hit — R1** (header under-describes the broadened body; discoverability, non-blocking, largely pre-existing) | clean (block/recommend split preserved verbatim across the three skills) | clean | N/A — live-spec has no sub-domain-scoped layout guarantee of its own; the duty is proven by its four homes + the four-home agreement + M-351 |
| Standard-facet sweep (INV-18) | clean | clean (facet list correctly stays viewport-only, incident-curated; the general ask rides the prover lens, not the facet list) | clean | clean | clean |
| Matrix rows (TEST_MATRIX) | clean | clean | clean | clean | clean (M-351 under `[node: spec-author]`, INV-138's owner; BUILT + test on disk + green) |
| Push/CI gates (reach map, hygiene net, budget gate) | clean | clean | clean | clean | clean |
| Suite & harness | clean | clean | clean | clean | clean (the two reach-scoped skips execute their skip path, INV-80) |
| Quality budgets (INV-41 watcher duty) | clean | clean | clean | clean | clean |
| ARCHITECTURE.md (nodes, seams, budgets) | clean | clean | clean | N/A — a document | clean (no node/seam/budget change; INV-138 stays on spec-author, no pin moved) |
| Queue & rows (ROADMAP) | clean | clean | clean | clean | clean |
| Decision page + batched questions | clean | clean | clean | clean | clean |
| Chat surfaces (echo, narration, board, reports) | clean | clean | clean | N/A — stateless lines | clean |
| Settings card / feature-map answer | clean | clean | clean | N/A — read-only | clean |
| Inbox (4 arms) + stranger monitor | clean | clean | clean | clean | clean |
| Feedback ledger + outbox / collector | clean | clean | clean | clean | clean |
| Problem ledger | clean | clean | clean | clean | clean |
| Prototype home + norms | clean | clean | clean | clean | clean |
| Review records (prover / design review / audit) | clean | clean | clean | clean | clean |
| Resume file + checkpoints | clean | clean | clean | clean | clean |
| Installed skills (E-23 / INV-180) | clean | clean | clean | clean | clean |
| Standalone mirrors (README shopfront) | clean | clean | clean | N/A — regenerated whole | clean |
| Adoption / catch-up / bootstrap walks | clean | clean | clean | clean | clean |

The imaginative probes ran without owed verdicts and produced R1 (the cognitive-load lens on the clause header). The surface-authority probe: I found no authoritative surface for a category this delta touches that the operations fail to register with — M-351 registers in TEST_MATRIX under its owning node, the general duty registers in the INV-138 clause, its Formal-index row, and the three skills. The over-breadth probe the delta most invited came out clean (the enforcement bound is honest; the breadth is the owner's word) and is recorded above rather than as a finding.

## Phase 3.5 — Acknowledged gaps

1. **D-1 attic layout, D-6 pair queues, D-7 pair-spec citation depth** — unchanged from the prior records; the three standing ⟨DECIDE⟩ markers (line 1803–1810), each waiting on its real triggering event, recommendations attached. Row 369 introduced no new ⟨DECIDE⟩. `acknowledged · missing-rule (invariant)`
2. **The [target] machines** — host-facing guardrails + surface registry [E-6, E-10], snapshot [E-7], design-sync [E-18], the measurement family. Each carries its owned row and the S-0 suite tie; consistent. `acknowledged · missing-scenario (state-space)`

## Phase 4 — Human and operational factors

- **Domain language**: strong. The delta's new prose (the general sub-domain duty across the four homes) speaks product words with anchors trailing; no leaked internal identifier on a promised visible string.
- **Observability**: unchanged; the row is a docs-only lift within one invariant.
- **Cognitive load**: the one spot a reader must know is R1 — the general duty lives under a bold header that names only the gate case. Also noted (below the finding bar, an observation): the general sub-domain ask, which now covers non-ranged domains (a user state, a locale), rides a prover lens titled "Edge-condition completeness"; a locale is not an "edge," so a reader scanning lens *names* for where locale-scoped guarantees are checked would not look there. This home follows the owning invariant (INV-138's prover surface is the edge-completeness lens) and was already settled at row 368, so it is an observation, not a delta-born finding.
- **Performance/scale budgets**: the suite runs 35.10 s against the ≤ 360 s budget — green with wide margin.
- **Security/privacy**: no change; the no-secrets line stands.
- **Architecture reconcile date** (observation, non-blocking): `ARCHITECTURE.md` reads "Last reconciled with the spec: 2026-07-16" while row 369 edits the spec on 2026-07-17. The lift moves no fact between nodes (INV-138 stays on spec-author, no pin moved), so no content re-reconcile is owed; at the landing the date may tick to 2026-07-17 as pure housekeeping. Not a prover defect.

## Phase 5 — Closing summary

**Top fixes before the 2.5.0 bump:** none blocking. The delta is document-clean and the suite is green; the bump can proceed. The one recommendation below queues for a taste call.

**Properties to state explicitly (paste-ready):**
- "A guarantee scoped to a named part of its domain draws the standing question about the remainder only where the remaining parts are reachable and ungoverned; a conditional whose complement is answered by another clause, or is not reachable, draws no finding." (Makes the enforcement bound the over-breadth probe verified explicit in the clause, if the author wants the bound on the page rather than only in the reader's judgment.)

**Open questions for the author** — none that inspection cannot resolve.

**Recommendations queued for a taste call:** R1 (broaden the INV-138 clause's bold lead header so a scanner finds the general sub-domain duty, or add a second lead sentence — later). Does not block.

**[default] count (FULL-pass duty):** ~44 live `[default]` mentions in PRODUCT_SPEC.md (65 literal occurrences; 21 backticked mentions of the tag itself, excluded) — about one more than the 2.4.0 record's ~43, from row 369's general-duty wording ("each remaining part gets its own decided or `[default]`-tagged sentence"), which describes the tagging convention rather than introducing a new unratified default decision. Row 369 closes **decided** (the lift is the owner's own word), not `[default]`-tagged. Oldest five unchanged from the prior records, restated for the taste call:
1. 2026-07-06 — the parallel-lane cap: "at most three build lanes roll at once without asking [default]" (T-18).
2. 2026-07-06 — the resume-file cap: "whole file ≤ 100 lines [default]" (INV-48).
3. 2026-07-07 — the brief bound: "~300 lines, ~8 files to edit [default]" (INV-55).
4. 2026-07-07 — feedback section: "Facets otherwise N/A [default]" (F-feedback).
5. 2026-07-07 — problem-ledger success measure: "checked at the milestone audit [default]" (F-problem-ledger).

**Architecture lens verdict (six checks):**
1. *Every spec fact owned by exactly one node* — clean. M-351 anchors INV-138 (owned by spec-author) and sits under that node; no orphan, no new anchor un-owned.
2. *No node without spec backing* — no new node; clean.
3. *Every seam names what crosses and which side owns the format* — no new seam; clean.
4. *Budgets stated with instrumentation homes, each naming its watcher* — no budget change this delta; the 2.4.0 watcher column stands; clean.
5. *Runtime view walks every promised flow* — no new flow; clean.
6. *Placement view with load-bearing technology* — unchanged; clean.
The header's reconcile date is 2026-07-16 (one day behind the spec's row-369 edit, but no fact moved homes, so no re-reconcile is owed — the date may tick at the landing as housekeeping); 54 pins strict-clean. This pass's row in the ARCHITECTURE Prover-record table is the orchestrator's append at the fold, with this record's name.

**Suite note:** the full run tail reads "965 passed, 2 skipped in 35.10s", exit 0 — no failures, no errors. The two skips are the reach-scoped suite-in-suite meta-tests (INV-45/M-345), which skip by design when the diff touches no gate-machinery file.

**Readiness: ready to build.** Fold nothing before the bump — the one recommendation (R1) queues for a taste call and does not hold the merge. The spec and architecture are ready for the 2.5.0 bump on the green suite and the strict-clean pins.

**Remaining open ⟨DECIDE⟩ markers:** 3 (D-1, D-6, D-7 — all pre-existing; none introduced this pass).

**Findings disposition** (the senior folds):

| Finding | Kind | Disposition |
|---|---|---|
| R1 — INV-138's bold lead header ("A gated behaviour names every side of its gate") under-describes the broadened clause whose center is now the general sub-domain duty; discoverability cost, largely pre-existing from row 368 | recommendation · later | OPEN (queues; non-blocking) |

No defects. The one finding is a non-blocking recommendation; the 2.5.0 bump proceeds on the green suite, the strict-clean pins, row 369 landing complete in every home it names, and the four-home agreement of the general-duty wording.


---

## Disposition addendum (2026-07-17 ~10:58, same session)

- R1 (the clause's bold lead under-described the now-general body): FOLDED — the lead reads "A gated behaviour names every side of its gate, and a scoped guarantee answers for the remainder of its domain."; the pinned substring survives verbatim, the general half now indexable by header.

- Post-audit lines (11:11): the design-reviewer sentence this record quotes ("It is the group-pass instance...") was rewritten at the gate fold into the standing named-part lens; the four-home agreement over the new text was re-proven by the 2.5.0 audit. And the coverage claim "no sub-domain-scoped layout guarantee of its own" reads too narrow for the second gate running: the settings-card guarantee (PRODUCT_SPEC.md ~1328) is such a guarantee, band-scoped and ANSWERED since the 2.4.0 fold — the carried-forward assumption line is retired.
