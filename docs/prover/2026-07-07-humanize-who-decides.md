# Prover cross-link — SPEC humanize batch: "Who decides what" (2026-07-07, session 25)

Register rewrite of the governance section (the most tested-phrase-dense yet: 28 verbatim phrases, ~50
bracket-code occurrences across a dozen bold-lead paragraphs). Gates green: 28/28 tested phrases
re-matched section-scoped; bracket-code multiset identical to baseline (every count matched, incl.
[ACT-3]×4, [E-13]×3, the doubles, and [T-18, INV-39]); full suite 175 green. One case-sensitivity trap
handled at write time: the lowercase tested phrase "the brief ARMS the worker for the workshop" was kept
lowercase inside a bullet rather than capitalized at the bullet start.

## Facts carried (all KEPT, paragraph by paragraph)
- Human owns taste/design/irreversible/publish-push gates/domain wording + own working contract [INV-9]
  [ACT-1]; personal-profile lines follow you, host profile at `.live-spec/profile.md` narrows [A-8], only
  checkpoints ignored [ACT-3] [E-8]; communicator reads the resolved contract [E-13]; mode & trust only on
  your word [INV-9].
- Done-claim answered by walking the evidence + method version [INV-25]: pinned to a checkable artifact,
  walked NOW; verified said apart from asserted; names the method version from the installed set [M-7]; the
  sample claim line kept verbatim (795 tests, commit `193d39d`); an absent version is itself an honest
  answer, never an invented one; track-coach origin.
- Four nested scopes ladder [E-13], pulled into a list (package defaults [E-12] / personal profile / host
  profile [E-8] / session); narrowest wins; inheritance + resolution order; profiles re-read at freshness
  [A-7]; unrecognized line ignored aloud.
- No override is ever silent [INV-14]: written line + dated journal note [INV-5]; push gate [M-6] worked
  example; session scope never a file; promotion on your word; self-compaction [M-2] carries session lines.
- Profile is the ONE home, global file a thin loader [E-16]: `~/.claude/CLAUDE.md` shrinks to loader +
  bootstrap lines; which-project rule the type specimen; loader is those lines' one home [INV-13]; fork by
  scope (drift [INV-13], attic [INV-7], private-repo git home outside the fence [INV-11], project line as a
  migration note, no foreign write [INV-10]).
- Senior owns judgment [ACT-2]. Workers (tiered) [INV-69]: three tiers pulled into a list (haiku/sonnet/
  senior), checkpoints in `.live-spec/checkpoints/`. The worker contract [ACT-3] pulled into a list: write-
  ownership NARROWED to the files its brief names [INV-10]; isolated-tree copy integrated under the pen
  [T-18, INV-39]; sibling writes fence-benign [INV-11]; live setting lines ride INTO the brief verbatim
  [E-13]; the brief ARMS the worker for the workshop with the WATCHED-line duty (never a silent retry)
  [INV-23]; carries the CLOCK [INV-24]; failed acceptance escalates ONE tier with a logged line, never a
  silent retry on the same tier, never a skipped rung.
- Routing rule [INV-69]: propose the cheapest tier that can pass the brief; judgment proposes the senior
  [ACT-2]; mechanical tiers pulled into a list; economy rung moves the threshold [T-19] (full/lean/tight
  list); the proposal is advisory, override LOGGED [D-2] as proposed tier → chosen tier → why; non-goals;
  success measure [default].
- Adversarial verify [INV-46]: report a lead not evidence; "tasks completed, goal missed"; fresh checker
  briefed with SPEC sentences + paths, never the worker's summary; the EXISTS→SUBSTANTIVE→WIRED→FLOWS
  ladder pulled into a list (step 8 grep list kept); MANDATORY when the code step was delegated + surface-
  sized; checker is a worker [ACT-3].
- Brief from read files [INV-53]: read in full, three lines per file (current state · what changes · what
  must survive), back-references + source cites, row 107 origin. Halt list [INV-54] pulled into a list
  (two consecutive unexplained failures kept). Brief sized to its worker's head [INV-55]: ~300 lines, ~8
  files [default], passes PATHS never inlined file bodies.

## Wording changes worth naming (meaning intact)
- Six enumerations pulled into sub-lists (scopes, worker tiers, worker contract, routing tiers, economy
  rungs, adversarial ladder, halt list), each keeping its exact tested phrases and trailing codes.
- Dash-contrasts restated positively ("a lead, never evidence" → "a lead and no more; it never counts as
  evidence"); no scissors introduced.

Verdict: **CLEAN.** Every fact and code carried; 28/28 tested phrases section-scoped; code multiset
identical; suite 175 green. No must-fix.
