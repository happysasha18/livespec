# Prover — row 253, the chat-law hook reminds the routing law (2026-07-12, short form per INV-61)

Small delta (kind: infra, no new surface; no architecture change — M-243 anchors to INV-69, already
build-pipeline's).

- **Previous records:** 2026-07-12-row224-mirror-ban clean (0 must-fix); no unfolded rows.
- **The delta in one line:** the hook's output gains the routing line — the orchestrator seat,
  whatever model leads, orchestrates, briefs, and accepts; work routes to the cheapest sufficient
  tier (opus judgment, sonnet mechanical, haiku one-shot); workers locate their own anchors — with
  the spec's hook clause naming the fourth reminded law and M-243 red-proven on the pre-edit hook;
  the law's homes stay base rule 5 + the routing rule [INV-69], the hook only reminds (the row-141
  boundary held a third time). Composition read: the hook stays a reminder (M-133's never-side
  green); the same word moved the harness itself — the project's local settings pin the orchestrator
  model to opus for the NEXT session and the loader's bootstrap line says so (his 2026-07-12 ~00:31
  word, the Fable weekly budget at 10%; playbook 2c6d4f7) — that half lives in the personal layer,
  outside the pack's own tree, recorded here as the seam.
- **Verdict:** ready to ship — 0 must-fix, 0 should-clarify.
