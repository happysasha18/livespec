# Prover pass — regression-fence delta (T-14 + INV-19), v0.11.1 → v0.12.0 (2026-07-05, fences)

Mode: **CROSS-LINK** on the new "What already works is promised before you touch it" delta — clauses
**T-14** (a feature-doored wish that touches an already-live surface opens its spec-delta with regression
fences, one sentence per neighbouring promise that must survive, each CITING the existing clause it guards,
written before work starts; a fence with no clause behind it discovered an unwritten promise → write the
fact first, then fence; the landing report lists the fences held) and **INV-19** (every fence cites its
clause + owns a matrix row; fences listed in the landing report), plus the two Formal-index rows and the
header bump v0.11.1 → v0.12.0 (SPEC L127–137, index L538 + L557).

Seams checked (task item 4): **INV-6** (matrix rows already carry a DO and a NEVER side, the NEVER side
named "the regression fence" — is a T-14 fence a duplicate of that negative side or a distinct thing?);
**E-15/INV-15** (do fence rows derive through the node×fact method + coverage validation?); **T-13/INV-18**
(both the facet sweep and the fences "open the spec-delta" — order/composition?); the **refactor + bug
doors** (T-12/INV-16 — the parenthetical's "inherits this for free" claim); **T-7** landing report (now
lists fences — consistent with the one-line LANDING wording?); **INV-5** (an unwritten promise a fence
discovers — does it route through a door?); **A-3/A-10/C-1** (fences on an adopted, still-unverified
surface); the **prototype law** (E-17/INV-17 — no fences for sketches?); and the Formal-index symmetry.

**Prior-record check (task item 5).** `2026-07-05-facets.md` records **all 7 findings FOLDED** same sitting;
closing line "Nothing left for the queue from this pass." **Zero unfolded rows carried forward** — nothing
owed from the facet-sweep pass. (F4 there discharges at the push gate as a FULL pass; see the process note
below — the same standing pattern applies to this delta.)

**Mechanical guard.** Delta anchor↔prose symmetry exact: T-14 (prose L137 / index L538) and INV-19 (prose
L137 / index L557) each appear once per direction; no duplicate anchors; index one-liners track the prose
(minor benign overlap — both index lines say "cites its clause," but T-14 owns the fence CONCEPT and INV-19
owns its ENFORCEMENT, so it is one fact split by role, not two homes). Header bumped v0.11.1 → v0.12.0. The
new paragraph reverses no earlier-proven clause; the seams below are gaps and one model-collision, not
contradictions of proven material.

**Process note (MINOR bump under CROSS-LINK — F4 precedent).** The bump 0.11.1 → 0.12.0 is a MINOR (`0.x.0`)
step, and the prover skill requires a FULL re-prove before any MINOR bump (M-1). Exactly as resolved for
the facet-sweep F4: the CROSS-LINK delta stands, and the **M-6 push gate for this landing runs FULL**
(whole spec, all lenses) — that pass, not this one, satisfies the MINOR gate. Recorded here so the FULL
pass owed at the push is not forgotten.

**Worker verdict: 1 must-fix · 5 should-clarify · 2 worth-considering.** Per M-6 the must-fix (F1) folds
before the push, or the delta does not ship. Outcomes PENDING — the senior triages.

| # | Finding (one line) | Severity | Outcome |
|---|---|---|---|
| F1 | **Two structurally incompatible models of "fence."** INV-6 defines the regression fence as the **negative SIDE of the guarded fact's row** ("the negative side IS the regression fence" — L406), a half of an existing row. INV-19 says each fence **"owns a matrix row"** (L557) and T-14 says each fence "has (or gets) a matrix row" (L131). But the T-14 precondition is "touches a surface that **already lives**" — so the guarded clause already exists and, per INV-15/E-15, already owns a row whose negative side (per INV-6) already IS its fence. Read literally, INV-19's "owns a matrix row" makes the T-14 fence a **second row for the same fact** → two homes, and E-15's coverage check ("every node carries its negative-side rows [INV-6]", L378) counts negative sides, so it is **blind to a fence taken as its own row** — the derivation can't be walked deterministically. Fix: reword so a fence for an already-live promise is **discharged by that clause's EXISTING row (its INV-6 negative side)** — no new row; a NEW row appears ONLY on the unwritten-promise path (new fact → INV-15 row, whose own negative side is then the fence). "has (or gets)" then reads cleanly: "has" = the existing negative-side row; "gets" = the unwritten-promise branch. | must-fix · internal-conflict (consistency) | **FOLDED**: a fence earns NO new matrix row — it discharges through the cited clause's existing never-side (INV-6); a new row appears only on the unwritten-promise path (T-14/INV-19 reworded) |
| F2 | **T-13 and T-14 both claim to "open the spec-delta"; order unstated.** T-13: "drafting its spec-delta **walks a fixed sweep** of the standard facets" (L100). T-14: "its spec-delta **opens with** regression fences … written BEFORE work starts" (L128). A feature touching a live surface runs BOTH, and the sequence is never pinned. State the order and the split: the delta opens with the **fences** (T-14 — preserve existing neighbour promises), THEN walks the **facet sweep** (T-13 — author the feature's OWN visible surfaces). Name the overlap so no fact is double-covered: a facet of the touched surface the feature CHANGES is authored by T-13; one it must PRESERVE is fenced by T-14 — a fact is fenced xor re-authored, never both. | should-clarify · undefined-path (transitions) | **FOLDED**: order pinned — fences open the spec-delta BEFORE the facet sweep; touched facts split fenced-and-stays vs re-authored-as-new-law, never both |
| F3 | **The landing report "lists fences," but T-7's LANDING line is one line of three parts.** T-1..T-7: "reported to you in one plain-language **LANDING line: position on the map · what landed · what remains**" (L50) — three named parts, one line. T-14/INV-19 add "The **landing report** lists the fences held" (L134). Two names (LANDING **line** vs landing **report**) and the three-part enumeration has no slot for fences. Reconcile: either name "**fences held**" as a fourth element of the LANDING line, or state the fence list is a distinct section of the landing report separate from the one-line summary — and unify the two names. | should-clarify · boundary-issue (composition) | **FOLDED**: fences ride the wish's queue row by cited anchor ("fences: …"); the LANDING line stays one line (T-7 untouched); "landing report" name dropped |
| F4 | **A fence that discovers an unwritten promise writes a new spec fact — routing unstated (INV-5 / door / A-3).** T-14: "A fence that finds no clause behind it has discovered an unwritten promise: that promise is **written as its own spec fact first, then fenced**" (L132–133). That fact describes **existing shipped behaviour on a NEIGHBOUR surface this wish did not build** — writing it inline in the current delta is (a) a silent micro-decision unless surfaced (INV-5, L152), and (b) a promise asserted about live behaviour without reconciliation, the very thing A-3 governs. State: the discovered promise is **reconciled-from-shipped, not invented** (A-3-style, since the surface already lives), **surfaced to the human and named in the report** (INV-5), and say whether it rides the current delta or opens its own queue row when it is more than one sentence. | should-clarify · missing-scenario (state-space) | **FOLDED**: an unwritten promise reconciles from shipped truth like an adopted claim (A-3), becomes its own spec fact + row, surfaced per INV-5 — never a silent inline promise |
| F5 | **Fencing an adopted, still-UNVERIFIED neighbour claim.** On an adopted host a neighbour clause may be re-engineered-and-unverified until reconciled (A-3, L224; C-1 provenance axis, L486). A fence asserts "this **stays true** through the change" — but you cannot fence a claim you have not confirmed is true NOW. A-3 reconciles an unverified claim "at the **FIRST landing that touches its surface**" — and a feature that fences a neighbour on that surface IS that first landing. State the composition: when a fence cites an unverified (provenance = re-engineered) claim, **the touch triggers that claim's A-3 reconciliation (pin to file:line) BEFORE it is fenced** — the fence then guards a verified promise, not a hope. | should-clarify · missing-prerequisite (precondition) | **FOLDED**: touching an adoption-born unverified neighbour claim triggers its A-3 reconciliation before it can be fenced |
| F6 | **The bug/refactor parenthetical over-claims "inherits this for free."** L135–136: "(The bug door inherits this for free: a bug fix runs the full suite, where every fence already sits as a test; the refactor door likewise re-runs the whole suite.)" Honest for the **regression-CATCHING** half — running the suite exercises every INV-6 negative-side test. NOT honest for the rest of T-14/INV-19: a bug fix does not author fences in a (often absent) spec-delta, does not run the **unwritten-promise DISCOVERY** step, and its report does not "**list the fences held**." Scope the claim: bug/refactor inherit the **protection** (the suite turns red on a broken neighbour) for free; they do not inherit the fence-authoring, discovery, or report-listing, which are specific to a feature-delta touching a live surface. | should-clarify · over-general (abstraction) | **FOLDED**: reworded — bug/refactor doors inherit only the CATCHING (full-suite runs exercise every never-side); fence-authoring belongs to the feature door |
| F7 | **Prototype carve-out present for T-13, absent for T-14.** T-13 was given an explicit sketch exemption — "A **fenced prototype is NOT swept** — a sketch has no facets to promise" (L110–111). T-14 says nothing about prototypes. It is in fact covered **by construction** (a prototype has no spec-delta, and the one-way fence forbids it from being wired into a prod surface [INV-17, L186], so it never "touches a surface that already lives"), so no defect ships — but the asymmetry invites the reader to wonder. Add one parallel clause ("a prototype authors no fences — it makes no promises, E-17") or confirm-by-construction in a half-sentence, matching T-13's explicit treatment. | worth-considering · missing-scenario (state-space) | **FOLDED**: explicit now — a prototype fences nothing because it promises nothing (E-17) |
| F8 | **"Fences exist" is a narrative promise with no machine home.** INV-18's sibling clause earned a literal, scannable `[default]` tag so a prover/matrix can tell a taken default from a hole (L119). Fences have **no equivalent marker** in the spec-delta: nothing a guardrail or prover can scan to confirm a live-surface-touching feature actually authored the fences its neighbours demand. "The landing report lists the fences held" (L134) is prose a human writes; nothing checks that list is **complete** against the touched surface's neighbour clauses — the same "checkable claim, not a mood" the clause aspires to, left un-mechanized. Consider a scannable form (a fences block in the delta, or a guardrail that cross-references the surface registry's neighbour clauses [E-10, target]) so completeness is derivable, not asserted. | worth-considering · hard-to-monitor (observability) | **FOLDED (first leg)**: fences named by cited anchor in the wish's row — greppable; the guardrail that cross-checks completeness against the surface registry rides E-10 [target] with the registry itself |

Eight findings, outcomes PENDING for the senior. F1 (must-fix) folds before the M-6 push or the delta does
not ship; F4/F5 tie the fence to the door/adoption machinery; F2/F3/F6 are wording reconciliations; F7/F8
are symmetry and mechanization nits. The FULL push-gate pass owed for the MINOR bump (process note above)
is the place the whole-spec re-prove lands.

All eight findings folded same sitting by the senior (Fable) -> SPEC v0.12.0 final wording; the MINOR-under-CROSS-LINK debt discharges at the 1.0 milestone FULL audit (M-1/M-6). Nothing left for the queue from this pass.
