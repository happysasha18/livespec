# Prover short-form — row 241 spec delta, the four checks' shipping contract (2026-07-10 ~16:58)

The delta adds one contract clause (INV-97) to the standing gate family and contradicts none:
the gate-language law (the typed failure line) is CITED as the output contract rather than
restated, keeping INV-47 one home; the E-10 registry law keeps content authorship with the host
(the clause's non-goal names it); E-6's boundary (structural, never semantic) is restated as the
same boundary, one hop, cited. The honest-failure ladder closes the quiet-pass hole the prover
would otherwise flag: missing config, dead path, and lacking precondition each have a NAMED
behaviour, and the waiver lives in the config where a reader sees it. Cross-links walked: the
adopt walk gains the attachment step at code landing (attach node cited in the row, not re-specced
here); M-060/M-061's TODO status stays truthful until the code lands — the clause promises the
SHAPE, row 241 owns the landing. Architecture: pure assignment to the existing guardrails [target]
node — no new node, no new seam, the architecture prove stands down by name. Matrix M-226 pins the
contract at string level, red-proven against the pre-delta tree (4 fails → 319/319 green); the
four function-level rows land WITH the code, per the iterative-matrix rule.
