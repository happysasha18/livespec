# Prover record — 2026-07-18 — rows 382 + 403 (the queue's far tier, kin pair)

Mode: CROSS-LINK. Two small deltas add to existing surfaces of the queue / status-report family, 403
depending on 382. The whole spec was proven FULL at the 2026-07-17 MINOR gate and re-checked at the
batch-4 push (2026-07-18 ~04:17), with no structural change since — this pass proves the two new
surfaces against the named existing surfaces they seam to. Single senior seat, no worker dispatch [INV-103].

Footprint: both cross-cutting in reach (they touch the queue status vocabulary, the spec, the
architecture, and the communicator report shape) yet small in size — each adds one Formal-index
invariant, one owning-node entry, one matrix row, and shares one report-shape checker and test file.
The door is feature for both; work-kind skill/prose. The full pipeline ran on both.

## Surfaces under change

- **INV-222** (ROADMAP 382) — the queue has a far tier the runnable report stands down by name. The
  `far` status: a row kept because discarding it would lose the thought, carrying no revisit trigger and
  no plan to run, distinct from `deferred` whose trigger the queue-take re-scans every time [INV-129].
  Owning node: build-pipeline (the queue-status sibling of INV-129). Report-shape carried by communicator
  as wiring (rule 14). New pin `guardrails/check-far-tier.py:1`. Homes: templates/ROADMAP.template.md
  (status vocabulary), skills/communicator/SKILL.md (report shape), PRODUCT_SPEC.md E-3 (queue entity).
- **INV-223** (ROADMAP 403) — the far backlog surfaces itself rarely and unasked. The status report
  carries a rare line at a settings-ladder cadence default (`far-tier.surface-cadence`, at most once /
  14 days) recorded against a dated marker. Owning node: communicator (the report shape). Consumes the
  touchpoint-frame classification already declared for `far-tier-surfacing` (asynchronous, agent-pushed)
  in guardrails/touchpoints.json — the frame [INV-205] is not re-opened.

## The far-vs-deferred distinction is mechanical (the coverage check, done first)

The row's own STOP condition: if the far-vs-deferred distinction cannot be made mechanical, stop. It can.
The two are told apart by two machine-readable facts at once — the status TOKEN (`far` vs `deferred`),
and the presence of a revisit trigger. A deferred row carries a trigger the queue-take re-scans every
time [INV-129]; a far row carries none, so nothing re-scans it and it never returns on its own. The
checker `guardrails/check-far-tier.py --vocab` reads the boundary BOTH ways and reds each violation:
a far row carrying a trigger (a deferred row wearing the wrong token), and a deferred row carrying no
trigger (the re-scan left with nothing to read). Verified this run against the fixtures:

| Fixture | Expected | Result |
|---|---|---|
| a far row carrying a trigger | red | red (INV-222, row 500) |
| a deferred row carrying no trigger | red | red (INV-129, row 300) |
| a clean far + deferred set | pass | pass |

## Why the machine is a suite check, not a push gate

The status report and the feature map are chat surfaces the agent speaks at runtime; no committed report
file exists for a push gate to scan, and a chat surface is not machine-gatable [INV-83]. So the checker
rides the suite over fixtures, and the real ROADMAP's far rows (411, 381) are asserted by the test
directly. The gate chain (pre-push, gates.yml, gate-red-proofs.json) is untouched — no gate z minted.
This is the row-382/403 report-shape road the prompt named as the alternative to a new gate.

## Findings

| # | Finding | Verdict |
|---|---|---|
| F1 | INV-222's ownership bundles the queue status (build-pipeline) and the report shape (communicator). Resolved by owning the invariant at build-pipeline — the queue-status sibling of INV-129 — and carrying the report shape in communicator as wiring, the same split INV-206 uses (guardrails owns, communicator wires). | folded |
| F2 | The runnable report excludes both terminal rows and `far` rows; a `deferred` row is also not runnable until its trigger fires. The distinction the report draws is that `far` is stood down BY NAME with an offer (nothing will re-scan it), while `deferred` returns on its own. Stated in INV-222 prose and the communicator rule. | folded |
| F3 | 403 depends on 382 (the rare line surfaces the tier 382 defines). Both landed one movement; 403's cadence default rides the settings ladder, its per-person value the profile [ROADMAP 414]. | noted, no defect |

No must-fix. Open ⟨DECIDE⟩ touched by these surfaces: none.
