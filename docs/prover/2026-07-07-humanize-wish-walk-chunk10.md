# Prover cross-link — SPEC humanize: "Throwing a wish" CHUNK 10 / FINAL (2026-07-07, session 25)

CHUNK 10 (final) = Trains may roll — one pen writes · the parallel-lanes edges · Lanes are picked by a
graph, never by mood · the wish-can-end-without-landing paragraph · the closing spec line. This completes
the giant wish-walk section: all ten chunks landed, and the section is fully in the register.

Gates (whole section after splice): 157/157 tested phrases section-scoped; code multiset identical to
baseline (165 occurrences / 86 groups, zero diffs); full suite 175 green. Chunk 10 carries 22 tested
phrases and ~25 code occurrences, all preserved.

## Facts carried (all KEPT)
- Trains may roll — one pen writes [T-18]: up to three pairwise-independent build lanes, narrated on the
  departures board [INV-27]; what-may-overlap vs what-takes-the-pen pulled into two lists; document stages
  never interleave (never against another lane's half-written draft); At most three build lanes roll at
  once [default]; a FOURTH lane opens only on the human's asked word; a lane waiting for the pen SAYS so and
  names the row it waits behind; batched decision page [E-22, INV-4]; a bug cuts the line [T-9], a pen-stage
  is never cut mid-edit; milestone one train only [M-1].
- Landing purity [INV-39]: a landing commit carries exactly one row's delta, gate on a clean tree, half of
  another train never rides a landing; on a land, every lane re-checks under the fence [INV-11], landed-
  first wins, the later lanes re-verify.
- Edges [T-18] + [INV-28]: fires on independent waiting work, not across sessions / shared surface / mid-
  milestone / bug-holds-pen; non-goals; success measure [default].
- Lanes are picked by a graph, never by mood [INV-49]: the DEPENDENCY GRAPH, edges on shared surfaces;
  INTEGRATION collisions pre-roll with order DECLARED at claim, first-declared lands first [INV-39]; tiny
  rows ride serial; narrated at opening [INV-27].
- End-without-landing [T-8]: declined / deferred / superseded pulled into a list; declining is not a black
  hole, listed rows declined BY NAME or RETURNED to the queue as its own row again, a superseded wish never
  dies by pointer [INV-1].
- Closer: what the wishes grow is the spec (SPEC.md) [E-4].

Verdict for chunk 10: **CLEAN.** The whole "Throwing a wish" section is now humanized; whole-section gates
green (157 phrases, 165 code occurrences, suite 175).
