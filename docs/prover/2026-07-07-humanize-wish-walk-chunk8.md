# Prover cross-link — SPEC humanize: "Throwing a wish" CHUNK 8 (2026-07-07, session 25)

CHUNK 8 = four bold-leads: Verify-by-deed walks the visit and watches the feel · A taste choice made
without asking is TOLD, never confirmed · The smallest sample is judged before the full artifact · A
rejected artifact reopens its SOURCE.

Gates (whole section after splice): 157/157 tested phrases section-scoped; code multiset identical to
baseline (165 occurrences, zero diffs); full suite 175 green. Chunk 8 carries 6 tested phrases and 10
code occurrences (incl. two literal `[default]` tags), all preserved.

## Facts carried (all KEPT)
- Verify-by-deed [INV-30]: named VISITOR WALK (first visit, return, another door, where-am-I, exits) + a
  FEEL pass judged against the approved prototype [E-17]; findings become rows or red; checklist in the
  build-pipeline step-8 product cell [E-12, INV-22]; runs in the medium's form; feel lens a partial skill,
  never forced on prose.
- A taste choice TOLD, never confirmed [INV-31]: small calls written with the `[default]` tag [INV-18]; the
  eight-untold-choices failure; report names each choice, no confirmation, silence is consent; `[default]`
  tags keep them findable; his 2026-07-06 word paraphrased.
- The smallest sample is judged first [INV-62]: build STOPS at the cheapest judgeable sample; five media-
  packs origin; kin split with the mockup-first entry condition [INV-43].
- A rejected artifact reopens its SOURCE [INV-63]: correct the source first, rebuild FROM it; line-by-line
  patching against an unchanged source is the five-round trap, banned (promoter case).
- One Russian quote rendered as a plain-English paraphrase with attribution; no new quotation introduced.

Verdict for chunk 8: **CLEAN.** Whole-section gates green.
