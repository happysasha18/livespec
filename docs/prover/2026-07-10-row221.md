# Prover record — CROSS-LINK on F-catchup (2026-07-10, session 32, row 221)

**Mode:** CROSS-LINK (the new surface's seams against the named existing surfaces).
**Prover skill version:** product-prover 1.0.0.
**Under review:** the new scenario "Bringing an adopted host up to the current pack (catch-up)"
[feature: F-catchup], anchors A-11, INV-89, INV-90, in PRODUCT_SPEC.md (lines 1139-1164), citing
A-0..A-10, INV-7, E-9, INV-86, INV-10, A-9, A-5, M-7.

**Previous record checked for unfolded rows:** `2026-07-10-onboarding-crosslink.md` — the latest
table-bearing prover record (CROSS-LINK on F-onboarding, session 32 morning). All six rows (PF-1..PF-6)
read FOLDED or DISCHARGED; no row rides unfolded into this pass. The later `2026-07-10-m1-audit.md`
(the five-angle pre-1.0 gate: 0 must-fix) and `2026-07-10-release.md` (the 1.0.0 version-alignment
short-form) are both clean with no open must-fix carried forward.

## Seams walked

Adoption phases (A-0..A-10, F-adoption) · the pair (F-pair, INV-85/86, INV-10/E-11) · the settings
ladder (E-13, host profile E-8) · version homes (M-7, installed-set record A-7) · the attic (INV-7,
E-9) · the wish/door machinery (T-12, T-17, INV-16, docs-only door) · the inbox law (T-10, E-11) · the
show rule (INV-67) · the settings catalog + completeness (INV-87) · the migration/promotion note law
(A-10 provenance, E-16). Procedure lenses applied: states-with-no-exit, actor per transition,
safety+liveness of the walk, atomicity of the merge step, interruption mid-execute resumed by a cold
session.

## What's working

The facet-sweep seam is handled cleanly and by name: the walk "creates no visible product surface, so
the standard facet sweep does not apply, and the plan document opens by the ordinary show rule
[INV-67]" — the one seam most easily missed on a procedure that isn't a product surface. The pair
routing correctly makes the cross-half handling asymmetric (own repo executes, the other half gets one
inbox wish), which is exactly what INV-86's "read-only on the other half save one inbox file" demands
rather than a symmetric queue split. The exit conditions are stated: a walk with nothing to do reports
and ends (phase 2), and INV-89's skip-as-done gives idempotent progress with no dead-end state.

## Findings

| id | severity | spec sentence at issue | seam / lens | finding | verdict |
|---|---|---|---|---|---|
| CF-1 | should-clarify | "The owner's named gates stay in force: the cruft-sweep delete gate [A-9] and the version-control gate [A-5]." / phase 3 "Execute, preserving facts." / phase 4 "The host's own gates run, including the test suite where one exists." | Adoption A-5 seam + atomicity/rollback/liveness lens | Adoption runs the VCS gate FIRST "before touching or moving anything, so the whole run stays reversible" [A-5]. Catch-up moves, merges, and retires many files in phase 3 but states no equivalent clean baseline commit immediately before execute — A-5 was discharged at first adoption (git exists), which does not guarantee a clean revert point for THIS walk. Three linked holes: (a) no single-commit rollback point before execute; (b) no stated recovery when phase-4 verify (the suite) goes red after the merge; (c) an interrupted-mid-execute walk resumed by a cold session relies on INV-89 per-step idempotency for correctness, but nothing marks "a walk is in progress" or says where the plan document lives during execute (phase 4 rests it in the attic only at the end), so the cold session cannot tell resume-me from start-fresh and may re-plan and re-gate the owner. Attic-never-delete keeps the data safe; the gap is clean reversibility and resume-detection. State: a pristine baseline commit at execute's start; the plan lives in `.live-spec/adopt/` from phase 2 with an in-progress marker a cold session reads; a red phase-4 verify parks for the owner against that baseline. | FOLDED — phase 3 now opens with a clean-tree baseline commit [A-5] and runs under the checkpoint discipline (checkpoint names the plan + per-step state; an interrupted walk resumes under the already-given gate); phase 4: a red gate keeps the walk open, the checkpoint carries the red state; phase 2: the plan lives in `.live-spec/adopt/` through the whole walk [A-8]. |
| CF-2 | should-clarify | "A file with differing content is reconciled by where each line's home sits under the settings ladder: a host-profile line whose home is the personal profile under the current ladder moves up, and a host-scoped line stays." [INV-89] | settings ladder (E-13) seam + over-general (abstraction) lens | INV-89 opens "merges file by file" for ANY old-and-new file, then gives one reconcile rule — ladder homes — that only makes sense for settings/profile files whose lines carry ladder scopes. A differing non-settings file (a template, a guide, a doc both forms carry, a diverged README) has no "line's home under the settings ladder," so the primary rule is undefined for it; only the catch-all "Any remaining conflict rides the plan to the owner's gate" rescues it. As written the ladder rule reads as THE reconcile method for all differing files. Say plainly: the ladder-home reconcile applies to settings-carrying files (the profiles); every other differing file rides the plan to the owner's gate. | FOLDED — INV-89 now scopes the ladder reconcile to profile files; any other differing file, and any remaining conflict, rides the plan to the owner's gate. |
| CF-3 | should-clarify | "a host-profile line whose home is the personal profile under the current ladder moves up" [INV-89] · "Steps that touch the machine's shared homes, the installed-skills folder and the personal profile, run once per machine." | host profile E-8 / E-16 seam + boundary (composition) lens | Moving a host-profile line "up" to the personal profile is a write to a machine-shared, cross-project file performed inside ONE host's catch-up walk — it changes what applies to every other project on the machine. The plan gate lists "every record that reformats," so the owner sees the move, but two exposures remain unstated: the once-per-machine clause only offers a directory-exists / record-reads-current check, not a content-write fence, so two hosts' catch-up walks (or a catch-up beside another machine-level session) can both write the personal profile with no concurrency rule. State that a line move-up is an owner-gated plan item AND that the personal-profile write takes the machine-level once-per-machine fence, not just an already-done check. | FOLDED — the move-up is a plan item by construction (every record that reformats), and the personal-profile write now follows the promotion law: re-read immediately before appending [E-16], the file's git home the recovery net. |
| CF-4 | should-clarify | "A host that adopted under its own names, such as a product spec named SPEC.md, keeps its names, recorded as one host-profile line (`spec.file: SPEC.md`)" [INV-90] | settings catalog (E-13, INV-87) seam + boundary lens | `spec.file` is a new host-profile key introduced here. INV-87 requires every card row to trace to a marked package-defaults table row OR a recorded profile line, and each card row to render a plain-words name plus a change-line. A bare recorded `spec.file: SPEC.md` traces to itself for completeness, but with no catalog row it has no source for its plain-words name and change-line, so the settings card would show a raw key or the completeness check has nothing to render it from. Add `spec.file` to the package-defaults catalog with its name + change-line, marked card-visible or internal; if internal, say so, so the card omits it deliberately. | FOLDED — spec now requires a `spec.file` row in the package-defaults table [E-13, INV-87]; the row itself lands with this walk's code step in the base skill (marked internal — recorded by the walk, spoken change still possible). |
| CF-5 | should-clarify | phase 1 "The session reads the host's installed-set record ... The work list is the difference between what the host carries and what the current package expects." vs INV-89 "the new record is read from the version lines of the skills actually installed on the machine" and "the tree is the truth when they disagree." | version homes (M-7, A-7) seam + consistency lens | Phase 1 computes the delta from the recorded installed-set; INV-89 says the truth is re-read from the versions actually installed on disk and retires the recorded set to the attic. When the record is stale (skills updated on disk without the record catching up — the exact drift A-7's freshness re-check guards), the two disagree and the delta is computed from the wrong base. Name the authoritative source: the installed-on-disk version lines win, and the recorded set is only a starting hint. | FOLDED — INV-89 closes with: the skills installed on disk are the authoritative set, a stale record is corrected from them. |
| CF-6 | should-clarify | "A pair half that carries no `.live-spec` records of its own is an under-attached host; its walk opens with the missing attach phases, the record-creation steps, before the catch-up items." | Adoption phases (A-0..A-10) seam + boundary lens | "The record-creation steps" under-scopes what a never-adopted host owes. Adoption is more than creating records: it re-engineers existing documents into live-spec shapes [A-3], and it gives every unbacked live surface a promote / quarantine / attic verdict [A-10] — a de-facto prototype is "the most common residue in an adopted host." Reducing under-attached to "record-creation steps" silently drops the A-10 surface verdict and the A-3 doc reconcile for a half that may carry both. Name whether an under-attached half runs the FULL adoption sequence (A-0..A-10) or a named subset, and if a subset, why A-10/A-3 are safe to skip for a carved-out pair half. | FOLDED — the under-attached pair half runs the full adoption for its repo [A-0..A-10] before the catch-up items. |
| CF-7 | note | "a fact leaves its home only by moving to a better one" [INV-90]; the walk re-homes settings lines (CF-3) | onboarding card (F-onboarding) seam | After catch-up re-homes settings lines, the owner sees the change only in the plan gate; the settings card does not auto-re-render at the walk's end (unlike founding and adoption's orient, which render it). This is acceptable — the card answers on ask from the current truth, so a later ask reflects the new homes — but worth one sentence so the seam is deliberate rather than forgotten. | FOLDED — one sentence added at INV-90: a re-homed line shows on the settings card at its next render, under the card's render-moment rule [INV-87]. |

## Coverage tables

CRUD and authorization tables are N/A: this is a single-owner local procedure with no user-mutated
persistent entities and no role model — the only actors are the session and the owner (gate), stated
throughout. The property table that applies:

Invariants per phase of the catch-up walk:

| Phase | Invariants stated | Invariants missing |
|---|---|---|
| Orient | reads installed-set + tree + package VERSION/journal; tree is truth on disagreement | authoritative source when record and installed-on-disk disagree (CF-5) |
| Plan | one plan document lists every move/merge/retire/rename/conflict; owner's word before any file moves; empty walk ends | where the plan persists during execute; in-progress marker for a cold-session resume (CF-1) |
| Execute | half-done-state law [INV-89] + preserve-and-re-home [INV-90]; skip-as-done; new form never overwritten; directory never nested in its replacement; attic-never-delete | pre-execute reversibility baseline (CF-1); reconcile rule for non-settings differing files (CF-2); machine-shared personal-profile write fence (CF-3) |
| Verify + re-record | host gates + suite run; installed-set re-recorded in current format; one journal chapter; plan + superseded files rest in attic | recovery path on a red verify (CF-1) |

## Verdict at review

Structurally sound and the highest-risk seam (the facet sweep on a non-surface procedure) is handled
by name. No must-fix: attic-never-delete keeps every fact safe even through an interrupted or a bad
walk, so the open items are clarity of reversibility, resume, and a few undefined edges — not a broken
build. Seven findings, all should-clarify or note, listed above for the senior's fold. All seven verdicts filled by the senior the same hour: every finding FOLDED into the spec text (see the verdict column); the architecture lens re-ran after the folds — six checks pass, no new node, F-catchup rides the attach node (coverage row + runtime-view flow added).

## Addendum (~12:25) — two owner words folded after the review

Alexander's ~12:10 and ~12:21 words extended the section after this review ran; both are one-hop
extensions of already-reviewed laws, recorded here short-form so the record matches the spec at
landing: INV-91 (a release owing host actions ships a dated MIGRATION.md chapter; the walk is
version-chained, oldest first, one plan per chain — leans on INV-89's resumability, reviewed above)
and INV-92 (the walk's before-and-after self-test: pre/post inventory of document fingerprints +
anchor multiset + suite verdict, unplanned difference blocks verify; the baseline commit reviewed at
CF-1 becomes the NAMED restore point with its restore command in the plan). No new seam: both ride
the plan/gate/checkpoint machinery already walked; matrix rows M-220/M-221 own them.

## Addendum (~13:06) — the cold dry-read cycle at verify

Verify-by-deed ran twice: a cold opus session role-playing the tlvphoto window (guides only, the
owner's trigger sentence, no spec access). First read: verdict NO — one procedural hole and two
weak spots. The hole: with an old-format installed-set record (commit pins, no readable version)
the guide never said where the migration-chapter chain STARTS. Folded into both homes the same
hour — the spec's INV-91 paragraph and MIGRATION.md orient step 3 now state: a record with no
readable version starts the chain at the earliest chapter, INV-89 making over-application
harmless; red-proven first by `test_versionless_record_starts_at_earliest_chapter` (10th test),
matrix row M-220 extended. The weak spots, both folded as guide sentences citing standing laws:
the pair tension (a verify gate red only because the other half's catch-up has not landed — the
walk stays open per the walk law, the block rides reports as the pair law's dated debt line,
INV-56/INV-27) and the machine-global loader sweep (the 1.0.0 chapter's once-per-machine step now
names `~/.claude/CLAUDE.md` per E-16, with its already-done check). Second cold read ordered after
the folds; its verdict closes this record.

Second cold read (~13:15, fresh opus, same scenario planted harder — half-renamed state dir,
commit-pin record, foreign spec name): VERDICT YES, zero holes; every planted trap mapped to an
explicit guide clause, the versionless record seated the chain at the earliest chapter exactly as
the folded sentence intends. Its named weakest spot is an owner-scope call (does the ask span the
pair) and a discovery fact (where the engine repo lives), both outside the package's duty. Record
CLOSED.
