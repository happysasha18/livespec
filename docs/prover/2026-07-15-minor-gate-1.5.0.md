# MINOR gate 1.5.0 — adversarial whole-spec + architecture pass

Reviewer: independent formal-methods pass (did not author any of this). Clock 08:37 15.07.2026.
Opening hypothesis under test: the recent changes introduced a contradiction, an unowned fact, or a fact with no test.
Delta under hardest scrutiny: INV-154 (prover ⇄ design-review bounded loop), its ARCHITECTURE seam + node,
M-299 / M-300, build-pipeline step-2, the design-reviewer README/LICENSE + product-prover younger-sibling section.

Scope read: whole PRODUCT_SPEC.md invariant band (INV-140..154 in full, plus INV-59, INV-125, INV-126, INV-130, INV-136, INV-138, INV-145, INV-150, INV-152), the M-1 milestone-gate enumeration, the whole ARCHITECTURE.md, the design-reviewer matrix block (M-283, M-284, M-299, M-300).

| # | finding | kind | ground (quoted) | suggested fold | delta? | outcome |
|---|---|---|---|---|---|---|
| 1 | INV-154's resting-state taxonomy is not exhaustive over reachable outcomes. A round whose design review surfaces a **confident** finding (an undeclared same-kind grouping written as a recommendation and queued for a taste call, per INV-142) but no open question fits **neither** named resting state: it is not CONVERGE because a grouping "beyond those already declared or decided" was surfaced, and it is not WAIT because a recommendation is not "a question the human has not yet answered." Under the literal CONVERGE test the loop can therefore **never converge** while any confident recommendation stands unconverted to a class sentence — and since a design-review pass's ordinary output is recommendations (asks are the capped minority, ≤3), this directly contradicts the same clause's "Convergence is the ordinary case." | defect | INV-154: "The loop has two resting states, each named on the record. It CONVERGES when a round's design review surfaces no grouping and no difference beyond those already declared or decided … It WAITS when the only findings a round surfaces are questions the human has not yet answered [INV-142]." + "Convergence is the ordinary case, and the cap is what keeps the loop live." Cross-checked identical in ARCHITECTURE (design-reviewer node / loop seam), build-pipeline SKILL.md:234-237, and skills/design-reviewer/SKILL.md:81 — the hole is carried in all four homes. INV-142: "A confident finding is written as a recommendation, so it queues for a taste call and never blocks." skills/design-reviewer/SKILL.md:89 makes it an outcome value: "`recommended` (a confident finding queued for a taste call)". | Redefine CONVERGE against *new, unhandled* groupings, not against "declared or decided": e.g. "It CONVERGES when a round surfaces no grouping or difference beyond those already declared, decided, **or already surfaced-and-queued as a recommendation on a prior round**" — so a standing confident recommendation is a settled output, not a permanent bar to convergence. Fold the same amendment into the ARCHITECTURE node/seam, build-pipeline step-2, and the design-reviewer skill so the four homes stay one fact. | delta | FOLDED (three resting states; a standing recommendation does not bar convergence) |
| 2 | The ARCHITECTURE loop seam states counter semantics that INV-154 never states. The seam adds "per prove pass, reset when the pass opens" — a reset scope for the round counter. INV-154 states only "three progressing rounds by default" and "counts its own progressing rounds and stops at the cap"; it never states when the counter resets. This is either an architecture fact with no spec backing (fails the every-fact-has-spec-backing lens) or a genuine hole in INV-154's liveness guarantee: without a stated reset scope the cap is under-defined, and if each asynchronous human answer re-enters through build-pipeline step-2 as a fresh pass that "resets when the pass opens," the three-round cap can be re-armed indefinitely and never bind. | recommendation | ARCHITECTURE.md seam "design review → re-prove (the loop)": "bounded at three progressing rounds counted by the design review (**per prove pass, reset when the pass opens**; a waiting round pauses rather than advances the count)". No matching phrase in INV-154 (line 442) or the design-reviewer node line. | State the counter's reset scope in INV-154 itself (the spec is the home of the fact), then let the seam cite it rather than originate it; and say explicitly whether an asynchronous re-entry via build-pipeline step-2 opens a new pass (counter resets) or continues the open one (counter persists), since that decides whether the cap actually bounds the cross-session loop. | delta | FOLDED (counter reset scope now stated in INV-154: per pass, persists across async answers, resets when a fresh pass opens) |
| 3 | INV-154 tags [INV-150] to justify "The design review is the net that counts its own progressing rounds and stops at the cap." INV-150 defines "net" as an *enforcement* net for a *declared cross-cutting law* (mechanical gate / prover station / design-review recommendation). The loop's round-cap is not a declared cross-cutting law, so the [INV-150] citation reuses "net" in a second, unrelated sense. Loose cross-reference, not a contradiction. | recommendation | INV-154: "The design review is the net that counts its own progressing rounds and stops at the cap [INV-150]". INV-150 governs "every declared cross-cutting law names the net that enforces it". | Drop the [INV-150] tag here or replace it with a pointer to the design-reviewer node, which is the actual owner of "the loop's round-count and cap" (ARCHITECTURE line 61). | delta | FOLDED (the [INV-150] "net" tag dropped from the clause) |
| 4 | INV-154 cites [INV-130] for "the held question resumes the loop when the human later answers it." The answered-closes-forever / recorded-answers-searched-before-any-ask behaviour is INV-59's; INV-130 is the two-withdrawal → recommended-default convergence bound. The citation points at the wrong convergence law. (The same [INV-130] usage exists in INV-142, which shipped in 1.3.0 — that instance is pre-existing; the INV-154 instance is new.) | recommendation | INV-154: "the held question resumes the loop when the human later answers it [INV-130]". INV-59 (index): "no question asked twice: recorded answers searched before any ask … an already-answered question is a defect; dialogues converge (answered = closed + harvested same session)". INV-130 (index): "a withdrawn decision converges: after two withdrawals the recommended option is taken as a surfaced `[default]`". | Cite [INV-59] for the answered-closes / resume-on-answer behaviour; keep [INV-130] only if the intent is the withdrawal-convergence bound (it does not appear to be). | delta | FOLDED ([INV-130] changed to [INV-59] for the answered-resumes citation) |
| 5 | The M-1 milestone-gate design-review bullet predates INV-154 and describes the pass as single-shot and terminating, with no mention of the loop or the confirmed-grouping re-entry now mandated by INV-154. A reader executing the gate list top-to-bottom (design review → matrix audit → …) would run one design-review pass and move on, never looping. Reconciled in practice by the WAIT resting state (at a gate the human rarely answers synchronously, so the pass waits and the lane proceeds), so it is incompleteness rather than a contradiction — but the gate bullet no longer reads as the whole story. | recommendation | PRODUCT_SPEC.md M-1 gate, line 739: "the design review [INV-141] on the re-proven spec: it runs in full here … **Its outcome folds and its dated design-review record lands beside the prover's**". No mention of INV-154 / the loop / re-entry. | Add one clause to the gate bullet noting that a confirmed grouping re-enters the prove step under INV-154, bounded at the cap, and that at the gate the loop typically rests in WAIT without holding the milestone. | delta | FOLDED (the M-1 gate bullet now names the loop re-entry and the WAIT rest) |

## Checks that passed (no finding)

- **Traceability.** INV-154 → M-299, and M-299 sits inside `### [node: design-reviewer]` (TEST_MATRIX.md line 505 block, row 511). M-300 (design-review shopfront: README+LICENSE + product-prover cross-reference) also sits under that node (row 512) and its asserted artifacts exist: `skills/design-reviewer/README.md` + `LICENSE` present; `skills/product-prover/README.md:134` carries the "Its younger sibling" section naming `design-reviewer`.
- **Single-node ownership.** INV-154 is owned by exactly one architecture node, design-reviewer (ARCHITECTURE line 61), which also carries the loop's round-count and cap. INV-141/INV-142 sit in the same node; INV-140 stays with product-prover. No double ownership.
- **Seam completeness.** The new seam "design review → re-prove (the loop)" names what crosses (a human-accepted class sentence [INV-125] or decided sentence [INV-59] re-entering the prover) and its format owner (spec-author — the class / decided sentence). Passes the "every seam names what crosses it and who owns the format" lens.
- **INV-154 vs INV-141 (never holds a landing).** Consistent: "Surfacing at the cap holds no landing … the landing proceeds with the unsettled groupings recorded."
- **INV-154 vs INV-150 (property routing).** No contradiction in the routing logic itself (only the loose "net" citation, finding 3).
- **INV-154 vs INV-142 (held unanswered ask) and INV-130 (recorded-state-before-re-ask).** Behaviourally consistent (a WAIT resumes on the human's answer); only the citation precision is flagged (finding 4).
- **INV-154 homes all present.** prove-step clause (line 442) · build-pipeline step-2 re-entry (SKILL.md:234-237) · design-review loop seam (ARCHITECTURE line 95) · matrix row (M-299) · guard test (`test_fixed_point_loop_bounded_and_nonblocking`, M-299). No orphan.
- **No ⟨DECIDE⟩ marked resolved but live; no INV referenced-but-missing** among INV-154's citations (INV-140, INV-141, INV-142, INV-125, INV-59, INV-130, INV-150 all exist and are indexed).

## Counts

Defects: 1 (delta-related). Recommendations: 4 (all delta-related). Pre-existing (outside the delta): 0 blocking — the INV-130 mis-citation twin in INV-142 is noted as pre-existing but is not a separate blocking defect.

Delta-related: 5 of 5. The single blocking item under the gate is finding 1.
