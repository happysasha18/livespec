# Prover record — INV-174 + INV-175, the local inbox arm and the config-health gate (2026-07-16, FEATURE-FIT)

Prover skill version: 1.1.4. Mode: FEATURE-FIT on one feature's delta (task 11 — the
co-located concurrency fixes).

## The delta
INV-174 — the inbox's local co-located arm: a same-filesystem depositor writes its one new file
and stops (no staging, no commit, no push); the assigned session's sweep commits the harvest.
INV-175 — the installed gate is the source gate: `check-config-health.sh` reds a missing or
drifted installed hook (suite + push-gate m; CI skips by name); the commit fence stops a file
both staged and carrying unstaged edits.

## Fit walk
- **Arrival** — the local arm fires the moment a co-located window deposits; backed by the
  amended inbox README and the INV-174 clause beside the remote arm (INV-112). The health gate
  fires on every suite run and every push. HOLDS.
- **Every next-step** — deposit → durable file → sweep commits (T-10); missing/drifted hook →
  red naming `guardrails/install.sh`; staged-with-unstaged file → fence stop naming the files.
  All exits stated and tested. HOLDS.
- **Second-sibling question (INV-169)** — the local arm is a THIRD member of an already-declared
  kind (the inbox's deposit arms: remote INV-112, stranger INV-146); the kind is declared, so
  the uniformity check governs (INV-125) rather than the design review. Uniformity read: all
  three arms share "one new file, source named, never edits an existing file, one writer to the
  index"; they differ only in who commits — stated per arm. HOLDS.
- **Quantifier re-verify (INV-170, first live run)** — the fence's benign-case sentence
  ("one fresh inbox file is the fence's expected benign case", INV-112 block) now reads against
  the grown arm set: the local arm narrows it to an UNTRACKED fresh file, stated in INV-174's
  closing sentence. The standing word in NEXT_STEPS ("a neighbour committing one inbox file is
  allowed") is falsified by this landing and is updated in the same landing. HOLDS after sweep.
- **Worked instance** — the health gate's first run on this repo found both installed hooks
  drifted (pre-push missing gates k and l); red-first proof came from the live tree itself,
  then `guardrails/install.sh` restored the match. HOLDS by deed.
- **Cross-entry** — a repo with no guardrails/ sources (a host before adoption) loops zero
  hooks and passes the health check trivially; the ratchet kit (INV-172) is what brings the
  sources in. Stated. HOLDS.

## Disposition
| Finding | Kind | Disposition |
|---|---|---|
| task 11 (local inbox-notify collision on the shared index) | feature | landed — INV-174, M-329 |
| task 11 (config-health gate; hooks believed wired but stale) | feature | landed — INV-175, M-330, gate m |
| task 11 (detect mid-session another window touching your files) | feature | landed — the fence's staged-vs-unstaged arm (INV-175) |

Verdict: the delta FITS; no open questions ride.
