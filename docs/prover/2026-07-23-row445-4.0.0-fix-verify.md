# Prover record — row 445 — pre-push re-verify of the 4.0.0 fix wave

PROVER-RECORD

Prover skill version: product-prover 4.0.0 (live-spec-base v4.0.0), the same lens set as the
2026-07-22 full audit, re-armed at the version bump. Mode: finding-scoped pre-push re-check — not a
fresh FULL prove. This pass re-verifies that the eight MUST-FIX findings of the 2026-07-22 clean-context
full audit (`docs/prover/2026-07-22-row445-4.0.0-full-audit.md`) are folded in the CURRENT root
documents, spot-checks the two restoration waves, and confirms the seven SHOULD-FIX items are visibly
tracked, not silently lost. The reviewing seat is a fresh context with no part in the fix wave that
produced these edits (INV-237). Every verdict below was checked against the files on disk by grep/read
at the cited location, never against the fix wave's own prose.

Subject: `/Users/sashaabramovich/live-spec/PRODUCT_SPEC.md` (590,695 bytes, matching the fix wave's
final-restoration byte count) and `ARCHITECTURE.md` (104,584 bytes, "Last reconciled 2026-07-23").
Companion source for the restoration checks: `prototype/2026-07-22-spec-format/assembly/DELTA.md`.

## Verdict

**PUSH-READY (prover dimension).** All eight MUST-FIX findings are folded in the current root
PRODUCT_SPEC.md and ARCHITECTURE.md; each was confirmed at its specific location, not taken on the fix
wave's word. Both restoration waves land: the zones-may-overlap decision is present at R196.19–.20, and
the three sampled final-wave restorations are each present in the root doc. The seven SHOULD-FIX items
are all accounted for — one reads resolved, six stand open as the queue rows the audit predicted; none
was silently dropped.

Counts: **8 / 8 MUST-FIX folded · 2 / 2 restoration waves confirmed · 7 / 7 SHOULD-FIX tracked
(1 resolved, 6 open → queue rows per INV-114).**

One standing precondition this record does not discharge: the M-6 push gate also stands on the suite
log's own N/N green, owned by the parallel re-pin sweep (LANDING-CHECKLIST step 7). This record clears
the prover half of the gate; the suite-green half is the re-pin sweep's to report. Push proceeds only
when both are green.

## The eight MUST-FIX findings — re-verify

Format per finding: the audit's claim, then the current-text evidence line with its location, then the
verdict.

---
**F1 — false GAP at R160.3 (critical undecidable while R9 decides it).** FIXED.

Audit claim: a `[GAP]` declared a bug's critical priority undecidable while R9.4 states the three
conditions. Current text (PRODUCT_SPEC.md:3411): "The system *shall* order waiting bugs critical-first —
a bug is critical *when* the shipped product is broken for its user, the same three conditions the
priority mark carries [INV-12] — and bugs of equal priority by arrival." The false GAP line is deleted;
the criterion now cites INV-12's conditions inline. Grep for "which bug counts as critical is
undecidable" / "names no judge or measure for a bug" returns zero. FIXED.

---
**F2 — cross-link-mode glossary entry defined the opposite of R66.** FIXED.

Audit claim: the glossary defined cross-link mode as a whole-document pass, the reverse of R66. Current
glossary (PRODUCT_SPEC.md:55): "**cross-link mode** — the prover's focused pass at a surface add, scoped
to the new surface's seams, carrying one mandatory whole-document step: it sweeps the document for
enumerations and universal quantifiers and re-verifies each against the surface set including the
newcomer." This is R66's own meaning (R66 Context/criterion at :1537, :1545). FIXED.

---
**F3 — glossary claimed one name for the seat while the body kept two.** FIXED.

Audit claim: "senior agent" appeared 34 times; the "keeps the one name seat throughout" claim was false.
Current count: `grep -c "senior agent"` returns 1, and that single occurrence is the seat glossary
entry's own alias recital (PRODUCT_SPEC.md:194: "…the source also names this actor the senior, the
senior agent, and the orchestrator, and this document keeps the one name seat throughout"). The body's
34 occurrences are swept to "the seat"; the duplicate `senior agent` glossary entry is gone. The
one-name claim is now true. FIXED.

---
**F4 — description claimed to live in a retired Formal-index field.** FIXED.

Audit claim: R191.4, its Context, R191.7, and the `description field` glossary entry claimed a dedicated
Formal-index field the migration removed. Current text: glossary entry (PRODUCT_SPEC.md:68) now reads
"the authored home of a code's plain statement: the criterion the code trails carries the code's rule …
the generated code-to-location table carries locations only"; R191.4 (:4234) and R191.7 (:4240) are
rewritten to the INV-271 home, both carrying `[INV-239, INV-271]`; the format-law requirement R278
(:6349–6351) restates it. `grep "Formal index" PRODUCT_SPEC.md` returns zero. FIXED.

---
**F5 — intro/glossary described a document shape this document does not have.** FIXED.

Audit claim: the intro promised "each section states one scenario" and "ten assembled sections"; the
scenario glossary said "the spec's body is a list of scenarios." Current text: line 11 reads "every
domain noun the twelve assembled sections introduce"; the scenario glossary entry (:193) now reads "a
requirement whose heading carries a `[feature: F-...]` tag … the spec's body is a list of requirements."
The genre is described as it is. FIXED.

---
**F6 — marker conventions the header declared were not the ones the body practiced.** FIXED.

Audit claim: zero line-of-its-own `[target]` markers while planned work rode a bare in-bracket `target`
token (R102.2/R102.4); six feature codes rode trailing brackets; only 4 features were heading-tagged.
Current text: nine standalone `[target]` markers on their own lines
(`grep -E "^\s*\[target\]\s*$"` = 9); no bare lowercase `target` token survives in a trailing anchor
(the sole `[target: …]` hit at :5585 is the restored design-sync wiring note, a labelled form, not the
bad token); 28 requirement headings carry `[feature: F-...]` tags; `F-wish` now sits on its owning
heading (Requirement 4, :301); and no F-code appears off a `##` heading (the off-heading grep returns
empty). The 16 architecture features all resolve to a tagged heading. FIXED.

---
**F7 — ARCHITECTURE.md still described the retired spec shape.** FIXED (with one residual noted below).

Audit claim: ARCHITECTURE.md's node-ownership preamble named "PRODUCT_SPEC.md's Formal index," the D-x
rows pointed at the Formal index, it said "ten skills," omitted text-audit, and was reconciled 2026-07-17.
Current text: the ownership preamble (ARCHITECTURE.md:37) now reads "a code anchored on a criterion in
PRODUCT_SPEC.md, located through the generated code-to-location table"; DECISIONS.md is referenced (2
hits); line 29 reads "eleven skills"; text-audit is homed as a node row (:64) pinned to
`skills/text-audit/SKILL.md`, carrying INV-266..268; the header (:4) reads "Last reconciled with the
spec: 2026-07-23." All four named sub-items fixed. FIXED.

Residual (not part of F7's named set; flagged for the gate-retire sweep, not a re-open): five
descriptive mentions of "Formal-index" remain in ARCHITECTURE.md at :46, :54 (×3), and :179 — all
naming `check-description-field.py` as "the Formal-index non-empty description-field gate." Per
INV-271 / R278.7 that gate retires at the conversion delivery, so these describe a gate slated to go.
They are not the ownership or decisions references F7 named (both repointed), and belong to the same
gate-retire step the re-pin sweep owns. Worth a one-line note when that gate is retired so the
architecture does not carry a dead gate description.

---
**F8 — the format-law requirements the delta promised were absent.** FIXED.

Audit claim: `grep INV-250..271` returned 0 hits; the highest invariant was INV-249; the six format-law
requirements were missing. Current text: all 22 codes INV-250..271 are present (spot count: INV-250 ×4,
INV-251 ×4, INV-266 ×3, INV-267 ×5, INV-268 ×2, INV-270 ×3, INV-271 ×6, and the rest); six new
requirements are homed — Requirement 277 (:6272), 278 (:6329), 279 (:6355), 280 (:6393), 281 (:6420),
282 (:6450) — the format-laws unit, inserted before the Reference tail. FIXED.

## Restoration wave (a) — zones may overlap

CONFIRMED. The decision is assembled at R196.19–.20 (PRODUCT_SPEC.md:4467–4468): "19. the system *shall*
let two agents' zones overlap … and *shall* force no agent to carve a disjoint zone. [INV-197, INV-225]"
and "20. the system *shall* build no uniqueness check over zone claims, the wrong referral alone earning
a name. [INV-225]" The Reference table homes INV-197 at R196.19 (:6722). Both the overlap-is-legal
decision and its consequence (no uniqueness check built) are present with the two named invariants.

## Restoration wave (b) — three sampled final-wave claims

Three of DELTA's fourteen final-restoration claims, confirmed in the root doc:

- **Item 2 — inbox-deposit fence carve-out.** Present at PRODUCT_SPEC.md:5791: "*when* a push's diff is
  exactly one new inbox file, the system *shall* have it owe the fence and no re-check record, more
  riding the full gate. [INV-11, INV-112]" (with its R33.7 twin at :5692). CONFIRMED.
- **Item 10 — tight-rung re-apply.** Present at PRODUCT_SPEC.md:4959: "the system reverts the batch to
  its last green base and re-applies the clean landings, so `HEAD` never sits red across a breakpoint.
  [INV-39, T-19]" — the git-ref backtick and register conversion the wave described. CONFIRMED.
- **Item 11 — two questions never collapsed.** Present at PRODUCT_SPEC.md:2978: "*shall* ask one
  contextual question at the first push moment only where … no push grant, one question per gap.
  [INV-82]" CONFIRMED.

(Bonus sighting: item 9's held-for-milestone state is also present at :1831.)

## SHOULD-FIX items — tracked, none silently lost

All seven of the audit's SHOULD-FIX findings are accounted for. The fix wave addressed only F1–F8; the
SHOULD-FIX set was left to become queue rows at landing per INV-114, except F11 which reads resolved by
the restructuring.

- **F9 — far excluded from R96's end-state enumeration.** OPEN → queue. R96 (:2141–2151) still
  enumerates only declined / deferred / superseded. Not silently lost: far is fully specified elsewhere
  as an active-queue kept state distinct from a terminal exit (:323 "A far row stays too, but carries no
  revisit trigger," R94 at :2104–2113, glossary far tier at :88) — the audit's preferred resolution
  (far is a kept state, not an end-state) is realized in structure, but R96 itself carries no explicit
  cross-reference. Queue row.
- **F10 — isolation-default rule stated in two verbatim homes.** OPEN → queue. Both homes stand:
  PRODUCT_SPEC.md:1890 and :5787, each stating the full write-set-overlap condition, [INV-105]. Not
  consolidated. Queue row.
- **F11 — scope-not-time rule and its GAP stated twice.** RESOLVED (reads fixed). The rule now has a
  single home (PRODUCT_SPEC.md:1026) with its one GAP at :1027; the distinctive GAP wording "judge and
  measure of a wish's effort against its worth" appears exactly once. The duplicate the audit named is
  gone. Not part of the MUST-FIX wave, so likely folded incidentally by the restructure — but the
  duplication no longer exists.
- **F12 — declared keyword set omits *where*.** OPEN → queue. The declared set (PRODUCT_SPEC.md:9, and
  the format-law INV-251 at :6289) still lists five keywords; italic `*where*` occurs 7 times as a
  genuine keyword (:524, :1663, :2460, :2633, :4128, :4864, :5549). Push-risk check: no armed gate
  enforces a closed keyword whitelist — INV-251 is the all-caps rule, not a keyword-set gate, and no
  guardrail reds an italic `*where*` — so this does not red the suite today and stays a queue row.
  Standing note the audit already flagged: it rises to blocking only if the re-pin sweep later arms a
  closed keyword-set gate on the five-word list; the one-word fix (add *where*) should land before any
  such gate arms.
- **F13 — fix-sized / story-sized GAP overstates "undecidable."** OPEN → queue. The full GAP stands at
  PRODUCT_SPEC.md:3271. Not softened to cite the user-story test. Queue row.
- **F14 — "activity generation" used as a term of art with no glossary entry.** OPEN → queue. The term
  is used at :5713/:5726/:5727; no glossary entry exists (none in the <230 glossary block). Queue row.
- **F15 — grammar defect "shall born it."** OPEN → queue. Unchanged at PRODUCT_SPEC.md:5438: "the system
  *shall* born it onto the waiting board." Queue row.

## Mandatory-sweep note

This is a finding-scoped pre-push re-check of an already-audited, already-fixed document, not a
first-pass FULL prove of a new surface; the mandatory-sweep verdict table was written in the
2026-07-22 full-audit record and is not re-derived here. This pass adds only the fold-verification the
authoring seat could not run from a clean context (INV-237): each MUST-FIX confirmed at its location,
the restorations confirmed present, the SHOULD-FIX set confirmed tracked.

## Bottom verdict

**PUSH-READY on the prover dimension: 8 / 8 MUST-FIX folded, both restoration waves confirmed, 7 / 7
SHOULD-FIX tracked (6 open queue rows + 1 resolved), zero new blocking finding.** The six open
SHOULD-FIX items open as queue rows at this landing per INV-114; F12 carries the standing "arm the fix
before the keyword gate" note. The push additionally stands on the suite log's own N/N green, owned by
the parallel re-pin sweep — this record clears the prover half of the M-6 gate and rides the landing
commit as its pre-push re-check.
