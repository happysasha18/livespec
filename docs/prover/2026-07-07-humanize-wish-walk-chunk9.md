# Prover cross-link — SPEC humanize: "Throwing a wish" CHUNK 9 (2026-07-07, session 25)

CHUNK 9 = two bold-leads + the four-always-true list: What already works is promised before you touch it
(regression fences) · A feature also says what it is NOT doing (non-goals + success measure) · the
"While it walks, four things are always true" bulleted list (one pen / pending question / no silent
micro-decisions / every landing cites its wish row).

Gates (whole section after splice): 157/157 tested phrases section-scoped; code multiset identical to
baseline (165 occurrences, zero diffs); full suite 175 green. Chunk 9 carries 17 tested phrases (incl. the
literal bracket phrase "[T-14, INV-19]") and ~25 code occurrences, all preserved.

## Facts carried (all KEPT)
- What already works is promised [T-14, INV-19]: regression fences (one sentence per neighbouring promise,
  each citing its clause); a fence earns NO new matrix row, the never-side [INV-6] + full-suite catch it;
  split in two (STAY fenced, cited, untouched / CHANGED re-authored); an unwritten promise reconciled from
  the shipped truth [A-3], surfaced [INV-5]; the row names its fences by the anchors they cite; LANDING line
  one-line [T-7]; a prototype fences nothing [E-17].
- A feature says what it is NOT doing [INV-20] + success measure [INV-21]: non-goals always written
  ("nothing deliberately left out this time" valid), narrowing rides the batched report [INV-4, INV-18]
  never silent [INV-5]; a success measure decided or `[default]`-tagged (the tag marking provenance only,
  no matrix row, reading machinery stays [target]); quantification questions ride the sweep [T-13, INV-18];
  both bind forward, adopted feature owes its pair [A-3]; A prototype writes neither [E-17].
- Four-always-true list: one landing at a time under one PEN [INV-2] (atomic claim, re-check under the
  fence [INV-11], up to three trains [T-18], a landing commit carries exactly one row's delta [INV-39],
  workers overlap on disjoint files or an isolated tree [INV-11, ACT-3]); a pending question never stops
  the work [INV-4]; no silent micro-decisions [INV-5]; every landing cites its wish row [INV-3].

Verdict for chunk 9: **CLEAN.** Whole-section gates green.
