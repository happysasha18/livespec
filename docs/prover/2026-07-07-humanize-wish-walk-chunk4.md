# Prover cross-link — SPEC humanize: "Throwing a wish" CHUNK 4 (2026-07-07, session 25)

CHUNK 4 = four bold-leads: Anything handed to the human opens with its passport · During an away-stretch,
windows accumulate · The showing channel matches the session's seat · The stretch's end is unmissable.

Gates (whole section after splice): 157/157 tested phrases section-scoped; code multiset identical to
baseline (165 occurrences, zero diffs); full suite 175 green. Chunk 4 carries 13 tested phrases and
7 code occurrences, all preserved.

## Facts carried (all KEPT)
- Passport [INV-51]: an artifact says which project + whether it wants him; project name in the visible
  content, never only the URL; the two example labels ("needs your word: what, by when" / "just an update");
  every artifact leads with it, the announcing chat line carries the same two facts.
- Away-stretch [INV-52]: windows accumulate on ONE page during an offline window [INV-35]; the stretch's
  end opens it once; mid-stretch re-opening only as the SAME page refreshed in place; his 2026-07-06 word
  paraphrased.
- Showing channel [INV-67]: matches the session's seat — local browser window for a local seat, the remote
  seat's own channel (host-rendered page or the chat) for a cloud seat; same passport [INV-51], same
  round-trip; a local file path handed to a remote reader is a defect; the profile show line is the local
  seat's arm.
- Stretch's end unmissable [INV-57]: a report that drowns above tool noise was never delivered (17-row
  night, his 2026-07-07 word paraphrased); the LAST rendered thing is one SHORT final line with four fields
  (what closed · what's next · what's needed · when the agent wakes); the final line comes LAST after every
  tool call; a page deliverable repeats its passport [INV-51]; Delivery, not existence, is the law.
- Two Russian quotes rendered as plain-English paraphrases with dates; no new quotation introduced.

Verdict for chunk 4: **CLEAN.** Whole-section gates green.
