# Prover — row 219, every skill-kind landing walks skill-creator's review (2026-07-11, short form per INV-61)

Small delta (kind: skill, no new surface; architecture touch = anchor assignment INV-99 →
build-pipeline) — the three-line short form, plus the law's own first run below.

- **Previous records:** 2026-07-11-row218-convergence-rule clean (0 must-fix); no unfolded rows.
- **The delta in one line:** whenever the classifier names a wish's work-kind `skill`, the verify
  step additionally runs the installed skill-creator's review of the touched skill (format,
  frontmatter, description-triggering — does the skill load when it should; evals where applicable),
  each finding folded or rejected by name in the landing record; the classifier is the trigger, mood
  plays no part; the milestone gate's whole-pack walk (already standing in the gate list) is the
  catch-up for earlier skills — clause beside the work-kind law (INV-99, prose + index), the
  work-kind table's verify/skill cell names the walk, M-235, four string tests red-proven.
  Composition read: T-16 (kind at intake) is the trigger's source and stays whole; the milestone
  walk keeps its own home (cited, never restated); INV-22's no-kind-scales-nothing-down still means
  an unresolved kind cannot dodge the walk.
- **Verdict:** ready to ship — 0 must-fix, 0 should-clarify.

## The law's first run — skill-creator review of the touched skill (build-pipeline), same landing

Eight findings from the installed skill-creator's craft review; each folded or rejected by name:

| # | Finding | Folded / rejected (+why) |
|---|---|---|
| F1 | Frontmatter clean | no action owed |
| F2 | Description undertriggers: only the feature door named, body owns bug/refactor/docs-only/removal doors | **folded** — description now names the other doors ("also the entry point for bugs, refactors, docs-only changes, and feature removals — a bug enters at the matrix step") |
| F3 | Description's routing summary inaccurate (named two skills, body sequences test-author too) | **folded** — now "orchestrating the pack's working skills (spec-author, product-prover, test-author)" |
| F4 | Description carries trigger-irrelevant bookkeeping (the PLAYBOOK.md provenance sentence) | **folded** — sentence dropped from the description; the body's opening note already holds the fact (one home) |
| F5 | Strengths recorded (when-NOT-to-use present, concrete triggers, sensible length) | no action owed |
| F6 | 446 lines near the ~500 ceiling; delegation + lanes blocks are progressive-disclosure candidates | **rejected for now, by name** — under the ceiling today, zero bundled resources; recorded as the split candidate (references/delegation.md + references/lanes.md) when the file next grows |
| F7 | Header pins live-spec-base's version in prose — "drifting version number" | **rejected, by name** — the inherit pin is the pack's own deliberate mechanism (base rule 8 freshness + the lockstep test in the suite); it moved mechanically at tonight's base bump, which is the anti-drift proof; the no-prose-pins law targets unowned numbers |
| F8 | Templates fetched from the pack repo at use-time; bundle instead? | **rejected, by name** — decided design (D-4 package-is-source; row 67: fixed pointers, a copy would fork the truth) |
