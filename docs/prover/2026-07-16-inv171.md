# Prover record — INV-171, the coverage record and the two-tier stress wall (2026-07-16, FEATURE-FIT)

Prover skill version: 1.1.4 (pre-fold; this record reviews the fold that lands INV-171).
Mode: FEATURE-FIT on one delta — the mandatory-sweep verdict record plus the lifecycle gathering.
The independent deep pass that motivated it is the Fable adversarial audit,
`docs/audit/2026-07-16-prover-fable.md` (findings D5 and R2); this record checks the fit of the
fold that answers them.

## The delta
INV-171 — "A full prover pass owes a coverage record: mandatory sweeps with verdicts, per
surface." 3e splits into five mandatory sweeps (declared laws, edge-condition completeness,
uniformity, the lifecycle sweep under the payload parent, unwritten seams), each owing one
verdict line — hit / clean / N/A-with-reason — rendered as a surface × sweep table where the
classic coverage tables go N/A. The imaginative probes stay discretionary. The five lifecycle
lenses gather under INV-168 with their anchors kept and a boundary paragraph naming what each
sub-question does not cover.

## Fit walk
- **Arrival** — the verdict duty fires on every FULL pass; the record rides the existing
  review-record class (INV-156) rather than minting a new shape. HOLDS.
- **Every next-step** — hit writes findings as before; clean writes the one verdict line; N/A
  demands its reason. A missing line reads as a skipped sweep — the failure mode the audit named
  (skip indistinguishable from clean) has no remaining path. HOLDS.
- **Anchor preservation** — the gathered lenses keep their bold names and [INV-x] anchors; the
  existing string tests over "**Transition payload**", "**Entry state**", "**Entry symmetry**"
  and kin stay green with no test edits (verified by the suite, 870 green). HOLDS.
- **Contradiction check** — the old tail sentence "a finding is not owed for every axis" would
  read against the verdict duty; it now carries the carve-out ("a mandatory sweep's verdict line
  is owed even when its finding count is zero"). The old sanctioned N/A line stays legal for a
  single table while the all-N/A kind owes the surface × sweep table — the two sentences state
  their split explicitly. HOLDS.
- **Mode seams** — FEATURE-FIT's verdict-per-lens duty is untouched; CROSS-LINK runs 3e's
  composition lenses as before and its new quantifier re-verify (INV-170) is independent of the
  tiering. HOLDS.

## Disposition
| Finding | Kind | Disposition |
|---|---|---|
| Fable audit D5 (no coverage record; N/A collapse on frontend kinds) | defect | folded — INV-171, M-326, the two-tier 3e |
| Fable audit R2 (lifecycle lens overlap, five colliding angles) | recommendation | folded — the lifecycle sweep gathers the five under INV-168, boundary paragraph added |

Verdict: the delta FITS; no open questions ride. The batch-close FULL pass runs under the new
verdict duty for the first time and will produce the first surface × sweep table.
