# Prover record — 2026-07-05 ~23:43, session 10: focused delta, SPEC v0.15.10 → v0.15.11 (row 103, INV-24)

One clause added to the Rhythm section: time is read off the clock — no future-dated file name,
journal heading, or ledger date; mechanical fence in the suite (gate b carries it into the push walk).

Delta walk:
- **Scope honesty (the one real edge):** prose QUOTING a past incident's wrong date must stay legal —
  the journal describes the invented-time defects by their wrong dates. The clause states the
  exemption; the test scopes to file names + `## ` journal headings + ledger table rows, and the
  repo's only future-dated string today is exactly such a quote (JOURNAL.md:669) — verified by grep,
  test green with it present. No conflict.
- **INV-23 seam:** the ledger entry this fence solves cites row 103; the fence clause does not restate
  the ledger's law — one home each way.
- **M-2/M-3 seam:** dated stamps stay mandatory; the fence bounds them from above only. Liveness
  untouched.
- **Red proven by deed:** synthetic `inbox/2027-01-01-*.md` → suite red with the file named; removed →
  94 green. The check is self-contained (os.walk + read), no git dependency, so it also runs on a
  fresh unzipped copy.

Verdict: READY TO PUSH (v0.15.11 covered by this record on top of row100 + v15-8-full).
