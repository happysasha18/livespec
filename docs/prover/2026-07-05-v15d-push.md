# Prover record — 2026-07-05 night (~23:50), v15d push gate (M-6, before the session-8 closing push)

Mode: **FULL by verification.** SPEC.md is BYTE-UNCHANGED since the v15c gate's READY-TO-PUSH verdict
(last SPEC-touching commit ccc88d5 = the state v15c reviewed; verified by `git log -1 -- SPEC.md`, not
from memory). The delta being pushed on top of v15c: row 61 only — guardrails/check-prover-record.sh +
tests/test_guardrails.py + matrix row M-015 re-authored + queue/journal bookkeeping; no normative spec
text moved. A verbatim re-run of the whole-spec pass over an identical document would re-produce v15c;
this record documents that verification instead of performing theater (the same reasoning the honest
boundary in evals/README applies to contaminated reds: say what was actually checked).

What WAS freshly checked for this push:
- Suite 70 green / 0 skips (row 61's two new gate tests included), run at this state.
- The new freshness gate itself, run live against the real repo: OK on record ≥ SPEC commit.
- M-015 re-authored to the strengthened fact; matrix coverage + ownership mechanized checks green.
- Previous records (all eight of tonight): zero unfolded rows.
- Publish walk (E-20): delta is infra scripts + tests; README untouched and current; secrets sweep of
  the delta clean; release notes stand down (no tag).

Verdict: **READY TO PUSH.**
