# Prover record — 2026-07-06, row 142 (CROSS-LINK), SPEC v0.15.34 delta

Scope: the parallel-lanes law's cap move (T-18: two → three [default], a fourth only on the human's
asked word; his 2026-07-06 ~21:04 word). Seams walked: INV-2/M-022 (the queue's in-work cap mirrors
the lane cap), INV-39 (landing purity — cap-neutral), M-1 (milestone single-train), T-9 (bug parks all
lanes), INV-27/INV-28 (board readability at three lines), INV-4/E-22 (the ask-for-a-fourth as a
question that never stops rolling work), ACT-3 (isolated trees per later train).

Previous record: docs/prover/2026-07-06-row141.md — no unfolded rows ride.

## Opening assessment

The delta moves a number inside an existing law; no new surface, no new state. The law's structure
(one pen, isolated trees, landing purity, milestone single-train) is cap-neutral and survives
verbatim. The real risk is the cap NUMBER living in more than one home — it did.

## Findings

| # | Finding | Severity · tag | Status |
|---|---|---|---|
| F1 | The cap number had three homes: T-18's prose, M-022's "at most TWO rows in-work" (anchored to INV-2, not T-18), and the test constant in `test_roadmap_in_work_cap`. A flip touching only T-18 would leave the queue law contradicting the lane law. | must-fix · one-home (rule 4) | FOLDED — M-022 reworded to take its number FROM T-18's cap (three [default]) so the next move edits one home; M-129's never-side becomes "never a fourth unasked"; the test constant moves to 3 in the same landing |
| F2 | The ask-for-a-fourth could read as a work-stopping gate, colliding with INV-4 (a pending question never stops the work). | should-clarify · liveness | CLARIFIED — the ask rides the batched decision page (E-22) like any cross-lane question; the three rolling lanes never pause on it; the fourth wish simply stays queued until his word. The law's sentence says "never opens it silently", the stopping side is governed by INV-4 unchanged |
| F3 | Working well, noted: the flip is the settings-ladder machinery doing exactly what it promised — session 19's report marked the cap "a tagged [default], one word moves it"; the word came and moved it through the queue with provenance, not as an untracked edit. | — | — |

## Coverage notes

Composition: with three lanes, TWO later trains each hold their OWN isolated tree — the law's prose
now says "each written in its own isolated copy"; integration still serializes under the one pen, so
pairwise independence is judged for every pair (no surface two lanes touch). Milestone and bug edges
re-read and unchanged. No ⟨DECIDE⟩ touched; D-1/D-2/D-3 stay open, untouched by this seam.
