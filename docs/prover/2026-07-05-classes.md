# Prover record — 2026-07-05 (classes) — CROSS-LINK pass, push gate for the classification landing

Scope: SPEC.md v0.5, rows 29+34 (one size vocabulary + priority) and row 35 (communicator rule 9).
Mode: CROSS-LINK — the new classification rules' seams against the surfaces they compose with.
Previous record: 2026-07-05.md (0 must-fix; its queued rows: 29 landed by this change, 30–32 still queued).

## Seams checked clean

- One-landing-at-a-time (INV-2) × quick-win bubble (T-11): a bubble happens only when the lane frees —
  never interrupts. Stated explicitly.
- Pending-question-never-blocks (INV-4) × ask-at-intake (INV-12): ambiguous class defaults to normal, the
  open question rides in the row, the lane keeps moving. Explicit cross-reference in the prose.
- Inbox harvest (T-10) × classification: harvested wishes become queue rows and every row walks the same
  classification step — covered by composition; intake for an outside wish is the harvest.
- Starvation (liveness) fence: after one bubbled landing the queue head goes next — stated in T-11.
- Tier router (D-2): row 29's landing unblocks keying on size; D-2 stays open, no contradiction.
- Milestone compaction (M-1) / queue archive (row 30): untouched by this change.

## Findings

| ID | Severity | Finding | Status |
|---|---|---|---|
| F1 | should-clarify | Two critical bugs waiting: order among them unstated (ambiguity/ties) | folded — T-9: critical bugs head the line among themselves by arrival |
| F2 | should-clarify | "Lands before everything" (T-11) could read as interrupting a bug already in the lane — nested parking undefined (undefined-path) | folded — T-9: a bug in the lane is never itself interrupted; at most one wish is ever parked |
| F3 | must-fix | templates/ROADMAP.template.md still taught the OLD scale (Size column, "S = one session · M = 2–5 sessions · L = multi-week") — a host bootstrapped from it would contradict the one-vocabulary rule the same day it adopts (direct-contradiction) | folded — template rewritten: Class column, the four spec words + priority marks, status vocabulary aligned with T-8 (declined/deferred/superseded) |

All findings folded before the push; per M-6, gate-pass folds ship with this record and do not re-trigger
the gate.

## Open ⟨DECIDE⟩ count after this pass

D-1 (attic layout) · D-2 (tier routing override) · D-3 (snapshot retention) — 3 open, none touched by the
surfaces under change beyond D-2's unblocking (it stays a decision, now decidable).
