# Prover record — 2026-07-06 ~23:26, push-gate re-check, session 22 (row 106 landing)

Previous records: `2026-07-06-pushgate-s22-3.md` (clean), `row147` (no unfolded) — nothing rides in.

Scope of the delta since the row-147 push: pytest.ini NEW (testpaths = tests — row 106's fix, the
bug door: no spec delta owed, the fact lives in packaging) · TEST_MATRIX.md M-143 (level string, the
closed vocabulary respected this time) · tests/test_guardrails.py TestPytestFromRoot (red-proven on
the pre-fix tree: pytest --collect-only from root errored; green after: 141 collected, zero errors,
template not collected) · ROADMAP rows 106 landed + 148 queued (his ~23:24 word; the row declares
"entry: mockup-first" — INV-43's door arm walked at intake the night it landed) · JOURNAL + ledger
(sixteenth clock catch) · suite 141 green.

Walk: no spec anchors changed (bug door; M-143 cites M-4, already owned by package-docs); no version
bump owed — pytest.ini is repo packaging, not a skill behaviour change (pack stays 0.8.54; the suite
count in the resume file updated). Reach note: this push's diff includes pytest.ini and tests/ —
full suite ran, correctly. Shopfront (INV-44): README makes no claim about how to run tests — nothing
stale; diagrams untouched. No ⟨DECIDE⟩ touched.

Verdict: no unfolded rows; push may proceed.
