# Prover record — 2026-07-05 evening (~21:30), row 94 CROSS-LINK + architecture lens (skill evals)

Mode: **CROSS-LINK** (surface add) + **architecture lens** (node ADD is a structure change). New surface:
the skill-evals machine — E-19. Spec under review: SPEC.md v0.15.0 + the E-19 delta. Previous records
(`2026-07-05-v15-push.md`, `-row86.md`): zero unfolded rows.

## Seams checked

- E-19 × M-1 — the milestone list gains "skill evals re-run"; grows, never shrinks (fence held).
- E-19 × E-12/INV-13 — evals TEST the skills, they never restate rules; one home per rule intact.
- E-19 × A-7/M-7 — "a pin/version-only bump owes no re-run": the behaviour-vs-pin call is the landing
  author's, journaled with the bump (recorded reading; a mechanical discriminator would over-promise).
- E-19 × ACT-3 — eval runs are worker-tier work (cheap agents run scenarios; the senior judges outputs).
- E-19 × INV-15 — owning node (skill-evals, added this landing) + matrix rows (M-086/M-087) + level
  string over the shipped eval files; the RUNS themselves are session-level deeds recorded in docs/evals/.
- E-19 × T-16/INV-22 (dogfood) — this landing's kind: infra. Facet sweep: no visible surface — facets
  N/A (stated aloud). Design-sync/snapshot: stand down (text/tooling). Prove-architecture: APPLIES this
  time (node add) — the contrast with row 86's stand-down is the axis working as law.
- Persistence lens — eval RUN records are dated files, append-only (a re-run adds a record, never
  rewrites one); the scenario files state current truth like spec prose.

## Findings

| # | Finding | Tag | Status |
|---|---|---|---|
| F1 | **"A bare session" is not bare on a developer machine.** The first real bare runs (2026-07-05, four Sonnet workers, zero tool uses) produced near-method output — the machine-global thin loader + profile leak the method into EVERY session here. The spec's own words survive ("the skill not loaded" — accurate), but an eval README that doesn't state this boundary would sell contaminated reds as clean. | must-fix · unenforceable-promise (discharge) | **FOLDED** — evals/README.md carries the honest boundary: red = bare-of-the-SKILL, not bare-of-the-loader; scoring is per named criterion, and criteria a loader-fed bare run already meets are recorded as such, not claimed as the skill's win |
| F2 | **Scenario prompts that enumerate the facets contaminate the red.** The first spec-writing scenario listed "two views, phones, two windows, renamed/deleted" in its context — steering the bare run to exactly the facets the skill would have swept unprompted. | must-fix · missing-prerequisite (precondition) | **FOLDED** — eval-authoring rule in evals/README.md: the scenario speaks like the human (the wish only); discoverable context may exist, enumerated hints may not; the first-run record notes the contamination where it happened |
| F3 | The eval completeness check should be self-closing, not a hand-list — a fifth working skill added to skills/ must go red until its eval exists. | should-clarify · missing-outcome-check (postcondition) | **FOLDED** — `test_skill_evals_present` derives the required eval set from the skills/ directory (minus the base rulebook) |

## Architecture lens (node add)

- New node **skill-evals** owns E-19 — no other node claims it; every other anchor keeps its owner
  (assignment table re-checked; ownership still 1:1).
- Node backed by spec (E-19) and live artifacts (evals/) — not speculative.
- New seam named: evals ↔ the working skills (what crosses: the scenario's green-criteria against the
  SKILL.md's promised behaviour; format owner: skill-evals). Second seam: evals → milestone gate (M-1
  re-run item; the gate list's home stays the spec).

## Verdict

Proceed to matrix + tests. All three findings folded within this pass; folds local (evals/README, the
test, this record).
