# Prover record — 2026-07-06 ~22:44, row 109 (prototype-norm pointer), CROSS-LINK

Scope: the INV-43 delta (SPEC v0.15.40 — one paragraph in "A prototype is not the product" + index row)
against its named neighbours: the prototype fence (E-17, INV-17 and its shipped guardrail), the door law
(INV-16), the verify walk's feel bar (INV-30), the board (INV-27), binds-forward (INV-21), and the
matrix derivation (E-15).

Opening assessment: the law is the right carve — one invariant with four arms, each arm assigned to the
skill that walks that step, the incident (a look rebuilt from text, 75 green tests proving the
misreading) named as the reason. One genuine composition conflict found against the SHIPPED fence check
and folded in-pass; one missing written home folded; one cross-link added. Ready to build.

| # | Finding | Severity · category | Status |
|---|---|---|---|
| F1 | The pointer fights the fence it lives beside: a `norm: prototype/door.html` line in SPEC.md trips the SHIPPED one-way fence check — `guardrails/check-prototype-fence.sh` scans SPEC.md (its exclude list is docs/, attic/, inbox/, journals, roadmap, resume file only), so the first real pointer goes RED at push and a session "fixes" it by weakening either the law or the fence. | must-fix · boundary-issue (composition) | FOLDED — approval FREEZES the artifact into the project's records: a copy lands in `docs/norms/` with a dated provenance line (what, approved when, from which sketch), and the pointer cites the FROZEN copy; a norm pointer never reaches into a live prototype home, the fence stays absolute, the sketch stays free to die |
| F2 | The mockup-first entry condition has no written home: declared in chat, it survives only in session memory — the exact tlvphoto failure (the condition was voiced, the next "давай фигачь" built anyway); a law that says "cancellable only by name" is unenforceable if no artifact records the condition. | should-clarify · missing-rule (invariant) | FOLDED — the condition is WRITTEN in the wish's queue row at intake ("entry: mockup-first"); the door step reads the row, not the air |
| F3 | The verify step's feel bar (INV-30) judges "against the approved prototype" but has no stated way to FIND it; the pointer is that way. | worth-considering · hard-to-monitor (observability) | FOLDED — one clause: the verify step's feel bar reads the same pointer |

Cross-links that hold without change: INV-16 — promotion still enters at the spec step; the norm
pointer adds evidence, grants no rights to prototype code (E-17's own sentence, unchanged). INV-27 — no
new station; the diff line rides the code step's landing accounting. INV-21 — binds-forward phrasing
matches the pack's standing style. E-15 — skill kind derives string rows on the four shipped SKILL.md
homes; no rendered level owed. Sibling sweep: the two other clauses that speak of the approved
prototype as a bar (the fit walk's FEEL lens, INV-29 text; the verify walk, INV-30) both point at the
same E-17 artifact — with F3's fold all three now meet in one pointer; no duplicate law written.

Findings: three, all folded in-pass.
