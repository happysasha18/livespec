# Prover record — INV-172 + INV-173, the turnkey ratchet kit and the canonical hooks (2026-07-16, FEATURE-FIT)

Prover skill version: 1.1.4. Mode: FEATURE-FIT on one feature's delta (task 13 — the
turnkey ratchet-adoption package, hook propagation included).

## The delta
INV-172 — adoption wires the ratchet gates in one pass: vendored scripts with source pins, debt
caps seeded at the host's current measured size (green at once, ratchets down), a generated guard
test, push-gate wiring, all under the four-checks shipping contract (INV-97).
INV-173 — pack hooks live as source in `hooks/`, install via the setup walk, split universal
(pack law) against personal (overlay file the personal layer owns); a scan hook skips quoted
demonstrations.

## Fit walk
- **Arrival** — the kit runs at adoption's Phase 5 tail (ADOPT.md now carries the step) and at a
  catch-up walk on an already-adopted host; the hook installer runs at machine setup. Backed. HOLDS.
- **Every next-step** — after install: green gate → ordinary pushes under the cap; debt growth →
  the lock test reds; cap raise → the guard test reds (deliberate, visible). All three exits
  stated and tested (`tests/test_ratchet_kit.py`). HOLDS.
- **Return visit** — a re-run of the installer skips existing vendored files without `--force`
  and re-seeding is explicit, so a later pass cannot silently loosen a cap the host had ratcheted
  down. HOLDS.
- **Cross-entry** — a host that already carries the pack's own gates (live-spec itself) does not
  run the kit; the kit is for hosts, the pack pins its own floor in `test_convergence_locks.py`.
  The two are members of one pattern, each with its own home (INV-163). HOLDS.
- **Second-sibling question (INV-169)** — YES: the kit is the second member of the "installable
  check kit" kind (`scaffold/guardrails/` is the first). The scoped design review runs on this
  delta — record `docs/design-review/2026-07-16-ratchet-kit.md`. HOLDS (the new law fired on its
  own author).
- **Neighbour state** — the source-pin manifest is what the update-watcher (queued task 6) will
  read; the manifest ships now so the watcher has a target. Stated as a forward seam. HOLDS.
- **Feel bar / invited-next** — no visual surface; the invited-next is the per-host adoption wish
  at batch close. N/A with reason.

## Disposition
| Finding | Kind | Disposition |
|---|---|---|
| task 13 (turnkey package; the enforcement-across-adoption hole) | feature | landed — INV-172, M-327, `adopt/install-ratchet.sh` |
| hook propagation + personal/universal split (owner 2026-07-16) | feature | landed — INV-173, M-328, `hooks/` + installer |
| scissors-hook false-fire on a quoted demo (queued hook tuning) | bug | folded into INV-173's quoted-demo clause + `test_hook_skips_quoted_demos` |

Verdict: the delta FITS; no open questions ride.
