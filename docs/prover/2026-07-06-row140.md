# Prover record — 2026-07-06, row 140 (CROSS-LINK), SPEC v0.15.35 delta

Scope: the economy ladder (new section "When money or time run short"; T-19, INV-40) — what a tight
budget may legally shed, and the never-bend list. Seams walked: E-13/INV-9 (word-only settings), M-6
(the push gate), INV-39 (landing purity under batched gates), INV-5/INV-31 (named sheds in the
report), D-2 (worker tiering, open), INV-26 (rows close whole vs deferred FULL prover), T-15/T-16
(the safety net), INV-35 (narration), the host-profile precedence (this repo's own tighter cadence).

Previous record: docs/prover/2026-07-06-row142.md — no unfolded rows ride.

## Opening assessment

The delta adds a SETTING and its semantics, no new visible surface. The dangerous seams are the two
where economy could eat a guarantee: batched landing gates vs landing purity, and deferred FULL
prover passes vs the push gate. Both are closed in the text itself.

## Findings

| # | Finding | Severity · tag | Status |
|---|---|---|---|
| F1 | Batched gates under `tight` could read as licensing a push with only the batch-end run somewhere behind HEAD. | must-fix · safety (M-6 seam) | FOLDED in the delta itself — the tight rung says a push still requires the full gate green at HEAD, and the never-bend list repeats the push gate as unbendable; M-6 unchanged |
| F2 | A deferred FULL prover pass could silently evaporate (nothing owns the debt). | must-fix · liveness | FOLDED — the deferral is written as a dated debt line in its queue row, never just remembered; the milestone's re-listing walk (M-1) re-surfaces open queue lines, so the debt cannot outlive a milestone unseen |
| F3 | A red at the batch's end is attributable to no single landing (several rows landed since the last green run). | should-clarify · atomicity | CLARIFIED — the delta orders a bisect by landing order before anything else lands; commits stay one-delta (INV-39), so the bisect is mechanical |
| F4 | The rung could collide with a host profile that pins tighter rigor (this repo's own full-prover-before-push). | should-clarify · composition (E-13) | CLARIFIED — stated twice: an explicit host line outlives any rung; the ladder resolution (narrower beats broader) already says so, the sentence makes it unmissable |
| F5 | Working well, noted: the never-bend list makes poverty the safety net's third named non-toucher (after work-kind and scope cuts, T-15/T-16) — one consistent shape across all three pressures. | — | — |

## Coverage notes

The rung is qualitative by design (non-goal: no token meters) — the prover accepts this as an honest
boundary: the human's word, not a measured threshold, moves it (INV-9 family). D-2 (tiering router)
stays open; the ladder cites it without resolving it. Open ⟨DECIDE⟩s untouched: D-1/D-2/D-3.
