# Prover short-form — row 237, no self-certification (2026-07-10 ~14:00)

One-hop extension of the register family (INV-28 / INV-83 / the profile's no-scissors line),
short-form per the cadence for a small prose law.

The clause (INV-94) composes clean: it adds a CLASS to the register lint's families and cites
INV-83's grows-by-one duty rather than restating it, so the growth law keeps one home; its reach
statement mirrors INV-83's split exactly (shown artifacts — the lint; chat — the hook plus the
session's read), no new enforcement machinery claimed. The example family names both languages
and all three flavours (honesty, directness, diligence — the diligence variant folded the same
hour it was caught in chat). The distinguishing rule is stated as its own test: naming not-A
informs only where not-A stood as a live alternative — which keeps legitimate contrast legal
("published in full" survives; "unsoftened" goes). Edge walked: "honest time range" in INV-93
stays — it states a REQUIREMENT on the estimate (read from observed runs), and certifies no
speaker; the lint patterns are specific collocations, so the constituent word "honest" stays
free. Cross-link lens: INV-94 cites INV-83; the index row lands beside the family; communicator
rule 8 trails the anchor. Matrix M-223 owns the fact at string level, red-proven; the README
sweep is part of the same row's delta, asserted by the owning test.
