# Prover record — 2026-07-06, second push gate of the day (batch: rows 125 · 123 · 124)

FULL-pass status, stated honestly: the 11:57 FULL pass (`2026-07-06-push.md`) covered the whole spec at
v0.15.19; since then the spec changed ONLY in the three clause areas this batch landed (verified by
`git diff 89e564f^..HEAD -- SPEC.md` reading: INV-27 board paragraph, ACT-3 worker contract, the new
INV-33 paragraph + index rows + version header). The morning record carries for every unchanged byte —
same rule the 12:26 addendum used; what got the fresh walk is the three deltas and every seam they
touch, each read against the whole document.

Previous record check (the walk's first act): `2026-07-06-push.md` carries no unfolded must-fix rows —
F1–F3 folded same hour, F4 became row 125 (folded by this batch), F5 rejected-with-reason (header
re-pinning is the milestone's own step, still true).

## The three deltas walked

**INV-27, nine-station board (row 125).** Station list = the pipeline's own nine step names + terminal
"landed"; cross-read against build-pipeline's step list, communicator rule 9, M-112, OVERVIEW,
ARCHITECTURE lifecycle — one vocabulary everywhere now. Seam to INV-26 clean ("landed … says the row
closed whole" composes with close-only-whole). Bug-lane features report under matrix/test/code — all
nine names cover the shortcut lanes, no orphan stop.

**ACT-3, the brief carries ledger + clock (row 123).** Seam to INV-23's own seam sentence ("the session
carries it over, unless the brief names the ledger among its files") — consistent, ACT-3 adds WHEN (at
verify). Seam to INV-24 — the brief's clock is the machine clock read at briefing, so a worker quoting
it can never stamp past the commit clock ahead of time… except by GUESSING elapsed time (see F2, proven
by deed the same hour it shipped).

**INV-33, the craft ladder (row 124).** SPEC binds the law and names the archetypes; the full ladder's
one home is build-pipeline (M-120's never-side guards a second full statement). Seam to INV-22 clean —
kind picks form, craft picks the head. Seam to INV-30's medium-scaling sentence exposed F3.

## Findings

| # | Finding | Severity | Folded / rejected |
|---|---|---|---|
| F1 | CLASS, the row-125 family one layer deeper: the board now names step 9 "commit & show" (its own heading), but the pipeline's FULL order lines still called it "commit" — build-pipeline's opening order line, its frontmatter description, README (twice), adopt/ADOPT.md. One step, two names, the exact drift INV-27 exists to stop. (The gates' "Order is law" lines end at `code` — a different granularity, not a name for step 9, left as is.) | must-fix (one-name law / consistency) | FOLDED same hour: all five full-list sites now read "… verify → commit & show"; swept by grep, records/journal untouched |
| F2 | The brief's clock does not stop GUESSED ELAPSED time: the row-123 worker, clock 12:45 in its brief, stamped "13:05 (clock+~20min)" when ~5 real minutes had passed — proven by deed the same hour the clause shipped. The fence (INV-24) never sees it (checkpoint is gitignored) | should-clarify (unenforceable-promise / discharge, mild — the stamp stayed off any committed surface) | FOLDED same hour into the delegation gate's clock sentence: a worker WITH a shell re-reads the machine clock itself; the brief's line is the floor for one without, and elapsed time is never guessed |
| F3 | The craft ladder is medium-blind — the same family Alexander already corrected in INV-30 (row 121, «проект может быть хоть книгу написать»): as shipped, a prose product's code step is worked "as a senior developer", which is wrong guidance for writing a chapter | should-clarify (over-specific / abstraction; recorded-word conflict with the row-121 family) | FOLDED same hour, both homes: the craft wears the KIND's face — on prose the code step is a strong writer's, on infra a toolsmith's; the ladder names archetypes, the kind says what their standards look like in its medium |

No new must-fix left open. ⟨DECIDE⟩ registry untouched by these surfaces: D-1/D-2/D-3 remain open, none
touched by the batch's clauses (D-2 borders ACT-3 — the tier ladder — but the ledger/clock arms neither
fix nor free the tier question).

## Gate legs beside the spec walk

- Suite: 110 tests, the only red being this gate's own prover-freshness tooth — green the moment this
  record commits (same-commit passes by M-6's own rule).
- Eval re-runs (E-19, behaviour-changed skills: build-pipeline, communicator; both arms, four Sonnet
  workers): records + scores in `docs/evals/2026-07-06-batch2-rerun/`. Headline: build-pipeline
  with-skill walks door → echo → recurrence-check → class sweep → both-sides rows → red-first →
  by-deed → close-only-whole; communicator with-skill delivers the icon map, the plain station line,
  and a consequences-framed question — and leaked bookkeeping numbers into the message for the SECOND
  consecutive re-run, which by the recurring-problems law got an owner that moment: queue row 126.
- Both eval briefs carried the clock (row 123's law): no wrong-hour lead this time — the wobble the
  clause was built for did not recur.

Versions at record time: SPEC v0.15.23 · build-pipeline 0.2.22 · communicator 0.1.18 · pack 0.8.34.
