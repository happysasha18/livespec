# Prover record — INV-179, draft-time anonymization of the upstream note (2026-07-16, FEATURE-FIT)

Prover skill version: 2.0.0 (pack version, INV-178). Mode: FEATURE-FIT on one delta (row 353).

## The delta
INV-179 — the upstream note is anonymized in the draft the user reads at consent: host entities
become neutral role words; pack-public names stay; the approved note is exactly the travelling
note.

## Fit walk
- **Arrival** — the masking runs at draft time, inside the existing on-the-yes procedure; no new
  trigger. HOLDS.
- **Consent seam** — the consent law (T-21, positive word) now reads the MASKED text, so approval
  and travel cannot diverge; masking after the yes is named as the defect. HOLDS.
- **Boundary** — the pack's own public names (skills, invariant codes) stay: a note about the
  pack must name the pack. Stated. HOLDS.
- **Off-by-default** — INV-161 untouched; a host that never opts in still reads nothing. HOLDS.

## Disposition
| Finding | Kind | Disposition |
|---|---|---|
| feedback-collector anonymization (owner idea) | feature | landed — INV-179, M-335 |

Verdict: the delta FITS; no open questions ride.
