# Prover record — 2026-07-05 night (~23:30), v15c push gate (FULL re-check before push, M-6)

Mode: **FULL** (push gate). Spec under review: SPEC.md v0.15.0 + row 99's re-authored T-15 (the only
delta since the v15b gate, itself CROSS-LINK-proven in `row99` minutes ago with its fold applied).
Previous records — all six of tonight plus v15b: **zero unfolded rows** (re-grepped).

## What the pass walked

- **T-15's re-authoring against the whole document:** every clause that leaned on the old mechanism
  re-read — T-12 (intake line: rider removed, scope-never-time cited), INV-22 ×2 (spec + build-pipeline
  preamble: "the same law a scope cut obeys"), spec-author's assembled-delta note, README step-0 line,
  M-076 — one law everywhere, no surviving reference to a time budget as an input (grep: the term
  absent from all live surfaces; present only in history, which is never rewritten).
- **The protected list byte-check:** fences [T-14] · kept surfaces' facets [INV-18] · non-goals
  [INV-20] · success measure [INV-21] — identical before/after the rewrite.
- **Index symmetry · ownership 1:1 · matrix coverage · blocks↔nodes:** mechanized, 68 green / 0 skips.
- **⟨DECIDE⟩ inventory:** 3, unchanged.
- **S-0 both directions:** unchanged since v15b except T-15's index line; no shipped/target drift.
- **Publish walk (E-20, this push):** delta is text-only rule surfaces; README claim re-checked (the
  step-0 line now speaks scope-never-time — true of the shipped skills); secrets/path sweep of the
  delta clean; release notes stand down (no tag); fresh-clone check carries from v15b (suite unchanged
  in dependencies — zero-dep; 68 green here).

## Findings

None. One clause re-authored on the human's explicit word, seams held, term retired with a mechanical
fence against its return.

## Verdict

**READY TO PUSH.**
