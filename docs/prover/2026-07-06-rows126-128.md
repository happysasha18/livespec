# Prover record — 2026-07-06, CROSS-LINK pass on the rows 126+127+128 delta (SPEC v0.15.24)

Mode: CROSS-LINK. Scope: the three delta clauses — the INV-28 bookkeeping NEVER-list, the new INV-34
pre-report walk paragraph, the INV-24 chat-face arm — each walked against the existing seams they
compose with (INV-25, INV-27, INV-31, INV-13, the Formal index). Prior record checked:
`2026-07-06-push-2.md` — no unfolded rows.

## Opening assessment

Three small clauses, all in the report/exchange family, each born of a dated incident this session or
last. The delta is internally consistent and the seams are mostly pre-reconciled because the clauses
were written citing their neighbours. One real gap found and folded in-pass; one boundary noted as
deliberate scope, recorded not expanded.

## Findings

F1 — The NEVER-list's carve-out named only one legal home for a number, but two exist

> "where the number IS the asked substance — the done-claim evidence walk … [INV-25]" — Throwing a wish / INV-28 paragraph (as first drafted)

A human asking directly "сколько тестов в сюите?" asks FOR the number; a literal reading of the
first draft would have the answer translate the number away ("tested clean") — the message failing
the very reader it serves. Folded in-pass: the carve-out now reads "a direct question about it, or
the done-claim evidence walk" — the asked-substance rule generalized, both instances named.

`must-fix · missing-scenario (state-space)` — **FOLDED** (same pass, wording localized to the same sentence)

F2 — The pre-report walk scopes to movement-end and milestone reports; the mid-work status line stays outside it

> "before any movement-end or milestone report goes to the human" — Throwing a wish / INV-34 paragraph

A mid-work narration line can still leak a pack name; the walk will not catch it. Deliberate scope,
matching row 128's Done-when (the incident was a movement-end report; the working-narration profile
line already binds narration to report-voice, and INV-28 binds every human-facing line's shape).
Recorded as a boundary, not expanded — a recurrence on narration lines would be a new ledger entry.

`worth-considering · over-specific (abstraction)` — **REJECTED (deliberate scope)**: the row's
acceptance names movement-end; widening now would be unasked law.

## Seams walked clean (no finding)

- INV-34 × INV-27: the departures board keeps its stations — station names trail as anchors (rule 9's
  own law); the walk governs what does the talking, and the paragraph says so explicitly.
- INV-34 × INV-31: the walk adds a pre-send check, not a question; defaults are still TOLD, silence
  stays consent — no confirmation-asking introduced.
- INV-34 × INV-13: the SPEC states the law, communicator owns the walked procedure — the same split
  INV-27/INV-28 already use with rule 9; no second normative home created.
- INV-28 NEVER-list × INV-27 capture echo: row numbers are not on the list and the echo's "row N" is
  the echo's own law; no conflict.
- INV-24 chat arm × the two fence arms: quoting a past recorded time stays legal on all three faces;
  the arm adds law where no fence can run, it does not weaken the fences.
- Formal index: INV-24 and INV-28 rows updated to match the prose; INV-34 row added, homed at
  "Throwing a wish" where its paragraph lives. Index re-derived, no drift.

## Traceability note (forward duty, not a finding)

INV-34 is a new invariant: the suite's traceability check will demand a matrix row for it. The matrix
step of this same sitting owes rows for INV-34, the INV-28 NEVER-list, and the INV-24 chat arm —
asserted against the SHIPPED communicator SKILL.md (skill kind, string level).

## Verdict

Delta proven for its seams; F1 folded in-pass (local to its sentence, no re-trigger); F2 recorded as
deliberate scope. Ready for the architecture step.
