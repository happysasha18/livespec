# Prover record — 2026-07-18 — rows 370 + 394 (the enumerate-versus-ride keying · the versioned founding-question set)

Mode: CROSS-LINK. Two surfaces changed — one on the spec-author node (INV-226), one on the attach
node (INV-227). The whole spec was proven FULL at the 2026-07-17 MINOR gate and re-checked on the
2026-07-18 batches, with no structural change since; this pass proves the two new laws against the
named existing surfaces they seam to. Single senior seat, no worker dispatch [INV-103]. Previous
prover records' unfolded rows: none open.

## Surfaces under change

- **INV-226** (ROADMAP 370) — a general law over concrete instances declares whether it enumerates
  its instances or lets them ride, keyed on whether the member set is closed-and-enumerable or
  open-ended. The class sentence is homed once, beside the range law in the composition-lens family,
  and the three named laws — the range law [INV-138], the budget law [INV-41], the facet law
  [INV-18] — each cite it. Owning node: spec-author. Spec + test, no new gate.
- **INV-227** (ROADMAP 394) — the founding-question set is versioned, a host records the version it
  answered on a `founding.set-version` profile line, and the daily update check [E-25] gains a
  founding arm that names each question added past the host's recorded version, beside the vendored
  files the pin arm [INV-177] already reports. Owning node: attach; host-contract carries the
  recorded profile line as wiring. Extends the existing update check — no new pre-push gate letter.

## Cross-link seams checked

| Seam | Both sides present + named the same? | Verdict |
|---|---|---|
| INV-226 ↔ INV-138 (the range law) | yes — INV-138's clause now cites INV-226 and states its member set is open-ended, so it names the viewport alone and lets the remainder ride | clean: INV-138's own prose stands whole; the citation is added at its composition-lens-family close, its worked-instance behaviour unchanged and now explained by the keying |
| INV-226 ↔ INV-41 (the budget law) | yes — INV-41's clause cites INV-226, naming the project kinds a closed, enumerable set | clean: INV-41 already enumerates each project kind; the citation names WHY (closed set), no enumeration changed |
| INV-226 ↔ INV-18 (the facet law) | yes — the curated-facet-list clause cites INV-226, naming the facets a closed set that grows by incident | clean: the curation law ("a facet joins only with a named real incident") stands whole; the citation names the enumerate side, consistent with the grows-by-incident rule |
| INV-226 ↔ the composition-lens family [INV-125, INV-126, INV-136] | yes — INV-226 declares itself the declaration member of the family | clean: the family groups these laws by cross-axis composition; INV-226 adds the orthogonal enumerate-versus-ride axis the family never decided (the design review's finding 2 boundary), no overlap with the uniformity or paired-transition members |
| INV-227 ↔ INV-177 (the vendored-pin arm) | yes — the founding arm runs beside the pin arm, one report carrying both | clean: INV-177's arm reads FILES, INV-227's arm reads QUESTIONS; the two arms are disjoint and both proposal-only under E-25; the plain-version-proposal path and the no-manifest path stand whole (asserted by the untouched test_update_watcher, all green) |
| INV-227 ↔ E-25 (the update check's own law) | yes — the founding arm rides the daily proposal-only check | clean: E-25's non-goals (no daemon, no auto-install, no per-skill remote diff) hold; the founding arm installs nothing and proposes only, and the manifest is read against the local tree, not a remote per-file diff |
| INV-227 ↔ INV-159 (a duty binds forward) | yes — INV-227 cites INV-159; a never-answered question is surfaced, answered on no one's behalf | clean: the older host owes nothing retroactively [INV-159]; the arm only tells it the question now exists, the same forward-binding the card's flag [INV-184] carries |
| INV-227 ↔ E-32 / INV-184 (the agent card) | yes — the card is the worked instance, every tree founded before it carrying an older set version | clean: INV-184 already flags a card-less tree at founding/orient and writes it at catch-up [A-11]; INV-227 generalizes that one flag to the whole set, the card being one member (since 4) of the versioned questions |
| INV-227 ↔ INV-91 (the version-chained catch-up) | yes — a versionless host owes every question, like a versionless package record starts the chain at the earliest chapter | clean: the half-done-state law makes over-naming harmless; the arm's `base = -1` for a versionless host mirrors INV-91's earliest-chapter start |

## The footprint / gate-chain question

Both rows read single-module. INV-226 is spec + test only — the class sentence and its three
citations, one matrix row, one test file; no gate touched. INV-227 extends `scripts/check-pack-update.sh`
with a second arm behind two new test-only flags (`--founding-manifest`, `--host-profile`); the check
is E-25's daily proposal, not a pre-push gate, so no new gate letter, and `.github/workflows/gates.yml`
and `guardrails/gate-red-proofs.json` are untouched. The meta-gate over the chain
(`check-every-gate-can-fail.py`) and the CI-mirror gate stay green, since the gate marker set is
unchanged (full suite green, 1462 passed).

## Red-first proof

Both test files were written against HEAD 573d8ea and run before any implementation: 14 failed, 1
passed (the one pass being the pre-existing-anchors sanity check). After the delta: 15 passed on the
two files, 1462 passed on the full suite, 0 skipped. The redundancy gate flagged one containment pair
(a founding-block pointer duplicating the main clause's own home statement); the pointer was removed
rather than reworded, since the main clause already names founding as a home — redundancy back to 0.

## Fresh-eyes audit (SPEC INV-46) and its fold

Both rows edit the method (two new invariants), so a fresh-context checker read the shipped files on
the "tasks completed, goal missed" hypothesis. It confirmed CLAIM 1 and the check-pack-update mechanism
of CLAIM 2, and found one real gap: INV-227's clause names two guide homes — founding/orient in
`adopt/ADOPT.md` and the catch-up walk's orient in `MIGRATION.md` (the operating guide for A-11) — that
the spec prose claimed but the guides themselves did not carry, and the guarding test
(`test_catchup_orient_names_the_gap`) was vacuous (a bare substring the spec's own clause satisfied). A
second minor finding: the row-370 test cited each law without asserting the SIDE it stated.

Folded before this landing: `adopt/ADOPT.md`'s founding/orient now records `founding.set-version` beside
project.kind/layers/proofs, and `MIGRATION.md`'s Phase 1 orient now names each never-answered founding
question (step 5) — the two claimed homes made real. Both tests strengthened: the catch-up test now
asserts ADOPT.md and MIGRATION.md carry the step, and the row-370 test asserts each law's open/closed
side textually. Full suite re-run green (1462) after the fold.

## Verdict

0 must-fix. Both laws stand in their one home each, seam cleanly to every named existing surface, and
carry their mechanical net (the traceability suite for INV-226, the founding-arm fixture tests for
INV-227). No open `⟨DECIDE⟩` touched. Pack stays 2.6.3 (no bump).
