# Prover record — 2026-07-06, row 141 (CROSS-LINK), SPEC v0.15.33 delta

Scope: the INV-28 paragraph's delivery sentence (the chat laws' mechanical voice — a prompt hook
reminding every window) + the grown index row. Seams walked: one-home (INV-28/INV-35 stay in the
skills), INV-24's hand (the sibling hook, same mechanism, same install road), the human's install
gate (ACT-1 / base rule 17), and the hook line's own compliance with the law it carries.

Previous record: docs/prover/2026-07-06-row139.md — no unfolded rows ride.

## Opening assessment

The delta adds a delivery mechanism, not a law — and says so in its own words ("the hook only
reminds and never legislates"), which closes the one-home seam it would otherwise open. The install
road reuses row 134's exactly (per-machine, the human's command — the harness classifier blocked the
agent's hand live this session, proving the gate is real, not ceremonial). One genuine seam found.

## Findings

| # | Finding | Severity · tag | Status |
|---|---|---|---|
| F1 | Two texts, one law — a freshness seam: the hook's reminder line paraphrases laws whose homes may evolve; nothing forces the paraphrase to follow (the reminder could one day contradict the skill it points at). | should-clarify · internal-conflict (consistency) | FOLDED mechanically — M-133 pins the reminder's teeth as suite strings; a law change that renames a tooth goes red on this row and forces the hook text to move with it |
| F2 | Working well, noted: the voice reaches exactly the windows the day's audit showed failing — the sessions that never invoked the pack's skills — because a prompt hook fires regardless of what the session loaded. | — | — |

## Coverage notes

CRUD / authorization: N/A — a one-line reminder emitter, no entities, no roles. The install path is
shared with row 134 (one installer, both hooks) — one home for the machine-install fact.
