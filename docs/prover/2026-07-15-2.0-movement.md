# 2.0 movement — rolling meaning-preservation record (2026-07-15)

The 2.0 release is a readability + compaction pass over the living spec-family docs (PRODUCT_SPEC.md,
ARCHITECTURE.md, TEST_MATRIX.md, ROADMAP.md, the skill docs), plus the INV-163 formalization and the
inbox harvest that opened it. A rewrite that only simplifies wording must not change what any rule
REQUIRES; this record tracks the meaning-preservation evidence per checkpoint, so the push gate reads a
record at least as fresh as the last spec change it covers (SPEC M-6, INV-116). The protocol is
`docs/spec-compaction-protocol.md`; the mechanical floor is `scripts/spec-freeze.py`.

## Checkpoints

- **INV-163 (ship-the-shape / host-owns-the-instance split minted as one root).** Full adversarial
  product-prover + fresh-eyes audit ran; one false-home defect folded (a homes line naming a
  non-existent prover lens, repointed to spec-author's real pole-declaration duty). Record:
  `docs/prover/2026-07-15-332-inv163-audit.md`. Suite green.

- **INV-162 sharpened (a shared install path is not a safe kill target).** Harvested from the
  track-coach inbox wish (ROADMAP 335): where two sessions drive their browsers from one shared install
  path, a path kill reaches the other session's live browsers, so a recorded PID / process group is the
  only always-in-run target. Single-clause spec sharpening; a red-first test asserts the new fact.
  Meaning check: the clause ADDS a boundary (a shared path is unsafe) and weakens nothing; the prior
  install-path target survives, now qualified by "unique to this run". No obligation dropped.

## Safety machine (Phase 0, built this movement)

`scripts/spec-freeze.py` freezes and verifies the anchor occurrence map (a dropped mid-paragraph
citation is caught by count, not only by the unique set), the structural marker lines
([target] / [default] / H3 heading tags), and the literal classes (numbers-with-units, backticked
paths). Proven by a negative test: a dropped `[INV-159]` citation and a drifted `4.5:1` both go red.
This is the reusable compaction machine's mechanical floor and wires into the push gate.

The full meaning-preservation prover pass over the rewritten docs lands as the final record before the
2.0.0 push; this rolling record carries the pre-rewrite deltas.

## PRODUCT_SPEC prose readability batch (2026-07-16) — style-lint to 0

The register lint over PRODUCT_SPEC.md is driven to zero errors (`spec-style-lint.py --gate` = 0). The
day's record with the full meaning-preservation reasoning is `docs/prover/2026-07-16-2.0-prose-batch.md`;
the short version:

- **Caps de-emphasis (95 findings).** ALL-CAPS emphasis lowercased where the plain statement carries
  the force (register R12). Two families were instead recognised as defined closed vocabularies and
  allowlisted, not lowercased: the design-review loop rest-states CONVERGES / WAITS / STANDS DOWN
  [INV-154] and the push-gate reach-map categories EXPLICIT / CONSERVATIVE / SELF-TESTED [INV-45];
  BMAD (a prior-art framework name) allowlisted for the same reason. Seven test needles re-cased,
  logged in `docs/restyle-repoint-log.md`.
- **Scissors (5), second-person (6), reassurance (4), machine-jargon (2), negation-opener (1),
  future-narration (1).** Each rewritten to a positive plain sentence with its law intact. The
  INV-137 rule name, itself a scissors frame ("reads to decide, not to discover"), was renamed to
  the positive "reads to decide; discovery reads go to workers" across every living home (spec, base
  rule 25, lenses, tests); dated records keep the old name.
- **Linter refinement.** `spec-style-lint.py` now exempts a second-person word that lies wholly
  inside a double-quoted span — a cited product-copy literal (the read contract "needs your word:
  what, by when") is the product's voice, not the spec's register. One such span exists in the spec.
- **Safety.** spec-freeze re-frozen after verifying the marker-line count is unchanged (77 → 77) and
  every changed marker line keeps its exact marker tokens; full suite 809 green.

## PRODUCT_SPEC redundancy → 0 (2026-07-16) — genuine dedup, no waivers

The debt cap forbids waivers (max_waivers 0), so all 21 mechanical redundancy candidates were genuinely
deduped, not waived: each duplicated restatement was reworded to a pointer at the law's home, unique
conditions unioned into the survivor, the anchor OCCURRENCE map unchanged (verified against HEAD — no
anchor added or dropped). A Fable drafter did the rewording under the Phase-1 protocol; the lead verified
the three gates, the anchor map, and spot-read the condition-moving cases (the skill-review enumeration
stays whole at its INV-99 home; the sketch-door no-commitment condition rides "before keeping it"). Two
traceability needles keyed to a reworded restatement were re-pointed to the surviving home phrasing, and
one dedup that dropped a two-homes-pinned phrase (INV-110) was corrected by restoring the phrase and
reducing the sibling clause instead (docs/restyle-repoint-log.md). Result: redundancy open 0, style 0,
freeze GREEN, suite green.

## The other living docs (2026-07-16)

- **ROADMAP.md** — English-only: ~110 verbatim Russian quotes of the owner's words across 74 rows were
  paraphrased into English in place, anchors/dates/row text untouched; the Cyrillic grep is now empty.
  (ROADMAP stays outside the anchor-freeze set — it is a living queue whose rows churn by design — and
  outside the shipped-language gate's scan, which spares it as an internal diary alongside JOURNAL.)
- **ARCHITECTURE.md / TEST_MATRIX.md** — the genuine prose register defects fixed (a scissors frame, a
  second person, a one-off caps verdict); the semantic-version tier names (MAJOR/MINOR/PATCH) and the
  data unit GB were allowlisted as literal terms. The bulk of their caps are the matrix's status
  vocabulary (BUILT/TODO/DO/NEVER) and dated changelog rows — legitimate, not shout — so these docs are
  not driven to a zero the register was never meant to impose on a table; their baselines were re-frozen
  to the current freeze logic (the stale baselines had frozen table rows as markers).

## The recurrence-stop: the ratchet wired (2026-07-16)

So no adopting project's docs can silently re-bloat:
- **Live ratchet lock** (tests/test_convergence_locks.py, M-217): the real PRODUCT_SPEC.md is asserted to
  sit AT the reached-clean floor — style-lint --gate 0 errors and redundancy open 0 — so a re-grown
  defect reddens the suite. The floors live in scripts/spec-debt-cap.json (max_waivers / max_redundancy_open
  / max_style_errors, all 0) and ratchet downward only (test_debt_cap_only_downward).
- **Freeze gate** (guardrails/check-freeze.sh, pre-push gate k): verifies the guarded docs' anchor map,
  marker lines, numbers, and paths against the session baseline; skips gracefully where no local baseline
  exists (a fresh clone), the permanent locks being the live ratchet and the shipped-language gate.
- **Shipped-language gate** (already present, INV-120, pre-push gate i): the English-only / no-owner-name
  check on the shipped set.

## The method rule minted: INV-164 / base rule 30 (2026-07-16)

The recurrence-stop is also stated as a law that reaches every adopting project. INV-164 — "compaction is
continuous, a gate on every push, with the milestone whole-read above it" — went through the full
traceability chain: the spec prose clause + Formal-index row, base rulebook rule 30 ("a quality a machine
can verify is enforced by a gate, held by no attention" — the deeper principle, so it reaches all projects
through the pack), build-pipeline's gates section (a new bullet), the architecture (owned by the
build-pipeline node), the matrix (M-313 under the build-pipeline block), and its tests
(tests/test_compaction_discipline.py). The nine skills' base-pin bumped 1.0.18 → 1.0.19 for rule 30. Suite
815 green; style 0; redundancy 0; freeze re-frozen to bless the new INV-164 anchor.

## Compaction batch 1 (index rows INV-155..163)

Eight duplicate Formal-index mega-rows compacted to true one-liners. Each row's full law lives in its
prose clause, so the compaction removes only the redundant second statement. Verified: `spec-freeze
--compaction` green (no anchor vanished, none invented; every cross-ref survives in the prose clause,
confirmed by index-refs ⊆ prose-clause-refs); the unique kinship marker `[INV-125 kin]` kept on INV-159;
the pinned phrase "the verify-by-deed audit is the one deliberate difference" kept on INV-156. Style
errors 154 → 147; suite 809 green. Meaning preserved: the one-liner names what each rule requires and
points at its home section; the obligations stay in the prose clause unchanged.

## Compaction batch 2 (the remaining 60 index rows, Fable)

Six Fable workers compacted the remaining 60 duplicate Formal-index mega-rows to one-liners. Each row
validated mechanically before applying: every only-in-index cross-ref, every row-pinned test phrase, and
every provenance date kept; no citation invented (refs a subset of the old row's). spec-freeze
--compaction green (numbers and markers strict: INV-139's legibility thresholds restored to its row;
the freeze marker check now excludes index-table rows, which describe rather than declare a marker).
Eight traceability needles re-pointed (docs/restyle-repoint-log.md). Style errors 154 → 119; suite 809
green. All 68 duplicate index rows are now one-liners; their full law is unchanged in the prose clauses.
