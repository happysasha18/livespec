# Prover record — 2026-07-05, row 86 CROSS-LINK (work-kind axis)

Mode: **CROSS-LINK** (surface add per build-pipeline step 2). New surface: the work-kind axis —
T-16 (kind named at intake) + INV-22 (kind scales each step's form; applies-or-stands-down by name).
Spec under review: SPEC.md v0.15.0 draft (delta over the v0.14.0 state proven clean in
`2026-07-05-v14-push.md`). Previous record checked first: **zero unfolded rows** (all 3 findings of the
v14 push pass marked FOLDED; verified by re-grep of that file's findings table).

## Opening assessment

The delta is small and lands where its neighbours already live: the intake line (T-12) gains a third
axis, the ask-at-intake law (INV-12) absorbs the new uncallable case, and the scaling contract (INV-22)
is written as the same shape as appetite's (T-15): dials the machinery, never the safety net. The
strongest part is the explicit no-silent-skip postcondition — a stood-down step must be NAMED in the
landing report, which makes "we skipped architecture because it's just a script" a written fact instead
of an omission. Three findings below, all found and folded within this pass (M-6 locality held: every
fold lands in the sections the finding names). Ready to proceed to architecture.

## Seams checked (both sides present and named at prove-time)

- T-16 × T-12 — the intake line's axis count and composition (size+priority · door · kind)
- T-16 × INV-12 — the uncallable-kind ask; interim value; lane keeps moving (INV-4)
- T-16 × E-8/E-13 — host-default kind as a recorded profile line on the settings ladder
- T-16 × T-13/INV-18 — the facet sweep is kind-proof (fires on the feature door regardless)
- INV-22 × T-15 — same "never the safety net" law, stated as kin, no contradiction
- INV-22 × T-12/INV-16 — door picks WHICH steps, kind picks their FORM; tripwires untouchable
- INV-22 × E-18 — design-sync's applicability now reads off the kind (binds when row 93 lands)
- INV-22 × E-12/INV-13 — per-kind table's ONE normative home: build-pipeline SKILL.md; spec binds only the contract
- INV-22 × T-7 — the landing report carries stood-down steps by name (communicator elaborates the shape)

## Findings

| # | Finding | Tag | Status |
|---|---|---|---|
| F1 | **The host-default example broke the axis's own one-kind-per-wish law.** Draft T-16 read "a host has a usual kind … live-spec's own is skill-and-prose" — a TWO-kind default cannot seed a single intake value, so the first uncallable wish on live-spec itself would have hit an undefined read (classifier reaches for "the" default and finds a pair). Fix: a host with ONE usual kind may record it; a host spanning kinds records none and calls each wish on its own. | must-fix · internal-conflict (consistency) | **FOLDED** — T-16 sentence rewritten (track-coach example = product; live-spec records none) |
| F2 | **An unresolved kind had no stated effect on scaling.** INV-12 lets the kind question ride the row while the lane moves — but nothing said what INV-22 does meanwhile: a walk could stand steps down against a GUESSED kind, which is the silent micro-decision INV-5 forbids. Fix: standing a step down requires a NAMED kind; an unresolved kind scales nothing down. | should-clarify · missing-rule (invariant) | **FOLDED** — sentence added to INV-22; INV-12's interim parenthetical points at it |
| F3 | **Door-vs-kind division of labour was inferable, not stated.** A docs-only wish never reaches the matrix step — is that a stood-down step INV-22 must name? Ambiguity between the door choosing steps and the kind shaping them. Fix: one clause — the door picks WHICH steps run [T-12]; the kind picks the FORM each running step takes. A step the door never grants owes no stand-down line. | worth-considering · boundary-issue (composition) | **FOLDED** — INV-22 opening clause rewritten |

## Facet sweep note (T-13, for the record)

The feature's only surfaces are conversation text (intake/landing lines) and a queue-row cell — **no
rendered visible surface: facets N/A**, stated here explicitly per T-13, never silently.

## Non-goals and success measure (INV-20, INV-21, for the record)

- Non-goals: no mechanical guardrail checks the kind's presence at intake this landing (prose law +
  string-level suite rows only; a guardrail leg may ride the host-facing set later); no per-kind document
  templates; design-sync's real wiring stays row 93's.
- Success measure: within the next milestone, every new queue row names a kind, and at least one
  non-product landing's report shows a step standing down BY NAME rather than silently skipped — checked
  by eye until the reading machinery lands `[default]`.

## Verdict

Zero unfolded findings. The delta composes with its neighbours; proceed to the architecture step.
The push-gate FULL pass (M-6) still runs before this session's push, as always.
