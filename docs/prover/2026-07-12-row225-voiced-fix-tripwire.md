# Prover — row 225, the voiced-fix tripwire (2026-07-12, short form per INV-61)

Small delta (kind: skill; architecture = anchor assignment INV-104 → build-pipeline).

- **Previous records:** 2026-07-12-row255-drafter-applier-form clean (0 must-fix); no unfolded rows.
- **The delta in one line:** the bug door and the skip door carry one added door-step tripwire —
  does this edit touch a spec-backed literal or clause (a version string, a pinned count, a named
  vocabulary, a promised wording)? — a yes binding docs-travel and the red-first small-fix path
  into one same-session duty; the tripwire reads the edit's CONTENT, so a one-word change owes what
  a feature owes (INV-104, clause + index beside the door law; the bug bullet + the skip boundary in
  the pipeline; M-239; four string tests red-proven). Composition read: the door law's existing
  tripwires untouched (this one ADDS, none weakened); the skip boundary keeps its three conditions
  and gains the duty, so the skip lane stays open and stops leaking doc sync; the row-249/252 class
  (spec-cited literals in hooks/matrix cells) is exactly the covered class.
- **Verdict:** ready to ship — 0 must-fix, 0 should-clarify.
