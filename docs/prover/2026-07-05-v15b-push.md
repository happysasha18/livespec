# Prover record — 2026-07-05 night (~23:05), v15b push gate (FULL re-check before push, M-6)

Mode: **FULL** (push gate). Spec under review: SPEC.md v0.15.0 + tonight's three proven deltas (T-16/
INV-22 work-kind · E-19 skill evals · E-20 publish + E-18 wired/target split) + this gate's own folds.
Previous records checked — `v15-push`, `row86`, `row93`, `row94`, `row98`: **zero unfolded rows** (every
must-fix marked FOLDED in place; re-grepped).

## Opening assessment

Since the last FULL pass (v15, ~21:00) the spec grew three surfaces, each CROSS-LINK-proven at landing
with folds applied locally. This gate's whole-document pass therefore hunted what per-landing passes
can't see: cross-surface consistency (three new machines naming each other), the honesty header against
the grown shipped set, enumeration rot, and the queue↔spec agreement. Two small findings, both folded
within the gate (locality held). Suite 68 green / 0 skips; fresh-clone run green from scratch (the
publish skill's own first real walk — record below). **Verdict: READY TO PUSH.**

## What the pass walked

- **Cross-surface composition of tonight's three machines:** the kind axis feeds both the pipeline
  (INV-22) and the publish checklist (E-20) — one vocabulary, checked by grep, no second wording; the
  eval law (E-19) binds the publish skill and CAUGHT it mechanically before its eval existed (deed
  recorded in row98); design-sync sits as one publish TARGET without restating its own law (INV-13).
- **S-0 honesty, both directions:** shipped list re-checked against the tree (six skills ✓ evals ✓
  guardrails slice ✓); target list re-checked (host-facing guardrails/registry [E-6,E-10] · snapshot
  [E-7] · CI mirror [M-5] · router [ACT-3] · design-sync MACHINE [E-18]) — no clause claims a sync or a
  registry ran. F1 below folded the shipped list's missing evals mention.
- **Enumeration rot sweep (the "two axes"/"fifth skill" class):** live surfaces clean; remaining
  four/five mentions sit in JOURNAL history and queue-row quotes of the human's words — history is
  never rewritten, correct to keep.
- **Index symmetry · anchor ownership 1:1 · matrix coverage · node↔block match:** mechanized, 68 green.
- **⟨DECIDE⟩ inventory:** 3 live (D-1 attic layout · D-2 tier routing · D-3 snapshot retention),
  untouched tonight.
- **Queue ↔ spec:** row 93 shows pack-side landed + remainder open, matching E-18's wired/target split;
  row 99 (appetite de-ceremonialization) waits on the human — T-15 UNCHANGED until his word, correct.
- **Publish walk on this very push (first real use, E-20):** floor — README current (six-skills table),
  claims true today, license explicit, secrets/personal-path sweep over the delta CLEAN; kind row
  (skill) — install + commands + when-NOT present in README/skills; GitHub target — release notes stand
  down (no tag tonight, said aloud) · description/topics unchanged-and-set · zero deps · name existing
  · fresh-clone check RUN: clone + suite = 68 green from scratch. The gate itself stays the human's
  standing arrangement for this repo (pushes ride the hooks; hooks green).

## Findings

| # | Finding | Tag | Status |
|---|---|---|---|
| F1 | S-0's shipped list omitted the evals machine — shipped-but-unlisted is not a lie, but the header is the map readers trust; an incomplete shipped list ages into "the spec doesn't know what it ships". | should-clarify · missing-outcome-check (postcondition) | **FOLDED** — "the skill evals with their run records" added to the shipped list |
| F2 | Row 99 (his appetite challenge) needed a queue home before the push so the open question survives any wipe (INV-1) — it existed only in chat. | must-fix · missing-scenario (state-space) | **FOLDED** — row 99 intaken as waiting-his-word; T-15 untouched pending it |

## Verdict

**READY TO PUSH.** Zero unfolded findings across all six of tonight's records; suite 68 green / 0
skips; fresh-clone green; folds local; the pushed state is the state reviewed.
