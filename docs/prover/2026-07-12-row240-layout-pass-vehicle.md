# Prover — row 240, the sanctioned light vehicle for an owner-scoped docs-layout pass (2026-07-12, short form per INV-61)

Small delta (kind: skill; architecture = anchor assignment INV-111 → attach).

- **Previous records:** 2026-07-12-row239-catchup-discriminator clean (0 must-fix); no unfolded rows.
- **The delta in one line:** a same-version docs-layout pass rides one sanctioned light vehicle — a
  host restructuring its own documents with no package-version delta (INV-110) runs one named shape:
  the owner's decisions locked in a checkpoint before any file moves (INV-107), the work built on a
  clean pushed base with a one-command restore, content proven by a word-token multiset check AND a
  punctuation multiset check (word-token identity alone passes a reflow that moved punctuation), and
  one journal chapter landed — spec clause (INV-111 + index row), build-pipeline's docs-only/
  restructure door, M-249, three string tests red-proven against the pre-delta tree. Composition
  read: no clash with the catch-up machinery (INV-89..INV-92, INV-110) — this vehicle is the sibling
  shape for the no-version-delta branch row 239's discriminator carves out, not a replacement for the
  heavy walk; composes with the checkpoint-discipline base rule and the restructure-safety proof
  already in the playbook, which the clause cites by name rather than restating. Born of the
  track-coach s63 audited pass (2026-07-10 ~14:23): track-coach, lacking a named vehicle, correctly
  improvised this exact shape for a pure docs-layout pass.
- **Landing dependency:** lands after row 239 — three of its anchors (the INV-110 index-row tail, the
  M-248 matrix-row tail, and INV-110 already present in the attach owns-list) sit on text row 239
  writes. Applied in that order; no anchor collision.
- **Verdict:** ready to ship — 0 must-fix, 0 should-clarify.
