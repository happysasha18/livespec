# Prover record — 2026-07-06 ~23:02, row 146 (fresh shopfront at every push), CROSS-LINK

Scope: the INV-44 delta (SPEC v0.15.41 — one paragraph in "Publishing" + index row) against its named
neighbours: the publish contract (E-20), the push gate (M-6, ACT-1), the landing report (INV-22), the
never-bend list (INV-40), the reach-map row (147, kin), track-coach's host README line (audit case).

Opening assessment: the law fills a real hole — the publish skill's fire-list was DIFF-triggered ("a
push that changes what a public reader sees") while the incident class is TRUTH-triggered (the push
changes the product under an untouched README). One seam finding folded by extending the fire-list;
the rest holds. Ready to build.

| # | Finding | Severity · category | Status |
|---|---|---|---|
| F1 | The fire-list contradicts the law at the seam: publish's "When it fires" names "a push that changes what a public reader sees (README, docs, screenshots)" — a diff-based trigger; INV-44 fires on every VERSION push even when no doc is in the diff; left as-is, a session reads the narrower list and skips the walk exactly in the incident case. | should-clarify · internal-conflict (consistency) | FOLDED — the fire-list gains "any push that ships a new version (the shopfront walk, SPEC INV-44)" and the new clause below it states the truth-based reason |
| F2 | "Version push" could be read as only-VERSION-file-bumping pushes, leaving a docs-typo push unowned. | worth-considering · over-specific (abstraction) | RECORDED, no change — the two triggers COMPOSE: E-20's diff-trigger already owns any push touching what a reader sees; INV-44 adds the version-trigger for untouched-doc pushes; together the space is covered |

Cross-links that hold without change: M-6/ACT-1 — the walk runs BEFORE the gates, the gates stay the
human's; INV-22 — the outcome line rides the landing report like any step's; INV-40 — an ADDED owed
check, no rung bends; row 147 — named as the candidate mechanical owner (non-goal here), no duplicate
law; track-coach's README-reshot-on-push host line — the pack law generalizes it, the host line stays
as its narrower instance. Sibling sweep: no other clause speaks of push-time doc freshness; the
prover-record freshness law (M-6 family) is gate-record freshness, different fact, no collision.

Findings: two — one folded in-pass, one recorded as composing by design.
