# Prover record — row 188: the architecture reads as an LLD reader expects

Mode: CROSS-LINK (format amendment to INV-74/75 against their own clauses and the template). Date:
2026-07-09 late evening, session 30. Prover skill version running this pass: 0.1.15. Spec under
review: PRODUCT_SPEC.md v0.16.11. Previous records: all folded.

## Findings

F1 — The fallback duty must not fork the seam table's ownership

> "the payload and format stay the seam table's fact" — the runtime-view template section

A fallback often IS a seam behaviour (a degrade the worker owns). Folded: the fallback cell states
what the READER of the flow sees happen; the mechanism's normative home stays wherever the spec/seam
already owns it — the cell is a walk sentence, never a second law. `should-clarify · boundary-issue`
— **folded** (the cell's examples read as observable outcomes).

F2 — The shape-at-a-glance section must not resurrect the deferred diagram mandate

> row 180's deferral: "an at-a-glance diagram stays the author's option"

Folded: the section owes two-to-five PLAIN LINES; a sketch stays welcome-never-owed, said in the
section itself. The deferral stands; the prose duty is new and small. `worth-considering ·
consistency` — **folded**.

F3 — Sibling sweep: our own doc and the derived tlvphoto doc

Both carried the old shape. Folded: ARCHITECTURE.md opens with the shape at a glance and every
runtime row carries its if-it-fails cell; the tlvphoto derivation re-rendered in the new shape
(v3, scratchpad) for his eye. `must-fix · sibling-instances` — **folded**.

## Closing

All folded; nothing rejected. Red proof: no shape-at-a-glance and no fallback column at HEAD.
Evidence for the wish itself: his 2026-07-09 ~23:31 word; the field counterparts named in the
crosswalk (BMAD architecture template: high-level overview, definitive tech-stack table,
error-handling strategy; Kiro design.md: overview + error handling; arc42 §6/§7).

## Row 189 addendum — the field-norm check he asked for (same lens family, same evening)

His ask ~23:44: verify the recommended contents against the sources themselves; adopt, never copy.
Research record: the session scratchpad's `arch-doc-norms.md` (primary sources: BMAD V4 templates +
v6 spine at github.com/bmad-code-org/BMAD-METHOD · kiro.dev/docs/specs · arc42.org/overview ·
c4model.com · industrialempathy.com/posts/design-docs-at-google + adr.github.io).

Adopted (each measurable here): a Decisions pointer section (arc42 §9 / ADR / BMAD-v6 AD-blocks /
Google alternatives-considered — ours is an INDEX to the queue/journal/decision-marks, one-home law
kept) · seam rows name where a crossing's schema lives (BMAD Data Models / Kiro data models) · the
placement view says where secrets live and which tier holds non-client verdicts (BMAD secrets
management / Google cross-cutting; INV-75 amended, spec v0.16.12).

NOT adopted, by name: Coding Standards and Monitoring Stack as sections (the craft ladder and the
budgets' instrumentation homes carry them; BMAD mandates them because downstream AI dev agents read
the doc literally — our pipeline reads the spec and matrix instead) · full database-schema catalogs
(Google's own line: trade-offs, never full schemas) · a mandated overview diagram (stays
welcome-never-owed). Already ours, verified against the table: overview/context (shape at a glance) ·
tiers/deployment · tech stack (placement table) · components (nodes) · flows (runtime view) · error
handling (if-it-fails) · testing strategy (the matrix, stronger than the corpus) · quality +
observability (budgets + instrumentation homes) · capability map (feature coverage — BMAD v6's
Capability → Architecture Map, independently converged) · deferred/non-goals (the queue's deferred
rows + the spec delta's non-goals sentence). Red proof for the three adoptions: none present at HEAD.
