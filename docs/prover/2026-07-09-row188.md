# Prover record — row 188: the architecture reads as an LLD reader expects

Mode: CROSS-LINK (format amendment to INV-74/75 against their own clauses and the template). Date:
2026-07-09 late evening, session 30. Prover skill version running this pass: 0.1.15. Spec under
review: PRODUCT_SPEC.md v0.16.11. Previous records: all folded.

## Findings

F1 — The fallback duty must not fork the seam table's ownership

> "the payload and format stay the seam table's fact" — the runtime-view template section

A fallback often IS a seam behaviour (a degrade the worker owns). Folded: the fallback cell states
what the READER of the flow sees happen; the mechanism's normative home stays wherever the spec/seam
already owns it — the cell is a walk sentence, never a second law. `should-clarify · boundary-issue`
— **folded** (the cell's examples read as observable outcomes).

F2 — The shape-at-a-glance section must not resurrect the deferred diagram mandate

> row 180's deferral: "an at-a-glance diagram stays the author's option"

Folded: the section owes two-to-five PLAIN LINES; a sketch stays welcome-never-owed, said in the
section itself. The deferral stands; the prose duty is new and small. `worth-considering ·
consistency` — **folded**.

F3 — Sibling sweep: our own doc and the derived tlvphoto doc

Both carried the old shape. Folded: ARCHITECTURE.md opens with the shape at a glance and every
runtime row carries its if-it-fails cell; the tlvphoto derivation re-rendered in the new shape
(v3, scratchpad) for his eye. `must-fix · sibling-instances` — **folded**.

## Closing

All folded; nothing rejected. Red proof: no shape-at-a-glance and no fallback column at HEAD.
Evidence for the wish itself: his 2026-07-09 ~23:31 word; the field counterparts named in the
crosswalk (BMAD architecture template: high-level overview, definitive tech-stack table,
error-handling strategy; Kiro design.md: overview + error handling; arc42 §6/§7).
