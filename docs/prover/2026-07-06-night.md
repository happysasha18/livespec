# Prover record — 2026-07-06 night session (session 11)

Passes this night, appended as they run. Previous record: docs/prover/2026-07-05*.md — zero unfolded
must-fix rows carried in (checked at open).

## Pass 1 — CROSS-LINK, row 101 (SPEC v0.15.12, new clause INV-25) — ~00:02

Scope: the done-claim answer clause (Who decides what) against its seams: base rule 13
(claims-need-primary-source), M-7 (version homes), E-13 (resolved contract), communicator rules 6/8.

| # | Finding | Severity | Folded / rejected |
|---|---|---|---|
| F1 | The clause promises the method version "read from that host's installed set" — but a host may have NO installed set (track-coach: not formally adopted; work may predate adoption). The feature's own first real run (re-answering his track-coach question) hits exactly this: the answerer either invents a version or stalls. Add the absent arm: no installed set ⇒ the answer says so plainly; an absent version is itself an answer, never invented. `must-fix · missing-scenario (state-space)` | must-fix | FOLDED — absent-version sentence added to the INV-25 clause (SPEC v0.15.12) and mirrored in communicator rule 11 |
| F2 | Communicator's new rule 11 risks restating base rule 13 instead of citing it (one home per shared rule, INV-13) — the walk-the-evidence core IS rule 13; only the exchange shape (claim → artifact → version, verified-vs-asserted aloud, the version line) is communicator's own domain. `should-clarify · boundary-issue (composition)` | should-clarify | FOLDED — rule 11 opens by citing base rule 13 and elaborates only the exchange shape |
| F3 | Kin surface, future: landing reports' "Done when: … — MET" lines are the same species of done-claim; today they cite evidence by convention, not by law. Not tonight's scope (his two quotes bound row 101); worth a later wish if a MET line ever ships unbacked. `worth-considering · missing-rule (invariant)` | worth-considering | RECORDED, not folded — outside row-101 scope by its own intake line |

Verdict: clause sound with F1 folded; no seam drift after F2. CROSS-LINK complete.

Post-walk correction (~00:12): F1's example was wrong in fact, right in law — the first real run found
track-coach DOES hold an attach record (`.live-spec/versions.md`, 2026-07-05: pack 0.5.3), so the
absent-version arm wasn't needed there; it remains correct for genuinely unadopted hosts and for work
predating adoption. The finding and its fold stand.

## Pass 2 — CROSS-LINK, row 102 (SPEC v0.15.13, new clauses T-17 + INV-26) — ~01:28

Scope: the one-story / close-only-whole clause (Throwing a wish) against its seams: T-15 (the two
knives), INV-12 (ask at intake), INV-1 (no wish lost), T-9 (parking), M-2 (LIVE-STATE supersession).

| # | Finding | Severity | Folded / rejected |
|---|---|---|---|
| F4 | A split could orphan provenance: two rows born of one spoken wish, neither citing the origin — the queue archives then lose the link between his words and the landings. Add: every row born of a split cites the one spoken wish [INV-1]. `should-clarify · reference-integrity (transitions)` | should-clarify | FOLDED — the citing sentence added to the T-17 clause |
| F5 | Sibling-instance sweep over the pack's OWN queue (the clause must not indict its own rows): row 12 (multi-leg, legs enumerated, still open — LEGAL: never closed), row 100 (landed pack-side, foreign-host leg named WAITING — LEGAL: the open leg is restated, not compressed). No live row closes over an unmet enumerated leg. `verification note, no defect` | — | RECORDED — the new law holds against the existing queue as-is |

The tlvphoto re-walk (the row's red test, walked here): under T-17 the fused row 5a (door + gallery)
is illegal at INTAKE — two things a person does and sees, two rows from the start; under INV-26 the
recorded "threshold story COMPLETE" beside the rejected gallery wall cannot close the row — half-done
is a status; under the M-2 arm, the adopt-era resume file filing the approved Room as "not yet
specified, later" while dropping it from LIVE STATE is the compressed-away leg the clause now bans.
All three sentences catch the real incident. Verdict: clauses sound with F4 folded.

## Pass 3 — CROSS-LINK, row 104 (SPEC v0.15.14, INV-24 second arm) — ~01:35

Scope: the same-day-time pre-commit fence against its seams: INV-11 (the same hook), M-6 (push walk),
the suite.

| # | Finding | Severity | Folded / rejected |
|---|---|---|---|
| F6 | The hook composition: the concurrent-edit fence is OPT-IN (armed by a fence file), but the clock is law — the time check must run even when the fence is unarmed. The wiring places the time check before the fence's early exit. `should-clarify · boundary-issue (composition)` | should-clarify | FOLDED — wired above the fence's early-exit in guardrails/pre-commit |
| F7 | Two accepted residual holes, named: (a) `git commit --no-verify` bypasses any hook — the known limit of every hook gate here, not new; (b) CHECK_TODAY/CHECK_NOW env overrides exist for tests and could in principle silence a run — this fence targets the HABIT (a hand guessing time), not an adversary; the suite's fixture tests keep the script itself honest. `worth-considering · unenforceable-promise (discharge)` | worth-considering | RECORDED — accepted, both named here rather than implied |
| F8 | The deliberate cost (a plan naming a future moment must not pair today's date with a clock time on one line) is a WRITING constraint the sessions must know — it lives in the SPEC sentence itself, and tonight's own resume file already complies (the wake plan is spelled without a paired stamp). `verification note` | — | RECORDED — checked by deed against NEXT_STEPS.md |

Verdict: arm sound; the ledger's TIME-variant entry flips to SOLVED at this landing.

Post-deed finding (~01:40), F9: the fence's FIRST LIVE RUN blocked the landing's own commit and split
its catches two ways — real ones (a journal heading written one minute ahead; five stale "~01:40"
references to a moment that never happened) and an over-broad reading: line-global date+time pairing
flagged the ledger's occurrence lists, which legally mix today's date with quoted past times. The
mechanization was narrowed to the ADJACENT stamp shape (`date [~]time`), faithful to the clause's
word "pairs"; a fourth fixture test pins the mixed-history line green. `must-fix · over-general
(abstraction)` — FOLDED same hour: script narrowed, SPEC sentence states the adjacency reading, M-110
prose updated. The fence catching its own author's landing, twice over, is the family's whole story
in one commit.
