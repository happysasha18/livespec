# Prover cross-link — SPEC humanize batch: "Starting a new project (bootstrap)" (2026-07-07, session 25)

Register rewrite of one scenario section. Gates green: 18/18 tested phrases re-matched section-scoped;
bracket-code multiset identical to baseline (incl. the four [E-13], the two [T-16], and the full-note
bracket `[INV-9 is this rule's ceiling: mode and trust move only on their word]` copied character-for-
character); full suite 175 green. Clean on first splice.

## Facts carried (all KEPT)
- BLOCK A (version-control gate): gate runs FIRST, same order as adoption [A-0]; git exists (init if not),
  remote settled or explicitly declined first, because a gate cannot protect files older than itself;
  copy the six templates plus the suite scaffold (`test_scaffold.py` into `tests/`) that DEFINES green for
  landing #1 (docs present, headers filled — a leftover placeholder is red — coverage checklist, one
  live-state block); green is a floor not a ceiling; Hooks are OFFERED at bootstrap exactly as at adoption
  [E-6]; first wish then runs from intake [B-1]; always-rule: no landing into an unversioned host, remote
  recorded not merely recommended [INV-8].
- BLOCK B (founding questions): The founding questions are asked, never inferred; first is personal tool,
  or reusable product?; resolves like any setting [E-13], profile answers or the pack asks, never from
  examples; deliberately STRONGER than the walk's proceed-on-default habit [INV-4, INV-12]; a founding
  answer blocks the FIRST wish; inferring it is the silent micro-decision [INV-5] at its most expensive;
  A-1 carries the pointer; origin story 2026-07-05 [B-2].
- BLOCK C (learns who): The pack learns WHO it is working with; look for the personal profile at its one
  home [E-13]; load + SAY, ignore unrecognized lines aloud [E-13]; when absent OFFER to create from
  `templates/profile.template.md`; human tells (chat and docs language, address, work, vocabulary), may
  name sources, pack PROPOSES lines; every line on the human's word — a proposed line is accepted or
  dropped one at a time [INV-9 is this rule's ceiling: mode and trust move only on their word]; may decline
  whole step (package defaults, offer returns next setup); a worker session never onboards anyone, brief
  carries its setting lines [ACT-3]; placeholders marked so nothing passes for the human's word [B-3].
- BLOCK D (project kind): The project knows what KIND of thing it is; second founding question, one line
  in the HOST profile (`project.kind`, the settings ladder's host scope [E-13]); adoption owes the same
  [A-1, B-2]; NOT the per-wish work-kind [T-16]; three verdicts kept as a list (project kind seeds defaults
  [T-13, INV-30]; wish work-kind; placement [INV-37]); seed proposes, written line disposes, a recorded
  `work-kind.host-default` [T-16, E-13] keeps it — the project kind never silently overrides an explicit
  profile line; ask is always the HUMAN's; kind vocabulary CURATED [T-16], custom kinds via the queue; the
  line is ALIVE, updates when the project outgrows its kind; attached-before projects owe no retro-ask,
  forward-binding [T-16 kin]; origin (his word 2026-07-06) rendered in plain English [INV-36].

## Wording changes worth naming (meaning intact)
- Long compound sentences split; the "three verdicts" pulled into a list; the "·"-separated kind list
  written as a comma list.
- The final Russian quote rendered as its plain-English paraphrase with the date (2026-07-06), per the
  English-only doc rule; no new quotation introduced.
- "a floor, not a ceiling" kept (a legitimate two-part description, not a scissors "X not Y" claim).

Verdict: **CLEAN.** Every fact and code carried; 18/18 tested phrases section-scoped; code multiset
identical (incl. the full-note bracket); suite 175 green. No must-fix.
