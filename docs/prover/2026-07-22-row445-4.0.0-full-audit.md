# Prover record — row 445 — fresh clean-context FULL audit of PRODUCT_SPEC.md v4.0.0

Pass run by product-prover 3.6.0 (pack VERSION reads 3.6.0 at audit time; the spec header already
carries v4.0.0 — the version sweep is a later landing step). FULL mode, the whole document, every
phase, plus the four migration lenses the brief named: glossary closure, cross-requirement seams,
the [GAP] lines, and the generated Reference table. The reviewing seat is a fresh context with no
part in the migration that produced the document (INV-237); every claim below was checked against
the file on disk, the Reference table re-derived independently, and the cited tests actually run.

Subject: /Users/sashaabramovich/live-spec/PRODUCT_SPEC.md (565,735 bytes, 276 requirements,
1,301 criteria, 196 glossary entries, 42 [GAP] lines). Companion read whole: ARCHITECTURE.md.

## Verdict

**LANDS WITH FIXES.** The conversion itself is sound — the Reference table is exact against the
body (verified by independent re-derivation, 333 codes, zero location mismatches), the criteria
read clean, and the [GAP] discipline is honest in 40 of 42 lines. Eight MUST-FIX items stand, all
small, none structural: one false GAP, three glossary/intro claims the body falsifies, two marker
conventions the body does not practice, the unreconciled companion architecture, and the format-law
clauses the delta promised the conversion delivery. Two context facts frame the verdict and are
findings of the tree, not the document: the suite is red against the new format (80 of 171
traceability tests fail; test_scenario_heading_tag.py and test_formal_index.py read the retired
shape) — known and owned by the re-pin sweep running in parallel (LANDING-CHECKLIST step 7), and
the push stands blocked on the suite log's own green regardless of this record.

Counts: **8 MUST-FIX · 7 SHOULD-FIX (queue rows) · 5 observations.**

## Opening assessment

The document restates the whole live-spec method in a requirements genre: glossary, then 276
requirements each carrying Context, one User Story, and acceptance criteria in named cases with
trailing code anchors, closed by a generated code-to-location table. What works: the criteria are
individually precise and testable; the *when/shall* discipline holds; the GAP lines turn what the
old prose left vague into named, honest holes instead of invented answers; the anchor bookkeeping
is mechanically perfect. What needs attention: the document's statements ABOUT ITSELF — the intro's
genre description, the glossary's claims about naming and about the index, the marker conventions —
were authored fresh at conversion and several are false against the body they describe; and the
companion ARCHITECTURE.md still describes the retired Formal-index shape. Confidence: ready to
ship once the eight folds land; nothing found asks for a re-conversion.

## Phase 1 — the model (summary) and what I assumed

Entities: wish/queue row (ROADMAP), spec + spec-delta, architecture node, matrix row, lane/pen,
worker/brief/checkpoint, host/profile/settings ladder, agent/card/inbox message/contract, prototype/
norm, ledgers (feedback, problem, waiting board, decisions), the gates and judges. Actors: the
person (owner), the seat (also named senior agent/orchestrator — see F3), workers, remote seats,
strangers, the monitor. Wish lifecycle closes: captured → classified → specced → proven → built →
landed | declined | superseded (terminal), with deferred (trigger) and far (no trigger) as resting
states — see F9 on the end-state enumeration. Message lifecycle closes (delivered | declined |
escalated) with its watcher hole already GAP-flagged at R196.15.

Assumed: (a) this audit is the milestone/major-gate FULL pass for the 4.0.0 migration, so the
restructure-merge law applies — a defect the migration created blocks, a defect equal on both
sides routes to a queue row (INV-114); "new side" below means text authored at conversion (intro,
glossary, GAP lines, marker conventions, Reference), "pre-existing" means content carried over.
(b) The suite red and the two spec-vs-skills renames (delivery report / landing report; harness
task panel / task list) are owned by the running re-pin sweep and are context, not new findings.
(c) I found no authoritative surface for the format-law requirements INV-250..271 named in this
document; if their home is somewhere other than this spec, F8 falls away — say so.

## Findings

Disposition column left open for the landing to fill (folded / rejected + why).

---
**F1 — A GAP claims critical is undecidable while the document decides it three lines of law away**

> "[GAP: the source orders waiting bugs critical-first but names no judge or measure for a bug's
> priority, so which bug counts as critical is undecidable at the line.]" — Requirement 160,
> criterion 3

R9.4 states the measure in full: a wish is critical "*when* the shipped product is broken for its
user — an unusable surface, lost data, or a violated safety gate," and R37/R38 route the mark. A
maintainer reading R160's GAP opens a work item for a hole that does not exist, and the GAP
convention's own promise (line 7: "the honest output for a real hole") is broken by a false member.

Delete the GAP line at R160.3, or replace it with a pointer: "critical is decided by R9's three
named conditions." — This is the one GAP of 42 whose answer exists in the document (the brief's
lens c).

`defect · MUST-FIX · direct-contradiction (contradiction)` | disposition: open

---
**F2 — The glossary defines cross-link mode as the opposite of what Requirement 66 states**

> "**cross-link mode** — the prover's whole-document pass that follows every cross-reference and
> re-verifies each quantified claim over the linked set." — Glossary

R66's Context says the reverse: "A seam-scoped pass misses these, so the cross-link mode carries
one mandatory whole-document step." Cross-link mode IS the seam-scoped surface-add pass carrying
one whole-document step (the quantifier re-verify); the glossary promotes that one step to the
definition of the whole mode. A host running the mode per the glossary re-proves the whole document
on every surface add — exactly the cost the mode exists to avoid.

Rewrite the entry to R66's meaning: "the prover's focused pass at a surface add, scoped to the new
surface's seams, carrying one mandatory whole-document step — the quantifier re-verify."

`defect · MUST-FIX · direct-contradiction (contradiction)` | disposition: open

---
**F3 — The glossary claims the document keeps one name for the seat; the body keeps two**

> "…the source also names this actor the senior, the senior agent, and the orchestrator, and this
> document keeps the one name seat throughout." — Glossary, **seat**

Grep: "senior agent" appears 34 times (Requirements 100, 206–218 headings and criteria among them),
"the seat" 30 times, and the glossary carries a second entry **senior agent** for the same actor.
The one-surface-one-name rule the glossary's own preamble states ("a term appears once, under one
name") is broken for the document's most-used actor, and the claim "keeps the one name seat
throughout" is false as written.

Either sweep the body to "seat" and demote the senior-agent entry to a see-reference, or drop the
"keeps the one name throughout" claim and state the two names as deliberate synonyms in one entry.
The first is the glossary preamble's own law; I recommend it.

`defect · MUST-FIX · internal-conflict (consistency)` | disposition: open

---
**F4 — The document still claims descriptions live in a Formal-index field the migration removed**

> "the system *shall* keep each code's description in one dedicated description field the Formal
> index carries, written once there and read by every reference. [INV-239]" — Requirement 191,
> criterion 4 (also the R191 Context and the glossary entry **description field**)

The migration replaced the Formal index with a generated code-to-location table that carries
locations only; the delta's own decision (row-445 record, INV-271) moved each code's plain
statement to the criteria and the glossary and retires check-description-field.py with that stated
successor. A reader following R191.4 or the glossary to the Reference table finds no description
field at all; the document contradicts its own closing section and the migration's recorded
decision. R191.7's mandated gate is the one slated to retire.

Reword R191.4, the R191 Context sentence, and the glossary entry to the new home (the criterion
and glossary sentence as the description's home; the Reference table as locations only), and align
R191.7 with the successor mechanism the delta named.

`defect · MUST-FIX · direct-contradiction (contradiction)` | disposition: open

---
**F5 — The intro and glossary describe a document shape this document does not have**

> "Each section states one scenario — what the reader does and what the reader sees — and the body
> of each is a list of requirements." — header, line 3; also "**scenario** — …the spec's body is a
> list of scenarios" (glossary) and "every domain noun the ten assembled sections introduce"
> (line 11)

The body is a flat list of 276 requirement sections — each `##` section IS one requirement, not a
scenario containing requirements; only 17 requirement headings carry a `[feature:]` tag; and the
document shows no ten-section structure a reader can find (the conversion's own LANDING-CHECKLIST
counts an 11-unit assembly). A cold reader is told to look for scenarios-containing-requirements
and for ten sections, and finds neither; the "ten sections" phrase is also assembly provenance in
the normative body, the class the style lint's provenance-narrative rule bans.

Rewrite the two intro sentences and the scenario glossary entry to the actual shape: the body is a
list of requirements; a requirement heading may carry a `[feature: F-…]` tag marking it a
person-facing scenario; drop or correct the assembled-sections count.

`defect · MUST-FIX · internal-conflict (consistency)` | disposition: open

---
**F6 — The marker conventions the header declares are not the conventions the body practices**

> "A `[target]` marker on a line of its own marks a feature or leg that is promised but not yet
> built… `F-` a feature, which a scenario heading carries as a `[feature: F-...]` tag." — header,
> line 7

Three breaks, mechanically verified: (a) the body contains zero line-of-its-own `[target]` markers,
while planned work is marked by prose (R1.4) and by a bare lowercase `target` token inside the
trailing anchor brackets at R102.2 and R102.4 — a token outside the declared code alphabet, which
the index generator silently drops; (b) six feature codes ride in trailing brackets, not on
headings — `F-wish` inside criterion R4.4 and `F-bootstrap`, `F-adoption`, `F-catchup`,
`F-onboarding`, `F-pair` inside the R169/R177/R180/R186/R187 User Story lines; (c) consequence for
the trace: ARCHITECTURE's feature-coverage table maps 16 features but only 4 distinct features are
heading-tagged in this document (F-feedback, F-feature-map, F-bug, F-problem-ledger), so the
two-way trace R224.2 demands (every row a tagged scenario) has no heading to resolve 12 of 16
features to, whatever the rebuilt test reads.

Fix mechanically: put `[feature: F-…]` tags on the owning requirement headings for the 12 untagged
features; replace the two bracket-`target` tokens with the declared line-of-its-own form (or amend
the header to the convention actually used and update R1/R40.3's wording in the same stroke); move
`F-wish` off R4.4's anchor list.

`defect · MUST-FIX · unenforceable-promise (discharge)` | disposition: open

---
**F7 — The companion ARCHITECTURE.md still describes the spec shape 4.0.0 retired**

> "Every spec fact (anchor in PRODUCT_SPEC.md's Formal index) is OWNED by exactly one node." —
> ARCHITECTURE.md line 37; also "the D-x rows of the Formal index" (line 291), "ten skills" (line
> 29), header "Last reconciled with the spec: 2026-07-17" (line 4)

The Formal index no longer exists; D-1/D-6/D-7 moved to DECISIONS.md at the conversion (the spec's
GAP lines at R179/R187 now point there); the pack ships eleven skills (ten working + base — the
spec glossary and README both name text-audit, the architecture does not, and no node or roster
line homes it, against R199's identical-set law). The M-6 push gate proves the architecture beside
the spec, and a companion that names retired structures fails that read the day the push runs.

Run the architecture reconciliation before the push: repoint the two Formal-index references to the
Reference table / DECISIONS.md, correct the skills count, home text-audit (a node or a stated
reason it needs none — it is a working skill with a SKILL.md body, so the coverage rule's "a node
that owns nothing / a body no node owns" both apply), and refresh the reconciled-date line.

`defect · MUST-FIX · boundary-issue (composition)` | disposition: open

---
**F8 — The format-law requirements the delta promised the conversion delivery are absent**

> "the delta's clauses land at the conversion delivery per its own arming rule (INV-270)" —
> docs/prover/2026-07-22-row445-spec-format-delta.md; grep INV-250..INV-271 in PRODUCT_SPEC.md: 0
> hits, and the Reference table's highest invariant is INV-249

The seven format gates are to be armed at re-pin (checklist step 7), yet the converted document
carries none of the requirements (INV-250..271) that state the format laws those gates enforce. A
gate armed with no spec clause behind it is machinery the spec does not name — the exact class the
build-never-contains-what-the-spec-doesn't-name law (R102.3) exists to stop, and the next prover
pass would flag every format gate as an unowned check.

Land the delta's six requirements into the spec in the same delivery that arms the gates (or, if a
different home was decided for them, record that decision and point the gates' headers at it). If
this is already sequenced inside step 7, fold this finding by citing where.

`defect · MUST-FIX · missing-rule (invariant)` | disposition: open

---
**F9 — The three-end-states enumeration excludes the far status the queue defines**

> "A wish can end without landing, and its row stays in the table in one of three end-states:
> declined… deferred… or superseded." — Requirement 96, Context and criterion 1

R94 defines a fourth resting state: far — "no revisit trigger and no plan to run." A far row has
ended without landing at least as much as a deferred one has, yet R96's closed enumeration omits
it; this is the quantifier-re-verify class the spec itself legislates (R66): a member list a later
member falsified. Pre-existing on both sides of the migration, so it routes to a queue row, not a
block.

Add far to R96's enumeration, or add one decided sentence stating why far is a kept state and not
an end-state (my read: the second is truer — far rows have no exit event at all — but it must be
written).

`defect · SHOULD-FIX (pre-existing → queue row per INV-114) · missing-scenario (state-space)` | disposition: folded 2026-07-23 (row 461 landing)

---
**F10 — The isolation-default rule has two verbatim homes while the spec claims it has one**

> "*when* two lanes' write-sets overlap, the system *shall* default the later lane to worktree
> isolation, its copy reaching the shared tree only through integration under the pen." —
> Requirement 256, criterion 6; near-verbatim twin at Requirement 83, criterion 3

R88.1 vendors a host line that "cites the isolation law's write-set condition rather than restating
it, keeping the condition's one home," and R90.5 keeps "the isolation condition's one home" — while
the document itself states the condition twice in full. The redundancy gate R132.1 demands zero
open pairs; this is one. Pre-existing (both statements carried over from source sections).

Pick the home (R256.6, beside the fence and the canonical state directory, matches the
architecture's ownership note) and turn R83.3 into a citation of it.

`defect · SHOULD-FIX (pre-existing → queue row) · internal-conflict (consistency)` | disposition: folded 2026-07-23 (row 462 landing)

---
**F11 — The scope-not-time rule and its GAP are stated twice**

> "…*shall* renegotiate a wish too big for its worth in scope rather than in time. [GAP: the judge
> and measure of a wish's effort against its worth are unstated…]" — Requirement 40, criterion 1;
> the same rule and the same hole at Requirement 10, criterion 2

One fact, two homes, and the same GAP filed twice under two wordings — a later fix to one leaves
the other stale. Compaction class: keep R10 (the rule's own requirement) as the home, cite it from
R40.

`recommendation · SHOULD-FIX (queue row) · over-specific (abstraction)` | disposition: folded 2026-07-23 (row 462 landing)

---
**F12 — The declared keyword set omits *where*, which the body uses seven times in italics**

> "The keywords *when*, *while*, *if*, *then*, and *shall* are set in lowercase italics and carry
> their standard requirements meaning" — header, line 9; `*where*` occurs 7 times (R14.1, R113.2,
> R121.4 among them)

A reader meeting italic *where* has no stated meaning for it, and a keyword gate armed on the
five-word set at step 7 reds seven lawful lines the day it lands. Add *where* to the declared set
(one word) before the format gates arm.

`defect · SHOULD-FIX (one-word fix; becomes MUST-FIX if the keyword gate arms first) · missing-rule (invariant)` | disposition: folded 2026-07-23 (row 462 landing)

---
**F13 — The fix-sized/story-sized GAP overstates: the boundary is derivable from the glossary**

> "[GAP: the source separates a fix-sized comment from a story-sized one but states no measure or
> judge for the size boundary…]" — Requirement 154, criterion 2

The glossary's **user story** entry plus R13's one-wish-one-story law give a usable test: a comment
that names a distinct new thing a person will do and see is a story and queues; a comment that
doesn't is fixed in-session. The GAP is half-true (no numeric measure) but "undecidable at the
line" oversells; soften it to cite the user-story test as the decider.

`recommendation · SHOULD-FIX (queue row) · over-general (abstraction)` | disposition: folded 2026-07-23 (row 462 landing)

---
**F14 — "activity generation" is used as a term of art with no glossary entry**

> "The system *shall* surface an item at most once per activity generation, reading the generation
> from comments that are not its own markers" — Requirement 254, criterion 3

The stranger-monitor's dedupe key rests on this term; a maintainer implementing R254 cannot tell a
generation's boundary from the two uses alone. Add the glossary entry (one sentence: what advances
a generation).

`recommendation · SHOULD-FIX (queue row) · confusing-for-users (cognitive-load)` | disposition: folded 2026-07-23 (row 462 landing)

---
**F15 — Grammar defect in a criterion: "shall born it"**

> "the system *shall* born it onto the waiting board already carrying the default the work took" —
> Requirement 241, criterion 1

Meaning is recoverable ("shall bear it onto" / "shall create it on"), but a normative *shall*
clause with a non-verb reads as an unfinished sentence to the outside reader the register laws
serve. One-word fix.

`recommendation · SHOULD-FIX (fold with any F-cluster edit) · confusing-for-users (cognitive-load)` | disposition: folded 2026-07-23 (row 462 landing)

---

Observations (no action owed beyond noting):

- **O1 — Reference table verified exact.** Independent re-derivation of every trailing anchor
  against the table: 333 codes, all locations match both ways. The only asymmetries are the two
  convention anomalies F6 names (`F-wish`, `target`), which the generator silently drops — once F6
  folds, add the generator's own red on an unknown token so the next stray never hides.
- **O2 — GAP discipline is strong.** 42 GAP lines; 40 name genuine source holes, several
  exemplary (R212.4's three-vs-four category mismatch; R216's self-aware repeat of R210's
  small-edit hole). Only F1 is false and F13 overstated.
- **O3 — R3's surface-registry planned-vs-owned GAP** is real and already owned: the landing
  checklist's step 10 queues "the surface-registry status defect." No second row needed.
- **O4 — Suite state at audit time:** full suite run by this audit: **398 failed, 1404 passed,
  2 skipped (228 s)** — the reds are the content-reader tests against the new format (80/171 in
  test_traceability.py; all of test_scenario_heading_tag.py's shape reads; test_formal_index.py
  splits on a heading that no longer exists) — expected mid-flight, owned by the re-pin sweep;
  recorded here so the record is honest about the tree it read. VERSION still 3.6.0 (step 11).
- **O5 — Known renames** (delivery report / landing report; harness task panel / task list) are
  spec-side sharpens the checklist already routes to the re-pin sweep; not re-filed.

## Mandatory-sweep verdict table (INV-171)

Surfaces are the document's requirement families; verdicts: hit (finding filed) / clean / N/A+why.

| Family (requirements) | declared laws | edge conditions | cross-surface uniformity | lifecycle / payload | unwritten seams |
|---|---|---|---|---|---|
| Intake & queue (4–16, 37–48, 92–96) | clean | clean (GAPs carry the open thresholds) | clean | **hit — F9 (far vs end-states)** | clean |
| Pipeline doors & spec work (40–59, 71–76, 98–104, 124) | clean | clean | clean | clean | clean |
| Communication & touchpoints (17–36, 236–241) | clean | clean | clean | clean | **hit — F15 (R241 clause unreadable as written)** |
| Review passes (60–70, 130–131, 213–215) | clean | clean | **hit — F2 (cross-link mode two framings)** | clean | clean |
| Parallel lanes & pen (77–91, 256–257) | clean | clean | **hit — F10 (isolation rule two homes)** | clean | clean |
| Tests & suite honesty (105–117, 223–226) | clean | clean (R109/R114 GAPs honest) | clean | clean | clean |
| Architecture (118–123, 244–245) | clean | clean | clean | clean | **hit — F7 (companion unreconciled)** |
| Adoption & founding (168–188, 268, 276) | clean | clean | clean | clean | clean |
| Feedback & ledgers (152–167) | clean | clean | **hit — F1 (false GAP vs R9)** | clean | clean |
| Agents, inbox, contracts (189–197, 250–255) | clean | clean | clean | clean (message terminal states GAP-flagged) | **hit — F14 (activity generation undefined)** |
| Delegation & economy (206–220) | clean | clean (GAPs carry the bounds) | **hit — F3 (seat / senior agent)** | clean | clean |
| Guardrail machinery (227–235, 242–249, 269–275) | clean | clean | clean | clean | **hit — F8 (gates without their format-law clauses)** |
| Document format: header, glossary, Reference | clean (three pack laws named with nets, R54.3) | **hit — F12 (*where* outside keyword set)** | **hit — F5, F6 (self-description vs body)** | n/a — no runtime lifecycle | **hit — F4 (description-field claim)** |

Declared-laws walk: the spec HAS its declared-laws home (R54.3: plain-language register,
clock-honest stamps, no self-certification — each naming a mechanical gate), and this document's
own surfaces answer them; clean. Classic CRUD/authorization tables: N/A for a method spec with no
user-mutated runtime entities and a single-owner trust model (R200, R251 state authorization in
full) — this table stands in their place per INV-171.

## Phase 3.5 — acknowledged gaps

The document's acknowledged-gap register is its 42 [GAP] lines plus the three open decisions moved
to DECISIONS.md (D-1 attic layout, D-6 pair queue view, D-7 engine-fact citation). Commentary
beyond F1/F13 (the two mis-filed ones): the highest-consequence honest GAPs are R196.15 (no watcher
moves a message to escalated — the one liveness hole in the agent-channel design; recommend the
receiving sweep as the natural reader, one clause) and R265.12 (the tablet touch+hover in-between
deliberately deferred — fine, but it is the one axis value a shipped visual host meets daily, so
the "later forcing step" deserves a queue row with a revisit trigger rather than open-ended
deferral). The rest are threshold/judge holes correctly parked on the seat's judgment with the GAP
as the record. `acknowledged · missing-rule (invariant)`

## Phase 4 — human and operational factors

The genre serves its reader: Context-then-story-then-criteria reads well cold, and plain-language
discipline holds (one grammar break, F15; the lowercase criterion openings in R189–197 versus
capitalized elsewhere is a taste matter, not filed). Domain language: no internal identifier leaks
into promised user-visible strings. Observability: every gate the document names emits the typed
failure line (R227) and the net-liveness meter reads the nets (R229) — good. One operational note:
the Reference preamble tells the reader the table is generated and hand-edit-proof but does not
name where a reader FINDS a code's meaning now (the criterion sentence + glossary); one pointer
sentence there would close the loop F4 opens.

## Phase 5 — closing

**Top fixes before the 4.0.0 push (the MUST-FIX list):** F1 (false GAP at R160.3 — R9.4 answers
it), F2 (cross-link-mode glossary entry), F3 (seat/senior-agent one-name claim), F4 (description
field claimed in a retired Formal index), F5 (intro/glossary self-description of the genre), F6
(target and feature-code marker conventions vs body practice, incl. the 12 features with no tagged
heading), F7 (ARCHITECTURE.md reconciliation to the new shape + text-audit's home), F8 (the
INV-250..271 format-law clauses landing with the gates they arm).

**Properties to paste in:** "A far row is a kept state, not an end-state: a wish ends without
landing only as declined, deferred, or superseded, and a far row rests until asked for" (or add far
to R96). "The isolation condition's one home is R256.6; every other statement cites it." "Every
message's need-by is read by the receiving sweep, which moves an overdue message to escalated."

**Open questions for the author (only what inspection cannot settle):** (1) F8 — was a different
home decided for INV-250..271 after the delta record? (2) F6 — is the intended 4.0.0 convention
line-of-its-own `[target]` (then fix R102) or bracket-carried (then fix the header and R1/R40.3)?

**Recommendations queued for a taste call:** F11, F13, F14, F15 (F12 rises to blocking only if the
keyword gate arms before the one-word fix).

**[default] census (FULL-pass duty):** 3 [default]-tagged sentences stand in the document —
R186.13 (card single-column on narrow viewports), R186.14 (card static, hover-free), R186.16 (card
render read-only, no live update). All three are 2026-07-10-era card facets; none older than the
current release cycle; listed here for the standing taste call.

**Readiness:** ready to build — lands with the eight folds; no re-iteration of the conversion
itself is warranted.
