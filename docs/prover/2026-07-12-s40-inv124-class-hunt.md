# Prover record — INV-124 (a confirmed bug drives a class hunt) — 2026-07-12 s40

Prover skill version at this pass: product-prover 1.0.4. Mode: CROSS-LINK short form (SPEC INV-61 — small
delta, skill kind, no new surface, no structure change).

## The delta

One invariant, INV-124: a confirmed bug drives a class hunt before it closes — four moves (find the
un-seen siblings · check the architecture · check the spec · escalate to the human). Homed in four places:
the F-bug spec clause + Formal-index row, build-pipeline's bug entry, product-prover's class lens (the
former "Sibling instances" lens widened), base rule 14 (sharpened from fix-the-class-in-view to
go-find-the-class + the escalate line). Owning node: build-pipeline (owns-list + M-265). Test:
`tests/test_class_hunt.py` (7 assertions, red-proven against the pre-delta repo tree, then green).

## Previous record's unfolded rows

`2026-07-12-s39-backlog-batch.md` — its findings were folded in-pass at s39 (1 must-fix + 2
should-clarify, all recorded resolved). No carry.

## Findings

**0 must-fix.** Cross-link checks walked:

- Owning node present (build-pipeline owns-list carries INV-124); matrix row M-265 sits under the
  build-pipeline block; Formal-index row present — the three suite-enforced cross-references hold.
- Composition clean: move 3 (fix the spec first so the prover can flag it) rests on INV-15
  (green says nothing about an unspecced surface); move 4 (escalate) rests on INV-4/INV-5 (a gap only the
  human can fill rides its row while the lane moves); the close condition rests on INV-26 (a row closes
  only whole — a point fix leaving siblings standing is a status). No contradiction with the F-bug queue
  mechanics (park/resume) — the four moves govern the fix's completeness, orthogonal to lane order.
- The active-search sharpening of base rule 14 does not weaken the existing sweep (repo + every surface);
  it adds the architecture and spec moves and the escalate line, each cited to its home.

Verdict: buildable, landed. No open ⟨DECIDE⟩ touched by this delta.
