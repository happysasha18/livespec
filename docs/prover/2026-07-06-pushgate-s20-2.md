# Prover record — 2026-07-06 ~21:55, push-gate re-check 2 (session 20, SPEC v0.15.37)

Scope: the second correction batch on his word (~21:50). Delta since the s20 gate record: (1) the
budgets law widened — the project's KIND proposes what is measurable (backend latency/throughput/
errors, CLI/pipeline run time and per-unit cost, skill-pack eval pass rate and suite time, prose only
what honestly has a number; a no-honest-number quality said by name, never a vanity metric) — INV-41's
own seam to INV-36 now explicit; (2) communicator's calque rule gains the report-coinage example (his
«что это такое???» on "открытое плечо" — a translation after the fact does not fix it); (3) the host
profile gains `other-projects: AUDIT-ONLY` — another project named in this window is a case study for
the pack, never a task there (narrower than base rule 7's inbox exception; INV-14 journal note rides
this record's landing).

Walk: INV-41 vs INV-36 (kind proposes dimensions — no collision, the kind's ask stays always-live);
INV-41 vs the prose kind (the honest-boundary sentence prevents forced metrics on unmeasurable
qualities); the profile line vs base rule 7 (a host profile may NARROW a package rule for its own
window — exactly the settings ladder's shape, E-13). Suite 132 green; no unfolded rows ride.

Findings: none new; push may proceed with this record.
