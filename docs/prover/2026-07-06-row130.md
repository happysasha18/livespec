# Prover record — 2026-07-06, row 130 (CROSS-LINK, minimal), session 17

Mode: CROSS-LINK on SPEC v0.15.28 delta — one clause added to the milestone checklist (M-1): the
pack's skills re-walk the standard skill-making skill at every milestone, and a skill newly joining
the pack walks it at birth. Seams checked: E-19 (the skill evals — the neighbouring checklist item),
M-1's own list shape, E-12/INV-13 (the rulebook boundary — the clause changes no shared rule),
INV-26 (the walk's findings close whole: folded / rejected-with-reason / re-queued, never dropped).

Previous record checked: `2026-07-06-row133.md` — no unfolded rows (both findings folded in-pass,
verified in its summary table).

## Findings

None. The clause joins an existing enumerated checklist beside its nearest kin (the evals item) and
names the seam itself — evals test BEHAVIOUR, the skill-creator lens tests the CRAFT of the skill
file — so the two items cannot be mistaken for one home stated twice. The at-birth arm binds forward
only; no shipped skill owes a retro walk (the first full walk ran this session and its record exists:
`docs/audit/2026-07-06-skill-creator-walk.md`, 13 findings each with a written fate).

## Verified seams (no finding)

- E-19: the evals stay untouched; the clause adds a second lens, not a second home for the first.
- Record discipline: the walk's record lives in docs/audit/ (an audit, not a document review); this
  file is the SPEC delta's own prover record — two artifacts, two homes, each named here.
- Enforcement: matrix M-128 pins both the clause and the record's existence; red-proven before the
  record was written, green after.

Verdict: ready (shipped in the same landing).
