# Prover record — 2026-07-06, row 136 (pack update check, E-25), CROSS-LINK

Mode: CROSS-LINK — the new clause (E-25 + A-7's one-line cite) against its seams: A-7 freshness walk,
E-21 installer road, E-23 dev-machine sync (the inward twin), ACT-1/base-17 install gate, M-7 version
homes, INV-28 line law. Lane A of the first double-lane run (T-18); the spec-delta was drafted with
the pen while lane B (row 137) builds in its isolated tree — drafted against committed law only.

Previous record checked: `2026-07-06-row135.md` — no unfolded rows (7/7 folded in-pass).

Opening assessment: a small, honest machine — one daily question to the public repo, propose-never-
install, with the human's gate untouched. Both findings were edge honesty, folded in the same pass.

## Findings

F1 — A dead URL would masquerade as a quiet offline day, forever
> "No network — or an unreadable answer — reads as one honest 'check skipped' line" — E-25 (draft)
If the repo is ever renamed, every machine skips daily with no way to tell a dead address from a plane
ride; the proposal liveness dies silently. Fold: the skip line NAMES the address it tried — a dead URL
becomes diagnosable at first read.
`should-clarify · stuck-state (liveness) — FOLDED in-pass`

F2 — The developer's machine runs AHEAD of the public repo mid-work; unstated, that's a downgrade proposal
> "when the remote is newer it PROPOSES" — E-25 (draft)
Between a local landing and its push, installed > remote; a naive compare would propose "updating" to
the older public version. Fold: spec states forward-only — ahead reads as up to date, never a
downgrade proposal (the script compares by version order, not inequality).
`should-clarify · missing-scenario (state-space) — FOLDED in-pass`

## Seams checked clean
- A-7: the walk stays whole; it gains one citing sentence (the mechanism's home is E-25) — no second home.
- E-21: the proposal names install.sh as the road; the attic backup already guards it — no new install path.
- E-23: twinship stated in the clause (sync = local truth to copies; update check = public truth to
  awareness) — two tools, two names, no overlap.
- ACT-1 / base rule 17: propose-never-install keeps the human's gate; nothing crosses it.
- M-7: the walk hands the script the installed version from its recorded home; the script never guesses
  a home itself (--installed-file).
- INV-28: the check's face is one line; no bookkeeping-as-content (versions ARE the answer here — the
  carve-out's own case).
- Concurrency (two sessions, one day): worst case both check and both propose — benign, stamp write is
  last-wins; no fence needed for a same-day duplicate proposal.
- T-18 composition: lane A never touches base/build-pipeline/communicator SKILL.md (a base bump would
  re-pin citations inside lane B's files — the independence judgment held by DESIGN, recorded here).

⟨DECIDE⟩ open elsewhere: D-1/D-2/D-3 — untouched.

| # | finding | severity | status |
|---|---------|----------|--------|
| F1 | dead URL masquerades as offline | should-clarify | folded in-pass |
| F2 | ahead-of-public would read as downgrade proposal | should-clarify | folded in-pass |
