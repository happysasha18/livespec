# Prover cross-link — SPEC humanize: "Throwing a wish" CHUNK 6 (2026-07-07, session 25)

CHUNK 6 = four bold-leads: The door is named before any code · The intake line also names WHAT is being
built · A kind scales the steps · Each step is worked in its craft's mindset.

Gates (whole section after splice): 157/157 tested phrases section-scoped; code multiset identical to
baseline (165 occurrences, zero diffs); full suite 175 green. Chunk 6 carries 16 tested phrases and ~24
code occurrences, all preserved.

Bonus correctness fix: the old text twice referenced the prototype section by its OLD title "A prototype
is not the product"; that section was renamed earlier this session to "A prototype stays a sketch". Both
in-prose references updated to the current title (a stale cross-reference the earlier rename missed).

## Facts carried (all KEPT)
- The door is named before any code [INV-16]: three axes in one intake line (size · priority · door ·
  work-kind); door names feature · bug · refactor · docs-only · skip; size/door share one word; the door
  procedure kept as a numbered (1)-(5) list with each door bold and its tripwires; the tripwire verdict
  outranks a casual label [INV-5]; queue-cutting only the bug door [T-9], a re-doored wish takes no
  preemption; door re-checked mid-work; casual loading stays the contract.
- The intake line also names WHAT is being built [T-16]: work-kind = product/infra/skill/prose (pulled
  into a list), one kind per wish [INV-12]; host default [E-8, E-13]; The vocabulary is CURATED like the
  facet list [T-13]; binds forward.
- A kind scales the steps — it never skips one silently [INV-22]: door picks which steps [T-12], kind picks
  the FORM; per-kind table in build-pipeline [E-12]; each step APPLIED or STOOD DOWN by name; An unresolved
  kind scales nothing down [INV-12]; the kind dials the machinery, never the safety net [T-14, INV-18,
  INV-20, INV-21] [T-12, INV-16] [T-15].
- Each step is worked in its craft's mindset [INV-33]: the step→craft ladder pulled into a list (a strong
  product manager … a careful release hand … the visitor's own eyes, not the builder's [INV-30 kin]); one
  home build-pipeline [E-12]; the craft wears the KIND's face [INV-22, INV-30 kin]; his 2026-07-06 word
  paraphrased.

## Wording changes worth naming (meaning intact)
- The door procedure kept as a numbered list; the four work-kinds and the step→craft ladder pulled into
  sub-lists; long compound sentences split; the Russian craft-mindset quote paraphrased.
- Stale prototype-title cross-reference corrected (see above).

Verdict for chunk 6: **CLEAN.** Whole-section gates green.
