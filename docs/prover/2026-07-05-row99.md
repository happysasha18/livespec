# Prover record — 2026-07-05 night (~23:20), row 99 CROSS-LINK (scope-never-time re-authoring of T-15)

Mode: **CROSS-LINK** (re-authoring of a normative clause on the human's explicit word ~23:10; no new
surface). Previous records: zero unfolded.

## What changed and why

Alexander killed the time-budget mechanism outright: "какой дебил спрашивает программера сколько это
займет… можно играться со скоупом а не с таймлайнами". T-15 re-authored: a time budget or estimate is
NEVER an input; a too-big wish is negotiated in scope — cut (fewer surfaces, plainer defaults) or
staged (each stage a full-pipeline landing). The imported term retired from every live surface.

## Seams checked

- T-15 × INV-4/INV-5/INV-18 — the proceed-on-recommended + batched-surfacing machinery is UNCHANGED;
  only the trigger vocabulary moved from "won't fit its budget" to "bigger than it is worth".
- T-15 × T-11 — a scope cut still never moves lane order; not a quick-win mark (kept verbatim).
- T-15 × T-14/INV-18/INV-20/INV-21 — the protected mandatory-sentence list byte-for-byte intact (the
  v14 gate's F1 generalization survives the rewrite).
- T-15 × INV-12 — staging cites the existing "large decomposes into several landings" size law; no
  second staging concept introduced.
- T-15 × INV-22 — "the same law appetite obeys" → "the same law a scope cut obeys"; one law, two
  citations, no drift.
- History untouched — JOURNAL entries, queue-archive rows, prover records keep the old word (history is
  never rewritten); the never-again assertions scope to SPEC + working skills only.

## Findings

| # | Finding | Tag | Status |
|---|---|---|---|
| F1 | The retired term could re-enter through a future skill edit citing old muscle memory. | should-clarify · missing-rule (invariant) | **FOLDED** — `test_skills_carry_intake_trio` now asserts the term ABSENT from SPEC, build-pipeline, and spec-author (the three surfaces that spoke it) |

## Verdict

Clean; suite 68 green with the re-authored assertions red-proven against the pre-sweep skills first.
Rides the next push window's FULL gate.
