# Prover record — property routing: each cross-cutting law names its net (INV-150), the sharpened class trigger (INV-125), the paired-transition means half (INV-126), 2026-07-14

**Skill version:** product-prover 1.1.4.

**Wish:** the Fable audit of the boundary between the prover and the design review (scratchpad
`fable-prover-vs-designreview-audit.md`), realizing the inbox case
`2026-07-14-tlvphotos-openable-face-exit-symmetry.md`. Route each cross-cutting property to its owning
review, driven by the openable-faces case: a family of surfaces a visitor opens over everything and later
closes, where one member states the mirror principle in words and a pinch-to-open sibling closes only by a
control with a fade exit.

**Door / kind / footprint:** feature (a new declared law and two sharpened lenses — behaviour no clause
backed) · skill (a change to two reviews' method; each running step takes its skill form) · three-source
(spec + architecture + the two review skills, plus the matrix and its tests).

**Pass:** FULL-mode reasoning scoped to the delta's clauses, in the author's context. This record carries
the author-side verdict and the folds. The orchestrator runs the required independent fresh-eyes
adversarial pass (INV-46 — a new blocking obligation promoted from a soft recommendation) and the deep
whole-spec Fable pass before push.

## What the delta declares

1. **INV-150 — every declared cross-cutting law names its enforcing net.** A law is the prover's where its
   violation pins to a stated sentence (a clause, a declared law, a declared class, a facet) and blocks, or
   the design review's where the deciding fact lives only in the human's intent and it recommends. The net
   is recorded beside the law in the declared-laws home (INV-101); the INV-101 station demands one per law;
   a law with no named net ranks as a broken invariant, and a watch-level law names the design review with a
   dated reason (the shape of a dated exemption). Declaration is the promotion lever: an undeclared grouping
   is the design review's, and the moment the author declares it the property becomes the prover's. The
   architecture's every-fact-owned-once check is the backstop. This is the primary gap control the audit
   asked for.

2. **INV-125 sharpened** — the cross-surface trigger now also fires on a kind-general rule worded inside one
   member's own section while siblings of that kind exist (the tlvphotos "the way out mirrors the way in"
   homed on one member). This is textually recognizable before any class is declared, and the finding
   demands lift-to-class or scope-by-decision. This is the prover-catchable core that would have caught the
   miss with the design review asleep.

3. **INV-126 means half** — the paired-transition law now reads as two halves: continuity of the transition
   (unchanged, the prover's already) and reversibility of the means (new). Where a surface opens over
   everything by a continuous, reversible gesture, the same gesture reversed stands among its ways to close,
   or a decided sentence states why it is absent; silence blocks (the blank-answer class of INV-72), and the
   rightness of any deciding sentence stays the human's gate (INV-30).

4. **The routing split** is stated in the design-reviewer skill: a declared class (or a class-general
   sentence homed on one member) is the prover's defect (INV-125); a genuinely undeclared grouping is the
   design review's discovery (INV-141). Neither pass claims it twice and neither drops it.

## Seams checked

| seam | claim | verdict |
|---|---|---|
| INV-150 ↔ INV-101 | the net field lives in the declared-laws home; the INV-101 station verifies it | consistent — INV-101 clause + station both name the net; the pack's own three laws each carry the prover net, so the dogfood home is compliant |
| INV-150 ↔ architecture | the every-fact-owned-once check is the backstop, not the primary control | consistent — stated as backstop; INV-150 owned once (spec-author node); the prover node names the net-check by owner without minting a second ownership |
| INV-150 net-value: design review | a watch-level law can name the design review as its net | consistent after fold — the dated-reason requirement mirrors a dated exemption; no live watch-level law exists yet in the pack |
| INV-125 sentence-form ↔ declared-class enumeration | the sentence-form trigger catches what enumeration presupposes | consistent — enumeration presupposes the kind is declared; the sentence-form fires before declaration, closing that hole |
| INV-126 two halves ↔ INV-30 | rightness of a deciding sentence is the human's gate | consistent — silence blocks (presence check, prover's); rightness escalates (INV-30), matching the audit's "soft edge stays the prover's, taste routes to the human" |
| the openable-faces case | the case is caught by the delta with the design review asleep | the pinch sibling's missing reverse pinch is now an INV-126 means-half finding; the room's mirror principle homed on one member is now an INV-125 sentence-form finding — both blockable, no reliance on the pass drawing the group |

## Findings

**F1 — the soft-edge presence-versus-rightness split is stated per-law, carried generally only by reference (recommendation).**
INV-150 states the prover owns pinnable laws and blocks on a missing answer; the presence-versus-rightness
split (the prover checks a justification exists, the human judges whether it is good) is stated concretely
inside INV-126 (silence blocks, rightness is the human's gate) and INV-101 (the human's gates), rather than
as one general sentence in INV-150 itself. This reads fine — the general principle lives at INV-30 and each
facet cites it — and a reader following INV-150's cites reaches it. Queued as a taste call for the
orchestrator: whether INV-150 should carry one explicit sentence of the split, or keep it by reference.
`recommendation · abstraction`

**F2 — no live watch-level law exercises the design-review net value (acknowledged).**
The design-review net value is declared and testable in prose, but the pack's own declared-laws home
assigns all three laws to the prover, so the watch-level path has no dogfood instance. This is honest — the
pack has no watch-level cross-cutting law today — and the value is exercised the day a host declares one.
Noted so a later reader does not read the absence as a gap. `acknowledged · state-space`

## Folds

- **Folded (must-fix):** INV-150 originally named the design review as a possible net without the audit's
  dated-reason requirement for a watch-level assignment. Added: a watch-level law names the design review
  with a dated reason, the shape of a dated exemption. Clause, index row, and a test assertion updated.

## Verdict

The delta holds together as written: the openable-faces case is caught two ways by the prover alone (the
means-half finding and the sentence-form finding), the net field is verified at the INV-101 station on
every pass, and the design review returns to owning only genuinely undeclared groupings. No blocking defect
remains in the author-side pass. The suite is green at 718. The orchestrator's independent adversarial pass
(INV-46) and the deep whole-spec pass are owed before push — this promotes a soft recommendation
(reversibility of means) to a blocking obligation, named to Alexander as a bar-change and proceeding by
default, and the edit is kept isolated (one self-contained paragraph in INV-126) so it can be softened if
he waves it off.

## Fold — four review findings (2026-07-14, later worker pass)

A follow-up review folded four findings into this same lane, one coherent landing.

1. **INV-126 means-half scope widened.** The means half read "opened over everything by a continuous,
   reversible gesture", which quietly limited the obligation to a full-overlay opening. The overlay
   restriction is removed in every home (spec clause, product-prover lens, spec-author facet, matrix
   M-294): the clause now binds any surface opened by a continuous, reversible gesture — a drag-opened
   drawer or a partial pinch panel carries the inverse-or-decided-sentence duty the same as a full-screen
   layer. The rest of the half is intact (inverse among the closes or a decided sentence; silence blocks
   [INV-72]; the deciding sentence's rightness stays the human's gate [INV-30]).

2. **The net vocabulary carries three kinds, not two.** INV-101/INV-150 formerly called the pack's three
   laws "the prover's", blocked at a register/clock/self-cert "station". Those three are not prover
   judgment lenses — they are enforced mechanically. INV-150 now names three net kinds: a mechanical gate
   (a named guardrail script or a dedicated test, deterministic, blocking in CI), the prover's judgment
   station (the agent walk blocks on a stated sentence with a missing or contradicted answer), and the
   design review's recommendation (soft, never blocking). The pack's three laws route to the mechanical-gate
   net, each naming its real gate: register → `guardrails/check-shipped-language.sh` +
   `tests/test_preshow_register_lint.py`, clock-honesty → `guardrails/check-future-times.sh`,
   no-self-certification → `tests/test_no_self_certification.py`. The audit's prover-vs-design-review split
   still governs the design properties (INV-125, INV-126 stay the prover's judgment station); the mechanical
   gate is the third net the generalization to ALL declared laws exposes. Homes updated: INV-101 clause and
   index row, INV-150 clause and index row, the product-prover declared-laws station, the spec-author
   declared-laws line, and matrix M-292.

3. **Orphan test cited.** `tests/test_design_reviewer.py::test_cross_sibling_routing_split` was cited in no
   matrix row. It is now cited in M-292, and the design-reviewer routing-split is added to M-292's homes —
   that paragraph on the design-review side is where INV-150 lands on the discovery half.

4. **A net-floor test.** `tests/test_declared_laws.py::test_pack_declared_laws_each_name_a_net` parses the
   declared-laws home, enumerates each listed law, and asserts each names a net, mirroring the per-item
   interface/coverage floors. A future declared law added with no net reds without the agent. Red-first
   proven by dropping the clock-honesty net (net assignments 2 ≠ declared-law count 3 → red), then restored.
   Its scope is the net-floor only; the INV-125 sentence-form and INV-126 means-half behavioural cases stay
   at the string level, the pack convention.

**Result.** No blocking defect remains. Suite 718 → 721 green (three net-floor assertions added).
`guardrails/check-shipped-language.sh` and `guardrails/check-future-times.sh` clean. The version stays held;
the orchestrator owns the bump, the independent adversarial pass (INV-46), and the push.
