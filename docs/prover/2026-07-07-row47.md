# Prover record — 2026-07-07, row 47 (CROSS-LINK: "Sending feedback in", SPEC v0.15.60 draft)

Mode: CROSS-LINK — the new section's seams against Throwing a wish (T-12, INV-27, INV-1, INV-4), the
inbox door (E-11, T-10), convergence (INV-59), the problem ledger (INV-23), success measures (INV-21),
write-ownership (INV-10/11), review pages (INV-64). Previous record check: 2026-07-07-row165.md — one
recorded row (open field leg), riding as designed; nothing unfolded.

Opening assessment: the section's routing idea is sound (every item takes one route, each route cites
an existing law) and the channels reuse the inbox door correctly. Two seams were wrong in the draft:
the ledger promised a line for EVERY item (double bookkeeping against the queue, the decision archive,
and the journal), and the inbox law's harvest destination ("each file into a queue row") contradicted
the new routing. Both foldable in place; ready after the fold.

| # | Finding | Severity · tag | Folded / rejected |
|---|---|---|---|
| F1 | The draft ledger takes one line per received item, so a wish lands in TWO homes (queue row + ledger line), an answer in two (decision archive + ledger), a fix in two (commit/journal + ledger) — double bookkeeping the INV-61 lesson warns rots into drift; and a wish-shaped item would get two echoes (wish echo + feedback echo). Consequence: a session either spams ledger lines for every spoken wish or silently skips them, and the ledger stops being trustworthy either way. Action: the item's ONE home is decided by its route; the ledger OWNS the routes that had no home before (field evidence, plain reactions, wordless drops pending their question); one echo per item, a wish-shaped item's echo IS the wish echo. | must-fix · boundary-issue (composition) | FOLDED — promise paragraph, channel (b), routing list and INV-68 index line rewritten to route-homes; echo sentence merged |
| F2 | The package-repo inbox law says a sweep "harvests each file into a queue row" — a feedback file (a screenshot with a reaction) has no queue row under the new routing, so the two clauses contradict. My fence list wrongly fenced the whole inbox law as unchanged. Sibling sweep of the same phrasing: SPEC package-repo paragraph, inbox/README.md, formal index T-10/E-11 lines, live-spec-base inbox mention. Action: split the fence — one-new-file + sweep-first stay fenced; the harvest DESTINATION is re-authored: a swept file routes by T-20 (wish files to rows as today, feedback files to their route's home), one commit still both lands the route and removes the file. | must-fix · direct-contradiction (contradiction) | FOLDED — SPEC inbox paragraph + inbox/README.md + T-10 index line re-authored; fence split recorded in the row |
| F3 | E-1 lists what a host owns (spec, matrix, queue, journal, surface registry, inbox, .live-spec/) — the feedback ledger is missing, so the one sentence that inventories a host's files contradicts the new section's "beside the queue at the host root". Action: add the ledger to E-1's list in the same delta. | must-fix · missing-rule (invariant) | FOLDED — E-1 sentence gains the feedback ledger |
| F4 | INV-21's index line says "reading machinery [target]" and the S-0 map binds every [target] to a still-open row; the new prose calls the ledger "the reading machinery's first honest slice". If the tag's owning row is 96 (which closes when 47-49 are re-scoped and one loop runs), landing it later turns the suite red with the tag still live. Action: state in the prose that plugins/aggregation stay [target] under row 48 (long-lived, deferred), and verify the suite's target map points INV-21's tag at row 48. | should-clarify · reference integrity | FOLDED — prose names row 48 as the tag's owner; target map checked at the matrix step |
| F5 | The ledger's birth is unstated (does a host ship an empty FEEDBACK.md or is it born on first item?). PROBLEMS.md precedent: born on first entry with its header. Action: one sentence in the skill (its authoring step), no spec change — the spec's empty-state sentence already calls a header-only ledger healthy. | worth-considering · missing-prerequisite (precondition) | FOLDED into the skill's authoring notes (code step) |

Open ⟨DECIDE⟩ count touched by this delta: 0. Remaining open elsewhere: none live in the touched
sections.

## Architecture lens (node add = structure change)

- Every new anchor owned exactly once: E-28, T-20, INV-68 → feedback-intake; no double owner (grep of
  the Nodes table run at the landing).
- The node is spec-backed: the "Sending feedback in" scenario is its charter; no speculative structure.
- Seams: "outside item → its home" names what crosses (one item file) and splits format ownership
  honestly (inbox owns the file naming, feedback-intake the routing); "handed-in item → its home" and
  "feedback ↔ the echo" name their crossing and owner. The echo's SHAPE stays communicator's — the
  routing law never grows a second echo format (one home held).
- Pin check deferred to verify: `skills/feedback-intake/SKILL.md` is written later this same landing;
  the pin is re-run against the real file before commit. A landing that somehow ships the node row
  with no file goes red at the loadability gate, so the deferral is fenced.
- Count-drift sweep: the two seam cells saying "the six working skills" reworded count-free.

Verdict: structure sound; proceed to the matrix.

## Pre-push re-check (surface-sized delta — the full walk, per INV-61)

- Previous records: row165's one recorded row is the open field leg, riding by design; nothing
  unfolded anywhere else.
- All five findings above FOLDED, each verified against the shipped text this session (route-homes in
  SPEC + skill; inbox paragraph + README + T-10/E-11 index lines re-authored; E-1 carries the ledger;
  row 48 named as the [target]'s owner; ledger birth in the skill's own text).
- Deferred pin re-run done: `skills/feedback-intake/SKILL.md` exists, loads (the session's own skill
  loader picked it up), pin gate green across all 34 pins — including the test-author label fixed
  this session.
- Suite 174 green, zero unexpected skips; matrix rows M-172..174 red-proven then green; eval two-arm
  record written with an honest red.

Verdict: push-ready.
