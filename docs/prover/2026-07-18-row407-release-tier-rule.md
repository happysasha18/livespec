# Prover record (short form) — ROADMAP 407, the release-tier rule [INV-217, M-398]

Date: 2026-07-18
Mode: CROSS-LINK (small delta — skill/prose kind, no new surface, no structure change; INV-61 short form)
Lane: 3 of a three-lane run (branch worktree-agent-acf0c9057f29c76f4), write-range INV-217 / M-398.

## Previous records
Clean — the prior prover records under docs/prover/ (batch 3 rows 390/392, row 420 gates u/v/w,
rows 415/408/419) carry no unfolded rows this delta reopens.

## The delta, in one line
State when a release is a patch, a minor, or a major, read off what taking the release costs a host
that vendored the previous version (nothing / re-run the catch-up walk / a migration), as a stated
judgment held by no gate — a new SPEC clause INV-217 beside version-is-one-fact [INV-178], its base
rulebook home (rule 32), a build-pipeline commit-and-show pointer, its owning node (base-rulebook),
and M-398.

## Cross-link check (the new clause against the surfaces it names)
- **INV-91 (migration chapter).** The major tier ties to the existing law that a release owing host
  actions ships a dated MIGRATION.md chapter. No conflict: INV-217 reads the tier off the host cost,
  INV-91 states the artifact a host-acting release owes. They compose — a major both forces host action
  and ships the chapter.
- **INV-178 (version is one fact).** INV-217 sits beside it and adds no second version home: one number,
  one home (VERSION), and now a stated meaning for its tiers. No duplication of the stamping law.
- **INV-141 (design review never blocks).** Cited as the precedent standing for a judgment that is a
  stated rule, not a gate. Consistent: the release-tier call reads meaning a machine cannot, so it is
  guidance the session applies and names, held by no blocking check.
- **One-home (base rule 4).** The operative home is base rulebook rule 32; the SPEC clause is the formal
  invariant; build-pipeline carries a pointer, not a duplicate. Ownership pins to exactly one node
  (base-rulebook) in ARCHITECTURE.md, matching the INV-65 → base-rulebook precedent.

## Honesty note (acceptance, examples from the pack's own history)
The 2.0.0 release is the honest boundary case: its own MIGRATION.md chapter records "Host action: none",
so by INV-217 it reads as a minor, and its major number marked significance by the session's feel. The
rule names this in prose and leaves 2.0.0 as cited rather than restamping VERSION — the full seven-release
re-read and any journal correction of a misnumbered release is out of this row's scope (this row states
the rule; VERSION stays 2.6.3). The 1.0.0 rename chapter (a genuine major) and the row-247 inbox remote
arm (a minor) are the other two worked examples.

## Findings
None must-fix. The scope of a mechanical release gate (the roadmap's "Machine" ambition) is deliberately
NOT taken: the minor-versus-major call is a judgment, so it stays a stated rule, and this is said plainly
in every home. Recorded, not folded: a future row could add a gate that reds a release whose number
under-states its host cost, but that gate would only catch the structural signals (a MIGRATION chapter
present under a minor bump), never the judgment itself.

## Verdict
Delta is internally consistent and cross-links cleanly against INV-91, INV-178, INV-141, and base rule 4.
M-398 red-proven against the pre-delta tree (all seven home assertions red, then green). Suite green.
