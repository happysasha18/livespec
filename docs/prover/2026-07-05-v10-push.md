# Prover pass — SPEC v0.10.0 → v0.10.1, push gate for 0.5.1 (2026-07-05 evening, session 6)

Mode: FULL (whole spec, all phases, all NINE stress lenses — the ninth, unbacked-surfaces, ran for the
first time in the very landing that added it). Run on an Opus worker per the model split; senior (Fable)
triage + fold wording. Push gate (M-6) for the doors landing (rows 70–71). Prior context: v0.9.1
FULL-proven this afternoon (`2026-07-05-v05-push.md`); the doors delta CROSS-LINK-proven
(`2026-07-05-doors.md`, 10/10 folded — this pass verified every fold reads as folded in the prose).

Worker verdict: **ready-to-push, zero must-fix**; no regression in the v0.9.1-proven material. Six
clarification-grade findings; per the gate precedent all six FOLDED same sitting → v0.10.1. Mechanical
guards, senior-run after the folds: index ↔ prose anchor symmetry exact (68 anchors, both directions
empty diff); suite 43 green / 0 skips.

| # | Finding (one line) | Severity | Outcome |
|---|---|---|---|
| F1 | Critical-priority FEATURE had no queue position once priority and door went orthogonal | should-clarify · undefined-path | **FOLDED**: critical heads the QUEUE whatever its door; only the bug DOOR preempts the lane (T-11) |
| F2 | Mid-work re-door left the lane token's fate unstated (re-queue? park?) | should-clarify · atomicity | **FOLDED**: the re-doored wish KEEPS the lane token and re-enters the walk in place — no re-queue, no park (INV-16) |
| F3 | [target] became a machine tripwire while written at four granularities (section/anchor/leg); 3 header↔body asymmetries | should-clarify · over-general | **FOLDED**: tag binds at the granularity written (S-0 rule); Machines section header de-tagged; registry bullet tagged; A-6 named in the header list as riding E-7 |
| F4 | docs-only door didn't exclude edits to normative spec clauses (a rule reword could skip the prover) | should-clarify · boundary | **FOLDED**: docs-only = prose OUTSIDE the normative spec; rewording a spec rule routes as feature/bug (INV-16) |
| F5 | A-10 quarantine of a live surface: user-visible consequence + provenance trail unstated | worth-considering · postcondition | **FOLDED**: quarantine named as a production change the human is choosing + dated one-line provenance record, the attic manifest's mirror (A-10) |
| F6 | The SEE/TRY ask-when-unclear rule stated in full in two homes (drift risk, INV-13 applied to the spec itself) | worth-considering · over-specific | **FOLDED**: full home = the prototype section; the door paragraph now points there |

Nothing left for the queue from this pass. Anchor set: unchanged by the folds (wording only; the [target]
tag moves are tags, not anchors). Readiness: **ready to push** — this record ships with the push.
