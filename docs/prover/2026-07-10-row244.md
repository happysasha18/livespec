# Prover short-form — row 244, the made-with attribution (2026-07-10, clock at write ~16:38)

One-hop extension of the publishing family, short-form per the cadence for a small prose+skill law.

The new clause (INV-96) composes with the standing publish laws and contradicts none: the shared
floor already carries "license/attribution state is explicit" — the new duty sharpens that item for
built-with artifacts rather than adding a second checklist (the kind table untouched, E-20's one-home
rule kept); the wording lives once, in the floor, the spec clause stating only the duty (one home per
fact held); the walk's outcome rides the landing report like every floor item (INV-22). Boundary
named and folded: the pack never writes foreign trees — each built-with project applies the line
through its own queue, so the law creates duties only at THIS project's publish step and wishes
elsewhere. Edge folded: the pack's own README self-attribution is out of scope (the pack IS the
subject, a made-with line on itself says nothing); the first application target is track-coach,
wish filed to its inbox on the owner's explicit word. No new entity, no state, no seam; the
architecture step is pure assignment (INV-96 → publish node), so the architecture prove stands
down by name. Matrix M-225 owns the fact at string level under the publish node, red-proven
against the pre-law homes (4 fails → 315/315 green; one needle adjusted to the floor's real
markdown rendering before green — the law text moved, the test never did).

Amendment, same day ~16:50 (his correction ~16:47): the duty softened to an OFFER — the walk says
once when the line is absent and proposes it, the owner's word decides, a declined offer is closed
and never re-asked (the answered-stays-answered family). All four homes moved together (clause,
index, floor, matrix row), the test's never-side now asserts the no-re-ask instead of the gate;
315/315 green after the move. The one-hop composition holds: the offer rides the same floor walk,
still creating no duty on foreign trees.
