# Release re-check — 2.8.0 (2026-07-18)

Prover skill version: 2.7.1 (the release stamps the pack to 2.8.0).

The 2.8.0 MINOR lands one new law: INV-237, "the authoring seat does not adversarially certify its own
work" — a release's adversarial pass is authored by a fresh seat, and a newly added lens or rule is
self-applied to its own introducing document before release. It is carried across the SPEC clause +
Formal-index row, base-rulebook rule 33, wiring in build-pipeline (verify station) and product-prover
(release self-application), the build-pipeline node's ownership in ARCHITECTURE.md, TEST_MATRIX row
M-419, and tests/test_clean_context_review.py.

## Clean-context adversarial pass (INV-237 dogfooded on itself)

Per the very rule being landed, the release's adversarial pass was authored by a FRESH seat — a
differently-contexted reviewer briefed from the primary sources on the "goal missed / duplication"
hypothesis, not the seat that authored the change. This record names its result, satisfying INV-237's
second carrier (a new rule self-applied to its own introducing document before release).

**Verdict of the clean-context pass:** sound; no blocking flaw. The invariant is justified — it binds
the fresh-eyes freshness INV-46 defines to the release-gate full re-prove [INV-116], a locus INV-46's
verify-station scope never reached, and carrier two (self-application of a new lens to its own
introducing document) is genuinely new with no prior home. The self-reference is non-circular: a
clean-context adversarial review of the release that introduces the clean-context rule is the correct
dogfood. Gate honesty holds — the clause states plainly that whether a review was truly clean-context
is a process fact no gate fully sees, and M-419's NEVER-clause bars a record gate minted as a full
proof of that fact.

**Findings, all folded before this record closed:**
- D1 (defect) — the change self-declares a MINOR yet had no version bump or docs-travel. Folded: VERSION
  stamped to 2.8.0, CHANGELOG and JOURNAL carry the entry this same landing.
- D2 (defect) — INV-237's self-application record was not deposited. Folded: this record is it.
- R1 (recommendation) — carrier-1 re-described INV-46's definition rather than pointing at it. Folded:
  the SPEC clause now points at the freshness INV-46 defines and states only the new binding to the
  release pass.
- R2 (recommendation) — the "as wiring" framing mislabeled build-pipeline, which OWNS INV-237. Folded:
  the homes sentence now reads "owned by build-pipeline whose verify station is its operative home,
  carried into product-prover as wiring."
- R3 (recommendation) — the ownership test under-asserted. Folded: it now asserts INV-237 is owned by
  exactly one node (build-pipeline), reusing the traceability parser.
- R2 was checked against the INV-164 precedent (a base rule owned by build-pipeline whose base-rule line
  build-pipeline does not pin) and conforms to house style; single-node ownership holds.

## Verdict
The Formal index is contiguous through INV-237. The spec and architecture register lints report zero
errors (the reached-clean floor). The full suite is green at the release state. Traceability owns every
anchor once, INV-237 among them. The gate chain runs a–z. This is a MINOR by INV-217: a host takes it by
re-running its catch-up walk with nothing rewritten. Push-ready.
