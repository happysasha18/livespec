# FEATURE-FIT retro run — the pack's own landed features walked by the new lens

Date: 2026-07-06 ~11:35, session 12. Mode: FEATURE-FIT (INV-29), applied RETROACTIVELY on Alexander's
word («давай ретроактивно сначала мини-прувер на твоих же фичах») — the mode's first real run, on its
own maker. Kind lenses used: the pack is a skill/infra/prose product, so the walk uses flows · failure
paths · trigger/correction/not-fire · reading path; "return visit" here means A NEW SESSION, "cross
entry" means arriving through the repo, an inbox file, or a cold resume instead of live chat.

Verdict vocabulary (INV-29): backed — an existing clause already promises it · closed — hole closed
trivially by the walker, the closing written here · row — a real hole, queued · owned — a hole already
owned by an existing queue row, confirmed by the walk.

## The walk, feature by feature

1. **Throwing a wish (echo + queue).** Arrival by chat, by inbox file, by harvest — all three echo
   (backed: INV-27, rule 12). Return visit "what happened to my wish?" — the board answers (backed:
   INV-27/28). Failure path: a session dying between the spoken echo and the written row — the law
   already orders write-then-speak; residual risk is seconds wide (backed, noted).
2. **Reading progress (reports, board).** A reader who is NOT the project's founder — his morning
   point «это не только мне» — the outcome-leads law speaks of "the reader", not of Alexander, so it
   generalizes (backed: INV-28). Arriving through GitHub with no chat context: the README reading
   path exists; the full stranger's onboarding is a known hole (owned: row 54, confirmed).
3. **Answering questions (decision pages).** The full flow walked: render → open → pick → Download
   JSON → Downloads folder → read back → archive → harvest. HOLE FOUND: the return leg assumed the
   ASKING session is still alive — an answer given hours later, after that session died, had no owner
   to pick it up. CLOSED, written how: communicator rule 10 now opens with "a session resuming a
   project first checks Downloads for that project's unclaimed decision files" (0.1.16, this hour).
   Withdrawn answers (backed: E-22); consequence-first cards (backed: INV-32, the NEXT page is its
   first deed); cross-project filename collisions (backed: rule 10 filename law).
4. **Resuming cold (NEXT_STEPS).** Cold-start walk: loader → name the project → LIVE STATE → verify
   git + suite — complete (backed: M-2/INV-26). Implied state on return: the unclaimed-answers sweep
   (closed above). Two sessions on one repo (backed: the concurrent-edit fence).
5. **The problem ledger.** The senior's walk is law (backed: E-24/INV-23). HOLE: workshop noise a
   WORKER hits gets silently retried unless the senior notices it in raw output — the brief contract
   never mentions the ledger (row: 123 queued). A host window escalating a third recurrence to the
   pack — one inbox file (backed).
6. **Installing and UPDATING the pack.** Fresh install into a clean home (backed: E-21, proven at the
   last push's publish walk). The UPDATE walk found today's live bug: the installer's backups landed
   INSIDE ~/.claude/skills, and the harness listed six stale copies as loadable duplicate skills the
   moment Alexander ran it (row 122 — landed this hour, red-proven test; strays moved out by hand).
   A running session holding old skill text in memory (backed: freshness, base rule 8). Uninstall:
   nobody has asked — named a NON-GOAL here rather than silently absent; becomes a wish when someone
   does.
7. **Outsider contribution (inbox).** Drop, collision law, echo-in-next-report — all backed; walked
   twice for real this week (tlvphoto's notes).
8. **Reading the pack as a stranger.** `pytest` from the root trips over a template (owned: row 106,
   confirmed by an outside analyst's first minute). Guided onboarding (owned: row 54).
9. **Delegating to workers.** Self-contained briefs, checkpoints, one-tier escalation — backed, and
   today's two correct HALTs are fresh deeds. The HALT list as an explicit enumerated contract is
   already queued (owned: row 112, value confirmed). Ledger gap → row 123 (above).
10. **Pushing/publishing.** Full prover + evals + publish walk before push (backed: host profile,
    E-19, row 98). Eval re-runs for this week's skill changes are OWED at the next push — riding.

## Outcome

- 2 holes closed the same hour, written how: the decision-answer return leg (communicator 0.1.16) ·
  installer backups out of the live dir (row 122, red-proven).
- 1 new row queued: 123 (workers and the ledger).
- 3 known holes confirmed, already owned: 54 (onboarding) · 106 (pytest from root) · 112 (HALT list).
- 0 questions for the human — nothing here was a taste call.

## Taste choices made without asking this morning (told per INV-31, tweakable, no answer needed)

- The journey lens list (arrival · what you do · where next · return visit · implied state · feel ·
  invited-next) — its composition is my pick; lenses can be added or dropped.
- "One fact = one standalone sentence" as a hard report-style rule — my pick of strictness.
- The installer's backup home `~/.claude/skills-attic` — any out-of-scan folder works.
- The prover mode's internal English name (FEATURE-FIT) — internal only, never user-facing.
