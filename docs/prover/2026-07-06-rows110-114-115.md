# Prover record — 2026-07-06 ~23:43, rows 110 + 114 + 115 (three lanes), CROSS-LINK ×3

Previous records: pushgate-s22-4 (clean) — nothing unfolded rides in. Three small deltas proven
against their own neighbourhoods; lanes share no surface, so the passes are independent by
construction (the lane claim named the graph).

## Row 110 — the adversarial verify option (INV-46, "Who decides what")

Opening assessment: the law mechanizes base rule 13 ("a worker's report is a lead") exactly where it
was weakest — the delegated large landing. Ready to build.

| # | Finding | Severity · category | Status |
|---|---|---|---|
| F1 | Who verifies the checker? An infinite regress is one careless sentence away. | should-clarify · boundary-issue (composition) | FOLDED — the checker's output is FINDINGS (rows or red) judged by the senior; no checker-of-checker exists; the senior's judgment closes the loop, exactly as with the prover's findings |
| F2 | "Surface-sized" could be read by mood. | should-clarify · over-general (abstraction) | FOLDED — defined in the clause: a new surface OR a multi-file behaviour change; both readable off the delta |

Cross-links that hold: ACT-3 (the checker IS a worker — contract, checkpoint, ledger, clock);
INV-30 (the visitor walk and the adversarial pass are two different step-8 eyes — walk = the product
kind's feel, checker = claim-by-claim derivation; both named in step 8 by the delta); M-6 untouched.
Binds forward; the first real run rides THIS row's own landing (delegated + multi-file ⇒ the mandatory
trigger fires on itself — dogfood at birth).

## Row 114 — gate hygiene (INV-47, "The machines that hold the bounds")

Opening assessment: three conventions, one home (guardrails README), one gate converted by deed.
Ready to build.

| # | Finding | Severity · category | Status |
|---|---|---|---|
| F1 | check-push-reach.sh exits 1 as a VERDICT (full suite), not a defect — a naive sweep would bolt a failure JSON onto a decider and confuse agents. | should-clarify · internal-conflict (consistency) | FOLDED — the clause scopes the contract to BLOCKING GATES; the reach decider is exempt by name in the README's contract section |
| F2 | Existing tests assert the prototype fence's human FAIL lines; adding the JSON line must not break them. | worth-considering · missing-outcome-check (postcondition) | FOLDED into the brief — the worker runs the gate-e tests and the full guardrails file after the edit |

Cross-links that hold: E-6 (the machines bullet already lists the gates; the contract is authoring
law, no new machine); INV-45 (unchanged); INV-21 kin (binds by deed from the first gate, sweeps on
touch — no retroactive mass edit).

## Row 115 — the resume-file cap (INV-48, "Rhythm")

Opening assessment: the number is honest (100 lines, GSD's neighbour lesson), the INV-26 tension is
resolved by FORM (one terse line per open leg), and the pack's own file is the dogfood. Ready to build.

| # | Finding | Severity · category | Status |
|---|---|---|---|
| F1 | The pack's own NEXT_STEPS is under the cap TODAY (≈68 lines) — the "compacted under it" leg would pass without proving anything. | worth-considering · missing-outcome-check (postcondition) | RECORDED — the red proof lives in the synthetic bloated fixture (the check red-proven there); the pack's file merely stays legal; the leg reads "under the cap by deed", honest |
| F2 | A hard cap invites gaming by ultra-long lines. | worth-considering · unenforceable-promise (discharge) | RECORDED, no change — the reader's one-minute law is the spirit; if long-line gaming ever appears in the field, it earns its own row; no speculative mechanism tonight |

Cross-links that hold: M-2 (replaced-never-stacked composes with the cap); INV-26 (resolved by form
IN the law's own sentence); the template (templates node) carries the cap note as shape, the suite
check owns the number.

Findings: seven across three lanes — four folded, three recorded with reasons.
