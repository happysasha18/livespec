# Prover record — FULL pass, pre-MINOR gate 2.2.0 → 2.3.0

- Pass run by: **product-prover 2.2.0** (the pack version, per INV-178's one-fact rule; read from `VERSION` and the skill frontmatter, which agree)
- Date: 2026-07-16
- Mode: **FULL** over `PRODUCT_SPEC.md` (2091 lines, read whole) + the **architecture lens** over `ARCHITECTURE.md` (six checks)
- Delta since the 2.2.0 full pass (this evening), weighted but not scoping the read — four ROADMAP rows landing in the working tree:
  - **361** — the suite wall-time budget gate arm: `guardrails/check-suite-budget.sh` reads a full run's tail duration against the architecture's stated budget; ARCHITECTURE Quality-budgets row + spec reach-map clause + INV-45 index row + matrix M-346 amended/added.
  - **362** — the reach map's scoped middle road: an infra-class diff runs the test files that name its changed files plus the traceability net; the suite-in-suite meta-test fires only on gate-machinery diffs (INV-45 prose + index, M-142 amended, M-344/M-345 new).
  - **363** — INV-126 grows the magnitude sub-question (spec clause, index row, product-prover lens, spec-author facet, M-341, `tests/test_paired_transition.py`).
  - **364** — INV-157 grows shell-first binary resolution and a launch frame probe (spec clause, index row, M-342/M-343, `templates/headless_harness.py`).
- Previous record checked (opening duty): the 2.2.0 gate record (`docs/prover/2026-07-16-full-2p2p0.md`) closes **fully folded** — its disposition addendum folds all four defects and four recommendations; F4's fold explicitly queued row 361 (this pass's budget gate). No unfolded row rides forward.
- Deed checks run this pass: `guardrails/check-pin-drift.sh` (54 pins, strict-clean — `check-suite-budget.sh` and the updated `check-push-reach.sh` pins resolve to real files); the full suite `python3 -m pytest -q tests` run in the foreground, its own tail line the verdict [INV-80] (**281.10 s, 6 failed + 1 error, 940 passed**); a serial-vs-parallel check of the gate's runner (`guardrails/check-tests.sh` runs `python3 -m pytest -q`, **xdist not installed** — the gate's wall-time is the serial figure); `guardrails/check-suite-budget.sh` exercised on a synthetic 302 s log (reds correctly against the stated 180 s); per-row existence checks of every test the new matrix rows cite.

## Triage

TRIAGE: PROCEED — a full product spec with states, transitions, actors, and invariants, paired with an architecture doc whose reconcile date is current (2026-07-16) and whose 54 node pins pass the strict drift check. The doc claims a shipped system; the pins stand, so findings are unconditional — with one deliberate caveat the caller named: the code for rows 361/362/364 is being built by parallel workers, so this pass proves the DOCUMENTS and reads the in-flight suite state as evidence, distinguishing a document defect from a not-yet-landed test.

## Opening assessment

Three of the four rows are clean, well-made document deltas. Row 363 (INV-126 magnitude) is exemplary: the sub-question lands in all three homes it names — the composition clause, the product-prover lens, the spec-author facet with its `0.82×` incident key — the index row carries it, and `tests/test_paired_transition.py` is green (8 passed). Row 364 (INV-157 shell-first + frame probe) composes cleanly into the existing law and its INV-158 string-check net without adding a class member, so INV-160's "nine members" enumeration stays true. Row 362 (the scoped middle road) is coherent prose with the conservative teeth intact.

Row 361 is where the pass stops. The suite wall-time budget — the very defect the 2.2.0 pass filed as F4 and marked folded — is **live again by deed**. ARCHITECTURE states the budget as "≤ 180 s ... (measured 92–97 s at 940 tests)", but the gate's own runner is serial `pytest -q` (xdist is not installed), and that run clocks **281 s** this pass (302 s at the 2.2.0 pass) — three times the recorded measurement and past the budget. Row 361's new gate is correct; run against reality it reds on every full push. The 2.2.0 fold recorded a measurement that does not match the machine. Second, a document placement defect: M-346 (the budget gate's row) sits under the `guardrails` node while citing only INV-41, which build-pipeline owns, so the traceability net reds and will stay red after the workers finish. A lighter third: the spec's prose enumeration of the reach map's infra classes omits `adopt/`, which the deciding script and M-344 both include.

Confidence: **needs one folding iteration** plus the in-flight test code landing green. Two blocking defects (F1 budget number, F2 M-346 placement), one light defect (F3 enumeration); nothing structural is wrong.

## Phase 1 — The model

The entity, state, actor, and composition model is unchanged from the 2.2.0 full pass (`docs/prover/2026-07-16-full-2p2p0.md`, Phase 1). The delta touches four laws, no new entity, node, or seam:

- **Push-gate reach (INV-45)** gains a third verdict class beside EXPLICIT / CONSERVATIVE / SELF-TESTED: **SCOPED** — an infra-class diff runs only the test files naming its changed files (one referrer level deep) plus the traceability net, with two named residents riding the same reach (the gate-machinery meta-test; the suite wall-time read).
- **Suite wall-time budget (INV-41)** gains a mechanical reader (`guardrails/check-suite-budget.sh`) and its asserting matrix row (M-346), closing the "budget no machine reads" hole F4 named.
- **Paired-transition law (INV-126)** gains a magnitude sub-question under its reversibility half.
- **Browser-harness law (INV-157)** gains shell-first binary resolution and a launch frame probe, both under the INV-158 string-check net.

### What I assumed

- I read the gate's real full-run wall-time as the **serial** `python3 -m pytest -q tests` figure, because `guardrails/check-tests.sh` runs exactly that and `python3 -c "import xdist"` fails — no parallel runner is in play. If a parallel runner is intended to be the budget's basis, that intent is nowhere in the documents and the budget row does not name it; on the documents as written, 281 s is the gate's number and F1 stands.
- I read the workers as still landing rows 361/362/364 code: `tests/test_push_reach.py` (M-344/M-345's home) does not exist yet, and `test_probe_covers_the_local_load_stall` (row 364 harness) is red. I treat the missing test code as in-flight, not a document defect — except where a document *claims* a status the tree does not yet support (R1).
- Carried forward unchanged: the FULL-pass table duty, and the surface-authority assumption lines from the 2.1.0/2.2.0 records.

## Phase 2 — Structural issues

F2 — The suite-budget matrix row sits under the wrong node: `guardrails` while its only cited anchor is build-pipeline's

> "| M-346 | The suite wall-time budget's mechanical net (INV-41, INV-164, row 361) ... | INV-41 | string | ... | BUILT |" — TEST_MATRIX.md, under `### [node: guardrails [target]]`
> Deed: `tests/test_traceability.py::TestMatrix::test_matrix_rows_sit_under_their_owning_node` fails — "M-346 sits under 'guardrails [target]' but cites only ['INV-41'] (owned elsewhere)". INV-41 is owned by build-pipeline (ARCHITECTURE node table); INV-45 is guardrails'.

The traceability net requires a matrix row to sit under the node that owns its cited anchor. M-346's gate (`check-suite-budget.sh`) is a guardrails artifact, but the row cites only INV-41 (the budget law, build-pipeline's), so the net reds — and it stays red after the workers land the row-362 tests, because this is a document defect, not missing code. A session at the push gate reads a red suite and cannot tell F1's budget breach from this placement slip until it opens the failure.

Fold one of two ways: (a) co-cite INV-45 in M-346's anchor cell — defensible, since INV-45's own scoped-road clause now carries "a full run's wall-time is read against the stated budget", so guardrails genuinely co-owns the behaviour and the row then sits correctly; or (b) move M-346 under `### [node: build-pipeline]` beside INV-41's other rows. (a) is cleaner: it keeps the gate's row beside its guardrails siblings M-344/M-345.

`defect · boundary-issue (composition)`

## Phase 3 — Property analysis

F1 — The suite wall-time budget is false by deed and its own new gate reds on every full push: F4 is live again, not folded

> "| full suite wall-time | ≤ 180 s on the dev machine [default] (measured 92–97 s at 940 tests, 2026-07-16) | the pytest tail line ... read by `guardrails/check-suite-budget.sh` on every full gate run and red past the budget ..." — ARCHITECTURE.md, Quality budgets
> Deed: the gate's runner is serial (`check-tests.sh` → `python3 -m pytest -q tests`; xdist not installed). This pass's full run tail: "6 failed, 940 passed, 1 error in **281.10s**". The 2.2.0 pass measured 302.26 s. `check-suite-budget.sh` on a 281 s (or 302 s) log against the stated 180 s exits FAIL.

The recorded measurement (92–97 s) does not match the machine the gate runs on: the serial suite is ~281–302 s, and the ≤ 180 s budget is exceeded three-fold. This is the exact defect F4 named at 2.2.0 — a budget number that does not match its own instrumentation home — reappearing because the fold wrote a figure (92–97 s) no serial run reproduces. Worse than F4: row 361 now ships the gate that reads the number, so once wired (`check-suite-budget.sh` is not yet in `pre-push`/CI — the workers' remaining wiring), it reds every full push, and the 2.3.0 landing gates itself out. A session trusting the "measured 92–97 s" annotation schedules a "quick" full run and waits five minutes.

Two folds together, both build-pipeline's to set and tell [INV-70]: (a) re-tune the budget to the measured truth — set it from a fresh serial `pytest -q tests` run (≈ 281–302 s; propose ≤ 330 s [default], or split a fast lane and budget both, or land xdist and re-measure and name that as the basis); and (b) correct the parenthetical to the real figure and basis, so the annotation and the gate agree. Never a budget whose recorded measurement no serial run reproduces.

`defect · unenforceable-promise (discharge)`

F3 — The spec's reach-map infra-class list omits `adopt/`, which the deciding script and M-344 both include

> "A diff confined to the infra classes — the guardrail scripts, the scaffold checks, the shipped templates, the helper scripts, the hooks, the guardrails config, or the suite's own test files — runs the test files that read its changed files ..." — PRODUCT_SPEC.md, "The push gate's reach and its blocking contract"
> against: M-344 lists "guardrails/, scaffold/guardrails/, templates/, scripts/, hooks/, **adopt/**, guardrails.config.json, tests/test_*.py"; `guardrails/check-push-reach.sh` line 58 matches `adopt/*` as INFRA.

Three homes describe one set — the spec prose, the matrix row, and the deciding script — and the spec prose is the one that disagrees: it presents a dash-bounded list that reads as the definition of "the infra classes" yet drops the adoption-walk scripts the shipped classifier scopes. An agent auditing or re-deriving the reach map from the spec concludes an `adopt/`-only diff falls to full, or "corrects" the script to match the incomplete clause, re-introducing the drift; the overlapping-data agreement is unstated where the spec is meant to be the definition [INV-140, INV-144].

Fold: add the adoption-walk scripts to the prose list ("... the hooks, the adoption-walk scripts, the guardrails config, or the suite's own test files"), so the clause's definitional enumeration matches M-344 and the script.

`defect · over-general (abstraction)`

R1 — The matrix marks M-344/M-345 "BUILT (red proven against HEAD)" while their test file and the six cited functions are not yet on disk

> "| M-344 ... `test_scoped_reach_guardrails_diff_exits_scoped` + `test_scoped_reach_unnamed_file_falls_full` + `test_scoped_reach_mixed_diff_falls_full` ... | BUILT |" and M-345's three `test_meta_reach_*` — TEST_MATRIX.md
> Deed: none of the six functions exist; `tests/test_push_reach.py` (their apparent home) is absent. `test_matrix_built_rows_name_real_tests` reds on M-344. (M-341, M-342, M-343, M-346's tests do exist — rows 363/364 harness and 361's budget tests have landed.)

This is largely the code-in-flight condition the caller named — the row-362 workers have not landed the scoped-reach and meta-test code — and the traceability net correctly reds until they do. It is flagged, not blocking on its own, because the fix is the workers' pending landing rather than a document edit. The document duty: the "BUILT / red proven against HEAD" status must be true by the fold — a row cannot claim red-proof for a test that never existed to be red. Verify at fold that the six functions land under the cited names (the matrix pins the names; a differently-named landing keeps the net red).

`recommendation · now · hard-to-operate (ops-ux)`

### Quantifier re-verify (INV-170)

Swept the enumerations the delta could falsify. INV-160's "nine members (INV-77/78/79/80/100/102/155/157/158)" holds — INV-157 grew two behaviours under one existing member, no member added, M-307 unchanged and true. M-142's full-reach list correctly dropped "tests, scripts" (now scoped) and no other clause still claims them full; INV-45's CONSERVATIVE list names only tested documents (SPEC/matrix/architecture/queue/SKILL.md), all correctly full. The gate-machinery "fires only on its guarded class" matches M-345. INV-157's index "prefers the dedicated chrome-headless-shell build ... and probes for one compositor frame" matches M-342/M-343. The **one falsified enumeration is the reach-map infra-class list — F3**.

### Coverage tables (INV-171)

The three classic tables collapse for this kind exactly as the prior records state (entities archive rather than delete; invariants-per-state live in the Formal index and matrix; no user roles). Per INV-171 the surface × sweep verdict table stands in their place:

| Surface | Declared laws (INV-101) | Edge completeness (INV-138) | Uniformity (INV-125) | Lifecycle (INV-168 parent) | Unwritten seams (INV-72) |
|---|---|---|---|---|---|
| Push/CI gates (reach map, budget gate) | clean | clean | clean | clean | **hit — F1** (budget number false by deed, gate reds on the real run) |
| Suite & harness (browser template, INV-157) | clean | clean | clean | clean | clean (shell-pref + probe compose under INV-158's net; probe test in-flight) |
| Composition laws (paired-transition INV-126) | clean | clean | clean | clean | clean (magnitude sub-question closed in all three homes, green) |
| Matrix rows (TEST_MATRIX) | clean | clean | clean | clean | **hit — F2, R1** (M-346 under wrong node; M-344/M-345 BUILT ahead of the tree) |
| ARCHITECTURE.md (nodes, seams, budgets) | clean | clean | clean | N/A — a document | **hit — F1** (budget row's measurement contradicts its instrumentation home) |
| Spec reach-map clause (INV-45 prose) | clean | clean | **hit — F3** (infra-class list omits `adopt/`, disagreeing with the classifier) | clean | clean |
| Queue & rows (ROADMAP) | clean | clean | clean | clean | clean |
| Decision page + batched questions | clean | clean | clean | clean | clean |
| Chat surfaces (echo, narration, board, reports) | clean | clean | clean | N/A — stateless lines | clean |
| Settings card / feature-map answer | clean | clean | clean | N/A — read-only | clean |
| Inbox (4 arms) + stranger monitor | clean | clean | clean | clean | clean |
| Feedback ledger + outbox / collector | clean | clean | clean | clean | clean |
| Problem ledger | clean | clean | clean | clean | clean |
| Prototype home + norms | clean | clean | clean | clean | clean |
| Review records (prover / design review / audit) | clean | clean | clean | clean | clean |
| Resume file + checkpoints | clean | clean | clean | clean | clean |
| Installed skills (E-23 / INV-180) | clean | clean | clean | clean | clean |
| Standalone mirrors (README shopfront) | clean | clean | clean | N/A — regenerated whole | clean |
| Adoption / catch-up / bootstrap walks | clean | clean | clean | clean | clean |

All five mandatory sweeps ran. The imaginative probes ran without owed verdicts and produced F1 (dependency-reality / budget read against its own instrumentation line, under the architecture lens), F2 (surface-authority: the matrix row's cited anchor vs its node home), and F3 (the overlapping-data / class lens on the reach-map enumeration across three homes).

## Phase 3.5 — Acknowledged gaps

1. **D-1 attic layout, D-6 pair queues, D-7 pair-spec citation depth** — unchanged from the prior records; each waits on its real triggering event, recommendations attached. `acknowledged · missing-rule (invariant)`
2. **The [target] machines** — host-facing guardrails + surface registry [E-6, E-10], snapshot [E-7], design-sync [E-18], the measurement family. Each carries its owned row and the S-0 suite tie; consistent. `acknowledged · missing-scenario (state-space)`
3. **In-flight worker code** — rows 361/362/364 code lands in parallel; the suite carries transient reds beyond the document defects (the row-362 scoped-reach/meta tests absent; `test_probe_covers_the_local_load_stall` red; `test_withdrawal_convergence` errors in the full run yet passes isolated — an ordering/shared-state flake to confirm as transient contamination, not a landed INV-155 isolation regression). Not new document discoveries; named so the gate reader distinguishes them from F1/F2/F3. `acknowledged · missing-scenario (state-space)`

## Phase 4 — Human and operational factors

- **Domain language**: strong; the delta's new prose (INV-126 magnitude, INV-157 shell/probe, the scoped-road paragraph) speaks product words with anchors trailing. No leaked internal identifier on a promised visible string.
- **Observability**: F1 is the headline observability break — an architecture annotation ("measured 92–97 s") that lies about the machine the gate runs on, so the one number a session would trust to plan a run is false. F2 is the second — a red suite whose cause (a mis-noded row) is indistinguishable from F1's real breach until opened.
- **Cognitive load**: F3's split enumeration is the one place a reader must already know the script's class list to catch that the spec's is short.
- **Performance/scale budgets**: the suite budget is the delta's whole subject and is falsified by this pass's own run (F1: 281 s against ≤ 180 s, annotation says 92–97 s).
- **Security/privacy**: no change; the no-secrets line stands.

## Phase 5 — Closing summary

**Top fixes before the 2.3.0 bump:**
1. **F1** — re-tune the suite wall-time budget to the measured serial truth (≈ 281–302 s) and correct the "92–97 s" annotation to the real figure and its basis; the gate must be green on the real run before the bump. The single blocker.
2. **F2** — co-cite INV-45 in M-346's anchor cell (or move the row under build-pipeline) so the traceability net greens.
3. **F3** — add the adoption-walk scripts to the spec's infra-class enumeration so the clause matches the classifier and M-344.
4. **R1 / in-flight** — land the row-362 scoped-reach and meta-test code under the names M-344/M-345 pin, and the row-364 harness probe, so the suite goes fully green; the matrix's BUILT status must be true by fold.

**Properties to state explicitly (paste-ready):**
- "The full suite's stated wall-time budget is measured on the gate's own serial `pytest -q tests` run, and its recorded measurement matches that run to within the run's variance." (F1's fold, one clause on the budget row.)
- "A matrix row sits under the node that owns an anchor the row cites." (Already the traceability net's rule; F2 is its live violation.)
- "The reach map's infra classes are exactly `guardrails/`, `scaffold/guardrails/`, `templates/`, `scripts/`, `hooks/`, `adopt/`, `guardrails.config.json`, and `tests/test_*.py` — the same set the deciding script matches." (F3's fold.)

**Open questions for the author** — none that inspection cannot resolve; F1's budget number is build-pipeline's to set and tell [INV-70], not a question the prover answers.

**Recommendations queued for a taste call:** R1 (verify the in-flight test code lands under the pinned names — now).

**[default] count (FULL-pass duty):** 42 live `[default]` lines in PRODUCT_SPEC.md (60 literal mentions; 18 backticked mentions of the tag itself, excluded). The delta adds none of its own — INV-126's magnitude and INV-157's shell/probe both close decided, not `[default]`-tagged. Oldest five unchanged from the 2.2.0 record, restated for the taste call:
1. 2026-07-06 — the parallel-lane cap: "at most three build lanes roll at once without asking [default]" (T-18).
2. 2026-07-06 — the resume-file cap: "whole file ≤ 100 lines [default]" (INV-48).
3. 2026-07-07 — the brief bound: "~300 lines, ~8 files to edit [default]" (INV-55).
4. 2026-07-07 — feedback section: "Facets otherwise N/A [default]" (F-feedback).
5. 2026-07-07 — problem-ledger success measure: "checked at the milestone audit [default]" (F-problem-ledger).

**Architecture lens verdict (six checks):**
1. *Every spec fact owned by exactly one node* — one hit: **F2**, M-346 sits under guardrails while citing build-pipeline's INV-41 (the mechanized net reds). No new anchor this delta, no orphan.
2. *No node without spec backing* — no new node; clean.
3. *Every seam names what crosses and which side owns the format* — no new seam; clean.
4. *Budgets stated with instrumentation homes* — **one hit: F1**, the full-suite wall-time row's recorded measurement (92–97 s) contradicts its own instrumentation home (the serial run's tail line, 281 s) and the budget is exceeded.
5. *Runtime view walks every promised flow* — no new flow; clean.
6. *Placement view with load-bearing technology* — unchanged; clean.
The header's reconcile date is current (2026-07-16); 54 pins strict-clean (the new `check-suite-budget.sh` and updated `check-push-reach.sh` pins resolve). This pass's row in the ARCHITECTURE Prover-record table is the orchestrator's append at the fold, with this record's name.

**Suite note:** the full run tail reads "6 failed, 940 passed, 1 error in 281.10s". Of these, two are document defects this pass files (F1's budget breach would red row 361's gate once wired; F2's `test_matrix_rows_sit_under_their_owning_node`); the rest are in-flight worker code (M-344/M-345 tests absent → `test_matrix_built_rows_name_real_tests`; `test_probe_covers_the_local_load_stall`; the `test_withdrawal_convergence` ordering flake). The 281 s wall time is F1's evidence.

**Readiness: needs another iteration.** Fold F1 (the budget number — the blocker), F2 (the mis-noded row), and F3 (the enumeration), and land the in-flight row-362/364 test code green; then the spec and architecture are ready for the 2.3.0 bump. Rows 363 and 361's-gate-machinery and 364's-law are document-clean; 361's budget *number* and 362's spec enumeration are not.

**Remaining open ⟨DECIDE⟩ markers:** 0 (none introduced this pass; F1's budget figure is build-pipeline's set-and-tell under INV-70, not a ⟨DECIDE⟩).

**Findings disposition** (the senior folds):

| Finding | Kind | Disposition |
|---|---|---|
| F1 — suite wall-time budget false by deed (measured 92–97 s vs real serial 281 s), ≤180 s exceeded, row 361's gate reds; F4 recurrence | defect | OPEN |
| F2 — M-346 under `guardrails` node cites only build-pipeline's INV-41; traceability red | defect | OPEN |
| F3 — spec reach-map infra-class enumeration omits `adopt/` that the script + M-344 include | defect | OPEN |
| R1 — matrix marks M-344/M-345 BUILT + red-proven while their tests are not yet on disk (in-flight); verify at fold | recommendation · now | OPEN |

## Disposition addendum (senior fold, same session, 2026-07-16 ~21:20)

- **F1 — FOLDED.** The budget row now states the measured serial truth: ≤ 360 s [default], measured
  281–302 s serial at 941 tests with the suite-in-suite meta-test firing; the stale "92–97 s"
  annotation is corrected in place with its correction named (ARCHITECTURE.md, quality budgets).
  Set-and-tell under INV-70 rides the landing report.
- **F2 — FOLDED.** M-346's anchor cell moved to INV-45 (the guardrails node owns the gate arm); the
  claim text keeps citing INV-41/INV-164 as the laws it serves.
- **F3 — FOLDED.** The reach clause's infra-class enumeration now names the adoption-walk scripts,
  matching the shipped classifier and M-344.
- **R1 — RESOLVED by the lanes landing.** The six pinned test names live in tests/test_guardrails.py
  and tests/test_suite_budget.py; the loopback probe test landed with its leg. The
  withdrawal-convergence red is checked on the clean post-landing run (concurrent mid-flight edits
  during the prover's own run are the working explanation; a reproduction would be its own bug row).
