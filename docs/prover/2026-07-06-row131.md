# Prover record — 2026-07-06, row 131 (CROSS-LINK)

Mode: CROSS-LINK — the new working-narration clause (INV-35, SPEC v0.15.25) against its named
neighbours: the capture echo (INV-27), the report-line laws + bookkeeping NEVER-list (INV-28), the
no-questions law (INV-31), the pre-report walk's scope (INV-34), the clock law's chat face (INV-24),
the "(себе)" working-notes register (communicator "When NOT to use"), and the personal-profile line
`chat.working-narration` (the one-home question). Previous record checked:
`2026-07-06-rows126-128.md` — no unfolded rows.

Opening assessment: the clause is small and sits cleanly between the echo and the report — it cites
its neighbours instead of re-legislating them (the walk's scope stays INV-34's, the no-questions law
stays INV-31's), and it is honest about enforceability (chat has no suite; acceptance = the ask not
recurring, same stance as INV-34 and INV-24's chat face). Two seams need one-line stitches and one
home needs shrinking; nothing structural. Ready to build after the folds.

| # | Finding | Severity · category | Folded / rejected |
|---|---|---|---|
| F1 | INV-28's line enumeration does not name narration lines. > "a human-facing report or board line (chat reports, report pages, decision pages, the capture echo — method-internal docs keep their anchors)" — Throwing a wish / INV-28 paragraph. INV-35 binds its lines to INV-28's laws from ITS side, but a session reading INV-28's list as exhaustive would let a narration line escape the outcome-leads law and the bookkeeping NEVER-list — the exact one-sided-seam hole CROSS-LINK exists to catch. Add "narration lines [INV-35]" to the enumeration. | should-clarify · boundary-issue (composition) | FOLDED in-pass — enumeration now names narration lines |
| F2 | The communicator's frontmatter description still reads "NOT for a passing mid-work status line" — once the narration rule lands in the same file, the description contradicts the rule it advertises. A future session deciding whether narration is communicator's business reads the description first and concludes it is not. Reword: a passing narration line is not a reason to LOAD the skill (the rule is learned once), which is what the NOT-clause always meant. | should-clarify · direct-contradiction (contradiction) | FOLDED at the code step (communicator 0.1.20 description rewords the NOT-clause) |
| F3 | The personal-profile line `chat.working-narration` now duplicates pack law. > "chat.working-narration: while working, narrate briefly…" — profile.md, Working habits. Two full statements of one rule in two homes will drift (this very row exists because the profile-only statement did not carry); after INV-35 the profile line's job is the person's own TUNING (his voice, his language), not the rule. Shrink the profile line to a pointer + tuning note. | should-clarify · one-home (consistency) | FOLDED in-pass — profile line now cites the pack law and keeps only his voice note |
| F4 | The narration-vs-"(себе)" boundary is a judgment call — which register a given line belongs to has no mechanical test. The clause already gives the discriminator (narration is FOR the reader; "(себе)" notes are the writer's own, skippable), and chat law cannot be sharper without a quota the non-goals refuse. | worth-considering · cognitive-load | REJECTED — deliberate judgment line; the FOR-the-reader test is the stated discriminator |

Coverage tables: N/A — a single chat-law clause; no persistent entities, no authorization, no CRUD
(single-user local pack, security/privacy out of scope by the pack's standing note).

⟨DECIDE⟩ touched by this change: none. Open elsewhere: D-1 / D-2 / D-3 (untouched).
