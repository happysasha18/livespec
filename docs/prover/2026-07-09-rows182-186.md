# Prover record — rows 182-186: the tlvphoto week's five test-method lessons

Mode: CROSS-LINK per row (each delta against the test-method neighbours). Date: 2026-07-09 evening,
session 30. Prover skill version running this pass: 0.1.15. Spec under review: PRODUCT_SPEC.md
v0.16.6 → v0.16.9 as the rows land. Previous records (`row180`, `row181`): all folded, nothing
carried. Wish (the intake material): `docs/wishes/2026-07-09-test-method-lessons-tlvphoto-week.md`.

## Opening assessment

Five escaped-bug classes from one real week, each folded as a method rule rather than a project
patch. The seams that matter: each rule must name its boundary honestly (what the suite CANNOT see),
must not fork a second home for an existing law (the feel gate, the pinned skip-set, gate-on-log),
and the enforcement story for row 185 must name the actual miss mechanism before pinning anything.
All five are buildable; the findings below were folded as the rows landed.

## Row 182 — the real-device boundary (INV-77)

F1: the rule risked forking the feel gate's law (feel is the human's call). Folded: the clause names
itself KIN of the feel gate [INV-30] and adds only the matrix-row form (a row the suite can never
green); the gate's own law stays where it lives. `should-clarify · boundary-issue` — **folded**.

F2: "the suite cannot green it" needed a form — else the row reads as a TODO. Folded: the row states
WHAT the hands check and on which device class; it counts as owed-before-ship, kin of the norm gates.
`should-clarify · missing-outcome-check` — **folded**. Red proof: no INV-77 and no walk-row text at
HEAD (`git show` count 0).

## Row 183 — relative, wide, and long geometry (INV-78)

F1: ε and N are parameters, not law — the clause must not freeze numbers the surface owns. Folded:
the clause states the FORM (relative · ≥2 viewports · N consecutive steps); the numbers stay each
surface's matrix cell, set at derivation [INV-70]. `should-clarify · over-specific` — **folded**.

F2: index-cell hygiene — the formula's pipe characters would split the table cell; reworded to prose
("the distance between the element's center and the viewport's center"). `must-fix · consistency`
(a mechanical table break) — **folded**. Red proof: no INV-78 at HEAD.

## Row 184 — the engine tests on its own generic fixtures (INV-79)

F1: the law has two halves in two skills — fixtures (test-author) and the content contract
(spec-author) — and a half-landed delta would enforce fixtures while donor constants stay unnamed.
Folded: one anchor, both skills carry their half, the matrix row cites both surfaces so a half-form
goes red. `must-fix · atomicity` — **folded**.

F2: "generic fixtures" needed a boundary — donor data stays LEGAL as an extra suite (it is real
data); the law only forbids donor-ONLY. Folded: worded "never only the donor's data".
`should-clarify · abstraction` — **folded**. Red proof: no INV-79 at HEAD.

## Row 185 — the composition sweep did not fire (bug door, enforcement)

The miss mechanism, named from primary sources: the prover's composition/unwritten-seam lens
(INV-72) landed 2026-07-09 morning (commit `c1ef03b`) — AFTER the week's specs were authored and
proven; and adoption's "first recommended action" allows skipping the whole-spec re-prove when the
spec was "proven recently with no drift", which lets a spec proven under an OLDER lens set keep its
green. So the law existed and the ENFORCEMENT seam leaked at the version boundary.

F1: the skip exemption is lens-blind. Folded: ADOPT.md's re-prove exemption now requires the same
prover version — a prover that grew a lens since the last record re-arms the full pass.
`must-fix · unenforceable-promise (discharge)` — **folded**.

F2: the exemption is only checkable if records name the prover version that ran. Folded:
product-prover's persist rule now opens each record naming the running skill version (this record
models it). `should-clarify · observability` — **folded**. Red proof: ADOPT.md at HEAD carries no
version condition; records before this one name no prover version.

## Row 186 — the suite's own plumbing must not lie (INV-80)

F1: three legs risk three homes — skip-path executability, shim re-exports, and gate-on-log are one
class (the harness lying about its own verdict). Folded: one clause, three named legs, test-author
carries the operational text; the pinned-skip-set law it extends is cited, never restated.
`should-clarify · abstraction` — **folded**.

F2: "the wrapper's exit is never the verdict" must not contradict the CI gate (which DOES read exit
codes). Folded: worded as "a wrapper's exit code is never the verdict for a background or delegated
run — the gate reads the suite log's own tail line"; a foreground gate process reading its own
child's exit stays legal. `must-fix · internal-conflict (consistency)` — **folded**. Red proof: no
INV-80 at HEAD.

## Coverage note

CRUD/authorization: N/A (method prose). The sibling-instances sweep ran per row: each clause was
grepped across SPEC + all seven skills for restatements — none found beyond the intended homes.

## Closing

All folded; nothing rejected. The five rows land serially (shared surfaces: SPEC's test section +
test-author), one commit per row.
