# Row 470 synthesis — per-symptom verdicts on the tlvphotos partial-execution report (2026-07-23)

Evidence base: docs/audit/2026-07-23-tlvphotos-conduct-evidence.md (read-only collection, same day).
Each symptom Alexander named gets one verdict: a mechanism row opened, an existing net's reach widened,
or out-of-scope with its reason. The class finding closes the file.

## Verdicts

1. **False deferral ("нужно твоё слово" on derivable items).** Reach widened TODAY: the chat net's
   personal overlay (`~/.claude/hooks/hedge-personal.json`) knew only first-person shapes and fired 0
   times in 973 runs; six third-person patterns added and proven on the four documented phrases (old
   list missed 4/4, new catches 4/4, three clean working lines stay silent). The paraphrase class
   beyond any literal list is already owned by row 432 (the text-semantic judge), which now carries
   this digest as its evidence. The push-time marker gate keeps checking shape; truth stays with the
   deferral re-test (INV-152) plus the judge.

2. **Findings told in jargon.** The register judge is armed and fires (27 verdicts in the window); its
   floor is post-hoc by nature (a sent reply cannot be recalled). The artifact-side half is symptom 7's
   verdict. No new mechanism owed beyond it: the chat side keeps the judge; out-of-scope reason for a
   pre-send chat gate: the harness has no such surface (the standing "chat cannot be machine-gated"
   boundary).

3. **Remainder revealed at delivery.** No net existed. Row 472 opened: a done-claim disclosing its
   remainder up front — the report shape where remaining work is stated beside or before the done
   sentence, held by a Stop-hook arm on the done-then-remainder ordering plus a communicator rule
   sharpening.

4. **Time estimates absent from reports.** No net existed anywhere; the rule lived only in auto-memory.
   Row 471 opened: the estimate line becomes a profile contract line plus a Stop-hook arm on
   report-shaped replies, with the promised-vs-actual calibration line at landings.

5. **Inbox deposits late or batched.** The receiving side landed at 3.6.0 (INV-249). The sending side
   has no net. Row 473 opened: a conduct-judge member — a session whose reply names a cross-project
   finding owes the deposit in the same session, read from the action trace.

6. **Pointwise fixes where the spec states a class.** The pack law exists (the class hunt, INV-124);
   the field failure is a gate that shares the code's blind spot (a pinned nine-name list on both
   sides). This is row 446's exact subject (a gate's green must state and assert its reach), widened by
   the digest's evidence; the tlvphotos-side derive-from-CSS gate is already that host's queue row. No
   new pack row; row 446 carries the digest citation.

7. **Built well, shown unreadably.** The pack's net (text-audit, the cold-read loop) shipped at 4.0.0
   and tlvphotos still runs the ten skills of 3.6.0 — the net exists and is simply not installed there.
   Action rides the deposit: update the installed pack to 4.0.1 and run the cold-read before any shown
   page (the blocking preshow law, INV-83 kin).

8. **Collector's extra patterns.** (a) Ten near-identical judge sub-sessions per reply: row 474 opened
   (dedupe the judge chain by reply hash — cost and noise, no added verdict). (b) The hedge net's blind
   reach: folded into verdict 1. (c) Chrome saturation recurring after a root fix: already owned by the
   host's own journal discipline; recurrence there re-doors by the host's method, out of pack scope
   with that reason. (d) Register phrasings returning next day under post-hoc correction: the async
   floor stands; the upstream fix is the writing habit the judge trains, no new machine derivable.

## The class finding (his sentence, answered as a class)

"Всё, что мы внедряем, лишь частично исполняется" is itself the reminder-dependent-capability class the
pack named on 2026-07-19: a rule whose only body is prose degrades to partial execution; only a machine
holds a floor. The evidence table shows the exact split — where a machine exists and reads its true
subject, the rule holds (register judge fires, push gates red); where the machine is absent, mis-aimed,
or not installed, the rule decays (estimates 0/46, hedge 0/973, text-audit absent). Row 475 opened as
the standing answer: an enforcement-coverage registry — every agreed working rule names its net, the
net's reach, and where it is armed, held as one table with a check that reds a rule with no net and no
stated no-net reason; the rule→net table in the evidence digest is its seed.

## Actions taken this landing

- Hedge overlay widened + red-proven (see verdict 1); global, so every window inherits it at once.
- Rows 471–475 opened; rows 432 and 446 carry the digest as evidence.
- One deposit to tlvphotos/inbox (atomic, .draft then rename): update pack to 4.0.1 (brings text-audit),
  the blocking cold-read before showing, the estimate and remainder-up-front report duties until the
  pack nets land, deposit-at-finding, and notice of the widened hedge net.
