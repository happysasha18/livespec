# Adversarial audit — v2.4.0 landing (rows 365–368 + gate folds), uncommitted tree vs HEAD 2.3.0

**Method:** every claimed artifact read whole from disk; the reach script run by deed on all four verdict classes plus three mutated copies (block emptied, member with a space, member absent from disk) on this machine's bash 3.2.57 under `set -u`; the hygiene net's scanner run by deed against the real suite; the suite-budget watcher run by deed on synthetic slow and fast logs; M-206's timing assertion read raw and its test run; `check-prover-record.sh` and `check-freeze.sh` run by deed on the working tree; the full suite run by deed (965 passed, 1 failed, 96.09 s); both gate records and both disposition addenda verified claim-by-claim against the tree; `stamp-versions.py --check` run (0 files to stamp); ten installed skill copies binary-compared to source; `.git/hooks/pre-push` and `pre-commit` binary-compared to source; the old-facet wording, the old hardcoded add, and stale 2.3.0 stamps swept by grep.

## Findings

**1. BLOCKS-PUSH — the spec freeze gate is RED on the tree as handed; the push cannot go until the baseline is re-blessed.**
Evidence by deed: `bash guardrails/check-freeze.sh` → exit 1, "SPEC-FREEZE: RED (7 violation(s))" — INV-138 6→7, INV-18 6→9, INV-59 12→13, INV-72 12→13, T-13 4→5, '640px' appeared, and the INV-138 heading line changed. All seven are this delta's own intended edits (the watcher clause's [INV-59] cite, the F3 curated-list [INV-18] cites, the grown INV-138 clause), so the fix is the designed one: run the re-freeze in the landing commit ("re-freeze in this same commit", the gate's own fix line). `guardrails/pre-push:102` runs this check, so the push as handed reds beyond the one declared known-red. Nothing in NEXT_STEPS or either record names the re-freeze as a pending step — the batch was handed as push-ready and it is not. The gate is doing its job; the finding is the missing landing step, named so it is run deliberately rather than discovered at the hook.

**2. BLOCKS-PUSH — ROADMAP row 366's MET cell and M-348's matrix claim text describe the PRE-fold shape; the tree lacks what they state.**
Evidence: ROADMAP.md:265 claims "a marked, **empty-today** `ALWAYS_SCOPED` pinned block merged into every scoped verdict **ahead of the traceability ride-along**" and "reds any such test that sits outside the pinned set **and outside test_traceability.py's own unconditional ride-along**"; TEST_MATRIX.md:473 (M-348) claims "test_traceability.py **pre-pinned via its unconditional ride-along**" and "**ALWAYS_SCOPED empty today**". After the design-review F2 fold (its own addendum, docs/design-review/2026-07-17-2.4.0.md:164) the block is NOT empty — `tests/test_traceability.py` is its first member (guardrails/check-push-reach.sh:109) — and the hardcoded `add_scoped "tests/test_traceability.py"` is gone (grep over the tree finds no `add_scoped` outside the script's loop). The fold rippled M-349's claim text (rewritten to the merged facet) but left M-348 and the row-366 MET cell at the pre-fold shape. This is the string-grep trap of the batch: `test_enumerating_infra_tests_are_pinned` greps the script, and no test reads the MET cell or the matrix claim text against the script's shape, so both stayed green while lying about the delta being pushed. Delta-born claim-vs-tree divergence in two tested documents — by the pack's own delta-equivalence rule it is fixed before the merge, and the fix is two sentence edits.

**3. QUEUE-ROW — NEXT_STEPS' LIVE STATE contradicts ROADMAP on which rows are landed.**
NEXT_STEPS.md:3–17 still reads "v2.3.0 SHIPPED … NEXT: row 365, then row 366" with rows 365 and 366 listed under "ON RESUME — the queue", while ROADMAP.md:263–266 carries all four rows landed-MET and the tree carries the work. Also its "freeze green" line (:9) is stale against finding 1. The 2.3.0 precedent is a separate close-out commit that rewrites the resume file after the push (931b667), so this may be the planned order — but a session dying between the push and the close-out leaves the next session resuming into re-doing two landed rows. Cheapest is folding the LIVE STATE rewrite into the landing commit itself; at minimum it is the close-out's named duty.

**4. QUEUE-ROW — the merged-away facet's old name survives inside INV-138's own clause.**
PRODUCT_SPEC.md:1780 still says "…the **short-viewport facet** the author-side of this same lens [T-13]" — but after the design-review F1 fold there is no short-viewport facet: both viewport facets merged into "the viewport bands" (spec :376, spec-author SKILL.md:271). One facet now wears two names inside one document (the one-name-per-surface law). The trap named: `test_viewport_quantifier_lens` pins the new words ("the other bands", "viewport quantifier") and nothing pins the absence of the old facet's name, so the stale echo is invisible to the suite. Five-word fix ("the band-general viewport facet" or "the viewport-bands facet"); folding it before the push is cheaper than the row.

**5. QUEUE-ROW — the pack's own spec carries a band-scoped layout guarantee, and the gate record's N/A cell denies the surface exists.**
PRODUCT_SPEC.md:1328: "On a phone or narrow window the card reads as one column, top to bottom. [default]" — the settings card is a rendered HTML page and this is a layout guarantee scoped to the narrow band, with the short band (this very delta's incident class) silent. The prover record's coverage table (docs/prover/2026-07-17-2.4.0-minor-gate.md:84) marks the INV-138 viewport row "N/A — live-spec has no layout guarantee of its own", and its assumption (:38) reads the pack as having no layout-bearing surface — contradicted by the spec's own sentence. The new lens's first live subject was in the document under review and the pass did not draw the standing question the lens demands. Pre-existing sentence (not delta-born), so it queues: one row to answer the card's other bands (likely one [default] sentence — a single column tolerates any band), and the next FULL pass should read the card's facet block as the layout surface it is.

**6. NOTE — an `ALWAYS_SCOPED` member absent from disk rides into the scoped set and reds pytest at collection.**
The pinned-member loop (check-push-reach.sh:169–172) has no `[ -f ]` guard, unlike the changed-test-file arm that earned one at the 2.3.0 fold (:132). By deed: a script copy with a nonexistent pinned member printed `SCOPED tests/test_nonexistent_pinned.py`. A deleted-but-pinned member (the traceability net renamed, say) makes gate b red at collection — a false red, conservative direction only, and the hygiene net does not assert pinned members exist on disk. Mirror of the 2.3.0 audit's finding 6 for the block's own members.

**7. NOTE — the emptied-block road survives bash 3.2 by deed but prints a lying reason line; a real test stands behind it.**
By deed on a mutated copy: `ALWAYS_SCOPED=()` under `set -u` on bash 3.2.57 does not choke (the `"${ALWAYS_SCOPED[@]:-}"` guard), the scoped run proceeds without the net — and the reason line still says "+ the traceability net". The safety is real: `test_scoped_reach_guardrails_diff_exits_scoped` (tests/test_guardrails.py:651) asserts `SCOPED tests/test_traceability.py` in the verdict output, so an emptied block cannot ship green. The reason line's hardcoded suffix is the residue.

**8. NOTE — two parsing fragilities in the hygiene net, both failing conservative.**
`_always_scoped` (tests/test_guardrails.py:749) matches `ALWAYS_SCOPED=\((.*?)\)` non-greedy — a future member's comment containing `)` truncates the parse and un-pins later members in the net's view (false red, never a silent pass). `_call_args` (:697) counts parens without string-awareness, so a paren inside a string literal in an enumeration call's args mis-bounds the extraction (worst case a spurious flag — again conservative). Also noted for the record: the scanner genuinely flags `test_traceability.py` today via its root rule — `os.walk(ROOT)` in the clock-discipline test (tests/test_traceability.py:2163) — so the "integrity rider" is in fact also an enumerating test the net would demand pinned; the pin is doubly load-bearing.

**9. NOTE — the eval-suite watcher is the one discipline-cadenced cell of the five.**
ARCHITECTURE.md's skill-evals Watcher cell names "the eval suite reds any red scenario at each milestone run (INV-99)". The scenarios exist (evals/*.md), the dated records exist (docs/evals/), and a run reds mechanically — but nothing wires the run into any gate; WHEN it runs is held by milestone discipline, unlike the other four cells (suite budget, resume cap, style lint, M-206), each verified by deed to red inside the suite or a hook. Within INV-41's letter (the budget's own stated cadence is "at each milestone"); named so the weakest cell is on the record.

**10. NOTE — the row-365 red proof is two spliced runs with no labels.**
docs/prover/red-proof-2026-07-16-row365.txt opens with a collection miss ("no tests ran" — the new test's nodeid against HEAD's test file) spliced directly into a bare AssertionError tail from a second run, and never shows which needle failed. The red is real (FAILED …test_budgets_owe_watchers) but the file is below the standard the rows-367-368 proof sets in the same batch.

**11. NOTE — the installed communicator skill carries a stray `SKILL.html` absent from the repo.**
`~/.claude/skills/communicator/SKILL.html` exists beside the byte-identical SKILL.md; harmless to skill loading, but out-of-band content in an installed copy the sync does not own.

**12. NOTE — the spec header's date rode through the version stamp.**
PRODUCT_SPEC.md:1 reads "(v2.4.0, 2026-07-16)"; `stamp-versions.py` stamps the title version only (scripts/stamp-versions.py:53–58), so the date is the 2.3.0 header's. The delta work is dated 07-16, so it is defensible as the spec's last-edit date; if the header date means the version's date, a 07-17 push ships it a day early.

## What I tried to break and could not

- **The ALWAYS_SCOPED fold (row 366):** the reach script by deed — infra diff → exit 2 with `SCOPED tests/test_traceability.py` present, prose → 0, PRODUCT_SPEC.md → 1, unnamed infra file → 1 conservative; the `SCOPED` output line byte-matches the addendum's byte-identical claim. bash 3.2.57 under `set -u` by deed: one member, zero members, and a member with a space all iterate correctly. No code path expects the old hardcoded add (`grep add_scoped` finds only the script's own loop; no scaffold copy of the script exists). The hygiene net run by deed against the real suite: `REFERRER_DIRS` and the pinned set are parsed from the script itself (one home, no second copy), today's flagged set is exactly the pinned set, and the synthetic-red test plants its fixture by concatenation so the fixture is scannable while the test file itself is not — the discipline holds both directions.
- **The facet merge (rows 367+368 fold):** no stale echo of "phone or narrow window" in any skill or template — the only spec hit is :1328, a feature's own facet sentence, not a list echo (finding 5 covers its band scope); dated records and evals legitimately quote the old wording. The echo-canonical declaration (:375) agrees with the "one home" sentence (:386), with INV-18's index row, and with the pinned tests, which assert the facet set in BOTH homes.
- **The watcher table (row 365):** all five ARCHITECTURE.md Watcher cells name checks that exist. By deed: `check-suite-budget.sh` reds a synthetic 9999.99 s log naming both figures and greens 12.34 s; M-206's timing assertion is real — it parses `render-ms:` from the renderer's own instrumentation and asserts < 1000 (tests/test_onboarding_card.py:115–118), not a string grep; the resume cap is a real `CAP = 100` line count (tests/test_resume_digest.py:9); the style lint runs 0-errors on the real spec by deed and `test_convergence_locks` holds the ratchet floor. `test_budgets_owe_watchers` itself is needle-level — the by-deed verification above is what stands behind its green.
- **The quantifier lens (rows 367+368):** the spec clause (:1780), the INV-138 index row (:1961), the prover's edge-completeness lens (product-prover SKILL.md:269), and the design-reviewer's new H2 state one bound in one band language — every layout guarantee names "on every viewport" or its band, a band-scoped guarantee draws the standing question; the design review routes the undeclared-group case and never blocks; the pinned "two producers" sentence is byte-intact with the curation clause appended after it, exactly as the addendum claims.
- **Records vs tree:** every addendum claim verified by deed or by read — prover R1's softened sentence stands at :1682 ("holds to that bound"), R2's echo sentence at :375; design F1's merged facet in both homes with both incident keys and M-349 rewritten; F2 verified by deed above; F3's three curated-list citations present (spec :1682, spec :693, design-reviewer's producer line); F4 no-action; the two side edits (the design-reviewer H2, build-pipeline's ", each naming its watcher") both present. The red proofs for 366 and 367-368 are well-formed and honest.
- **The known red:** `check-prover-record.sh` red verified by deed with the exact "none are committed" branch; the mechanism confirms the claim — committing the record in the landing commit satisfies the tracked check, and same-commit counts fresh for both PRODUCT_SPEC.md and ARCHITECTURE.md (the script accepts SPEC_COMMIT == RECORD_COMMIT). The full suite by deed: 965 passed, 1 failed — only that gate — in 96.09 s, matching both addenda.
- **Version stamps:** `stamp-versions.py --check` → 0 files to stamp; VERSION, plugin.json, the spec header, all ten skill frontmatters and their in-text base references read 2.4.0; installed copies under ~/.claude/skills byte-identical (finding 11's stray html aside); the only 2.3.0 strings outside history/records are two comment citations of the 2.3.0 audit in templates/headless_harness.py — legitimate provenance, not stamps. Hooks byte-identical to source.

## Verdict

**BROKEN — on findings 1 and 2, both minutes to fix.** Every behavioural claim of the landing holds by deed: the reach script, the hygiene net, the five watchers, the lens homes, the addendum folds, the stamps, the suite. But the tree as handed does not pass its own push gate (the freeze is RED on seven delta-born drifts, and no record names the owed re-bless), and the row-366 MET cell plus M-348's claim text state a gate-machinery shape the same batch's own fold removed — the one divergence in this landing no test can see. Re-bless the freeze in the landing commit, rewrite the two pre-fold sentences (and take the finding-4 five-word fix while the file is open), and the landing HOLDS.


---

## Disposition addendum (2026-07-17 ~00:45, same session)

| # | Finding | Disposition |
|---|---|---|
| 1 | freeze RED on the delta's own edits, re-bless unnamed anywhere | FOLDED by process: the freeze re-bless runs as this landing's own step (spec-freeze.py --compaction over the three guarded docs) before the push, and the landing commit carries the re-blessed baseline; the resume file names the step done. |
| 2 | ROADMAP 366 MET + M-348 claim text still described the pre-fold shape | FOLDED: both rewritten to the post-fold shape — the block carries the integrity rider, the hardcoded add is gone (ROADMAP row 366 cell; TEST_MATRIX M-348 claim). |
| 3 | NEXT_STEPS LIVE STATE stale (rows named as next were landed) | FOLDED: LIVE STATE rewritten at this landing to the shipped-2.4.0 state. |
| 4 | PRODUCT_SPEC :1780 still named "the short-viewport facet" after the merge | FOLDED: renamed to "the viewport-bands facet" (one phrase). |
| 5 | the spec's own settings-card layout guarantee is band-scoped while the prover record claimed the pack has none | FOLDED both halves: the guarantee at :1328 now answers the other bands with a [default] sentence (the new lens's first live subject); the prover record's coverage cell corrected by this addendum's cross-reference — the record's "N/A" cell reads too narrow, the live subject stands at PRODUCT_SPEC.md:1328 and now carries its quantifier answer. |
| 6 | an ALWAYS_SCOPED member missing from disk rode into pytest collection as a bare red | FOLDED: the merge loop now reds loudly under its own name on a stale pin (BLOCK naming the file), proven by deed on a mutated copy; the honest red arrives before pytest. |
| 7 | the reason line would lie if the block were emptied | ACCEPTED-NO-ACTION: tests/test_guardrails.py:651 reds that state before it could ship; the suite is the watcher. |
| 8 | net parser bounds (first `)`, string-blind) — both conservative; the traceability pin doubly load-bearing | ACCEPTED-NO-ACTION: conservative directions recorded; the pin's second duty is stated in the block's comment. |
| 9 | the eval-suite watcher is discipline-cadenced, the other four red mechanically | ACCEPTED-NO-ACTION: the cell already names the milestone cadence; the cadence itself is the method's own gate step. |
| 10 | the row-365 red proof spliced two runs without labels | FOLDED: a reading guide now heads the file naming both runs and the failed needle. |
| 11 | stray installed communicator/SKILL.html absent from the repo | FOLDED: the stray removed from the installed copies. |
| 12 | the spec header date rode through the stamp | FOLDED: header now reads (v2.4.0, 2026-07-17). |
