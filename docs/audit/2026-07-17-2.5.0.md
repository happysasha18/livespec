# Adversarial audit — v2.5.0 landing (row 369 + gate folds), uncommitted tree vs HEAD 2.4.0

**Method:** every claimed artifact read whole from disk; the full diff read against HEAD (17 files at the first status, 18 by the end — see finding 1); the full suite run by deed (964 passed, 1 failed, 2 skipped, 35.76 s); the four pinned lens tests run by deed (`test_paired_transition.py` + `test_edge_completeness.py` + `test_gesture_overlay_parity.py`, 22/22 green); `spec-style-lint.py` run on PRODUCT_SPEC.md (0 errors); `check-freeze.sh`, `check-prover-record.sh`, `check-pin-drift.sh` (54 pins strict-clean), `check-push-reach.sh`, and `stamp-versions.py --check` (0 files to stamp) run by deed; both gate records and both disposition addenda verified claim-by-claim against the tree; the three anchor swaps read whole with INV-146's index row and the whole Formal index scanned for self-citing rows; the old bold lead, "viewport-band lens", stale 2.4.0 stamps, and stray INV-138 cites swept by grep across the repo and the installed copies; ten installed skill directories recursively compared to source (byte-identical, no strays).

## Findings

**1. BLOCKS-PUSH — the suite is RED on a delta-born line, and the tree moved during the audit.**
Evidence by deed: `python3 -m pytest tests/ -q` → 1 failed, 964 passed — `test_gate_green_on_the_swept_tree` reds on the shipped-language gate: FEEDBACK.md:13 `[owner-name]` ("2026-07-17 10:59 · Alexander in chat, on the lifted named-part law …"). That ledger line did not exist when the audit opened: the first `git status` (≈10:58) showed 17 modified files without FEEDBACK.md; by 10:59:47 (the file's mtime, checked by deed) the line was there and the file modified. Two consequences. First, the prover record's suite claim ("965 passed, 2 skipped … exit 0", record :9 and :157) is true of the tree the record saw and false of the tree as handed — the batch reds its own suite beyond the declared freeze red, and `guardrails/pre-push` runs `check-tests.sh`. Second, the audit's subject moved while the audit ran — the batch was handed as a fixed thing and was not. The fix is the gate's own fix line: state the ledger line impersonally (the ledger's other lines already write "the tlvphotos window", never a name) or allowlist deliberately; and hold the tree still while a fresh-eyes pass is on it.

**2. BLOCKS-PUSH — ROADMAP 369's MET cell and M-351's claim text describe the PRE-fold lens; the same batch's own last fold removed what they name.**
Evidence: ROADMAP.md:268 and TEST_MATRIX.md:191 both claim "the design review's **viewport-band lens** and the spec-author viewport facet **cite it** as their group-pass and author-side instances." After the design-review F1 fold — the generalization written by hand last, after both reviews ran — there is no viewport-band lens: the H2 is `## The standing named-part lens (SPEC INV-138)` (skills/design-reviewer/SKILL.md:60), the lens body is general (any named part of a guarantee's domain forms the same-kind group), and the viewport bands are demoted to "the worked group" (:64). The tree does more than the claim says, and the claim's named object no longer exists under that name. The trap named: `test_general_sub_domain_duty` checks only that the M-351 row contains "INV-138", and no test reads the MET cell or the claim text against the skill's shape, so both stayed green — the exact string-grep trap the 2.4.0 audit's finding 2 caught in the same two documents. Delta-born claim-vs-tree divergence in tested documents; by the pack's delta-equivalence rule it is fixed before the merge, and the fix is two sentence edits (say the lens IS the named-part lens with viewport its worked group; the spec-author facet's "cites it as the author-side instance" half stays true, skills/spec-author/SKILL.md:274–276).

**3. QUEUE-ROW — the prover record quotes a design-reviewer sentence the fold removed, and no addendum names the rewrite.**
docs/prover/2026-07-17-2.5.0-minor-gate.md:62 quotes, as evidence of the four-home agreement, `"It is the group-pass instance of the general sub-domain duty the range law carries — … with the viewport bands its worked case…"` — `grep "group-pass" skills/design-reviewer/SKILL.md` finds nothing; the sentence was the intermediate lens text the pass reviewed, rewritten whole by the post-pass fold. Record :7 carries the same stale shape ("the design-reviewer band lens cites itself as the group-pass instance"). The record's only addendum folds R1 and never names that the fourth home's text changed after the pass — so the landing's central agreement claim rests on a quote pointing at nothing, and nobody has re-proven agreement over the rewritten lens. This audit re-checked it by read: the new lens carries the same core duty ("a guarantee scoped to one named part … the parts of that domain are a same-kind group"), routes through the same channel ("a recommendation or a question, never a blocker"), and hands declared groups to "the prover's named-part ask" — the agreement holds in fact. One addendum line naming the fold and re-stating the agreement over the current text closes it.

**4. QUEUE-ROW — the prover record again denies the spec's own band-scoped guarantee exists, against the 2.4.0 audit's explicit instruction to the next FULL pass.**
Record :32 assumes live-spec has "no layout-bearing surface," and the INV-138 coverage cell (:93) reads "N/A — live-spec has no sub-domain-scoped layout guarantee of its own." PRODUCT_SPEC.md:1328 carries one: "On a phone or narrow window the card reads as one column, top to bottom. [default] On every other viewport band the card keeps its multi-column arrangement… [default]" — the settings-card guarantee the 2.4.0 audit's finding 5 established as the lens's first live subject, answered by that audit's own fold, with the disposition stating "the next FULL pass should read the card's facet block as the layout surface it is." This FULL pass repeated the denial instead. No spec hole rides on it (the guarantee is answered), so it queues as a record correction — but it is a recurrence of an agreed-fixed miss, the second FULL pass in a row whose N/A cell contradicts a sentence in the document under review; the standing-assumption line the record carries forward from prior passes is the mechanism, and the fix is to retire that assumption, not re-correct the cell each release.

**5. QUEUE-ROW — NEXT_STEPS' LIVE STATE contradicts the tree, and no home names the freeze re-bless step.**
NEXT_STEPS.md:3–8 reads "v2.4.0 SHIPPED … Queue below is all that remains" and :14–20 lists row 369 as a queue item "IN WORK this session," while ROADMAP.md:268 marks it landed-MET, VERSION reads 2.5.0, and the tree carries the work. The 2.4.0 audit flagged this same class (its finding 3) and the disposition folded the LIVE STATE rewrite into that landing; this landing hands the stale state again. Likewise its finding 1's disposition made naming the freeze re-bless the landing's own step ("the resume file names the step done") — nothing in NEXT_STEPS or either record names it this time (finding 6 below carries the red itself). Both are the close-out's minutes; folding them into the landing commit is the precedent the 2.4.0 addendum set.

**6. NOTE — the freeze gate is RED on exactly the delta's own two edits; the re-bless is the landing's named step, not a defect.**
By deed: `bash guardrails/check-freeze.sh` → exit 1, "SPEC-FREEZE: RED (2 violation(s))" — INV-146 count 9→12 (the three anchor swaps) and the changed marker line (the broadened bold lead). Both are this batch's intended edits, so the designed fix applies: re-freeze in the landing commit. Named here so it is run deliberately rather than discovered at the hook — and see finding 7 before blessing the new INV-146 count.

**7. NOTE — two of the three anchor swaps land as self-cites; the third is right; no fourth miscite survives.**
The swap's meaning verified whole: the re-surface fact ("surfaced afresh … rather than sitting durably-recorded but operationally invisible", PRODUCT_SPEC.md:1721) and the marker-exclusion reasoning are INV-146's own — the old [INV-138] cites were the miscitation the design review's note called them, and INV-146 is the right family. But at :1721 the cite now sits inside INV-146's own paragraph (whose trailing anchor is already [INV-146]), and at :1908 INV-146's Formal-index row now cites [INV-146] inside its own definition — a scan of the whole index finds no other self-citing row, so the swap minted the index's only circular cite. Only :1731 is a genuine cross-cite (the coordinator paragraph naming INV-146's reasoning from inside INV-149's section) and stands right. The freeze gate's own words for the risen count were "rose — invented cite"; dropping the two self-cites (leaving the prose) is cheaper now than blessing them into the baseline. Swept for a fourth of the family by deed: the remaining INV-138 cites (spec :376, :451, :1779, :2050, :2071) are all legitimately range-law; no stranger-door sentence still cites it.

**8. NOTE — two wording shades in the hand-written lens generalization, the batch's one unreviewed text.**
Read hardest, as the only text no review saw. (a) "every other spec **has** a same-kind group of the same shape" (skills/design-reviewer/SKILL.md:62) — overclaims; a spec with no part-scoped guarantee has none ("can have" is the fact). (b) "the guarantee is **meant to hold the same element across every part**" leans uniformity-as-default, where the owner's same-morning word (FEEDBACK.md:13) pins the demand as completeness of ANSWERS — "a legitimate decided difference" satisfies it, never 100% behavioural uniformity. The lens honors the boundary operationally (its output is "a recommendation or a question, never a blocker", and the phrasing is inherited verbatim from the 2.4.0 band wording), so this is a shade, not a contradiction — but the generalization widens the sentence's reach to every domain, so the shade travels. Every pin held by deed: "viewport band" (test_paired_transition.py:146), "two producers" (:87 intact), the three motion groups, the H2's SPEC INV-138 — 22/22 green; no cross-reference anywhere in the pack still says "viewport-band lens" outside the two claim texts of finding 2.

**9. NOTE — the MET cell's "leads the INV-138 clause and its Formal-index row" overstates by a hair.**
In both homes the general sentence leads the sub-domain passage, not the clause or the row — the bold lead and the both-ends duty lead both (spec :1779, :2071). The prover record states it precisely ("leads the sub-domain portion", record :7); the ROADMAP cell rounds up. Covered by finding 2's rewrite if taken in the same pass.

## What I tried to break and could not

- **The lens generalization (finding 2's subject, on its own terms):** every pinned needle by deed — 22/22 across the three test files, including the `"viewport band"` substring (:146's pin), `"two producers"`, the three motion-group headings, and the row-368 viewport needles in all three homes. The new lens contradicts neither the prover lens nor the spec clause: all three state one duty in one order (scoped part → standing question about the remainder → decided or `[default]` per part), all three keep the block/recommend split (design review recommends on undeclared groups, the prover holds declared ones), and the incident parenthetical rides intact in the worked-group paragraph. The H2 rename orphaned nothing in the skill or the pack — the only survivors of the old name are the two claim texts of finding 2.
- **The bold lead:** the pinned substring "A gated behaviour names every side of its gate" survives inside the broadened lead (spec :1779); `test_edge_completeness.py` green by deed; grep over skills/, templates/, docs/, and the installed copies finds no other home quoting the old one-sentence lead verbatim — the only hits are the test's substring and the prover record's R1 history quote, both legitimate.
- **The anchor swap's completeness:** the design-review note named three sites; all three are swapped (spec :1721, :1731, index row), the stranger-door region carries zero INV-138, and the DR addendum's F3 claim matches the tree exactly. (The two self-cites are finding 7 — placement, not family.)
- **The addenda vs the tree:** prover R1's fold — the new lead stands verbatim at :1779 with the pinned substring surviving, exactly as claimed. DR F1's fold — the standing named-part lens stands with viewport as the worked group. DR F2 — row 370 queued at ROADMAP.md:269 citing the record. DR F3 — verified above. Every addendum claim points at real current text.
- **The red proof:** docs/prover/red-proof-2026-07-17-row369.txt is single-run, labeled, names the failed needle ("'a named part of its domain' not found"), the exact command, and the invariant decision — the standard the 2.4.0 audit asked for after its finding 10.
- **Stamps and installed copies:** `stamp-versions.py --check` → 0 files; VERSION, plugin.json, the spec header (v2.5.0, 2026-07-17), all ten skill frontmatters and their in-text base references read 2.5.0; grep finds no stale 2.4.0 stamp outside dates and history; all ten installed skill directories under ~/.claude/skills byte-identical to source by recursive diff, no stray files (the 2.4.0 audit's SKILL.html stays gone).
- **The other gates by deed:** `spec-style-lint.py` → 0 errors 0 warnings on the real spec; `check-pin-drift.sh` → 54 pins strict-clean (matching the record's claim); `check-prover-record.sh` → green (the committed 2.4.0 record dated 2026-07-17 satisfies it; the 2.5.0 record joins at the landing commit); `check-push-reach.sh` → conservative fall-through to FULL on the unstaged tree, the safe direction. Suite budget: 35.76 s against 360 s.
- **M-350 vs M-351 coexistence and the matrix's traceability:** both rows anchor INV-138 under `### [node: spec-author]`, the new test is on disk, BUILT, and green forward; red-proven backward per the proof file.

## Verdict

**BROKEN — on findings 1 and 2, both minutes to fix.** The lift itself holds by deed everywhere it was tested and read: the four homes state one duty, the pins survive, the red proof is honest, the stamps and installed copies are clean, the style lint and pin drift are green. But the tree as handed reds its own suite on a ledger line written after the record's green run (and after this audit opened — the batch moved under review), and the ROADMAP MET cell plus M-351's claim text name a "viewport-band lens" the batch's own last fold renamed and generalized — the one divergence in this landing no test can see, the same trap class as 2.4.0's finding 2, in the same two documents. Reword the ledger line, rewrite the two claim sentences (take findings 5 and 7's minutes while the files are open), run the named re-bless, and the landing HOLDS.


---

## Disposition addendum (2026-07-17 ~11:11, same session)

1 (owner-name line reds the shipped-language gate): FOLDED — the ledger line and the fresh inbox seed now say "the owner"; gate green by deed. 2 (landed claims named the pre-fold lens): FOLDED — ROADMAP 369 and M-351 now say the standing named-part lens. 3 (record quotes a rewritten sentence): FOLDED — addendum line in the prover record. 4 (the "no layout guarantee" claim recurred): FOLDED — addendum line retires the carried-forward assumption. 5 (resume file stale, re-bless unnamed): FOLDED at this landing — NEXT_STEPS rewritten, freeze re-blessed as the landing's own step. 6 (freeze red pre-re-bless): named, the landing step. 7 (two self-cites): FOLDED — the two [INV-146] self-cite anchors dropped, the genuine cross-cite kept. 8 (lens overclaims): FOLDED — "any spec can carry"; the parity sentence now states answers-completeness (the owner's same-morning boundary: a decided difference satisfies the law). 9 (MET cell overstate): FOLDED — "leads the sub-domain passage".
