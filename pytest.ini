# pytest.ini — a stranger's `pytest` from the repo root runs the real suite (ROADMAP row 106).
# The suite lives in tests/; templates/ ships document shapes (including a *.template.py whose
# test_* NAME pytest would otherwise collect and trip over) — templates stay templates.
[pytest]
testpaths = tests
