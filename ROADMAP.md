# live-spec Roadmap (dated version: 2026-07-06 — updated at every edit; SPEC M-3)

The wish queue. Intake is continuous — a wish lands here the moment it is spoken. Execution is serial:
the current landing finishes before the next starts. Class = size (bug / small / surface / large — one
vocabulary with the spec) plus a priority mark when it isn't normal: a critical bug lands before
everything; a quick win may bubble up between landings, the jump marked in its row (SPEC T-11, INV-12).
A row also names its DOOR — feature · bug · refactor · docs-only · skip — said at intake, before any
code; feature tripwires outrank a casual label and queue-cutting stays with the bug door (SPEC T-12, INV-16).
And it names its WORK-KIND — product · infra · skill · prose — what the wish builds; the kind scales the
form each pipeline step takes, and a stood-down step is named in the landing report, never silently
skipped (SPEC T-16, INV-22).

Closed rows live in dated archives under docs/queue-archive/ (INV-1); this table holds the ACTIVE queue.

| # | Wish (plain words) | Class | Status | Decision / acceptance |
|---|---|---|---|---|
| 74 | A plain-language map of the whole structure (Alexander 2026-07-05 evening: "помоги навести порядок. чето структура странная очень выходит" — he can't hold where things live: pack vs profile vs playbook vs projects, base vs four working skills). One readable page: what each piece IS, where each kind of rule lives, one home per fact — in his words, no jargon; likely grows the README or a new OVERVIEW doc | small | **landed 2026-07-05 ~20:19, session 8** (door: docs-only; built session 7) — Alexander read the rendered OVERVIEW and thanked; his follow-up question (why the playbook loads per-session, not always) answered in chat | Done when: Alexander reads it and says the structure is clear; every "where does X live" question in it answerable in one line — met on his read |
| 12 | Mine CLAUDE.md + PLAYBOOK for rules not yet in the package skills — map rule → skill → gap, fold gaps (Alexander 2026-07-04) | large | mapping done, folding started | 45 rules mapped (22 full / 13 partial / 10 gap); 11 package-worthy gaps ranked in `~/.claude/playbook/claudemd_mining_s51.md` (moved from /tmp 2026-07-05 — a map quoting the private playbook stays out of the public repo). Folded: gaps 1+2 as one class (primary-source discipline: base rule 13 + build-pipeline reconcile sentence + prover meta-rule, skills → 0.1.1, 2026-07-05); gap 3 (fix-the-class sweep: base rule 14 + build-pipeline bug-path sentence + prover 3e sibling-instances lens; header rule-list + base pin swept across all four working skills, 2026-07-05 ~15:05 — unblocks row 52's flip); gap 4 folded (recurring bug re-doors to feature — build-pipeline 0.2.12, M-100, session 9); gaps 5+8 folded as one docs-discipline class (CHANGELOG speaks to the user · no drifting version pins — build-pipeline 0.2.13 step 9 + spec-author 0.1.11 anti-pattern, M-101, session 9); gaps 6 (delegation savings line, M-102) · 9 (prover domain-language lens) · 10 (step 5 teaches DO+NEVER) folded session 9; REMAIN: gap 7 verify-only (standing default-delegate list — likely folded by the delegation gate, cite it in the map) + the stale-copy sweep rule (map line 59) |
| 14 | Custom-steps / CI adapter guidance — e.g. a host wires the guardrails into Jenkins/GitHub Actions as a second net (Alexander 2026-07-04) | small | queued | ADOPT/README section: same checks, CI mirror; one worked example |
| 27 | Name: "live-spec" (hyphen) instead of "livespec"? (Alexander floated it 2026-07-04) | small | **decided 2026-07-05** | Alexander picked **live-spec** (hyphen) on the decision page, over the keep-the-old-spelling recommendation; execution is row 40 (full one-name sweep + host migration note + GitHub rename at the reviewed push) |
| 33 | Is the playbook repo + CLAUDE.md symlink still needed, or obsolete now that live-spec exists? (Alexander 2026-07-05) | small | **decided 2026-07-05** | Alexander accepted the recommendation: keep as the thin PRIVATE personal layer for now; his caveat — this may overlap with live-spec's own personal settings, think with examples — moves to row 43, which owns the final split |
| 42 | Package structure design (from the decision-page answer on the base skill): Alexander leans ONE package (base inside), though prover already lives as a separate public repo too — design how the package and the standalone skill repos relate (single source? copies? install pulls?) and how live-spec loads/attaches other repos. Needs worked examples → a decision page | surface | **decided 2026-07-05** (page 2, 12:48) | Alexander picked **package-is-source** (recommended): the pack repo is the single source, standalone repos are mirrors. His note — reusable, self-contained parts (prover, maybe base, maybe all) must stay findable alone — is exactly what mirrors give; execution = row 51 (a mirror per skill + one sync command). D-4 closes |
| 43 | Personal-settings architecture (from the decision-page answer on the profile home): what lives in CLAUDE.md vs the playbook vs live-spec's own settings — Alexander's framing: "live-spec global personal settings" + "live-spec per-project settings"; absorbs row 33's overlap caveat and row 38's git-home debt. Needs worked examples → a decision page | surface | **decided 2026-07-05** (page 2, 12:48) | Alexander picked **all-into-profile** (over the three-homes recommendation): everything personal moves into live-spec's settings with servlet-like SCOPES — nested, inherited ("у каждой штуки свой скоуп"); CLAUDE.md becomes a thin loader. Plus a setup step: understand who you work with (profile bootstrap). Execution = rows 52 (migration) + 53 (scope model) + 54 (onboarding); D-5 closes as a direction, the design lands through those rows |
| 44 | Learn from other projects/frameworks — study how others run spec-driven / agent-driven development, harvest ideas worth folding (Alexander 2026-07-05: "поучиться у других проектов… но это отдельный version bump") | large | deferred — own version bump; HOMEWORK DONE 2026-07-05 evening | Two research reports in: `docs/research/2026-07-05-methods.md` (spec-kit/BMAD/Kiro/Shape Up/Google/Amazon — top-10 candidates incl. regression-fence sentences, waived-risk verdict, non-goals, facet curation) + `docs/research/2026-07-05-skill-patterns.md` (Anthropic/superpowers/Trail of Bits — top-10 incl. when-NOT-to-use sections, skill evals, loadability check). Candidates go to Alexander on a decision page 2026-07-05; his picks become queue rows. Revisit trigger unchanged: lands as its own MINOR |
| 47 | Feedback-collection skill — a generic pack skill for the human (or their users) to SAY, SHOW, or SEND something into the project at any moment (the intake side of communicator: decision pages go out, this brings feedback in; the inbox pattern generalized beyond wishes) (Alexander 2026-07-05: "шикарная вещь") | surface | queued — design after rows 40–43 | Done when: skill spec'd (channels: text, screenshot/artifact, file drop → each lands as a queue/inbox item with provenance + timestamp), proven, shipped in the pack |
| 48 | Maintenance skill with measurement PLUGINS — after shipping, the product is MEASURED: e.g. a Google Analytics plugin; each user story / canonical axis states how (or explicitly whether) it is measured in the field; findings feed back into the queue (Alexander 2026-07-05) | large | deferred — own version bump | Revisit trigger: first host with a live audience; design pairs with row 47 (feedback in → measurement in) |
| 49 | A/B tests as a pack capability — for software hosts (not every host: a book has none), a wish may land as an EXPERIMENT with two variants and a measured pick; builds on row 48's measurement layer (Alexander 2026-07-05) | large | deferred — own version bump | Revisit trigger: row 48 lands; spec must state where experiments live in the wish lifecycle |
| 54 | Onboarding step — "understand who you're working with": at attach (or first run on a new machine/human) the pack builds or loads the human's profile — the human tells about themselves and/or agrees to a data pull; nothing written without their word (INV-9 holds) (Alexander 2026-07-05) | surface | queued | Done when: ADOPT/bootstrap gains the step with a done-when, profile template exists, first real run recorded |
| 55 | (prover 2026-07-05-architecture, F1) Snapshot machinery gets its own queue row — SPEC S-0 promises every [target] is owned by a row, and the snapshot (E-7, A-6: baseline of the last accepted run, declared-scope diff) had none; design pairs with the guardrails scaffold (row 3) which consumes its diff | surface | queued | Done when: snapshot design spec'd (retention D-3 decided with it), baseline advance wired to *landed*, declared-scope diff feeds the guardrails |
| 56 | (prover 2026-07-05-architecture, F1 — same class as row 55) The model router (ACT-3 [target]: cheapest sufficient tier, budget-aware) had no owning queue row either; D-2 (who fixes the tier) closes with this design | large | deferred — after rows 52–54 | Done when: router rule spec'd (queue class → proposed tier, senior override logged per D-2), first real routed landing recorded |
| 57 | (prover 2026-07-05-architecture, F2+F3) Two mechanisms live in the pack but have no spec sentence: `install.sh` (how skills arrive on a machine — attach node carries it today via A-7's record clause) and the decision page (communicator rule 10 — backed today only by the batched-questions clause in the wish walk). Give each one sentence + anchor at the next spec touch | small | **landed 2026-07-05 ~22:15, session 9** (door: bug; kind: prose; steps stood down by name: facet sweep — bug door, no feature delta; design-sync — text product; architecture = assignment only, no re-prove) — SPEC v0.15.1: E-21 installer clause (adoption home) + E-22 decision-page clause (wish-walk home), index + owns-columns extended, matrix M-091/M-092; M-091's REAL install run (fresh temp home, twice) caught a real bug — install.sh lacked mkdir -p and failed on a genuinely fresh machine; fixed same change; suite 73 green; pack 0.8.7 | Done when: SPEC names the installer and the decision page, each with an anchor; index updated; architecture owns-columns extended to the new anchors. Partial 2026-07-05 ~15:05: the hook-install half landed in E-6 — hooks OFFERED never imposed: git hosts only, installed only after asking with a plain-words explanation (Alexander's word this session); installer + decision-page sentences remain |
| 59 | Worker contract: a worker inherits its session's write-ownership narrowed to its brief's files; same-session sibling worker files are fence-benign; live session-scope lines pass into worker briefs; a result failing its brief's acceptance escalates one tier, logged | small | **landed 2026-07-05 ~22:40, session 9** (door: feature; kind: skill; fences held: INV-10 · INV-11 (sharpened not weakened — prover D1) · E-13 · D-2 stays open (D2); stood down by name: facet sweep + design-sync — skill kind; non-goals: no mechanical enforcement of the narrowed ownership yet — rides the router design (row 56); success measure [default]: the next delegated brief carries the contract lines, visible in its checkpoint) — SPEC v0.15.6 ACT-3 contract + pipeline 0.2.10 delegation elaboration; matrix M-095; eval-re-run duty recorded per E-19 (prover D3); suite 79 green; pack 0.8.12 | Source: audit 2026-07-05 (fable F2, opus F-12, composition H2) |
| 60 | One name-collision law, stated once in the base skill: prefix/base name, then numeric suffix -2, -3 everywhere (attic second collision, inbox concurrent same-slug — add a per-session token if needed) | small | **landed 2026-07-05 ~22:23, session 9** (door: feature; kind: skill; fences held: E-9 attic manifest · INV-10/E-11 inbox one-new-file · rule-10 ordinal mechanics — all intact, now citing; steps stood down by name: facet sweep — no visible surface, skill kind; design-sync — text product; non-goals: no renaming of already-archived files, no third scheme legalized; success measure [default]: the next real collision in any home resolves by the law, checked by eye at the milestone audit) — base rule 18 (0.1.11) states it ONCE; SPEC v0.15.2 attic+inbox clauses, ADOPT and communicator rule 10 CITE it (the prover's F2 caught rule 10 as the unswept fourth); pin sweep across five skills; matrix M-093; prover record rows57-60; suite 74 green; pack 0.8.8 | Source: audit (fable F3, opus F-7, comp N6) |
| 61 | Pre-push gate freshness: the prover record must be newer than the last commit touching SPEC.md, not merely today-dated | small | **landed 2026-07-05 ~23:45, session 8** (door: bug; kind: infra; Sonnet worker on a self-contained brief, red→green in checkpoint, senior verified by re-running gate + suite) — gate a now checks record-commit ≥ last SPEC.md commit (same-commit passes, M-6's fold clause honored); tests test_stale_record_fails + test_record_with_spec_same_commit_passes; suite 70 green; M-015 re-authored | Source: audit (fable F4); lands in guardrails/pre-push + its tests |
| 62 | Bootstrap order + first landing: VCS gate FIRST, then templates + a minimal runnable suite scaffold; define green-suite for landing #1; hooks offered at bootstrap as at adoption | small | **landed 2026-07-05 ~22:45, session 9** (door: feature; kind: infra; fences held: INV-8 intact · A-0 parity · E-6 cited not restated (prover E4) · template set unchanged; stood down by name: facet sweep + design-sync — infra kind; non-goals: no real bootstrap run tonight — the simulation is the test, the first REAL bootstrap journals the walk; success measure [default]: the next new project starts gate-first with a green scaffold, its journal says so) — SPEC v0.15.7 B-1 rewrite + templates/test_scaffold.template.py shipped + by-deed simulation test both ways; M-034 re-authored; suite 81 green; pack 0.8.13 | Source: audit (fable F6, opus F-13) |
| 63 | Declining a wish lists its absorbed (superseded-into-it) rows; each declined by name or returned to the queue | small | **landed 2026-07-05 ~22:27, session 9** (door: feature; kind: prose; fences held: T-8 three-exit vocabulary intact, INV-1 cited not restated; stood down by name: facet sweep + design-sync — prose kind, no visible surface; architecture: assignment only, T-8 already build-pipeline's; non-goals: no mechanical queue check yet — the behavioural side is a named M-1 audit item; success measure [default]: the first real declined absorber names its absorbed rows, checked at the milestone audit) — SPEC v0.15.3 T-8 extension + ROADMAP template status-values line (prover A3: the template was the half-form surface); matrix M-094; prover addendum in rows57-60 record; suite 75 green; pack 0.8.9 | Source: audit (fable F8) |
| 64 | Mechanize S-0: traceability test — every [target] token maps to a non-landed queue row; a spec section whose architecture pin is real carries no [target] | small | **landed 2026-07-05 ~22:31, session 9** (door: feature; kind: infra; fences held: S-0 granularity clause intact, existing traceability checks untouched; stood down by name: facet sweep + design-sync — infra kind, no visible surface; non-goals: prose-level [target] tokens NOT parsed — the index is the normative surface, S-0's granularity rule makes it so; success measure [default]: the next target drift is caught by the suite, not by an audit pass) — declared map anchor→open-row in `test_targets_owned_by_open_rows` + pin honesty in `test_target_nodes_pin_honesty`; building it caught TWO live drifts (E-10 index mark lost; M-061 citing landed row 3) — both folded, red proven vs pre-fix spec; SPEC v0.15.4, M-052 TODO→BUILT; suite 77 green; pack 0.8.10 | Source: audit (opus F-10) |
| 65 | Milestone audit item: the thin loader stays thin — every line passes the must-hold-before-pack-loads test, count stated in the audit report | small | **landed 2026-07-05 ~22:34, session 9** (door: feature; kind: prose; fences held: M-1 gate list grows, never shrinks — all prior items intact; INV-7 checked at the prover (C2: migration moves the RULE, loses nothing); stood down by name: facet sweep + design-sync — prose kind; non-goals: no mechanical test of the loader file itself — it lives outside the repo, the item binds the audit WALK (prover C1); success measure [default]: next milestone's report states the count) — SPEC v0.15.5 M-1 item + first walk done NOW: 16 non-empty lines, all pass, two provenance pointers named prune-candidates; M-029 extended; suite 78 green; pack 0.8.11 | Source: audit (fable F7); pairs with row 52 |
| 66 | Freshness on the pack developer's own machine: repo skill copy vs installed copy — a named sync rule/script + the A-7 re-read | small | **landed 2026-07-05 ~22:48, session 9** (door: feature; kind: infra; fences held: A-7 cited not restated · D-4 (repo stays source) · E-21 distinct by actor+direction (prover H1); stood down by name: facet sweep + design-sync — infra kind; non-goals: no auto-sync on commit — the tool is called, not hooked, until a real stale-skill incident argues otherwise; success measure [default]: next skill-editing session's journal shows the script's old→new line instead of a hand-copy) — scripts/sync-skills.sh shipped + real-run tested twice + run for real on this machine; SPEC v0.15.8 E-23; matrix M-096; suite 83 green; pack 0.8.14 | Source: audit (composition H4) |
| 67 | Standalone skill installs ship template-less: spec-author and build-pipeline point at templates that don't resolve from the skill dir — decide ship-inside-skill vs fixed pointers | small | **landed 2026-07-05 ~22:51, session 9** (door: bug; kind: skill; fork taken on the recommended option: FIXED POINTERS to the pack repo — a copy would fork the truth (D-4); surfaced for veto, a veto is a new wish; fences held: skill texts' template steps intact, D-4 direction obeyed; stood down by name: facet sweep + design-sync — skill kind; non-goals: no template fetch automation — the pointer informs, the human/agent fetches; success measure [default]: the next standalone install finds its templates on the first read) — both skills name the pack repo as the templates' home + negative side tested (in-skill templates/ dir = red); A-7 sync via the row-66 tool (its first real job): build-pipeline 0.2.11, spec-author 0.1.10; matrix M-097/M-098; suite 84 green; pack 0.8.15 | Source: skill-creator eval 2026-07-05 |
| 68 | Communicator description over-triggers (fires on any progress report, no OFF-switch) — narrow it | small | **landed 2026-07-05 ~22:53, session 9** (door: bug; kind: skill; fences held: movement-end report stays a named trigger (prover J1) · rule texts untouched, only the description; stood down by name: facet sweep + design-sync — skill kind; success measure [default]: the next mid-work status line ships without loading the skill, the next landing report still loads it) — description narrowed with a stated NOT-side, over-trigger phrase asserted GONE; communicator 0.1.10 synced; matrix M-099; eval re-run duty recorded (J2); suite 85 green; pack 0.8.16 | Source: skill-creator eval |
| 69 | product-prover SKILL.md: extract the category table + glossary into references/ per skill-creator sizing guidance | small | deferred | Source: skill-creator eval; deferred |
| 84 | Milestone 0.8.0 (renamed from 1.0 on Alexander's word, 2026-07-05 late evening: "может это 0.8 или 0.9, не надо спешить" — 1.0 waits for polish: rows 86, 93, 94 + tlvphoto lessons): full 3-pass preventive audit, terminal rows archived, README refreshed, VERSION 0.8.0, gated push if maximally clean | large | **landed 2026-07-05 night, session 7** | 3-pass audit: FULL prover READY-TO-PUSH 0 must-fix (84 anchors symmetric, all 24 night folds verified) · matrix audit 1 must-fix + 8 (ALL folded, placement check mechanized) · architecture/composition 0 must-fix + 6 (ALL folded, design-sync node re-proven, 16 pins re-pinned); VERSION 0.8.0; suite 61 green; pushed on the clean verdict |
| 86 | The pack understands WHAT it is building (Alexander 2026-07-05 night, decision-page note: "скилл сет должен понимать с чем работает: делаем инфру или продукт, скилл или рассказ пишем — задействовать необходимые функционалы, не все всегда нужны"): a work-kind axis (product / infra / skill / prose…) that scales which pipeline parts apply | surface | **landed 2026-07-05 ~20:55, session 8** (door: feature; kind: skill; fences held: T-12/INV-16 · T-15 · INV-12 · T-13/INV-18 · T-14/INV-19 · INV-20/INV-21 — full suite green) | Done when: kind named at intake; each pipeline step says what it means per kind (or that it stands down), never silently skipped — MET: SPEC v0.15.0 T-16+INV-22, per-kind table in build-pipeline 0.2.7 (its one home), base rule 15 (0.1.8), communicator report line (0.1.8), matrix M-084/M-085, suite 64 green, prover CROSS-LINK record row86 (3 findings folded). Non-goals: no mechanical intake-kind guardrail yet; no per-kind templates; design-sync wiring = row 93. Success measure: every new row names a kind + first non-product landing shows a named stand-down, by eye until the reading machinery [default] |
| 93 | Design-sync wiring (E-18 [target] machine): base default + host-profile switch vocabulary + communicator/verify channel lines + first real run on a visual host; lands with/after row 86 (work-kind axis) | surface | **pack-side half LANDED 2026-07-05 ~22:20, session 8; row WAITING** (door: feature; kind: skill; fences held: E-13 · INV-14 · base rule 17) — wired: E-18 honest split, base 0.1.9 switch off-by-default, communicator/pipeline channel lines, M-088 + test, suite 67 green. Remainder: the machine's FIRST REAL SYNC on a visual host (track-coach/tlvphoto window) through Alexander's gate | Done when: E-18 loses its [target]; a real host synced with the human's gate shown working |
| 94 | Skill-behavior evals (research adopt #6's third leg, superpowers "no skill without a failing test"): per skill, a scenario where a bare session errs and the skill corrects it; run at milestones | surface | **landed 2026-07-05 ~22:00, session 8** (door: feature; kind: infra; fences held: E-12/INV-13 · M-1 grows · T-16/INV-22 — suite green) | Done when: one eval per working skill, red proven without the skill — MET: SPEC E-19, evals/ (4 files + README), 8 real runs recorded (docs/evals/2026-07-05-first-run/), reds scored per criterion, suite 66 green (self-closing check), M-1 carries the re-run, skill-evals node re-proven. Honest boundary recorded: no truly bare session on this machine (the loader feeds method) — red = bare-of-the-SKILL. Non-goals: no clean-machine CI arm yet (candidate rides row 14); scenario de-contamination at next re-run. Success measure: next milestone's eval re-run flips ≥0 criteria silently (any flip = a caught regression), checked at M-1 [default] |
| 95 | Marathon phase — play, watch, learn (Alexander 2026-07-05 evening, back from the sunset: "это марафон а не спринт; поиграться с проектами, тебя потестировать, смотреть как я веду проекты"): NO version race — 1.0 has no date; the pack proves itself on real project play; Fable OBSERVES how Alexander leads the different windows (read-only: their journals/transcripts) and folds what it learns as inbox wishes/rows | large | queued 2026-07-05 evening (mis-dated 07-06 at intake; corrected against git clock, session 8), frames everything after 0.8.0 | Done when: it never is — this row is the phase's charter; concrete lessons land as their own rows |
| 96 | Feedback loops, raised (his word tonight "мы говорили что можно как-то фидбеки делать" + INV-21's success measures now wait on a reading machine): the feedback → measurement → A/B family (deferred rows 47-49) moves from the deferred tail to next-up after the tlvphoto lessons | surface | queued 2026-07-05 evening (mis-dated 07-06 at intake; corrected, session 8; priority raised on his word) | Done when: rows 47-49 re-scoped against INV-21 and the first feedback loop runs on a real project |
| 97 | Docs he reads get typography, not a code editor (Alexander 2026-07-05: Sublime for prose = "как когда-то на VT220"): `scripts/render-doc.py` renders any .md into one readable self-contained page; the show-a-doc rule now goes render→browser, Sublime retired for prose | small | **landed 2026-07-05 evening, session 7 (door: feature; date corrected from 07-06, session 8)** | Script shipped + inventoried + smoke-tested; profile line show.text-docs updated (playbook); OVERVIEW.md was its first render |
| 99 | De-ceremonialize the appetite wording (Alexander 2026-07-05 ~22:40: "я так и не понял зачем нужен аппетит… этот прикол bmad который закомпьютеризовал ажайл процессы это перебор, нет?"): keep the LAW (a "по-быстрому" wish cuts richness — surfaces in, how considered the defaults — never the safety net: fences, facets, non-goals, success measure), drop the CEREMONY — rewrite T-15 in plain words without the imported term, never surface the word in intake lines unless he says it; his call on wording | small | **in-work 2026-07-05 ~23:15, session 8 — his word arrived ~23:10 and WIDENED the row** ("сделай по-быстрому это неправильно… какой дебил спрашивает программера сколько это займет… можно играться со скоупом а не с таймлайнами"): not just the term — the TIME-BUDGET mechanism dies; scope is the only negotiable dimension (cut surfaces / split into stages), time estimates are never an input (door: feature; kind: prose; fences held: INV-4 · INV-5/INV-18 · T-11 · protection list byte-intact) — **landed 2026-07-05 ~23:25, session 8**: T-15 scope-never-time (cut or stage, estimates never an input), term swept from SPEC/build-pipeline 0.2.9/spec-author 0.1.9/README/M-076, suite ASSERTS the term absent (muscle-memory fence), history untouched, pack 0.8.5, suite 68 green, prover record row99 | Done when: T-15 re-authored to scope-never-time; the word and the time-budget rider gone from every live surface (spec, skills, README, matrix, tests re-authored); safety-net list intact; suite green — MET |
| 100 | **The problem ledger** (Alexander 2026-07-05 ~23:00, watching tlvphoto transcripts: "если есть проблема особенно повторяющаяся… надо ее решать блин. решать!! …либо решать проблему, либо договориться что это не проблема. такое не должно вообще происходить"): recurring OPERATIONAL noise — flaky test harness, missing deps, environment errors, anything that "вылетает" mid-work — gets a per-host DYNAMIC LIST; an entry seen a SECOND time gets an owner that moment: a queue row (solve it) or an explicit dated "agreed non-problem" with the human's word; a third silent recurrence of a listed signature is a defect of the METHOD, not of the day. Distinct from the recurring-BUG rule (that covers the product; this covers the workshop). Evidence at intake: tlvphoto's "element not clickable: #ex-skip" ×5, "readyState=complete" timeout in two different sessions, missing PIL — all retried, none owned | surface | **pack-side LANDED 2026-07-05 23:39 (git), session 10; row WAITING on the first foreign-host run** (door: feature; kind: skill+infra — two legs, each step's form named: spec full delta · prover CROSS-LINK record row100, 5 findings ALL folded (owned-entry recurrence, SOLVED's actor, archive home, signature merge, worker-brief seam) · architecture assignments only (INV-23 → base-rulebook, E-24 → templates, dogfood pin → package-docs), no re-prove per the doc's own rule · facet sweep: headless, the one explicit N/A sentence in the spec · design-sync: stands down, no visual product; fences held: T-9 bug lane · recurring-BUG re-door stays the product's law · INV-9 · INV-4/E-22 · E-8 · ACT-3 · INV-11 · M-1; delegation savings: the whole implementation bundle — tests, template, ledger, rule-19 insert, 5-skill pin sweep, VERSION, row-103 insert, red-first runs — went to a Sonnet worker, ~15 min senior time saved; the worker correctly STOPPED on two brief defects (M-105's owning node, a wrapped-line assertion), senior folded both) — SPEC v0.15.10 E-24+INV-23 · base rule 19 (0.1.12, pins swept across the five working skills, installed copies synced) · templates/PROBLEMS.template.md · the pack's own ledger OPENED with two live entries: invented-time OWNED by new row 103 (the ledger's first real catch mechanized) + zsh `===` AGREED NON-PROBLEM recommended, awaiting his word · matrix M-103/M-104/M-105 · suite 93 green · pack 0.8.21 | Done when: the ledger's home + shape spec'd and proven — MET; the second-occurrence law in the base skill — MET; the pack's own ledger opened with tonight's live entries — MET; first real host run on tlvphoto/track-coach — REMAINS, rides those windows (this window is fenced to one inbox file per host) |
| 101 | **Done-claims are verified, versioned answers** (Alexander 2026-07-05 ~23:15, after asking track-coach "ты сделал тесты по методологии лайвспека?" — the answer was right but he couldn't tell verified from narrative; his words: "верификация того что произошло это тоже важная фича" + "если сделал — то по какой версии сделал"): when the human asks "is X done / adopted / true?", the answer is an EVIDENCE WALK, never a story — each claim pinned to a checkable artifact (adoption record, prover record, suite run, git commit, matrix row), verified-vs-asserted said apart, AND the answer names the METHOD VERSION it was done by (pack + skill versions from the host's `.live-spec/` installed set, M-7's homes) so "done by live-spec" always means "done by live-spec vX". Kin: reverse-verify spec-vs-shipped (his standing rule), M-7 version homes, A-7 freshness | surface | **landed 2026-07-06 ~00:11, session 11** (door: feature; kind: skill; prover CROSS-LINK record `docs/prover/2026-07-06-night.md` pass 1 — F1 must-fix FOLDED (a host with NO installed set: the answer says so plainly, an absent version is never invented — folded into the clause AND rule 11), F2 folded (rule 11 cites base rule 13, elaborates only the exchange shape), F3 recorded (landing-report MET lines are kin, out of scope by intake); architecture: assignment + pin only (INV-25 → communicator, rule 11 pinned `:122`); stood down by name: facet sweep — skill kind, no visible surface; design-sync — text, stands down; non-goals: nothing left out of his two quotes; success measure [default]: the next done-claim answer he reads carries artifacts + version and he can tell verified from asserted; delegation savings: the whole mechanical bundle — red-first test, matrix row, five SKILL.md edits, VERSION, sync, pin fix — went to a Sonnet worker, ~20 min senior time saved, one honest deviation flagged (a line-wrap point) and accepted) — SPEC v0.15.12 INV-25 · matrix M-107 · `test_done_claim_evidence_walk` RED PROVEN then green · communicator 0.1.11 · pack 0.8.23 · **first real run DONE**: his track-coach question re-answered by the walk — verified: commit `193d39d` exists · 797 test functions in 26 files (795 green + 2 skip = 797, matches the claim) · SPEC + TEST_MATRIX present (docs/) · method version FROM THE ATTACH RECORD `.live-spec/versions.md` 2026-07-05: pack 0.5.3, prover 0.1.5, pipeline 0.2.5 — "done by live-spec 0.5.3", not tonight's 0.8.x; asserted, not re-checked: that all 795 are green at this hour (suite not re-run) | Done when: the rule spec'd (communicator's domain) — MET; answer template claim → artifact → version — MET (rule 11); first real run = re-answering his track-coach question with pinned evidence — MET (see status); suite covers the clause — MET by deed |
| 102 | **A multi-story row can't close by half** (from the tlvphoto investigation, 2026-07-05 ~23:17, session 10): tlvphoto's MVP fused TWO user stories (door + gallery) into one queue row 5a; shipping the door half let the row be declared COMPLETE while the gallery was still the rejected grid wall — four drops in a row, each caught by his EYES, never the pipeline. The law: one wish = one user story (his standing word, not yet a pack sentence); where a row DOES carry several stories, its Done-when enumerates per-story acceptance and the row cannot close with an unmet leg; the resume file's LIVE-STATE supersession must not compress an unfinished story out of existence. Evidence: tlvphoto NEXT_STEPS.md:19 "threshold story COMPLETE" vs the wall still shipped; adopt-era SPEC filed the approved Room as "not yet specified, later" | surface | **landed 2026-07-06 ~01:29, session 11** (door: feature; kind: skill; prover CROSS-LINK record `docs/prover/2026-07-06-night.md` pass 2 — F4 folded (a split's rows cite the one spoken wish, INV-1), F5 verification note (the pack's own multi-leg rows 12/100 already legal); the tlvphoto case re-walked in the record as the red test: T-17 outlaws the fused row at intake, INV-26 refuses the half-close, the M-2 arm bans the compressed-away leg — all three catch the real incident; architecture: assignment only (T-17/INV-26 → build-pipeline); stood down by name: facet sweep + design-sync — skill kind, no visible surface; non-goals: nothing left out; success measure [default]: the next fused wish is split at intake and no row again reads COMPLETE beside an unmet leg; delegation savings: red-first test, M-108/M-109, three SKILL.md edits, two template lines, VERSION, sync — a Sonnet worker, ~15 min senior saved; the worker STOPPED correctly on one brief defect (my anchor quoted the wrong file) — corrected, resumed same tier, logged) — SPEC v0.15.13 T-17+INV-26 · matrix M-108/M-109 · `test_one_story_close_whole` RED PROVEN then green · build-pipeline 0.2.15 · pack 0.8.24 · suite 96 green | Done when: one-story law + close-only-whole stated in the pipeline (one home) — MET; resume + roadmap templates carry the per-leg check — MET; the tlvphoto case re-walked against the new sentences as the red test — MET (prover pass 2) |
| 103 | **Invented-time gets a mechanical owner** (born in the pack's own problem ledger, 2026-07-05 ~23:40 — third recurrence of the family: rows 95–97 mis-dated at intake, re-swept session 8, and tonight the prototype-norm inbox file + tlvphoto's prover record both dated 07-06 while the clock said 07-05; the hand-sweep ran twice and did not hold): a suite + pre-push check that no repo file NAME and no journal/queue/ledger date stamp is LATER than the clock at commit time | small | **landed 2026-07-05 ~23:43, session 10** (door: feature; kind: infra; quick win taken right after row 100 — the ledger's first entry closed the same night it was owned; fences held: M-3 dated versions · M-2 journal discipline · INV-23 (the ledger cites the fence, the fence doesn't restate the ledger); stood down by name: facet sweep + design-sync — infra kind, no visible surface; non-goals: no rewrite of history lines QUOTING the wrong dates — prose quoting a past incident stays legal, the fence scopes to file names + journal headings + ledger dates; success measure [default]: the next invented stamp never reaches a push) — SPEC v0.15.11 INV-24 (Rhythm) · matrix M-106 (guardrails node) · `test_no_future_dated_stamps`, RED PROVEN by deed on a synthetic future-named file, then suite 94 green; pack 0.8.22 | Done when: the check runs in the suite and the pre-push walk (suite runs IN the push walk, gate b) — MET; a future-dated stamp goes red — MET by deed; the pack ledger entry flips to SOLVED — MET |
| 104 | **Invented-time, the TIME variant** (ledger-born 2026-07-05 23:43, session 10 — the fence row 103 had just landed and the session's own landing stamps were STILL written ahead of the wall clock ("~23:50"/"~23:58"/"00:02" at 23:35–23:43), same failure as session 8's journaled one (JOURNAL:853): the date fence can't see same-day TIMES): a pre-commit check — an ADDED line stamping TODAY with a time later than the commit clock goes red; not racy, the commit moment is the reference | small | **landed 2026-07-06 ~01:37, session 11 — QUICK-WIN BUBBLE, jump marked here (T-11): taken ahead of the queue after the hand guessed time ahead TWICE MORE the same night (occurrences 3 and 4 on the ledger entry)** (door: feature; kind: infra; prover CROSS-LINK record `docs/prover/2026-07-06-night.md` pass 3 — F6 folded (time check wired ABOVE the opt-in fence's early exit: the fence is opt-in, the clock is law), F7 recorded (named residual holes: --no-verify, test-only env overrides — habit fence, not adversary fence), F8 verified by deed (the future-plan writing cost; tonight's resume file complies); stood down by name: facet sweep + design-sync — infra, no visible surface; non-goals: no sweep of PAST wrong times in history (prose quoting stays legal, same as row 103); success measure [default]: no same-day future stamp ever reaches a commit again; delegation savings: script, hook wiring, 3 red-first tests, M-110, VERSION, install — a Sonnet worker, ~15 min senior saved; TWO brief defects both caught by the worker/suite and corrected (wrong anchor file quoted · matrix level vocabulary) — the contract and the traceability teeth each did their job) — SPEC v0.15.14 INV-24 second arm · matrix M-110 · `TestGateTimeFence` 3 tests RED PROVEN (exit 127 bare) then green · guardrails/check-future-times.sh + pre-commit wiring · pack 0.8.25 · suite 99 green · **proven by deed in the real repo: a staged "23:59" stamp BLOCKED at 01:36 — and the fence then blocked its own landing commit (F9): real catches (a heading one minute ahead; five stale time references) fixed, the over-broad line-global reading narrowed to the adjacent stamp shape the same hour, 5 fixture tests green** | Done when: the pre-commit check runs and a same-day future-timed added stamp goes red by deed — MET (blocked live at 01:36); the ledger entry flips to SOLVED — MET (this landing) |
| 105 | **A captured wish is echoed aloud, and every feature reports its station on the pipeline** (Alexander 2026-07-05 ~23:50, before sleep: "надо просто необходимо показывать роадмап и статус… более регламентированно. например 'captured this that request, it's a feature, we'll call it this and that' а потом рапортовать как каждая фича идет по пайплайну"): two halves of one ritual — (a) the CAPTURE ECHO: the moment a wish is intaken the human hears one plain sentence — what was heard, the door called, the name it will answer to, its row number — so he KNOWS the request survived; (b) the PIPELINE BOARD: every status report names each in-flight feature by that name with its current station (spec → prove → architecture → matrix → tests → code → verify → landed), so progress reads like a departures board, not prose archaeology. Kin: his status-report-format standing word (reports anchored to roadmap/stage, 2026-07-04) — this row REGIMENTS it | surface | **landed 2026-07-06 ~01:45, session 11 — per-leg status (INV-26): pack-side legs MET, first-real-run leg = this morning's report, closes in the same session's final commit** (door: feature; kind: skill; prover CROSS-LINK record `docs/prover/2026-07-06-night.md` pass 4 — F10 must-fix folded (silent arrivals echo in the NEXT report, never an interruption), F11 folded (station vocabulary = the pipeline's own step names), F12 folded (the echo IS the T-12 intake line spoken, not a second classification; pipeline cites rule 12); architecture: assignment + pin (INV-27 → communicator rule 12 `:135` + rule 9 station line); stood down by name: facet sweep + design-sync — skill kind, no visible surface; non-goals: no per-feature progress FILE, the board lives in reports (the queue table stays the one written home); success measure [default]: he can say where any feature stands without reading a table; delegation savings: red-first test, M-111/M-112, six SKILL.md edits across two skills, VERSION, sync — a Sonnet worker, ~15 min senior saved, zero brief defects this round) — SPEC v0.15.15 INV-27 · matrix M-111/M-112 · `test_capture_echo_and_board` RED PROVEN then green · communicator 0.1.12 rule 12 + rule 9 stations · build-pipeline 0.2.16 step-zero echo citation · pack 0.8.26 · suite 102 green | Done when: capture-echo sentence + pipeline-board format spec'd in one home — MET; the report announcing this landing uses the board itself (first real run) — MET this morning (the report page + the chat report are the run); suite covers the clause on the shipped SKILL.md — MET by deed |
| 106 | **pytest from the repo root trips over the scaffold template** (found 2026-07-06 ~00:00 by a clean-context analyst running our own suite: `python3 -m pytest` collects `templates/test_scaffold.template.py` and errors; only `python3 -m unittest discover tests` is green — a packaging rough edge a stranger hits in their first minute) | bug | queued 2026-07-06 ~00:06, session 11 (door: bug; kind: infra; ask-at-intake: none) | Done when: a stranger's `pytest` from the root either runs the suite green or is cleanly told where the suite lives (collect-ignore / config), red-proven first; the template itself stays a template |
| 107 | **Learn from the neighbours at the implementation level** (Alexander 2026-07-06 ~00:05: "посмотреть чему поучиться у других похожих проектов… на уровне 'как это они реализовали' — посгружать и посмотреть"): clone/download the comparable projects (GitHub Spec Kit — the constitution gate + cross-artifact consistency checks; OpenSpec — change-folder audit trail; GSD — clean-context sub-agent split; BMAD — story files, already partly absorbed) and read HOW they built what our research doc admired from the outside; harvest concrete mechanisms as candidate wishes, each through the normal queue. Research first, adoption never implied | surface | **landed 2026-07-06 ~01:57, session 11** (door: skip — research, no pipeline steps owed; three Sonnet workers cloned and read Spec Kit / OpenSpec / GSD / BMAD at source level; headline: Spec Kit's consistency checks are prompt text — zero mechanical enforcement in its own repo, only file-existence shell tests) | Done when: the named repos read at code level — MET (workers' reports with file-path quotes); harvest doc rendered for him — MET (docs/research/2026-07-06-neighbours-implementation-harvest.md + .html, linked from the morning report); each worth-stealing mechanism filed as its OWN wish row — MET (rows 110–115) |
| 108 | **A feature is interrogated for PRODUCT FIT at intake — a minimal prover on the feature itself** (Alexander 2026-07-06 ~00:25, at night, with the tlvphoto evidence pasted in: "кучу вопросов когда фичер принимается в систему не задается… не надо как в бмад но надо минимальный прувер на фичу… спросить всякие аспекты, как она вписывается в контекст продукта (если юикс то навигация, если бэкенд то флоус)… ты такое даже не додумываешь сам… может все же лайвспек не очень процесс структурирует, потому что кучу вопросов про фичер адопшен не задано"): today's facet sweep asks DEVICE facets (phone/touch/empty/a11y/perf, T-13) but nobody asks the PRODUCT-FIT questions — how the feature sits in the user's JOURNEY (his door example: enter → browse → re-enter → stuck at the 10th picture — a navigation loop nobody specced), what neighbouring behaviours it IMPLIES (no-repeat across doors ⇒ state tracking), what the FEEL owes (premium, animation smoothness), what the next feature it invites is (the greeting). The wish: a kind-scaled FIT interrogation at the spec step — product/UX kind asks journey · navigation · continuity-state · feel · invited-next; infra/backend kind asks flows · data lifecycle · failure paths — each answer becoming a spec sentence or a batched question, same law as the facet sweep (INV-18). His five tlvphoto comments forwarded to tlvphoto/inbox same hour (their window owns the fixes; ours owns the METHOD hole) | surface | **landed 2026-07-06 ~10:55, session 12 — repo side whole; ONE OPEN LEG: installed-copy sync rides Alexander's `! sh ~/live-spec/install.sh` (the harness classifier blocks an agent's own hand in ~/.claude/skills, both worker and senior tried)** (kind: skill; door: feature; his morning words folded: the walk is a small prover on the wish — «прувер может валидировать что угодно», trivially-closable holes closed and WRITTEN how, «не мучать пользователя вопросами»; prover CROSS-LINK record `docs/prover/2026-07-06-rows108-119.md` F1/F2/F7/F8 folded; architecture: INV-29 → spec-author node; facet sweep + design-sync stood down by name — skill kind; non-goals: no retro-walk en masse (binds forward), no mechanical fit-gate; success measure [default]: the next product-kind wish in an adopted host ships with journey sentences and he never asks «а отсюда куда?»; eval criteria + re-runs ride the push gate (E-19); delegated to a Sonnet worker with rows 117–119) | Done when: the fit-lens list spec'd kind-by-kind in one home (spec-author's sweep section widened, prover gains the FEATURE-FIT focused mode the way CROSS-LINK exists); every fit answer lands as spec sentence / [default] / batched question, never silently; first real run on the next product-kind wish in any adopted host; suite covers the shipped clause |
| 109 | **Visual clauses carry their prototype pointer; the build step opens it** (harvested 2026-07-06 ~01:20 from inbox/2026-07-05-prototype-norm-lens.md — the tlvphoto incident where an approved prototype's clause was rebuilt from TEXT alone and shipped a cheap look-alike, 75 tests green proving the misreading; same incident family as row 102, DIFFERENT law, so its own row by 102's own one-story rule): four pack sentences — spec-author: a clause born of an approved visual prototype CITES the artifact ("norm: <file>"); build-pipeline code step: a norm-pointered surface OPENS the artifact before building, one-line plan-vs-prototype diff recorded; build-pipeline door step: a declared mockup-first entry condition is cancellable only BY NAME, never by a general "go build"; prover: a lens — every visual clause backed by an artifact, text never contradicting its own artifact | surface | queued 2026-07-06 ~01:20, session 11 (kind: skill; door: feature; ask-at-intake: none — the inbox file is precise; kin: Alexander's standing prototype-is-the-norm word) | Done when: the four sentences live in their four homes (one home each); norm-pointer format spec'd; suite covers the shipped clauses; inbox file archived with a pointer to this row |
| 110 | **The verify step turns adversarial: a fresh-context checker re-derives from the spec** (row-107 harvest, 2026-07-06 ~01:25; GSD's verifier — opening hypothesis "tasks completed, goal missed", claims checked exists → substantive → wired → data-flows, literal stub-detection greps; their reason is our law: workers report PASSED even when wrong): our verify-by-deed is the senior's own eyes on work the senior briefed — add the option (mandatory for large landings) of a FRESH-context verifier that re-derives what must be true from the SPEC sentences, never from the worker's summary, and greps for stub patterns | surface | queued 2026-07-06 ~01:25, session 11 (kind: skill; door: feature; source: docs/research/2026-07-06-neighbours-implementation-harvest.md §1) | Done when: the adversarial-verify option spec'd (when it fires: which sizes/kinds), the stance + level ladder + stub-grep list in the pipeline's step 8 or a verify skill; first real run on a real landing; suite covers the clause |
| 111 | **The brief is born from read code** (row-107 harvest ~01:25; BMAD's create-story critical step): before authoring a worker brief that edits existing files, the senior (or the brief-writer) READS IN FULL every file the work will modify and the brief records current-state / what-changes / what-must-survive; every step carries a back-reference to the spec sentence it serves (we half-do this), every technical claim a source citation | small | queued 2026-07-06 ~01:25, session 11 (kind: skill; door: feature; source: harvest §2) | Done when: the read-first rule + the three recorded lines join the pipeline's delegation gate (one home); suite covers; first real brief written under it |
| 112 | **Workers get a named HALT list** (row-107 harvest ~01:25; BMAD's dev contract): a worker stops early ONLY on an enumerated condition — ambiguous requirement · N consecutive failures · missing config/dependency · acceptance impossible as briefed — otherwise it runs to completion; sharper than "ask if unsure", composes with our one-tier escalation law | small | queued 2026-07-06 ~01:25, session 11 (kind: skill; door: feature; source: harvest §3) | Done when: the HALT list in the worker-contract clause (SPEC ACT-3 + pipeline delegation gate, one home); suite covers |
| 113 | **Brief sizing gets numbers** (row-107 harvest ~01:25; GSD's degradation curve): the delegation gate learns quantities — a brief targets a bounded share of the worker's context, splits above it; pass PATHS, never inlined file bodies | small | queued 2026-07-06 ~01:25, session 11 (kind: skill; door: feature; source: harvest §4) | Done when: the sizing sentence + paths-not-bodies rule in the delegation gate; suite covers |
| 114 | **Gate scripts learn the neighbours' hygiene** (row-107 harvest ~01:25; OpenSpec's archive/CLI): all-or-nothing batched writes (validate all rebuilt outputs before writing any), a typed failure contract per blocked gate ({severity, code, message, fix} — one JSON for agents, the same fix line for humans), an explicit advisory-vs-blocking taxonomy per check | small | queued 2026-07-06 ~01:25, session 11 (kind: infra; door: feature; source: harvest §5) | Done when: the three conventions stated in guardrails' README/contract and applied to at least one existing gate script by deed; suite covers the shape |
| 115 | **The resume file is a digest with a size cap** (row-107 harvest ~01:25; GSD's STATE.md <100 lines): NEXT_STEPS LIVE-STATE bounded — growth is a design failure; detail flows to the journal it points at; a suite check owns the cap | small | queued 2026-07-06 ~01:25, session 11 (kind: infra; door: feature; source: harvest §6; tension to resolve at spec step: INV-26's restate-every-open-leg vs the cap — enumerate tersely, never drop) | Done when: the cap chosen and spec'd, the check red-proven on a synthetic bloated file, our own NEXT_STEPS compacted under it |
| 116 | **The outcome does the talking — a coined name never leads a report line** (Alexander 2026-07-06 ~10:05, morning after the first real departures board: «"Прогулка по уликам" (строка 101) это что такое? … Часы получают зубы. ЧТО??? … я даже эти файлы не открываю»): the board's first real run passed its eval and FAILED its reader — lines LED with coined metaphor-names, row numbers he never opens, and riddle-compression («семь раз — дважды забор»); third strike of the jargon family in two days (calques twice 2026-07-05, profile line already standing) ⇒ recurring bug re-doors to FEATURE: the area was missing an invariant. Two arms: echo-names are short DESCRIPTIVE phrases parseable cold; a human-facing report line opens with the reader's outcome, every handle (codes, numbers, coined names) only trails, one fact = one standalone sentence | small | **landed 2026-07-06 ~10:33, session 12** (door: feature — recurring-bug escalation; kind: skill; prover CROSS-LINK record `docs/prover/2026-07-06-row116.md` — F1 folded (naming arm: INV-27's board and INV-28 compose — names lead only because names become plain), F2 folded (scope fence: human-facing surfaces only, method-internal docs keep anchors), F3 recorded (obedience is judged by eval + reader, the string test only pins the shipped clause); architecture: INV-28 assigned to the communicator node — and the assignment was NOT free: the senior briefed "no architecture change", the ownership test went red, the worker HALTed by contract and the node's owned-anchor cell + rule-9 pin were added on the senior's word (the teeth bit their author, correctly); facet sweep + design-sync stood down by name — skill kind, no visible surface; non-goals: past reports not rewritten (the record stands, the translation lives in chat), method-internal docs unchanged; success measure [default]: the next morning-style report draws zero "а это что?"; delegation savings: test, matrix row, ten file edits, base-version sweep, VERSION, suite runs, install sync — a Sonnet worker, ~20 min senior saved; two contract HALTs, both correct, one catching the senior's own skipped step) | Done when: INV-28 spec'd with both arms; communicator rules 6/9 + base rule 2 name coined names as handles; eval criterion added; suite covers the shipped clauses red-first; the session's closing report itself written under the law |
| 117 | **Verify-by-deed learns to walk the visit and watch the feel** (tlvphoto inbox 2026-07-06 ~10:10, §1 + Alexander's morning word «давай учитывать»): the product kind's step 8 gains a named VISITOR WALK — the whole journey as the person lives it (first visit · return · cross-entry · from-any-point navigation · exits) — and a FEEL pass: motion quality (easing, duration, choreography) and affordance craft judged against the approved prototype's bar; findings become rows, never vibes. Evidence: tlvphoto's transitions and its open-work button shipped green through the full pipeline and read cheap on sight. Kin: row 108 owns the intake half (journey questions at spec time); this row owns the verify half | surface | **landed 2026-07-06 ~10:55, session 12 — repo side whole; ONE OPEN LEG: installed-copy sync rides Alexander's `! sh ~/live-spec/install.sh` (the harness classifier blocks an agent's own hand in ~/.claude/skills, both worker and senior tried)** (kind: skill; door: feature; source: inbox note §1 + his «давай учитывать»; prover record `docs/prover/2026-07-06-rows108-119.md` F3 folded (SPEC binds THAT, the step-8 cell holds the checklist); architecture: INV-30 → build-pipeline node; stood down by name: facet sweep + design-sync — skill kind; non-goals: tlvphoto's own five fixes stay in their window; success measure [default]: nothing green-but-cheap reaches his eye before the walk names it; eval re-runs ride the push gate) | Done when: visitor-walk + feel pass spec'd into step 8's product-kind form (one home); prover/eval cover the clause; first real run on a product-kind landing in an adopted host; inbox file archived with a pointer at landing |
| 118 | **A taste default may not outlive two landings unreviewed** (tlvphoto inbox 2026-07-06 ~10:10, §2): the [default]-tag is right for velocity — the lane never blocks — but nothing closes the loop: tlvphoto carries 8+ open defaults across two landings and the product reads unfinished everywhere. The law: a landing report LISTS its [default]s; the next human review resolves them (one decision page, communicator rule 10); a default surviving two landings without the human's word is flagged loudly by the gate/report | small | **landed 2026-07-06 ~10:55, session 12 — repo side whole; ONE OPEN LEG: installed-copy sync rides Alexander's `! sh ~/live-spec/install.sh` (the harness classifier blocks an agent's own hand in ~/.claude/skills, both worker and senior tried)** (kind: skill; door: feature; source: inbox note §2; the spec step answered its own tension: the lane still never blocks — the LOOP closes via the landing report's restated open-defaults list (no counter file, F4) and the two-landing expiry onto the decision page; prover record F4 folded, F5 recorded honestly (age-checker guardrail = candidate wish, not implied); architecture: INV-31 → communicator node; stood down by name: facet sweep + design-sync — skill kind; success measure [default]: no default older than two landings anywhere; eval re-runs ride the push gate) | Done when: the expiry law spec'd (count home + flag mechanics chosen); communicator's decision-page rule cites it; suite covers; tlvphoto's open defaults become the first real decision page after their window adopts the clause |
| 119 | **A decision card asks in consequences, not mechanisms** (Alexander 2026-07-06 ~10:40: «то что ты выше написал понятно, а то что ты мне в хтмл выдал непонятно!!!» — the shell-separator card explained the mechanism and he could not tell what he was deciding, twice): every card opens with what the choice CHANGES for the person under each option, in the product's words; options labelled by consequence; mechanism only if it helps | small | **landed 2026-07-06 ~10:55, session 12 — repo side whole; ONE OPEN LEG: installed-copy sync rides Alexander's `! sh ~/live-spec/install.sh` (the harness classifier blocks an agent's own hand in ~/.claude/skills, both worker and senior tried)** (kind: skill; door: feature — same family as row 116, its decision-page face; prover record `docs/prover/2026-07-06-rows108-119.md` F6 checked (composes with ask-by-showing); architecture: INV-32 → communicator node; stood down by name: facet sweep + design-sync — skill kind; success measure [default]: he answers every card without asking what it means; eval re-runs ride the push gate) | Done when: INV-32 spec'd; communicator rule 10 opens cards with consequences; suite covers; the next decision page written under it |
| 120 | **A taste choice made without asking is told, never confirmed** (Alexander 2026-07-06 ~11:20: «если сделал какие-то штуки по умолчанию — расскажи их, обозначь; если мне всё ок — не надо подтверждать; можно сказать вот разные штуки, можно твикать, и всё; дальше уже, если надо, пользователь спросит»): supersedes the two-landing forced-review mechanism row 118 landed the same morning — the TELLING half stands (silent accumulation stays illegal), the confirmation half dies; the landing report names each choice with an example and a tweakable mark, silence is consent, nothing re-asked | small | **landed 2026-07-06 ~11:30, session 12** (door: feature — same-day correction on the human's word, journal owns the story; kind: skill; architecture: INV-31 ownership moves communicator → build-pipeline node — the landing report owns the telling; success measure [default]: he never sees a confirmation request about a taste call and never finds an untold one) | Done when: INV-31 rewritten both arms; pipeline step 9 tells; matrix/test updated; suite green |
| 121 | **The feel lens takes the medium's own form** (Alexander 2026-07-06 ~11:20: «"чувствуется" применимо скорее к фронтенду, а проект может быть хоть книгу написать — это частичный навык, по контексту»): the visitor walk stays for everything a person visits, but WHAT it watches scales — motion and affordance craft for a browser product, reading path and chapter flow for a book, command round-trip for a CLI; never a frontend checklist forced on prose | small | **landed 2026-07-06 ~11:30, session 12** (door: feature — same-day correction on his word; kind: skill; one scaling sentence in INV-30 + index + M-115) | Done when: INV-30 carries the scaling sentence; suite green |
| 122 | **install.sh backups polluted the live skills folder** (found by deed 2026-07-06 ~11:25: Alexander ran the installer and six stale skill copies appeared to the harness as loadable duplicate skills — backups went to `<skill>.bak_<ts>` INSIDE ~/.claude/skills, which the harness scans as skills): backups move to an attic folder BESIDE the skills dir; today's six strays moved out by hand the same hour | bug | **landed 2026-07-06 ~11:30, session 12** (door: bug; kind: infra; red-proven: `test_install_backup_home` on the shipped script) | Done when: install.sh writes backups outside the scanned dir; test red→green; the strays gone — done by hand |
| 123 | **A worker's brief carries the problem-ledger walk** (retro fit-walk find, 2026-07-06 session 12, record `docs/prover/2026-07-06-feature-fit-retro.md`): workshop noise a WORKER hits (flaky harness, missing dependency, tool misbehaving) today gets silently retried unless the senior happens to see it in raw output — the delegation gate should put the ledger path and the WATCHED-line duty into every brief | small | **landed 2026-07-06 ~12:53, session 13** (door: feature; kind: skill; source: the retro run; the CLOCK arm folded into the same contract clause — the eval re-runs' wrong-hour wobble was this row's kin and shares the fix (a brief states the date/time read at briefing, worker stamps come off it, INV-24); homes: SPEC ACT-3 + delegation gate + M-119; dogfooded on its own landing — the row-123 brief carried both lines, the worker wrote a ledger line and HALTed by contract on the push gate's expected mid-batch red; stood down by name: facet sweep + design-sync — skill kind, no visible surface; non-goals: no mechanical brief-linter, past briefs not retro-edited; success measure [default]: the next delegated worker's stamps carry the right hour and its noise reaches the ledger without the senior digging; delegation: a Sonnet worker ran the seven verbatim edits + red/green proof, ~15 min senior saved) | Done when: the delegation gate's brief checklist includes the ledger walk — MET (ACT-3 + gate elaboration, one home each); suite covers the shipped clause — MET (test_brief_carries_ledger_and_clock red first, then green; suite 109, the one red being the push gate's own prover-freshness tooth awaiting the batch-closing pass) |
| 124 | **Each pipeline step is worked in its craft's mindset** (Alexander 2026-07-06 ~11:35: «когда ты делаешь продукт-спеку — ты крутой продакт, когда архитектуру — крутой архитект, когда матрицу тестов — крутой QA-автоматчик, в таком духе»): every step of the pipeline names the profession whose head you wear while walking it — product manager at the spec step, architect at architecture, QA automation engineer at matrix and tests, senior developer at code, and the visitor's own eyes at the verify walk — so each artifact is judged by its craft's standards, not by a generalist's | small | **landed 2026-07-06 ~13:05, session 13** (door: feature; kind: skill; the spec step picked a compact craft LADDER beside the pipeline's step list over per-step scattered sentences — one home, nine lines; SPEC INV-33 binds the law, M-120 covers it; prove-architecture bit correctly: the senior briefed "assignment-free", the suite went red on INV-33 having no owning node, the worker HALTed by contract, INV-33 → build-pipeline node + four pins re-run on the senior's word; stood down by name: facet sweep + design-sync — skill kind, no visible surface; non-goals: no per-artifact hat stamp forced into documents — the report's step accounting speaks the standards, the hat is namable on ask; success measure [default]: the next landing's artifacts read like their craft made them, and the hats are named in its report; delegation: a Sonnet worker ran the seven verbatim edits + red/green proof off a brief carrying the clock + ledger walk, ~15 min senior saved) | Done when: each pipeline step carries its craft line in build-pipeline (one home) — MET (the craft ladder, SKILL.md:31); suite covers the shipped clause — MET (test_craft_ladder red first, then green; suite 110, the one red being the push gate's prover-freshness tooth awaiting the batch-closing pass); the next landing's artifacts each name the hat they were made under — OPEN LEG, rides the next landing's report |
| 127 | **A chat reply's lead [HH:MM] keeps drifting ahead of the wall clock** (problem-ledger CHAT variant, second occurrence 2026-07-06 session 13: mid-session stamps ran up to 7 minutes fast, and a queue stamp born from one had to be corrected before the fence let it commit; first occurrence 2026-07-05 23:47): the date/time fences guard FILES and COMMITS, chat has no mechanical fence — so the pack states the habit as law where human-facing exchange shapes live: communicator gains one sentence — a human-facing timestamp is read off the clock at write time, never continued or extrapolated from an earlier stamp (kin INV-24, the invented-time family's chat face) | small | queued 2026-07-06 ~13:14, session 13 (door: feature — the area lacks a stated rule, nothing to patch; kind: skill; source: .live-spec/PROBLEMS.md CHAT-variant entry, owner assigned at second occurrence by the recurring-problems law) | Done when: the sentence lives in communicator (one home); suite covers it; the ledger entry flips OWNED→SOLVED at landing |
| 128 | **The senior's own movement-end report relapsed into pack jargon** (Alexander 2026-07-06 ~13:20, bouncing the session-13 closing report: «это ты на каком языке вообще разговариваешь??? сформулируй еще раз… все что было непонятно» — the report led with pack-internal names, Russian calques of doc metaphors included, fourth strike of the jargon family in two days and the first AFTER INV-28 landed): the law exists and did not hold on the senior's own chat, where no eval or test can see; the fix is a walked step, not another sentence — before any movement-end or milestone report, the communicator rules are RE-READ and the draft is passed phrase by phrase through one question: does this sentence stand for a reader who does not live inside the pack (a pack surface it names is explained in the reader's words or dropped) | small | queued 2026-07-06 ~13:22, session 13 (door: feature — the area's invariant exists but has no enforcement step on the senior's chat; kind: skill; source: his bounce message, this session) | Done when: communicator states the pre-report walk (one home); the next movement-end report is drafted under it and draws zero "а это что?" from its reader — that reader reaction IS the acceptance |
| 126 | **The with-skill communicator keeps leaking bookkeeping into the human's message** (eval evidence, 2026-07-06: the morning push re-run leaked the test count; the batch-2 re-run leaked "all 64 checks green, v0.9.16" into the message body — two consecutive with-skill runs violating rule 8, which already forbids exactly this): rule 8 states the law but doesn't land in workers; give it teeth — an explicit NEVER-list of bookkeeping tokens as message content (test counts, version numbers, suite sizes; "tested clean / saved" is the translation) and one worked before/after example, so the eval criterion has a hard edge to score against | small | queued 2026-07-06 ~13:13, session 13 (door: bug — the shipped skill text fails its own promise on repeat; kind: skill; source: docs/evals/2026-07-06-batch2-rerun/scores.md + the morning re-run record; owner assigned at the second recurrence by the recurring-problems law) | Done when: rule 8 carries the NEVER-list + example; the next eval re-run's with-skill arm ships a message with zero bookkeeping tokens; suite covers the shipped clause |
| 125 | **The departures board has no name for two pipeline stops** (push-gate prover find, 2026-07-06 session 12, record `docs/prover/2026-07-06-push.md` F4): the board's station list (spec → prove → architecture → matrix → tests → code → verify → landed) promises "the pipeline's own step names, one name per step", but the pipeline walks NINE steps — prove-architecture and commit-and-show have no station, so a feature paused exactly there gets an improvised name, the drift the one-name law exists to stop; fix = either name all nine stations or state in one sentence which steps report under which station (both homes: SPEC INV-27 + communicator's board rule) | small | **landed 2026-07-06 ~12:42, session 13** (door: bug; kind: skill; all nine steps named as stations, landed = the terminal state, not a step; class-swept beyond the two named homes: M-112 + OVERVIEW's pipeline bullet + ARCHITECTURE's lifecycle line — records untouched; red-proven in test_capture_echo_and_board; delegation: a Sonnet worker ran the eight verbatim edits + red/green proof, ~15 min senior saved) | Done when: station list and step list reconciled, one home each — MET (SPEC INV-27 the law's home, build-pipeline the step list's, every other mention now matches); suite covers the mapping sentence — MET (station assertions red first, then green, suite 108) |
| 98 | Publish skill (Alexander 2026-07-05 ~21:15: "короткий паблиш скилл — ридми грамотный; если скилл — команды показать; сравнительный анализ, скриншоты или диаграммы — всё по типу того что заливается"): a publish-quality gate for anything deposited outward — the README/landing-page owes what its artifact KIND owes (a skill: install + commands + when-to-use; a tool: run examples; a visual product: fresh screenshots; comparisons/diagrams where they sell the idea) — the work-kind axis (T-16) applied to PUBLISHING; and target PLUGINS (GitHub repo, plugin directory, design project…) contribute their own steps into the publish walk — his word: "плагин гитхаба должен встраивать свои этапы", same plugin pattern as measurement (row 48); kin: the distributor idea (his voice + follow responses) stays its own later skill | surface | **landed 2026-07-05 ~22:50, session 8** (door: feature; kind: skill; fences held: E-12/INV-13 · rule 17 + M-6 gates · E-19 · T-16 — suite green) | Done when: publish skill spec'd + proven + shipped in the pack; per-kind publish checklist stated once; the plugin seam named (how a target adds steps); first real use on one of our own pushes — MET except the last leg: SPEC E-20 + skills/publish 0.1.0 (fifth working skill), per-kind table one home, target plugins (GitHub · directory · design project), eval red-proven both arms (and it cut both ways: 5 hygiene steps folded INTO the skill from the bare arm), suite 68 green, pack 0.8.4. First real use = the next public push of THIS repo (tonight's push window walks it, journaled) |
