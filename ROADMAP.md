# live-spec Roadmap (dated version: 2026-07-09 — updated at every edit; SPEC M-3)

The wish queue. Intake is continuous — a wish lands here the moment it is spoken. Execution is serial:
the current landing finishes before the next starts. Class = size (bug / small / surface / large — one
vocabulary with the spec) plus a priority mark when it isn't normal: a critical bug lands before
everything; a quick win may bubble up between landings, the jump marked in its row (SPEC T-11, INV-12).
A row also names its DOOR — feature · bug · refactor · docs-only · skip — said at intake, before any
code; feature tripwires outrank a casual label and queue-cutting stays with the bug door (SPEC T-12, INV-16).
And it names its WORK-KIND — product · infra · skill · prose — what the wish builds; the kind scales the
form each pipeline step takes, and a stood-down step is named in the landing report, never silently
skipped (SPEC T-16, INV-22).

Closed rows live in dated archives under docs/queue-archive/ (INV-1); this table holds the ACTIVE queue.

| # | Wish (plain words) | Class | Status | Decision / acceptance |
|---|---|---|---|---|
| 14 | Custom-steps / CI adapter guidance — e.g. a host wires the guardrails into Jenkins/GitHub Actions as a second net (Alexander 2026-07-04) | small | **landed 2026-07-07 ~00:23, session 22** (door: feature; kind: infra; claimed ~00:17; map: extends the guardrails feature; fences held — E-6 gate set unchanged, the reach map stays a LOCAL optimization (the second net runs full, said in all three homes); the [target] came OFF M-5 in the index, the header list, and the suite's declared target map — the self-closing tooth caught each in turn; two more line-wrap needle catches serviced without ceremony (row 153's law applied); delegation decision aloud: kept on the senior — CI semantics are judgment, the rest was three small files; facets N/A) — SPEC v0.15.46 M-5 live · M-154 · `TestCIMirror` RED PROVEN then green · `.github/workflows/gates.yml` (fetch-depth 0, author-day TZ) · guardrails README host guidance · artifact inventory entry · pack 0.8.58 · suite 154 green · the REAL first run watched on this landing's own push (gh) — it went RED on a real find (a machine-local pin demanded in CI), fixed + regression-tested the same half-hour, the fix push green | Done when: ADOPT/README section: same checks, CI mirror — MET (guardrails README, the adopt-facing home); one worked example — MET (the pack repo's own workflow, run watched live) |
| 27 | Name: "live-spec" (hyphen) instead of "livespec"? (Alexander floated it 2026-07-04) | small | **decided 2026-07-05** | Alexander picked **live-spec** (hyphen) on the decision page, over the keep-the-old-spelling recommendation; execution is row 40 (full one-name sweep + host migration note + GitHub rename at the reviewed push) |
| 33 | Is the playbook repo + CLAUDE.md symlink still needed, or obsolete now that live-spec exists? (Alexander 2026-07-05) | small | **decided 2026-07-05** | Alexander accepted the recommendation: keep as the thin PRIVATE personal layer for now; his caveat — this may overlap with live-spec's own personal settings, think with examples — moves to row 43, which owns the final split |
| 42 | Package structure design (from the decision-page answer on the base skill): Alexander leans ONE package (base inside), though prover already lives as a separate public repo too — design how the package and the standalone skill repos relate (single source? copies? install pulls?) and how live-spec loads/attaches other repos. Needs worked examples → a decision page | surface | **decided 2026-07-05** (page 2, 12:48) | Alexander picked **package-is-source** (recommended): the pack repo is the single source, standalone repos are mirrors. His note — reusable, self-contained parts (prover, maybe base, maybe all) must stay findable alone — is exactly what mirrors give; execution = row 51 (a mirror per skill + one sync command). D-4 closes |
| 43 | Personal-settings architecture (from the decision-page answer on the profile home): what lives in CLAUDE.md vs the playbook vs live-spec's own settings — Alexander's framing: "live-spec global personal settings" + "live-spec per-project settings"; absorbs row 33's overlap caveat and row 38's git-home debt. Needs worked examples → a decision page | surface | **decided 2026-07-05** (page 2, 12:48) | Alexander picked **all-into-profile** (over the three-homes recommendation): everything personal moves into live-spec's settings with servlet-like SCOPES — nested, inherited ("у каждой штуки свой скоуп"); CLAUDE.md becomes a thin loader. Plus a setup step: understand who you work with (profile bootstrap). Execution = rows 52 (migration) + 53 (scope model) + 54 (onboarding); D-5 closes as a direction, the design lands through those rows |
| 44 | Learn from other projects/frameworks — study how others run spec-driven / agent-driven development, harvest ideas worth folding (Alexander 2026-07-05: "поучиться у других проектов… но это отдельный version bump") | large | deferred — own version bump; HOMEWORK DONE 2026-07-05 evening | Two research reports in: `docs/research/2026-07-05-methods.md` (spec-kit/BMAD/Kiro/Shape Up/Google/Amazon — top-10 candidates incl. regression-fence sentences, waived-risk verdict, non-goals, facet curation) + `docs/research/2026-07-05-skill-patterns.md` (Anthropic/superpowers/Trail of Bits — top-10 incl. when-NOT-to-use sections, skill evals, loadability check). Candidates go to Alexander on a decision page 2026-07-05; his picks become queue rows. Revisit trigger unchanged: lands as its own MINOR |
| 47 | Feedback-collection skill — a generic pack skill for the human (or their users) to SAY, SHOW, or SEND something into the project at any moment (the intake side of communicator: decision pages go out, this brings feedback in; the inbox pattern generalized beyond wishes) (Alexander 2026-07-05: "шикарная вещь") | surface | **LANDED 2026-07-07 ~11:47, session 24** (door: feature; kind: skill; claimed ~11:07; map: NEW scenario "Sending feedback in" + NEW node/skill **feedback-intake**, the intake half beside communicator; prover CROSS-LINK + architecture lens `docs/prover/2026-07-07-row47.md` — 5 findings, 5 folded, the big one: route-HOMES killed the draft's double bookkeeping (a wish lives in its row, an answer in its archive, a fix in its commit — the ledger owns only the routes with no prior home: field evidence, reactions, wordless drops), and the inbox harvest widened to route-destination (fence split said: one-file + sweep-first fenced, destination re-authored); SPEC v0.15.60 E-28/T-20/INV-68 · E-1 gains the ledger · skill 0.1.0 with eval two-arm run honestly red (bare arm scattered field signals into queue+journal, no one home; with-skill routed all six by the table) · M-172..174 red-proven then green · count-drift sweep rode along (SPEC header "six skills", README heading, two seam cells — the rows-167/169 class) · test-author pin label fixed, pin gate fully clean · installed copies synced, the loader picked the new skill up · pack 0.8.73 · suite 174 green; delegation: context dump + both eval arms on Sonnet workers (~25 min senior saved), footer edits kept on senior — smaller than a brief; facets: no visible surface, the ledger and echo are prose [default: ledger home at host root beside the queue]; re-scope per row 96: the INV-21 tie is authored — field evidence accumulates beside the feature's success measure; rows 48/49 re-scope + the first real loop remain row 96's own legs) | Done when: skill spec'd (channels: text, screenshot/artifact, file drop → each lands with provenance + timestamp in the home its route owns — the prover corrected the one-home shape) — MET; proven — MET; shipped in the pack — MET |
| 48 | Maintenance skill with measurement PLUGINS — after shipping, the product is MEASURED: e.g. a Google Analytics plugin; each user story / canonical axis states how (or explicitly whether) it is measured in the field; findings feed back into the queue (Alexander 2026-07-05) | large | deferred — own version bump | Revisit trigger: first host with a live audience; design pairs with row 47 (feedback in → measurement in) |
| 49 | A/B tests as a pack capability — for software hosts (not every host: a book has none), a wish may land as an EXPERIMENT with two variants and a measured pick; builds on row 48's measurement layer (Alexander 2026-07-05) | large | deferred — own version bump | Revisit trigger: row 48 lands; spec must state where experiments live in the wish lifecycle |
| 54 | Onboarding step — "understand who you're working with": at attach (or first run on a new machine/human) the pack builds or loads the human's profile — the human tells about themselves and/or agrees to a data pull; nothing written without their word (INV-9 holds) (Alexander 2026-07-05) | surface | **build legs LANDED 2026-07-07 ~08:33, session 23; field leg OPEN — first real run rides the next real founding/adoption** (door: feature; kind: skill; map: attach — the founding walk, the step runs before any founding question resolves; prover CROSS-LINK docs/prover/2026-07-07-row54.md — 5 findings, 4 folded in the delta, 1 recorded (the open field leg); architecture: assignment only, B-3 → attach; delegation: the whole mechanical bundle (red-first test, all inserts, template, versions, five pin bumps) ran on a Sonnet worker (~15 min senior saved) — the worker STOPPED correctly on one brief defect (the requirement's test row belonged in the attach component's section; my brief placed it by number order), senior placed it, zero silent fixes; SPEC v0.15.54 B-3 · base 0.1.21 · adopt/ADOPT.md first-breath paragraph · templates/profile.template.md NEW · M-165 · `test_onboarding_step` red-proven then green · pack 0.8.66 · suite 163 green · installed copies synced) | Done when: ADOPT/bootstrap gains the step with a done-when — MET; profile template exists — MET; first real run recorded — OPEN (the row stays in-work on its field leg) |
| 55 | (prover 2026-07-05-architecture, F1) Snapshot machinery gets its own queue row — SPEC S-0 promises every [target] is owned by a row, and the snapshot (E-7, A-6: baseline of the last accepted run, declared-scope diff) had none; design pairs with the guardrails scaffold (row 3) which consumes its diff | surface | **design LANDED 2026-07-07 ~10:51, session 23; machine leg stays [target] on row 3** (door: feature; kind: skill — design prose; map: the snapshot machine in the bounds-holding section; prover folded inline per the small-delta law — the git-as-archive argument closes D-3 on its current pick with the revisit trigger kept, the E-8 tracked-folder law cross-cited, the heavy-surface hash edge stated; delegation: kept on senior, the work was smaller than a brief; SPEC v0.15.58 E-7 design + D-3 Decided · M-169 · `test_snapshot_design` red-proven then green · pack 0.8.70 · suite 168 green) | Done when: snapshot design spec'd, D-3 decided with it — MET; baseline advance wired to *landed* — MET (the law names the transition and the declared-scope asymmetry); declared-scope diff feeds the guardrails — MET as the named seam and the E-6 check clause (the mechanical slice rides row 3, said) |
| 59 | Worker contract: a worker inherits its session's write-ownership narrowed to its brief's files; same-session sibling worker files are fence-benign; live session-scope lines pass into worker briefs; a result failing its brief's acceptance escalates one tier, logged | small | **landed 2026-07-05 ~22:40, session 9** (door: feature; kind: skill; fences held: INV-10 · INV-11 (sharpened not weakened — prover D1) · E-13 · D-2 stays open (D2); stood down by name: facet sweep + design-sync — skill kind; non-goals: no mechanical enforcement of the narrowed ownership yet — rides the router design (row 56); success measure [default]: the next delegated brief carries the contract lines, visible in its checkpoint) — SPEC v0.15.6 ACT-3 contract + pipeline 0.2.10 delegation elaboration; matrix M-095; eval-re-run duty recorded per E-19 (prover D3); suite 79 green; pack 0.8.12 | Source: audit 2026-07-05 (fable F2, opus F-12, composition H2) |
| 62 | Bootstrap order + first landing: VCS gate FIRST, then templates + a minimal runnable suite scaffold; define green-suite for landing #1; hooks offered at bootstrap as at adoption | small | **landed 2026-07-05 ~22:45, session 9** (door: feature; kind: infra; fences held: INV-8 intact · A-0 parity · E-6 cited not restated (prover E4) · template set unchanged; stood down by name: facet sweep + design-sync — infra kind; non-goals: no real bootstrap run tonight — the simulation is the test, the first REAL bootstrap journals the walk; success measure [default]: the next new project starts gate-first with a green scaffold, its journal says so) — SPEC v0.15.7 B-1 rewrite + templates/test_scaffold.template.py shipped + by-deed simulation test both ways; M-034 re-authored; suite 81 green; pack 0.8.13 | Source: audit (fable F6, opus F-13) |
| 63 | Declining a wish lists its absorbed (superseded-into-it) rows; each declined by name or returned to the queue | small | **landed 2026-07-05 ~22:27, session 9** (door: feature; kind: prose; fences held: T-8 three-exit vocabulary intact, INV-1 cited not restated; stood down by name: facet sweep + design-sync — prose kind, no visible surface; architecture: assignment only, T-8 already build-pipeline's; non-goals: no mechanical queue check yet — the behavioural side is a named M-1 audit item; success measure [default]: the first real declined absorber names its absorbed rows, checked at the milestone audit) — SPEC v0.15.3 T-8 extension + ROADMAP template status-values line (prover A3: the template was the half-form surface); matrix M-094; prover addendum in rows57-60 record; suite 75 green; pack 0.8.9 | Source: audit (fable F8) |
| 64 | Mechanize S-0: traceability test — every [target] token maps to a non-landed queue row; a spec section whose architecture pin is real carries no [target] | small | **landed 2026-07-05 ~22:31, session 9** (door: feature; kind: infra; fences held: S-0 granularity clause intact, existing traceability checks untouched; stood down by name: facet sweep + design-sync — infra kind, no visible surface; non-goals: prose-level [target] tokens NOT parsed — the index is the normative surface, S-0's granularity rule makes it so; success measure [default]: the next target drift is caught by the suite on its own, before any audit pass) — declared map anchor→open-row in `test_targets_owned_by_open_rows` + pin honesty in `test_target_nodes_pin_honesty`; building it caught TWO live drifts (E-10 index mark lost; M-061 citing landed row 3) — both folded, red proven vs pre-fix spec; SPEC v0.15.4, M-052 TODO→BUILT; suite 77 green; pack 0.8.10 | Source: audit (opus F-10) |
| 67 | Standalone skill installs ship template-less: spec-author and build-pipeline point at templates that don't resolve from the skill dir — decide ship-inside-skill vs fixed pointers | small | **landed 2026-07-05 ~22:51, session 9** (door: bug; kind: skill; fork taken on the recommended option: FIXED POINTERS to the pack repo — a copy would fork the truth (D-4); surfaced for veto, a veto is a new wish; fences held: skill texts' template steps intact, D-4 direction obeyed; stood down by name: facet sweep + design-sync — skill kind; non-goals: no template fetch automation — the pointer informs, the human/agent fetches; success measure [default]: the next standalone install finds its templates on the first read) — both skills name the pack repo as the templates' home + negative side tested (in-skill templates/ dir = red); A-7 sync via the row-66 tool (its first real job): build-pipeline 0.2.11, spec-author 0.1.10; matrix M-097/M-098; suite 84 green; pack 0.8.15 | Source: skill-creator eval 2026-07-05 |
| 69 | product-prover SKILL.md: extract the category table + glossary into references/ per skill-creator sizing guidance | small | deferred | Source: skill-creator eval; deferred |
| 93 | Design-sync wiring (E-18 [target] machine): base default + host-profile switch vocabulary + communicator/verify channel lines + first real run on a visual host; lands with/after row 86 (work-kind axis) | surface | **pack-side half LANDED 2026-07-05 ~22:20, session 8; row WAITING** (door: feature; kind: skill; fences held: E-13 · INV-14 · base rule 17) — wired: E-18 honest split, base 0.1.9 switch off-by-default, communicator/pipeline channel lines, M-088 + test, suite 67 green. Remainder: the machine's FIRST REAL SYNC on a visual host (track-coach/tlvphoto window) through Alexander's gate | Done when: E-18 loses its [target]; a real host synced with the human's gate shown working |
| 95 | Marathon phase — play, watch, learn (Alexander 2026-07-05 evening, back from the sunset: "это марафон а не спринт; поиграться с проектами, тебя потестировать, смотреть как я веду проекты"): NO version race — 1.0 has no date; the pack proves itself on real project play; Fable OBSERVES how Alexander leads the different windows (read-only: their journals/transcripts) and folds what it learns as inbox wishes/rows | large | queued 2026-07-05 evening (mis-dated 07-06 at intake; corrected against git clock, session 8), frames everything after 0.8.0 | Done when: it never is — this row is the phase's charter; concrete lessons land as their own rows |
| 96 | Feedback loops, raised (his word tonight "мы говорили что можно как-то фидбеки делать" + INV-21's success measures now wait on a reading machine): the feedback → measurement → A/B family (deferred rows 47-49) moves from the deferred tail to next-up after the tlvphoto lessons | surface | queued 2026-07-05 evening (mis-dated 07-06 at intake; corrected, session 8; priority raised on his word) | Done when: rows 47-49 re-scoped against INV-21 and the first feedback loop runs on a real project |
| 99 | De-ceremonialize the appetite wording (Alexander 2026-07-05 ~22:40: "я так и не понял зачем нужен аппетит… этот прикол bmad который закомпьютеризовал ажайл процессы это перебор, нет?"): keep the LAW (a "по-быстрому" wish cuts richness — surfaces in, how considered the defaults — while the safety net always stays: fences, facets, non-goals, success measure), drop the CEREMONY — rewrite T-15 in plain words without the imported term, never surface the word in intake lines unless he says it; his call on wording | small | **in-work 2026-07-05 ~23:15, session 8 — his word arrived ~23:10 and WIDENED the row** ("сделай по-быстрому это неправильно… какой дебил спрашивает программера сколько это займет… можно играться со скоупом а не с таймлайнами"): not just the term — the TIME-BUDGET mechanism dies; scope is the only negotiable dimension (cut surfaces / split into stages), time estimates are never an input (door: feature; kind: prose; fences held: INV-4 · INV-5/INV-18 · T-11 · protection list byte-intact) — **landed 2026-07-05 ~23:25, session 8**: T-15 scope-never-time (cut or stage, estimates never an input), term swept from SPEC/build-pipeline 0.2.9/spec-author 0.1.9/README/M-076, suite ASSERTS the term absent (muscle-memory fence), history untouched, pack 0.8.5, suite 68 green, prover record row99 | Done when: T-15 re-authored to scope-never-time; the word and the time-budget rider gone from every live surface (spec, skills, README, matrix, tests re-authored); safety-net list intact; suite green — MET |
| 100 | **The problem ledger** (Alexander 2026-07-05 ~23:00, watching tlvphoto transcripts: "если есть проблема особенно повторяющаяся… надо ее решать блин. решать!! …либо решать проблему, либо договориться что это не проблема. такое не должно вообще происходить"): recurring OPERATIONAL noise — flaky test harness, missing deps, environment errors, anything that "вылетает" mid-work — gets a per-host DYNAMIC LIST; an entry seen a SECOND time gets an owner that moment: a queue row (solve it) or an explicit dated "agreed non-problem" with the human's word; a third silent recurrence of a listed signature is a defect of the METHOD itself, regardless of the day. Distinct from the recurring-BUG rule (that covers the product; this covers the workshop). Evidence at intake: tlvphoto's "element not clickable: #ex-skip" ×5, "readyState=complete" timeout in two different sessions, missing PIL — all retried, none owned | surface | **pack-side LANDED 2026-07-05 23:39 (git), session 10; row WAITING on the first foreign-host run** (door: feature; kind: skill+infra — two legs, each step's form named: spec full delta · prover CROSS-LINK record row100, 5 findings ALL folded (owned-entry recurrence, SOLVED's actor, archive home, signature merge, worker-brief seam) · architecture assignments only (INV-23 → base-rulebook, E-24 → templates, dogfood pin → package-docs), no re-prove per the doc's own rule · facet sweep: headless, the one explicit N/A sentence in the spec · design-sync: stands down, no visual product; fences held: T-9 bug lane · recurring-BUG re-door stays the product's law · INV-9 · INV-4/E-22 · E-8 · ACT-3 · INV-11 · M-1; delegation savings: the whole implementation bundle — tests, template, ledger, rule-19 insert, 5-skill pin sweep, VERSION, row-103 insert, red-first runs — went to a Sonnet worker, ~15 min senior time saved; the worker correctly STOPPED on two brief defects (M-105's owning node, a wrapped-line assertion), senior folded both) — SPEC v0.15.10 E-24+INV-23 · base rule 19 (0.1.12, pins swept across the five working skills, installed copies synced) · templates/PROBLEMS.template.md · the pack's own ledger OPENED with two live entries: invented-time OWNED by new row 103 (the ledger's first real catch mechanized) + zsh `===` AGREED NON-PROBLEM recommended, awaiting his word · matrix M-103/M-104/M-105 · suite 93 green · pack 0.8.21 | Done when: the ledger's home + shape spec'd and proven — MET; the second-occurrence law in the base skill — MET; the pack's own ledger opened with tonight's live entries — MET; first real host run on tlvphoto/track-coach — REMAINS, rides those windows (this window is fenced to one inbox file per host) |
| 101 | **Done-claims are verified, versioned answers** (Alexander 2026-07-05 ~23:15, after asking track-coach "ты сделал тесты по методологии лайвспека?" — the answer was right but he couldn't tell verified from narrative; his words: "верификация того что произошло это тоже важная фича" + "если сделал — то по какой версии сделал"): when the human asks "is X done / adopted / true?", the answer is an EVIDENCE WALK, never a story — each claim pinned to a checkable artifact (adoption record, prover record, suite run, git commit, matrix row), verified-vs-asserted said apart, AND the answer names the METHOD VERSION it was done by (pack + skill versions from the host's `.live-spec/` installed set, M-7's homes) so "done by live-spec" always means "done by live-spec vX". Kin: reverse-verify spec-vs-shipped (his standing rule), M-7 version homes, A-7 freshness | surface | **landed 2026-07-06 ~00:11, session 11** (door: feature; kind: skill; prover CROSS-LINK record `docs/prover/2026-07-06-night.md` pass 1 — F1 must-fix FOLDED (a host with NO installed set: the answer says so plainly, an absent version is never invented — folded into the clause AND rule 11), F2 folded (rule 11 cites base rule 13, elaborates only the exchange shape), F3 recorded (landing-report MET lines are kin, out of scope by intake); architecture: assignment + pin only (INV-25 → communicator, rule 11 pinned `:122`); stood down by name: facet sweep — skill kind, no visible surface; design-sync — text, stands down; non-goals: nothing left out of his two quotes; success measure [default]: the next done-claim answer he reads carries artifacts + version and he can tell verified from asserted; delegation savings: the whole mechanical bundle — red-first test, matrix row, five SKILL.md edits, VERSION, sync, pin fix — went to a Sonnet worker, ~20 min senior time saved, one honest deviation flagged (a line-wrap point) and accepted) — SPEC v0.15.12 INV-25 · matrix M-107 · `test_done_claim_evidence_walk` RED PROVEN then green · communicator 0.1.11 · pack 0.8.23 · **first real run DONE**: his track-coach question re-answered by the walk — verified: commit `193d39d` exists · 797 test functions in 26 files (795 green + 2 skip = 797, matches the claim) · SPEC + TEST_MATRIX present (docs/) · method version FROM THE ATTACH RECORD `.live-spec/versions.md` 2026-07-05: pack 0.5.3, prover 0.1.5, pipeline 0.2.5 — "done by live-spec 0.5.3"; tonight's version is 0.8.x; asserted only, unverified: that all 795 are green at this hour (suite not re-run) | Done when: the rule spec'd (communicator's domain) — MET; answer template claim → artifact → version — MET (rule 11); first real run = re-answering his track-coach question with pinned evidence — MET (see status); suite covers the clause — MET by deed |
| 107 | **Learn from the neighbours at the implementation level** (Alexander 2026-07-06 ~00:05: "посмотреть чему поучиться у других похожих проектов… на уровне 'как это они реализовали' — посгружать и посмотреть"): clone/download the comparable projects (GitHub Spec Kit — the constitution gate + cross-artifact consistency checks; OpenSpec — change-folder audit trail; GSD — clean-context sub-agent split; BMAD — story files, already partly absorbed) and read HOW they built what our research doc admired from the outside; harvest concrete mechanisms as candidate wishes, each through the normal queue. Research first, adoption never implied | surface | **landed 2026-07-06 ~01:57, session 11** (door: skip — research, no pipeline steps owed; three Sonnet workers cloned and read Spec Kit / OpenSpec / GSD / BMAD at source level; headline: Spec Kit's consistency checks are prompt text — zero mechanical enforcement in its own repo, only file-existence shell tests) | Done when: the named repos read at code level — MET (workers' reports with file-path quotes); harvest doc rendered for him — MET (docs/research/2026-07-06-neighbours-implementation-harvest.md + .html, linked from the morning report); each worth-stealing mechanism filed as its OWN wish row — MET (rows 110–115) |
| 108 | **A feature is interrogated for PRODUCT FIT at intake — a minimal prover on the feature itself** (Alexander 2026-07-06 ~00:25, at night, with the tlvphoto evidence pasted in: "кучу вопросов когда фичер принимается в систему не задается… не надо как в бмад но надо минимальный прувер на фичу… спросить всякие аспекты, как она вписывается в контекст продукта (если юикс то навигация, если бэкенд то флоус)… ты такое даже не додумываешь сам… может все же лайвспек не очень процесс структурирует, потому что кучу вопросов про фичер адопшен не задано"): today's facet sweep asks DEVICE facets (phone/touch/empty/a11y/perf, T-13) but nobody asks the PRODUCT-FIT questions — how the feature sits in the user's JOURNEY (his door example: enter → browse → re-enter → stuck at the 10th picture — a navigation loop nobody specced), what neighbouring behaviours it IMPLIES (no-repeat across doors ⇒ state tracking), what the FEEL owes (premium, animation smoothness), what the next feature it invites is (the greeting). The wish: a kind-scaled FIT interrogation at the spec step — product/UX kind asks journey · navigation · continuity-state · feel · invited-next; infra/backend kind asks flows · data lifecycle · failure paths — each answer becoming a spec sentence or a batched question, same law as the facet sweep (INV-18). His five tlvphoto comments forwarded to tlvphoto/inbox same hour (their window owns the fixes; ours owns the METHOD hole) | surface | **landed 2026-07-06 ~10:55, session 12 — repo side whole; ONE OPEN LEG: installed-copy sync rides Alexander's `! sh ~/live-spec/install.sh` (the harness classifier blocks an agent's own hand in ~/.claude/skills, both worker and senior tried)** (kind: skill; door: feature; his morning words folded: the walk is a small prover on the wish — «прувер может валидировать что угодно», trivially-closable holes closed and WRITTEN how, «не мучать пользователя вопросами»; prover CROSS-LINK record `docs/prover/2026-07-06-rows108-119.md` F1/F2/F7/F8 folded; architecture: INV-29 → spec-author node; facet sweep + design-sync stood down by name — skill kind; non-goals: no retro-walk en masse (binds forward), no mechanical fit-gate; success measure [default]: the next product-kind wish in an adopted host ships with journey sentences and he never asks «а отсюда куда?»; eval criteria + re-runs ride the push gate (E-19); delegated to a Sonnet worker with rows 117–119) | Done when: the fit-lens list spec'd kind-by-kind in one home (spec-author's sweep section widened, prover gains the FEATURE-FIT focused mode the way CROSS-LINK exists); every fit answer lands as spec sentence / [default] / batched question, never silently; first real run on the next product-kind wish in any adopted host; suite covers the shipped clause |
| 109 | **Visual clauses carry their prototype pointer; the build step opens it** (harvested 2026-07-06 ~01:20 from inbox/2026-07-05-prototype-norm-lens.md — the tlvphoto incident where an approved prototype's clause was rebuilt from TEXT alone and shipped a cheap look-alike, 75 tests green proving the misreading; same incident family as row 102, DIFFERENT law, so its own row by 102's own one-story rule): four pack sentences — spec-author: a clause born of an approved visual prototype CITES the artifact ("norm: <file>"); build-pipeline code step: a norm-pointered surface OPENS the artifact before building, one-line plan-vs-prototype diff recorded; build-pipeline door step: a declared mockup-first entry condition is cancellable only BY NAME, never by a general "go build"; prover: a lens — every visual clause backed by an artifact, text never contradicting its own artifact | surface | **landed 2026-07-06 ~22:54, session 22** (door: feature; kind: skill; claimed ~22:40; fences: E-17 one-way fence stays · INV-16 promotion-enters-at-spec stays · INV-30 feel-bar-vs-prototype stays — full suite green proves them held; map: changes the pipeline feature — spec-author / build-pipeline / product-prover nodes, no new surface; prover CROSS-LINK `docs/prover/2026-07-06-row109.md` — 3 findings ALL folded in-pass: F1 must-fix, the pointer would trip the SHIPPED prototype-fence scan (SPEC.md is in its scan set), folded as approval FREEZES the artifact into `docs/norms/` with a dated provenance line, a pointer never reaching a live prototype home; F2, the mockup-first condition gets a written home — the wish's queue row ("entry: mockup-first"), never chat memory alone; F3, the verify feel bar reads the same pointer; architecture: assignment only — INV-43 → build-pipeline, wiring notes on spec-author (format) + product-prover (lens) phrased without anchor tokens to keep owns-once parse clean; prove-architecture stood down by name (no structure change); facet sweep + design-sync stood down by name — skill kind, no visible surface ("facets N/A" in the law); fit walk closed in-clause (trigger: approval / norm-pointered build / declared entry condition / prover read · correction: cite+freeze / open+diff / cancel-only-by-name / finding · not-fire: unapproved sketch, text-born clause); non-goals: no mechanical pointer-grep guardrail (candidate after real usage), norm artifact format free; success measure [default]: the next prototype-born surface lands with pointer + diff line and the look-alike class does not recur; delegation savings: red-first test + five verbatim edits + four version bumps + installed-copy sync — a Sonnet worker off a clock+ledger-armed brief, ~15 min senior saved, zero brief defects, zero WATCHED lines) — SPEC v0.15.40 INV-43 · M-140 · `test_prototype_norm_pointer` RED PROVEN then green · spec-author 0.1.17 · build-pipeline 0.2.30 · product-prover 0.1.11 · pack 0.8.52 · suite 135 green · README pipeline section refreshed same push (his 22:44 shopfront word walked by deed ahead of row 146) | Done when: the four sentences live in their four homes (one home each) — MET (spec-author §norm-pointer, build-pipeline door bullet + step 7, product-prover Phase 3e lens); norm-pointer format spec'd — MET (`norm: <path>` at line end, frozen copy in `docs/norms/` with dated provenance); suite covers the shipped clauses — MET (`test_prototype_norm_pointer`, M-140, red first then green); inbox file archived with a pointer to this row — MET (docs/queue-archive/2026-07-05-prototype-norm-lens.md) |
| 110 | **The verify step turns adversarial: a fresh-context checker re-derives from the spec** (row-107 harvest, 2026-07-06 ~01:25; GSD's verifier — opening hypothesis "tasks completed, goal missed", claims checked exists → substantive → wired → data-flows, literal stub-detection greps; their reason is our law: workers report PASSED even when wrong): our verify-by-deed is the senior's own eyes on work the senior briefed — add the option (mandatory for large landings) of a FRESH-context verifier that re-derives what must be true from the SPEC sentences, never from the worker's summary, and greps for stub patterns | surface | **landed 2026-07-06 ~23:55, session 22** (door: feature; kind: skill; claimed ~23:39, lane A of three off the dependency graph; prover CROSS-LINK in `docs/prover/2026-07-06-rows110-114-115.md` — F1 folded (no checker-of-checker: findings judged by the senior, regress closed), F2 folded (surface-sized defined: new surface OR multi-file behaviour change); architecture: assignment only — INV-46 → build-pipeline; facet sweep + design-sync stood down by name (skill kind, no visible surface); non-goals: no separate verify skill (step 8 is the home), no checker on small senior-built edits; success measure [default]: a delegated large landing never ships a looks-done facade — the checker's verdict rides its report; delegation: lane-A Sonnet worker (test + step-8 text + version), ~10 min saved; FIRST REAL RUN ON ITS OWN LANDING: a fresh-context checker briefed with only the spec sentences + paths walked all six claims — verdict GOAL MET + 3 findings (a matrix over-claim, a weak test needle, a self-contradicting row phrase), ALL folded before the landing commit — the law caught its own senior twice in its first hour) — SPEC v0.15.43 INV-46 · M-144 · `test_adversarial_verify_option` RED PROVEN then green · build-pipeline 0.2.33 · suite 147 green | Done when: the adversarial-verify option spec'd (when it fires: which sizes/kinds), the stance + level ladder + stub-grep list in the pipeline's step 8 or a verify skill; first real run on a real landing; suite covers the clause |
| 115 | **The resume file is a digest with a size cap** (row-107 harvest ~01:25; GSD's STATE.md <100 lines): NEXT_STEPS LIVE-STATE bounded — growth is a design failure; detail flows to the journal it points at; a suite check owns the cap | small | **landed 2026-07-06 ~23:55, session 22** (door: feature; kind: infra; claimed ~23:39, lane C of three; prover record shared — F1 recorded honestly (the pack's own file is under the cap today, the red proof lives in the synthetic fixture), F2 recorded (long-line gaming: no speculative mechanism, earns a row if seen in the field); the INV-26 tension resolved by FORM: one terse line per open leg, detail flows to journal/queue; architecture: assignment only — INV-48 → templates; facets N/A; non-goals: no per-section caps, no auto-compactor; success measure [default]: a cold start reads the resume file in one minute and no open leg is ever dropped by compaction; delegation: lane-C Sonnet worker (new test file + template note, red→green on the template needle), ~8 min saved) — SPEC v0.15.43 INV-48 · M-146 · `tests/test_resume_digest.py` (3 tests; template needle RED PROVEN then green; synthetic 120-line bloat flagged) · suite 147 green | Done when: the cap chosen and spec'd, the check red-proven on a synthetic bloated file, our own NEXT_STEPS compacted under it |
| 117 | **Verify-by-deed learns to walk the visit and watch the feel** (tlvphoto inbox 2026-07-06 ~10:10, §1 + Alexander's morning word «давай учитывать»): the product kind's step 8 gains a named VISITOR WALK — the whole journey as the person lives it (first visit · return · cross-entry · from-any-point navigation · exits) — and a FEEL pass: motion quality (easing, duration, choreography) and affordance craft judged against the approved prototype's bar; findings become rows, never vibes. Evidence: tlvphoto's transitions and its open-work button shipped green through the full pipeline and read cheap on sight. Kin: row 108 owns the intake half (journey questions at spec time); this row owns the verify half | surface | **landed 2026-07-06 ~10:55, session 12 — repo side whole; ONE OPEN LEG: installed-copy sync rides Alexander's `! sh ~/live-spec/install.sh` (the harness classifier blocks an agent's own hand in ~/.claude/skills, both worker and senior tried)** (kind: skill; door: feature; source: inbox note §1 + his «давай учитывать»; prover record `docs/prover/2026-07-06-rows108-119.md` F3 folded (SPEC binds THAT, the step-8 cell holds the checklist); architecture: INV-30 → build-pipeline node; stood down by name: facet sweep + design-sync — skill kind; non-goals: tlvphoto's own five fixes stay in their window; success measure [default]: nothing green-but-cheap reaches his eye before the walk names it; eval re-runs ride the push gate) | Done when: visitor-walk + feel pass spec'd into step 8's product-kind form (one home); prover/eval cover the clause; first real run on a product-kind landing in an adopted host; inbox file archived with a pointer at landing |
| 118 | **A taste default may not outlive two landings unreviewed** (tlvphoto inbox 2026-07-06 ~10:10, §2): the [default]-tag is right for velocity — the lane never blocks — but nothing closes the loop: tlvphoto carries 8+ open defaults across two landings and the product reads unfinished everywhere. The law: a landing report LISTS its [default]s; the next human review resolves them (one decision page, communicator rule 10); a default surviving two landings without the human's word is flagged loudly by the gate/report | small | **landed 2026-07-06 ~10:55, session 12 — repo side whole; ONE OPEN LEG: installed-copy sync rides Alexander's `! sh ~/live-spec/install.sh` (the harness classifier blocks an agent's own hand in ~/.claude/skills, both worker and senior tried)** (kind: skill; door: feature; source: inbox note §2; the spec step answered its own tension: the lane still never blocks — the LOOP closes via the landing report's restated open-defaults list (no counter file, F4) and the two-landing expiry onto the decision page; prover record F4 folded, F5 recorded honestly (age-checker guardrail = a candidate wish standing on its own); architecture: INV-31 → communicator node; stood down by name: facet sweep + design-sync — skill kind; success measure [default]: no default older than two landings anywhere; eval re-runs ride the push gate) | Done when: the expiry law spec'd (count home + flag mechanics chosen); communicator's decision-page rule cites it; suite covers; tlvphoto's open defaults become the first real decision page after their window adopts the clause |
| 119 | **A decision card asks in consequences, leaving mechanisms aside** (Alexander 2026-07-06 ~10:40: «то что ты выше написал понятно, а то что ты мне в хтмл выдал непонятно!!!» — the shell-separator card explained the mechanism and he could not tell what he was deciding, twice): every card opens with what the choice CHANGES for the person under each option, in the product's words; options labelled by consequence; mechanism only if it helps | small | **landed 2026-07-06 ~10:55, session 12 — repo side whole; ONE OPEN LEG: installed-copy sync rides Alexander's `! sh ~/live-spec/install.sh` (the harness classifier blocks an agent's own hand in ~/.claude/skills, both worker and senior tried)** (kind: skill; door: feature — same family as row 116, its decision-page face; prover record `docs/prover/2026-07-06-rows108-119.md` F6 checked (composes with ask-by-showing); architecture: INV-32 → communicator node; stood down by name: facet sweep + design-sync — skill kind; success measure [default]: he answers every card without asking what it means; eval re-runs ride the push gate) | Done when: INV-32 spec'd; communicator rule 10 opens cards with consequences; suite covers; the next decision page written under it |
| 121 | **The feel lens takes the medium's own form** (Alexander 2026-07-06 ~11:20: «"чувствуется" применимо скорее к фронтенду, а проект может быть хоть книгу написать — это частичный навык, по контексту»): the visitor walk stays for everything a person visits, but WHAT it watches scales — motion and affordance craft for a browser product, reading path and chapter flow for a book, command round-trip for a CLI; never a frontend checklist forced on prose | small | **landed 2026-07-06 ~11:30, session 12** (door: feature — same-day correction on his word; kind: skill; one scaling sentence in INV-30 + index + M-115) | Done when: INV-30 carries the scaling sentence; suite green |
| 128 | **The senior's own movement-end report relapsed into pack jargon** (Alexander 2026-07-06 ~13:20, bouncing the session-13 closing report: «это ты на каком языке вообще разговариваешь??? сформулируй еще раз… все что было непонятно» — the report led with pack-internal names, Russian calques of doc metaphors included, fourth strike of the jargon family in two days and the first AFTER INV-28 landed): the law exists and did not hold on the senior's own chat, where no eval or test can see; the fix is a walked step, an action carried out — before any movement-end or milestone report, the communicator rules are RE-READ and the draft is passed phrase by phrase through one question: does this sentence stand for a reader who does not live inside the pack (a pack surface it names is explained in the reader's words or dropped) | small | **landed 2026-07-06 ~13:52, session 14 — whole: the open leg (the reader's read of the landing's own report, INV-26) CLOSED 2026-07-06 ~13:57 on his resume words «отчет был понятен» — zero "а это что?"** (door: feature; kind: skill; fit walk, skill lenses: trigger — a movement-end or milestone report about to be sent · correction — re-read the communicator rules, pass the draft phrase by phrase through the outside-reader question · not-fire — mid-work narration lines, working notes, machine-read docs (prover F2: deliberate scope — narration stays under the report-shape law alone); facet sweep + design-sync stood down by name — skill kind, no visible surface; SPEC INV-34 — the report law is walked afresh each time — the walk's one home in communicator's own new section; matrix M-122 · `test_pre_report_walk` red first, then green; prover record `docs/prover/2026-07-06-rows126-128.md` (F1 folded in-pass, F2 rejected as deliberate scope); fences held: the walk adds no questions — defaults still TOLD, silence stays consent (INV-31); trailing anchors stay legal (INV-28's own law); non-goals: the walk scopes to movement-end and milestone reports; success measure = the acceptance leg itself) | Done when: communicator states the pre-report walk (one home) — MET (v0.1.19, its own section); the next movement-end report is drafted under it and draws zero "а это что?" from its reader — MET 2026-07-06 ~13:57, his resume words: «отчет был понятен» |
| 129 | **A project knows its own KIND, and the kind evolves** (Alexander 2026-07-06 ~13:27, before the session-13 wipe: «нужно понять (и апдейтить если надо) какой это проект… если это книжка пишется или это только бэкенд или это только сайт статический или это фуллстек… тебе важно знать и апдейтить походу поскольку все эволюционирует», custom kinds explicitly expected): today the pack classifies each WISH (product/infra/skill/prose, T-16) and a host may set one intake default (`work-kind.host-default`, base defaults table) — but nothing states the PROJECT's own kind (a book · a backend service · a static site · a fullstack app · a CLI · a skill pack · a custom kind), asks it at founding/adoption beside the personal-vs-reusable question, records it in ONE home (the host profile), and lets it scale which steps, facets and feel-lenses apply by default across the whole project; and nothing RE-CHECKS it as the project evolves — a static site that grows a backend must update the line on the human's word, journaled, the moment the evolution is noticed, ahead of any audit. Custom kinds join the same way every table grows: through the queue | surface | **landed 2026-07-06 ~15:58, session 16 — repo side whole; ONE OPEN LEG (INV-26): the first real HOST carries a `project.kind` line by deed — rides the next adopted-host window (this window is fenced to its own tree)** (door: feature; kind: skill; map: changes the founding walk + the intake defaults; worked as ONE head with row 132; prover CROSS-LINK record `docs/prover/2026-07-06-rows129-132.md` — F2 folded: the seed proposes, a written `work-kind.host-default` line disposes; F4 folded: project.kind is always asked, never profile-seeded; architecture: INV-36 → attach (B-2's own precedent), assignment only, no re-prove per the doc's rule; stood down by name: facet sweep + design-sync — skill kind, no visible surface; non-goals: no retro-ask for already-attached hosts (the line arrives at the next landing that leans on it), no mechanical kind-linter; success measure [default]: every founding/orient from now on records the line and he never has to re-explain what a project is mid-work; delegation savings: the whole mechanical bundle — matrix rows, two red-first tests, eight skill-text edits, version sweep, installed-copy sync — one Sonnet worker, ~20 min senior saved, zero brief defects) | Done when: the project-kind line spec'd (one home: host profile; asked at founding/adopt; re-checked on evolution with a journaled update on the human's word) — MET (SPEC INV-36); the kind vocabulary + custom-kind path stated — MET; composition with T-16 stated in one sentence — MET (three verdicts, one intake breath); suite covers the shipped clauses — MET (M-125, red-proven); the pack's own repo carries the line by deed — MET (`.live-spec/profile.md`); one real host — OPEN LEG |
| 131 | **Work is narrated while it runs** (Alexander 2026-07-06 ~13:57, resuming session 15: «не забывай (была просьба) отчитываться и по ходу действия. мне надо чтобы это тоже работало. это должно быть и в проекте коммуникации зафиксировано» — the SECOND ask in one day; the morning's word had landed only as a personal-profile line (chat.working-narration), and a habit held only in a personal profile did not carry across sessions, so the law moves into the pack): between the capture echo and the landing report the lane never goes silent — each beat worth a sentence (a pipeline station passed, a load-bearing find, a change of direction) is said as it happens, one-two plain sentences in the roadmap's terms, the same voice as the reports; the mechanical grind stays quiet; a narration line is chat only — no pre-report walk, no questions, the plain-language and bookkeeping laws still bind | small | **landed 2026-07-06 ~14:18, session 15 — OPEN LEG (INV-26): the next working session narrates unprompted, no third ask** (door: feature; kind: skill; SPEC v0.15.25 INV-35 — the third voice between the echo and the report; communicator 0.1.20: rule 13 + the (e) when-it-fires arm + description NOT-side reworded — narration is a standing habit, never a load-trigger + "thirteen rules"; prover CROSS-LINK record `docs/prover/2026-07-06-row131.md`: F1 folded in-pass (INV-28's line enumeration now names narration lines), F3 folded in-pass (profile line chat.working-narration shrunk to tuning + pointer — one home), F2 folded at the code step (the description rewording), F4 rejected — the narration-vs-«(себе)» boundary is a deliberate judgment line, FOR-the-reader is the discriminator; architecture: assignment only, INV-35 → communicator; matrix M-124 · `test_working_narration` red first (proven against the pre-edit communicator), then green; the row-68 fence renegotiated in wording alone, its fact unchanged: the description keeps its NOT-side, only the test's pinned phrase updated to the new wording; fences held: INV-34 walk scope · INV-27 echo · INV-31 no-questions · INV-28 laws now explicitly binding narration lines; stood down by name: facet sweep — no visible surface, facets N/A; design-sync — skill kind; non-goals: no cadence quota, no mechanical enforcement — chat has no suite, kin INV-34; success measure [default]: the ask does not recur — no third "рассказывай по ходу" reminder ever needed; delegation: a Sonnet worker ran the matrix/test/skill edits/versions/sync + three suite runs, ~15 min senior saved, and STOPPED correctly twice — once on the senior's wrong matrix anchor (corrected, resumed same tier, logged), once on the row-68 needle conflict (the senior's call); dogfood: the landing session itself narrated by the rule as it built it) | Done when: SPEC states the law (INV-35) — MET; communicator carries the narration rule (one home) — MET (rule 13); suite covers both — MET; the next working session narrates unprompted — PENDING, the open leg |
| 130 | **The pack's skills pass through the standard skill-making skill** (Alexander 2026-07-06 ~13:27, same message: «не забывай все время прогонять через стандартный скилл который скилы делает»): the six pack skills are authored by hand and tested by our own evals, but they never walk skill-creator — the standard skill for making and validating skills (format/frontmatter validation, description-triggering quality, eval machinery); run each pack skill through it, fold what it flags, and put the re-walk on the milestone list so it happens "все время" — every time, recurring | small | queued 2026-07-06 ~13:32, session 13 (door: feature; kind: infra — tooling for the pack's own workshop; source: his word this session; kin E-19 — our evals stay, skill-creator adds the format/trigger lens). Pre-evidence 2026-07-06 ~16:05 (session 16, his ask): the SIZE lens already walked — all six SKILL.md under the 500-line ideal (largest: product-prover 382, spec-author 343), no references/ split needed yet; row 69's extraction pressure eased, cite this when the full walk runs | Done when: all six skills walked through skill-creator once with findings folded or rejected-with-reason in a dated record — MET (docs/audit/2026-07-06-skill-creator-walk.md: 13 findings, 7 folded, 4 rejected with reasons, 2 re-queued as row 137); the milestone checklist (M-1 family) carries the re-walk item — MET (SPEC v0.15.28 M-1 clause, matrix M-128, test red-proven then green); the next skill added to the pack walks it at birth — the clause binds forward. **LANDED 2026-07-06 ~16:36, session 17** (walk delegated: three read-only analysis agents, two skills each, raw evidence in scratchpad checkpoints — roughly an hour of senior reading returned as three findings lists; folds: spec-author's prover link repointed to the pack repo, base + prover + communicator descriptions gain their NOT-sides / lose history scars and bare rule numbers (row-68 fence held — pinned phrases verbatim), build-pipeline's PLAYBOOK refs name the private playbook repo, publish gains a worked before/after example; versions: base 0.1.15 · spec-author 0.1.15 · product-prover 0.1.10 · build-pipeline 0.2.24 · communicator 0.1.23 · publish 0.1.1, all six base-version citations re-pinned, installed copies synced; suite 118) |
| 133 | **«Покажи все фичи» — the feature map readable on demand** (Alexander 2026-07-06 ~15:52, mid-landing of rows 129/132: «возможно нам нужны команды какие-то типа "покажи все фичи" или что-то такое. какая-то прозрачность»): the map now exists as law (spec scenarios + architecture nodes + the rows' `map:` notes, INV-37) but only the agent reads it — a spoken command should render it FOR THE HUMAN: every feature by its plain name, its state (shipped / in-work / queued / idea), what changed lately, in the product's words; likely one rendered page per ask (the render-doc road), kin to the departures board (INV-27 shows in-flight wishes; this shows the WHOLE product) | surface | queued 2026-07-06 ~15:58, session 16 (door: feature; kind: skill — the exchange shape first; a rendered-page form may add a prose leg; map: NEW feature beside the departures board; ask-at-intake: none — the spec step decides the reading's exact form and batches any taste call). **First informal run 2026-07-06 ~16:05, same session, on the pack itself**: the full feature list spoken in chat off the spec's scenarios + the header's shipped-vs-target split, on his ask («можешь же список фич сказать») — evidence for the spec step: the reading derives cleanly from existing documents, no third document needed | Done when: the command's trigger + the map-reading's form spec'd (one home) — MET; a real «покажи все фичи» ask in any window renders the map and he can tell shipped from queued at a glance — PENDING, the open leg; suite covers the shipped clause — MET. **LANDED (legs 1+3) 2026-07-06 ~16:27, session 17** (SPEC v0.15.27 — new scenario "Asking what the product does", INV-38: read at ask-time off spec scenarios + the header at the promised-tag's own granularity + the queue's open rows, queued NEW-verdict wishes included; no third document; chat by default, rendered page on his word [default]; never fires uninvited; empty host answered honestly; communicator 0.1.22 rule 14 + when-it-fires arm (f); architecture: assignment only, INV-38 → communicator; matrix M-127 · `test_feature_map_on_demand` red first (proven against the pre-edit communicator), then green; prover CROSS-LINK `docs/prover/2026-07-06-row133.md` — F1 statuses at the promised-tag's granularity (must-fix, folded in-pass), F2 queued NEW wishes were invisible to a scenarios-only read (folded in-pass); fences held: INV-27 board scope · INV-37 intake placement · E-14 no-third-document; stood down by name: design-sync — skill kind; non-goals: no standing feature document, no auto-dashboard, no per-feature history. **OPEN LEG (INV-26): the law forbids firing uninvited, so the first real post-law ask rides HIS next «покажи все фичи», any window**) |
| 134 | **The chat clock gets a mechanical hand** (problem-ledger CHAT variant, re-opened 2026-07-06 15:50, session 16 — the next-session recurrence its 14:18 note named: leads extrapolated up to ~6 min ahead again while the wall clock read 15:50; the habit + the shipped sentence did not hold across sessions, third session in a row ⇒ METHOD defect by the ledger's own law): the reply's leading [HH:MM] stops being handwritten — a harness hook (candidate: a session hook that injects the wall clock into every reply context, wired via settings.json) makes the stamp read off the machine — the hand no longer sets it | small | **build leg MET 2026-07-06 ~20:42, session 19; field legs OPEN** — built as LANE B of this session's double-lane run by a Sonnet worker in an isolated worktree while lane A (row 139) held the pen (T-18): `scripts/clock-hook.sh` + `tests/test_clock_hook.py`, RED-proven on the absent script (3 tests failing) then green, suite whole in the worktree and re-run on the integrated tree; integrated under the pen after row 139 landed — SPEC v0.15.32 INV-24 chat face gains the mechanical-hand sentence, matrix M-132 (guardrails node), architecture pin `scripts/clock-hook.sh:1`; the landing itself evidenced the need twice: the commit fence BLOCKED row 139's first landing on 20:42/20:43 stamps written at a 20:38 clock, and the session's chat leads had drifted the same ~4 min (occurrence appended to the ledger); INSTALL leg: the agent's own hand was blocked by the harness classifier (self-config), exactly the Done-when's named road — `scripts/install-session-hooks.sh` awaits Alexander's one `!` command; delegation: the whole build = one Sonnet worker off a self-contained brief, ~20 min senior saved; facet sweep + design-sync stood down by name — infra, no visible surface; non-goals: no rewrite of past wrong stamps (prose quoting stays legal, the family's standing scope); success measure [default]: the next session's leads carry machine-read times, zero drift catches; queued 2026-07-06 ~15:58, session 16; map: changes the clock law's chat face (INV-24 family) | Done when: the hook exists — MET; installed on this machine (Alexander's `!` hand if the classifier blocks the agent's — it did; his command pending) — OPEN; a full session's leads carry machine-read times with zero drift catches — OPEN, next session's evidence; the ledger entry flips back to SOLVED citing this row — OPEN, rides the zero-drift session |
| 135 | **Parallel lanes — some features (or their stages) run at the same time** (Alexander 2026-07-06 ~16:20, session 17: «надо продумать параллелизм — некоторые фичи можно делать параллельно, или этапы фич; я понимаю что это тратит токены, но зато ускоряет процесс»): today the lane law serializes landings (one wish in-work at a time, INV-2) while parallelism already exists BELOW the lane — workers on disjoint files under the senior (ACT-3) and read-only background analysis; the wish is to LIFT parallelism to feature level where it is safe: which stages of two features may overlap, what must stay serialized (the shared docs — spec/matrix/queue edits, the integration, the closing of a row), how the departures board reports two moving trains, and what the human sees when both want his word | large | landed (pack side) 2026-07-06 ~17:20, session 18 — per-leg (INV-26): spec states the overlaps in one home MET (SPEC v0.15.29: T-18 + INV-39 born, INV-2 re-authored to the pen, T-9 parks per-lane, ACT-3 gains the isolated tree); proven MET (CROSS-LINK `docs/prover/2026-07-06-row135.md` — 7 findings, all folded in-pass: the T-9 parked-count contradiction, the lost waiting-lane board face, the shared-tree worker leak into landings, four smaller); pipeline + base carry MET (build-pipeline 0.2.25 "Two trains, one pen" gate · base 0.1.16 rule 7 pen sentences · communicator 0.1.24 rule 9 waiting-lane face); suite covers MET (M-129/M-130 born, M-022/M-019 re-derived to the cap, `test_parallel_lanes_law` + `test_landing_purity` red-proven then green, suite 120); **final leg MET 2026-07-06 ~18:12, session 18 — the first real double-lane run: rows 136 (lane A, pen) + 137 (lane B, isolated worktree) landed clean the same session, each landing commit carrying exactly its own row's delta, the two-train board read live in chat (the waiting/working faces held); row CLOSES WHOLE**; in-work 2026-07-06 ~17:03, session 18 (fences: INV-2's atomic claim + foreign-session back-off · INV-26 · INV-3 · INV-4 · INV-11 · T-9 bug preemption · INV-27's echo + stations · ACT-3's checkpoint/clock/escalation duties); queued 2026-07-06 ~16:26, session 17 (door: feature; kind: skill — the method's own law; map: CHANGES the wish lifecycle — the lane law (INV-2) and the worker contract (ACT-3); ask-at-intake: none — the spec step drafts the legal overlaps and batches the taste calls). Pre-evidence, same session: row 130's three read-only analysis agents ran in the background while row 133 walked the pipeline in the lane — the safe pattern already carries real work | Done when: the spec states which pipeline stages of distinct features may legally overlap and what always serializes (one home, composed with INV-2/ACT-3/INV-11); proven; the pipeline + base carry the rule; suite covers it; one real double-lane run lands clean with its board readable |
| 136 | **The pack checks GitHub for its own updates** (Alexander 2026-07-06 ~16:24, session 17: «в бейз скилл может можно встроить механизм обновления с гитхаба? например раз в день проверять и предлагать обновить»): today freshness (base rule 8 / A-7) re-reads the LOCAL installed copies, and delivery to another machine is by hand; the wish: a mechanism — likely a daily check (launchd or session-start hook) comparing the installed pack version against the public repo's — that PROPOSES an update when the remote is newer, never installs silently (the human's gate; install.sh already carries the attic backup road) | small | landed 2026-07-06 ~18:00, session 18 — LANE A of the first double-lane run (SPEC v0.15.30 E-25 + A-7's citing sentence; prover CROSS-LINK `docs/prover/2026-07-06-row136.md`, 2 findings folded in-pass — the dead-URL-as-offline mask, the ahead-of-public downgrade case; architecture: E-25 → attach, pin `scripts/check-pack-update.sh:1`; matrix M-131; `test_pack_update_check` red-proven then green, REAL runs against fixtures + one live run by deed ("up to date"); README install section carries the staying-current note; pack 0.8.41; base deliberately UNTOUCHED — a base bump would have re-pinned citations inside lane B's files, the independence judgment held by design); in-work 2026-07-06 ~17:51, session 18 (T-18; fences: A-7's freshness walk stays whole · M-7 version homes · E-21 installer road untouched · the human's install gate stays his, base rule 17/ACT-1); queued 2026-07-06 ~16:26, session 17 (door: feature; kind: infra — tooling for the pack; map: NEW feature beside install/freshness (A-7, M-7 family); ask-at-intake: none — the spec step picks the check's home (hook vs launchd) and batches it if it turns out a taste call) | Done when: the check exists and is installed on this machine; a version gap yields a spoken proposal with the changelog, never a silent install; the freshness law cites the mechanism; suite covers the shipped clause |
| 137 | **The densest skill rules get scannable shapes** (skill-creator walk 2026-07-06, findings 6+11 of docs/audit/2026-07-06-skill-creator-walk.md): build-pipeline's step-zero bullet (~18 lines unbroken, dozens of inline codes) and communicator's rules 6/8/9/10/11 (single dense paragraphs) fail the 30-second scan a fresh agent needs; break each into a lead imperative + short sub-points WITHOUT losing law — the test-pinned strings stay verbatim, every sentence keeps its home, the suite proves nothing moved | small | landed 2026-07-06 ~18:11, session 18 — LANE B of the first double-lane run, built ENTIRELY in an isolated worktree by a Sonnet worker while lane A held the pen, integrated under the pen after lane A's landing with the gate re-run on the NEW truth (T-18/INV-39 lived exactly as written): build-pipeline's step-zero bullet + communicator rules 6/8/9/10/11 reshaped to lead-line + sub-points, zero words added/removed/moved (programmatic token-equality per block), every test-pinned string verbatim, line counts 300/284 — under the 500 ideal; suite green in the worktree AND on the integrated tree (122); the walk record's findings 6+11 flipped to FOLDED citing this row; build-pipeline 0.2.26 · communicator 0.1.25 · pack 0.8.42, installed copies synced; queued 2026-07-06 ~16:36, session 17 (door: refactor — form moves, no behaviour; kind: skill; map: changes build-pipeline + communicator, their FORM only; entry at the pipeline's refactor door: full suite + matrix audit of the touched sections) | Done when: both skills' densest rules read as lead-plus-subpoints; every pinned string survives verbatim (suite green before/after); line counts stay under the 500 ideal; the walk record's two re-queued findings flip to folded citing this row |
| 138 | **Offline windows — say when he can step away** (Alexander 2026-07-06 ~17:26, session 18: «надо понять иногда писать когда можно оффлайн (например если тесты локально бегут)»): when a stretch of work needs nothing from the human for a while — a local suite run, a delegated worker batch, a long render — the working narration SAYS so plainly: that he may step away and roughly for how long (an honest range, never a guess dressed as a promise), and says again when he is needed back | small | **landed 2026-07-06 ~22:10, session 21** (door: feature; kind: skill; queued 2026-07-06 ~17:51, session 18; map: changes working narration (INV-35 family, communicator); shape: NOT a fourth tooth — the heartbeat tooth grows a second, forward-looking face, so the three-teeth structure and its suite pin stay intact and the fence sentence row 139 left is exactly the socket filled; prover CROSS-LINK `docs/prover/2026-07-06-row138.md` — 2 findings folded in-pass (the window has three ends — overrun, done sooner, blocked on his word — all spoken; the needed-again beat is a chat line awaiting his return, never a summons) + the class sweep covering both communicator copies; architecture + its prove stood down by name — no structure change, INV-35 stays owned by the communicator node; facet sweep + design-sync stood down by name — skill kind, no visible surface; budgets (INV-41) untouched — no new surface; homes: SPEC v0.15.38 INV-35 (scenario + invariant row) + communicator 0.1.30 heartbeat tooth; M-138; `test_offline_window` RED-proven on the communicator side first, then green, suite whole; non-goals: no timer or hook mechanics (spoken judgment, like the clock law's chat face), no push/summons machinery (reaching an absent human stays outside), no question-queue machinery (batching rides the existing ask law); success measure [default]: he steps away on a spoken window and on return finds the range was honest and the needed-again line waiting) | Done when: the narration law carries the offline-window sentence in one home — MET (communicator heartbeat tooth; SPEC INV-35 the spec-side home, as with the teeth); suite covers the shipped clause — MET (`test_offline_window`, M-138); the first real "можешь отойти" line spoken in a live session — MET by deed the same landing (session 21, minutes before the close: «остались записи и коммит — можешь отойти минуты на три-четыре») |
| 139 | **Working narration accounts for the time — stations named, stage-ends digested** (Alexander 2026-07-06 ~20:23, session 19: «ты отчитываешься, но в конце… мне надо понимать чем сейчас (то есть в каждый момент) ты занимаешься… когда архитектуру сделал — дай выжимку… когда тесты сделал — дай выжимку… а то заход на полчаса-час и непонятно, на что реально ушло время и токены»; the narration family's third ask — the landing reports live [INV-28/34], the mid-work trail still thin): three teeth onto the narration law — IDENTITY: every beat names which wish is in hand and which pipeline station it stands at; DIGEST: a station's completion is a beat BY LAW, its line digesting what the station produced (spec → what the delta promises · architecture → the shape and what changed structurally · tests → what is now covered · code → what now works); HEARTBEAT: a long grind gets a line naming what is grinding — so the chat trail itself accounts for where the session's time went | small | **landed 2026-07-06 ~20:38, session 19** (door: feature; kind: skill; prover CROSS-LINK `docs/prover/2026-07-06-row139.md` — 3 findings, all folded in-pass: the worker-closed-station voice (a lane-B station closing silently — now the senior's beat when the result lands), the ~10-minute heartbeat threshold [default], the outside-pipeline identity wording; architecture + its prove stood down by name — no structure change, INV-35 stays owned by the communicator node; facet sweep + design-sync stood down by name — skill kind, no visible surface; homes: SPEC INV-35 three teeth + communicator rule 13 sub-points + M-124 re-derived with the never side; `test_narration_three_teeth` RED-proven on the communicator side first, then green, suite whole; fences held: INV-27 echo untouched · INV-28 digests never speak in counters · INV-31 narration asks nothing · INV-34 walk scope unchanged · row 138's offline-window territory fenced by name in both homes; non-goals: no mechanical fence for chat (nothing to hook — the clock law's chat-face boundary), no per-stage token reporting (bookkeeping law); success measure [default]: a live session's trail read top-to-bottom answers «на что ушло время» without a token counter; queued 2026-07-06 ~20:30, session 19; map: changes working narration (INV-35, communicator rule 13)) | Done when: the narration law carries the three teeth in one home (SPEC INV-35 + communicator rule 13) — MET; suite covers the shipped clause — MET; a live session's trail read top-to-bottom answers «на что ушло время» without a token counter — MET by deed on this very session's own trail (each station of this row was narrated with its digest as it passed) |
| 140 | **Budget heuristics — what the method sheds when money or time run short** (Alexander 2026-07-06 ~20:23, session 19: «сейчас я на крутом плане, деньги не заканчиваются, а если бы заканчивались (ну и время тоже не резиновое) — что бы мы делали? может тесты гоняли селективно или что-то ещё экономили; нужны эвристики»): today the pack runs full rigor always (full suite before push, prover at every gate, senior model on judgment); the wish: a named ECONOMY ladder — which savings are legal at which pressure (selective test runs scoped by touched node, CROSS-LINK instead of FULL prover, cheaper worker tiers, batched landings, narration unchanged — it is cheap) and which rungs NEVER bend (the door law and its tripwires, red-before-fix, the human's gates, the landing report) — so a tight budget is a setting on the ladder, chosen deliberately rather than improvised | small | **build legs MET 2026-07-06 ~21:26, session 20; field leg OPEN; AMENDED ~21:44 on his word: the rung is asked — or the standing default told — at a project's setup (founding/adoption) alongside the kind question, so the preference is his from day one (SPEC v0.15.36 T-19, base 0.1.19, ADOPT orient line, M-134)** (door: feature; kind: skill; map: NEW feature in the method's rulebook — the SPEC's economy-ladder section, owned by the base-rulebook node; prover CROSS-LINK `docs/prover/2026-07-06-row140.md` — F1 batched gates never license a stale-HEAD push (M-6 unbendable, folded in the delta), F2 a deferred FULL prover pass is a dated debt line the milestone walk re-surfaces, F3 batch-end red bisects by landing order, F4 an explicit host line outlives any rung; homes: SPEC v0.15.35 T-19+INV-40 (the section "When money or time run short") + base 0.1.18 settings row `budget.pressure` + M-134/M-135 + `test_economy_ladder` red-proven on the base then green, suite 130; facet sweep: no visible surface — facets N/A by kind; non-goals: no numeric budgets/token meters, no automatic rung-switching; success measure [default]: the first budget-named session names its rung and sheds aloud, checked by his read) | Done when: the economy ladder lives in one home with its never-bend list — MET; suite covers the shipped clause — MET; applied once for real — a budget-named session names its cuts aloud — OPEN (awaits the first session he names a rung for) |
| 141 | **The chat laws get a mechanical voice in every window** (Alexander 2026-07-06 ~20:41, session 19, on tlvphoto pasting «новый раздел EX-SHARE… перед надгробием стены» into his chat: «ты вообще что-то можешь сделать с коммуникацией??»; the day's audit found the same class twice more — raw plan codes in two other windows): the language law (INV-28) and the narration law (INV-35) live in skills a session may never load — so the same prompt-hook mechanism as the clock (row 134) injects ONE reminder line into every prompt on the machine: plain words do the talking, codes only trail; beats name wish+station, station ends digest, silence past ~10 min owes a heartbeat; the skills stay the laws' homes — the hook reminds, never legislates | small | **build leg MET 2026-07-06 ~20:49, session 19; field legs OPEN** (door: feature; kind: infra — harness tooling; prover CROSS-LINK `docs/prover/2026-07-06-row141.md` — F1 the two-texts-one-law freshness seam FOLDED mechanically (M-133 pins the reminder's teeth as suite strings, a law rename goes red here), F2 notes the mechanism reaches exactly the windows the audit showed failing (hooks fire regardless of what a session loaded); homes: SPEC v0.15.33 INV-28 delivery sentence + M-133 + `scripts/chat-law-hook.sh` + the shared installer `scripts/install-session-hooks.sh` (covers rows 134+141, one home for the machine-install fact); 3 tests RED-proven on the absent scripts then green; facet sweep + design-sync stood down by name — infra, no visible surface; non-goals: no per-project hook variants (one machine, one line), no enforcement beyond reminding (chat still has no suite — the honest boundary stands); success measure [default]: the next tlvphoto session's chat carries no leading codes — the field is the test; INSTALL leg: awaits Alexander's one `!` command, same classifier road as row 134; map: changes the language + narration laws' DELIVERY (INV-28/INV-35 family)) | Done when: the reminder script exists and its line carries both laws' teeth — MET; one installer covers clock + laws — MET; suite covers the shipped strings — MET; installed on this machine by his hand — OPEN; the field evidence (a following tlvphoto session with no leading codes) — OPEN |
| 143 | **The architecture step owes measurable quality criteria** (Alexander 2026-07-06 ~21:03, session 20, on tlvphoto's Room first image loading long with no loader and no timing numbers — his question: what happened, did the architect not set criteria? how was measuring timings never even planned? if so the written architecture is poor): today the architecture step pins nodes and seams but demands NO measurable non-functional criteria, so a user-facing slowness is found only when the human notices it, ahead of any check; the wish: the architecture step and the spec's performance facet must produce measurable budgets (e.g. first image ≤ N s cold) plus a timing-instrumentation home for every user-facing surface, and the story's acceptance asserts the budgets; tlvphoto's own half rides its inbox wish (filed 2026-07-06 ~21:04) | small | **build legs MET 2026-07-06 ~21:44, session 20; field leg OPEN** (door: feature; kind: skill; map: changes the architecture step + spec-author's performance facet; homes: SPEC v0.15.36 INV-41 ("The architecture owes numbers, not only names", binds from a surface's next landing, never retroactively) + build-pipeline 0.2.28 step 3 + spec-author 0.1.16 performance facet + M-136 + `test_architecture_owes_budgets` red-proven then green; prover CROSS-LINK `docs/prover/2026-07-06-rows143-144.md` F1 retroactivity FOLDED; field evidence already breathing: the gallery host landed budgets + a timings export in its own window the same evening, before this clause — the clause makes it law; AMENDED ~21:53 on his word — budgets are NOT frontend-only: the project's KIND proposes what is measurable (backend: latency/throughput/errors; CLI/pipeline: run time, per-unit cost; skill pack: eval pass rate, suite time; prose: what honestly has a number), a no-honest-number quality said by name, never a vanity metric (SPEC v0.15.37, build-pipeline 0.2.29, M-136)) | Done when: build-pipeline's architecture step + spec-author's facet sweep name the budget-and-instrumentation duty; suite covers the shipped clause; first real use — one host's architecture doc carries asserted budgets |
| 144 | **The task list on his screen speaks plain words** (Alexander 2026-07-06 ~21:22, session 20, sending a screenshot of the session's task list: a thing like this strongly helps to see what is happening — if only it communicated in understandable words; the visible list read "Row 142: prove (CROSS-LINK), matrix M-022/M-129, tests red→green"): the harness task list is a human-facing surface the language law already claims (INV-28) but no rule NAMES it — task subjects and spinner lines must speak plain product words in the chat language, codes only trailing; applied live the same minute (the open tasks renamed), the rule still needs its home in communicator's board family so it survives sessions | small | **build legs MET 2026-07-06 ~21:44, session 20; field leg OPEN** (door: feature; kind: skill; map: the language law's delivery — board family; HIS CORRECTION ~21:33 folded: task subjects speak plain ENGLISH — the docs language, the list is a work artifact with its own register, apart from chat — the session's first Russian renaming was wrong; homes: communicator 0.1.28 rule 6 task-list bullet + M-137 + `test_task_list_plain_words` red-proven then green; prover record `2026-07-06-rows143-144.md` F2) | Done when: communicator names the task list among the language law's surfaces; suite covers the shipped sentence; a following session's task list reads plain by his eye |
| 145 | **His word is read as meant — cuts stay cut, sarcasm is not instruction** (re-scoped 2026-07-06 ~22:20 on Alexander's word: the confident-specialist VOICE core belongs to the promoter's own voice skill — «это он перестарался»; the pack keeps only the general elements; two of the six bans already live here — no empty drama = the no-disclaimers rule, no approval-begging = silence-is-consent; the promoter cross-links, never duplicates): ONE story, two acceptance clauses — (a) a phrasing the human killed in a review round stays killed in every later draft of that artifact (the kill-list); (b) a vivid phrase of his is adopted only as meant — mockery of a bad draft is not guidance; unsure ⇒ ask; original wish: the promoter window's inbox file (committed f59419d, since removed from inbox by its own window — git history is the record) | small | **landed 2026-07-06 ~22:26, session 21** (door: feature; kind: skill; queued ~22:13, re-scoped ~22:20 on his word; map: a NEW rule in communicator — reading the human's word on shown artifacts; prover CROSS-LINK `docs/prover/2026-07-06-row145.md` — 1 finding folded in-pass (the kill-list got a HOME: the artifact's project records, never only session memory — a wipe must not resurrect a cut); architecture: owns-column extended, no structure change — prove stood down by name; facet sweep + design-sync stood down by name — skill kind, no visible surface; homes: SPEC v0.15.39 INV-42 + communicator 0.1.31 rule 15 (now fifteen rules); M-139; `test_his_word_read_right` RED-proven, then green, suite whole; non-goals: the confident-specialist voice core NOT taken — it is the promoter's craft, its skill references the pack's two existing bans instead of duplicating; no mechanical fence — review rounds live in chat and prose, the clause is law + kill-list record; success measure [default]: a killed phrasing never resurfaces across drafts and wipes, and no parody of his is ever shipped earnestly) | Done when: communicator carries both clauses — MET (rule 15); SPEC backs them — MET (INV-42); suite covers the shipped clause — MET (`test_his_word_read_right`, M-139); verdict on the voice core recorded for the promoter window to read — MET (this row + the prover record carry it; the promoter window reads the public repo) |
| 148 | **The pack's spec reads like its craft wrote it** (Alexander 2026-07-06 ~23:24: «сделать спеки читабельными, как будто их человек писал или хотя бы правил — продакт, архитект и так далее; пока что спеки для тебя; в конец очереди, или раньше если несложно»): the human-first SHAPE law already binds (spec-author's "How it reads"; the craft ladder INV-33) — this row makes the pack's own SPEC.md walk it BY DEED: a readability rewrite, section by section, in each section's craft voice (scenarios talk like a person, sentences breathe, codes stay trailing), the anchor-set guard held (identical anchor sets before/after, any delta named), FULL prover re-prove after the reshape; **entry: mockup-first** (INV-43's door arm, declared at intake) — a rendered before/after sample of TWO sections goes to his morning eye FIRST, his taste is the bar, the whole-doc rewrite runs only after his word on the sample; **sample rounds 1–2 KILLED; round 3 STRUCTURE APPROVED, language killed** (round 2 killed ~07:59: rule-book genre out, bar = USE-CASE genre per BMAD+Kiro; round 3 ~08:11: genre stands, three language kills — scissors frame banned FOREVER globally (profile law `language.no-scissors`), overcompressed narrative, non-native English (`language.native-english`); round 4 = plain-English rewrite of the same page, in place; **round 4 ANSWERED ~09:34 (JSON archived): genre+language APPROVED with three adjustments — pre/postconditions stated where they exist · an EPIC grouping proposed · a SERIOUS VALIDATION with a thorough plan BEFORE the whole-doc rewrite (the genre must not break the prover, the anchors, or the mechanical checks — his «крайне важно, проверь немедленно»); criteria voice = plain sentences, the WHEN/THEN/SHALL caps KILLED («псевдодраматичность»)**; kill-list on the decisions page) | surface | queued 2026-07-06 ~23:25, session 22 (kind: prose; door: feature; map: the pack's own SPEC.md as a READABLE document — package-docs node; kin: spec-author's human-first law, INV-33, his standing «spec too jargony» word) | Done when: the sample rendered and his word taken on it; the full pass lands with anchor sets identical (or every delta named); FULL prover re-prove green; his eye says a section reads human |
| 149 | **Lanes are picked by a dependency graph, integration declared up front** (Alexander 2026-07-06 ~23:33: «научись параллельно работать несвязанные фичи, 2–3; или те, которые можно связать в конце; не просто параллельно — граф зависимостей: лейны, потом интеграции; это надо прописать»): the lane law (T-18/INV-39) grants up to three independent lanes but never said HOW a session PICKS them — the law to write: at queue-take the session reads the runnable head (next K without-him rows), builds the mini dependency graph (an edge wherever two rows share a surface, a spec section, a skill file, or a doc region), opens lanes on a pairwise-independent set up to the cap; rows joined only at their INTEGRATION (same file, different concerns) may pre-roll isolated build stages with the integration ORDER DECLARED at claim; the graph and lane set are NARRATED at opening, the departures board names whom a waiting row waits behind | small | **landed 2026-07-07 ~00:09, session 22** (door: feature; kind: skill; claimed ~00:00, one serial lane by the graph's own word — the law's tiny-rows clause applied to its own landing; prover `docs/prover/2026-07-07-rows149-152.md` — F1 folded (the cap counts LANES, one lane's serial rows ride together), F2 recorded ("tiny" stays the senior's said-aloud call, a numeric threshold would be a vanity metric); walked by DEED before the law landed: three medium rows as lanes ~23:39, five tiny rows serial after; architecture: assignment only — INV-49 → build-pipeline; facets N/A; non-goals: no automatic independence checker (senior judges, says aloud), no per-lane sub-boards; success measure [default]: every multi-lane night names its graph and no cross-lane clobber ever lands; delegation: shared worker for the four rows, ~20 min saved) — SPEC v0.15.44 INV-49 · M-147 · `test_lanes_by_graph` RED PROVEN then green · build-pipeline 0.2.34; suite covers; one real multi-lane night names its graph aloud |
| 150 | **The prover gains the entry-symmetry lens — faces, not just states** (inbox/2026-07-05-prover-loop-lens.md; tlvphoto door incident: CROSS-LINK found six seams and MISSED the one-way face Alexander caught in one read — «всегда в автомате должен быть луп: если есть гет, есть сет»; the dead-end lens tests STATES for exits, but door/gallery were FACES of one surface, and the asymmetry lived in face reachability over the visit's lifetime): add to product-prover's stress lenses — for every face, mode, or panel entered under a CONDITION (first visit, empty state, onboarding, a one-time banner): what deliberate path re-enters it later? A get with no set is a finding unless the spec states the one-way as a decision; trigger patterns: "only on first visit / first run / until dismissed" | small | **landed 2026-07-07 ~00:09, session 22** (door: feature; kind: skill; claimed ~00:00; prover record shared — F1 folded (SPEC owns the LAW, the prover lens cites it, spec-author's journey clause points at it — one home, two wirings); architecture: INV-50 → spec-author, wiring notes on product-prover; facets N/A; non-goals: no retroactive sweep of shipped specs (binds forward, INV-21 kin); success measure [default]: the next one-way face in any spec is caught at prove or named as a decision; delegation: shared worker; the worker's mid-run matrix-block find was the SENIOR's misplacement, moved to the owning block same hour) — SPEC v0.15.44 INV-50 · M-148 · `test_entry_symmetry` RED PROVEN then green · product-prover 0.1.12 · spec-author 0.1.18 · inbox wish archived (docs/queue-archive/2026-07-05-prover-loop-lens.md); the tlvphoto case re-told as its red test; suite covers the shipped text; inbox file archived with a pointer |
| 151 | **Anything handed to the human opens with its passport** (Alexander 2026-07-06 ~23:34, twice, on a window opening unannounced: «если рапорт — скажи, нужен ли мой инпут; если не нужен — напиши что просто апдейт» + «всегда пиши имя проекта, не только в URL»): every artifact handed or opened to him — rendered page, report, decision page — LEADS with a one-line passport: the PROJECT NAME in the visible content (never only the URL/path) and the read contract — NEEDS YOUR WORD (what + by when) or JUST AN UPDATE (no action needed); the chat line announcing an opened window carries the same two facts; voiced instance fixed the same minute on the live decisions page | small | **landed 2026-07-07 ~00:09, session 22** (door: feature; kind: skill; claimed ~00:00; prover record shared — the three-rules collision folded by naming precedence (INV-52 = WHEN, show rule = HOW, passport = WHAT); voiced instance fixed live ~23:35 on the decisions page (its passport header), the law generalizes it; facets N/A; non-goals: no retroactive re-render of old artifacts; success measure [default]: he never again writes «в браузере открылось что-то» — every page names itself and its ask; delegation: shared worker) — SPEC v0.15.44 INV-51 · M-149 · `test_artifact_passport` RED PROVEN then green · communicator 0.1.32 (rule 16, seventeen rules) + SPEC backing; suite covers; the next handed artifact carries it by deed |
| 152 | **Windows accumulate to one end-of-stretch opening** (Alexander 2026-07-06 ~23:37: «я бы всё-всё-всё открыл бы в конце, ну или если переоткрывать — аккумулировать»): during a stretch he stepped away from (an overnight loop, a long offline window), nothing OPENS a browser window mid-stretch — artifacts accumulate on ONE page (the stretch's decisions/report page), and the stretch's END opens that one window once; re-opening mid-stretch is legal only as the SAME accumulated page refreshed, never a new window per artifact; composes with the offline-window narration (INV-35) and the passport (row 151) | small | **landed 2026-07-07 ~00:09, session 22** (door: feature; kind: skill; claimed ~00:00; prover record shared — F2 folded (the away-stretch trigger IS the offline window, INV-35; outside it the normal show rule stands); walked by DEED since ~23:37 (no new windows, the decisions tab the accumulator); facets N/A; non-goals: no queueing mechanism beyond the one page; success measure [default]: an overnight stretch ends with exactly one opening; delegation: shared worker) — SPEC v0.15.44 INV-52 · M-150 · `test_windows_accumulate` RED PROVEN then green · communicator 0.1.32 (rule 17) + SPEC backing; suite covers; tonight's loop walks it by deed (no new windows till the morning open) |
| 154 | **The stretch's end is unmissable** (Alexander 2026-07-07 ~00:53: «закончил вообще непонятно, без ничего, без сообщения» — the 00:38 cumulative report existed but drowned above tool noise; delivery is the law — mere existence does not count): when a work stretch ends (a loop iteration going to sleep, an away-stretch closing, a session ending), the LAST rendered thing is a SHORT final line — what closed · what's next · what's needed from him · when the agent wakes — kept short, clear of any wall of text above machinery; the long report lives ABOVE, the final line comes LAST; if a page is the deliverable, its passport line repeats in that final line | small | **landed 2026-07-07 ~00:57, session 22** (door: feature; kind: skill; claimed ~00:54; map: a communicator rule beside the passport and the offline window; prover folded inline — the final line is a CHAT shape, no collision with INV-52's one-opening law (the line stays plain text, appearing inline rather than as a window) nor with INV-28's outcome-leads (the line IS an outcome digest); facets N/A; non-goals: no fixed template beyond the four beats; success measure [default]: he never again reads a stretch's end as silence; delegation: kept on senior — one rule + one test) — SPEC v0.15.48 INV-57 · M-156 · `test_stretch_end_unmissable` RED PROVEN then green · communicator 0.1.33 (eighteen rules) · pack 0.8.60 · suite 157 green | Done when: the final-line rule in communicator (one home) + SPEC backing; suite covers; tonight's own stretch-ends walk it by deed |
| 155 | **Process cost scales to the delta** (Alexander 2026-07-07 ~00:58: «каждая итерация фичи очень длинная — понять почему; если необходимо — ок, если нет — что можно сделать, не принося качество в жертву»; the senior's own audit of tonight's 18 landings): a TINY row pays the same fixed bookkeeping as a surface — claim commit, own pushgate record, own journal entry, resume rewrite — ~40% of its wall time; the safety net (red-first test, delta prover, the gates) is NOT the cost driver and never bends; the fix is the reach idea applied to PROCESS: records and bookkeeping scale to the delta — a tiny skill-kind row rides a SHORT-form pushgate record (three lines), claims batch per declared lane (already tonight's practice), one journal entry and one resume rewrite per batch (already practice) — codify what the night already proved and name what stays irreducible (writing the law well IS the quality) | small | **landed 2026-07-07 ~01:27, session 22** (door: feature; kind: skill; claimed ~01:25; prover folded inline — the short form scopes by KIND+SHAPE (small skill/prose/infra, no new surface/structure), never by mood, and the rigor triad (previous · delta · verdict) survives in both forms; host profile line reconciled in the same landing; RED proven against HEAD (order slip owned — same shape as row 153's); facets N/A; success measure [default]: the next tiny landing's bookkeeping demonstrably thinner with the net whole — THIS landing is the first proof (its own record is the short form); delegation: kept on senior) — SPEC v0.15.50 INV-61 · M-160 · `test_process_cost_scales` · build-pipeline 0.2.36 · pack 0.8.62 · suite 159 green (queued ~00:59 (kind: skill; door: feature; map: pipeline + prover cadence; kin: INV-45 reach, T-19 economy, the host pushgate profile line) | Done when: the short-form record shape + batch bookkeeping spec'd (one home), the host profile line reconciled; suite covers; the next tiny landing demonstrably cheaper with the net intact |
| 156 | **The smallest sample is judged before the full artifact** (promoter audit: FIVE full media-packs died on the same tone failure a one-paragraph sample would have caught — promoter-voice now carries it; the pack generalizes): for a taste-heavy deliverable (voice, copy, visual style, spec prose), the build STOPS at the cheapest judgeable sample — one paragraph, one card, two sections — and the human's word on the sample sets the bar before the full build spends anything; kin: the prototype-norm door arm (INV-43) is the human-side "show first", this is the agent-side "build smallest first"; row 148's two-section sample walked it tonight by instinct | small | **landed 2026-07-07 ~01:29, session 22** (door: feature; kind: skill; claimed ~01:28, pipeline lane; prover folded inline — the kin split with the human-side mockup-first (INV-43) stated in the law itself; facets N/A; success measure [default]: no full taste artifact ever built before its sample is judged; delegation: kept on senior) — SPEC v0.15.51 INV-62 · M-161 · red proven vs HEAD · build-pipeline 0.2.37 (queued ~00:59 (kind: skill; door: feature; map: build-pipeline code step + communicator; source: promoter case, promoter/JOURNAL.md:349-371) | Done when: the smallest-sample clause in its one home + SPEC backing; suite covers; the next taste-heavy build stops at a sample by deed |
| 157 | **A rejected artifact reopens its SOURCE, never line-patches** (promoter audit: the five-round trap — each round patched the rejected OUTPUT against an unchanged card, so the same failure re-emerged; promoter-voice 0.1.2 named it; the pack generalizes): when the human rejects an artifact, the fix starts at the artifact's SOURCE (the spec clause, the card, the brief) — the source is corrected first, the artifact rebuilt from it; patching rejected output line-by-line against an unchanged source is the trap, named and banned | small | **landed 2026-07-07 ~01:29, session 22** (door: feature; kind: skill; claimed ~01:28, same lane; prover folded inline — the trap named by its incident (five rounds, unchanged card); composes with INV-58: frozen APPROVED text vs reopened REJECTED source, two ends of one review; facets N/A; success measure [default]: no rejected artifact ever line-patched; delegation: kept on senior) — SPEC v0.15.51 INV-63 · M-162 · shared test · build-pipeline 0.2.37 · pack 0.8.63 · suite 160 green (queued ~00:59 (kind: skill; door: feature; map: pipeline verify/rework + spec-author; source: promoter/JOURNAL.md:349-371) | Done when: the reopen-the-source clause in one home + SPEC backing; suite covers |
| 158 | **Approved text is frozen — a revision applies only the named correction** (promoter audit: a v1.1 opener REWROTE an approved sample instead of trimming as asked, and the rewrite introduced a fresh banned pattern the approved wording never had; instance law since 2026-07-07, the pack generalizes as the kill-list law's third arm): once the human approved a text, it is FROZEN — a later revision applies exactly the correction he named, never a fresh rewrite around it; churn of approved material is a defect, kin of a resurfaced cut (INV-42) | small | **landed 2026-07-07 ~01:03, session 22** (door: feature; kind: skill; claimed ~01:00, serial communicator lane; prover folded inline — kin-boundary named: INV-42 guards CUT phrasings, INV-58 guards APPROVED ones, two sides of one settled-material family; facets N/A; success measure [default]: no approved wording ever churns; delegation: kept on senior — one rule block + shared test) — SPEC v0.15.49 INV-58 · M-157 · `test_promoter_harvest_trio` RED PROVEN then green · communicator 0.1.34 (queued ~00:59 (kind: skill; door: feature; map: communicator rule 15's family; source: promoter-alexander/JOURNAL.md:3-18) | Done when: the frozen-approved-text arm beside INV-42 (one home) + suite covers |
| 159 | **The kill-list goes mechanical** (promoter audit: the scissors returned INTO THE HEADLINE after the ban was «written on the forehead» — only an executable scanner ended it; the pack's INV-42 has the law but no teeth and no template): the pack ships a kill-list TEMPLATE (dated literals, never removed) + guardrail guidance — a killed literal reappearing in the artifact's surfaces turns a check red; hosts with taste-reviewed artifacts adopt it like the ledger | small | **landed 2026-07-07 ~01:34, session 22** (door: feature; kind: infra; claimed ~01:32; prover folded inline — the law stays INV-42's, the template+scanner are its TEETH (one law, one mechanism, two homes by role); no pack-side scanner test (the pack has no taste-reviewed artifact — hosts wire theirs; honest boundary named); facets N/A; success measure [default]: the next host with review rounds adopts the template and a cut never resurfaces; delegation: kept on senior) — SPEC v0.15.53 E-26 · M-164 · red vs HEAD (template absent there) · templates/KILL_LIST.template.md + guardrails README scanner note · inventory entry · pack 0.8.65 · suite 162 green (queued ~00:59 (kind: infra; door: feature; map: templates + guardrails guidance; source: promoter-alexander/artifacts/track-coach/kill-list.md + tests/test_kill_list.py — the working instance) | Done when: template + guidance shipped (one home each); suite covers the shape; INV-42 cross-links the mechanism |
| 160 | **No question is asked twice — recorded answers are searched first, dialogues converge** (promoter audit, his escalation in one hour: «на кучу похожих вопросов уже давали ответы» → «продебагируй скилл» → «сделай так, чтобы диалоги сходились»; promoter-base rules 16-17 carry it, the pack generalizes): before ANY ask, grep the recorded word — decision archives, review records, the journal, the profile; a question a record already answers is a defect — the record already settles it; and dialogues CONVERGE — an answered question closes forever and is harvested the same session, a named problem returns SOLVED with evidence, round N+1 carries only new material | small | **landed 2026-07-07 ~01:03, session 22** (door: feature; kind: skill; claimed ~01:00, serial lane; prover folded inline — the search-first list names its sources (archives, records, journal, profile) so the grep is walkable, and convergence composes with silence-is-consent (INV-31): a taken default is not an open question; facets N/A; success measure [default]: he never repeats an answer; delegation: kept on senior) — SPEC v0.15.49 INV-59 · M-158 · `test_promoter_harvest_trio` red→green · communicator 0.1.34 (rule 20) (queued ~00:59 (kind: skill; door: feature; map: communicator ask rules + base; source: promoter/JOURNAL.md:12-32) | Done when: the search-first + converge clauses in their one home + SPEC backing; suite covers |
| 161 | **Every claim shown carries its source; review surfaces are commentable** (promoter audit: «я не знаю, откуда ты всё это взял» — promoter-base rule 10 ships per-claim SOURCE tags (artifact / his-word / MY INFERENCE, inferences loudest) + commentable line-by-line review pages; his standing cross-project word since 2026-07-06 — kept open for comment, clear of any read-only wall): the pack adopts both as communicator law for anything shown for REVIEW: per-claim provenance chips and a commentable surface with answer capture (the decision page's JSON law extends to review pages) | small | **landed 2026-07-07 ~01:31, session 22** (door: feature; kind: skill; claimed ~01:30; prover folded inline — the provenance triad names its three sources so a chip is never judgment; the commentable law reuses rule 10's JSON round-trip, one home, no second mechanism; facets: the review page inherits the decision page's medium facets, N/A beyond it; success measure [default]: he never again asks where a claim came from; delegation: kept on senior) — SPEC v0.15.52 INV-64 · M-163 · red proven vs HEAD · communicator 0.1.35 (rule 22, twenty-two rules) · pack 0.8.64 · suite 161 green (queued ~00:59 (kind: skill; door: feature; map: communicator show rules; source: promoter-base/SKILL.md:94-111 + his memory-standing word) | Done when: the provenance + commentable clauses in communicator (one home) + SPEC backing; suite covers; the next review page walks it by deed |
| 162 | **A taste ask arrives carrying the agent's own researched proposal** (promoter audit: «тон сначала ВЫБРАТЬ — сам нашёл, предложил, потом показывай» — bank-first; the pack's ask-with-recommendation exists, this sharpens it for taste calls): a genuine taste question never arrives empty — the agent mines the material first (exemplars, precedents, real options with citations) and asks with a chosen recommendation and its evidence; asking the human to supply what the agent should have mined is a defect | small | **landed 2026-07-07 ~01:03, session 22** (door: feature; kind: skill; claimed ~01:00, serial lane; prover folded inline — sharpens INV-4, never replaces it: the recommendation law already exists, this binds the MINING for taste calls; facets N/A; success measure [default]: no empty-handed taste question ever reaches him; delegation: kept on senior) — SPEC v0.15.49 INV-60 · M-159 · `test_promoter_harvest_trio` red→green · communicator 0.1.34 (rule 21, twenty-one rules) (queued ~00:59 (kind: skill; door: feature; map: communicator ask rules; kin: INV-4's recommended option; source: promoter-voice/SKILL.md:169-171) | Done when: the mined-proposal clause beside the batched-questions law (one home); suite covers |
| 163 | **test-author skill — the test methodology extracted into the pack** (inbox wish from the track-coach close, 2026-07-05 late night, born of his direct question whether tests-by-the-method was truly done; the NOT-done half): the method that rebuilt track-coach's suite — matrix organized architecture-node × spec-fact, a pinned assertion-level ladder per fact, real-artifact assertions over string checks, red-first, pinned skip-set, traceability enforcement — lives only in track-coach's TEST_MATRIX header prose, its overhaul inventories, and two build-pipeline paragraphs (steps 4–5); extract it into a **test-author skill** invoked by build-pipeline at steps 4–5 the way steps 1–2 invoke spec-author and product-prover; donor material named in the wish: cross-audit F11 charter sketch, track-coach TEST_MATRIX.md as worked exemplar, s34/s52 inventories as the classification scheme, test_traceability.py as the enforcement pattern; field urgency dated 2026-07-02: two user-visible bugs slipped past ~660 string-only tests — the exact class the ladder prevents | surface | queued 2026-07-07 ~08:19, session 23 (door: feature; kind: skill — a NEW seventh pack skill; map: NEW node beside spec-author/product-prover, wired from build-pipeline steps 4–5; source: inbox/2026-07-05-test-author-extraction.md, archived to docs/queue-archive/ at intake; walks skill-creator at birth per the row-130 clause) | Done when: the skill authored + versioned + loadability-gated; build-pipeline steps 4–5 invoke it by name (one home for the method, pipeline cites); SPEC + matrix + suite cover the wiring; skill-creator walk at birth recorded; first real use on a host recorded |
| 165 | **Skill discovery — search for applicable skills at setup and at every struggle** (Alexander 2026-07-07 ~08:28, from the promoter-window audit: five review rounds died on scissors/voice failures that a PUBLIC skill — stop-slop, shipped months earlier — already carried as a written checklist («Any "not X, it's Y" contrasts? State Y directly»); his word: «надо сделать поиск скиллов для проекта особенно если страггл какой… это возможно можно в лайвспекбейз прописать… или это не только про спек»): the pack gains a skill-search move with two triggers — (a) at project SETUP (founding / adoption, beside the founding questions): scan installed and available skills for ones matching the project's kind and its crafts, propose the fit list with a recommendation; (b) on STRUGGLE — a problem-ledger entry reaching its second occurrence, a taste artifact rejected twice, any recurring failure family: before the next attempt, search whether an existing skill or checklist already owns this failure class, and adopt or reject it by name (the promoter's stop-slop case is the type specimen); adoption practice rides the row (his 08:32 ask on how to borrow without stealing): prefer INVOKING a found skill as-is; fold its lessons by paraphrase plus a named credit line; verbatim text only under its license (MIT-class, with the notice kept) — the pack never republishes unlicensed text | surface | **build legs LANDED 2026-07-07 ~09:38, session 23; field leg OPEN — first real struggle run** (door: feature; kind: skill; map: base-rulebook, INV-65 → base-rulebook; prover CROSS-LINK docs/prover/2026-07-07-row165.md — 5 findings, 4 folded (kin-not-duplicate of the researched-proposal law · the human's word picks · reachable-catalogs honesty · borrowing practice closes the clause), 1 recorded (open field leg); delegation: the whole mechanical bundle on a Sonnet worker, zero deviations, ~12 min senior saved; SPEC v0.15.55 INV-65 · base 0.1.22 rule 20 · ADOPT setup line · M-166 · `test_skill_discovery` red-proven then green · pack 0.8.67 · suite 164 green · installed copies synced) | Done when: the trigger pair + the search's form live in one home, the setup arm rides ADOPT/bootstrap beside the founding questions, the struggle arm cites the problem ledger's second-occurrence law; suite covers the shipped clauses; first real run recorded — a struggling session finds and adopts or rejects a skill by name |
| 166 | **The live work board — what the agent is doing, visible at every moment** (Alexander 2026-07-07 ~09:36, his out-of-turn word: «походу отчитывания плохие. я хотел бы чтобы было понятно чем вообще занимается агент в каждый момент времени и в идеале весь процесс всегда. можешь такое сделать внеочередным?»): a standing visible surface that always shows the CURRENT picture — which task is in hand and at which pipeline station, which workers run with which briefs, which lanes roll or wait, the last landing, the queue head, and what waits on the human — the departures board (INV-27) shows in-flight wishes on ask; this shows the WHOLE working process, always, updated at every station change | surface | queued 2026-07-07 ~09:38, session 23, PRIORITY: his out-of-turn word — next in hand (door: feature; kind: product — a user-facing surface; map: NEW surface beside the departures board, communicator's domain; **entry: mockup-first** (visible surface, the prototype-norm law) — a labelled prototype on REAL current data goes to his eye before the spec delta); **mockup round 1 KILLED ~09:44** («кейвмен бредогенератор»: coined workshop words on the board; the bar — the board speaks FEATURE language: each feature by its plain name + its stage, wording drawn from the spec's own genre, zero internal coinages; his diagnosis recorded: the communication struggle feeds on auto-generated text in context); round 2 live (feature-language cards, page self-refreshes) | Done when: the board's form + update moments spec'd (one home); the mockup approved and frozen as the norm; the surface ships with its registry row and rendered-level matrix rows; a session updates it at every station change, checked by his eye over one real stretch |
| 168 | **The showing channel adapts to the session's seat** (Alexander 2026-07-07 ~10:57: «если ведем сессию локально это хтмл, а если ты бежишь на удаленке — тогда это выходит в клод в браузере, и тогда не хтмл а другой формат; можешь это распознавать и адаптироваться?»): a locally-seated session shows rendered artifacts as local HTML opened in a new browser window (today's law); a remotely-seated session (Claude Code in the cloud/browser) cannot open local windows — the same content goes through the remote seat's own channel: an artifact page the host renders in the human's browser, or the chat itself — same passport, same commentable round-trip, same kill-list discipline. The session DETECTS its seat from environment markers (platform, display, filesystem reach) and SAYS which channel it picked; a local file path handed to a remote reader is a defect of the exchange, exactly like an unopened window | surface | **build legs LANDED 2026-07-07 ~11:02, session 23; field leg OPEN — the first real remote-seat session** (door: feature; kind: skill; map: communicator rule 5 + a new SPEC clause beside the passport/window laws; prover folded inline per the small-delta law — the profile's show line stays the LOCAL arm (said in both homes, no second law), the remote channel inherits passport + round-trip by citation; INV-67 → communicator; delegation: kept on senior, smaller than a brief; SPEC v0.15.59 INV-67 · communicator 0.1.37 rule-5 seat paragraph · profile show-line marked local-arm (playbook pushed) · M-170 · `test_showing_seat` red-proven then green · pack 0.8.71 · suite 169 green · installed copy synced) | Done when: the seat rule in one home — MET; the profile's show line marked as the local arm — MET; first real remote-seat session shows through its channel and says so — OPEN (rides the first cloud-seated session) · Alexander re-raised 2026-07-08 ~09:07: does the session actually KNOW it is remote, and how do we communicate then? — the same open field leg, sharpened: seat-detection is spec'd (environment markers) but unverified in a real cloud seat, and today's session is local, so it cannot be proven from here |
| 170 | A mechanical pre-show lint at the presentation gate (communicator skill): before any text/doc is shown or rendered for a human, scan the human-facing text for BANNED constructs and refuse-with-flag so the author rewrites positively before the human sees it (a warning to clear, never a silent auto-rewrite). First pattern set: the scissors/negation-contrast family — a neighbour denied after a dash or a comma, the deny-then-assert shape, and the Russian negation-then-replacement forms — a GLOBAL PERMANENT profile ban (`language.no-scissors`) that keeps leaking (~7 leaks in two days, memory-alone isn't holding it), run over EN + RU, tuned against real text (the mechanical detector now ships in `scripts/spec-style-lint.py`, catching all four shapes). SIBLING pattern to fold in: coined doc-metaphor titles on human-facing pages (the board's «витрина/эталон/дожать» class, 2026-07-07). (inbox wish from the tlvphoto window, 2026-07-07, per the inbox exception) | surface | queued — OPEN, PRIORITY RAISED by his 2026-07-10 ~00:14 word (a jargon strip shipped on the onboarding mockup that evening — «это бред»; his read: the next user just says that and never comes back — so shown-artifact copy must pass the register gate MECHANICALLY, mockups and prototype surfaces included, before any window opens); the enforcement half of a stated language law; rides the docs pass / global pass | Done when: communicator's "before you show" walk carries the lint with a tunable pattern set (scissors first, coined-metaphor titles folded), red-proven on real leaking text, a warning the author clears; runs EN + RU |
| 171 | **The pack works for a host with no hooks and no GitHub** (Alexander 2026-07-08 ~09:07): today the pack leans on Claude Code session hooks (the chat clock, the chat-law reminder, the skill-search prompt) and on git/GitHub (the push gate, the CI mirror, directory submission). A host that has neither — no hook support, no remote, maybe no git at all — must still get the method whole: the pipeline, the living documents, the review round-trip. Work out which parts are the CORE method that is always on, and which are OPTIONAL machinery that degrades gracefully when its host support is absent; and make the pack SAY plainly what it turned off and why, never silently assume a hook fired or a remote exists | large | queued — OPEN; needs his word on priority (a generalization row, kin to the migration sub-skill D4 and adopt's VCS gate) | Done when: the pipeline runs end-to-end on a host with no session hooks and no GitHub (git optional), each disabled capability named with its degraded fallback, red-proven that the core method still lands a wish |
| 172 | **The agent sets tunable parameters itself and reports; push to prod on its own OK** (Alexander 2026-07-08, from his tlvphoto conversation, handed to the pack as a general rule — no switch to tlvphoto): move every task that can move and ask only genuine questions; where a task carries a parametrizable KNOB (his example: image resolution), pick a sensible value as seems okay, proceed, and report it after — at most the parameter is updated together later; and pushing to prod is fine whenever the work is okay to the agent | small | **LANDED 2026-07-08 ~10:30, session 26** (door: feature; kind: skill/prose; map: grows the taste-told scenario — INV-70 beside INV-31, no new node; carried to build-pipeline by assignment; walked spec → prove(cross-link) → matrix M-176 → test red-first → land) | Done when: SPEC states the parameter-default law with its [default]-tag + landing-report duty, a matrix row red-proven pins it, and the agent's push-on-own-certification is written as the human's granted trust — MET |
| 188 | **The architecture doc reads as an LLD reader expects** (Alexander 2026-07-09 ~23:31, his read of the derived tlvphoto doc: understandable, wrong format — he expects "here is the architecture, here are our tiers, here is how the flows walk those tiers, if X fails the fallback is Y, recommended technologies are Z", the way Kiro's design.md and BMAD's architecture template read): the doc OPENS with the shape at a glance — the tiers first; the runtime view carries a fallback column (a failure point without its "what happens then" is an unfinished sentence); the placement table is framed as THE tiers-and-technology table (BMAD's definitive tech-stack table's kin); the crosswalk names the BMAD/Kiro counterparts | small | **LANDED 2026-07-09 ~23:37, session 30** (door: feature; kind: skill; map: refines row 180's format — same homes; SPEC v0.16.11 (INV-74 gains the fallback duty, INV-75 the tiers-first reading order) + template's shape-at-a-glance section + If-it-fails column + own ARCHITECTURE.md models both + spec-author crosswalk names the BMAD/Kiro counterparts (0.1.24); prover CROSS-LINK `docs/prover/2026-07-09-row188.md`, F1-F3 folded; M-193 red-proven against HEAD, `test_lld_reading_order_and_fallbacks`; derivation v3 rendered + opened for his eye) | Done when: template + spec carry the reading order and the fallback column, red-proven — MET; own ARCHITECTURE.md models both — MET; the tlvphoto derivation re-rendered in the new shape and shown — MET |
| 189 | **Three field-norm pieces the architecture doc still owed** (his 2026-07-09 ~23:44 ask: check the sources yourself — BMAD, Kiro, arc42, C4, Google design-doc culture — adopt, never copy; research record in the session scratchpad, URLs in the prover record): a DECISIONS section — one pointer table to where this project's decisions live (queue rows, journal, open decision marks), the arc42 §9 / ADR content as an index, never a second home; the seam section names where each crossing's SCHEMA lives (the data-contracts notion of BMAD/Kiro); the placement view says where SECRETS live and which tier holds each verdict that must not be client-side (BMAD's secrets-management, Google's cross-cutting). Consciously NOT adopted, by name: coding-standards and monitoring-stack sections (our craft ladder + instrumentation homes carry them), full DB-schema catalogs (trade-offs, never full schemas — Google's own line) | small | **LANDED 2026-07-09 ~23:51, session 30** (door: feature; kind: skill; map: refines rows 180/188 — same homes; SPEC v0.16.12 (INV-75 gains the secrets duty) + template's Decisions/schema-home/secrets pieces + own doc models them; record: the row-189 addendum in `docs/prover/2026-07-09-row188.md` incl. the NOT-adopted list; M-194 red-proven against HEAD, `test_field_norm_pieces`; research: scratchpad `arch-doc-norms.md`, five primary sources) | Done when: template + own doc carry the three pieces, red-proven — MET; the not-adopted list is named in the prover record — MET |
| 190 | **The method proposes the engine/instance split and knows how to lead a split project** (Alexander 2026-07-09 late evening: propose separating engine from content BY DEFAULT — the same engine later hangs Bangkok photos or anything else; how do we run projects where the engine is a public repo and the content/infra private?): at founding/adoption, when the product is an instance of a reusable mechanism (his standing reuse answer), the kind question PROPOSES the split — engine repo (public, generic fixtures, content contract per INV-79) + instance repo/home (content, corrections, private fragments); the method states how the pack attaches to a PAIR (which repo carries which documents, one queue or two, where the wish door lives, how a lesson travels engine↔instance), and the placement view says where heavy binary content lives (a git repo of images is the wrong home — object storage / the machine's archive + backup, named per project) | surface | queued 2026-07-09 ~23:53 (door: feature; kind: skill; map: new scenario beside adoption/founding; kin: INV-79 content contract, project.kind law, the reuse profile line; tlvphoto's planned exhibition-engine split is the first real case — its own window executes, this window only teaches the pack) | Done when: founding/adoption carry the split proposal + the pair-attachment shape — LANDED 2026-07-10 night (INV-85/86, F-pair scenario, D-6/D-7 open with picks); the content-placement guidance names the binary-storage homes — FIRST LEG LANDED 2026-07-10 ~00:10 (the template's placement view now PROMPTS the deriving agent: heavy binary content names its home, a git repo of large binaries is the wrong home — so a project's own agent reaches the storage answer itself, per his word: teach the skill, never write it into their tree from here); red-proven; validated against the tlvphoto/engine case read-only — OPEN |
| 191 | **Check the field's TEST norms against our test method** (Alexander 2026-07-09 late evening: while you are at Kiro/BMAD, look at their test / test-matrix practice too): diff test-author + the matrix template against BMAD's Test Strategy and Standards (testing philosophy · unit/integration/E2E organization · test DATA management · continuous testing) and Testing Pyramid, Kiro's always-generated Testing Strategy, and one or two strong general sources; adopt what is real for our kinds (candidates already visible: a named test-data/fixture management duty beyond the engine case; the unit/integration/E2E axis beside our level ladder — same research discipline as the architecture check, adopt never copy | small | queued 2026-07-09 ~23:54 (door: feature; kind: skill; map: test-author + matrix template; research rides the same primary-sources habit, record committed) | Done when: the diff is recorded with adopted/not-adopted named, each adoption red-proven |
| 192 | **Scenario entry/exit contracts — pre/postconditions where they matter** (Alexander 2026-07-09 late evening, his words: the prover should say, where needed, which pre/postconditions hold — e.g. in tlvphoto how we EXIT scenarios, how we ENTER; "большая тема, отложи но не забывай, запиши"): the spec's flows state how a scenario is entered (from where, with what already true) and how it exits (to where, leaving what behind) — the prover's per-operation pre/postcondition lenses lifted to the SCENARIO level, kin of the entry-symmetry lens and the runtime view's walks; a flow whose entry or exit is unstated is a finding | large | **DEFERRED on his word 2026-07-09 ~23:54** — revisit trigger: the next prover-method landing, OR the first real spec where a scenario-level entry/exit hole ships a bug, OR his word; recorded so the milestone gate's deferred-row re-scan keeps it alive (door: feature; kind: skill; map: spec-author flow format + product-prover lenses) | Done when: spec-author states the entry/exit duty per flow, the prover carries the scenario-level lens, red-proven; validated on one real spec |
| 193 | **Which pipeline stages are we still missing for the kinds we support?** (Alexander 2026-07-09 late evening): a gap analysis — sweep the supported project kinds (product · backend · CLI · skill pack · prose/book · data/ML tier · static-first+edge blend) against the pipeline's steps and ask, per kind, which stage the field considers necessary and we do not name; first candidates from tonight's sources: a RELEASE/DEPLOY stage for hosted products (BMAD: Deployment Architecture + Rollback Strategy; we end at commit-and-show, deploy rides ad-hoc host scripts) · environment promotion (dev/preview/prod) · a data-quality/eval stage for data-ML projects · a security review step (BMAD Security section; our prover asks or names the skip) · post-ship monitoring walk (our success measures + field legs, unwired mechanically) | surface | queued 2026-07-09 ~23:54 (door: feature; kind: skill; map: build-pipeline step list + the work-kind table; method: same primary-sources research, adopt never copy; propose the stage list with a recommendation, his word picks) | Done when: the per-kind gap table is recorded; each adopted stage lands as its own row through the pipeline; the not-adopted named |
| 194 | **Accepted work reaches the project's remote — by rule in the skill** (Alexander 2026-07-10 ~00:07: if the project has GitHub, same-or-better MUST be pushed there — write it into the skill; ask which repo; if it is GitLab or anything else they name, the same law): where the host has a remote, work that passed every gate the diff reaches is pushed, never parked locally; the remote is DISCOVERED from the tree first (inspect, never ask what `git remote -v` answers); only a host with NO remote gets one contextual question at the first push moment — create one (GitHub · GitLab · whatever they name) or stay local, the answer recorded in the host profile; PLUS his 00:14 word: every push to a remote re-walks the README against the pushed truth — README crisp and current, mandatory; a milestone gate the human named in person still waits for his word | small | **LANDED 2026-07-10 ~00:10, session 30** (door: feature; kind: skill; map: grows the push law — new clause beside the CI/push gates; SPEC v0.16.13 + build-pipeline step 9 sentence (0.2.44) + the onboarding wish gains the remote note; M-195 red-proven against HEAD, `test_push_to_remote_law`) | Done when: SPEC + build-pipeline carry the law (discover-first · push same-or-better · one contextual question only when no remote), red-proven — MET |
| 195 | **Rendered docs resolve their cross-links** (deferral's trigger FIRED 2026-07-10 night: nine reader docs landed cross-linking each other and deep-linking `ARCHITECTURE.md#...` anchors, while `render-doc.py` emits `.md` hrefs verbatim and resolves no anchor — rendered pages ship broken transitions; compounding find: spec-author + the format doc CLAIMED the resolution exists, corrected to the truth tonight): render-doc.py rewrites relative `.md` links to their rendered `.html` neighbours and resolves heading anchors; trailing bracket codes may also link their Formal-index row later — that leg stays optional | small | **LANDED 2026-07-10 night, session 31** (door: feature; kind: infra; map: the render script beside the show law INV-27/show.text-docs; source: the render-time cross-links deferral named in spec-author + tonight's deferred-triggers re-scan) | Done when: a rendered page's links to sibling rendered docs open the rendered neighbour and its anchors land, red-proven at the string level against render output; the false claim stays corrected — MET (4 tests red on the old script, green after; anchors verified on the re-rendered nine docs + ARCHITECTURE; the false claim in spec-author/format-doc now states the shipped truth; README bullet removed per the Known-issues duty) |
| 196 | **The Formal index gets a standing symmetry test** (tonight's index audit was fully clean but four of its five checks exist only as a manual walk): a suite test asserts index↔body anchor symmetry both ways, no numbering holes/duplicates, and no dangling cross-reference between codes; summary-drift stays the prover's judgment call, named as such | small | **LANDED 2026-07-10 night, session 31** (M-204; the red-proof SEEDS three defects into a spec copy and stands forever) (door: feature; kind: skill; map: tests/ beside test_traceability; source: night formal-index re-check, scratchpad record) | Done when: the test exists, red-proven by seeding a synthetic hole, and the manual checks it covers are named in the matrix |
| 197 | **A live-but-busy worker is never declared dead** (night re-prove F3 on the liveness checks): the two checks' quiet verdict is conservative on ambiguity — a checkpoint showing an in-progress long operation (a render, a suite run) means ALIVE until the worker's own reply or a finished-marker; the watch windows are read from the brief's longest named operation | small | queued 2026-07-10 ~01:00 (door: feature; kind: skill; map: refines the resume protocol INV-76; source: docs/prover/2026-07-10-night-full-reprove.md F3) | Done when: INV-76 + base rule 6 state the conservative verdict, red-proven |
| 198 | **A dead worker's output is not yet a finished worker's output** (night re-prove F4): the dead verdict clears the FILES to touch, never the WORK as done; completeness is read from the worker's own checkpoint finished-marker, and an unconfirmed output walks the verify ladder [INV-46] before integration | small | queued 2026-07-10 ~01:00 (door: feature; kind: skill; map: refines INV-76 beside INV-46; source: same record, F4) | Done when: INV-76 states dead≠done with the completeness check, red-proven |
| 199 | **The real-device walk row has one defined representation** (night re-prove F5): a human-walk marker in the matrix template that the coverage gate counts as covered-at-real-device-level and the suite lists as owed-to-human — neither a blocking red nor a forbidden silent skip [INV-77, INV-80, E-15] | small | queued 2026-07-10 ~01:00 (door: feature; kind: skill; map: matrix template + test-author; source: same record, F5) | Done when: template + INV-77 state the marker, a string test pins it, red-proven |
| 200 | **Every published skill ships README + LICENSE** (night craft walk: test-author and feedback-intake, the two newest, ship neither while publish's own kind-checklist demands explicit license state): add both files to the two skills, matching the siblings' shape | small | **LANDED 2026-07-10 night, session 31** (READMEs drafted by the clean-writer road; MIT license files; inventory rows added) (door: feature; kind: skill; map: publish parity across the 8 skills; source: night skill-craft walk, scratchpad record) | Done when: both skills carry README + LICENSE and the publish checklist finds no license gap |
| 201 | **build-pipeline stops re-teaching the base's delegation rules** (night craft walk): the ~55-line junior-delegation block restates base rule 5 + the delegation contract nearly in full; trim to a pointer plus what is genuinely build-pipeline's own | small | **LANDED 2026-07-10 night, session 32** (door: refactor; kind: skill; map: build-pipeline body vs live-spec-base one-home; the audit narrowed the claim — the true restatements were the tier ladder and the raw-output sentence, both now one pointer at base rule 5; the rest of the block is pinned own-delta the needle tests assert (routing rule, brief laws, worker contract, ledger+clock arming, the saving line) and stays; bp 0.2.46, installed copy synced) | Done when: the block points at base, the skill's own delta stays, needle tests stay green — MET (277 green) |
| 202 | **Small craft fixes from the night walk, each its own edit**: product-prover's frontmatter names architecture documents as a reviewable artifact; publish folds its twice-stated version-push line to one; spec-author's standard-vocabulary crosswalk moves to its design-note home with a pointer; base's frontmatter rule-nickname list is completed or marked illustrative | small | **LANDED 2026-07-10 night, session 31** (prover 0.1.16 · publish 0.1.5 · spec-author 0.1.27 · base 0.1.29 + pins; the two needle tests follow the crosswalk to its design-note home — the fact moved lawfully, the skill keeps a live pointer) (door: refactor; kind: skill; map: four one-line homes; source: same walk) | Done when: each edit lands with its skill's patch bump, lints green |
| 203 | **Chat-cleanliness Stop-hook backstop gets a durable home** (his-word backlog item living only in NEXT_STEPS/memory; tonight's deferred-scan flagged the homelessness): the plan — a Stop-hook self-scan over the outgoing reply as the mechanical net for the chat laws — sits here until its trigger (recurring chat-law leaks) or his word | large | queued 2026-07-10 ~01:00 (door: feature; kind: infra; map: hooks beside INV-24/28/35; trigger: a chat-law leak class recurring after row 170 lands, or his word) | Done when: decided on trigger — built or agreed-closed by name |
| 204 | **Readability-restructure word-preservation verifier gets a durable home** (same homelessness class): the prover leg that proves a prose restructure moved meaning nowhere — word-token identity plus punctuation multiset plus the full prover pass — becomes a script the restructure operation must run | large | queued 2026-07-10 ~01:00 (door: feature; kind: infra; map: scripts beside the anchor-multiset check; trigger: the next big prose restructure, or his word) | Done when: decided on trigger — built or agreed-closed by name |
| 205 | **Thin-loader tidy** (his-word backlog, now homed): ~/.claude/CLAUDE.md carries only what must precede pack load; sweep it against the profile/pack homes once more | small | queued 2026-07-10 ~01:00 (door: refactor; kind: prose; trigger: next personal-layer landing, or his word) | Done when: every loader line proves its must-precede claim or moves |
| 206 | **Pack orchestration guidance** (his-word backlog, now homed): how a session runs many workers — parallelism, phase gating, model tiering (his 2026-07-10 word: the senior orchestrates and validates; opus drafts and audits; sonnet does mechanics), spawn liveness — stated once in the pack | surface | queued 2026-07-10 ~01:00 (door: feature; kind: skill; map: base/build-pipeline delegation section; trigger: the night-run experience is the first field record) | Done when: the guidance lands with the night run as its worked example, red-proven |
| 207 | **Artifact registry: pointers, never secrets** (his-word backlog, now homed): a per-project index of handed-in artifacts surviving /clear — records WHERE a thing lives (Keychain name, path), never the value | surface | queued 2026-07-10 ~01:00 (door: feature; kind: skill; trigger: the next real handed-in-artifact loss, or his word) | Done when: decided on trigger — built or agreed-closed by name |
| 208 | **Human-facing prose is drafted by a clean writer** (Alexander 2026-07-10 ~00:53, his word after the third onboarding bounce: write ALL docs that way, with a clean head — then the pack's own rules read clean too): any text a human reads — reader docs, spec prose, reports, decision pages, product copy, and the pack's own rule bodies when touched — is DRAFTED by a fresh writer session working from a plain brief (the facts, the reader, the register laws), never by the pack-marinated session; the marinated session briefs, gates, and folds; the law binds forward — existing text walks the road when it bounces off the human or is opened by an edit anyway, never as a blanket rewrite (the restructure-safety risk is named) | small | queued 2026-07-10 ~01:08, INTENDED TONIGHT (door: feature; kind: skill; map: base rulebook beside the register laws; evidence: tonight's nine clean-drafted docs passed his bar while the marinated-session onboarding text bounced three times) | Done when: the base rule + spec clause state the drafting road and its forward binding, red-proven by a string test |
| 209 | **The README carries a Known issues section** (Alexander 2026-07-10 ~01:17, his word off a real field read): while real known problems exist, the README names them honestly — each with its fix status — and the list is re-read at every push (a resolved issue leaves the list the push its fix ships); the duty joins the publish kind-checklist floor beside the shopfront law | small | **LANDED 2026-07-10 night, session 31** (door: feature; kind: skill; map: publish checklist floor + the pack's own README as the first instance — five honest issues listed, drafted by the clean-writer road per INV-84; publish 0.1.4) | Done when: the checklist states the duty and the pack's own README carries the section — MET; the per-push re-read rides the existing README re-walk (INV-44/82) |
| 210 | **Plainer spec prose — the clean judge's eleven findings as the worklist** (his-word backlog row 148 gains its first concrete list; the clean-agent judge protocol confirmed on his 2026-07-10 ~01:22 word — a fresh opus spawn, locked rubric, planted-defect canary): eleven surviving findings in settled text, all metaphor/redundancy family («the shell eats a command», «a limping thing never dams the flow», «the outcome does the talking», the no-third-document restatement, and kin) — each gets a plain rewording through the clean-writer road (INV-84), anchors untouched, folded family-by-family; the judge output is the dated record | surface | **LANDED 2026-07-10 night, session 32** (door: refactor; kind: prose; map: grew lawfully by the class law — nine surviving findings + one nit in PRODUCT_SPEC, plus the flagged phrases' exact siblings in the base rulebook, communicator, and the matrix row (base 0.1.30 · communicator 0.1.43; two needle tests follow the new wording in lockstep; 7 base-version pins refreshed); spec v0.16.21; drafted by a clean writer per INV-84, record docs/prover/2026-07-10-row210.md) | Done when: a re-run clean judge leaves zero surviving definite/likely findings on the settled text, anchors proven unchanged (multiset), suite green — MET (judge selftest passed · surviving 0 · anchors IDENTICAL · 277 green) |
| 211 | **Push-law and register-lint wording seams from the post-fold re-check** (docs/prover/2026-07-10-night-postfold.md P1, P2): the create-remote path says plainly that a remoted-but-ungranted host still gets the grant question (one question per gap, never capped at one total); INV-83's "anything shown" names its true reach — the script gates artifacts, chat's register stays with the hook + the walk (and row 203's backstop) | small | **LANDED 2026-07-10 night, session 31** (door: refactor; kind: prose; map: INV-82/INV-83 sentences only) | Done when: both sentences state the boundary, anchors unchanged, suite green |
| 212 | **Clean-writer law boundaries** (same record, P3, P4): INV-84 names its "reports" scope against the walk's own report authorship (a chat report stays the session's, a rendered report page rides the clean road) and defines the redraft unit when an edit opens settled text (the touched section, never the whole page by default) | small | **LANDED 2026-07-10 night, session 31** (door: refactor; kind: prose; map: INV-84 sentence + base rule 21) | Done when: the two boundaries stand in the law, red-proven by the presence test's extension |
| 213 | **Pair-law small wording seams from the pair-wave re-check** (docs/prover/2026-07-10-night-pair-wave.md PW-3..5): the scenario names its liveness net (the parked blocked-on-engine debt line surfaces in every status report until the engine sweeps); the inbox sentence states its own carve-out in one breath (one new inbox file IS the single legal foreign write); the declined-split reuse note gets a field handle in the host profile like its sibling settings | small | **LANDED 2026-07-10 night, session 32** (door: refactor; kind: prose; map: INV-85/86 sentences; spec v0.16.20; drafted by a clean writer per INV-84; the liveness-net clause adds one cited anchor [INV-27] — the fix's own substance; the reuse-note key `reuse.split-declined: <date>` also lands in the INV-85 index row) | Done when: the three sentences stand, anchors unchanged, suite green — MET |
| 214 | **The small-fix path for red-first is stated** (tlvphoto inbox wish 2026-07-10 ~02:15, his word behind it: if the order is written and was missed, hand it to live-spec and rewrite the process): test-author's red-first law names the order for a tiny reversible edit inside the pipeline's skip boundary — red before code stays the default at every size; a one-batch fix+test is legal only when the batch closes with the mechanical red proof (restore the pre-change file via `git show HEAD:<file>`, run the suite, watch the new rows fail, restore the fix) named in the landing record; build-pipeline's skip sentence points at the path | small | **LANDED 2026-07-10 night, session 32** (door: feature; kind: skill; map: changes the test-method feature — test-author's red-first bullet + build-pipeline's skip-boundary sentence; source: the tlvphoto wish, now docs/wishes/2026-07-10-from-tlvphoto-red-first-slips-on-small-voiced-fixes.md; the wish's option (b) taken with red-first kept as the default order — the mechanical proof is what the field session already did, now written; test-author 0.1.3 · bp 0.2.47 · matrix M-205; drafted by a clean writer per INV-84) | Done when: both homes state the path, red-proven by a string needle test, suite green — MET (test_small_fix_red_path proven red against the pre-change tree, then 278 green) |
| 215 | **Plan for parallel work: the architecture step gains a parallelism lens** (Alexander 2026-07-10 ~10:08, his word: «как распланировать так чтобы можно было параллельно делать вещи в проекте. например, код на более мелкие модули. когда делается дизайн, это надо учитывать»): when the architecture step carves nodes, it weighs parallel buildability — smaller, independently testable modules with narrow seams let several build lanes run at once (the lanes-by-graph law INV-49 then has real independent sets to pick); the lens joins the architecture step's checks and the prover's architecture lens; sibling of row 206 (pack orchestration guidance) — the two land together or cite each other | surface | queued 2026-07-10 ~10:08, POST-1.0 on his word (door: feature; kind: skill; map: changes the architecture step — build-pipeline step 3 + product-prover's architecture lens; trigger: after the 1.0 release, alongside row 206) | Done when: the architecture step and the prover lens both name the parallelism check, red-proven by a string test; the first real landing that used it cited in the row |
| 216 | **A norm-pointered surface owes a norm-conformance matrix row** (born of the onboarding card's bounce, 2026-07-10 ~10:15 — the first render invented its own row format, dropped three norm sections, and shipped green; his word: think what other checks were forgotten, such failures are unacceptable): test-author's derivation rules gain the law — when a spec clause carries a `norm:` pointer, the matrix owes a conformance row asserting the render's sections and row names against the frozen norm (DOM-text; the look itself stays the human's eye per the feel gate), and build-pipeline's step-7 norm-open rule already owes the plan-vs-prototype diff line in the landing report — the two halves cited together | small | **landed 2026-07-10 ~17:13 (1.0.7, rule 7 in test-author's derivation walk, M-227 red-proven)** (door: feature; kind: skill; map: changes the test-method feature — test-author derivation rules; the card's M-211 is the first live instance) | Done when: test-author states the law, red-proven by a string needle test, suite green |
| 217 | **The convergence audit — every quality lock is itself tested to hold** (Alexander 2026-07-10 ~10:22-10:24, his words: «нужна сходимость всех процессов… это базовый принцип» and «потести всё такое что оно работает и всегда сходится, без этого мы не можем пойти на 1.0»): enumerate every convergence mechanism the pack claims — frozen norms, norm-conformance tests, the register lint's growing pattern set, the debt-cap ratchet, the anchor-multiset guard, waiver expiry, version pins — and give each a test that it HOLDS and cannot silently regress; known holes at registration: norm files have no content-fingerprint test (a frozen norm can drift silently), the lint pattern set has no grows-only test, the debt ratchet's only-downward claim needs its own check; 1.0-BLOCKING — rides the M-1 audit as its own pass | surface | queued 2026-07-10 ~10:24 on his explicit word (door: feature; kind: infra; map: guardrails + the M-1 gate list) | Done when: the lock inventory is written, every listed lock has its holding test (each new one red-proven), and the M-1 audit's list names the convergence pass |
| 218 | **The convergence principle enters the base rulebook** (Alexander 2026-07-10 ~10:24, his frame: «сходимость — про любой процесс, любой вид артефакта; у тебя есть цель, у нас есть цель, и мы к ней идём. всегда»): the base gains the rule stated once — every piece of work names its GOAL up front as an artifact one can hold the work against (a frozen norm, an exemplar bank, a failing test, a written acceptance), never a paraphrase; every iteration measures distance to the goal itself, a proxy never replaces it; the distance only shrinks, reached levels lock by mechanism; a deliberately divergent phase is named and bounded by its convergence point; the playbook repo gets the principle's chapter, the base rule cites it | small | **landed 2026-07-11 ~23:26, session 36** (door: feature; kind: skill; map: live-spec-base shared rules + the private playbook — rule 22, no new surface; fences held: the stated-once law (working skills reference the base, none restates the rule), rule 16's prototype fence cited as the named divergent stretch, rules 11/13 unweakened; architecture: assignment only — INV-98 → base-rulebook node, rule-22 pin added; prove-architecture stood down by name (no structure change); facet sweep + design-sync stood down by name — skill kind; fit walk closed in-clause (trigger: any piece of work at intake and at every iteration · correction: goal-as-artifact + distance to the goal itself + mechanism locks · not-fire: a named, bounded divergent stretch); non-goals: no retro-walk over already-landed rows (binds forward), no new mechanical gate beyond the rows-216/217 locks already standing; success measure [default]: the next lookalike traces to a proxy swap named at review, and no reached level regresses silently; delegation: the mechanical bundle (red-first test, 13 edits incl. the seven base pins) on a Sonnet worker, ~12 min senior saved — the worker HALTED correctly on one brief defect (the new invariant had no owning architecture node and the matrix row sat under the wrong node's section), senior assigned INV-98 to base-rulebook and moved M-234 under its block, zero silent fixes; prover short form docs/prover/2026-07-11-row218-convergence-rule.md — 0 must-fix; playbook chapter PUSHED to happysasha18/playbook cfbbf72 the same hour, the intro's section-order note updated to stay true) — SPEC v1.0.6 INV-98 (clause + index) · base rule 22 (live-spec-base 1.0.2, seven pins lockstepped) · M-234 · tests/test_convergence_rule.py (4 tests) RED PROVEN then green · pack 1.0.12 · suite 360 green | Done when: the base rule stands (red-proven by a string test) — MET (rule 22, 4F then green); the playbook chapter written — MET (chapter opens the book, commit cfbbf72); both citing his 2026-07-10 word — MET (both texts carry the dated word) |
| 219 | **Every skill-kind landing walks skill-creator's review — always, by the classifier** (Alexander 2026-07-10 ~10:26 + his raise ~10:29: «всегда надо все скиллы проводить через него. у тебя же классифайер проектов. это обязано быть в нашей спеке»): a SPEC clause binds it — whenever a wish's work-kind is `skill` (a pack skill created or edited), the verify step also runs the installed skill-creator's review (description/trigger quality — does the skill load when it should; evals where applicable), findings folded or rejected by name in the landing record; the work-kind classifier IS the trigger, never mood; the work-kind table's skill/verify cell names the walk; the M-1 audit additionally sweeps ALL skills once (the catch-up for skills landed before this law) | small | **landed 2026-07-11 ~23:52, session 36** (door: feature; kind: skill; map: new spec clause beside the work-kind law T-16 + the pipeline's work-kind table verify row, no new surface; fences held: T-16 intake law whole, the milestone gate's whole-pack walk cited never restated, INV-22 unresolved-kind-scales-nothing intact; architecture: assignment only — INV-99 → build-pipeline; prove-architecture + facet sweep + design-sync stood down by name — skill kind; fit walk closed in-clause (trigger: the classifier says kind=skill · correction: verify also walks skill-creator's craft review, findings folded/rejected by name · not-fire: other kinds; the milestone walk owns the catch-up); non-goals: no retro-walk outside the milestone gate, the review scopes to craft (format/frontmatter/triggering/evals) with the method's content out of its scope; success measure [default]: no skill lands without its review line in the record, and undertriggering skills stop reaching the field; delegation: the mechanical bundle (red-first test, 9 edits) on a Sonnet worker, ~10 min senior saved, zero brief defects; the review itself on a fresh general-purpose agent with the installed skill-creator; prover short form + the review's fold/reject table docs/prover/2026-07-11-row219-skill-kind-review.md — 0 must-fix) — SPEC v1.0.7 INV-99 (clause + index) · pipeline work-kind verify cell + description re-cut per the review (build-pipeline 1.0.1) · M-235 · tests/test_skill_kind_review.py (4 tests) RED PROVEN then green · pack 1.0.13 · suite 364 green | Done when: the spec clause stands with its anchor — MET; the work-kind table cell names the walk — MET; red-proven by a string needle test — MET (4F then green); the first skill-kind landing after it shows the review in its record — MET on this very landing (its own kind is skill: 8 findings, 3 folded, 3 rejected by name, 2 clean — the record's table) |
| 220 | **Audit the last 24 hours of the tlvphoto exchanges — what must enter live-spec** (Alexander 2026-07-10 ~10:43, his word: «проаудируй 24 часа назад коммуникации с тлвфото и посмотри что можно и нужно прописать в лайвспек. там вылезли существенные проблемы. это первым делом»): read the tlvphoto (and exhibition-engine) journals, resume files, prover records, and checkpoint trails of the last day READ-ONLY, name every problem class that surfaced, check each against the pack's existing laws, and queue or land the missing rules here; FIRST in the queue after the onboarding landing on his word | surface | queued 2026-07-10 ~10:44, PRIORITY: first after the onboarding landing (door: feature; kind: skill; map: findings decide — each lands in its own home; the window stays read-only on their trees per the audit-only law) | Done when: the audit record lists every surfaced problem with its verdict (already covered · new rule queued/landed · agreed non-problem on his word), and the landed rules cite it |
| 221 | **tlvphoto's move onto the new documentation — teach-only** (Alexander 2026-07-10 ~10:43, his word: help the migration «только либо инструкциями, либо в инбокс», preferred shape: he tells the tlvphoto window «подгрузи лайвспек и давай переверстывать всю документацию»): make that one sentence WORK — verify the adoption path + the pair-adoption guide carry a document-relayout walk a cold tlvphoto session can follow alone (their windows execute; this window answers, reviews artifacts read-only, and may file at most one inbox wish); any gap the dry-read finds becomes a fix HERE, never a write THERE | surface | queued 2026-07-10 ~10:44, SECOND after row 220 (door: feature; kind: skill; map: adopt/ADOPT.md + docs/pair-adoption.md read as the migration's own user; source: his spoken plan for today) | Done when: the one-sentence start is dry-read against the guides with every step answerable from them, gaps fixed here, and the real migration runs from their windows with this one supervising |
| 222 | **Tests clean up after themselves — a matrix-template law** (Alexander 2026-07-10 ~10:46, his lead on the tlvphoto problems: «там просто тесты не делают clean-up — это в матрице тестов по любому надо прописать»): test-author's writing rules and the matrix template gain the hygiene law — every test removes what it creates (temp files, fixtures on disk, spawned processes, mutated shared state), a suite run leaves the machine as it found it, and a leak is a defect of the test; the pack's own suite is the first instance swept (the new card tests already leak NamedTemporaryFile fixtures — caught the same minute the law was spoken); row 220's audit checks the tlvphoto suites against it | small | **landed 2026-07-12 ~00:07, session 36** (door: feature; kind: skill; map: test-author writing rules + matrix template + spec clause, no new surface; fences held: the red-first law untouched beside the new bullet, INV-77's boundary whole, gate B's scratch-copy runs the same leak diff scoped to the suite's own prefixes; architecture: assignment only — INV-100 → test-author; prove-architecture + facet sweep + design-sync stood down by name — skill kind; fit walk closed in-clause (trigger: any test creating files/processes/state · correction: tracked temp home + cleanup + the session before/after diff · not-fire: fixtures shipped in the repo tree); non-goals: no sweep of the system temp dir's PRE-EXISTING litter (macOS purges it; only our prefix is watched), foreign hosts' suites ride row 220's audit; success measure [default]: a leak turns the run red the day it is born, and no test ever writes where a human sees; delegation: the bundle (2 new test files, the conftest diff, the card-leak sweep, 10 law edits) on a Sonnet worker, ~15 min senior saved — the worker HALTED correctly on one brief defect (an illegal level value in the matrix row; the level vocabulary is pinned) and surfaced the midnight rollover of the prover-record gate (ledger appended, the agreed-non-problem ask rides the morning report), zero silent fixes; prover short form docs/prover/2026-07-12-row222-test-hygiene.md — 0 must-fix) — SPEC v1.0.8 INV-100 (clause + index) · test-author 1.0.1 writing rule · template checklist item · tests/conftest.py session diff (red-proven by a planted leak) · card tests' leak swept (livespec-test- prefix + module teardown, 0 files after a full run) · M-236 · tests/test_suite_hygiene.py (5 tests) RED PROVEN then green · pack 1.0.14 · suite 369 green | Done when: the law stands in both homes red-proven — MET; the pack's own suite passes a leak check (before/after temp-dir diff in a test) — MET (tests/conftest.py, planted-leak red then clean); the law's reach includes side effects OUTSIDE the artifacts (a browser dropping real files into ~/Downloads: the worked incident, inbox/…-test-runs-polluted-downloads-incident.md, 42 files before his eye caught it, fixed in both their harnesses the same hour) — and the PLACEMENT half (his 2026-07-10 ~14:11 word): a test's files are born in a temp home (the system temp dir or the host's gitignored state dir) and erased at the run's end; a user-visible folder — Downloads, Desktop, Documents — is never a test's workspace, and a headless browser's download directory is pointed at the temp home explicitly; the visible cost that taught it: every dropped file makes the Downloads dock icon bounce, reading as a phantom update; incident record harvested 2026-07-10 — 42 files by ~10:41, both harnesses fixed the same hour via the browser's download-behaviour switch (file in git history: inbox/2026-07-10-from-tlvphoto-test-runs-polluted-downloads-incident.md) |
| 223 | **The prover sweeps cross-cutting requirements** (tlvphoto inbox wish 2026-07-10, thrown on his ~10:38 word; the worked example: the site's analytics covered some beats while whole surfaces emitted nothing, and only HIS eye found it — plus the same-shape composition miss, five appended works not refreshing the told story): the spec names its cross-cutting laws in one place (measurement, accessibility, error handling — what the product declares); the prover walk gains one station — per declared law, enumerate every surface and transition and demand the law's clause or a dated exemption per item, a missing clause ranking as a broken invariant; spec-author owns the twin habit — a new surface's section states its line against each declared law before the prover sees it | surface | **landed 2026-07-12 ~00:22, session 36** (door: feature; kind: skill; map: prover walk + spec-author checklist + the spec clause, no new visible surface; fences held: the facet sweep and curated-list laws untouched beside the new habit, the entry-symmetry lens whole, the prover's lens-growth law honoured — the FULL pass re-arms and rides the 1.1.0 audit; architecture: assignment — INV-101 → spec-author, the prover node carries the station as wiring; prove-architecture + facet sweep + design-sync stood down by name — skill kind; fit walk closed in-clause (trigger: any spec delta where a declared-laws home exists · correction: per-law per-item clause-or-dated-exemption · not-fire: a spec with no home yet earns ONE finding naming the home first); non-goals: no retro-fill of every old section's declared-law line (the law binds forward; the global enumerations cover the stock), no mechanical grep-gate yet (a candidate after field use); success measure [default]: the next whole-surface miss of a declared law is caught by the station, ahead of his eye; delegation: apply-bundle on a Sonnet worker with self-located anchors (zero halts), the law prose senior-authored, the FIRST REAL RUN on a fresh Opus reviewer — 11 items × 3 laws, 31/33 covered, 2 n/a, 0 MISSING, clean, one homing observation recorded; prover record + the run digest docs/prover/2026-07-12-row223-declared-laws.md — 0 must-fix) — SPEC v1.0.9 INV-101 (clause + index + the pack's own declared list with two dated exemptions) · product-prover 1.0.1 station lens · spec-author 1.0.1 habit · M-237 · tests/test_declared_laws.py (4 tests) RED PROVEN then green · pack 1.0.15 · suite 373 green | Done when: both skill homes state the station red-proven — MET; the spec clause names the one home — MET; the first real prover run walks it — MET (clean, 0 MISSING; record's table) |
| 224 | **The mirror-assertion ban** (row 220's audit, item 3: green suites missed real walk bugs because tests recomputed the code's own formula as the expected value): test-author's writing rules state it — a test's expected value derives INDEPENDENTLY of the code under test (a hand-computed constant, a fixture, an independent oracle); an assertion that mirrors the implementation proves only that the code equals itself | small | **landed 2026-07-12 ~00:29, session 36** (door: feature; kind: skill; map: test-author writing rules + spec clause, no new surface; fences held: red-first law and hygiene rule untouched beside the new bullet; the one-batch mechanical red proof stays legal (restores the pre-change file — an independent source by construction); architecture: assignment — INV-102 → test-author; prove-architecture + facet sweep + design-sync stood down by name; fit walk closed in-clause (trigger: any assertion whose expected value needs a source · correction: constant / independent derivation / recorded real output · not-fire: round-trip and property tests — the honest boundary); non-goals: no mechanical grep-gate for mirror patterns (a candidate after field use), foreign suites ride row 220's audit; success measure [default]: the next formula bug turns a test red instead of passing its own reflection; delegation: drafted by an Opus worker (self-verified needles), applied by a Sonnet worker (zero halts) — the drafter-applier form; senior wrote the brief and accepts; prover short form docs/prover/2026-07-12-row224-mirror-ban.md — 0 must-fix) — SPEC v1.0.10 INV-102 (clause + index) · test-author 1.0.2 · M-238 · tests/test_mirror_assertion_ban.py (4 tests) RED PROVEN then green · pack 1.0.16 · suite 377 green | Done when: the rule stands red-proven and the audit's cited mirror cases are named as the born-of incident — MET (both homes red-proven 4F then green; the boundary and the three legal sources stated) |
| 225 | **A voiced fix owes its landing checklist by tripwire** (audit item 4: one-line fixes touching spec-backed literals shipped without same-session doc sync): the rules exist (docs travel with the change; red-first small-fix path) — the gap is a TRIPWIRE: build-pipeline's bug/skip doors state that a diff touching a spec-cited literal fires the doc-sync check mechanically | small | queued 2026-07-10 ~11:00 from the audit (door: feature; kind: skill; map: build-pipeline door rules; kin of row 214) | Done when: the tripwire sentence stands red-proven |
| 226 | **A checkpoint closes at its landing** (audit item 5: two engine checkpoints still read "not started" after everything in them shipped — a resuming session would redo done work): the checkpoint law gains the closing half — the landing that ships a checkpoint's items flips the checkpoint CLOSED the same session; a checkpoint whose items are all in git history is stale by definition and a resume defect | small | queued 2026-07-10 ~11:00 from the audit (door: feature; kind: skill; map: base rule 6 + build-pipeline landing steps) | Done when: the closing sentence stands in both homes red-proven |
| 227 | **One canonical state dir, worktree isolation by default on overlap** (audit item 6: a ghost `.livespec` dir with a different profile beside the real `.live-spec` — two competing truths; and overlapping write-sets ran unisolated): attach/base state the canonical dir name once and the adoption sweep retires look-alikes to the attic; the lanes law makes an isolated worktree the DEFAULT when two lanes' write-sets overlap | small | queued 2026-07-10 ~11:00 from the audit (door: feature; kind: skill; map: attach + base rule 7/T-18; the tlvphoto ghost dir is the born-of incident, THEIR window sweeps their tree) | Done when: the canonical-name sentence + the worktree default stand red-proven; the pack's own tree has no ghost |
| 228 | **The push walk reads the remote gate's verdict — a red CI is the session's own immediate bug** (Alexander 2026-07-10 ~11:00, his word: «зачем мне мейл приходит? ты сам не можешь чинить когда деплой на гитхаб валится?» — today's norm-file miss was fixed by the session but DISCOVERED from his screenshot): the push law gains the closing step — after every push the session reads the CI mirror's verdict itself (one `gh run` read, minutes, no human wait) and a red run opens an immediate bug lane, fixed and re-pushed before anything else; the human hears the outcome from the session's report with the fix done, never first from a GitHub email | small | queued 2026-07-10 ~11:01 on his word (door: feature; kind: skill; map: the push law's walk (INV-82 family) + build-pipeline step 9; today's incident is the born-of) | Done when: the sentence stands red-proven and the next push's record shows the verdict read |
| 229 | **Parametrizability audit — the full settings model** (Alexander 2026-07-10 ~11:00, POST-1.0 on his word: «аудит параметризуемости — если были бы прожект сеттингс, какие были бы параметры, какие типы и секции»): derive the complete settings model from the shipped pack — every parameter that COULD exist, its type (enum · string · boolean · path · conditional), its section (about the project · about the person · the workshop), and its applicability condition (GitHub-or-GitLab exists only when the project kind carries software/skills; author name; languages; …); the existing Card-marked table and the settings card are the seed; the outcome decides whether the catalog grows a typed schema | surface | queued 2026-07-10 ~11:01, POST-1.0 on his word (door: feature; kind: skill; map: the settings ladder + the onboarding card; kin of row 215's post-1.0 batch) | Done when: the model document lists every parameter with type/section/condition, reviewed by him, and the accepted deltas become queue rows |
| 230 | **Standing self-audit on every drift axis** (Alexander 2026-07-10 ~11:02, his word: «надо селф-аудит делать чтобы спеки не дрифтили по формулировкам… любая ось где может быть дрейф — её надо всегда постоянно присобирать»): enumerate every axis where drift can happen — spec wording vs the Formal index, docs vs shipped code, installed skill copies vs the repo, matrix vs tests, README claims vs truth, chat register vs the laws — and give EACH a standing re-tightening mechanism with a named cadence (continuous test where possible, milestone sweep where not); the M-1 convergence pass (row 217) produces the axes inventory as its byproduct, this row turns it into standing sweeps; the convergence family's third leg beside 217 (locks hold) and 218 (goal-anchors) | surface | queued 2026-07-10 ~11:02 on his word, POST-1.0 (door: feature; kind: infra; map: guardrails + the existing traceability/freshness machines as the seed) | Done when: the axes inventory stands, every axis names its sweep and cadence, each new sweep red-proven |
| 231 | **Version alignment is a stated rule** (Alexander 2026-07-10 ~11:29, at the 1.0.0 release, his word: «это тоже правило такое… вопрос в какие моменты синкать. major version стопудово»): the pack states WHEN every skill's version aligns to the pack's — at a MAJOR version certainly (done at 1.0.0: pack, all eight skills, the spec header, the pins); the open half is the other moments (minors? never? on his word?) — refine with him after the wipe; also fold here the convergence-locks leftovers F4/F5 (standing anchor guard; branch protection — his one GitHub click) and the skill-description polish trio | small | queued 2026-07-10 ~11:30 on his word, POST-1.0 (door: feature; kind: skill; map: base versioning rules + the release walk) | Done when: the alignment rule stands with its moments named on his word, red-proven; F4/F5 dispositioned |
| 232 | **Every ask gets a time estimate — said, then TRACKED** (Alexander 2026-07-10 ~11:36, his word at the 1.0 release: «когда прошу что-то — всегда говори сколько займёт, а потом отслеживай! и если долго — объясни что надо сделать, нормальным языком»): the intake echo carries an honest time range read from observed runs; the landing report closes the loop — estimated vs actual, said plainly; long work (past ~an hour) is explained up front in plain steps — what has to happen and why it takes that long; the ranges live beside the offline-window habit (communicator) and never dress a guess as a promise | small | **LANDED 2026-07-10 ~13:45, session 33** (door: feature; kind: skill; map: communicator's echo + landing-report rules — spec clause INV-93 + rule 12 echo range + rule 13 remaining-time heartbeat + rule 8 estimate-beside-actual; matrix M-222; red-proven by test_report_estimates; communicator 1.0.1, pack 1.0.2); was queued ~11:37 POST-1.0, RAISED to the law batch's FIRST row on his ~13:20 word — session 33 reported estimates loosely and no standing plan-status shape, the reminder is the born-of's second case | Done when: the echo and report rules state it red-proven, and the first landing after shows estimate → actual |
| 233 | **A sound thought is never silently dropped** (Alexander 2026-07-10 ~12:10, his word after the night docs pass compressed the README's «why live-spec when BMAD/Kiro/spec-kit exist» section to one pointer line: «нельзя вообще никогда просто выкидывать вещи которых не понимаешь… если была мысль здравая то хотя бы спроси себя или меня может она нужна дальше» — and no per-line paralysis): a rewrite or restyle that REMOVES substantive content — a section, an argument, a rationale, a worked example — lists every removal in its landing report with one line of judgment each (kept the fact elsewhere · owner killed it by name · proposes dropping, asked); a removal the rewriter cannot justify becomes a question, never a silent cut; scoped to removals of substance, never to line-level wording, so the flow stays; the README section restored same session (2026-07-10 ~12:14) is the born-of incident | small | queued 2026-07-10 ~12:15 on his word, rides the law batch (door: feature; kind: skill; map: communicator's pre-report walk + build-pipeline's docs-only door + the restyle loop's rules) | Done when: the rule stands in its homes red-proven, the landing-report shape names the removals list, and the born-of incident is cited |
| 234 | **Managing one project's independent parallel lanes** (Alexander 2026-07-10 ~13:06, his word: «надо тебе понять тоже как менеджить проект в котором независимые параллельные лейны. добавь в туду»): the pack states how a senior session RUNS several independent lanes at once — a per-lane state line in the resume file (each lane names its station and its next step), a heartbeat that names every live lane, lane-scoped workers with non-overlapping write-sets (worktree default on overlap, row 227's half), one report that walks the lanes one by one without mixing them, and the commit law across lanes: one lane, one commit, never a sweep that carries a second lane's files; the 2026-07-10 midday session is the born-of worked example (the catch-up-walk landing lane + the Superpowers/gstack research-and-README lane + a promoter hand-off, run at once) | surface | queued 2026-07-10 ~13:06 on his word, rides the law batch beside rows 215 (parallel buildability) and 206 (pack orchestration) — the three cite each other (door: feature; kind: skill; map: build-pipeline orchestration + the lanes law (INV-49, T-18) + communicator's report shape + NEXT_STEPS template) | Done when: the lanes-management rules stand in their homes red-proven, and the first real multi-lane session after them cites the rules with its per-lane commits |
| 235 | **The leave-command: «я ухожу» reaches a safe stop and says so** (Alexander 2026-07-10 ~13:30, his word from the café: «если мне надо из кафе уйти, я даю команду — ты доходишь до точки… и смогу выключить комп»): one spoken command makes the session wind down to a shutdown-safe state — background workers halted or landed (a sleeping machine kills them, INV-76's plain truth), every lane at its checkpoint, work committed, and one closing line says «можно выключать» with what resumes where; the twin habit rides long work the whole way: an occasional plain «осталось примерно N минут» line so the human can plan the exit (kin of row 232's estimates and INV-71's heartbeat) | small | **first leg landed 2026-07-10 ~14:55 (1.0.4, INV-95/M-224 red-proven, three homes)**; open leg: the first real «я ухожу» reaching the safe stop with the closing line — expected the same afternoon (door: feature; kind: skill; map: communicator's reporting rules + base checkpoint rule 6 + the worker-liveness law) | Done when: the command's walk stands in its homes red-proven, and the first real «я ухожу» reaches the safe stop with the closing line said |
| 236 | **Projects declare their technologies; an engine parametrizes them** (Alexander 2026-07-10 ~13:48, his word: «надо чтобы проекты рассказывали про технологии которые они используют… движок жёстко Cloudflare и Google Analytics? или кастомизируемо? надо параметризовать и прописать; мы не боимся миграций»): a host's docs state in one findable place the external technologies and services the product stands on — hosting, deploy target, analytics, build tools, third-party APIs — each with its role and a swappability verdict (fixed by choice · swappable with a named seam · hard-wired, flagged); for a reusable ENGINE every such dependency becomes a named contract entry like content does (INV-79's infrastructure twin): the engine names the seam, ships a works-without-it default or the swap instruction, and a hard-wired service found in an engine is a finding, not a fact of life; adoption's inventory raises hard-wired services the same way it raises unbacked surfaces; born-of: the tlvphotos engine carries Cloudflare + Google Analytics hard today, and installing it elsewhere has no stated road | surface | queued 2026-07-10 ~13:49 on his word, rides the law batch (door: feature; kind: skill; map: spec — new clause beside the content contract INV-79 and the placement view INV-75; architecture-method's placement section; adoption Phase 2 inventory; the real parametrization of the exhibition-engine is that repo's own row after its adoption) | Done when: the law stands in its homes red-proven, the engine content-contract template names infrastructure entries, and the first real engine lists its services with swap verdicts |
| 237 | **No line certifies its own sincerity** (Alexander 2026-07-10 ~13:53, his word off the README's "we say so plainly": «если все честно не надо говорить "честно" — надо прописать это как правило, но умно, не именно на одно слово… если ты говоришь об А не надо говорить о не-А когда это не добавляет информации»): a sentence praising its author's honesty or directness carries no information — naming not-A informs only where not-A was a live alternative; the content carries the honesty, the label comes off; each caught phrase joins the register lint's family as its own class | small | **LANDED 2026-07-10 ~13:57, session 33, same hour as spoken** (door: feature; kind: skill; map: spec INV-94 beside the lint law INV-83 + communicator rule 8's never-list + five lint patterns (floor 17→22) + the README swept of the class — four phrasings rewritten to carry the fact; M-223 red-proven by test_no_self_certification; kin: the profile's no-scissors line is this law's chat-side sibling) | Done when: the law stands in both homes red-proven, the lint carries the caught family, the README reads clean — MET |
| 238 | **The rationalization table — approved 2026-07-05, never built** (found by the 2026-07-10 originality audit: the owner answered "now" in the dated decision record, and no trace exists in the shipped pack; a sound approved thought is never silently dropped, row 233's law): build the mechanism as approved — the idea traces to Superpowers/Trail of Bits practice, source credited in the audit doc — or bring the decision back to the owner if its ground has shifted since | small | queued 2026-07-10 ~14:15 from the audit finding (door: feature; kind: skill; map: the 2026-07-05 decision record names the shape; docs/research/2026-07-10-originality-audit.md is the finder) | Done when: the mechanism stands as approved (red-proven) or the owner's fresh word re-decides it, either way citing the audit |
| 239 | **The catch-up routing gets its clean discriminator** (found by the track-coach audit 2026-07-10 ~14:19: MIGRATION.md lists "re-layout the documentation" among the walk's trigger wordings, and a host asked for a pure layout pass with NO pack-version delta falls into the gap — the trigger list would justify the heavy walk where a host queue row is the truth): the routing states it in one sentence — the catch-up walk fires only when the host's recorded pack version is BEHIND the current package VERSION; a docs restructure with no version delta is the host's own queue row through its pipeline, whatever wording the ask used; the spec's skill-behaviour paragraph and the guide's "When to run this" both carry it | small | queued 2026-07-10 ~14:23 from the audit (door: feature; kind: skill; map: MIGRATION.md routing + the F-catchup skill-behaviour clause; the track-coach s63 case is the born-of; sibling: row 240) | Done when: the discriminator stands in both homes red-proven, and the trigger list reads as examples under it |
| 240 | **The light vehicle for an owner-scoped docs-layout pass is blessed** (the same audit: track-coach, lacking a named vehicle, correctly improvised one — a checkpoint carrying the owner's locked decisions, work on a clean pushed base so restore is one command, and a word-multiset proof that content survived; worth making that the pack's sanctioned shape so hosts neither skip discipline nor over-apply the catch-up machinery): the pack names the pattern — where it lives (checkpoint discipline + the restructure-safety proof), what it owes (locked decisions before moves · clean-base restore · word-token AND punctuation multiset check · journal chapter at landing) and when it applies (host-owned layout/restructure, no pack-version delta — row 239's other branch) | small | queued 2026-07-10 ~14:23 from the audit (door: feature; kind: skill; map: base checkpoint rule + build-pipeline's docs-only door + the restructure-safety proof already in the playbook; born-of: track-coach s63's improvised pass, audited APPROVE) | Done when: the pattern stands named in its homes red-proven, and the next host layout pass cites it instead of improvising |
| 241 | **The four guardrail checks ship as runnable code a host attaches** (the web session's read, relayed by Alexander 2026-07-10 ~14:22: Known issues admits the host-side checks ship as descriptions while the README's core claim is executable gates — the moat must be portable, «я бы закрывал это раньше всего остального»): the pack's own repo gates are executable today (push gate, lints, traceability — the analysts ran them); the unshipped half is the generic, host-attachable form of the four checks (completeness · tests-present · behaviour-traces-to-spec · conflicts) that today each host wires by hand from descriptions; build them as parameterized runnable checks + the adopt wiring that instantiates them per host, with the pack repo and one real host as the two proofs | large | **first leg landed 2026-07-10 ~17:34 (1.0.8): code shipped by the cloud worker red-first (22 tests), verified by deed at integration, the pack repo attached as its own first host (SURFACES.md + guardrails.config.json + pre-push gate h), Known-issues line retired**; open leg: one real EXTERNAL host attaches (track-coach's catch-up is the natural first) | Done when: the four checks run as shipped code on the pack repo AND on one real host from the adopt wiring, red-proven each, and the Known-issues entry leaves the list |
| 242 | **The README states the feels boundary as the method's own position** (the web session's third point, relayed ~14:22: the spec covers the formalizable, and "eighty-percent-finished everywhere" formalizes badly — that boundary is where formal thinking ends): one clean-writer paragraph in the why-section's critique block states the stance — the spec owns what can be written and tested; feel belongs to the owner's eye; the method's answer to taste is ROUTING, never fake formalization — the feel gate, the visitor walk, and the named-taste-defaults list exist to bring the right calls to the eye at the right moments; the portfolio critique stays cited as the case that taught it | small | queued 2026-07-10 ~14:24 (door: feature; kind: prose; map: README why-section + the clean-writer road INV-84; kin: rows 117/118's taste-defaults family) | Done when: the paragraph stands (clean-authored, lint-clean), and the critique block reads as claim → critique → stance, no dangling admission |
| 243 | **README ↔ the article series, linked both ways** (the web session's distribution point, relayed ~14:22: the funnel exists — Product Prover ↔ live-spec is linked, articles ↔ live-spec is not): when the Verifiable Spec series goes live, the why-section links it the same day; the other half rides the campaign (wish filed 2026-07-10: articles carry the repo link, campaign reports the URLs back by inbox) | small | queued 2026-07-10 ~14:24, BLOCKED on the campaign's publish — the block rides status reports (door: feature; kind: prose; map: README + the campaign's inbox wish promoter-alexander-live-spec/inbox/2026-07-10-from-live-spec-funnel-linkage.md) | Done when: both links live — each article names the repo, the README names the series |
| 244 | **Every skill and repo built with the method carries "made with live-spec" + a link** (Alexander 2026-07-10 ~16:27, his word: attribution in all skills, on GitHub and everywhere): the pack states ONE standard attribution line (wording + placement: README footer + SKILL.md, link to the pack repo), the publish step checks the line is present on anything leaving the machine, and each built-with project (track-coach first) receives a wish in its inbox to apply it in its own window | small | **landed 2026-07-10 ~16:39 (1.0.5, INV-96/M-225 red-proven; wording in the publish floor; first wish filed to track-coach inbox)** (door: feature; kind: prose+skill; map: publish skill + a pack template line; applications ride each project's own queue) | Done when: the standard line stands in the pack with its publish-step check red-proven, and the first project wish is filed |
| 245 | **The README's opening claim scopes itself to the shipped truth** (the web session's curator read, relayed ~16:27: "executable scripts gate the pipeline" reads wider than a host's out-of-box experience; the pack repo's own gates ARE executable — eight scripts blocked two commits today — but the four host-side checks ship as descriptions until row 241): the opening sentence names the boundary itself, via the clean-writer law; Known issues stays as is — it already tells the truth | small | **landed 2026-07-10 ~16:33** (door: docs-only; kind: prose; map: README opening paragraph; kin of row 241 which retires the boundary) | Done when: the reworded sentence is live on GitHub and the register lint passes it |
| 246 | **The standalone product-prover repo refreshes its shopfront** (his question 2026-07-10 ~16:47: «не нужно ли обновить ридми для прувера?» — checked: github.com/happysasha18/product-prover last pushed 2026-07-06, four days behind the pack's 1.0.x run): re-walk its README against today's truth (pack version references, the pair/sibling links, any claim drifted), sync the in-pack copy vs the standalone (one wording, two homes is a defect), and offer it the made-with line (our own repo — the offer is accepted by default, his word pending only if he objects) | small | **landed 2026-07-11 ~22:01** (the cloud branch verified by deed and SUPERSEDED — the fixes landed in the PACK, the mirror's one write channel: README bullets to truth, the sync script stamps the made-with line from the live VERSION (INV-96's self-mirror sentence added, M-225 extended, two pins + the floor-lockstep guard red-proven), prover record 2026-07-11-row246-mirror-attribution.md — 0 must-fix, all three findings folded; mirror synced at pack c506c8f, SKILL.md byte-identical to canon + footer; was: briefed to a manual cloud session 2026-07-10 ~17:59; door: docs-only; kind: prose) | Done when: the standalone README matches the shipped truth and the push lands green — MET (mirror commit 4ea6612, pack CI green) |
| 247 | **The inbox reaches over GitHub — the remote arm of the intake door** (Alexander 2026-07-10, thinking the cloud seat through mid-session: today's inbox law assumes a shared filesystem; a cloud seat, a routine, or another machine reaches a repo only through git): state the remote arm — the deposit stays ONE new file in inbox/, committed with the source in the message and PUSHED (the commit-touching-inbox-only exception already written); the grant model made explicit: the Claude environment links the owner's GitHub account once (his click — done 2026-07-10), each repo is granted to the app once (his click, the ask carries the exact settings path), the grant recorded in the host profile like the push grant; a seat with NO grant fails honestly — names what it lacks and hands the owner the one action (today's routine alert is the live example); DECIDE at landing: do GitHub Issues become the stranger's inbox door for public repos (no push rights needed), swept beside inbox/ | small | queued 2026-07-10 ~17:15, waits on the cloud-seat test's first real run for its facts (door: feature; kind: skill; map: inbox law + adopt/cloud guidance) | Done when: the remote arm stands in the inbox law red-proven, the grant ask is scripted with its exact path, and one real remote deposit landed |
| 248 | **Long-running work's progress is VISIBLE, unasked** (track-coach inbox on Alexander's word 2026-07-10 ~17:10: twice he lost the thread of a multi-minute background run — «где сюита? я не вижу агентов» — a detached background shell streams nothing and shows in no agent panel, so silence reads as lost): communicator's narration/heartbeat rule tightens for detached ops — any operation expected past ~2 minutes (background command OR subagent) surfaces a START line (what runs, log path, honest ETA), a beat every ~2 minutes or per stage, and a DONE digest; the background-vs-subagent visibility trap named in the rule; his stated indifference recorded — the mechanism is free, visibility is the requirement; complements the no-running-theater note (a timer is not work; real work is not silent) | small | **landed 2026-07-11 ~23:06, session 36** (door: feature; kind: skill; map: communicator heartbeat family INV-35/71/93 — the heartbeat clause tightened, no new surface; fences held: identity/digest/offline-window/leave-word clauses untouched, INV-93/95 intact — their tests green in the same run; architecture: assignment only, INV-35 stays communicator's; prove-architecture stood down by name (no structure change); facet sweep + design-sync stood down by name — skill kind, no visible surface; fit walk closed in-clause (trigger: a detached op expected past ~2 min · correction: START line + ~2-min/per-stage beats + DONE digest · not-fire: streamed foreground work under the ~10-min heartbeat, a waiting timer); non-goals: no mechanical monitor of the cadence (a candidate after field use), the mechanism stays free by his word; success measure [default]: the next multi-minute detached run reaches him as START + beats + DONE and no run reads as lost; delegation: the mechanical bundle (red-first test, 7 edits, suite runs) on a Sonnet worker, ~10 min senior saved — the worker HALTED correctly on two brief defects (the matrix cell cited the test FILE where the trace check wants function names; plugin.json's version copy sat outside its write-set), senior folded both, zero silent fixes; prover short form docs/prover/2026-07-11-row248-detached-visibility.md — 0 must-fix) — SPEC v1.0.4 INV-35 tightened (prose + index) · M-232 · tests/test_detached_work_visibility.py (4 tests) RED PROVEN then green · communicator 1.0.3 · pack 1.0.10 · suite 355 green | Done when: the tightened cadence stands in communicator red-proven, suite green — MET (rule 13's heartbeat bullet + SPEC prose/index; red 4F then green, full suite 355) |
| 249 | **The chat-laws reminder carries the no-scissors law** (promoter live-spec campaign inbox, his stop 2026-07-10 ~15:11: the naming-by-denial frame — «без воды», «старый вариант отменён» — slipped into chat; the reminded laws held, the unreminded one broke): one sentence joins the chat-law hook's output — say what a thing IS in its own sentence; the contrast/denial frame is banned in every text — the law's home stays the personal profile (language.no-scissors, his permanent 2026-07-07 word), the hook only reminds; the register self-scan checklist gains the same line where it exists | small | **landed 2026-07-11 ~23:12, session 36** (door: feature; kind: infra; map: scripts/chat-law-hook.sh + hook test — the reminder machinery, no new surface; fences held: the law's home stays the personal profile (language.no-scissors, his permanent 2026-07-07 word) — the hook only reminds, the row-141 boundary intact (M-133 'never legislating' green in the same run); the register self-scan already carries the line (communicator checklist item 9, the scissors scan) — verified, no edit owed; architecture: assignment only, the hook stays the guardrails node's chat hand; prove-architecture + facet sweep + design-sync stood down by name — infra kind, no visible surface; fit walk closed in-clause (trigger: every prompt on the working machine · correction: the say-what-it-IS reminder line · not-fire: the hook never legislates, docs keep their own register gates); non-goals: no mechanical scissors BLOCK in the hook (the lint scripts/spec-style-lint.py stays the mechanical floor for docs; chat stays self-scan + reminder — the chat-cannot-be-machine-gated fact stands); success measure [default]: the frame stops slipping into chat in reminded windows — the next slip is a recurrence to name; delegation: the mechanical bundle (red-first test edit, 7 edits, suite runs) on a Sonnet worker, ~8 min senior saved, zero brief defects, zero silent fixes; prover short form docs/prover/2026-07-11-row249-hook-no-scissors.md — 0 must-fix; installed-copy refresh stays HIS hand by M-133's never side: `! sh ~/live-spec/scripts/install-session-hooks.sh`) — SPEC v1.0.5 hook clause names all three reminded laws · M-233 · test_output_carries_the_no_scissors_law RED PROVEN then green · pack 1.0.11 · suite 356 green | Done when: the hook's output carries the line red-proven, suite green — MET (third echo line, run by deed; red 1F then green, full suite 356) |
| 250 | **The prover sweeps cross-cutting requirements across every surface** (tlvphoto inbox on his word 2026-07-10 ~10:38: site analytics covered SOME beats while whole surfaces emitted nothing — downloads, the polaroid room; he had to find it himself and expected the prover to have prescribed it; second same-shape gap the same morning — «open more» appends five works but the told story never refreshes, a composition no section owns): the spec names its declared cross-cutting laws in one place (measurement, accessibility, error handling — what the product declares); the prover walk gains one station — per declared law, enumerate every surface and transition and demand the law's clause or a dated exemption per item, a miss ranking as a broken invariant; spec-author's twin habit — a NEW surface's delta states its line against each declared law before the prover sees it | surface | queued 2026-07-10 ~17:16 from inbox (door: feature; kind: skill; map: product-prover walk + spec-author delta rules) | Done when: the station stands in the prover and the twin habit in spec-author, both red-proven, one real sweep run on a host spec |
| 251 | **The attach walk leads with the config, loudly** (the external gate check's observation 2026-07-10: a missing guardrails.config.json fails closed with an attach-me line — correct, but a first-time host can read the red as breakage): scaffold/guardrails/README.md's walk opens with "step 0: copy the example config — every check is red until you do, by design", and the no-config fix line points at that step; plus tomorrow's verification item — re-run the full suite on a FRESH full clone (the analyst's shallow-clone self-test noise, ruled out properly) | small | **landed 2026-07-11 ~22:01** (the walk opens with step 0 — copy the config, the pre-config red named as by-design; the no-config fix line points at step 0; the discovery-pattern bullet now says what a null disarms, citing the push-probe; test_walk_opens_with_the_config_step red-proven against the pre-edit walk, M-226; the fresh FULL clone ran 319/319 green with CI=true — the analyst's shallow-clone noise ruled out; was: queued 2026-07-10 ~18:24; door: docs-only; kind: prose) | Done when: the walk opens with the config step and a fresh-clone run reads green, recorded — MET |
| 252 | **Installs move to the agent's own hand — said aloud, never silent** (Alexander 2026-07-11 ~23:30, his word: full authorization — update everything yourself, push yourself, make the hooks yourself): the grant recorded as `trust.self-install` in the personal profile (playbook bb7f664) and executed the same minute (session hooks refreshed by the agent's hand); the wording sweep it owes: M-133's cell still names the installer the human's hand to run — update to both-hands-legal with the said-aloud duty, the silent-install ban standing; named per-host gates (track-coach push) stand until he names them | small | **landed 2026-07-11 ~23:54, session 36** (door: docs-only; kind: prose; map: TEST_MATRIX.md M-133 cell wording; no spec/matrix-coverage delta — the cell's prose only; the law's home is the profile, playbook bb7f664, recorded and executed the same minute the word was spoken) | Done when: M-133's cell states the granted hand, suite green — MET (suite 364 green) |
| 253 | **The chat-law hook reminds the routing law** (Alexander 2026-07-12 ~00:14 + ~00:20, his words: use opus/sonnet/haiku to the maximum where quality holds, protect Fable context — and asked what stops the recurring glitch: rules in prose lose to mid-turn momentum, the fix is a reminder at the decision moment, the clock hook's own proven pattern): one line joins the hook's output — the senior orchestrates and accepts, work routes to the cheapest sufficient tier, workers locate their own anchors; the law's homes stay base rule 5 + the routing rule (INV-69), the hook only reminds; his raise ~00:27: the duty is the ORCHESTRATOR'S, whatever model leads — an opus-led session routes down the same («неважно кто оркестратор, даже если опус» — paraphrased into the law: the routing duty binds the orchestrator seat, never one model) | small | **landed 2026-07-12 ~00:34, session 36 — the priority jump honoured, first of the three** (door: feature; kind: infra; map: scripts/chat-law-hook.sh + hook test + the spec's hook clause; fences held: the hook only reminds (M-133 never-side green), the law's homes stay base rule 5 + INV-69; the same word moved the harness: the project's LOCAL settings pin the next session's orchestrator to Opus and the loader's bootstrap line says so (playbook 2c6d4f7; the local pin gitignored — the public pack imposes no model on cloners); non-goals: no mechanical block in the hook, the reminder is the mechanism; success measure [default]: the routing line reaches every window's every prompt and the next non-delegated grunt stretch is visible the day it happens (row 254's check is the after-the-fact tooth); delegation: applied by a Sonnet worker, zero halts; prover short form docs/prover/2026-07-12-row253-routing-reminder.md — 0 must-fix) — SPEC v1.0.11 hook clause names the routing line · M-243 · test_output_carries_the_routing_law RED PROVEN then green · pack 1.0.17 · suite 378 green | Done when: the hook's output carries the routing line red-proven, suite green — MET (the hook prints the line, run by deed; red 1F then green, suite 378) |
| 254 | **The landing report's delegation line grows a mechanical check** (same exchange 2026-07-12 ~00:20: the second half of the fix — a check after the fact; the pipeline's own text says a session that never writes the delegation line quietly stopped delegating, and tonight showed prose alone does not hold): a landed queue row's status cell must carry its delegation accounting (what went to a worker or a stood-down line naming why the senior kept it); the conflicts/traceability layer gains the check red-proven on a synthetic landed row without the line; binds forward from its landing; the duty binds the orchestrator seat regardless of its model (his ~00:27 word) | small | queued 2026-07-12 ~00:21 on his word; **rides the 253-255 priority jump (~00:27)** (door: feature; kind: infra; map: guardrails/tests conflicts family + build-pipeline's delegation gate) | Done when: the check stands red-proven on a synthetic violation, the pack's own new landings pass it, suite green |
| 255 | **The drafter-applier pipeline enters build-pipeline as the standard colliding-rows form** (born live tonight 2026-07-12: law rows all touch the spec/matrix/version chain, so landings serialize — and the queue still moved at two-rows-an-hour once an opus drafter prepared row N+1 while the sonnet applier landed row N; Alexander's parallelism word made it standing): the trains law's pre-roll paragraph names the form — on colliding rows the penless DRAFT stage (an opus worker returns exact edit strings, self-verified needles) overlaps the current landing, the applier executes under the pen serially; landing order stays declared | small | queued 2026-07-12 ~00:21; **rides the 253-255 priority jump (~00:27)** (door: feature; kind: skill; map: build-pipeline trains/pre-roll section, cites T-18/INV-39/INV-49) | Done when: the form stands in the pipeline's text red-proven, suite green |
