# live-spec Roadmap (dated version: 2026-07-05 — updated at every edit; SPEC M-3)

The wish queue. Intake is continuous — a wish lands here the moment it is spoken. Execution is serial:
the current landing finishes before the next starts. Class = size (bug / small / surface / large — one
vocabulary with the spec) plus a priority mark when it isn't normal: a critical bug lands before
everything; a quick win may bubble up between landings, the jump marked in its row (SPEC T-11, INV-12).
A row also names its DOOR — feature · bug · refactor · docs-only · skip — said at intake, before any
code; feature tripwires outrank a casual label and queue-cutting stays with the bug door (SPEC T-12, INV-16).

Closed rows live in dated archives under docs/queue-archive/ (INV-1); this table holds the ACTIVE queue.

| # | Wish (plain words) | Class | Status | Decision / acceptance |
|---|---|---|---|---|
| 74 | A plain-language map of the whole structure (Alexander 2026-07-05 evening: "помоги навести порядок. чето структура странная очень выходит" — he can't hold where things live: pack vs profile vs playbook vs projects, base vs four working skills). One readable page: what each piece IS, where each kind of rule lives, one home per fact — in his words, no jargon; likely grows the README or a new OVERVIEW doc | small | built 2026-07-05 evening, session 7 (door: docs-only) — awaiting his read; closes on his word | Done when: Alexander reads it and says the structure is clear; every "where does X live" question in it answerable in one line |
| 12 | Mine CLAUDE.md + PLAYBOOK for rules not yet in the package skills — map rule → skill → gap, fold gaps (Alexander 2026-07-04) | large | mapping done, folding started | 45 rules mapped (22 full / 13 partial / 10 gap); 11 package-worthy gaps ranked in `~/.claude/playbook/claudemd_mining_s51.md` (moved from /tmp 2026-07-05 — a map quoting the private playbook stays out of the public repo). Folded: gaps 1+2 as one class (primary-source discipline: base rule 13 + build-pipeline reconcile sentence + prover meta-rule, skills → 0.1.1, 2026-07-05); gap 3 (fix-the-class sweep: base rule 14 + build-pipeline bug-path sentence + prover 3e sibling-instances lens; header rule-list + base pin swept across all four working skills, 2026-07-05 ~15:05 — unblocks row 52's flip); 8 gaps remain, one landing each |
| 14 | Custom-steps / CI adapter guidance — e.g. a host wires the guardrails into Jenkins/GitHub Actions as a second net (Alexander 2026-07-04) | small | queued | ADOPT/README section: same checks, CI mirror; one worked example |
| 27 | Name: "live-spec" (hyphen) instead of "livespec"? (Alexander floated it 2026-07-04) | small | **decided 2026-07-05** | Alexander picked **live-spec** (hyphen) on the decision page, over the keep-the-old-spelling recommendation; execution is row 40 (full one-name sweep + host migration note + GitHub rename at the reviewed push) |
| 33 | Is the playbook repo + CLAUDE.md symlink still needed, or obsolete now that live-spec exists? (Alexander 2026-07-05) | small | **decided 2026-07-05** | Alexander accepted the recommendation: keep as the thin PRIVATE personal layer for now; his caveat — this may overlap with live-spec's own personal settings, think with examples — moves to row 43, which owns the final split |
| 39 | Interactive decision page instead of an MD questionnaire: several open human picks render as ONE local HTML opened in the browser — radio options with the recommendation marked, a free-form field per question, a Download JSON button; the agent then reads the downloaded JSON from Downloads (Alexander 2026-07-05, from tuning images this way in tlvphoto) | surface · quick win | prototype landed 2026-07-05 | First real use same session; **full round-trip completed 2026-07-05 morning**: JSON read back, all five picks harvested (rows 20/27/33 closed, rows 40–46 opened), the JSON archived in `docs/decisions/`. Mechanism folded into communicator same day (rule 10 + fork-template pointer + `docs/decisions/` archive home + row 45's naming law), communicator 0.1.2 — **landed 2026-07-05** |
| 42 | Package structure design (from the decision-page answer on the base skill): Alexander leans ONE package (base inside), though prover already lives as a separate public repo too — design how the package and the standalone skill repos relate (single source? copies? install pulls?) and how live-spec loads/attaches other repos. Needs worked examples → a decision page | surface | **decided 2026-07-05** (page 2, 12:48) | Alexander picked **package-is-source** (recommended): the pack repo is the single source, standalone repos are mirrors. His note — reusable, self-contained parts (prover, maybe base, maybe all) must stay findable alone — is exactly what mirrors give; execution = row 51 (a mirror per skill + one sync command). D-4 closes |
| 43 | Personal-settings architecture (from the decision-page answer on the profile home): what lives in CLAUDE.md vs the playbook vs live-spec's own settings — Alexander's framing: "live-spec global personal settings" + "live-spec per-project settings"; absorbs row 33's overlap caveat and row 38's git-home debt. Needs worked examples → a decision page | surface | **decided 2026-07-05** (page 2, 12:48) | Alexander picked **all-into-profile** (over the three-homes recommendation): everything personal moves into live-spec's settings with servlet-like SCOPES — nested, inherited ("у каждой штуки свой скоуп"); CLAUDE.md becomes a thin loader. Plus a setup step: understand who you work with (profile bootstrap). Execution = rows 52 (migration) + 53 (scope model) + 54 (onboarding); D-5 closes as a direction, the design lands through those rows |
| 44 | Learn from other projects/frameworks — study how others run spec-driven / agent-driven development, harvest ideas worth folding (Alexander 2026-07-05: "поучиться у других проектов… но это отдельный version bump") | large | deferred — own version bump; HOMEWORK DONE 2026-07-05 evening | Two research reports in: `docs/research/2026-07-05-methods.md` (spec-kit/BMAD/Kiro/Shape Up/Google/Amazon — top-10 candidates incl. regression-fence sentences, waived-risk verdict, non-goals, facet curation) + `docs/research/2026-07-05-skill-patterns.md` (Anthropic/superpowers/Trail of Bits — top-10 incl. when-NOT-to-use sections, skill evals, loadability check). Candidates go to Alexander on a decision page 2026-07-05; his picks become queue rows. Revisit trigger unchanged: lands as its own MINOR |
| 47 | Feedback-collection skill — a generic pack skill for the human (or their users) to SAY, SHOW, or SEND something into the project at any moment (the intake side of communicator: decision pages go out, this brings feedback in; the inbox pattern generalized beyond wishes) (Alexander 2026-07-05: "шикарная вещь") | surface | queued — design after rows 40–43 | Done when: skill spec'd (channels: text, screenshot/artifact, file drop → each lands as a queue/inbox item with provenance + timestamp), proven, shipped in the pack |
| 48 | Maintenance skill with measurement PLUGINS — after shipping, the product is MEASURED: e.g. a Google Analytics plugin; each user story / canonical axis states how (or explicitly whether) it is measured in the field; findings feed back into the queue (Alexander 2026-07-05) | large | deferred — own version bump | Revisit trigger: first host with a live audience; design pairs with row 47 (feedback in → measurement in) |
| 49 | A/B tests as a pack capability — for software hosts (not every host: a book has none), a wish may land as an EXPERIMENT with two variants and a measured pick; builds on row 48's measurement layer (Alexander 2026-07-05) | large | deferred — own version bump | Revisit trigger: row 48 lands; spec must state where experiments live in the wish lifecycle |
| 54 | Onboarding step — "understand who you're working with": at attach (or first run on a new machine/human) the pack builds or loads the human's profile — the human tells about themselves and/or agrees to a data pull; nothing written without their word (INV-9 holds) (Alexander 2026-07-05) | surface | queued | Done when: ADOPT/bootstrap gains the step with a done-when, profile template exists, first real run recorded |
| 55 | (prover 2026-07-05-architecture, F1) Snapshot machinery gets its own queue row — SPEC S-0 promises every [target] is owned by a row, and the snapshot (E-7, A-6: baseline of the last accepted run, declared-scope diff) had none; design pairs with the guardrails scaffold (row 3) which consumes its diff | surface | queued | Done when: snapshot design spec'd (retention D-3 decided with it), baseline advance wired to *landed*, declared-scope diff feeds the guardrails |
| 56 | (prover 2026-07-05-architecture, F1 — same class as row 55) The model router (ACT-3 [target]: cheapest sufficient tier, budget-aware) had no owning queue row either; D-2 (who fixes the tier) closes with this design | large | deferred — after rows 52–54 | Done when: router rule spec'd (queue class → proposed tier, senior override logged per D-2), first real routed landing recorded |
| 57 | (prover 2026-07-05-architecture, F2+F3) Two mechanisms live in the pack but have no spec sentence: `install.sh` (how skills arrive on a machine — attach node carries it today via A-7's record clause) and the decision page (communicator rule 10 — backed today only by the batched-questions clause in the wish walk). Give each one sentence + anchor at the next spec touch | small | queued | Done when: SPEC names the installer and the decision page, each with an anchor; index updated; architecture owns-columns extended to the new anchors. Partial 2026-07-05 ~15:05: the hook-install half landed in E-6 — hooks OFFERED never imposed: git hosts only, installed only after asking with a plain-words explanation (Alexander's word this session); installer + decision-page sentences remain |
| 59 | Worker contract: a worker inherits its session's write-ownership narrowed to its brief's files; same-session sibling worker files are fence-benign; live session-scope lines pass into worker briefs; a result failing its brief's acceptance escalates one tier, logged | small | queued | Source: audit 2026-07-05 (fable F2, opus F-12, composition H2) |
| 60 | One name-collision law, stated once in the base skill: prefix/base name, then numeric suffix -2, -3 everywhere (attic second collision, inbox concurrent same-slug — add a per-session token if needed) | small | queued | Source: audit (fable F3, opus F-7, comp N6) |
| 61 | Pre-push gate freshness: the prover record must be newer than the last commit touching SPEC.md, not merely today-dated | small | queued | Source: audit (fable F4); lands in guardrails/pre-push + its tests |
| 62 | Bootstrap order + first landing: VCS gate FIRST, then templates + a minimal runnable suite scaffold; define green-suite for landing #1; hooks offered at bootstrap as at adoption | small | queued | Source: audit (fable F6, opus F-13) |
| 63 | Declining a wish lists its absorbed (superseded-into-it) rows; each declined by name or returned to the queue | small | queued | Source: audit (fable F8) |
| 64 | Mechanize S-0: traceability test — every [target] token maps to a non-landed queue row; a spec section whose architecture pin is real carries no [target] | small | queued | Source: audit (opus F-10) |
| 65 | Milestone audit item: the thin loader stays thin — every line passes the must-hold-before-pack-loads test, count stated in the audit report | small | queued | Source: audit (fable F7); pairs with row 52 |
| 66 | Freshness on the pack developer's own machine: repo skill copy vs installed copy — a named sync rule/script + the A-7 re-read | small | queued | Source: audit (composition H4) |
| 67 | Standalone skill installs ship template-less: spec-author and build-pipeline point at templates that don't resolve from the skill dir — decide ship-inside-skill vs fixed pointers | small | queued | Source: skill-creator eval 2026-07-05 |
| 68 | Communicator description over-triggers (fires on any progress report, no OFF-switch) — narrow it | small | queued | Source: skill-creator eval |
| 69 | product-prover SKILL.md: extract the category table + glossary into references/ per skill-creator sizing guidance | small | deferred | Source: skill-creator eval; deferred |
| 84 | Version 1.0 milestone (Alexander 2026-07-05 night: findings integrated + design-sync thought = 1.0, push overnight): full 3-pass preventive audit, terminal rows archived, README status refreshed, VERSION 1.0.0, gated push | large | queued 2026-07-05 night | Done when: audit records in docs/audit/, archive done, 1.0.0 pushed |
| 86 | The pack understands WHAT it is building (Alexander 2026-07-05 night, decision-page note: "скилл сет должен понимать с чем работает: делаем инфру или продукт, скилл или рассказ пишем — задействовать необходимые функционалы, не все всегда нужны"): a work-kind axis (product / infra / skill / prose…) that scales which pipeline parts apply | surface | queued 2026-07-05 night (door: feature; post-1.0 unless the night leaves room) | Done when: kind named at intake; each pipeline step says what it means per kind (or that it stands down), never silently skipped |
| 93 | Design-sync wiring (E-18 [target] machine): base default + host-profile switch vocabulary + communicator/verify channel lines + first real run on a visual host; lands with/after row 86 (work-kind axis) | surface | queued 2026-07-05 night (post-0.8 with row 86) | Done when: E-18 loses its [target]; a real host synced with the human's gate shown working |
| 94 | Skill-behavior evals (research adopt #6's third leg, superpowers "no skill without a failing test"): per skill, a scenario where a bare session errs and the skill corrects it; run at milestones | surface | queued 2026-07-05 night (post-0.8) | Done when: one eval per working skill, red proven without the skill |
