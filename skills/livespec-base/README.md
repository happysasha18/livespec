# livespec-base

The livespec pack's shared rulebook: the working rules every pack skill references (stated once, here) and the three-step settings ladder (package defaults → personal profile → host profile). Part of [livespec](https://github.com/happysasha18/livespec).
