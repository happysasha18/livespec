# livespec — NEXT_STEPS (resume file: LIVE STATE + queue only; history → JOURNAL.md)

## LIVE STATE (2026-07-05 midday)
PUBLIC: github.com/happysasha18/livespec — pushed today (4th push): **rows 29+34 landed as one
classification scheme** (SPEC v0.5: size = four words everywhere, queue column swept S/M → Class;
priority: critical lands first / quick win may bubble, one bubble then queue head / ambiguous is asked at
intake, T-11+INV-12) · **row 35 landed** (roadmap shown as icon bullet list — communicator rule 9, synced)
· prover CROSS-LINK record docs/prover/2026-07-05-classes.md (3 findings folded; the catch:
ROADMAP.template.md taught the old S/M/L — rewritten). JOURNAL 2026-07-05 (four entries).
Standing setup: one window = one project; **livespec = Fable only**.
OPEN for Alexander: row 27 naming (keep livespec — recommended) · row 33 playbook repo fate (keep as thin
private layer — recommended) · row 20's pick (adopt artifacts TRACKED — flip if he prefers).

## Forward queue (mirrors ROADMAP.md; priority marks live there)
1. Base skill ("Object class") every skill references (row 24) — design TOGETHER with row 36 (package
   defaults → global personal profile [language: docs-EN/chat-RU] → per-host override, all overrides
   recorded).
2. Fold the 11 CLAUDE.md-mining gaps into the skills (row 12) — via the pipeline, one at a time.
3. Prover-pass debts: queue archive at milestones (row 30) · version homes (row 31) · global profile path
   (row 32, feeds row 36).
4. Guardrails scaffold (row 3: + fence & push-gate mechanization) · CI-mirror example (row 14).
5. track-coach becomes the first formal adopt-host → test-suite rework by architecture node (pre-1.0) ·
   skill-creator eval pass (row 5).
