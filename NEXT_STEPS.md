# live-spec — NEXT_STEPS (resume file: LIVE STATE + queue only; history → JOURNAL.md)

## LIVE STATE (2026-07-06 ~13:32, session 13 — rows 125·123·124 landed, gate walked, pushed; report bounce → row 128; his closing words → rows 129·130; wipe-ready)
PUBLIC: github.com/happysasha18/live-spec. **Pack 0.8.34, SPEC v0.15.23, suite 110 green, all pushed;
after the push only queue/journal/ledger docs moved (rows 128 + stamp fixes — commit them).** Verify on
resume: `git status -sb` + the suite.
- **Row 125 LANDED (~12:42)**: the departures board names all NINE pipeline stops — prove architecture
  and commit & show got stations, landed = terminal state; class-swept (SPEC INV-27 · communicator ·
  M-112 · OVERVIEW · ARCHITECTURE; at the gate, one layer deeper: the pipeline's own order lines +
  README + ADOPT said "commit" for step 9 — five more sites folded).
- **Row 123 LANDED (~12:53)**: every worker brief carries the problem-ledger walk + the CLOCK read at
  briefing (SPEC ACT-3 + delegation gate, M-119). Dogfooded same hour: the row-124 and eval briefs
  carried both; no wrong-hour lead recurred. Gate fold: a sighted worker re-reads the machine clock,
  elapsed time never guessed (the row-123 worker had guessed +20 min, by deed).
- **Row 124 LANDED (~13:05)**: each pipeline step worked in its craft's head — the craft ladder, one
  home in build-pipeline (SPEC INV-33, M-120); prove-architecture bit correctly (INV-33 needed its
  owning node; worker HALTed, senior assigned + refreshed four shifted pins). Gate fold: the craft
  wears the KIND's face (prose code step = writer). **OPEN LEG (INV-26): the next landing's artifacts
  each name the hat they were made under — rides the next landing's report.**
- **PUSH GATE WALKED (~13:15)**: fresh prover record `docs/prover/2026-07-06-push-2.md` (morning FULL
  pass carries for unchanged bytes, three deltas walked fresh; F1–F3 all folded same hour). Eval
  re-runs E-19 for both behaviour-changed skills, both arms — records + scores in
  `docs/evals/2026-07-06-batch2-rerun/`.
- **Rows 126–128 BORN this sitting, all echoed to Alexander**: 126 — in the eval scenario the
  with-skill communicator put bookkeeping numbers ("64 checks green, v0.9.16") into the human's message
  a SECOND consecutive run → rule 8 gets an explicit NEVER-list; 127 — chat [HH:MM] stamps drifted
  ahead of the wall clock a second time (then a third, ~13:22, date appended on the owned entry) →
  communicator gains "a human-facing timestamp is read off the clock at write time, never extrapolated";
  128 — **the session's own closing report BOUNCED** («это ты на каком языке разговариваешь???»):
  it led with pack-internal names and calques, the jargon family's fourth strike and the first AFTER
  INV-28 landed → communicator gains a walked pre-report step: re-read the rules, pass every phrase
  through "does this stand for a reader outside the pack"; acceptance = the next movement-end report
  draws zero "а это что?". The report was re-given plainly in the same exchange.
- Versions: SPEC v0.15.23 · spec-author 0.1.13 · communicator 0.1.18 · build-pipeline 0.2.22 ·
  product-prover 0.1.9 · base 0.1.13 · publish 0.1.0 · pack 0.8.34. Installed copies synced after every
  bump this session (verify: version grep repo vs installed).
- Profile gained `chat.working-narration` on Alexander's word (~12:56): narrate work in progress in
  plain roadmap terms, report-voice; artifacts still shown only at milestones (playbook ba04046).
- ⟨DECIDE⟩ open: D-1/D-2/D-3 (none touched by this batch).

## Forward queue (mirrors ROADMAP.md — next session starts at item 1)
1. **First real FEATURE-FIT run**: the next product-kind wish in any adopted host walks INV-29/30 by
   deed — that first run is each family row's remaining acceptance evidence (also closes row 117's
   open leg and row 124's name-the-hats leg on its landing report).
2. Row 128 FIRST (the pre-report walk — highest human pain, just bounced) · row 126 (rule 8
   NEVER-list — bookkeeping never talks to the human) · row 127 (human-facing timestamps read at
   write time — the chat face of the invented-time family). Kin: consider landing 126+127+128 as one
   communicator sitting — three small clauses, one skill, one eval re-run.
3. Row 129 (a project knows its own KIND — book/backend/static-site/fullstack/custom — asked at
   founding, recorded in the host profile, re-checked as the project evolves; his word at session-13
   close) · row 130 (walk all six pack skills through skill-creator, fold findings, put the re-walk on
   the milestone list; same message).
4. Row 109 (prototype-norm pointer) · 110–115 (harvest picks; 110 adversarial verify strongest) ·
   106 (pytest-from-root bug).
5. Standing queue: rows 12 tail · 54 onboarding · 55 snapshot · 14 CI · 56 router · 47-49/96
   feedback family · track-coach formal adopt · tlvphoto lessons through one real feature.
6. Standing habits: read `date` BEFORE any stamp — chat included (drifted three times this session,
   owned by row 127; a queue stamp even tripped the commit fence once — fence works); reports obey
   INV-28 AND the row-128 lesson (before a movement-end report: re-read communicator, no pack name
   unexplained, no calques — the bounced report is the cautionary example); decision cards obey
   INV-32; communicator eval scenario still owes a taken-default beat so INV-31 can score honestly;
   inbox: the feel-pass file is ARCHIVED (removed in 9f59d88, rows 117/118 landed); the three
   2026-07-05 files ride until their rows land (109 open).
