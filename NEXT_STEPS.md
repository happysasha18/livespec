# live-spec — NEXT_STEPS (resume file: LIVE STATE + queue only; history → JOURNAL.md; ≤100 lines, INV-48)

## LIVE STATE (2026-07-16 ~01:20 — v2.0.0 SHIPPED, the readability + compaction release)
The 2.0.0 major is cut and green. It made the living documents read plainly and stay compact, and — the
point of the major — turned the cleanup into a machine so no project re-bloats. Resume COLD from JOURNAL.md
(the 2026-07-16 entry) + docs/prover/2026-07-15-2.0-movement.md + the compaction protocol.

**What landed (ROADMAP 336):**
- PRODUCT_SPEC register lint 154 → 0 (caps de-emphasised, defined vocabularies allowlisted; scissors,
  second person, reassurance, metaphor rewritten). Redundancy 21 → 0 by real dedup to one-home pointers,
  zero waivers (the debt cap forbids them). Meaning preserved: the spec-freeze anchor-occurrence map, a
  Fable dedup drafter, and two fresh pack-not-loaded adversarial reads (an LLM register/redundancy judge,
  0 surviving; an old-vs-new meaning-diff that caught 2 cross-document literals every mechanical gate
  missed — MET/OWED/FLAG and MIGRATION — both folded).
- The other docs: ROADMAP scrubbed to English (110 Russian quotes paraphrased); ARCHITECTURE/TEST_MATRIX
  genuine register defects fixed, their table vocabulary and changelog history left alone.
- The recurrence-stop: the live ratchet lock (test_convergence_locks, real spec at style 0 / redundancy 0),
  max_style_errors in spec-debt-cap.json (down-only), the freeze gate (pre-push gate k, guardrails/
  check-freeze.sh), and the method rule INV-164 / base rule 30 (a machine-verifiable quality is a gate,
  held by no attention; compaction every push). The nine skills' base-pin bumped to 1.0.19.
- Capstone folded the tlvphotos prover-miss as INV-165 — the design review's standing motion-parity lens
  (entry mirrors exit · every object type behaves alike · every position behaves alike), full chain + test.
- VERSION / plugin / spec-header → 2.0.0; MIGRATION 2.0.0 chapter (host action: none); done-gate GREEN;
  suite 820 green.

## ON RESUME — the queue after 2.0
- **Confirm the push landed** and the remote CI gate read green (the pushing session owns a red, INV-106).
- **The standalone mirrors** (product-prover, design-reviewer, etc.) re-sync from the pack on push
  (sync-mirrors.sh); INV-165 + rule 30 reach them there. Verify no mirror drifted.
- Open ROADMAP rows remain: 330, 331, 333, 334, 335 (harness/temp-hygiene + cross-workspace sweep family)
  and any inbox arrivals. None is blocked on 2.0.
- **Housekeeping (owner-side, not repo):** the global MEMORY.md index is > 19 KB (hook-flagged) — compact it.

## Standing word / OWNER-HELD
- Owner's 2026-07-15 word: do the whole movement solo with Fable, don't ask, push on green; plain English
  in docs, plain Russian in chat. Version bumps are not owner-reserved. All honoured.
- Memory can be wiped — the story is in JOURNAL.md + docs/prover/ + docs/restyle-repoint-log.md + here.
- Next free codes: INV-166, M-315, E-31, T-22 (INV-164, INV-165, M-313, M-314 used this release).
