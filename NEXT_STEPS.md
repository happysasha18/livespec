# live-spec — NEXT_STEPS (resume file: LIVE STATE + queue only; history → JOURNAL.md)

## LIVE STATE (2026-07-06 ~16:09, session 16 — rows 129+132 landed + обкатка checks green; WIPE-READY)
PUBLIC: github.com/happysasha18/live-spec. **Pack 0.8.37, SPEC v0.15.26, suite 116 green.** Verify on
resume: `git status -sb` + the suite. **Anthropic pulls FABLE from Claude Code 2026-07-07 (API-only
after; his word 2026-07-06 ~16:03, return expected); live-spec runs on Fable only (loader law) — if
resuming on another model, ASK Alexander before working this window. His direction: обкатка phase —
the tlvphoto window already exercises the pack live; the pack keeps exercising itself.**
- **Обкатка checks (his ask, ~16:05, all green)**: feature list spoken in chat = row 133's first
  informal run (the map reads off spec scenarios + the shipped-vs-target header, no third document);
  skill sizes all under the 500-line ideal (largest: product-prover 382 — row 69 pressure eased,
  evidence in row 130's cell); spec format laws hold (17 scenario sections lead, anchors trail,
  line-start codes are wrap artifacts only).
- **Rows 129+132 LANDED (~15:58) as one head — the product-self-knowledge family.** INV-36: a
  project knows its own KIND (asked at founding/orient, never profile-seeded, one home the host
  profile, updated on his word as the project evolves; the pack's own profile carries
  `project.kind: skill pack` by deed). INV-37: every wish is PLACED on the feature map at intake —
  spoken with the echo AND written in the row's `map:` note (changes X / new / restructure); the map
  = spec scenarios + architecture nodes, no third document; a restructure verdict queues its own row,
  re-carve only through the architecture step's re-prove. Prover CROSS-LINK record
  `docs/prover/2026-07-06-rows129-132.md`, 4 findings ALL folded in-pass. **Row 129 OPEN LEG
  (INV-26): the first real HOST carries a `project.kind` line by deed — rides the next adopted-host
  window.**
- **Row 131's open leg CLOSED by deed**: session 16 narrated unprompted from its first minute — no
  third ask. The success measure held.
- **Row 133 QUEUED (~15:58)**: «покажи все фичи» — the feature map readable on demand, transparency
  commands (his word 2026-07-06 ~15:52); map: NEW feature beside the departures board.
- **Row 134 QUEUED (~15:58)**: the chat clock gets a mechanical hand — the CHAT-variant ledger entry
  RE-OPENED as a METHOD defect on its named next-session recurrence (session 16: leads ~6 min fast
  again); owner: a harness hook injecting the wall clock into the reply (settings.json / update-config
  road; Alexander's `!` hand if the classifier blocks).
- Versions: SPEC v0.15.26 · spec-author 0.1.14 · communicator 0.1.21 · build-pipeline 0.2.23 ·
  product-prover 0.1.9 · base 0.1.14 · publish 0.1.0 · pack 0.8.37. Installed copies synced
  (verify: version grep repo vs installed).
- Push gate: fresh CROSS-LINK record rides this commit; the morning FULL pass
  (`2026-07-06-push-2.md`) carries for unchanged bytes.
- ⟨DECIDE⟩ open: D-1/D-2/D-3 (untouched).

## Forward queue (mirrors ROADMAP.md — next session starts at item 1)
1. **First real FEATURE-FIT run** (INV-29/30 acceptance evidence; closes row 117's open leg) and
   **row 129's open leg** (first real host `project.kind` line) — both ride the next product-kind
   wish in an adopted host window (track-coach / tlvphoto).
2. **Row 133** («покажи все фичи» — the map readable on demand): fresh, his transparency ask; then
   **row 130** (walk all six pack skills through skill-creator, fold findings, milestone re-walk item).
3. **Row 134** (clock hook — mechanical hand for the [HH:MM] lead; quick win if the hook proves
   simple) · row 109 (prototype-norm pointer) · 110–115 (harvest picks; 110 adversarial verify
   strongest) · 106 (pytest-from-root bug).
4. Standing queue: rows 12 tail · 54 onboarding · 55 snapshot · 14 CI · 56 router · 47-49/96
   feedback family · track-coach formal adopt · tlvphoto lessons through one real feature.
5. Standing habits: `date` at the start of EVERY reply, before any stamp (recurred session 16 →
   row 134 now owns the mechanical fix); narrate while working (INV-35 — held by deed session 16);
   every echo now speaks door · kind · name · row · PLACE ON THE MAP (INV-37) and every new row
   carries a `map:` note; movement-end reports walk the PRE-REPORT WALK (communicator); decision
   cards obey INV-32; communicator eval scenario still owes a taken-default beat so INV-31 can score
   honestly; eval WATCHED: station-name drift ("review" for "prove"), first occurrence 2026-07-06;
   inbox: the three 2026-07-05 files ride until their rows land (109 open).
