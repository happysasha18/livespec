# live-spec — NEXT_STEPS (resume file: LIVE STATE + queue only; history → JOURNAL.md)

## LIVE STATE (2026-07-06 ~20:52, session 19 — row 139 CLOSED WHOLE; rows 134+141 build legs MET,
ONE install command awaits HIS `!` hand; WIPE-READY after push)
PUBLIC: github.com/happysasha18/live-spec. **Pack 0.8.45, SPEC v0.15.33, suite 129 green.** Verify on
resume: `git status -sb` + the suite (from tests/, row 106's bug). **Anthropic pulls FABLE from Claude
Code 2026-07-07 (API-only after; return expected); live-spec runs on Fable only (loader law) — if
resuming on another model, ASK Alexander before working this window.**
- **Row 139 CLOSED WHOLE: working narration's three teeth (SPEC v0.15.31 INV-35 + communicator rule
  13 + M-124).** IDENTITY — every beat names the wish and station in hand (outside the pipeline the
  work's own name), and mends-or-builds; DIGEST — a station's completion is a beat whose line says
  what the station PRODUCED (a worker-closed station = the senior's beat on result arrival; digests
  never speak in counters, INV-28 seam held); HEARTBEAT — a beatless stretch past ~10 min [default]
  owes a line naming what grinds (offline-window face stays row 138's). Lived on its own landing's
  trail the same evening.
- **Rows 134+141 IN-WORK, builds MET, install pending — HIS ONE COMMAND:**
  `! bash ~/live-spec/scripts/install-session-hooks.sh`
  installs BOTH session hooks machine-wide: the wall-clock injection (INV-24's mechanical hand — the
  lead [HH:MM] stops being handwritten; the commit fence caught the hand AGAIN at this very landing,
  8th ledger catch) and the chat-laws reminder (INV-28/INV-35's voice — plain words talk, codes only
  trail, narrate with digests — into EVERY window, loaded pack or not). The classifier blocked the
  agent's self-config hand, exactly the road row 134's Done-when named. OPEN LEGS after install: a
  full zero-drift session (134; ledger CHAT entry flips SOLVED then) · a following tlvphoto session
  with no leading codes in his chat (141).
- **The tlvphoto 24h transcript audit (two reader agents, seven sessions) — what ordered this
  session's rows:** narration silence 12–70 min in EVERY session (one explicitly broken "continuing
  without pause" promise; «did something stall?» asked twice); the pipeline skills fully invoked in
  only 1 of 4 evening sessions — and THAT session landed a live bug same-session cleanly (the pack
  works when loaded); jargon leaked to him 3× across windows (raw plan codes twice, the «EX-SHARE …
  надгробие» line live during this session); the 13:48 outburst = voiced fixes lost across sessions;
  he had to demand a live-spec reload himself. Root cause named: the laws lived only in skills a
  session may never load → rows 134/141 deliver them mechanically at every prompt.
- **Row 140 QUEUED — next lane A: the economy ladder** (his word, echo given 20:30): what a tight
  budget may legally shed (selective test runs by touched node, CROSS-LINK vs FULL prover, cheaper
  worker tiers, batched landings) and what never bends (door law + tripwires, red-before-fix, his
  gates, the landing report). One home + never-bend list + suite row.
- His «только двойной параллелизм???» answered in the session report: the two-train cap is a tagged
  [default] (T-18), one word moves it; sub-lane parallelism (workers + read-only agents) is already
  uncapped and carried three simultaneous tracks this session.
- Versions: SPEC v0.15.33 · spec-author 0.1.15 · communicator 0.1.26 · build-pipeline 0.2.26 ·
  product-prover 0.1.10 · base 0.1.16 · publish 0.1.1 · pack 0.8.45. Installed copies synced.
- ⟨DECIDE⟩ open: D-1/D-2/D-3 (untouched).

## Forward queue (mirrors ROADMAP.md — next session starts at item 1)
1. **Row 140** (economy ladder — his 20:23 wish, echo given) · **rows 134+141 open legs** (after his
   install: watch a zero-drift session, watch the next tlvphoto session's language; flip the ledger).
2. **First real FEATURE-FIT run** (INV-29/30; row 117's open leg) + **row 129's open leg** (first real
   host `project.kind` line) — ride the next product-kind wish in an adopted host window. **Row 133's
   open leg** (first real «покажи все фичи» ask) rides HIS ask in any window.
3. **Row 138** (offline windows — "you may step away" narration; INV-35 family, fenced from 139's
   heartbeat by name) · row 109 (prototype-norm pointer) · 110–115 (harvest picks) · 106
   (pytest-from-root bug).
4. Standing queue: rows 12 tail · 54 onboarding · 55 snapshot · 14 CI · 56 router · 47-49/96 feedback
   family · track-coach formal adopt · tlvphoto lessons through one real feature.
5. Standing habits: `date` at the start of EVERY reply until the clock hook is installed; narrate by
   the three teeth (INV-35 — identity · digest · heartbeat); every echo speaks door · kind · name ·
   row · place on the map (INV-37) and every new row carries a `map:` note; movement-end reports walk
   the PRE-REPORT WALK (communicator); decision cards obey INV-32; communicator eval scenario still
   owes a taken-default beat (INV-31); eval WATCHED: station-name drift ("review" for "prove"), first
   occurrence 2026-07-06; inbox: the three 2026-07-05 files ride until their rows land (109 open).
