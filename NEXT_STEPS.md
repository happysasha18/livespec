# live-spec — NEXT_STEPS (resume file: LIVE STATE + queue only; history → JOURNAL.md)

## LIVE STATE (2026-07-06 ~17:21, session 18 — row 135 landed pack-side; WIPE-READY after push)
PUBLIC: github.com/happysasha18/live-spec. **Pack 0.8.40, SPEC v0.15.29, suite 120 green.** Verify on
resume: `git status -sb` + the suite (from tests/, row 106's bug). **Anthropic pulls FABLE from Claude
Code 2026-07-07 (API-only after; return expected); live-spec runs on Fable only (loader law) — if
resuming on another model, ASK Alexander before working this window.**
- **Row 135 LANDED pack-side (~17:20): parallel lanes — two trains, one pen.** SPEC v0.15.29: T-18
  (one session may roll TWO independent build lanes; penless stages overlap — the second train's code
  and tests in an ISOLATED tree only, read-only analysis free; pen-stages serialize — every shared
  doc, integration, row close; opening narrated; board shows both trains, the waiting lane names whom
  it waits behind; cross-lane questions one batched page, cards name their rows; max two lanes
  [default]; never across sessions / dependent wishes / mid-milestone) + INV-39 (a landing commit
  carries exactly one row's delta, gate on a clean tree; landed-first wins, second re-verifies).
  INV-2 re-authored to the pen (atomic claim + foreign back-off kept whole); T-9 now parks per-lane;
  ACT-3 gains the isolated-tree road. Prover CROSS-LINK `docs/prover/2026-07-06-row135.md` — 7
  findings, ALL folded in-pass. Carried by build-pipeline 0.2.25 (gate "Two trains, one pen") + base
  0.1.16 (rule 7) + communicator 0.1.24 (rule 9 waiting-lane face). M-129/M-130 born, M-022/M-019
  re-derived (in-work cap now ≤2: `test_roadmap_in_work_cap`). **OPEN LEG (INV-26): the first real
  double-lane run lands clean with its board readable — rides the next pair of independent wishes,
  any window.** Delegation: full mechanical batch → one Sonnet worker (~25 min senior saved).
- **Row 133's OPEN LEG still rides**: the first real post-law «покажи все фичи» ask — HIS next ask.
- ⟨DECIDE⟩ open: D-1/D-2/D-3 (untouched).

## Forward queue (mirrors ROADMAP.md — next session starts at item 1)
1. **First real FEATURE-FIT run** (INV-29/30; row 117's open leg) + **row 129's open leg**
   (first real host `project.kind` line) — ride the next product-kind wish in an adopted host window
   (track-coach / tlvphoto). **Row 133's open leg** (first real «покажи все фичи» ask) rides HIS ask
   in any window. **Row 135's open leg** (first real double-lane run, board readable) rides the next
   pair of independent wishes — rows 136 + 137 are candidates (disjoint: infra vs skill-form).
2. **Row 136** (pack self-update check from GitHub, daily, propose-never-install) · **row 134**
   (clock hook — the mechanical hand; two more future-time catches this session say do it soon) ·
   **row 137** (dense-rule scanability) · row 109 (prototype-norm pointer) · 110–115 (harvest picks)
   · 106 (pytest-from-root bug).
3. Standing queue: rows 12 tail · 54 onboarding · 55 snapshot · 14 CI · 56 router · 47-49/96
   feedback family · track-coach formal adopt · tlvphoto lessons through one real feature.
4. Standing habits: `date` at the start of EVERY reply, before any stamp (row 134 owns the mechanical
   fix; THREE more hand-catches this session); narrate while working (INV-35); every echo speaks door ·
   kind · name · row · place on the map (INV-37) and every new row carries a `map:` note;
   movement-end reports walk the PRE-REPORT WALK (communicator); decision cards obey INV-32;
   communicator eval scenario still owes a taken-default beat (INV-31); eval WATCHED: station-name
   drift ("review" for "prove"), first occurrence 2026-07-06; inbox: the three 2026-07-05 files ride
   until their rows land (109 open).
