# live-spec — NEXT_STEPS (resume file: LIVE STATE + queue only; history → JOURNAL.md)

## LIVE STATE (2026-07-06 ~18:12, session 18 — rows 135+136+137 CLOSED WHOLE, the first double-lane
run lived; WIPE-READY after push)
PUBLIC: github.com/happysasha18/live-spec. **Pack 0.8.42, SPEC v0.15.30, suite 122 green.** Verify on
resume: `git status -sb` + the suite (from tests/, row 106's bug). **Anthropic pulls FABLE from Claude
Code 2026-07-07 (API-only after; return expected); live-spec runs on Fable only (loader law) — if
resuming on another model, ASK Alexander before working this window.**
- **Row 135 CLOSED WHOLE: parallel lanes — two trains, one pen (SPEC v0.15.29 T-18+INV-39).** Law:
  one session may roll TWO independent build lanes (max two [default]); the second train builds ONLY
  in an isolated tree; pen-stages serialize (every shared doc, integration, row close); board shows
  both trains, waiting lane names whom it waits behind; a landing commit carries exactly one row's
  delta, landed-first wins, second re-verifies on the new truth. INV-2 re-authored (atomic claim +
  foreign back-off whole), T-9 parks per-lane, ACT-3 isolated-tree road. Carried by build-pipeline +
  base + communicator. Final leg MET by the run below.
- **The FIRST double-lane run happened and both trains landed clean (T-18/INV-39 lived as written):**
  lane A = row 136 (pack update check, SPEC v0.15.30 E-25: daily stamp, propose-never-install,
  forward-only, honest offline skip naming the address; `scripts/check-pack-update.sh`, attach owns,
  M-131, red-proven, live run "up to date"); lane B = row 137 (dense rules → lead-line +
  sub-points: build-pipeline step-zero + communicator rules 6/8/9/10/11, zero words moved,
  token-equality verified by the worker, pins verbatim; built in an isolated worktree by a Sonnet
  worker WHILE lane A held the pen, integrated after A landed, gate re-run on the new truth; walk
  findings 6+11 folded). Workshop find, fixed by class: worker worktrees live at
  `.claude/worktrees/` inside the repo — gitignored so no train's tree rides a landing.
- **Row 138 QUEUED**: offline windows — narration says when he may step away and for roughly how
  long, and when he's needed back (his word ~17:26; communicator/INV-35 family).
- **Clock fence caught the hand TWICE more this session (4th + 5th catches)** — 17:22@17:21 and
  18:21/18:22@18:11; row 134 (mechanical clock hook) now carries five catches in two days: do it next.
- **Row 133's OPEN LEG still rides**: the first real post-law «покажи все фичи» ask — HIS next ask.
- Versions: SPEC v0.15.30 · spec-author 0.1.15 · communicator 0.1.25 · build-pipeline 0.2.26 ·
  product-prover 0.1.10 · base 0.1.16 · publish 0.1.1 · pack 0.8.42. Installed copies synced.
- ⟨DECIDE⟩ open: D-1/D-2/D-3 (untouched).

## Forward queue (mirrors ROADMAP.md — next session starts at item 1)
1. **First real FEATURE-FIT run** (INV-29/30; row 117's open leg) + **row 129's open leg**
   (first real host `project.kind` line) — ride the next product-kind wish in an adopted host window
   (track-coach / tlvphoto). **Row 133's open leg** (first real «покажи все фичи» ask) rides HIS ask
   in any window.
2. **Row 134** (clock hook — the mechanical hand; FIVE catches in two days, the newest two this very
   session: do it next) · **row 138** (offline windows — "you may step away" narration) · row 109
   (prototype-norm pointer) · 110–115 (harvest picks) · 106 (pytest-from-root bug).
3. Standing queue: rows 12 tail · 54 onboarding · 55 snapshot · 14 CI · 56 router · 47-49/96
   feedback family · track-coach formal adopt · tlvphoto lessons through one real feature.
4. Standing habits: `date` at the start of EVERY reply, before any stamp (row 134 owns the mechanical
   fix; THREE more hand-catches this session); narrate while working (INV-35); every echo speaks door ·
   kind · name · row · place on the map (INV-37) and every new row carries a `map:` note;
   movement-end reports walk the PRE-REPORT WALK (communicator); decision cards obey INV-32;
   communicator eval scenario still owes a taken-default beat (INV-31); eval WATCHED: station-name
   drift ("review" for "prove"), first occurrence 2026-07-06; inbox: the three 2026-07-05 files ride
   until their rows land (109 open).
