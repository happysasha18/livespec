# live-spec — NEXT_STEPS (resume file: LIVE STATE + queue only; history → JOURNAL.md; ≤100 lines, INV-48)

## LIVE STATE (2026-07-16 afternoon — v2.1.0 SHIPPED)
VERSION 2.1.0 pushed to origin (`fed1339`), all pre-push gates green, suite 907 green. The whole
2026-07-16 queue is CLOSED: rows 337-356 landed (the Fable prover-audit folds INV-168..171 + six point
fixes, the turnkey ratchet-adoption kit INV-172, canonical hooks INV-173, the local inbox arm INV-174,
config-health INV-175, retroactive gates INV-176, the update watcher INV-177, version-is-one-fact
INV-178, collector anonymization INV-179, the seat law sharpened, base scope pinned, README gate list
current). The 2.1.0 MINOR gate ran three independent passes (full prover with the first INV-171 verdict
table · full design review · batch adversarial audit); every defect folded same-day (row 355), leftovers
queued by name (row 356). Records: docs/prover/2026-07-16-full-2p1p0.md,
docs/design-review/2026-07-16-full-2p1p0.md, docs/audit/2026-07-16-batch-2p1p0.md.

## ON RESUME — the queue
- **Row 356 — gate-pass leftovers, queued by name:** refresh ARCHITECTURE pin caches; name the
  FEATURE-FIT record home; the muted-gate extensionless-shebang sweep; a one-command scaffold-kit
  installer (so a scaffold-only host gains the manifest INV-177 reads); declare the installed-copy
  drift-net class.
- **Row 354 — ARCHITECTURE register debt (pre-existing):** 3 universal-tier hits (two possibly lint
  noise on flow-table arrows) + 22 register warnings; judge, fold or waive, and decide whether
  ARCHITECTURE.md joins the convergence-lock floor.
- **OWNER-HELD (policy — an outward-facing reply to a stranger is his call; one batched question, design review 2026-07-16, recommended default attached):** should
  the stranger arm carry the capture echo back to its source Issue (posted at harvest, Issue closed on a
  terminal exit — the recommended default), or is the stranger door intake-only by intent? Held on the
  design-review record; nothing blocks on it.
- **Adoption follow-through (his windows):** the three "go" wishes are dropped
  (track-coach / tlvphotos / promoter inbox, 2026-07-16); each window runs install-ratchet on its own
  tree. The live-spec window stays audit-only there.

## Standing word / OWNER-HELD
- Do the whole movement solo, push on green; plain English in docs, plain Russian in chat. Gates
  mandatory everywhere.
- CONCURRENCY: multiple windows share ~/live-spec. Commit narrowly by explicit path, never `git add -A`;
  re-check HEAD before writing (fence). A co-located deposit is the FILE ALONE — no staging, no commit
  (INV-174); the commit-time fence also stops a staged file carrying unstaged edits (INV-175).
- Next free codes: INV-180, M-336, next ROADMAP row 357. Memory can be wiped — story in JOURNAL.md +
  the three 2026-07-16 gate records.
