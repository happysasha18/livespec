# livespec — NEXT_STEPS (resume file: LIVE STATE + queue only; history → JOURNAL.md)

## LIVE STATE (2026-07-05 morning, session 2)
PUBLIC: github.com/happysasha18/livespec. **Row 39 prototype landed: the interactive DECISION PAGE**
(Alexander's idea — radio options + notes + Download JSON in the browser instead of an MD questionnaire) —
first real use is live NOW: the five standing open picks are on a page in his Chrome window
(`scratchpad/livespec-decisions-2026-07-05.html`); when `~/Downloads/livespec-decisions-2026-07-05.json`
appears, fold the picks into the queue rows. **Row 12 folding started: gaps 1+2 landed as one class** —
primary-source discipline = base rule 13 + build-pipeline reconcile sentence + prover meta-rule; base bump
0.1.0 → 0.1.1 swept all four inherit-note pins same session (E-12); package VERSION 0.1.1; mining map moved
to `~/.claude/playbook/claudemd_mining_s51.md` (private — it quotes the playbook). Installed copies synced
after commit. PUSH HELD until Alexander reviews (then: fresh prover pass first — every-push gate, M-6).
Standing: one window = one project; **livespec = Fable only**.
WAITING on Alexander: the decision-page JSON (5 picks: base folder name D-4 · profile home D-5 · name
row 27 · playbook fate row 33 · adopt artifacts row 20 — recommendations marked on the page).

## Forward queue (mirrors ROADMAP.md; priority marks live there)
1. Fold the remaining 9 mined gaps (row 12) — one landing each, next up: gap 3 "fix the class, sweep
   look-alikes" (build-pipeline guardrail + prover lens).
2. Fold the decision-page mechanism into communicator (row 39: fork-template update + rule sentence).
3. Prover debts: queue archive at milestones (row 30) · personal-profile git home (row 38).
4. Guardrails scaffold (row 3: + fence & push-gate mechanization) · CI-mirror example (row 14).
5. track-coach becomes the first formal adopt-host → test-suite rework by architecture node (pre-1.0) ·
   skill-creator eval pass (row 5).
