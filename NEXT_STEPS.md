# live-spec — NEXT_STEPS (resume file: LIVE STATE + queue only; history → JOURNAL.md)

## LIVE STATE (2026-07-06 ~21:28, session 20 — rows 142 landed whole + 140 build legs MET; rows
143+144 queued; the tlvphoto urgent wish filed; WIPE-READY after push)
PUBLIC: github.com/happysasha18/live-spec. **Pack 0.8.47, SPEC v0.15.35, suite 130 green.** Verify on
resume: `git status -sb` + the suite (from tests/, row 106's bug). **Anthropic pulls FABLE from Claude
Code 2026-07-07 (API-only after; return expected); live-spec runs on Fable only (loader law) — if
resuming on another model, ASK Alexander before working this window.**
- **Row 142 LANDED WHOLE ~21:17: the parallel-lanes cap moved on his word** (his ~21:04 message — no
  hard two-train default). Up to THREE independent lanes roll without asking [default]; a FOURTH opens
  only on his asked word, never silently (SPEC v0.15.34→35 T-18/INV-39; M-022 now takes its number
  FROM T-18 — one home; base 0.1.18 / build-pipeline 0.2.27 / communicator 0.1.27, installed copies
  synced; prover record `docs/prover/2026-07-06-row142.md`).
- **Row 140 IN-WORK, build legs MET ~21:26: the economy ladder.** SPEC section "When money or time run
  short" — `budget.pressure` full [default] · lean · tight, moved ONLY by his word; lean = node-scoped
  mid-work test runs + CROSS-LINK with FULL deferrable as dated debt + one-tier-cheaper workers;
  tight = + batched landing gates (one-delta commits kept, batch-end red bisects by landing order,
  push still full-green at HEAD) + cheapest sufficient tier; the never-bend list (door law,
  red-before-fix, his gates, the report with NAMED sheds, landing purity, push gate, safety net,
  narration; an explicit host line outlives any rung) — T-19/INV-40, base-rulebook node, M-134/M-135,
  `test_economy_ladder`, prover record `2026-07-06-row140.md`. OPEN leg: the first session he names a
  rung for — it must name its sheds aloud in the landing report.
- **Row 143 QUEUED: the architecture step owes measurable quality criteria** (his ~21:03 tlvphoto
  outburst — the Room's first image loaded long, no loader, no timing numbers, and the architecture
  named no budgets: "фиговая архитектура прописана если так"). The pack half: architecture step +
  spec-author's performance facet must produce measurable budgets + an instrumentation home, acceptance
  asserts them. **The tlvphoto half rides `~/tlvphoto/inbox/2026-07-06-room-first-image-slow-plus-timings.md`
  — marked URGENT, above that queue's order: measure first (network vs own delay), loader (minimalist,
  solemn — mockup-first) OR cut the delay ~3×, plus standing timing instrumentation with an export he
  can see.**
- **Row 144 QUEUED: the task list on his screen speaks plain words** (his ~21:22 screenshot — the list
  helps, if only it spoke understandable words). The harness task list is a language-law surface
  (INV-28) nothing names; communicator's board family gets the sentence. Applied live already: open
  tasks renamed to plain Russian this session.
- **Rows 134+141 IN-WORK, builds MET, install pending — HIS ONE COMMAND:**
  `! bash ~/live-spec/scripts/install-session-hooks.sh`
  installs BOTH session hooks machine-wide (wall-clock injection + chat-laws reminder). The commit
  fence caught the invented-time hand AGAIN this session (ninth ledger catch, row-142 landing stamps).
  OPEN LEGS after install: a full zero-drift session (134) · a following tlvphoto session with no
  leading codes in his chat (141).
- Versions: SPEC v0.15.35 · spec-author 0.1.15 · communicator 0.1.27 · build-pipeline 0.2.27 ·
  product-prover 0.1.10 · base 0.1.18 · publish 0.1.1 · pack 0.8.47. Installed copies synced.
- Push gate for this push: `docs/prover/2026-07-06-pushgate-s20.md` (whole-spec walk, no new findings).
- ⟨DECIDE⟩ open: D-1/D-2/D-3 (untouched; D-2 cited by the ladder, not resolved).

## Forward queue (mirrors ROADMAP.md — next session starts at item 1)
1. **Row 143** (architecture owes measurable quality criteria — his priority pain, the tlvphoto lesson) ·
   **row 144** (task list plain words — small, board family).
2. **Rows 134+141 open legs** (after his install: watch a zero-drift session, watch the next tlvphoto
   session's language; flip the ledger) · **row 140's field leg** (first budget-named session names its
   rung and sheds).
3. **First real FEATURE-FIT run** (INV-29/30; row 117's open leg) + **row 129's open leg** (first real
   host `project.kind` line) — ride the next product-kind wish in an adopted host window. **Row 133's
   open leg** (first real «покажи все фичи» ask) rides HIS ask in any window.
4. **Row 138** (offline windows — "you may step away" narration; INV-35 family) · row 109
   (prototype-norm pointer) · 110–115 (harvest picks) · 106 (pytest-from-root bug).
5. Standing queue: rows 12 tail · 54 onboarding · 55 snapshot · 14 CI · 56 router · 47-49/96 feedback
   family · track-coach formal adopt · tlvphoto lessons through one real feature.
6. Standing habits: `date` at the start of EVERY reply until the clock hook is installed; narrate by
   the three teeth (INV-35 — identity · digest · heartbeat); task-list subjects in plain chat-language
   words (row 144's rule, applied ahead of its landing); every echo speaks door · kind · name · row ·
   place on the map (INV-37) and every new row carries a `map:` note; movement-end reports walk the
   PRE-REPORT WALK (communicator); decision cards obey INV-32; communicator eval scenario still owes a
   taken-default beat (INV-31); eval WATCHED: station-name drift ("review" for "prove"), first
   occurrence 2026-07-06; inbox: the three 2026-07-05 files ride until their rows land (109 open).
