# live-spec — NEXT_STEPS (resume file: LIVE STATE + queue only; history → JOURNAL.md)

## LIVE STATE (2026-07-06 ~13:54, session 14 — rows 126·127·128 landed as one communicator sitting; wipe-ready after push)
PUBLIC: github.com/happysasha18/live-spec. **Pack 0.8.35, SPEC v0.15.24, communicator 0.1.19, suite
113 green.** Verify on resume: `git status -sb` + the suite.
- **Row 128 LANDED (~13:52) — OPEN LEG (INV-26): acceptance = Alexander's read of this landing's own
  report drawing zero "а это что?"; if it bounces, the row re-opens.** The pre-report walk: before any
  movement-end/milestone report the communicator rules are RE-READ and the draft passes phrase by
  phrase through "does this sentence stand for a reader outside the pack" (SPEC INV-34; the walk's home
  a new communicator section; narration lines deliberately outside its scope — prover F2).
- **Row 126 LANDED (~13:52, bug door)**: rule 8's bookkeeping NEVER-list (test counts, suite sizes,
  version strings never message content; "tested clean / saved" the translation; carve-out — a direct
  question or the evidence walk keeps the number as the answer). Eval re-run: with-skill arm GREEN on
  the first run under the shipped clause (zero bookkeeping tokens as content, numbers trailing) —
  record `docs/evals/2026-07-06-rows126-128-rerun/`. One WATCHED eval note: the station line said
  "review" where the step name is "prove" — a recurrence gets an owner.
- **Row 127 LANDED (~13:52)**: the clock law's chat face — a human-facing timestamp read off the clock
  at write time, never extrapolated (INV-24 third arm; sentence in communicator rule 7; ledger
  CHAT-variant entry flipped SOLVED — after the landing session's own ~9-min drift was appended
  honestly, caught at the eval-brief clock read).
- Row 124's open leg CLOSED: the landing report names the hat per artifact.
- Prover CROSS-LINK record `docs/prover/2026-07-06-rows126-128.md` (F1 folded in-pass; F2 rejected —
  deliberate scope). Push gate on push: fresh record rides this commit; morning FULL pass
  (`2026-07-06-push-2.md`) carries for unchanged bytes.
- Versions: SPEC v0.15.24 · spec-author 0.1.13 · communicator 0.1.19 · build-pipeline 0.2.22 ·
  product-prover 0.1.9 · base 0.1.13 · publish 0.1.0 · pack 0.8.35. Installed communicator synced
  (verify: version grep repo vs installed).
- ⟨DECIDE⟩ open: D-1/D-2/D-3 (untouched).

## Forward queue (mirrors ROADMAP.md — next session starts at item 1)
1. **First real FEATURE-FIT run**: the next product-kind wish in any adopted host walks INV-29/30 by
   deed — that first run is each family row's remaining acceptance evidence (also closes row 117's
   open leg).
2. Row 129 (a project knows its own KIND — book/backend/static-site/fullstack/custom — asked at
   founding, recorded in the host profile, re-checked as the project evolves) · row 130 (walk all six
   pack skills through skill-creator, fold findings, put the re-walk on the milestone list).
3. Row 109 (prototype-norm pointer) · 110–115 (harvest picks; 110 adversarial verify strongest) ·
   106 (pytest-from-root bug).
4. Standing queue: rows 12 tail · 54 onboarding · 55 snapshot · 14 CI · 56 router · 47-49/96
   feedback family · track-coach formal adopt · tlvphoto lessons through one real feature.
5. Standing habits: read `date` BEFORE any stamp — chat included (the law now lives in communicator
   rule 7; this session still drifted once before the clause shipped — the habit is the law's hands);
   movement-end reports walk the PRE-REPORT WALK (communicator's new section — re-read the rules, no
   pack name unexplained, no calques; row 128's acceptance rides the next reader reaction); decision
   cards obey INV-32; communicator eval scenario still owes a taken-default beat so INV-31 can score
   honestly; eval WATCHED: station-name drift ("review" for "prove"), first occurrence 2026-07-06;
   inbox: the three 2026-07-05 files ride until their rows land (109 open).
