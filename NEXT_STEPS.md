# live-spec — NEXT_STEPS (resume file: LIVE STATE + queue only; history → JOURNAL.md; ≤100 lines, INV-48)

## LIVE STATE (2026-07-17 ~14:23 — v2.6.0 SHIPPED, PUSHED, CI GREEN)

Two commits pushed: `3774685` (v2.6.0, the agent-communication stage) and `4823f17` (the prover's twelve
defects folded + the research record). All fourteen gates green locally; the CI run this push triggered
came back success, read at 14:22. The playbook repo is also pushed (`71fec48`). **Memory may be wiped —
the story is JOURNAL.md's v2.6.0 chapter, and this file is the whole live state.**

**What shipped.** The layer: two channels and no third, a card per agent with the roster in the owner's
profile, a message that names its birth (blocked work · a lived fault with its evidence · a concern no
zone owns), a referral that travels back to the asker and never forward to the zone's owner, default-deny
on every contract field, non-duplication, proposal authority, the birth walk. Plus base rule 31, a live
gate (`guardrails/check-earned-message.py`, gate n), the pack's own card, a template, 100 tests.
Anchors E-31..E-33, INV-182..INV-197, T-22. INV-153 grew from three controls to four.

**Read JOURNAL.md's v2.6.0 chapter for the day's real lesson** — it is the through-line and the thing a
resuming session most needs: a written law with no machine is a wish, and a machine looking in the wrong
place is the same as no machine. Four instances in one afternoon, each proven on this tree.

## ON RESUME — in order

1. **HIS RANKING, in his words: parallel lanes with branches and worktrees (row 386) matter MORE than the
   communication layer, and they wait until the layer has run in the field.** Row 396 rides with it.
2. **The field run is what he asked for.** The layer ships; the projects must now use it. Each window
   writes its own card at its own catch-up, and the roster rows land on his ratification. The major
   version (3.0) waits on what the field breaks — his word: this was an ordinary release.

## Standing word / OWNER-HELD
- The whole movement solo, push on green; plain English in docs, plain Russian in chat. Gates mandatory.
- **The roster needs his ratification — the human-only fact is POLICY: who counts as a standing agent on
  his machine is his act, and a tree's presence on a filesystem grants nothing [INV-152].** The machine's
  real trees, swept 2026-07-17 (facts, not a proposal): tlvphotos (instance of exhibition-engine, live) ·
  exhibition-engine (engine, live) · promoter (engine, live) · promoter-alexander (instance, hub of five
  campaign spokes, quiet since 07-11) · the REAL track-coach is `~/.claude/skills/track-coach` (the most
  active tree on the machine; `~/track-coach` is an inbox-only stub with no git) · tc-cloud-validate is a
  frozen 11-minute clone carrying an unmerged branch. Ambiguous by their own records and his to settle:
  exhibition-engine's `project.kind`, whether `~/track-coach` is a deliberate inbox mount, whether
  tc-cloud-validate is disposable.
- CONCURRENCY: multiple windows share ~/live-spec. Commit narrowly by explicit path, never `git add -A`;
  re-check HEAD before writing (fence). A co-located deposit is the FILE ALONE (INV-174, INV-175).
- **The clock is read, never continued.** This session stamped its own docs two hours ahead by
  extrapolating between prompts; the commit fence caught it. Read the clock at each prompt (INV-24).
- Next free codes: INV-198, E-34, T-23, M-372, next ROADMAP row 397.

## The queue's near head

- **386 + 396 — parallel lanes, and the transport premise he refused.** 386: both mechanisms exist
  (`isolation: worktree` on a subagent, `EnterWorktree`/`ExitWorktree` on a session); INV-105 already
  calls worktree isolation the default when write-sets overlap while the tool's contract says only on his
  explicit word — the law and the tool contradict, so neither fires; the method has no branch and no merge
  road; the three-lane cap is his 07-06 word and he is questioning it. 396: agents on one machine talk
  directly, git carries what needs no answer. **Two things ride 396 and must not be skipped:** the fact
  nobody has verified — whether two Claude Code sessions on one machine have a direct channel at all
  (independent processes; a socket, a local endpoint, a watched file may or may not exist) — and the
  research he actually asked for and did not get: direct agent-to-agent protocols (sockets, TCP/IP, UDP,
  REST). The sweep that ran posed the question with git as the transport, which was the premise he was
  refusing, so it answered the premise instead of the question.
- **395 — the expensive-decision class**, agent birth its first member: his word is that creating an agent
  must be very well grounded (Fable, adversarially AND directly, or another smart road), and that the
  class needs its other members swept. He named an unverified reference: a Karpathy skill with an advocate
  and a prosecutor deciding together. Verify it before citing it.
- **390 + 392 — growth, two different questions.** 390 (Fable's consult, docs/design/): nodes-per-file
  read from the architecture's pin column; size rejected as a vanity metric for the split question.
  392 (his pushback on 390): everything that can grow declares the number bounding it and its watcher —
  our own ROADMAP is 494 KB, the spec 446 KB, the journal 468 KB, and nothing bounds any of them.
- **383/384/391 — the machine-holds-the-rule family**: dramatization's docs arm; the vacuous-pass class;
  every net keeps its own runs and fires. 391's personal arm is armed (`~/.claude/hooks/hook-meter.py`,
  wrapping the Stop scan; `--report` prints runs/fires with the reading).
- **394** — a grown founding question reaches the hosts that founded before it (the agent card is today's
  own example: every existing tree predates it and none knows to write one).
- **385, 387, 388, 389** — the layer's own owed arms: the contract's three machines [target], the card's
  gate + adoption's line [target], the wrong referral, the remote read grant.
- **379 (bullets), 380 (reach classes to host config), 370, 393 (worker teardown reaps its process group)**
  — smaller, unblocked.
- **There is also a far tier** (row 382 gives it its law; row 381 is its first member). The what's-left
  answer leaves it out and offers it on request.

## Research and records in hand
- `docs/research/2026-07-17-agent-routing-prior-art.md` — the Contract Net Protocol (1980) already ships
  the router-in-every-node and ownership by two-way mutual selection with no central allocator, and its
  directed contract IS this pack's referral law under another name (2 messages against a 20-message
  broadcast baseline). Non-blocking is delivered by no protocol; the literature's fix is opportunistic
  negotiation over a baseline where every agent can act alone — which is his own never-stall clause,
  arrived at independently. Two open threads: the summary reports nothing on Google's A2A protocol, which
  the question asked about; and the whole sweep carries the git premise row 396 refuses.
- `docs/design/2026-07-17-node-growth-law.md` — the node-growth consult (row 390).
- `docs/prover/2026-07-17-agent-communication.md` — the twelve defects, all folded this landing.
