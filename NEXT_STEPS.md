# livespec — NEXT_STEPS (resume file: LIVE STATE + queue only; history → JOURNAL.md)

## LIVE STATE (2026-07-05 afternoon)
PUBLIC: github.com/happysasha18/livespec. **Rows 24+36 landed (SPEC v0.6): the base skill
`skills/livespec-base/` — 12 shared rules stated once (list from an evidence sweep, raw greps in session
scratchpad) + the settings ladder (host > personal > package default, no silent override, INV-14)** — with
debts 31 (VERSION 0.1.0 + `version:` frontmatter ×5, M-7) and 32 (personal profile at
`~/.claude/livespec/profile.md`, Alexander's written from his recorded words). livespec's own host profile
(`.livespec/profile.md`) records the every-push prover gate as the worked override example. Four working
skills carry an inherit note pinning base v0.1.0 — a base bump MUST sweep those pins same session (E-12).
Pruning pre-base restatements: deferred to milestone compaction by design (INV-13). Also today: rows
29+34+35+37 (classification, icon-list reporting + its detail refinement). Prover records:
docs/prover/2026-07-05-{classes,rule9-detail,base-skill}.md. JOURNAL has all entries. Installed copies
synced. Standing: one window = one project; **livespec = Fable only**.
OPEN for Alexander: base folder name (D-4, keep `livespec-base` — recommended) · personal-profile home
(D-5, keep `~/.claude/livespec/` — recommended) · row 27 naming (keep livespec — recommended) · row 33
playbook fate (keep as thin private layer — recommended) · row 20 pick (adopt artifacts TRACKED — flip if
he prefers).

## Forward queue (mirrors ROADMAP.md; priority marks live there)
1. Fold the 11 CLAUDE.md-mining gaps into the skills (row 12) — via the pipeline, one at a time; several
   now have their home ready (the base skill).
2. Prover debts: queue archive at milestones (row 30) · personal-profile git home (row 38, new).
3. Guardrails scaffold (row 3: + fence & push-gate mechanization) · CI-mirror example (row 14).
4. track-coach becomes the first formal adopt-host → test-suite rework by architecture node (pre-1.0) ·
   skill-creator eval pass (row 5).
