# livespec Spec

> How to read: the prose is the meaning; the short codes (INV-x, CR-x, ⟨DECIDE⟩) are quiet machine handles at line-ends for the prover and test matrix. Edit history lives in JOURNAL.md, not here. This spec states today's truth.

**Status: skeleton only.** This header and note are placeholders. The full spec will be authored via `spec-author` once the package scope is confirmed — entities, states, transitions, actors, invariants, and cross-section composition across all surfaces. That is the next movement (see NEXT_STEPS.md and ROADMAP.md item 2).

---

*Authored via spec-author — next movement.*
