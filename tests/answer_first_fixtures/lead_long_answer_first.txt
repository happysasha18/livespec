[15:40] The migration is safe to run tonight, and it will take about forty minutes end to end.

I want to give you the full picture underneath so you can see why I am confident. I mapped every table that references the account column and traced which of the six services read it and which write it. The write order matters, so I worked out the exact sequence in which each service switches over, and confirmed that at no point does any service observe a row that is half migrated. The backfill is the long pole: at the current row count of roughly 2.3 million it runs a little under half an hour on the replica, and I rehearsed it twice against a snapshot to be sure that number holds. Each of the three phases has a rollback that I have actually run, so a failure at any step undoes cleanly without data loss. The step-by-step plan, the timing for each phase, and the rollback commands are all in the document below.
