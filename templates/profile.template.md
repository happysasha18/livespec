# Personal live-spec profile — <the human's name> (created <date>, SPEC B-3)

Settings about the human; they follow the human across every host. Resolution reads narrowest-out:
the human's live word in a session beats a host profile, which beats these lines, which beat the
package defaults — the settings ladder (SPEC E-13). Every line below is written on the human's word:
told directly, or proposed from a source the human named and accepted one at a time. Mode and trust
lines exist only on the human's word (SPEC INV-9).

## Contract

- `language.docs: <placeholder — e.g. English>` — docs, commits, code, artifacts (<their word, date>)
- `language.chat: <placeholder — e.g. mirror the human | Russian>` (<their word, date>)
- `address: <placeholder — how to address them>` (<their word, date>)
- `proactivity.mode: <placeholder — ask-at-max | max-proactive>` (<their word, date; SPEC INV-9>)
- trust: not set — the package default stands until the human names a level (SPEC INV-9).

## About the human (their own words, dated)

- <placeholder — what they do, their background, their domain vocabulary>

## Working habits

- <placeholder — one habit per line, each with their word and its date>

<!-- Template notes — delete after filling:
  · every placeholder above is a placeholder, never a default: a line lands only on the human's word;
  · a proposed line the human declined is dropped, never parked;
  · a line an older pack does not recognize is ignored ALOUD (SPEC E-13) — new lines are safe to add;
  · git home: a PRIVATE repo the human owns may hold this file (SPEC E-16). -->
