# guardrails owns-field relocation audit — PART A

Range: anchors E-6, E-10, M-5, and INV-17 through INV-212.

The bare anchors in range (E-6, E-10, M-5, INV-17, INV-24, INV-45, INV-47, INV-66, INV-73, INV-132) carry no attached prose in the owns field, so they contribute no fragments. Only anchors with a parenthetical fragment appear below.

| fragment (quoted; shorten with … past 40 words) | anchor | class | evidence |
|---|---|---|---|
| "the four host checks' shipping contract; code pin lands with row 241, [target]" | INV-97 | KEEP | Carries a [target] / ROADMAP-row pointer ("code pin lands with row 241, [target]") stating the code is not yet landed; the "shipping contract" phrase only labels the anchor's own rule (spec R228, e.g. line 5148). |
| "the chat arm's sentence is carried by communicator as wiring; ownership stays elsewhere" | INV-97 | KEEP | Explicit wiring note — another node (communicator) carries the text, ownership stated to stay elsewhere. Stays in architecture. |
| "the shipped-language gate, wired into the pack's own pre-push as gate i and the CI mirror — row 279" | INV-120 | KEEP | Wiring note (gate i, CI mirror) plus a ROADMAP-row pointer ("row 279"); the spec (R150/R151) states the gate's rule but not this gate-letter wiring or the row. |
| "canonical hooks home + universal/personal split + quoted-demo skip" | INV-173 | DUPLICATE | All three facts are spec rules under INV-173: line 6136 "keep a live-channel hook … as source in the pack's `hooks/` home reached through an installer" (home); line 6140 "split the set … a universal hook … ships with the pack; a personal hook … lives in the personal layer" (split); line 6146 "*when* a scan hook reads text inside quotation marks or code fences … *shall* skip it … a demonstration is never flagged" (quoted-demo skip). |
| "config health: installed gate = source gate" | INV-175 | DUPLICATE | Line 6160: "*when* an expected hook is missing from the hooks directory or differs from its source, the system *shall* red the config-health check and name the one fix …". |
| "a gate scans the whole tree, retroactive by construction" | INV-176 | DUPLICATE | Line 6204: "*when* a law is extracted into a mechanical gate, the system *shall* scan the entire tracked tree or the whole gated artifact set, reaching beyond the changed lines, so debt that predates the gate is found the day the gate lands …". |
| "the net-liveness meter: the shared shape records every net's runs and fires and reads them into three readings — broken zero-run net, silent retirement candidate, live; shipped complete and usable on demand" | INV-202 | PARTIAL | Mechanism is spec: line 5174 "record one line per invocation … aggregate runs and fires"; line 5178 zero runs → "broken trigger"; line 5179 zero fires → "retirement candidate"; line 5181 "read every other net as live". Spec lacks the shipped-status note — the fragment adds: "shipped complete and usable on demand". |
| "the register judge: one class-reading model mechanism, two surfaces — chat hook + document pre-show ceiling; universal/personal law split like the literal scan [INV-173]; stands down silently on its own breakage; runs/fires read by the meter [INV-202] …" | INV-203 | PARTIAL | Mechanism is spec: line 5195 (model tier reads outgoing text); 5204 chat arm + 5205 "the same judge *shall* stand as the ceiling of the pre-show register gate"; 5200 universal/personal split; 5209 breaks → "leave the literal-list verdict standing rather than red"; 5260 "runs and fires read by the net-liveness meter". Spec lacks the ownership rationale the fragment adds: "it holds the register class the retracted growth-list never could". |
| "the touchpoint-kind frame: every point of contact has a kind — synchronous or asynchronous — declared in the manifest `guardrails/touchpoints.json`, and the gate reds a surface speaking in a kind its touchpoint lacks; the frame's home for its four instance rows 402/403/408/409" | INV-205 | PARTIAL | Frame is spec: line 5348 "declare each touchpoint's kind in the manifest `guardrails/touchpoints.json`"; 5349 synchronous/asynchronous kinds; 5353 "*when* a surface speaks in a kind its touchpoint lacks … *shall* red it". Spec lacks the ROADMAP-row pointer the fragment adds: "the frame's home for its four instance rows 402/403/408/409". |
| "the waiting list: the board `WAITING.md` at the host root holds everything waiting for the person's eyes … an item clearing on his acknowledgement alone; the gate `guardrails/check-board.py` reds a closing report omitting a still-open item, a demotion with no matching line (a silent loss), or an over-cap shown set" | INV-206 | DUPLICATE | Line 5368: "hold every item waiting for the person in the board `WAITING.md`, and *shall* clear an item on the person's acknowledgement alone." Line 5378: "*when* a closing report omits a still-open board item, or an item is demoted with no matching line, or the shown set runs over cap, the system *shall* red the board gate." |
| "the authority-anchor writing rule: a decision recorded as the person's names the exchange it came from, a claim the pack reasoned out written in the pack's own voice … every live on-record entry carries its date, a struck one skipped, an unanchored one reds — while the spec/base-rulebook/roadmap get the one-time first sweep …" | INV-207 | PARTIAL | Writing rule + gate are spec: line 5392 (person's decision names its exchange); 5393 (pack's claim in its own voice); 5399 "*when* a live on-record entry … carries no exchange, the system *shall* red the authority-anchor gate, a struck entry being skipped." Spec lacks the first-sweep scope note the fragment adds: "while the spec/base-rulebook/roadmap get the one-time first sweep". |
| "the skill-review gate: a push that changes a skill's body reds until its skill-creator review is on record; the gate `guardrails/check-skill-review.sh` … exempts a pure version-frontmatter stamp … and requires a committed record … naming the skill with a `SKILL-REVIEW` marker and a `Verdict:` line, fresh against the skill's last change …" | INV-208 | DUPLICATE | Line 5472: "*when* a push substantively changes a skill under `skills/`, the system *shall* require a committed review naming the skill, carrying a verdict, and at least as new as the skill's last change." Line 5477 exempts the version-stamp-only diff ("its only changed lines are the machine-stamped version and base-reference lines … pass it quiet"). |
| "the doc-rotation gate: the pack's append-only working docs are split and rotated and nothing rotated is lost … reds a rotation that drops content …, a rotation with no manifest …, or an ambiguous rotation …; it composes with the growth law rows 390 + 392 carry — the bound governs what is shown, the archive keeps all and stays grepable so a row cited by number stays findable" | INV-209 | PARTIAL | Mechanism + reds are spec: line 5492 (move closed rows to dated archive + manifest line); 5497 dropped content → "nothing-lost violation"; 5498 orphan archive or row-both-places → "red as ambiguous". Spec lacks the composition note the fragment adds: "it composes with the growth law rows 390 + 392 carry — the bound governs what is shown, the archive keeps all and stays grepable so a row cited by number stays findable" (ROADMAP-row pointer + composition rationale). |
| "the CI-mirror gate: the push gate lives twice … the gate `guardrails/check-ci-mirror.sh` reads the `-- gate X:` markers … and reds on any local gate absent from CI, the CI carve-out set declared with reasons in `guardrails/ci-mirror.json`; kin of config-health [INV-175]" | INV-210 | DUPLICATE | Line 5561: "*when* the push gate runs a gate letter locally that the remote mirror does not run, the system *shall* red, naming the gate and the one fix, a legitimate remote-skip living in `guardrails/ci-mirror.json` with its reason." (Line 5562 covers carve-out drift.) |
| "the judge-listed gate: every judge the pack runs is referenced in the installed `~/.claude/settings.json` … reds a wired judge unreferenced in its settings.json surface; personal-layer, so it stands down by name where settings.json cannot be read …" | INV-211 | DUPLICATE | Line 5566: "*when* a hook under `hooks/` is not classified in the wired-hook declaration, or a wired hook is missing from its array in the installed settings, the system *shall* red, naming the hook, the surface, and the fix." Line 5567: "*where* the personal-layer settings cannot be read, the system *shall* stand the wiring check down by name rather than falsely pass." |
| "the meta-gate over the chain: every gate carries a known-red proof, so a gate that cannot fail … is caught; the gate `guardrails/check-every-gate-can-fail.py` (gate w) … requires each classified in `guardrails/gate-red-proofs.json` as a `proof` … or a `covered` gate that runs no independent check and rides another gate's red … an unclassified gate or a hollow proof or a `cannot_red` gate reddening …" | INV-212 | DUPLICATE | Line 5571: "*shall* require each pushed gate letter to be classified with a red-first proof driving its check to a non-zero exit, or a covered entry naming the gate it rides and the reason." Line 5572: "*if* a gate marker is classified nowhere, or a gate can by construction never be made to fail, *then* the system *shall* red it loudly." |
