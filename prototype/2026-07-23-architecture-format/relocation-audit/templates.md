# templates — owns-field relocation audit

The **owns** field carries bare codes and no prose (E-3, E-5, INV-6, B-1, E-24, INV-48, E-26). Nothing to relocate.

## Pins carrying provenance
None. (Pins are template file paths only; no date, session number, or landed-row note.)
