# package-docs — owns-field relocation audit

The **owns** field carries bare codes and no prose (S-0, M-3, M-4, D-1, D-2, D-4, D-6, D-7, E-23). Nothing to relocate.

## Pins carrying provenance
None. (Pins are dogfood file paths only; no date, session number, or landed-row note.)
