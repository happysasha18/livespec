# host-contract — owns-field relocation audit

owns codes: E-8, E-16, E-32, INV-184. One prose fragment (on INV-184).

| fragment (quoted; … past 40 words) | anchor | class | evidence |
|---|---|---|---|
| "the card's flag at founding and at adoption's orient is carried by attach as wiring; ownership stays here" | INV-184 | KEEP | Wiring note: it states another node (attach) carries the text while ownership stays at host-contract — an architecture-only fact. The spec states the flag law itself (R193.12, "*when* an inventoried live-spec host tree carries no `.live-spec/agent.md`, the system *shall* flag it as an incomplete record … *shall* have the host write its card at its catch-up walk"), but never the ownership-vs-carrier split, which is the fragment's whole content. |

## Pins carrying provenance
- personal: `~/.claude/live-spec/profile.md` — "row 38 landed 2026-07-05" (landed-row + date).
- loader: `~/.claude/CLAUDE.md:1` — "thin loader live — row 52 landed 2026-07-05" (landed-row + date).

Count: 2.
