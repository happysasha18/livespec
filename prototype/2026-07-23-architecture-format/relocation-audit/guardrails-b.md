# guardrails node — owns-field relocation audit, PART B (INV-213 through INV-277)

Source: `prototype/2026-07-23-architecture-format/out/ARCHITECTURE.converted.md`, `### [node: guardrails] [target]`, **owns** field.
Spec: `PRODUCT_SPEC.md`. Codes E-6/E-10/M-5/INV-17..212 and no-code part-A fragments belong to the other auditor and are skipped.

| fragment (quoted; … past 40 words) | anchor | class | evidence |
|---|---|---|---|
| "the parked-question default arm on the same waiting-list gate `guardrails/check-board.py`" | INV-229 | KEEP | wiring — names the existing gate this arm extends |
| "a question the pack parks for the person carries the recommended default the work already took so the work rolls on" | INV-229 | DUPLICATE | R241.1 (5452): "place it on the waiting board already carrying the default the work took, and *shall* proceed on that recommendation" |
| "a board item marked a parked question with a `[[park]]` tag carrying no `default:` note reds" | INV-229 | DUPLICATE | R241.2 (5453): "*when* a board item marked a parked question records no default, the system *shall* red the board gate" |
| "an arm extending the existing gate q, no new gate letter" | INV-229 | KEEP | gate-letter/placement note |
| "it consumes the parked-feedback-question touchpoint classification the frame declares" | INV-229 | KEEP | cross-node wiring (touchpoint frame) |
| "the project-name arm on gate i" | INV-245 | KEEP | wiring — the gate it rides |
| "a core spec names no foreign project and tells no dated incident" | INV-245 | DUPLICATE | R151.1 (3215): "red a bare project name in the product spec or the architecture, and *shall* red a project name standing beside a calendar date in any of the three core specs" |
| "the product spec and the architecture red on a bare project name and on a project name beside a calendar date" | INV-245 | DUPLICATE | R151.1 (3215), same clause |
| "the test matrix reds a dated incident while permitting the fixture-ledger kind names and a test-function-name substring" | INV-245 | DUPLICATE | R151.2 (3216): "have the test matrix red a dated incident while permitting a bare fixture-ledger kind name and a project-name substring of a test-function name" |
| "the forbidden names held as allowlist data under `project_name_patterns` so the detector names no project" | INV-245 | DUPLICATE | R151.3 (3220): "hold the forbidden project names as data in the shipped-language allowlist so the detector's own source names no project" |
| "riding gate i's mechanism and known-red proof with no new gate letter" | INV-245 | KEEP | gate-letter/placement note |
| "ROADMAP 441" | INV-245 | KEEP | roadmap-row pointer |
| "the runaway-child notice: a finished worker leaves no runaway child that burns unseen" | INV-213 | DUPLICATE | R235 heading (5312) "A finished worker leaves no runaway child"; Context (5314) |
| "a Stop-time notice, never a push gate, since a push gate runs long after the cores burn" | INV-213 | PARTIAL | R235.1 (5322) has the "*when* a stopping point is reached" trigger; spec lacks "never a push gate, since a push gate runs long after the cores burn" |
| "the mechanism `guardrails/check-runaway-child.py`, its core `find_runaways` reporting a descendant the run provably owns (its own process group, or its own temp tree), orphaned and burning a core" | INV-213 | DUPLICATE | R235.1 (5322): "report a descendant that is owned, orphaned, and burning … reasoning over process group, parent liveness, and processor share alone"; Context (5314) names process group / temp tree |
| "reading no command/name field so a bare-name match in a foreign process group under no owned tree never fires" | INV-213 | DUPLICATE | R235.2 (5324): "read no command or name field for that verdict, so a process whose command merely matches a known burner in a foreign group is never targeted" |
| "the owned-identity discipline the test-author node owns, the browser-kill lesson of row 334" | INV-213 | KEEP | cross-node wiring + row pointer |
| "notice-first through the shared `guardrails/cleanup_notice.py`, the cleanup-notice shape row 417 landed on the test-author node … so it can never become the broad-sweep footgun it guards" | INV-213 | KEEP | wiring (shape owned elsewhere) + row pointer + rationale |
| "live wiring a documented owner-run install step, kept out of any auto-wire into a running session's Stop hook" | INV-213 | KEEP | install/wiring note (no spec hit for "install step"/"auto-wire") |
| "the cleanup-notice and owned-identity disciplines applied to an orphaned worker descendant" | INV-213 | KEEP | ownership summary |
| "the dead-permission-path arm of config-health" | INV-216 | KEEP | wiring — names the parent check |
| "a permission rule the harness enforces can name a filesystem path, and a rule whose path no longer exists enforces nothing while looking correct" | INV-216 | DUPLICATE | R246.7 (5576) enacts the guard: "*when* a filesystem path named inside a permission rule is absent, the system *shall* red the rule" |
| "deploy permissions left dead a week after their folder was renamed" | INV-216 | ABSENT | dated incident; spec no-history carries nothing (grep empty) |
| "the arm `guardrails/check-config-health-perms.py` resolves every filesystem path … in the personal `~/.claude/settings.json` and the host's project settings and reds a rule whose path is absent, reads absolute and `~/` paths, strips a trailing glob to its literal ancestor, keeps a spaced path whole …" | INV-216 | PARTIAL | R246.7 (5576) covers red-on-absent, "absolute and home-rooted paths" and count-resolved; spec lacks "the host's project settings" scope and "strips a trailing glob to its literal ancestor, keeps a spaced path whole" (grep empty) |
| "personal-layer" | INV-216 | KEEP | layer classification |
| "stands down by name where the settings cannot be read and reds a present-but-unreadable one, never a false pass" | INV-216 | ABSENT | no such stand-down rule under INV-216; "present-but-unreadable" absent from spec (grep empty) |
| "rides gate m's CI carve-out and known-red proof, no new gate letter" | INV-216 | KEEP | gate-letter/placement note |
| "kin of config-health [INV-175] and the judge-listed gate [INV-211]" | INV-216 | KEEP | relationship/wiring |
| "a check that looked at nothing is not a pass: the shared shape `guardrails/nonempty_input.py` lets a check declare its expected-non-empty input set and red by name on an empty one" | INV-218 | DUPLICATE | R113.1 (2472): "have a check declare the input set it expects to be non-empty and *shall* red by name when that set is empty" |
| "the sibling of the unexpected-skip law" | INV-218 | DUPLICATE | R113.1 (2472): "the way an unexpected skip is a failure outright" |
| "its first instance is the index prose gate `guardrails/check-index-prose.py` (gate x), reddening a zero-anchor index by name and demanding every Formal-index code be carried in its home prose, a range citation counting for its members" | INV-218 | ABSENT | "index prose"/"home prose"/"zero-anchor"/"Formal-index" all grep-empty in spec |
| "the card's gate `guardrails/check-agent-card.py` (gate y) reds a live-spec host tree carrying no card at `.live-spec/agent.md`" | INV-219 | DUPLICATE | R193.13 (4327): the gate "*shall* … fail by name *when* the root carries no `.live-spec/agent.md`" |
| "the sibling of the kind-with-no-layers flag" | INV-219 | KEEP | relationship note (not in spec) |
| "the pack is its own first host and carries its own card so the gate reads this tree and passes" | INV-219 | DUPLICATE | R193.13 (4327): "the pack carries its own card so the gate reads the pack's tree and passes" |
| "it is the declaration law's mechanical net, the target deferral coming off that law with it" | INV-219 | KEEP | relationship + target note |
| "the answer-first arm: a Stop-hook notice `hooks/answer-first-scan.py` reddening a chat reply over a length floor whose opening block is a wall with no short lead, a lead present when any one of a short opening sentence, a short opening paragraph, or scannable structure holds, and it reds only when all three fail" | INV-220 | DUPLICATE | R231.1 (5223): reply past length floor "fails all three lead signals — a short opening sentence, a short opening paragraph, or scannable opening structure" → flag |
| "honest about what it cannot read (lead presence, never the right answer; the final reply, never the inter-tool narration)" | INV-220 | DUPLICATE | R231.3 (5228) "judge only whether an opening lead is present … leave whether that lead answers the right question to the person"; R231.4 (5229) "judge only the final reply … leave the short inter-tool narration lines alone" |
| "a notice never a pre-push gate, since a chat reply is already emitted" | INV-220 | DUPLICATE | R231.5 (5233): "since a chat reply is already emitted and cannot be blocked" |
| "ships universal, installs by the setup walk [INV-173], covered by config-health parity [INV-175] and classified in judge-hooks.json [INV-211], runs/fires read by the meter [INV-202]" | INV-220 | KEEP | cross-anchor wiring (install/parity/classification/meter) |
| "the hedge gate: a literal-pattern Stop-gate on the seat's own reply text, modeled exactly on the scissors scan — reads the last assistant turn, strips a quoted span, a backticked span, and a fenced code block before matching, matches an inline universal English pattern list plus an optional personal-overlay JSON a host tunes, and blocks the stop with a rewrite instruction on a surviving match" | INV-238 | PARTIAL | R232.1 (5248) covers the strip-then-match-then-block mechanism modelled on the scissors scan; spec lacks "an optional personal-overlay JSON a host tunes" (overlay grep-empty for this feature) |
| "reds a reply carrying an offering-hedge — an offer to do a derivable, git-reversible act only once the human gives a cue, held open for that cue instead of done and reported" | INV-238 | DUPLICATE | R232 Context (5240): "a reply that offers to do a thing the seat could already derive and reverse, holding the offer open for a cue, is an offering-hedge"; R232.1 |
| "stands clear of a legitimate taste, policy, or irreversible question naming its human-only fact" | INV-238 | DUPLICATE | R232.2 (5249): "leave clear of a genuine taste, policy, or irreversible question that names its human-only fact" |
| "installs by the setup walk beside the scissors scan and the answer-first arm [INV-173], covered by config-health parity [INV-175] and classified in judge-hooks.json [INV-211], runs/fires read by the meter [INV-202]" | INV-238 | KEEP | cross-anchor wiring |
| "the machine for the standing no-only-say-hedge behaviour, profile `proactivity.no-only-say-hedge`" | INV-238 | KEEP | ownership/wiring (profile binding) |
| "a worker's teardown reaps the process group the run itself spawned, scoped to the run's own group" | INV-230 | DUPLICATE | R235.3 (5328): "reap the process group the run itself spawned … *shall* refuse any group absent from the run's owned set"; R207.8 (4730) |
| "safe for that reason under the owned-identity discipline the test-author node owns" | INV-230 | KEEP | cross-node wiring |
| "`guardrails/reap_owned_group.py`'s `reap_owned_group` takes a numeric process group and refuses any group absent from the run's owned set (a program name is no legal target) and reports what it ended through the shared cleanup-notice shape row 417 landed" | INV-230 | DUPLICATE | R235.3 (5328) refuse group absent from owned set; R235.4 (5329): "report what it ended through the shared cleanup-notice shape" |
| "`find_idle_output_workers` catches a worker whose status reads running while its output mtime has gone idle by the liveness-via-mtime read the base-rulebook worker-liveness law owns" | INV-230 | DUPLICATE | R235.5 (5333): "*when* a worker's status reads running while its output file has stopped growing … read the stall from that file's modification time" |
| "the reap and detection arms of the runaway-child class this node owns, a process-space habit at teardown taking no gate letter" | INV-230 | KEEP | ownership + gate-letter note |
| "the idle habit's worker-contract home is the base-rulebook worker contract, carried here as wiring" | INV-230 | KEEP | explicit wiring note |
| "the listener tripwire: a deferred row's mechanical revisit trigger" | INV-231 | DUPLICATE | R95.1 (2141): "*when* a deferred row's awaited event is mechanically observable … make its revisit trigger a check the queue-take runs" |
| "`guardrails/check-listener-tripwire.py`'s `find_listeners` firing on a session record carrying a non-empty socket field and silent on an empty one, field-gated so today's listenerless harness leaves it quiet" | INV-231 | DUPLICATE | R95.2 (2142): "fire the listener-tripwire check only on a session record carrying a non-empty socket field … stay silent on an empty or absent one, so a listenerless harness leaves it quiet" |
| "returning the row to the runnable head on firing under the deferred-row revisit habit build-pipeline owns" | INV-231 | DUPLICATE | R95.3 (2146): "*when* the check fires … return the row to the runnable head" |
| "riding the queue-take scan and the suite and taking no gate letter the way the far-tier report-shape check does since a chat-surface read is no committed file to gate" | INV-231 | DUPLICATE | R95.3 (2146): "ride the queue-take scan and the suite with no push-gate letter, the way the far-tier check takes none, since a queue-cadence read is no committed file" |
| "the growable-artifact bound: everything that can grow declares the number that bounds it and the watcher that reads it" | INV-234 | DUPLICATE | R245 heading (5527) "Everything growable declares the number that bounds it"; Story (5531): "each growable artifact to declare a byte ceiling with a reason and a watcher to read it" |
| "the architecture-budget rule's budget-plus-watcher shape lifted to every growable artifact" | INV-234 | KEEP | lineage/rationale ("architecture-budget"/"budget-plus-watcher" grep-empty) |
| "the four large working docs (PRODUCT_SPEC.md, ROADMAP.md, TEST_MATRIX.md, JOURNAL.md) each declare a byte ceiling with a recorded reason in `guardrails/doc-bounds.json`, and the watcher `guardrails/check-doc-bound.py` (gate z, wired local + CI) reds a doc past its ceiling" | INV-234 | DUPLICATE | R245.1 (5537): the four named docs "declare a byte ceiling with a non-empty recorded reason in `guardrails/doc-bounds.json`, and *shall* red a document past its ceiling" |
| "it composes with the doc-rotation gate [INV-209] — crossing the bound earns a rotation … a doc rotated today passing even over its ceiling while a rotation from another day does not clear today's overflow" | INV-234 | DUPLICATE | R245.3 (5542): "*when* a document over its ceiling carries a manifest naming an archive dated the same day, the system *shall* pass it" |
| "a ceiling rises only with a recorded reason, the ceilings seeded above the current sizes with rotation headroom … the ratchet pointing down and reset by rotation" | INV-234 | DUPLICATE | R245.4 (5543): "seed the ceilings above the current file sizes with rotation headroom, and *shall* let a ceiling rise only with a recorded reason" |
| "the growable-doc sibling of gate t [INV-209] in this node's own doc-grooming family" | INV-234 | KEEP | relationship note |
| "the traffic-kind transport router: which transport carries a message between two agents is decided by the traffic's kind" | INV-236 | DUPLICATE | R190.4/R190.5 (4218-4219) route by message kind (no-answer → store; live peer → direct channel) |
| "correcting the two-channel law's refused git-universal premise [the owner's word 2026-07-17]" | INV-236 | KEEP | owner's-word provenance/rationale |
| "the router `guardrails/route_agent_transport.py`'s `route` sends a no-answer message — a durable record or notification — to the git store road, available today [ROADMAP 247], and a conversation needing a live peer to the direct channel" | INV-236 | DUPLICATE | R190.4 (4218) store for no-answer message; R190.5 (4219) direct channel for a live-peer back-and-forth |
| "its availability read from the machine through the listener tripwire's own socket-field truth [INV-231] so today's listenerless harness leaves it unavailable and the result names the listener it waits on" | INV-236 | DUPLICATE | R190.6 (4220): "*while* the harness has shipped no listener, the direct channel *shall* stand unavailable, and the router *shall* name the listener it waits on" |
| "rides the suite and takes no push-gate letter the way the listener tripwire does [INV-231]" | INV-236 | KEEP | placement/gate-letter wiring (not in R190) |
| "the transport arm of the two-channel law the base-rulebook states" | INV-236 | KEEP | ownership pointer (law homed in base-rulebook) |
| "the pair travels everywhere, armed by two presence gates" | INV-239 | DUPLICATE | R191.2 (4243): "carry that pair in a message across the agent channel and in a human-facing report alike" |
| "`guardrails/check-description-field.py`, the Formal-index non-empty description-field gate, dormant until the back-describe migration" | INV-239 | KEEP | gate wiring/arming-timing ("dormant until" is not a stated rule) |
| "`guardrails/check-deposit-description.py`, the agent-channel deposit-time lint reading `from-<agent>` inbox files beside the earned-message gate `guardrails/check-earned-message.py`" | INV-239 | DUPLICATE | R191.8 (4255): "the deposit-time lint over each `from-<agent>` inbox file being the mechanism the law declares" |
| "both check presence only and ship with this feature's build, target" | INV-239 | KEEP | target/wiring note |
| "the conduct judge: the register judge [INV-203] generalized to read the turn's action trace — the ordered tool_use events in the transcript, the reply text set aside — against the standing orchestration laws, redding an act-level violation after the fact" | INV-241 | DUPLICATE | R233.1 (5274): "read the turn's action trace against the standing orchestration laws and red a violation after the fact"; glossary "conduct judge" |
| "an async two-arm Stop/UserPromptSubmit shape reusing register_judge_core.py, off by default and outside the deterministic gate, standing down silently on its own breakage" | INV-241 | DUPLICATE | R233.6 (5285) Stop arm + prompt-submit arm; R233.7 (5286): "keep the conduct judge outside the deterministic suite and push gate, opt-in per host and off by default" |
| "the net for the orchestration laws that had none" | INV-241 | KEEP | purpose/rationale note |
| "the owner's word 2026-07-19, the reminder-dependent capability audit's bucket 1" | INV-241 | KEEP | owner's-word provenance |
| "the lean-orchestrator arm: a Stop-hook soft signal `hooks/lean-orchestrator-scan.py` warning a session that hoards raw file content inline without dispatching a worker" | INV-246 | DUPLICATE | R232.4 (5254): "*when* cumulative inline raw file content across the session reaches the threshold and the worker-dispatch count is zero … warn that the reading rode no worker dispatch" |
| "the mechanical net the lean-orchestrator law lacked, one of the orchestration laws the conduct judge holds [INV-241]" | INV-246 | KEEP | relationship/wiring |
| "`scan_transcript` sums inline raw file content across the whole session (the tool_result byte size for a main-thread Read or Bash file-dump, a sidechain read never counted …) and counts main-thread Agent/Task dispatches … a last-message scan would miss" | INV-246 | DUPLICATE | R232.5 (5255): "count only a main-thread Read or one of six literal file-dump verbs, a read inside a worker riding a sidechain never counted" |
| "fires the hedge scan's block-and-suppress shape [INV-238] when cumulative inline raw content ≥ the threshold and the dispatch count is zero, the dispatch-count-zero gate the honest false-positive floor a dispatched session clears" | INV-246 | DUPLICATE | R232.4 (5254) threshold + zero dispatch → warn; R232.6 (5256) first dispatch clears the warning |
| "threshold a tunable parameter, default 50 kilobytes, tuned through the `~/.claude/hooks/lean-orchestrator-personal.json` overlay's `threshold_bytes` with a silent default fallback" | INV-246 | DUPLICATE | R232.5 (5255): "read the threshold as a tunable parameter defaulting to 50 kibibytes" (overlay filename is mechanism detail) |
| "off by default and opt-in, classified as a library entry in judge-hooks.json [INV-211] and not wired into the pack's default settings.json, the orchestration-law family's boundary [INV-241]" | INV-246 | KEEP | config/wiring note (not in R232.4-6) |
| "stands down silently on its own breakage [INV-203], runs/fires read by the net-meter [INV-202]" | INV-246 | KEEP | cross-anchor wiring |
| "a landing is a commit whose diff moves a ROADMAP.md row from the body to the queue archive with its archived status naming *landed* — a row moving out as *declined* or *superseded* owes no refresh — and a landing whose diff does not also touch NEXT_STEPS.md reds" | INV-242 | DUPLICATE | R257.1 (5823) landed-flip requires touching NEXT_STEPS.md; R257.3 (5828) declined/superseded owe no refresh |
| "reads the pushed commit range through the same base ladder the other range checks use [INV-208]" | INV-242 | DUPLICATE | R257.1 (5823): "reading the pushed commit range through the same base ladder … the declared base, then `origin/main`, then the previous commit" |
| "rides the suite the way the far-tier report-shape check does and takes no push-gate letter since the push-gate letters are exhausted" | INV-242 | DUPLICATE | R257.4 (5829): "*when* the push-gate letters are exhausted … ride this check on the suite, so a red here reds the suite gate and blocks the push" |
| "`guardrails/check-landing-next-steps.py`" | INV-242 | KEEP | mechanism/pin pointer |
| "the owner's word 2026-07-19, the reminder-dependent capability audit's bucket 2" | INV-242 | KEEP | owner's-word provenance |
| "the config-health skill-copy arm: the installed skill copy is the source skill" | INV-243 | DUPLICATE | R271.3 (6189): "hold a shipped skill's installed copy byte-pristine" (installed copy == source) |
| "a named future row now filled" | INV-243 | ABSENT | provenance/history; spec no-history carries nothing |
| "a second arm on `guardrails/check-config-health.sh` diffs each installed skill tree against the pack's `skills/` source and reds an un-synced or drifted copy, naming the fix `scripts/sync-skills.sh`, running inside the suite and the push gate with the hook arm [INV-175]" | INV-243 | DUPLICATE | R271.1 (6184): "red the config-health check's skill-copy arm and name the one fix, to re-run `scripts/sync-skills.sh`"; R271.2 (6185) |
| "the owner's word 2026-07-19, the reminder-dependent capability audit's bucket 2" | INV-243 | KEEP | owner's-word provenance |
| "the requirements-format laws and their format-gate family, armed at the row-445 conversion delivery" | INV-250..265 | KEEP | family label + roadmap-row pointer + arming note |
| "the gate-script → code map: `check-requirement-shape.py` [INV-250,251,252,257], `check-no-history.py` [253], `check-vocabulary.py` [254], `check-one-name.py` [255], `check-weak-words.py` [256], `check-index-generated.py` + `scripts/build-index.py` [258,259] gate x, `check-delta-record.py` [260..263] …, `check-size-ratchet.py` + `spec-ratchet.json` [264,265] seeded at that freeze" | INV-250..265 | KEEP | wiring — script↔code map, gate-letter, arming/seeding |
| "the shared parser `guardrails/specformat.py` is their one reader" | INV-250..265 | KEEP | wiring (shared reader; "specformat" grep-empty) |
| "gate reach for the format family: each gate's green line names the files opened and the rows matched of scanned" | INV-269 | DUPLICATE | R282.1 (6478): "print a green line that names the files it opened and the count of rows it matched of the rows it scanned" |
| "`gatelib`-shaped reach lines asserted in each gate's own tests" | INV-269 | KEEP | test-assertion wiring ("gatelib" grep-empty) |
| "the arming rule: the whole spec converted in one delivery and every format gate armed in that same delivery" | INV-270 | DUPLICATE | R277.19 (6342) convert whole document in one delivery; R277.20 (6343) "every gate … *shall* arm in that same conversion delivery" |
| "the suite-riding armed tests are the record" | INV-270 | KEEP | evidence/wiring note (not in spec) |
| "the generated index carries locations only" | INV-271 | DUPLICATE | R278.6 (6368): "The generated index *shall* carry locations only" |
| "the criteria and the glossary are the authored home of every code's plain statement" | INV-271 | DUPLICATE | R278.5 (6367): "The criteria and the glossary *shall* be the authored home of every code's plain statement" |
| "`check-description-field.py` retired with that stated successor" | INV-271 | DUPLICATE | R278.7 (6369): "the description-field gate — `check-description-field.py` … *shall* retire, with the criteria and the glossary as its stated successor" |
| "the test-matrix format member: node-grouped criterion rows inheriting the family laws from the spec format" | INV-272 | DUPLICATE | R283.1 (6493) inherits the family laws from `docs/spec-format.md`; R283.5 (6503) rows group into node blocks |
| "defined in `docs/test-matrix-format.md`" | INV-272 | DUPLICATE | R283 Context (6485): "The matrix's own definition … lives in `docs/test-matrix-format.md`" |
| "armed at the matrix conversion delivery" | INV-272 | DUPLICATE | R283.7 (6508): "The matrix's conversion *shall* follow the family's one-delivery arming rule … this member's gates arm inside that same delivery" |
| "ROADMAP 477" | INV-272 | KEEP | roadmap-row pointer |
| "the matrix-reference gate and its builder sibling: the generated spec-anchor-to-row-ids Reference, output only, body and table agreeing, reach on the green line" | INV-273 | DUPLICATE | R284.1-3 (6522-6527) build Reference from body rows, output-only, disagreement reds; R284.4 reach on green line |
| "arms at the conversion delivery" | INV-273 | DUPLICATE | R284.4 (6531): "stay unarmed until the conversion delivery, *shall* arm in it" |
| "the matrix row lint homed in the traceability suite's row check, extended to name the offending row and state its reach" | INV-274 | DUPLICATE | R285.1/R285.2 (6545-6546) red naming the row; R285.3 (6547) state reach on the green line |
| "arms at the conversion delivery" | INV-274 | DUPLICATE | R277.20 (6343) family arming rule; R285.4 (6551) conversion-delivery landing |
| "the roadmap format member: live wish rows inheriting the family laws from the spec format" | INV-275 | DUPLICATE | R286.1 (6565) follows the family genre by reference to `docs/spec-format.md`; R286.2 (6566) one table of live wish rows |
| "defined in `docs/roadmap-format.md`" | INV-275 | DUPLICATE | glossary "roadmap format" (199) + R286 Context (6557): "The page `docs/roadmap-format.md` defines the member" |
| "arms at the queue conversion delivery" | INV-275 | DUPLICATE | R286.3 (6570): "the whole document moves at once … and the row lint arms inside it" |
| "ROADMAP 480" | INV-275 | KEEP | roadmap-row pointer |
| "[target]" | INV-275 | KEEP | target marker |
| "the live-body law: a terminal-exit row moves verbatim with its delivery report to the monthly queue-archive file in its closing commit" | INV-276 | DUPLICATE | R287.1 (6588) move verbatim with delivery report in the closing commit; R287.2 (6589) a calendar month's rows in one archive file |
| "the rotated-manifest and the doc-rotation gate [INV-209] proving nothing lost" | INV-276 | DUPLICATE | R287.3 (6590): "cross-check the manifest against the archive under the nothing-lost gate, a moved row found in neither the body nor its archive reading as a violation" |
| "arms at the conversion delivery" | INV-276 | DUPLICATE | R277.20 (6343) family one-delivery arming rule |
| "[target]" | INV-276 | KEEP | target marker |
| "the queue row lint homed in the traceability suite's row check: five cells, closed status and class vocabularies, ascending ids, reach on the green line" | INV-277 | DUPLICATE | R288.1-4 (6612-6618) five cells / status+class vocabularies / ascending ids / red naming row; R288.5 (6619) reach on green line, homed in the traceability tests |
| "arms at the conversion delivery" | INV-277 | DUPLICATE | R277.20 (6343) family one-delivery arming rule |
| "[target]" | INV-277 | KEEP | target marker |

## Pins-field provenance list

Every pin label in the guardrails **pins** field carrying a date, a session number, or a landed-row provenance note:

- `guardrails/check-push-reach.sh:1` — "the reach map's deciding script, gate b's scope: prose stand-down · the scoped middle road · full, **row 362**" (roadmap-row provenance)
- `guardrails/check-suite-budget.sh:1` — "the suite wall-time budget's net, **row 361**" (roadmap-row provenance)

No pin in this node carries a calendar date or a session number; `landed` appears only inside the behavioural description of `check-landing-next-steps.py` (a `landed`-flipping commit), not as this pin's provenance. Provenance-pin count: 2.
