# onboarding-card — owns-field relocation audit

The **owns** field carries bare codes and no prose (INV-87, INV-88). Nothing to relocate.

## Pins carrying provenance
- `docs/norms/onboarding-card-2026-07-10.html` ("the frozen norm") — the pin path carries a date (2026-07-10), the frozen-norm snapshot date. (Not a landed-row note; a dated artifact filename.)

Count: 1.

(The "session 32, F-onboarding" note sits in the responsibility line, not the pins field.)
