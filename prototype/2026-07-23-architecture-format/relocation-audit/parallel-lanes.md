# Relocation audit — node `parallel-lanes`, owns field

Source: `prototype/2026-07-23-architecture-format/out/ARCHITECTURE.converted.md`, `### [node: parallel-lanes]` (owns at line 77, pins at line 79).

The owns field's prose carries no per-anchor spec restatement. It is four semicolon-separated meta clauses: a wiring note (where the live half's law is carried), a target/ROADMAP pointer (the branch road's status), and two ownership-split notes (what stays base-rulebook's and product-prover's). Every fragment is KEEP — architecture-only ownership/wiring/target information that the spec does not and should not carry. Where a clause cites a spec fact as its justification, that fact is noted, but the clause's assertion is the ownership/status boundary, which lives only in the architecture.

| fragment (quoted; shortened past 40 words with …) | anchor | class | evidence |
|---|---|---|---|
| "the node's live half — the pen and the claim's atomicity, the cap, the graph, the one-row landing commit and its clean-tree precondition, the re-fence after a landing, and the independence re-check a re-door fires — is carried in shipped skill text today and pinned below" | INV-2, INV-39, INV-49, INV-131, T-23 (live-half set) | KEEP | Wiring note: its assertion is that the live half's law "is carried in shipped skill text today and pinned below" — a carrier/home statement (text lives in the two skills, ownership stays at this node), not a rule the spec states. The em-dash list only names the owned anchors. |
| "the branch road is specified with its carriers still ahead (the lane branch and its walk, the pen-moves-main clause, the fast-forward landing, the conflict law, the vendored project-instructions line), riding ROADMAP row 386 [target] with the prover's station as its net meanwhile" | E-34, T-23, INV-198, INV-199, INV-200, INV-201 (branch road) | KEEP | ROADMAP-row / [target] pointer: names "ROADMAP row 386 [target]" and the interim net (the prover's station). Implementation-status wiring the spec does not carry. |
| "also carries the mechanism the worktree-isolation default fires, the condition itself and the session identity the pen tie-break orders on staying base-rulebook's" | INV-105, INV-117 | KEEP | Ownership-split / wiring note: this node carries the firing mechanism while the isolation condition [INV-105] and the session identity [INV-117] "stay base-rulebook's". The ownership boundary is architecture-only; spec states the rules (R83.3 / R82.2) but never which node owns them. |
| "the restructure merge gate stays product-prover's, since it judges a restructure's delta by token identity, where an ordinary lane's landing gate is the full suite on the rebased tree" | INV-111 (contrast: INV-39) | KEEP | Ownership note (why this anchor does NOT sit here): "the restructure merge gate stays product-prover's". The two justifying facts do echo the spec — INV-111 line 4011 "prove content survived by a word-token multiset check and a punctuation multiset check", INV-39 line 1858 "run its gate — the full suite plus the guardrails — on a tree holding nothing of any other lane's unfinished work" — but they serve the ownership assignment, which the spec does not carry. Kept. |

## Pins provenance

Pins field (line 79) labels carrying a date, a session number, or a landed-row provenance note:

- None. Every pin label is a plain content gloss (`:451` cap/pen-stages/re-fence, `:464` graph at queue-take, `:94` mid-work re-door, live-base `:78`/`:81`, `scripts/open-lane.sh:1` the lane-open act). No label carries a calendar date, a session number, or a "row N landed …" provenance note. The trailing "the branch road's carriers land with ROADMAP row 386 [target]" is a forward-looking target pointer, not a landed-row provenance note.
