# publish — owns-field relocation audit

The **owns** field carries bare codes and no prose (E-20, INV-44, INV-96, INV-119, INV-181, INV-228). Nothing to relocate.

## Pins carrying provenance
None. (Pins are skill/script file paths only; no date, session number, or landed-row note. The INV-228 release-note description in the pins field is a wiring note, not a provenance note.)
