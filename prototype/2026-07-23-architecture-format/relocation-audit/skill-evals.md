# skill-evals — owns-field relocation audit

The **owns** field carries a bare code and no prose (E-19). Nothing to relocate.

## Pins carrying provenance
None. (Pins are eval/test file paths only; no date, session number, or landed-row note. The "session 8, row 94" note sits in the responsibility line, not the pins field.)
