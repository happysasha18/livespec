# snapshot — owns-field relocation audit

The node heading carries a [target] mark. The **owns** field carries bare codes and no prose (E-7, A-6). Nothing to relocate.

## Pins carrying provenance
None. (Pins field is empty: "— (spec'd; code still ahead)".)
