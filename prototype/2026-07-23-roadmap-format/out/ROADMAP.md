# live-spec Roadmap (dated version: 2026-07-23 — updated at every edit; SPEC M-3)

The wish queue: the live record of what is asked of the product and where each ask stands. A wish is a
request for a change the product does not yet carry, and it lands when the delivery that completes it
ships. Intake is continuous, a wish entering the moment it is spoken; execution is serial, the current
landing finishing before the next starts.

The roadmap is a member of the format family. Its shared rules — the closed-vocabulary glossary, the
keyword form, the no-capitals rule, the trailing code anchor, the no-history law, the comprehension
gate — live once in `docs/spec-format.md` and hold here unchanged. Its own rules — the row shape, the
status and class vocabularies, the live-body law, the row lint — are defined in `docs/roadmap-format.md`.
The class cell names the wish's size, one vocabulary shared with the spec: *bug*, *small*, *surface*,
or *large*, with a priority mark when it is other than normal. The status cell carries one of *queued*,
*in-work*, *deferred*, or *far*, each with its date, a *deferred* row naming its revisit trigger. A
bracket code such as `[INV-277]` points to its home in `PRODUCT_SPEC.md`; a reader may ignore it.

The body below is the live queue, one row per open wish, the rows in ascending id order. When a wish
reaches a terminal exit it moves verbatim to the month's archive under `docs/queue-archive/` in the
commit that closes it, and the rotated-manifest block records the move.

<!-- rotated-manifest -->
Rotated closed rows (base rule 10 — nothing lost; the archive keeps every moved row, grepable by number; the live queue below holds live material):
- rows 14, 27, 33, 42, 43, 62, 63, 67, 101, 121, 172, 189, 194, 196, 200, 201, 202 → docs/queue-archive/rotated-ROADMAP-2026-07-18.md
- rows 47, 59, 64, 99, 107, 109, 110, 115, 135, 136, 137, 138, 139, 145, 149, 150, 151, 152, 154, 155, 156, 157, 158, 159, 160, 161, 162, 188, 195, 209, 210, 211, 212, 213, 214, 216, 218, 219, 222, 223, 224, 225, 226, 227, 228, 232, 233, 237, 239, 240, 242, 244, 245, 246, 248, 249, 250, 251, 252, 253, 254, 255, 256, 257, 258, 259, 262, 263, 264, 265, 266, 267, 268, 269, 270, 271, 272, 273, 274, 275, 276, 277, 278, 280, 281, 282, 283, 284, 285, 286, 287, 288, 289, 290, 291, 292, 293, 294, 295, 296, 297, 298, 299, 300, 301, 303, 304, 305, 306, 310, 311, 312, 313, 314, 315, 316, 317, 318, 319, 320, 321, 322, 323, 324, 325, 326, 327, 328, 329, 330, 331, 333, 334, 335, 336, 337, 338, 339, 340, 341, 342, 343, 344, 345, 346, 347, 348, 349, 350, 351, 352, 353, 354, 355, 356, 357, 358, 359, 360, 361, 362, 363, 364, 365, 366, 367, 368, 369, 370, 371, 372, 373, 374, 375, 376, 377, 378, 379, 380, 382, 383, 384, 387, 388, 390, 391, 392, 393, 394, 395, 397, 402, 403, 406, 407, 408, 409, 413, 414, 415, 416, 417, 418, 419, 422, 423, 429, 430, 431, 433, 434, 438, 439, 441, 442, 443, 444, 445, 461, 462, 463, 464, 468, 470, 476, 477, 478 → docs/queue-archive/rotated-ROADMAP-2026-07.md
<!-- /rotated-manifest -->

| # | Wish (plain words) | Class | Status | Decision / acceptance |
|---|---|---|---|---|
| 44 | Learn from other projects/frameworks — study how others run spec-driven / agent-driven development, harvest ideas worth folding (Alexander 2026-07-05: learn from other projects — but that is its own separate version bump) (status note: deferred — own version bump; HOMEWORK DONE 2026-07-05 evening) | large | *deferred* 2026-07-05 — revisit trigger: see the status note | Two research reports in: `docs/research/2026-07-05-methods.md` (spec-kit/BMAD/Kiro/Shape Up/Google/Amazon — top-10 candidates incl. regression-fence sentences, waived-risk verdict, non-goals, facet curation) + `docs/research/2026-07-05-skill-patterns.md` (Anthropic/superpowers/Trail of Bits — top-10 incl. when-NOT-to-use sections, skill evals, loadability check). Candidates go to Alexander on a decision page 2026-07-05; his picks become queue rows. Revisit trigger unchanged: lands as its own MINOR |
| 48 | Maintenance skill with measurement PLUGINS — after shipping, the product is MEASURED: e.g. a Google Analytics plugin; each user story / canonical axis states how (or explicitly whether) it is measured in the field; findings feed back into the queue (Alexander 2026-07-05) (status note: deferred — own version bump) | large | *deferred* 2026-07-05 — revisit trigger: see the status note | Revisit trigger: first host with a live audience; design pairs with row 47 (feedback in → measurement in) |
| 49 | A/B tests as a pack capability — for software hosts (not every host: a book has none), a wish may land as an EXPERIMENT with two variants and a measured pick; builds on row 48's measurement layer (Alexander 2026-07-05) (status note: deferred — own version bump) | large | *deferred* 2026-07-05 — revisit trigger: see the status note | Revisit trigger: row 48 lands; spec must state where experiments live in the wish lifecycle |
| 54 | Onboarding step — "understand who you're working with": at attach (or first run on a new machine/human) the pack builds or loads the human's profile — the human tells about themselves and/or agrees to a data pull; nothing written without their word (INV-9 holds) (Alexander 2026-07-05) (status note: **build legs LANDED 2026-07-07 ~08:33, session 23; field leg OPEN — first real run rides the next real founding/adoption** (door: feature; kind: skill; map: attach — the founding walk, the step runs before any founding question resolves; prover CROSS-LINK docs/prover/2026-07-07-row54.md — 5 findings, 4 folded in the delta, 1 recorded (the open field leg); architecture: assignment only, B-3 → attach; delegation: the whole mechanical bundle (red-first test, all inserts, template, versions, five pin bumps) ran on a Sonnet worker (~15 min senior saved) — the worker STOPPED correctly on one brief defect (the requirement's test row belonged in the attach component's section; my brief placed it by number order), senior placed it, zero silent fixes; SPEC v0.15.54 B-3 · base 0.1.21 · adopt/ADOPT.md first-breath paragraph · templates/profile.template.md NEW · M-165 · `test_onboarding_step` red-proven then green · pack 0.8.66 · suite 163 green · installed copies synced)) | surface | *deferred* 2026-07-07 — revisit trigger: see the status note | Done when: ADOPT/bootstrap gains the step with a done-when — MET; profile template exists — MET; first real run recorded — OPEN (the row stays in-work on its field leg) |
| 55 | (prover 2026-07-05-architecture, F1) Snapshot machinery gets its own queue row — SPEC S-0 promises every [target] is owned by a row, and the snapshot (E-7, A-6: baseline of the last accepted run, declared-scope diff) had none; design pairs with the guardrails scaffold (row 3) which consumes its diff (status note: **design LANDED 2026-07-07 ~10:51, session 23; machine leg stays [target] on row 3** (door: feature; kind: skill — design prose; map: the snapshot machine in the bounds-holding section; prover folded inline per the small-delta law — the git-as-archive argument closes D-3 on its current pick with the revisit trigger kept, the E-8 tracked-folder law cross-cited, the heavy-surface hash edge stated; delegation: kept on senior, the work was smaller than a brief; SPEC v0.15.58 E-7 design + D-3 Decided · M-169 · `test_snapshot_design` red-proven then green · pack 0.8.70 · suite 168 green)) | surface | *queued* 2026-07-07 | Done when: snapshot design spec'd, D-3 decided with it — MET; baseline advance wired to *landed* — MET (the law names the transition and the declared-scope asymmetry); declared-scope diff feeds the guardrails — MET as the named seam and the E-6 check clause (the mechanical slice rides row 3, said) |
| 69 | product-prover SKILL.md: extract the category table + glossary into references/ per skill-creator sizing guidance (status note: deferred) | small | *deferred* 2026-07-05 — revisit trigger: the next edit to the product-prover skill | Source: skill-creator eval; deferred |
| 93 | Design-sync wiring (E-18 [target] machine): base default + host-profile switch vocabulary + communicator/verify channel lines + first real run on a visual host; lands with/after row 86 (work-kind axis) (status note: **pack-side half LANDED 2026-07-05 ~22:20, session 8; row WAITING** (door: feature; kind: skill; fences held: E-13 · INV-14 · base rule 17) — wired: E-18 honest split, base 0.1.9 switch off-by-default, communicator/pipeline channel lines, M-088 + test, suite 67 green. Remainder: the machine's FIRST REAL SYNC on a visual host (track-coach/tlvphoto window) through Alexander's gate) | surface | *deferred* 2026-07-05 — revisit trigger: see the status note | Done when: E-18 loses its [target]; a real host synced with the human's gate shown working |
| 95 | Marathon phase — play, watch, learn (Alexander 2026-07-05 evening, back from the sunset: this is a marathon not a sprint — play with the projects, test the agent out, watch how he runs projects): NO version race — 1.0 has no date; the pack proves itself on real project play; Fable OBSERVES how Alexander leads the different windows (read-only: their journals/transcripts) and folds what it learns as inbox wishes/rows (status note: queued 2026-07-05 evening (mis-dated 07-06 at intake; corrected against git clock, session 8), frames everything after 0.8.0) | large | *queued* 2026-07-05 | Done when: it never is — this row is the phase's charter; concrete lessons land as their own rows |
| 96 | Feedback loops, raised (his word tonight — we talked about somehow building feedback loops — + INV-21's success measures now wait on a reading machine): the feedback → measurement → A/B family (deferred rows 47-49) moves from the deferred tail to next-up after the tlvphoto lessons (status note: queued 2026-07-05 evening (mis-dated 07-06 at intake; corrected, session 8; priority raised on his word)) | surface | *queued* 2026-07-05 | Done when: rows 47-49 re-scoped against INV-21 and the first feedback loop runs on a real project |
| 100 | **The problem ledger** (Alexander 2026-07-05 ~23:00, watching tlvphoto transcripts: if there's a problem, especially a recurring one, it has to be solved — solved!! Either solve the problem or agree that it isn't one; this should never happen at all): recurring OPERATIONAL noise — flaky test harness, missing deps, environment errors, anything that crashes mid-work — gets a per-host DYNAMIC LIST; an entry seen a SECOND time gets an owner that moment: a queue row (solve it) or an explicit dated "agreed non-problem" with the human's word; a third silent recurrence of a listed signature is a defect of the METHOD itself, regardless of the day. Distinct from the recurring-BUG rule (that covers the product; this covers the workshop). Evidence at intake: tlvphoto's "element not clickable: #ex-skip" ×5, "readyState=complete" timeout in two different sessions, missing PIL — all retried, none owned (status note: **pack-side LANDED 2026-07-05 23:39 (git), session 10; row WAITING on the first foreign-host run** (door: feature; kind: skill+infra — two legs, each step's form named: spec full delta · prover CROSS-LINK record row100, 5 findings ALL folded (owned-entry recurrence, SOLVED's actor, archive home, signature merge, worker-brief seam) · architecture assignments only (INV-23 → base-rulebook, E-24 → templates, dogfood pin → package-docs), no re-prove per the doc's own rule · facet sweep: headless, the one explicit N/A sentence in the spec · design-sync: stands down, no visual product; fences held: T-9 bug lane · recurring-BUG re-door stays the product's law · INV-9 · INV-4/E-22 · E-8 · ACT-3 · INV-11 · M-1; delegation savings: the whole implementation bundle — tests, template, ledger, rule-19 insert, 5-skill pin sweep, VERSION, row-103 insert, red-first runs — went to a Sonnet worker, ~15 min senior time saved; the worker correctly STOPPED on two brief defects (M-105's owning node, a wrapped-line assertion), senior folded both) — SPEC v0.15.10 E-24+INV-23 · base rule 19 (0.1.12, pins swept across the five working skills, installed copies synced) · templates/PROBLEMS.template.md · the pack's own ledger OPENED with two live entries: invented-time OWNED by new row 103 (the ledger's first real catch mechanized) + zsh `===` AGREED NON-PROBLEM recommended, awaiting his word · matrix M-103/M-104/M-105 · suite 93 green · pack 0.8.21) | surface | *deferred* 2026-07-05 — revisit trigger: see the status note | Done when: the ledger's home + shape spec'd and proven — MET; the second-occurrence law in the base skill — MET; the pack's own ledger opened with tonight's live entries — MET; first real host run on tlvphoto/track-coach — REMAINS, rides those windows (this window is fenced to one inbox file per host) |
| 108 | **A feature is interrogated for PRODUCT FIT at intake — a minimal prover on the feature itself** (Alexander 2026-07-06 ~00:25, at night, with the tlvphoto evidence pasted in: a bunch of questions never get asked when a feature is accepted into the system — not as heavy as BMAD, but a minimal prover on the feature is needed — asking about various aspects, how it fits the product's context (navigation for UX, flows for backend) — the agent doesn't even think of this on its own — maybe live-spec doesn't structure the process well enough, because a bunch of feature-adoption questions go unasked): today's facet sweep asks DEVICE facets (phone/touch/empty/a11y/perf, T-13) but nobody asks the PRODUCT-FIT questions — how the feature sits in the user's JOURNEY (his door example: enter → browse → re-enter → stuck at the 10th picture — a navigation loop nobody specced), what neighbouring behaviours it IMPLIES (no-repeat across doors ⇒ state tracking), what the FEEL owes (premium, animation smoothness), what the next feature it invites is (the greeting). The wish: a kind-scaled FIT interrogation at the spec step — product/UX kind asks journey · navigation · continuity-state · feel · invited-next; infra/backend kind asks flows · data lifecycle · failure paths — each answer becoming a spec sentence or a batched question, same law as the facet sweep (INV-18). His five tlvphoto comments forwarded to tlvphoto/inbox same hour (their window owns the fixes; ours owns the METHOD hole) (status note: **landed 2026-07-06 ~10:55, session 12 — repo side whole; ONE OPEN LEG: installed-copy sync rides Alexander's `! sh ~/live-spec/install.sh` (the harness classifier blocks an agent's own hand in ~/.claude/skills, both worker and senior tried)** (kind: skill; door: feature; his morning words folded: the walk is a small prover on the wish — a prover can validate anything, trivially-closable holes closed and WRITTEN how, don't torment the user with questions; prover CROSS-LINK record `docs/prover/2026-07-06-rows108-119.md` F1/F2/F7/F8 folded; architecture: INV-29 → spec-author node; facet sweep + design-sync stood down by name — skill kind; non-goals: no retro-walk en masse (binds forward), no mechanical fit-gate; success measure [default]: the next product-kind wish in an adopted host ships with journey sentences and he never asks "and from here, where?"; eval criteria + re-runs ride the push gate (E-19); delegated to a Sonnet worker with rows 117–119)) | surface | *deferred* 2026-07-06 — revisit trigger: see the status note | Done when: the fit-lens list spec'd kind-by-kind in one home (spec-author's sweep section widened, prover gains the FEATURE-FIT focused mode the way CROSS-LINK exists); every fit answer lands as spec sentence / [default] / batched question, never silently; first real run on the next product-kind wish in any adopted host; suite covers the shipped clause |
| 117 | **Verify-by-deed learns to walk the visit and watch the feel** (tlvphoto inbox 2026-07-06 ~10:10, §1 + Alexander's morning word let's account for that): the product kind's step 8 gains a named VISITOR WALK — the whole journey as the person lives it (first visit · return · cross-entry · from-any-point navigation · exits) — and a FEEL pass: motion quality (easing, duration, choreography) and affordance craft judged against the approved prototype's bar; findings become rows, never vibes. Evidence: tlvphoto's transitions and its open-work button shipped green through the full pipeline and read cheap on sight. Kin: row 108 owns the intake half (journey questions at spec time); this row owns the verify half (status note: **landed 2026-07-06 ~10:55, session 12 — repo side whole; ONE OPEN LEG: installed-copy sync rides Alexander's `! sh ~/live-spec/install.sh` (the harness classifier blocks an agent's own hand in ~/.claude/skills, both worker and senior tried)** (kind: skill; door: feature; source: inbox note §1 + his let's account for that; prover record `docs/prover/2026-07-06-rows108-119.md` F3 folded (SPEC binds THAT, the step-8 cell holds the checklist); architecture: INV-30 → build-pipeline node; stood down by name: facet sweep + design-sync — skill kind; non-goals: tlvphoto's own five fixes stay in their window; success measure [default]: nothing green-but-cheap reaches his eye before the walk names it; eval re-runs ride the push gate)) | surface | *deferred* 2026-07-06 — revisit trigger: see the status note | Done when: visitor-walk + feel pass spec'd into step 8's product-kind form (one home); prover/eval cover the clause; first real run on a product-kind landing in an adopted host; inbox file archived with a pointer at landing |
| 118 | **A taste default may not outlive two landings unreviewed** (tlvphoto inbox 2026-07-06 ~10:10, §2): the [default]-tag is right for velocity — the lane never blocks — but nothing closes the loop: tlvphoto carries 8+ open defaults across two landings and the product reads unfinished everywhere. The law: a landing report LISTS its [default]s; the next human review resolves them (one decision page, communicator rule 10); a default surviving two landings without the human's word is flagged loudly by the gate/report (status note: **landed 2026-07-06 ~10:55, session 12 — repo side whole; ONE OPEN LEG: installed-copy sync rides Alexander's `! sh ~/live-spec/install.sh` (the harness classifier blocks an agent's own hand in ~/.claude/skills, both worker and senior tried)** (kind: skill; door: feature; source: inbox note §2; the spec step answered its own tension: the lane still never blocks — the LOOP closes via the landing report's restated open-defaults list (no counter file, F4) and the two-landing expiry onto the decision page; prover record F4 folded, F5 recorded honestly (age-checker guardrail = a candidate wish standing on its own); architecture: INV-31 → communicator node; stood down by name: facet sweep + design-sync — skill kind; success measure [default]: no default older than two landings anywhere; eval re-runs ride the push gate)) | small | *deferred* 2026-07-06 — revisit trigger: see the status note | Done when: the expiry law spec'd (count home + flag mechanics chosen); communicator's decision-page rule cites it; suite covers; tlvphoto's open defaults become the first real decision page after their window adopts the clause |
| 119 | **A decision card asks in consequences, leaving mechanisms aside** (Alexander 2026-07-06 ~10:40: what you wrote above is understandable, but what you gave me in the HTML is not!!! — the shell-separator card explained the mechanism and he could not tell what he was deciding, twice): every card opens with what the choice CHANGES for the person under each option, in the product's words; options labelled by consequence; mechanism only if it helps (status note: **landed 2026-07-06 ~10:55, session 12 — repo side whole; ONE OPEN LEG: installed-copy sync rides Alexander's `! sh ~/live-spec/install.sh` (the harness classifier blocks an agent's own hand in ~/.claude/skills, both worker and senior tried)** (kind: skill; door: feature — same family as row 116, its decision-page face; prover record `docs/prover/2026-07-06-rows108-119.md` F6 checked (composes with ask-by-showing); architecture: INV-32 → communicator node; stood down by name: facet sweep + design-sync — skill kind; success measure [default]: he answers every card without asking what it means; eval re-runs ride the push gate)) | small | *deferred* 2026-07-06 — revisit trigger: see the status note | Done when: INV-32 spec'd; communicator rule 10 opens cards with consequences; suite covers; the next decision page written under it |
| 128 | **The senior's own movement-end report relapsed into pack jargon** (Alexander 2026-07-06 ~13:20, bouncing the session-13 closing report: what language are you even speaking??? say it again — all of it was incomprehensible — the report led with pack-internal names, Russian calques of doc metaphors included, fourth strike of the jargon family in two days and the first AFTER INV-28 landed): the law exists and did not hold on the senior's own chat, where no eval or test can see; the fix is a walked step, an action carried out — before any movement-end or milestone report, the communicator rules are RE-READ and the draft is passed phrase by phrase through one question: does this sentence stand for a reader who does not live inside the pack (a pack surface it names is explained in the reader's words or dropped) (status note: **landed 2026-07-06 ~13:52, session 14 — whole: the open leg (the reader's read of the landing's own report, INV-26) CLOSED 2026-07-06 ~13:57 on his resume words the report was understandable — zero "what is this?"** (door: feature; kind: skill; fit walk, skill lenses: trigger — a movement-end or milestone report about to be sent · correction — re-read the communicator rules, pass the draft phrase by phrase through the outside-reader question · not-fire — mid-work narration lines, working notes, machine-read docs (prover F2: deliberate scope — narration stays under the report-shape law alone); facet sweep + design-sync stood down by name — skill kind, no visible surface; SPEC INV-34 — the report law is walked afresh each time — the walk's one home in communicator's own new section; matrix M-122 · `test_pre_report_walk` red first, then green; prover record `docs/prover/2026-07-06-rows126-128.md` (F1 folded in-pass, F2 rejected as deliberate scope); fences held: the walk adds no questions — defaults still TOLD, silence stays consent (INV-31); trailing anchors stay legal (INV-28's own law); non-goals: the walk scopes to movement-end and milestone reports; success measure = the acceptance leg itself)) | small | *queued* 2026-07-06 | Done when: communicator states the pre-report walk (one home) — MET (v0.1.19, its own section); the next movement-end report is drafted under it and draws zero "what is this?" from its reader — MET 2026-07-06 ~13:57, his resume words: the report was understandable |
| 129 | **A project knows its own KIND, and the kind evolves** (Alexander 2026-07-06 ~13:27, before the session-13 wipe: you need to understand (and update as needed) what kind of project this is — whether it's a book being written, or just a backend, or just a static site, or a fullstack app — it matters to know this and keep updating it as things evolve, custom kinds explicitly expected): today the pack classifies each WISH (product/infra/skill/prose, T-16) and a host may set one intake default (`work-kind.host-default`, base defaults table) — but nothing states the PROJECT's own kind (a book · a backend service · a static site · a fullstack app · a CLI · a skill pack · a custom kind), asks it at founding/adoption beside the personal-vs-reusable question, records it in ONE home (the host profile), and lets it scale which steps, facets and feel-lenses apply by default across the whole project; and nothing RE-CHECKS it as the project evolves — a static site that grows a backend must update the line on the human's word, journaled, the moment the evolution is noticed, ahead of any audit. Custom kinds join the same way every table grows: through the queue (status note: **landed 2026-07-06 ~15:58, session 16 — repo side whole; ONE OPEN LEG (INV-26): the first real HOST carries a `project.kind` line by deed — rides the next adopted-host window (this window is fenced to its own tree)** (door: feature; kind: skill; map: changes the founding walk + the intake defaults; worked as ONE head with row 132; prover CROSS-LINK record `docs/prover/2026-07-06-rows129-132.md` — F2 folded: the seed proposes, a written `work-kind.host-default` line disposes; F4 folded: project.kind is always asked, never profile-seeded; architecture: INV-36 → attach (B-2's own precedent), assignment only, no re-prove per the doc's rule; stood down by name: facet sweep + design-sync — skill kind, no visible surface; non-goals: no retro-ask for already-attached hosts (the line arrives at the next landing that leans on it), no mechanical kind-linter; success measure [default]: every founding/orient from now on records the line and he never has to re-explain what a project is mid-work; delegation savings: the whole mechanical bundle — matrix rows, two red-first tests, eight skill-text edits, version sweep, installed-copy sync — one Sonnet worker, ~20 min senior saved, zero brief defects)) | surface | *queued* 2026-07-06 | Done when: the project-kind line spec'd (one home: host profile; asked at founding/adopt; re-checked on evolution with a journaled update on the human's word) — MET (SPEC INV-36); the kind vocabulary + custom-kind path stated — MET; composition with T-16 stated in one sentence — MET (three verdicts, one intake breath); suite covers the shipped clauses — MET (M-125, red-proven); the pack's own repo carries the line by deed — MET (`.live-spec/profile.md`); one real host — OPEN LEG |
| 130 | **The pack's skills pass through the standard skill-making skill** (Alexander 2026-07-06 ~13:27, same message: don't forget to keep running things through the standard skill-making skill, every time): the six pack skills are authored by hand and tested by our own evals, but they never walk skill-creator — the standard skill for making and validating skills (format/frontmatter validation, description-triggering quality, eval machinery); run each pack skill through it, fold what it flags, and put the re-walk on the milestone list so it happens every time, recurring (status note: queued 2026-07-06 ~13:32, session 13 (door: feature; kind: infra — tooling for the pack's own workshop; source: his word this session; kin E-19 — our evals stay, skill-creator adds the format/trigger lens). Pre-evidence 2026-07-06 ~16:05 (session 16, his ask): the SIZE lens already walked — all six SKILL.md under the 500-line ideal (largest: product-prover 382, spec-author 343), no references/ split needed yet; row 69's extraction pressure eased, cite this when the full walk runs) | small | *queued* 2026-07-06 | Done when: all six skills walked through skill-creator once with findings folded or rejected-with-reason in a dated record — MET (docs/audit/2026-07-06-skill-creator-walk.md: 13 findings, 7 folded, 4 rejected with reasons, 2 re-queued as row 137); the milestone checklist (M-1 family) carries the re-walk item — MET (SPEC v0.15.28 M-1 clause, matrix M-128, test red-proven then green); the next skill added to the pack walks it at birth — the clause binds forward. **LANDED 2026-07-06 ~16:36, session 17** (walk delegated: three read-only analysis agents, two skills each, raw evidence in scratchpad checkpoints — roughly an hour of senior reading returned as three findings lists; folds: spec-author's prover link repointed to the pack repo, base + prover + communicator descriptions gain their NOT-sides / lose history scars and bare rule numbers (row-68 fence held — pinned phrases verbatim), build-pipeline's PLAYBOOK refs name the private playbook repo, publish gains a worked before/after example; versions: base 0.1.15 · spec-author 0.1.15 · product-prover 0.1.10 · build-pipeline 0.2.24 · communicator 0.1.23 · publish 0.1.1, all six base-version citations re-pinned, installed copies synced; suite 118) |
| 131 | **Work is narrated while it runs** (Alexander 2026-07-06 ~13:57, resuming session 15: don't forget (this was already asked) to report as work happens too — I need that to actually work — it should be written into the project's communication rules as well — the SECOND ask in one day; the morning's word had landed only as a personal-profile line (chat.working-narration), and a habit held only in a personal profile did not carry across sessions, so the law moves into the pack): between the capture echo and the landing report the lane never goes silent — each beat worth a sentence (a pipeline station passed, a load-bearing find, a change of direction) is said as it happens, one-two plain sentences in the roadmap's terms, the same voice as the reports; the mechanical grind stays quiet; a narration line is chat only — no pre-report walk, no questions, the plain-language and bookkeeping laws still bind (status note: **landed 2026-07-06 ~14:18, session 15 — OPEN LEG (INV-26): the next working session narrates unprompted, no third ask** (door: feature; kind: skill; SPEC v0.15.25 INV-35 — the third voice between the echo and the report; communicator 0.1.20: rule 13 + the (e) when-it-fires arm + description NOT-side reworded — narration is a standing habit, never a load-trigger + "thirteen rules"; prover CROSS-LINK record `docs/prover/2026-07-06-row131.md`: F1 folded in-pass (INV-28's line enumeration now names narration lines), F3 folded in-pass (profile line chat.working-narration shrunk to tuning + pointer — one home), F2 folded at the code step (the description rewording), F4 rejected — the narration-vs-"(for myself)" boundary is a deliberate judgment line, FOR-the-reader is the discriminator; architecture: assignment only, INV-35 → communicator; matrix M-124 · `test_working_narration` red first (proven against the pre-edit communicator), then green; the row-68 fence renegotiated in wording alone, its fact unchanged: the description keeps its NOT-side, only the test's pinned phrase updated to the new wording; fences held: INV-34 walk scope · INV-27 echo · INV-31 no-questions · INV-28 laws now explicitly binding narration lines; stood down by name: facet sweep — no visible surface, facets N/A; design-sync — skill kind; non-goals: no cadence quota, no mechanical enforcement — chat has no suite, kin INV-34; success measure [default]: the ask does not recur — no third "narrate as you go" reminder ever needed; delegation: a Sonnet worker ran the matrix/test/skill edits/versions/sync + three suite runs, ~15 min senior saved, and STOPPED correctly twice — once on the senior's wrong matrix anchor (corrected, resumed same tier, logged), once on the row-68 needle conflict (the senior's call); dogfood: the landing session itself narrated by the rule as it built it)) | small | *queued* 2026-07-06 | Done when: SPEC states the law (INV-35) — MET; communicator carries the narration rule (one home) — MET (rule 13); suite covers both — MET; the next working session narrates unprompted — PENDING, the open leg |
| 133 | **"Show me all the features" — the feature map readable on demand** (Alexander 2026-07-06 ~15:52, mid-landing of rows 129/132: maybe we need some kind of commands like "show all features" or something — some kind of transparency): the map now exists as law (spec scenarios + architecture nodes + the rows' `map:` notes, INV-37) but only the agent reads it — a spoken command should render it FOR THE HUMAN: every feature by its plain name, its state (shipped / in-work / queued / idea), what changed lately, in the product's words; likely one rendered page per ask (the render-doc road), kin to the departures board (INV-27 shows in-flight wishes; this shows the WHOLE product) (status note: queued 2026-07-06 ~15:58, session 16 (door: feature; kind: skill — the exchange shape first; a rendered-page form may add a prose leg; map: NEW feature beside the departures board; ask-at-intake: none — the spec step decides the reading's exact form and batches any taste call). **First informal run 2026-07-06 ~16:05, same session, on the pack itself**: the full feature list spoken in chat off the spec's scenarios + the header's shipped-vs-target split, on his ask (can't you just tell me the feature list) — evidence for the spec step: the reading derives cleanly from existing documents, no third document needed) | surface | *queued* 2026-07-06 | Done when: the command's trigger + the map-reading's form spec'd (one home) — MET; a real "show me all the features" ask in any window renders the map and he can tell shipped from queued at a glance — PENDING, the open leg; suite covers the shipped clause — MET. **LANDED (legs 1+3) 2026-07-06 ~16:27, session 17** (SPEC v0.15.27 — new scenario "Asking what the product does", INV-38: read at ask-time off spec scenarios + the header at the promised-tag's own granularity + the queue's open rows, queued NEW-verdict wishes included; no third document; chat by default, rendered page on his word [default]; never fires uninvited; empty host answered honestly; communicator 0.1.22 rule 14 + when-it-fires arm (f); architecture: assignment only, INV-38 → communicator; matrix M-127 · `test_feature_map_on_demand` red first (proven against the pre-edit communicator), then green; prover CROSS-LINK `docs/prover/2026-07-06-row133.md` — F1 statuses at the promised-tag's granularity (must-fix, folded in-pass), F2 queued NEW wishes were invisible to a scenarios-only read (folded in-pass); fences held: INV-27 board scope · INV-37 intake placement · E-14 no-third-document; stood down by name: design-sync — skill kind; non-goals: no standing feature document, no auto-dashboard, no per-feature history. **OPEN LEG (INV-26): the law forbids firing uninvited, so the first real post-law ask rides HIS next "show me all the features", any window**) |
| 134 | **The chat clock gets a mechanical hand** (problem-ledger CHAT variant, re-opened 2026-07-06 15:50, session 16 — the next-session recurrence its 14:18 note named: leads extrapolated up to ~6 min ahead again while the wall clock read 15:50; the habit + the shipped sentence did not hold across sessions, third session in a row ⇒ METHOD defect by the ledger's own law): the reply's leading [HH:MM] stops being handwritten — a harness hook (candidate: a session hook that injects the wall clock into every reply context, wired via settings.json) makes the stamp read off the machine — the hand no longer sets it (status note: **build leg MET 2026-07-06 ~20:42, session 19; field legs OPEN** — built as LANE B of this session's double-lane run by a Sonnet worker in an isolated worktree while lane A (row 139) held the pen (T-18): `hooks/clock-hook.sh` + `tests/test_clock_hook.py`, RED-proven on the absent script (3 tests failing) then green, suite whole in the worktree and re-run on the integrated tree; integrated under the pen after row 139 landed — SPEC v0.15.32 INV-24 chat face gains the mechanical-hand sentence, matrix M-132 (guardrails node), architecture pin `hooks/clock-hook.sh:1`; the landing itself evidenced the need twice: the commit fence BLOCKED row 139's first landing on 20:42/20:43 stamps written at a 20:38 clock, and the session's chat leads had drifted the same ~4 min (occurrence appended to the ledger); INSTALL leg: the agent's own hand was blocked by the harness classifier (self-config), exactly the Done-when's named road — `scripts/install-session-hooks.sh` awaits Alexander's one `!` command; delegation: the whole build = one Sonnet worker off a self-contained brief, ~20 min senior saved; facet sweep + design-sync stood down by name — infra, no visible surface; non-goals: no rewrite of past wrong stamps (prose quoting stays legal, the family's standing scope); success measure [default]: the next session's leads carry machine-read times, zero drift catches; queued 2026-07-06 ~15:58, session 16; map: changes the clock law's chat face (INV-24 family)) | small | *deferred* 2026-07-06 — revisit trigger: see the status note | Done when: the hook exists — MET; installed on this machine (Alexander's `!` hand if the classifier blocks the agent's — it did; his command pending) — OPEN; a full session's leads carry machine-read times with zero drift catches — OPEN, next session's evidence; the ledger entry flips back to SOLVED citing this row — OPEN, rides the zero-drift session |
| 140 | **Budget heuristics — what the method sheds when money or time run short** (Alexander 2026-07-06 ~20:23, session 19: right now I'm on a great plan, money isn't running out — but if it were running out (and time isn't infinite either), what would we do? maybe run tests selectively or economize on something else; heuristics are needed): today the pack runs full rigor always (full suite before push, prover at every gate, senior model on judgment); the wish: a named ECONOMY ladder — which savings are legal at which pressure (selective test runs scoped by touched node, CROSS-LINK instead of FULL prover, cheaper worker tiers, batched landings, narration unchanged — it is cheap) and which rungs NEVER bend (the door law and its tripwires, red-before-fix, the human's gates, the landing report) — so a tight budget is a setting on the ladder, chosen deliberately rather than improvised (status note: **build legs MET 2026-07-06 ~21:26, session 20; field leg OPEN; AMENDED ~21:44 on his word: the rung is asked — or the standing default told — at a project's setup (founding/adoption) alongside the kind question, so the preference is his from day one (SPEC v0.15.36 T-19, base 0.1.19, ADOPT orient line, M-134)** (door: feature; kind: skill; map: NEW feature in the method's rulebook — the SPEC's economy-ladder section, owned by the base-rulebook node; prover CROSS-LINK `docs/prover/2026-07-06-row140.md` — F1 batched gates never license a stale-HEAD push (M-6 unbendable, folded in the delta), F2 a deferred FULL prover pass is a dated debt line the milestone walk re-surfaces, F3 batch-end red bisects by landing order, F4 an explicit host line outlives any rung; homes: SPEC v0.15.35 T-19+INV-40 (the section "When money or time run short") + base 0.1.18 settings row `budget.pressure` + M-134/M-135 + `test_economy_ladder` red-proven on the base then green, suite 130; facet sweep: no visible surface — facets N/A by kind; non-goals: no numeric budgets/token meters, no automatic rung-switching; success measure [default]: the first budget-named session names its rung and sheds aloud, checked by his read)) | small | *deferred* 2026-07-06 — revisit trigger: see the status note | Done when: the economy ladder lives in one home with its never-bend list — MET; suite covers the shipped clause — MET; applied once for real — a budget-named session names its cuts aloud — OPEN (awaits the first session he names a rung for) |
| 141 | **The chat laws get a mechanical voice in every window** (Alexander 2026-07-06 ~20:41, session 19, on tlvphoto pasting a new EX-SHARE section... before the wall's tombstone" into his chat: "can you actually do something about communication??"; the day's audit found the same class twice more — raw plan codes in two other windows): the language law (INV-28) and the narration law (INV-35) live in skills a session may never load — so the same prompt-hook mechanism as the clock (row 134) injects ONE reminder line into every prompt on the machine: plain words do the talking, codes only trail; beats name wish+station, station ends digest, silence past ~10 min owes a heartbeat; the skills stay the laws' homes — the hook reminds, never legislates (status note: **build leg MET 2026-07-06 ~20:49, session 19; field legs OPEN** (door: feature; kind: infra — harness tooling; prover CROSS-LINK `docs/prover/2026-07-06-row141.md` — F1 the two-texts-one-law freshness seam FOLDED mechanically (M-133 pins the reminder's teeth as suite strings, a law rename goes red here), F2 notes the mechanism reaches exactly the windows the audit showed failing (hooks fire regardless of what a session loaded); homes: SPEC v0.15.33 INV-28 delivery sentence + M-133 + `hooks/chat-law-hook.sh` + the shared installer `scripts/install-session-hooks.sh` (covers rows 134+141, one home for the machine-install fact); 3 tests RED-proven on the absent scripts then green; facet sweep + design-sync stood down by name — infra, no visible surface; non-goals: no per-project hook variants (one machine, one line), no enforcement beyond reminding (chat still has no suite — the honest boundary stands); success measure [default]: the next tlvphoto session's chat carries no leading codes — the field is the test; INSTALL leg: awaits Alexander's one `!` command, same classifier road as row 134; map: changes the language + narration laws' DELIVERY (INV-28/INV-35 family))) | small | *deferred* 2026-07-06 — revisit trigger: see the status note | Done when: the reminder script exists and its line carries both laws' teeth — MET; one installer covers clock + laws — MET; suite covers the shipped strings — MET; installed on this machine by his hand — OPEN; the field evidence (a following tlvphoto session with no leading codes) — OPEN |
| 143 | **The architecture step owes measurable quality criteria** (Alexander 2026-07-06 ~21:03, session 20, on tlvphoto's Room first image loading long with no loader and no timing numbers — his question: what happened, did the architect not set criteria? how was measuring timings never even planned? if so the written architecture is poor): today the architecture step pins nodes and seams but demands NO measurable non-functional criteria, so a user-facing slowness is found only when the human notices it, ahead of any check; the wish: the architecture step and the spec's performance facet must produce measurable budgets (e.g. first image ≤ N s cold) plus a timing-instrumentation home for every user-facing surface, and the story's acceptance asserts the budgets; tlvphoto's own half rides its inbox wish (filed 2026-07-06 ~21:04) (status note: **build legs MET 2026-07-06 ~21:44, session 20; field leg OPEN** (door: feature; kind: skill; map: changes the architecture step + spec-author's performance facet; homes: SPEC v0.15.36 INV-41 ("The architecture owes numbers, not only names", binds from a surface's next landing, never retroactively) + build-pipeline 0.2.28 step 3 + spec-author 0.1.16 performance facet + M-136 + `test_architecture_owes_budgets` red-proven then green; prover CROSS-LINK `docs/prover/2026-07-06-rows143-144.md` F1 retroactivity FOLDED; field evidence already breathing: the gallery host landed budgets + a timings export in its own window the same evening, before this clause — the clause makes it law; AMENDED ~21:53 on his word — budgets are NOT frontend-only: the project's KIND proposes what is measurable (backend: latency/throughput/errors; CLI/pipeline: run time, per-unit cost; skill pack: eval pass rate, suite time; prose: what honestly has a number), a no-honest-number quality said by name, never a vanity metric (SPEC v0.15.37, build-pipeline 0.2.29, M-136))) | small | *deferred* 2026-07-06 — revisit trigger: see the status note | Done when: build-pipeline's architecture step + spec-author's facet sweep name the budget-and-instrumentation duty; suite covers the shipped clause; first real use — one host's architecture doc carries asserted budgets |
| 144 | **The task list on his screen speaks plain words** (Alexander 2026-07-06 ~21:22, session 20, sending a screenshot of the session's task list: a thing like this strongly helps to see what is happening — if only it communicated in understandable words; the visible list read "Row 142: prove (CROSS-LINK), matrix M-022/M-129, tests red→green"): the harness task list is a human-facing surface the language law already claims (INV-28) but no rule NAMES it — task subjects and spinner lines must speak plain product words in the chat language, codes only trailing; applied live the same minute (the open tasks renamed), the rule still needs its home in communicator's board family so it survives sessions (status note: **build legs MET 2026-07-06 ~21:44, session 20; field leg OPEN** (door: feature; kind: skill; map: the language law's delivery — board family; HIS CORRECTION ~21:33 folded: task subjects speak plain ENGLISH — the docs language, the list is a work artifact with its own register, apart from chat — the session's first Russian renaming was wrong; homes: communicator 0.1.28 rule 6 task-list bullet + M-137 + `test_task_list_plain_words` red-proven then green; prover record `2026-07-06-rows143-144.md` F2)) | small | *deferred* 2026-07-06 — revisit trigger: see the status note | Done when: communicator names the task list among the language law's surfaces; suite covers the shipped sentence; a following session's task list reads plain by his eye |
| 148 | **The pack's spec reads like its craft wrote it** (Alexander 2026-07-06 ~23:24: make the specs readable, as if a human wrote or at least edited them — a product person, an architect, and so on; right now the specs are written for the agent; put it at the end of the queue, or earlier if it's easy): the human-first SHAPE law already binds (spec-author's "How it reads"; the craft ladder INV-33) — this row makes the pack's own SPEC.md walk it BY DEED: a readability rewrite, section by section, in each section's craft voice (scenarios talk like a person, sentences breathe, codes stay trailing), the anchor-set guard held (identical anchor sets before/after, any delta named), FULL prover re-prove after the reshape; **entry: mockup-first** (INV-43's door arm, declared at intake) — a rendered before/after sample of TWO sections goes to his morning eye FIRST, his taste is the bar, the whole-doc rewrite runs only after his word on the sample; **sample rounds 1–2 KILLED; round 3 STRUCTURE APPROVED, language killed** (round 2 killed ~07:59: rule-book genre out, bar = USE-CASE genre per BMAD+Kiro; round 3 ~08:11: genre stands, three language kills — scissors frame banned FOREVER globally (profile law `language.no-scissors`), overcompressed narrative, non-native English (`language.native-english`); round 4 = plain-English rewrite of the same page, in place; **round 4 ANSWERED ~09:34 (JSON archived): genre+language APPROVED with three adjustments — pre/postconditions stated where they exist · an EPIC grouping proposed · a SERIOUS VALIDATION with a thorough plan BEFORE the whole-doc rewrite (the genre must not break the prover, the anchors, or the mechanical checks — his extremely important, check it immediately); criteria voice = plain sentences, the WHEN/THEN/SHALL caps KILLED (pseudo-dramatic tone)**; kill-list on the decisions page) (status note: queued 2026-07-06 ~23:25, session 22 (kind: prose; door: feature; map: the pack's own SPEC.md as a READABLE document — package-docs node; kin: spec-author's human-first law, INV-33, his standing «spec too jargony» word)) | surface | *queued* 2026-07-06 | Done when: the sample rendered and his word taken on it; the full pass lands with anchor sets identical (or every delta named); FULL prover re-prove green; his eye says a section reads human |
| 163 | **test-author skill — the test methodology extracted into the pack** (inbox wish from the track-coach close, 2026-07-05 late night, born of his direct question whether tests-by-the-method was truly done; the NOT-done half): the method that rebuilt track-coach's suite — matrix organized architecture-node × spec-fact, a pinned assertion-level ladder per fact, real-artifact assertions over string checks, red-first, pinned skip-set, traceability enforcement — lives only in track-coach's TEST_MATRIX header prose, its overhaul inventories, and two build-pipeline paragraphs (steps 4–5); extract it into a **test-author skill** invoked by build-pipeline at steps 4–5 the way steps 1–2 invoke spec-author and product-prover; donor material named in the wish: cross-audit F11 charter sketch, track-coach TEST_MATRIX.md as worked exemplar, s34/s52 inventories as the classification scheme, test_traceability.py as the enforcement pattern; field urgency dated 2026-07-02: two user-visible bugs slipped past ~660 string-only tests — the exact class the ladder prevents (status note: queued 2026-07-07 ~08:19, session 23 (door: feature; kind: skill — a NEW seventh pack skill; map: NEW node beside spec-author/product-prover, wired from build-pipeline steps 4–5; source: inbox/2026-07-05-test-author-extraction.md, archived to docs/queue-archive/ at intake; walks skill-creator at birth per the row-130 clause)) | surface | *queued* 2026-07-05 | Done when: the skill authored + versioned + loadability-gated; build-pipeline steps 4–5 invoke it by name (one home for the method, pipeline cites); SPEC + matrix + suite cover the wiring; skill-creator walk at birth recorded; first real use on a host recorded |
| 165 | **Skill discovery — search for applicable skills at setup and at every struggle** (Alexander 2026-07-07 ~08:28, from the promoter-window audit: five review rounds died on scissors/voice failures that a PUBLIC skill — stop-slop, shipped months earlier — already carried as a written checklist («Any "not X, it's Y" contrasts? State Y directly»); his word: there should be a skill search for the project, especially in a struggle — maybe this could be written into live-spec-base — or maybe it's not only about the spec): the pack gains a skill-search move with two triggers — (a) at project SETUP (founding / adoption, beside the founding questions): scan installed and available skills for ones matching the project's kind and its crafts, propose the fit list with a recommendation; (b) on STRUGGLE — a problem-ledger entry reaching its second occurrence, a taste artifact rejected twice, any recurring failure family: before the next attempt, search whether an existing skill or checklist already owns this failure class, and adopt or reject it by name (the promoter's stop-slop case is the type specimen); adoption practice rides the row (his 08:32 ask on how to borrow without stealing): prefer INVOKING a found skill as-is; fold its lessons by paraphrase plus a named credit line; verbatim text only under its license (MIT-class, with the notice kept) — the pack never republishes unlicensed text (status note: **build legs LANDED 2026-07-07 ~09:38, session 23; field leg OPEN — first real struggle run** (door: feature; kind: skill; map: base-rulebook, INV-65 → base-rulebook; prover CROSS-LINK docs/prover/2026-07-07-row165.md — 5 findings, 4 folded (kin-not-duplicate of the researched-proposal law · the human's word picks · reachable-catalogs honesty · borrowing practice closes the clause), 1 recorded (open field leg); delegation: the whole mechanical bundle on a Sonnet worker, zero deviations, ~12 min senior saved; SPEC v0.15.55 INV-65 · base 0.1.22 rule 20 · ADOPT setup line · M-166 · `test_skill_discovery` red-proven then green · pack 0.8.67 · suite 164 green · installed copies synced)) | surface | *deferred* 2026-07-07 — revisit trigger: see the status note | Done when: the trigger pair + the search's form live in one home, the setup arm rides ADOPT/bootstrap beside the founding questions, the struggle arm cites the problem ledger's second-occurrence law; suite covers the shipped clauses; first real run recorded — a struggling session finds and adopts or rejects a skill by name |
| 166 | **The live work board — what the agent is doing, visible at every moment** (Alexander 2026-07-07 ~09:36, his out-of-turn word: the reporting along the way is apparently bad — he wants it to be clear what the agent is doing at every moment, ideally the whole process, always; can this be made a priority?): a standing visible surface that always shows the CURRENT picture — which task is in hand and at which pipeline station, which workers run with which briefs, which lanes roll or wait, the last landing, the queue head, and what waits on the human — the departures board (INV-27) shows in-flight wishes on ask; this shows the WHOLE working process, always, updated at every station change (status note: queued 2026-07-07 ~09:38, session 23, PRIORITY: his out-of-turn word — next in hand (door: feature; kind: product — a user-facing surface; map: NEW surface beside the departures board, communicator's domain; **entry: mockup-first** (visible surface, the prototype-norm law) — a labelled prototype on REAL current data goes to his eye before the spec delta); **mockup round 1 KILLED ~09:44** ("caveman gibberish-generator": coined workshop words on the board; the bar — the board speaks FEATURE language: each feature by its plain name + its stage, wording drawn from the spec's own genre, zero internal coinages; his diagnosis recorded: the communication struggle feeds on auto-generated text in context); round 2 live (feature-language cards, page self-refreshes)) | surface | *queued* 2026-07-07 | Done when: the board's form + update moments spec'd (one home); the mockup approved and frozen as the norm; the surface ships with its registry row and rendered-level matrix rows; a session updates it at every station change, checked by his eye over one real stretch |
| 168 | **The showing channel adapts to the session's seat** (Alexander 2026-07-07 ~10:57: if the session runs locally it's HTML, but if the agent runs remotely, it comes out in Claude in the browser, and then it's not HTML but a different format — can this be detected and adapted to?): a locally-seated session shows rendered artifacts as local HTML opened in a new browser window (today's law); a remotely-seated session (Claude Code in the cloud/browser) cannot open local windows — the same content goes through the remote seat's own channel: an artifact page the host renders in the human's browser, or the chat itself — same passport, same commentable round-trip, same kill-list discipline. The session DETECTS its seat from environment markers (platform, display, filesystem reach) and SAYS which channel it picked; a local file path handed to a remote reader is a defect of the exchange, exactly like an unopened window (status note: **build legs LANDED 2026-07-07 ~11:02, session 23; field leg OPEN — the first real remote-seat session** (door: feature; kind: skill; map: communicator rule 5 + a new SPEC clause beside the passport/window laws; prover folded inline per the small-delta law — the profile's show line stays the LOCAL arm (said in both homes, no second law), the remote channel inherits passport + round-trip by citation; INV-67 → communicator; delegation: kept on senior, smaller than a brief; SPEC v0.15.59 INV-67 · communicator 0.1.37 rule-5 seat paragraph · profile show-line marked local-arm (playbook pushed) · M-170 · `test_showing_seat` red-proven then green · pack 0.8.71 · suite 169 green · installed copy synced)) | surface | *deferred* 2026-07-07 — revisit trigger: see the status note | Done when: the seat rule in one home — MET; the profile's show line marked as the local arm — MET; first real remote-seat session shows through its channel and says so — OPEN (rides the first cloud-seated session) · Alexander re-raised 2026-07-08 ~09:07: does the session actually KNOW it is remote, and how do we communicate then? — the same open field leg, sharpened: seat-detection is spec'd (environment markers) but unverified in a real cloud seat, and today's session is local, so it cannot be proven from here |
| 170 | A mechanical pre-show lint at the presentation gate (communicator skill): before any text/doc is shown or rendered for a human, scan the human-facing text for BANNED constructs and refuse-with-flag so the author rewrites positively before the human sees it (a warning to clear, never a silent auto-rewrite). First pattern set: the scissors/negation-contrast family — a neighbour denied after a dash or a comma, the deny-then-assert shape, and the Russian negation-then-replacement forms — a GLOBAL PERMANENT profile ban (`language.no-scissors`) that keeps leaking (~7 leaks in two days, memory-alone isn't holding it), run over EN + RU, tuned against real text (the mechanical detector now ships in `scripts/spec-style-lint.py`, catching all four shapes). SIBLING pattern to fold in: coined doc-metaphor titles on human-facing pages (the board's "showcase/benchmark/push-it-through" class, 2026-07-07). (inbox wish from the tlvphoto window, 2026-07-07, per the inbox exception) (status note: queued — OPEN, PRIORITY RAISED by his 2026-07-10 ~00:14 word (a jargon strip shipped on the onboarding mockup that evening — "this is nonsense"; his read: the next user just says that and never comes back — so shown-artifact copy must pass the register gate MECHANICALLY, mockups and prototype surfaces included, before any window opens); the enforcement half of a stated language law; rides the docs pass / global pass) | surface | *queued* 2026-07-10 | Done when: communicator's "before you show" walk carries the lint with a tunable pattern set (scissors first, coined-metaphor titles folded), red-proven on real leaking text, a warning the author clears; runs EN + RU |
| 171 | **The pack works for a host with no hooks and no GitHub** (Alexander 2026-07-08 ~09:07): today the pack leans on Claude Code session hooks (the chat clock, the chat-law reminder, the skill-search prompt) and on git/GitHub (the push gate, the CI mirror, directory submission). A host that has neither — no hook support, no remote, maybe no git at all — must still get the method whole: the pipeline, the living documents, the review round-trip. Work out which parts are the CORE method that is always on, and which are OPTIONAL machinery that degrades gracefully when its host support is absent; and make the pack SAY plainly what it turned off and why, never silently assume a hook fired or a remote exists (status note: queued — OPEN; needs his word on priority (a generalization row, kin to the migration sub-skill D4 and adopt's VCS gate)) | large | *queued* 2026-07-08 | Done when: the pipeline runs end-to-end on a host with no session hooks and no GitHub (git optional), each disabled capability named with its degraded fallback, red-proven that the core method still lands a wish |
| 190 | **The method proposes the engine/instance split and knows how to lead a split project** (Alexander 2026-07-09 late evening: propose separating engine from content BY DEFAULT — the same engine later hangs Bangkok photos or anything else; how do we run projects where the engine is a public repo and the content/infra private?): at founding/adoption, when the product is an instance of a reusable mechanism (his standing reuse answer), the kind question PROPOSES the split — engine repo (public, generic fixtures, content contract per INV-79) + instance repo/home (content, corrections, private fragments); the method states how the pack attaches to a PAIR (which repo carries which documents, one queue or two, where the wish door lives, how a lesson travels engine↔instance), and the placement view says where heavy binary content lives (a git repo of images is the wrong home — object storage / the machine's archive + backup, named per project) (status note: queued 2026-07-09 ~23:53 (door: feature; kind: skill; map: new scenario beside adoption/founding; kin: INV-79 content contract, project.kind law, the reuse profile line; tlvphoto's planned exhibition-engine split is the first real case — its own window executes, this window only teaches the pack)) | surface | *queued* 2026-07-09 | Done when: founding/adoption carry the split proposal + the pair-attachment shape — LANDED 2026-07-10 night (INV-85/86, F-pair scenario, D-6/D-7 open with picks); the content-placement guidance names the binary-storage homes — FIRST LEG LANDED 2026-07-10 ~00:10 (the template's placement view now PROMPTS the deriving agent: heavy binary content names its home, a git repo of large binaries is the wrong home — so a project's own agent reaches the storage answer itself, per his word: teach the skill, never write it into their tree from here); red-proven; validated against the tlvphoto/engine case read-only — OPEN |
| 191 | **Check the field's TEST norms against our test method** (Alexander 2026-07-09 late evening: while you are at Kiro/BMAD, look at their test / test-matrix practice too): diff test-author + the matrix template against BMAD's Test Strategy and Standards (testing philosophy · unit/integration/E2E organization · test DATA management · continuous testing) and Testing Pyramid, Kiro's always-generated Testing Strategy, and one or two strong general sources; adopt what is real for our kinds (candidates already visible: a named test-data/fixture management duty beyond the engine case; the unit/integration/E2E axis beside our level ladder — same research discipline as the architecture check, adopt never copy (status note: queued 2026-07-09 ~23:54 (door: feature; kind: skill; map: test-author + matrix template; research rides the same primary-sources habit, record committed)) | small | *queued* 2026-07-09 | Done when: the diff is recorded with adopted/not-adopted named, each adoption red-proven |
| 192 | **Scenario entry/exit contracts — pre/postconditions where they matter** (Alexander 2026-07-09 late evening, his words: the prover should say, where needed, which pre/postconditions hold — e.g. in tlvphoto how we EXIT scenarios, how we ENTER; "a big topic, defer it but don't forget, write it down"): the spec's flows state how a scenario is entered (from where, with what already true) and how it exits (to where, leaving what behind) — the prover's per-operation pre/postcondition lenses lifted to the SCENARIO level, kin of the entry-symmetry lens and the runtime view's walks; a flow whose entry or exit is unstated is a finding (status note: **DEFERRED on his word 2026-07-09 ~23:54** — revisit trigger: the next prover-method landing, OR the first real spec where a scenario-level entry/exit hole ships a bug, OR his word; recorded so the milestone gate's deferred-row re-scan keeps it alive (door: feature; kind: skill; map: spec-author flow format + product-prover lenses). Trigger fired 2026-07-12 (new prover lenses landed, rows 257/258); activates as its own movement (per `docs/audit/2026-07-12-deferred-trigger-rescan.md`)) | large | *deferred* 2026-07-09 — revisit trigger: see the status note | Done when: spec-author states the entry/exit duty per flow, the prover carries the scenario-level lens, red-proven; validated on one real spec |
| 193 | **Which pipeline stages are we still missing for the kinds we support?** (Alexander 2026-07-09 late evening): a gap analysis — sweep the supported project kinds (product · backend · CLI · skill pack · prose/book · data/ML tier · static-first+edge blend) against the pipeline's steps and ask, per kind, which stage the field considers necessary and we do not name; first candidates from tonight's sources: a RELEASE/DEPLOY stage for hosted products (BMAD: Deployment Architecture + Rollback Strategy; we end at commit-and-show, deploy rides ad-hoc host scripts) · environment promotion (dev/preview/prod) · a data-quality/eval stage for data-ML projects · a security review step (BMAD Security section; our prover asks or names the skip) · post-ship monitoring walk (our success measures + field legs, unwired mechanically) (status note: queued 2026-07-09 ~23:54 (door: feature; kind: skill; map: build-pipeline step list + the work-kind table; method: same primary-sources research, adopt never copy; propose the stage list with a recommendation, his word picks)) | surface | *queued* 2026-07-09 | Done when: the per-kind gap table is recorded; each adopted stage lands as its own row through the pipeline; the not-adopted named |
| 197 | **A live-but-busy worker is never declared dead** (night re-prove F3 on the liveness checks): the two checks' quiet verdict is conservative on ambiguity — a checkpoint showing an in-progress long operation (a render, a suite run) means ALIVE until the worker's own reply or a finished-marker; the watch windows are read from the brief's longest named operation (status note: queued 2026-07-10 ~01:00 (door: feature; kind: skill; map: refines the resume protocol INV-76; source: docs/prover/2026-07-10-night-full-reprove.md F3)) | small | *queued* 2026-07-10 | Done when: INV-76 + base rule 6 state the conservative verdict, red-proven |
| 198 | **A dead worker's output is not yet a finished worker's output** (night re-prove F4): the dead verdict clears the FILES to touch, never the WORK as done; completeness is read from the worker's own checkpoint finished-marker, and an unconfirmed output walks the verify ladder [INV-46] before integration (status note: queued 2026-07-10 ~01:00 (door: feature; kind: skill; map: refines INV-76 beside INV-46; source: same record, F4)) | small | *queued* 2026-07-10 | Done when: INV-76 states dead≠done with the completeness check, red-proven |
| 199 | **The real-device walk row has one defined representation** (night re-prove F5): a human-walk marker in the matrix template that the coverage gate counts as covered-at-real-device-level and the suite lists as owed-to-human — neither a blocking red nor a forbidden silent skip [INV-77, INV-80, E-15] (status note: queued 2026-07-10 ~01:00 (door: feature; kind: skill; map: matrix template + test-author; source: same record, F5)) | small | *queued* 2026-07-10 | Done when: template + INV-77 state the marker, a string test pins it, red-proven |
| 203 | **Chat-cleanliness Stop-hook backstop gets a durable home** (his-word backlog item living only in NEXT_STEPS/memory; tonight's deferred-scan flagged the homelessness): the plan — a Stop-hook self-scan over the outgoing reply as the mechanical net for the chat laws — sits here until its trigger (recurring chat-law leaks) or his word (status note: queued 2026-07-10 ~01:00 (door: feature; kind: infra; map: hooks beside INV-24/28/35; trigger: a chat-law leak class recurring after row 170 lands, or his word)) | large | *queued* 2026-07-10 | Done when: decided on trigger — built or agreed-closed by name |
| 204 | **Readability-restructure word-preservation verifier gets a durable home** (same homelessness class): the prover leg that proves a prose restructure moved meaning nowhere — word-token identity plus punctuation multiset plus the full prover pass — becomes a script the restructure operation must run (status note: queued 2026-07-10 ~01:00 (door: feature; kind: infra; map: scripts beside the anchor-multiset check; trigger: the next big prose restructure, or his word)) | large | *queued* 2026-07-10 | Done when: decided on trigger — built or agreed-closed by name |
| 205 | **Thin-loader tidy** (his-word backlog, now homed): ~/.claude/CLAUDE.md carries only what must precede pack load; sweep it against the profile/pack homes once more (status note: queued 2026-07-10 ~01:00 (door: refactor; kind: prose; trigger: next personal-layer landing, or his word)) | small | *queued* 2026-07-10 | Done when: every loader line proves its must-precede claim or moves |
| 206 | **Pack orchestration guidance** (his-word backlog, now homed): how a session runs many workers — parallelism, phase gating, model tiering (his 2026-07-10 word: the senior orchestrates and validates; opus drafts and audits; sonnet does mechanics), spawn liveness — stated once in the pack (status note: queued 2026-07-10 ~01:00 (door: feature; kind: skill; map: base/build-pipeline delegation section; trigger: the night-run experience is the first field record)) | surface | *queued* 2026-07-10 | Done when: the guidance lands with the night run as its worked example, red-proven |
| 207 | **Artifact registry: pointers, never secrets** (his-word backlog, now homed): a per-project index of handed-in artifacts surviving /clear — records WHERE a thing lives (Keychain name, path), never the value (status note: queued 2026-07-10 ~01:00 (door: feature; kind: skill; trigger: the next real handed-in-artifact loss, or his word)) | surface | *queued* 2026-07-10 | Done when: decided on trigger — built or agreed-closed by name |
| 208 | **Human-facing prose is drafted by a clean writer** (Alexander 2026-07-10 ~00:53, his word after the third onboarding bounce: write ALL docs that way, with a clean head — then the pack's own rules read clean too): any text a human reads — reader docs, spec prose, reports, decision pages, product copy, and the pack's own rule bodies when touched — is DRAFTED by a fresh writer session working from a plain brief (the facts, the reader, the register laws), never by the pack-marinated session; the marinated session briefs, gates, and folds; the law binds forward — existing text walks the road when it bounces off the human or is opened by an edit anyway, never as a blanket rewrite (the restructure-safety risk is named) (status note: queued 2026-07-10 ~01:08, INTENDED TONIGHT (door: feature; kind: skill; map: base rulebook beside the register laws; evidence: tonight's nine clean-drafted docs passed his bar while the marinated-session onboarding text bounced three times)) | small | *queued* 2026-07-10 | Done when: the base rule + spec clause state the drafting road and its forward binding, red-proven by a string test |
| 215 | **Plan for parallel work: the architecture step gains a parallelism lens** (Alexander 2026-07-10 ~10:08, his word: how to plan things so work in the project can be done in parallel — for example, code split into smaller modules; this needs to be accounted for when doing the design): when the architecture step carves nodes, it weighs parallel buildability — smaller, independently testable modules with narrow seams let several build lanes run at once (the lanes-by-graph law INV-49 then has real independent sets to pick); the lens joins the architecture step's checks and the prover's architecture lens; sibling of row 206 (pack orchestration guidance) — the two land together or cite each other (status note: queued 2026-07-10 ~10:08, POST-1.0 on his word (door: feature; kind: skill; map: changes the architecture step — build-pipeline step 3 + product-prover's architecture lens; trigger: after the 1.0 release, alongside row 206)) | surface | *queued* 2026-07-10 | Done when: the architecture step and the prover lens both name the parallelism check, red-proven by a string test; the first real landing that used it cited in the row |
| 217 | **The convergence audit — every quality lock is itself tested to hold** (Alexander 2026-07-10 ~10:22-10:24, his words: convergence of all processes is needed — that's a basic principle — and test everything so it works and always converges; without that we can't go to 1.0): enumerate every convergence mechanism the pack claims — frozen norms, norm-conformance tests, the register lint's growing pattern set, the debt-cap ratchet, the anchor-multiset guard, waiver expiry, version pins — and give each a test that it HOLDS and cannot silently regress; known holes at registration: norm files have no content-fingerprint test (a frozen norm can drift silently), the lint pattern set has no grows-only test, the debt ratchet's only-downward claim needs its own check; 1.0-BLOCKING — rides the M-1 audit as its own pass (status note: queued 2026-07-10 ~10:24 on his explicit word (door: feature; kind: infra; map: guardrails + the M-1 gate list)) | surface | *queued* 2026-07-10 | Done when: the lock inventory is written, every listed lock has its holding test (each new one red-proven), and the M-1 audit's list names the convergence pass |
| 220 | **Audit the last 24 hours of the tlvphoto exchanges — what must enter live-spec** (Alexander 2026-07-10 ~10:43, his word: audit the last 24 hours of communications with tlvphoto and see what can and should be written into live-spec — significant problems surfaced there — this comes first): read the tlvphoto (and exhibition-engine) journals, resume files, prover records, and checkpoint trails of the last day READ-ONLY, name every problem class that surfaced, check each against the pack's existing laws, and queue or land the missing rules here; FIRST in the queue after the onboarding landing on his word (status note: queued 2026-07-10 ~10:44, PRIORITY: first after the onboarding landing (door: feature; kind: skill; map: findings decide — each lands in its own home; the window stays read-only on their trees per the audit-only law)) | surface | *queued* 2026-07-10 | Done when: the audit record lists every surfaced problem with its verdict (already covered · new rule queued/landed · agreed non-problem on his word), and the landed rules cite it |
| 221 | **tlvphoto's move onto the new documentation — teach-only** (Alexander 2026-07-10 ~10:43, his word: help the migration only through instructions or through the inbox, preferred shape: he tells the tlvphoto window load up live-spec and start relaying all the documentation into it): make that one sentence WORK — verify the adoption path + the pair-adoption guide carry a document-relayout walk a cold tlvphoto session can follow alone (their windows execute; this window answers, reviews artifacts read-only, and may file at most one inbox wish); any gap the dry-read finds becomes a fix HERE, never a write THERE (status note: queued 2026-07-10 ~10:44, SECOND after row 220 (door: feature; kind: skill; map: adopt/ADOPT.md + docs/pair-adoption.md read as the migration's own user; source: his spoken plan for today)) | surface | *queued* 2026-07-10 | Done when: the one-sentence start is dry-read against the guides with every step answerable from them, gaps fixed here, and the real migration runs from their windows with this one supervising |
| 229 | **Parametrizability audit — the full settings model** (Alexander 2026-07-10 ~11:00, POST-1.0 on his word: a parametrizability audit — if there were project settings, what would the parameters be, what types and sections): derive the complete settings model from the shipped pack — every parameter that COULD exist, its type (enum · string · boolean · path · conditional), its section (about the project · about the person · the workshop), and its applicability condition (GitHub-or-GitLab exists only when the project kind carries software/skills; author name; languages; …); the existing Card-marked table and the settings card are the seed; the outcome decides whether the catalog grows a typed schema (status note: queued 2026-07-10 ~11:01, POST-1.0 on his word (door: feature; kind: skill; map: the settings ladder + the onboarding card; kin of row 215's post-1.0 batch)) | surface | *queued* 2026-07-10 | Done when: the model document lists every parameter with type/section/condition, reviewed by him, and the accepted deltas become queue rows |
| 230 | **Standing self-audit on every drift axis** (Alexander 2026-07-10 ~11:02, his word: a self-audit needs to be done so specs don't drift in wording — any axis where drift can happen needs to be continuously re-tightened, always): enumerate every axis where drift can happen — spec wording vs the Formal index, docs vs shipped code, installed skill copies vs the repo, matrix vs tests, README claims vs truth, chat register vs the laws — and give EACH a standing re-tightening mechanism with a named cadence (continuous test where possible, milestone sweep where not); the M-1 convergence pass (row 217) produces the axes inventory as its byproduct, this row turns it into standing sweeps; the convergence family's third leg beside 217 (locks hold) and 218 (goal-anchors) (status note: queued 2026-07-10 ~11:02 on his word, POST-1.0 (door: feature; kind: infra; map: guardrails + the existing traceability/freshness machines as the seed)) | surface | *queued* 2026-07-10 | Done when: the axes inventory stands, every axis names its sweep and cadence, each new sweep red-proven |
| 231 | **Version alignment is a stated rule** (Alexander 2026-07-10 ~11:29, at the 1.0.0 release, his word: this is also a rule of sorts — the question is at which moments to sync — a major version, for sure): the pack states WHEN every skill's version aligns to the pack's — at a MAJOR version certainly (done at 1.0.0: pack, all eight skills, the spec header, the pins); the open half is the other moments (minors? never? on his word?) — refine with him after the wipe; also fold here the convergence-locks leftovers F4/F5 (standing anchor guard; branch protection — his one GitHub click) and the skill-description polish trio (status note: queued 2026-07-10 ~11:30 on his word, POST-1.0 (door: feature; kind: skill; map: base versioning rules + the release walk) **SPLIT 2026-07-12 ~01:07 (session 36, the one-story law):** the drafter's read confirmed a bundle — (a) the version-alignment rule's open half waits HIS word on the non-major moments (the ask rides the morning report; **RESERVATION LIFTED 2026-07-12 (orchestrator's call, row 233's landing): codes consume in landing order for index density — this row takes the next free INV/M pair at its own landing, not a held-back one**); (b) the F4 standing-anchor guard queues as its own row when taken, F5 branch protection stays his one GitHub click (morning report); (c) the skill-description polish trio routes to a clean writer (base rule 21) as its own small row. This row stays open as (a)'s home; delegation: the split verdict from an Opus drafter's read, senior accepted.) | small | *queued* 2026-07-10 | Done when: the alignment rule stands with its moments named on his word, red-proven; F4/F5 dispositioned |
| 234 | **Managing one project's independent parallel lanes** (Alexander 2026-07-10 ~13:06, his word: you also need to understand how to manage a project with independent parallel lanes — add it to the to-do): the pack states how a senior session RUNS several independent lanes at once — a per-lane state line in the resume file (each lane names its station and its next step), a heartbeat that names every live lane, lane-scoped workers with non-overlapping write-sets (worktree default on overlap, row 227's half), one report that walks the lanes one by one without mixing them, and the commit law across lanes: one lane, one commit, never a sweep that carries a second lane's files; the 2026-07-10 midday session is the born-of worked example (the catch-up-walk landing lane + the Superpowers/gstack research-and-README lane + a promoter hand-off, run at once) (status note: queued 2026-07-10 ~13:06 on his word, rides the law batch beside rows 215 (parallel buildability) and 206 (pack orchestration) — the three cite each other (door: feature; kind: skill; map: build-pipeline orchestration + the lanes law (INV-49, T-18) + communicator's report shape + NEXT_STEPS template) **ROUTED 2026-07-12 ~01:16 (session 36):** an Opus drafter's read confirmed surface size — a bundle of five separable stories (per-lane resume line = a template structure change · the every-lane heartbeat · lane-scoped write-sets, largely covered by the row-227 isolation law, de-dup owed · the lane-by-lane report shape · the one-lane-one-commit law); enters the full pipeline at the spec step with the one-story split, sequenced with rows 215/206 which it cites; stays queued behind the small-law tail (delegation: the routing verdict from an Opus drafter's read, senior accepted).) | surface | *queued* 2026-07-10 | Done when: the lanes-management rules stand in their homes red-proven, and the first real multi-lane session after them cites the rules with its per-lane commits |
| 235 | **The leave-command: "I'm leaving" reaches a safe stop and says so** (Alexander 2026-07-10 ~13:30, his word from the café: if he needs to leave the café, he gives the command — the agent reaches a stopping point... and he can turn off the computer): one spoken command makes the session wind down to a shutdown-safe state — background workers halted or landed (a sleeping machine kills them, INV-76's plain truth), every lane at its checkpoint, work committed, and one closing line says "you can turn it off now" with what resumes where; the twin habit rides long work the whole way: an occasional plain "about N minutes left" line so the human can plan the exit (kin of row 232's estimates and INV-71's heartbeat) (status note: **first leg landed 2026-07-10 ~14:55 (1.0.4, INV-95/M-224 red-proven, three homes)**; open leg: the first real "I'm leaving" reaching the safe stop with the closing line — expected the same afternoon (door: feature; kind: skill; map: communicator's reporting rules + base checkpoint rule 6 + the worker-liveness law)) | small | *deferred* 2026-07-10 — revisit trigger: see the status note | Done when: the command's walk stands in its homes red-proven, and the first real "I'm leaving" reaches the safe stop with the closing line said |
| 236 | **Projects declare their technologies; an engine parametrizes them** (Alexander 2026-07-10 ~13:48, his word: projects need to state which technologies they use — is the engine hard-wired to Cloudflare and Google Analytics, or is it customizable? this needs to be parametrized and written down; we're not afraid of migrations): a host's docs state in one findable place the external technologies and services the product stands on — hosting, deploy target, analytics, build tools, third-party APIs — each with its role and a swappability verdict (fixed by choice · swappable with a named seam · hard-wired, flagged); for a reusable ENGINE every such dependency becomes a named contract entry like content does (INV-79's infrastructure twin): the engine names the seam, ships a works-without-it default or the swap instruction, and a hard-wired service found in an engine is a finding, not a fact of life; adoption's inventory raises hard-wired services the same way it raises unbacked surfaces; born-of: the tlvphotos engine carries Cloudflare + Google Analytics hard today, and installing it elsewhere has no stated road (status note: queued 2026-07-10 ~13:49 on his word, rides the law batch (door: feature; kind: skill; map: spec — new clause beside the content contract INV-79 and the placement view INV-75; architecture-method's placement section; adoption Phase 2 inventory; the real parametrization of the exhibition-engine is that repo's own row after its adoption)) | surface | *queued* 2026-07-10 | Done when: the law stands in its homes red-proven, the engine content-contract template names infrastructure entries, and the first real engine lists its services with swap verdicts |
| 238 | **The rationalization table — approved 2026-07-05, never built** (found by the 2026-07-10 originality audit: the owner answered "now" in the dated decision record, and no trace exists in the shipped pack; a sound approved thought is never silently dropped, row 233's law): build the mechanism as approved — the idea traces to Superpowers/Trail of Bits practice, source credited in the audit doc — or bring the decision back to the owner if its ground has shifted since (status note: queued 2026-07-10 ~14:15 from the audit finding (door: feature; kind: skill; map: the 2026-07-05 decision record names the shape; docs/research/2026-07-10-originality-audit.md is the finder)) | small | *queued* 2026-07-05 | Done when: the mechanism stands as approved (red-proven) or the owner's fresh word re-decides it, either way citing the audit |
| 241 | **The four guardrail checks ship as runnable code a host attaches** (the web session's read, relayed by Alexander 2026-07-10 ~14:22: Known issues admits the host-side checks ship as descriptions while the README's core claim is executable gates — the moat must be portable, he would close this out before everything else): the pack's own repo gates are executable today (push gate, lints, traceability — the analysts ran them); the unshipped half is the generic, host-attachable form of the four checks (completeness · tests-present · behaviour-traces-to-spec · conflicts) that today each host wires by hand from descriptions; build them as parameterized runnable checks + the adopt wiring that instantiates them per host, with the pack repo and one real host as the two proofs (status note: **first leg landed 2026-07-10 ~17:34 (1.0.8): code shipped by the cloud worker red-first (22 tests), verified by deed at integration, the pack repo attached as its own first host (SURFACES.md + guardrails.config.json + pre-push gate h), Known-issues line retired**; open leg: one real EXTERNAL host attaches (track-coach's catch-up is the natural first)) | large | *deferred* 2026-07-10 — revisit trigger: see the status note | Done when: the four checks run as shipped code on the pack repo AND on one real host from the adopt wiring, red-proven each, and the Known-issues entry leaves the list |
| 243 | **README ↔ the article series, linked both ways** (the web session's distribution point, relayed ~14:22: the funnel exists — Product Prover ↔ live-spec is linked, articles ↔ live-spec is not): when the Verifiable Spec series goes live, the why-section links it the same day; the other half rides the campaign (wish filed 2026-07-10: articles carry the repo link, campaign reports the URLs back by inbox) (status note: queued 2026-07-10 ~14:24, BLOCKED on the campaign's publish — the block rides status reports (door: feature; kind: prose; map: README + the campaign's inbox wish promoter-alexander-live-spec/inbox/2026-07-10-from-live-spec-funnel-linkage.md)) | small | *queued* 2026-07-10 | Done when: both links live — each article names the repo, the README names the series |
| 247 | **The inbox reaches over GitHub — the remote arm of the intake door** (Alexander 2026-07-10, thinking the cloud seat through mid-session: today's inbox law assumes a shared filesystem; a cloud seat, a routine, or another machine reaches a repo only through git): state the remote arm — the deposit stays ONE new file in inbox/, committed with the source in the message and PUSHED (the commit-touching-inbox-only exception already written); the grant model made explicit: the Claude environment links the owner's GitHub account once (his click — done 2026-07-10), each repo is granted to the app once (his click, the ask carries the exact settings path), the grant recorded in the host profile like the push grant; a seat with NO grant fails honestly — names what it lacks and hands the owner the one action (today's routine alert is the live example); DECIDE at landing: do GitHub Issues become the stranger's inbox door for public repos (no push rights needed), swept beside inbox/ (status note: **build legs LANDED 2026-07-12 ~02:41, session 37; field leg OPEN — Done-when (c) "one real remote deposit landed" stays open, closable only by the owner's own real cloud/browser session at claude.ai/code, never self-certified on a local run [INV-94]** (door: feature; kind: skill; map: the inbox law's remote-arm clause + adopt/cloud guidance's remote-settle step, no new surface; fences held: the existing inbox-law paragraphs stand whole (the new paragraph appends after the commit-rule clause in all three prose homes); architecture: assignment — INV-112 → inbox (ARCHITECTURE.md owns-list); other steps stood down by name — skill kind; DECIDE resolved: GitHub Issues as a public repo's stranger door split into its own follow-up queue row, not folded here — a different actor (a stranger with no push rights, not a granted seat) needing its own mechanic and its own field grounding; non-goals: no automation of the grant click itself (the owner's one-time GitHub action stays his); success measure [default]: the next remote-seat deposit either pushes clean under a recorded grant or names the missing grant and hands over the one action, no silent fallback; delegation: drafted by an Opus analyst, applied by a Sonnet worker — one applier-side STOP on block E's grant-ask URL fill-in (the exact per-installation settings URL lives only inside an authenticated github.com session, unreachable by this worker's tools — `gh api /user/installations` 403'd, no browser tool, no prior record in the tree); the orchestrator's call substituted the STABLE settings path (`https://github.com/settings/installations`) plus the named click path in its place, reasoned as exact and durable where the numeric URL is session-locked and reinstall-fragile; zero other deviations; prover short form docs/prover/2026-07-12-row247-inbox-remote-arm.md — 0 must-fix) — SPEC v1.0.21 INV-112 (clause + index) · inbox/README.md (remote-arm paragraph) · adopt/ADOPT.md (step-4 remote-arm extension) · scripts/grant-ask.md NEW (the scripted grant ask, stable settings path) · .live-spec/profile.md (trust.github-grant line) · ARCHITECTURE.md (inbox owns-list) · M-251 · tests/test_inbox_remote_arm.py (4 tests) RED PROVEN then green · pack 1.0.29 · suite 415 green) | small | *deferred* 2026-07-12 — revisit trigger: see the status note | Done when: the remote arm stands in the inbox law red-proven — MET (red 4F then green against the pre-delta tree; suite 415 green); the grant ask is scripted with its exact path — MET (scripts/grant-ask.md, the stable settings URL plus its named click path, per the orchestrator's call on the draft's read-at-draft-time fill-in); one real remote deposit landed — OPEN (field-gated; a local run cannot self-certify it [INV-94], closes only from the owner's real browser cloud deposit) |
| 261 | **GitHub Issues as a public repo's stranger door — a DECIDE** (split off row 247's landing, 2026-07-12 ~02:41, session 37: row 247 stated the inbox's remote arm for a GRANTED seat reaching a repo over git; its fourth point asked whether GitHub Issues becomes the stranger's inbox door for a PUBLIC repo — a contributor with no push rights, no grant — swept beside inbox/ rather than committed into it): the question, unresolved — does a public live-spec repo accept a stranger's wish or feedback through a GitHub Issue, swept by a live session the same way inbox/ is swept; if so, what shape (a label, a template, a required field naming the source) keeps a stranger's Issue as parallel-safe as a granted seat's inbox deposit; row 247's own analysis recommends NOT folding this into INV-112: it is a genuinely different actor (a stranger with no push rights, not a granted seat) needing its own mechanic, and its facts wait on a real public-repo contribution case to ground them — the same "waits on a first real run" posture that gates row 247's own field leg (c) (status note: queued 2026-07-12 ~02:41 from row 247's landing split, **RESOLVED ON RECOMMENDED DEFAULT 2026-07-12 s40 — DEFER (no stranger-door build; a public repo takes a stranger's wish through the existing inbox/ arm by a granted seat, not through GitHub Issues, until real demand appears); his flip to accept-Issues reopens it and opens the build** (a DECIDE, no build) (door: feature; kind: skill; map: none yet — the DECIDE's answer names the home, likely the inbox law's own section or a new stranger-door clause; kin: row 247 (INV-112, the granted-seat remote arm this is NOT), the inbox law [INV-10, E-11]; born-of: row 247's own analysis, `.live-spec/checkpoints/pending-row247-analysis.md`, "Recommendation on the DECIDE" section). **LANDED 2026-07-14 (his flip "all at once", opus orchestrator seat, full pipeline; footprint: cross-cutting — a new arm on the shared inbox law): INV-146 the stranger arm + INV-147 the monitor. A contributor with no push/grant (a private-repo read-only collaborator or anyone on a public repo) opens a templated GitHub Issue or Discussion requesting a source; `scripts/stranger-wish-monitor.py` bridges each open un-surfaced item into ONE committed inbox/ file and records the item's update generation on a marker comment, so from that file on it is an ordinary inbox wish under the git-atomic harvest already proven safe [T-10, INV-11] — the sweep race the first draft carried is dissolved, not re-solved. Re-surface has a named actor (the monitor compares generations). Both channels served (Issue over `gh issue`, Discussion over GraphQL). Two independent adversarial passes (INV-46): the first (on the draft) found 9 holes incl. two must-fix (an unsound GitHub claim-race, a public-repo-only scope missing private read-only collaborators) — folded by the bridge redesign; the second (on the fold) caught the incomplete re-surface actor + an uncommitted-file break + silent deposit failure + a stale-lock wedge — all folded. Ships: INV-146/147 + index rows, ARCHITECTURE inbox node + pins, TEST_MATRIX M-288/289, `tests/test_stranger_door.py` (8 tests, red-first), the monitor script, `.github/ISSUE_TEMPLATE/wish.yml` + `.github/DISCUSSION_TEMPLATE/wish.yml`, inbox/README.md stranger section; prover record `docs/prover/2026-07-14-stranger-door.md`; VERSION/plugin 1.4.0→1.4.1; suite 692 green; monitor verified by deed on the live repo (no-op clean, Discussion read path clean). Field beat that waits [INV-94]: the live Discussion WRITE round-trip (a real discussion bridged end-to-end) — the auto-mode classifier rightly declined the agent creating a test discussion under the owner's identity. Delegation (INV-103): the mechanical born-of sweep (row 315) to a sonnet worker, the two adversarial passes to independent opus workers; the spec/design judgment held on the senior seat.**) | small | *queued* 2026-07-12 | Done when: the owner's word answers whether GitHub Issues becomes the stranger's door for public repos, and if yes, the shape lands red-proven — MET (his "all at once"; INV-146/147 red-proven, suite green) |
| 279 | **live-spec's own shipped tree adopts the impersonal voice and the shipped-language gate** (born of rows 274/275 landing 2026-07-12: the voice rule INV-118 and the machine INV-120 shipped, but the pack's own spec/matrix/roadmap keep ~138+ owner attributions as a deliberate provenance record, held under a dated exemption): decide whether the pack's own docs drop personal attribution into the JOURNAL (keeping dates + reasons) and wire guardrails/check-shipped-language.sh into live-spec's own pre-push + gates.yml as blocking, OR keep attribution as the pack's provenance and bind INV-118/INV-120 only to the products it ships for others; an OWNER-DECIDES (the recommended default: keep the pack's provenance attribution, the exemption stands) (status note: queued 2026-07-12 s38, **RESOLVED ON RECOMMENDED DEFAULT 2026-07-12 s40 — KEEP THE PROVENANCE EXEMPTION: the pack's own spec/matrix/roadmap keep owner attribution as their dated provenance record, and INV-118/INV-120 bind only the products the pack ships for others; the exemption already recorded in INV-118's clause stands (no docs surgery, the ~138 attributions stay); his flip to adopt-the-impersonal-voice reopens the row and runs the attribution-to-JOURNAL move + the gate wiring**; **ADOPTED + LANDED 2026-07-12 (build-worker): LANE A (e7665de) swept 46 owner-name + 88 Cyrillic offences from the shipped set to ZERO active offences — every attribution rewritten impersonally in place (name dropped, date + reason kept), provenance moved to JOURNAL, the INV-118 self-exemption retired; LANE B wired guardrails/check-shipped-language.sh into live-spec's own pre-push (gate i) + gates.yml as blocking, red-first proven (a seeded attribution reds, the swept tree greens); suite 588 green** (door: refactor; kind: prose/infra; map: PRODUCT_SPEC/TEST_MATRIX owner attributions + live-spec pre-push/gates.yml wiring + the allowlist; kin: row 274 the voice, row 275 the machine; born-of: rows 274/275 landing)) | small | *queued* 2026-07-12 | Done when: the owner decides adopt-or-exempt; if adopt, the pack's own docs move attribution to JOURNAL and the gate wires into live-spec's own pre-push/CI with zero active offences; if exempt, the dated exemption is recorded as standing and INV-118/INV-120 bind only shipped-for-others products |
| 302 | **The prover-record guardrail test reads live git state instead of a fixture** (found during row 301's verify, 2026-07-13): `tests/test_guardrails.py::TestGateA_ProverRecord::test_missing_record_fails` runs `check-prover-record.sh` with the default cwd (the real repo) rather than an isolated `_init_repo(tmp)` like its siblings, so whenever the real HEAD is an inbox-only commit the script's remote-deposit carve-out fires and the "must-fail" assertion flips — a red that tracks the repo's commit history, not the code under test (status note: **QUEUED** (a real test-quality defect surfaced by row 301, not part of its delta; resolves for row 301 because that landing commits a spec change with its prover record, moving HEAD off the inbox commit, but it will bite again on the next inbox-only HEAD; fix in the MINOR-gate matrix audit or sooner). door: bug; kind: skill; footprint: single-module (one test)) | small | *queued* 2026-07-13 | Done when: `test_missing_record_fails` builds its own scratch repo (a spec change, no record) and asserts returncode 1 regardless of the real repo's HEAD; suite green |
| 307 | **Cross-cite the async edge-completeness face to the loading/error facet** (MINOR-gate audit R2, 2026-07-13): the async face of edge completeness (pending/arrived/failed) sharpens the spec-author empty/error/loading facet; the facet points at it, but INV-138's spec clause could cross-cite that facet for one-home hygiene (status note: **QUEUED** (a should-clarify recommendation, non-blocking; surfaced by the MINOR audit). door: docs; kind: skill; footprint: single-module) | small | *queued* 2026-07-13 | Done when: INV-138's clause cross-cites the loading/error facet it sharpens |
| 308 | **Complete the product-prover node's carried-versus-owned wiring note** (MINOR-gate audit R3, 2026-07-13): ARCHITECTURE's product-prover node lists its carried lenses, but the wiring note may not name every law it carries as against the ones it owns (status note: **QUEUED** (a should-clarify recommendation). door: docs; kind: skill; footprint: single-module) | small | *queued* 2026-07-13 | Done when: the product-prover node's wiring note names each carried-versus-owned law completely |
| 309 | **Add a `project.design-principles` row to the defaults table** (MINOR-gate audit R4, 2026-07-13): the setting exists (INV-136) but the base defaults table carries no `project.design-principles` row (status note: **QUEUED** (a should-clarify recommendation). door: docs; kind: skill; footprint: single-module) | small | *queued* 2026-07-13 | Done when: the defaults table carries a `project.design-principles` row |
| 332 | **Lift the ship-the-shape / host-owns-the-instance split to its own home** (1.8.0 MINOR-gate design review, 2026-07-15, finding 2): the split "the pack ships an identical body → centralize [INV-158]; the body is host-specific → ship the shape and the host owns the instance" now recurs across INV-125 (no DOM of its own), INV-136/139 (kind principles the product instantiates), E-26 (the removal scanner), and INV-158 (the harness, the centralize case). E-26 was reframed to name the discriminator and cite the family (322/329 landing), so the split is STATED, but it has no single declared home the way the forward-binding law [INV-159] and the suite-honesty class [INV-160] now do. Consider minting the split its own invariant so each site cites one root rather than re-deriving the contrast. (status note: **idea — not a build until it earns one** (a design-review recommendation whose minimal half already landed in E-26; formalizing is optional hygiene, surface only if a third site re-derives the split or Alexander asks). door: docs; kind: skill; footprint: single-module) | small | *queued* 2026-07-15 | Done when: either the split is minted as one invariant every site cites, or a dated note agrees it stays stated-in-E-26-and-cited |
| 381 | **A promised flow reads as a diagram** (split from row 379 on the owner's word, 2026-07-17, after he asked whether a diagram is readable to the agent that later analyses the spec and wondered whether to wait for more capable models; answered from the medium: a mermaid diagram is source text the agent reads directly, so readability carries no risk — the real risk is a flow living twice, once in the paragraph and once in the picture, which breaks the one-home law [INV-3]; his verdict on hearing it: the far tier): a lifecycle or message flow earns a mermaid diagram in its spec section, the flow's ONE home, the surrounding prose pointing at it and stating no step twice; the render road carries mermaid to the rendered page; a redundancy check flags a paragraph re-telling a diagrammed flow. Homes: spec-author, scripts/render-doc.py, the redundancy gate. (status note: **far** — no revisit trigger; waits for the owner to ask (row 382's tier, now the formal `far` status [INV-222])) | small | *far* 2026-07-17 | Done when: a flow's one home is the diagram, the render road carries it, red-proven |
| 385 | **The contract's mechanical arms ship when a host declares its first contract** [target] (split from the multi-agent stage at its landing, 2026-07-17, by the scope law [T-15]: the contract's LAWS ship today as spec [E-33, INV-185, INV-186, INV-187], and this pack contains no data producer, so there is nothing for a permission gate, a freshness check, or a compatibility test to exercise — shipping them here would ship code no red ever proved): three arms, each red-proven against the first real producer and consumer — a producer-side gate reading the card's declared contracts and redding a published field with no dated permission record [INV-185]; a consumer-side freshness check redding an artifact past the consumer's declared staleness bound before any analysis [INV-187]; and a compatibility test redding when the pinned version and the artifact's version diverge [INV-187]. The permission record's own format lands with them (author + date + field, one home in the producer's tree). Revisit trigger: the first host declaring a contract in its card — the promoter↔site pair is the expected first, and that work belongs to those windows [INV-190]. (status note: queued 2026-07-17 [target]) | surface | *queued* 2026-07-17 | Done when: the three arms ship red-proven against a real producer and consumer, and the permission record has one declared format |
| 386 | **Parallel lanes get branches and worktrees, and the three-lane cap is re-read** (the owner's word, 2026-07-17 and ~14:15: can we not run different agents in different branches in parallel? why do we wait? why is this written nowhere? — and then, checking me: there are commands in Claude Code for each agent to work on its own tree isolated, verify. Verified: two mechanisms exist — a subagent launched with `isolation: worktree` gets a fresh git worktree and its own branch, auto-cleaned when unchanged; and `EnterWorktree`/`ExitWorktree` move a whole session into one, with keep-or-remove on exit): three gaps, one row. (a) The law names worktree isolation as the DEFAULT when two lanes' write-sets overlap [INV-105], and the tool's own contract says to use it only on the human's explicit word or a project instruction — so the law's default and the tool's gate contradict, and neither fires; his word today lifts it, recorded as a standing profile line. (b) The method has no BRANCH and no merge road: integration is described only through the pen (one lane edits a shared doc at a time, T-18/INV-39), so parallelism today lives inside one tree and cannot exceed it. (c) The three-lane cap is his 2026-07-06 word and the method holds it as given; he is questioning it now, so the cap is re-read against the worktree road, where the binding cost is the merge rather than the tree. Worked evidence to carry in: this landing ran serial because its eight rows all edit PRODUCT_SPEC.md, which is the graph law working correctly [INV-49]; the parts that did not collide (the gate, the template, the card, the matrix, the tests) ran on four concurrent workers. Homes: base rule 5 / T-18 / INV-39 / INV-49 / INV-105, build-pipeline's trains section, the personal profile's autonomy block. **In flight, unparked: an earlier note parked this behind a field run of the communication layer on the strength of a ranking recorded as his that no message of his backs — struck on the read-back surface (DECISIONS.md, 2026-07-17). The pack imposes no such ordering; the lanes work is open now, and its scheduling is the pack's own call until an exchange says otherwise [row 415].** (status note: in flight 2026-07-18 — the branch-and-worktree road landed; the lane-open ACT landed (INV-214, `scripts/open-lane.sh`, M-395) so a session now performs opening a lane rather than defaulting single-file; the cap re-read holds at three and re-homed to the profile (row 414 landed); the live double/three-lane run in one window stays OPEN, the field beat the orchestrator runs) | surface | *in-work* 2026-07-18 | Done when: the branch-and-worktree road stands with its merge law, the cap is re-read with his word recorded, the law and the tool agree, red-proven |
| 389 | **A remote consumer of a private contract has no read grant** (the 2026-07-17 audit's finding 8): INV-187 sends the consumer to read "over git when remote" and cites [INV-112], whose home defines a PUSH grant on the deposit path; no read grant is defined anywhere in the spec. tlvphotos is a private repo and the promoter↔site pair is the expected first real consumer [row 385], so the first contract read across machines has no stated road. The remote-seat law grows its read arm: what a consumer needs, where the grant is recorded (the host profile, beside the push grant [INV-82]), and the honest failure a grantless consumer gives, naming the grant it lacks [INV-67]. (status note: **LAW arm LANDED 2026-07-18; the real cross-machine read OPEN — field-gated, tied to rows 385/247** (door: feature — grows the remote-seat law with a read arm the audit's finding 8 found missing; kind: infra; map: changes feature — the remote-seat law's read arm beside the push arm [INV-112]; footprint: single-module — the inbox node's grant model; prover CROSS-LINK docs/prover/2026-07-18-rows-393-405-389.md; fences held: INV-112's push grant and INV-82's push-law stand untouched, the read grant added beside them; the read grant's recording home is the host profile [INV-82] as `trust.read-grant`, and the honest grantless failure names the grant it lacks [INV-67]; red-proven — a fixture consumer with no read grant fails honestly naming `trust.read-grant` and the one action, a consumer with the grant recorded passes; stood down by name — FEATURE-FIT and design-sync (infra kind); non-goals: no real cross-machine read attempted — field-gated on the first private producer and consumer, the promoter↔site pair over the private tlvphotos repo [rows 385/247]; success measure [default]: the first remote private-contract read either runs under a recorded read grant or names the missing grant and hands over the one action, no silent fallback; SPEC INV-232 + Formal index + INV-187 read-bullet cross-ref; ARCHITECTURE inbox owns-list + pins; M-413; scripts/read-grant.py + scripts/read-grant-ask.md; tests/test_read_grant.py red-proven then green; no gate-chain change; VERSION held at 2.6.3)) | small | *deferred* 2026-07-18 — revisit trigger: see the status note | Done when: the read grant stands beside the push grant with its recording home — MET (INV-232, `trust.read-grant` in the host profile beside INV-82); a grantless remote read fails naming its grant, red-proven — MET; the real cross-machine read stays OPEN, field-gated on rows 385/247 |
| 396 | **Two agents on one machine talk directly; git carries what needs no answer** (the owner's word, 2026-07-17, refusing the transport premise this stage was built on: agents will not talk through git, that is dumb — there should be direct protocols between agents on one machine, certainly on one machine; git carries the requests that need no particular reply, and notifications): the premise came from the seed and I built INV-183 on it — a remote seat reaches a repo through git alone [INV-112], therefore git is the universal transport and co-location is only a fast path. That reasons from the worst case to the general rule, and his split cuts by the TRAFFIC's kind instead: a thing needing no timely answer takes the durable, auditable, slow road (a file, a commit); a conversation takes a direct channel. The channel law survives untouched — INV-183 is about WHO talks and WHEN, and one sentence inside it about transport is what needs correcting. **The fact nobody has checked: whether two Claude Code sessions on one machine have a direct channel at all.** They are independent processes and may have none by default; a socket, a watched file, or a harness mechanism may exist. That is verified, never derived — check it before designing. This also re-reads the 2026-07-17 research (docs/research/): its closing line was that no prior art fits a repo-as-unit, git-as-transport setting; drop the git premise and the question becomes what direct agent-to-agent protocols exist, which is a different sweep. Carry in from that research: the Contract Net Protocol's directed contract (when the owner is known the award goes straight to it, no announcement, no bids — the pack's referral law under another name) and its measured ladder, 2 messages after learning against ~20 broadcast; and its own transport is the half to reject. (status note: **TRANSPORT-SPLIT CORRECTION LANDED 2026-07-18; the conversation channel's real first use OPEN — field-gated on the harness shipping a listener [INV-231, row 405]** (door: feature; kind: infra; map: changes feature — the transport arm of the two-channel law [INV-183], corrected, plus the new traffic-kind split law [INV-236] beside the inter-agent communication layer; footprint: single-module — the transport law and its guardrails router; the machine-fact CONFIRMED against INV-231 before building, read from the machine not derived: row 405's tripwire established that the socket plumbing — messagingSocketPath, the uds client — is built and switched off and no harness listener has shipped, so there is no live direct channel between two local sessions today [INV-231], and this landing rests on that read; prover CROSS-LINK docs/prover/2026-07-18-row-396-transport-split.md — 0 must-fix; INV-183's transport sentence corrected — the refused git-universal premise removed, the sentence now stating the cut runs by the traffic's kind and handing the detail to INV-236; the traffic-kind split red-proven — `guardrails/route_agent_transport.py`'s `route` routes a no-answer message (a durable record or notification) to the git store road, available today [INV-112, ROADMAP 247], and a conversation needing a live peer to the direct channel, unavailable and naming the listener it waits on [INV-231], a fixture session record with a socket field flipping it available so the read is machine-driven; stood down by name — FEATURE-FIT and design-sync (infra kind); non-goals: no private HTTP broker built (not worth it for a few crossings a day, per row 405), no conversation channel shipped (it waits on the harness listener), no fired state simulated as real; success measure [default]: a no-answer message routes to the store road and a conversation routes to the not-yet-shipped direct channel naming its listener, the router reading the machine for the channel's availability; SPEC INV-236 (clause + index) + INV-183 (transport sentence corrected); ARCHITECTURE guardrails owns-list + router pin; M-417; tests/test_traffic_transport.py red-proven (14F) then green; rides the queue-take scan and the suite, no gate-chain change [INV-83]; VERSION held at 2.6.3)) | surface | *deferred* 2026-07-18 — revisit trigger: see the status note | Done when: the direct-channel fact is verified rather than assumed — MET (confirmed against INV-231, read from the machine not derived: the socket plumbing is built and switched off, no listener has shipped, so no live direct channel stands between two local sessions today); INV-183's transport sentence states the traffic split — MET (the refused premise removed, the sentence now cuts by the traffic's kind and points to INV-236 [INV-236]); the road each kind takes is red-proven — MET (`guardrails/route_agent_transport.py` routes a no-answer message to the git store road and a conversation to the not-yet-shipped direct channel naming the listener [INV-231], red 14F then green). **The conversation channel's real first use stays OPEN** — field-gated on the harness shipping a listener [INV-231, row 405], closable only when a listener ships and never self-certified on a local run [INV-94]. On the second paragraph's research: the git-premise-free re-read the owner asked for is answered for co-located agents by the machine-fact itself — the harness's own dormant unix-domain-socket plumbing (messagingSocketPath, the uds client) IS the local direct channel, built and switched off [INV-231], which is what two long-lived local Claude Code sessions would use; a broader cost-and-portability sweep across the other shapes (a local HTTP endpoint per agent, a message queue, a watched file, an OS notification — what each costs, which survive an agent moving to another machine) stays available as a research row if wanted, off the critical path of this transport-split law. |
| 398 | **A request typed in the wrong zone routes itself to the right one** (the owner's third live word this afternoon, 2026-07-17: every prompt he types carries the implicit first question — whose zone is this — and a foreign request should be deposited automatically in any project that has loaded the pack): a UserPromptSubmit hook, vendored by adoption beside the existing guardrails and active only in adopted projects, injects a compact routing preamble at every prompt — the roster, each agent's zone in one line, and the rule that a request pinning to another agent's zone gets deposited there with a one-line notice back to him. The trigger is mechanical (the hook fires every prompt); the classification stays the session's judgment, since zones are prose. One clause rides along to keep this legal under INV-190: the relaying window deposits his words unchanged and adds none of its own — his instruction carries his own source authority [E-11, INV-193], and forwarding it is delivery of his word; a relay that expands or reinterprets the request would cross back into the defect INV-190 already names. The preamble's size rides the growth-bound law (row 392) under its own declared cap. (status note: queued 2026-07-17) | surface | *queued* 2026-07-17 | Done when: the vendored UserPromptSubmit hook injects the routing preamble in adopted projects, red-proven on a fixture prompt naming a foreign zone (T-24: the fixture deposit lands in the fixture tree with the one-line notice); the no-rewrite clause stands in the spec beside INV-190; the adoption gate reds a pack-loaded fixture carrying no hook; the preamble carries its declared size cap under row 392's law. |
| 399 | **A message is gated where it is born** (this morning's incident, 2026-07-17: a bogus deposit passed the receiving sweep's gate, and the cheapest lesson is to gate both ends rather than only the destination): the depositing agent's own guardrail reads the birth before the file is written, so a birthless message never leaves home; the receiving sweep keeps its existing gate as the second net. (status note: queued 2026-07-17) | small | *queued* 2026-07-17 | Done when: the sender-side arm stands beside the receiving sweep's gate, red-proven on a birthless fixture deposit attempt that never leaves the sending tree. |
| 400 | **A design element shaped after a human institution names the machine constraint that forces that shape** (the owner's atavism word this afternoon, 2026-07-17, generalized from the day's own worked example: the mail shape carried a real cost where it was copied past its forcing constraint — a conversation held in a drop-box — and it carried a real benefit where the machine's own reality forces it — durable state for a receiver that is asleep): where no constraint forces a borrowed shape, the shape is redesigned from the machine's own reality. The law rides the expensive-decision road (row 395) as one of the standing questions its adversarial read asks, and the design reviewer carries it as a lens. (status note: queued 2026-07-17) | small | *queued* 2026-07-17 | Done when: the law stands in the spec; the expensive-decision record's required sections include the borrowed-shape answer; the design reviewer carries the lens; the record gate reds a record missing the section, red-proven. |
| 401 | **Receiving the same message twice does no harm** (found 2026-07-17 while stating the transport law: every message already carries a stable identifier, and the half the tree states nowhere is what happens when that identifier arrives a second time): the receiving sweep keys by the message's own identifier and records what it has already processed, so a replayed deposit yields a stated no-op. (status note: queued 2026-07-17) | small | *queued* 2026-07-17 | Done when: the law stands (INV-202); the receiving sweep keys by the message identifier and records it; a replayed fixture deposit produces no second row, red-proven. |
| 404 | **The field run has a bar and a record derived from what the machinery already keeps** (section 7 of the 2026-07-17 parallelism-and-communication plan: the v2.6.0 laws — cards, births, gates, referrals, terminal states, contracts — need real traffic between the real windows before the major release can claim which of them held): a deriving script assembles the record from the queue archives, the gates' exit codes, and the roster, imposing no new duty on any window; the bar states what counts as evidence — every carded agent green under the card gate; at least one lived crossing of each kind (an earned request with its birth named, a reply reaching a terminal state, a referral traveling back, a gate red on a birthless attempt, a contract declared and consumed if its trigger fires); ten working days elapsed; zero crossings that bypassed the gates, itself evidence in either direction since the layer exists to suppress traffic. The bar doc states the closing rule, including the honest-rarity clause: the run may close on time plus zero-incident evidence, naming any kind that never fired as untested. **Correction folded in at minting (the owner's word, 2026-07-17 ~15:26, overturning the plan's roster-ratification step — whoever is a standing agent will say so itself, the system being dynamic, so his approval of a list decides nothing):** the run opens on no ratification gate — an agent declares itself by carrying a card in its own tree and doing work, and the deriving script reads whichever cards exist rather than waiting on a ratified list. (status note: queued 2026-07-17) | surface | *queued* 2026-07-17 | Done when: the deriving script assembles the record from archives, gate logs, and whatever cards exist with no ratification wait; the bar doc states the closing rule including the rarity clause; an empty required input reds by name (row 384's law applied), red-proven on a fixture archive missing a kind. |
| 405 | **The day the harness ships a listener, addressed push becomes available under the standing transport law** (section 3 of the 2026-07-17 parallelism-and-communication plan: the dormant socket plumbing — messagingSocketPath, the uds client, the local-session recipient kind — is built and switched off, and a private HTTP broker daemon is not worth building to bridge a few crossings a day): a deferred row carries a mechanical revisit trigger — a session record in `claude agents --json` showing a non-empty socket field, re-scanned at every queue-take under INV-129's existing habit — plus the one-shot check script that reads it. When it fires, the live channel gains addressed push: the push carries the pointer, the state stays the truth. (status note: **tripwire-build LANDED 2026-07-18; firing DEFERRED — field-gated on the harness shipping a listener** (door: feature; kind: infra; map: changes feature — a deferred row's mechanical revisit trigger beside INV-129/INV-222 and the transport law [INV-183]; footprint: single-module — the guardrails node's queue-take checks; prover CROSS-LINK docs/prover/2026-07-18-rows-393-405-389.md; the tripwire build is complete and red-proven now — `guardrails/check-listener-tripwire.py` fires on a fixture session record with a non-empty socket field and stays silent on an empty one; the FIRING waits on the harness actually shipping a listener and is not simulated as real, so the row stays DEFERRED with its mechanical trigger [INV-129], distinct from `far` [INV-222] — the build legs land, the firing leg stays deferred, the honest split when real machinery ships now and an external event gates the payoff [rows 247/385 pattern]; stood down by name — FEATURE-FIT and design-sync (infra kind); non-goals: no private HTTP broker built (not worth it for a few crossings a day), no fired state simulated as real; success measure [default]: when a real session record carries a socket field the queue-take's tripwire returns this row to the runnable head; SPEC INV-231 + Formal index; ARCHITECTURE guardrails owns-list + pin; M-412; tests/test_listener_tripwire.py red-proven then green; rides the queue-take scan and the suite, no gate-chain change [INV-222, INV-83]; VERSION held at 2.6.3)) | small | *deferred* 2026-07-18 — revisit trigger: see the status note | Done when: the deferred row stands with its mechanical trigger — MET; the one-shot check script reads the socket field and reports whether it fired — MET (`guardrails/check-listener-tripwire.py`, re-scanned every queue-take under INV-129); the firing stays field-gated on a real listener, not self-certified |
| 410 | **What the pack learns climbs a ladder, and the rung above retires the rung below** (his word, 2026-07-17 ~15:44: the model does not train, so learning must live in notes that never bloat; plus his recorded 2026-07-17 wish that a hook be removed once it stops firing; the ladder itself is the pack's judgment): a lesson starts as an incident, becomes a journal line, may earn a note, may harden into a law, and ends as a machine. Each rung retires the one below, so the store gets better while its shown set stays small. (status note: queued 2026-07-17) | small | *queued* 2026-07-17 | Bounded at the shown end, unbounded at the archive end [his 15:57 word + base rule 10]: a note that graduates into a machine leaves the live index and moves to the archive with its date and its graduation named, and a machine whose fire count stays zero for its declared silence is removed by the same road (`hooks/hook-meter.py --report` already records runs and fires, so the evidence exists). Every number here is a settings-ladder default the pack picks and reports [E-13]: the shown index's size, the silence before a machine retires. Machine: the cap gate plus the meter's report extended to notes. Red-proofs: an over-cap live index reds; a graduated note still sitting in the live index beside its installed machine reds; the archive missing a demoted note reds. |
| 411 | **The orchestrator shows who is working on what, and where the work stands on the map** (his word, 2026-07-17 ~15:54, sending it away himself: nothing today lets him follow who is working on what, and it would be good to have — he placed it in the far backlog, described the orchestrator showing a half-kanban of who works on what and where the work stands on the map, and named 4.0 as its release): a person running several windows and their workers has no view of the whole, and reads the state by asking each window in turn. He named the tier and the version himself, so this row rides the far tier's law [row 382] — it stays out of the what's-left answer and surfaces on his ask or on the rare self-surfacing line [row 403]. (status note: queued 2026-07-17, FAR TIER — his word, no revisit trigger; he named 4.0 as its release; the `far` status now stands formally in the queue vocabulary [INV-222]) | surface | *far* 2026-07-17 | The bricks are already on the machine, and the row records that as its own finding so its cost is honest when it is picked up: the session registry (`~/.claude/sessions/<pid>.json`, verified 2026-07-17) names every live window with its state (busy/idle) and its working directory, which is "who is running"; a claimed queue row is a committed act [INV-2], which is "what they took"; the queue's own rows carry the position on the map. So the missing part is the hand that draws the view and keeps it fresh, plus the answer to what a stalled claim means — his own framing of the neighbouring problem [15:44]: knowing who took a thing and did not do it, and how that is mitigated. Nothing is designed here and nothing is promised: the far tier's law is that a row waits for his ask. |
| 412 | **Opening a lane is an act a session performs, and running independent work single-file reds** (his field report, 2026-07-17 ~15:59: he asked the exhibition-engine window for three items in parallel and nothing ran in parallel at all — his own words, "guess! nothing!"): the lanes law grants up to three lanes [T-18] and grants is all it does. No step of the method OPENS a lane, so there is nothing for a session to perform; the isolation the law calls default is refused by the tool [row 386]; and no gate asks whether independent work rolled in parallel, so single-file work costs nothing and takes the easy road every time. This is the day's own class found in the field by the owner in under a minute: a law with no machine is a wish, and here even the ACT the machine would fire was never written. (status note: in flight 2026-07-18 — the lane-open ACT and the serial-work DISCIPLINE landed (INV-214, `scripts/open-lane.sh`, M-395); the serial-work check is recorded as a discipline rather than a gate (independence is a senior read no diff settles, and lane branches are torn down at each landing so no push-time signal survives — see docs/prover/2026-07-18-rows386-412-414-lane-open-act.md); the live three-lane run in one window stays OPEN, a field beat the orchestrator runs and reports with its cost) | surface | *in-work* 2026-07-18 | Two things this row owes beyond the branch road's prose. First, the ACT: a lane-open step in the pipeline that a session performs — the row's claim commits, the branch is cut, the worker takes the lane in its own isolated copy — with the subagent road usable today under no permission at all (`isolation: "worktree"` carries no gate, verified 2026-07-17), so the first proof of this row is a live run of three lanes on three independent rows in one window. Second, the NET: the dependency graph already computes which queued rows are independent [INV-49], so the graph is the gate's evidence — a session closing a movement that rolled independent rows one after another, with lanes free under the cap and no recorded reason, reds. Acceptance: red-proven on a fixture movement whose graph shows three independent rows landed serially with no reason recorded; quiet when the rows share a surface, when the cap is full, or when a reason is recorded; and the live three-lane run in one window is the row's own worked instance, reported with what it cost. |
| 420 | **Which standing habits should become blocking gates gets audited** (his word, Alexander 2026-07-17 ~18:27: beyond the skill-creator review, reconsider what else makes sense to turn from a reminder into a gate): a habit that is forgotten and is mechanically checkable is a gate candidate, and a taste call stays a habit. The task walks the standing habits and reminders — the profile's chat and proactivity habits, the memory's recurring-failure notes, the reminder hooks — and judges for each whether a machine gate is the right holder. (status note: queued 2026-07-17) | surface | *queued* 2026-07-17 | The output is a list of promotions, each with its reasoning, and then the gates get built. Row 419 is the first member this walk already found, so it lands ahead of the audit and the audit generalises it — row 419 LANDED 2026-07-17 (the skill-creator-review gate, INV-208), so this audit resumes with one promotion already built and it generalises the pattern to the rest. The audit doc `docs/gate-audit/2026-07-18-habits-to-gates.md` ranked four promotions; two more members LANDED 2026-07-18 — candidate 1, the CI-mirror gate (gate u, INV-210: `guardrails/check-ci-mirror.sh` reds a local pre-push gate absent from `.github/workflows/gates.yml`, the CI carve-out set declared with reasons in `guardrails/ci-mirror.json`, compliance reached by adding runnable gates h and n to CI), and candidate 2, the judge-listed gate (gate v, INV-211: `guardrails/check-judge-listed.py` reds a judge missing from the installed `~/.claude/settings.json` hook arrays, the wired-hook set declared in `guardrails/judge-hooks.json`, standing down by name where settings.json cannot be read). Candidate 3, the meta-gate, LANDED 2026-07-18 (gate w, INV-212: `guardrails/check-every-gate-can-fail.py` enumerates the `-- gate X:` markers `pre-push` invokes and reds unless every gate carries a known-red proof declared in `guardrails/gate-red-proofs.json` — a red-first test that drives the gate's own check to a non-zero exit, or a `covered` gate that rides another gate's red; run against the real chain every check-invoking gate already carried its proof, so nothing was back-filled, gate c alone rides the suite; the redundant hardcoded `test_ci_mirror_carries_every_local_gate` retired into gate u). Candidate 4, the runaway-child notice, LANDED 2026-07-18 (INV-213, M-394: `guardrails/check-runaway-child.py`, its core `find_runaways` reporting at a stopping point a descendant the run provably owns — a process in the run's own process group, or under the run's own temp tree — that is orphaned (its owning parent no longer alive) and burning a full core, reading no command/name field so a bare-name match in a foreign process group under no owned tree never fires (the browser-kill lesson, INV-162); notice-first through the shared `guardrails/cleanup_notice.py` new `runaway_notice` (row 417's cleanup-notice shape, INV-204) and ending no process, so the report-only mechanism can never become the broad-sweep footgun it guards; it is a Stop-time notice rather than a push gate — a push gate runs long after the cores burn — so it takes no push-gate letter and rides no CI step, and its live wiring is a documented owner-run install step in `guardrails/README.md` rather than an auto-wire into a running session's Stop hook, since wiring a process scanner into a live session mid-movement could report against that session's own background workers). With candidate 4 landed the row-420 audit-and-build is COMPLETE: the audit doc `docs/gate-audit/2026-07-18-habits-to-gates.md` plus its four built members — row 419 (gate s, INV-208), candidate 1 (gate u, INV-210), candidate 2 (gate v, INV-211), candidate 3 (gate w, INV-212), and candidate 4 (the Stop-time notice, INV-213). |
| 421 | **How an open-source engine and its instances live as separate repos, and how one window rules several instances** (his word, Alexander 2026-07-18 ~21:00): the machine already runs the pattern twice — exhibition-engine (released open-source so anyone can build the site) with tlvphotos as one instance, and promoter (an open-source backbone) with several instances planned, one each for the site, the music, the articles, and the assistant, all ruled from one window. The pack has no stated position on this shape, so a third project reopens the question from scratch. The row asks the pack to state it: when an engine earns its own repo (the trigger is a real or planned second instance, never before), which zones the engine's agent card owns versus the instance's, how a change fans out across the two repos and their specs, and — the new part the current rules do not cover — how one orchestrator window rules several instance-agents against the "one window, one project" law, which the pack's own agent layer (cards, discovery, referral, contracts) is the machinery for. (status note: queued 2026-07-18, OPEN DESIGN QUESTION — the rule's content is Alexander's architectural call; this row only holds the question so the fourth project does not re-derive it) | surface | *queued* 2026-07-18 | The two repo boundaries are verified: exhibition-engine + tlvphotos and promoter + promoter-alexander are each two GitHub repos under happysasha18. The framework-plus-apps split is standard practice and justified here by real multi-instance reuse; the genuinely unaddressed piece is the single-window orchestrator over many instance-agents, which touches the one-window bootstrap rule and should be resolved together with the far-tier orchestrator view [row 411]. |
| 424 | **Named references travel everywhere, and earned notifications are deposited automatically** (the owner's word, 2026-07-19): every reference to an internal item — in a message across the agent channel and in any human-facing report — carries a pair, its stable code beside a one-sentence description pinned to the item's canonical line [E-35, INV-239]; an agent deposits an earned message in the course of its own work for any earned birth, the whole class rather than a fixed pair of cases, gated by the earned-message births unchanged [T-24, INV-189, INV-195]; and the user is told both when a message is deposited and when one is declined, with the reason, the user named by role [T-24]. (status note: MACHINERY LANDED 2026-07-20 v2.9.0 — spec (E-35, INV-239, INV-240, T-24) + architecture + matrix (M-421..M-425) + two suite-riding presence gates (guardrails/check-description-field.py dormant, guardrails/check-deposit-description.py) + tests, through two prover passes and a fresh-seat adversarial audit. REMAINS (the his-gate): the back-describe of every registered code to the quality bar — a 15-code form sample sits at docs/migration-sample/2026-07-20-backdescribe-sample.md for Alexander to accept the FORM, then the full ~330-code back-describe + arming description-field.json lands as MAJOR with a MIGRATION.md chapter) | large | *deferred* 2026-07-20 — revisit trigger: see the status note | Probable MAJOR by INV-217 — pinning the pair everywhere forces every host to back-describe its codes with human-clear descriptions, so the landing owes a MIGRATION.md chapter; the success measure is description quality (the human-clear descriptions read as clear to a non-engineer, sampled from real references and accepted). Out of scope this build: the live wake / listener (a neighbour still reads its inbox on its next run [INV-192]) and a static translation dictionary (the description is translated in real time [INV-83], a translations file a later pass). ⟨DECIDE⟩ items RESOLVED 2026-07-19 (owner's decision, folded from prover pass docs/prover/2026-07-19.md): each item's pinned description lives in a dedicated Formal-index description field — the description's one home — netted by a presence gate that reds a registered code carrying no description [base rule 30]; the existing code set is back-described in one pass at the major release's MIGRATION.md chapter; and INV-239's agent-channel reach is netted by a deposit-time lint on `from-<agent>` inbox files homed beside the earned-message gate, the prover's station its net until it ships [target]. The provisional reference to T-24 from row 398's done-when is tracked for reallocation by row 426. |
| 425 | **Rows 422 and 423 carry an invalid Class** (found 2026-07-19 folding the E-35 / INV-239 / T-24 prover pass, docs/prover/2026-07-19.md): both rows are marked Class `method`, which sits outside the queue's size vocabulary — bug / small / surface / large, one vocabulary with the spec [SPEC T-11, INV-12] — so their owner reclassifies each to a valid size. (status note: queued 2026-07-19) | small | *queued* 2026-07-19 | Done when: rows 422 and 423 each carry a size from {bug, small, surface, large} in their Class column, chosen by their owner. |
| 426 | **T-24's provisional reference in row 398's done-when is reallocated when row 398 lands** (found 2026-07-19 folding the E-35 / INV-239 / T-24 prover pass, docs/prover/2026-07-19.md): row 398's done-when borrows the code `T-24` as a fixture label for its routing-preamble deposit, a label minted before T-24 was assigned to the earned-auto-notification feature [ROADMAP 424]; when row 398 lands, that fixture reference is re-pointed at row 398's own artifact so the two never collide on one code. (status note: queued 2026-07-19) | small | *queued* 2026-07-19 | Done when: row 398's done-when no longer borrows the T-24 code as a fixture label — the reference reallocated to row 398's own identifier at its landing. |
| 427 | **A dynamic parameters registry — an always-current, self-updating list of every tunable per-person parameter, surfaced as a live settings window** (Alexander 2026-07-19): the pack keeps one live registry of every per-person parameter a host may tune, and an incoming request is recognized for whether it adjusts a parameter, updating the registry the moment it does, so the list never drifts from the truth and the settings window renders the current set on demand. This folds into the onboarding movement, which already owns the settings window [ROADMAP 414]; the delegation-strictness setting (how strictly the pack delegates work to cheaper workers) is its first prospective entry. (status note: **OPEN DESIGN 2026-07-19 — capture only, owner-held for scope** (Alexander 2026-07-19; the onboarding movement owns the settings window, so the registry lands inside that movement)) | surface | *queued* 2026-07-19 | Done when: the onboarding movement's owner has decided the registry's shape — how an incoming request is recognized as a parameter change, how the registry stays current and self-updating, and how the live settings window surfaces it — with the delegation-strictness setting captured as its first entry. |
| 428 | **A class clause for the after-the-fact user-tell family** (design review, docs/design-review/2026-07-19.md REC-1, 2026-07-19): the design review found eleven standing members of one undeclared class — every agent action the user did not watch, surfaced to the user after the fact — sharing one role and one register yet with their report home decided ad hoc per member (taste choice [SPEC INV-31], tunable parameter [INV-70], economy shed [T-19], footprint re-classification, substance removal [INV-109], step stand-down [INV-22], plan-vs-prototype diff [INV-30], escalation [INV-192], wrong referral [INV-225], deposit-tell, decline-tell [T-24]); recommend spec-author add a class-level clause naming the class and its members, stating the two shared properties (the plain report register with no bookkeeping numbers as content, the user named by role never a person [INV-28]), and the one home-keying rule the members do not yet share explicitly — home keyed by cadence, a landing-time choice or record in the landing report [INV-31] and a status-cadence agent-channel message-lifecycle event in the status report [INV-27] — after which the prover's uniformity check holds the rule for every future sibling [INV-125]. (status note: **CAPTURE ONLY 2026-07-19 — recommended next-pass class clause, lands on the human's taste call** (from the design review's REC-1; the deposit-tell/decline-tell home has already been fixed to the status report in this delta, so this row generalizes that fix into a stated class)) | surface | *queued* 2026-07-19 | Done when: a class clause naming the after-the-fact user-tell family, its two shared properties, and its cadence-keyed home rule has landed on the human's word, and the prover's uniformity check holds the home rule for the class [INV-125]. |
| 432 | **The hedge paraphrase class needs a text-semantic judge, distinct from the action-trace judge** (a fresh-seat audit residual, 2026-07-20): INV-238's hedge gate is a literal net, and its prose defers the whole paraphrase class — a hedge in any phrasing — to a semantic judge; that class is a phenomenon of the reply's words, so the action-trace conduct judge [INV-241] cannot hold it. The residual is a text-reading semantic judge, a chat-surface generalization of the register judge or an extension of it, that reads the reply's meaning for an offering-hedge in any phrasing. (status note: queued 2026-07-20 (surfaced by the conduct-judge adversarial audit); field evidence 2026-07-23: the literal net fired 0 times in 973 runs against documented third-person deferrals (docs/audit/2026-07-23-tlvphotos-conduct-evidence.md §1)) | small | *queued* 2026-07-20 | Done when INV-238's paraphrase-class deferral points at a judge that actually reads the reply text for the hedge meaning, or the promise is narrowed to what the literal net catches. |
| 435 | **The skill-copy arm misses an executable-bit-only drift** (surfaced by the conduct-audit stories 2-3 adversarial audit, 2026-07-20): `diff -rq` compares content, so an installed skill script identical in bytes with a lost `+x` bit passes green. Exposure is low — skills load as markdown plus referenced files and the installer preserves mode — so this is a robustness nit. (status note: far 2026-07-20 (surfaced by the INV-243 adversarial audit)) | small | *far* 2026-07-20 | Done when the skill-copy arm also compares the executable bit, or the mode is declared out of scope with a stated reason. |
| 436 | **The composition-axis set is derived from `project.kind`, known in advance** (from the tlvphotos 2026-07-20 inbox deposit + Alexander's clarification the same day): `project.kind` (INV-36) already proposes a project's node structure; it should equally carry the STANDARD set of composition axes a product of that kind renders under, so spec-author composes every surface against that set from the start rather than discovering an axis from a bug. A visitor-facing web product's standard set: input modality (touch / pointer / keyboard-assistive), browser engine, viewport, locale delivery, connection/data class, first-vs-returning, accessibility, measurement reach. A backend's: load, version, tenant. Input modality (the tlvphotos mobile-grab bug) is one member of the web set, not a one-off addition. The extrapolation runs one level deeper too: each axis's VALUE space is its own class to extrapolate, with an explicit test for the member BETWEEN the two obvious poles — the device/input axis is coarse-touch-only (phone), fine-pointer-with-hover (desktop), the hybrid (tablet / 2-in-1 / touchscreen laptop, both at once), and keyboard/assistive-only, so a binary touch-vs-pointer split already drops the tablet. The mechanism must force naming the value space and its in-between member, since a hand-written list writes the two obvious values and drops the third. (status note: queued 2026-07-20 (Alexander named the class: the axes are typical per application type and derivable up front, and the extrapolation reflex reaches down to each axis's values — his tablet question; the mobile-grab bug was the symptom of a reactive, incomplete axis set); **smallest-first opening landed 2026-07-20 v3.2.0 — SPEC INV-244** (a visual kind owes input-capability as the first named member of an open owed set, the owed-vs-covered gap a finding, per-kind axis set a mandatory `project.axes` declaration flagged if absent); still deferred and owned by this row: the value-space forcing step for each axis's in-between member, the other kinds' elementary axis sets, and the recursive registry sweep (row 437)) | surface | *queued* 2026-07-20 | Done when `project.kind` carries a per-kind standard composition-axis set, spec-author derives a surface's axes AND each axis's value space from it (input modality among them, with its cross-modality reachability invariant and the between-the-poles member named), and the prover walks the kind's axes per surface. Enters through spec-author + product-prover. The per-kind axis set is authored smallest-first with Alexander (INV-62). Sibling of row 437. |
| 437 | **The look-alike sweep is recursive — it runs at every level, the axis registry included** (from the tlvphotos 2026-07-20 inbox deposit + Alexander's clarification): base rule 14 and INV-124/INV-125 sweep sibling SURFACES within a class; the same reflex applies one level up, at the registry of composition axes, and at every level thereafter — at any point one can always ask what other siblings or axes exist. When a session adds or derives a composition axis, it runs a bounded look-alike pass over the kind's registry (row 436): what other axes does this product's kind render under that the spec composes against nowhere, each named composed / added / out-of-scope-with-reason. (status note: queued 2026-07-20 (Alexander: "always можно сделать ещё lookalike" — the sweep is a standing recursive reflex, not a one-time act; finding input modality missing surfaced browser engine, keyboard, and measurement honesty as siblings)) | small | *queued* 2026-07-20 | Done when adding or deriving a composition axis triggers a bounded registry-level look-alike pass with the three verdicts, stated in spec-author plus a product-prover line, and the recursive nature (sweep at any level) is named. Enters through spec-author + product-prover. Sibling of row 436. |
| 440 | **A request is classified one-time or standing before it is actioned** (Alexander's word, 2026-07-21, said sharply after the seat treated a standing ask as a one-off and made him say "permanent" and "every project" twice): every incoming request or wish first passes an understanding step that names its persistence class. A ONE-TIME task is done once and closed. A STANDING ask is a rule that owes a permanent mechanism — a gate, a base rule, a judge — holding it forever and across every host by shipping in the pack, and the sweep to floor is only the one-time half of a standing ask. The seat states the class itself at intake, and the history-cleanup gate is one instance of the standing kind. This generalizes the answer-the-class reflex [INV-125] from an example to a request's own persistence. (status note: queued 2026-07-21 (Alexander named it as a standing classifier the seat must run on any request)) | surface | *queued* 2026-07-21 | Done when intake carries a named one-time-versus-standing classification step, a standing request routes to a permanent pack mechanism inherited by every host, and the seat states the class without being asked. Enters through spec-author + product-prover. |
| 446 | **Every gate declares its reach** (inbox 2026-07-21-from-tlvphotos-gate-reach-defects.md): ten field defects share one shape — a gate reports green after reading a subset of its subject (an anchor regex blind to 93 of 249 codes, hardcoded core-doc filenames, a truncation at column 110, no way to state "no CI here"). The pack adds a declared-reach convention: a gate's green line states what it read (files, rows matched of rows total), and the suite asserts reach where countable. (status note: queued 2026-07-22; widened 2026-07-23: the class also covers a suite test sharing the code's blind spot — a pinned member list on both sides (docs/audit/2026-07-23-tlvphotos-conduct-evidence.md §6)) | surface | *queued* 2026-07-22 | Done when the vendored gates print their reach, the ten reported defects are fixed or fenced, and a reach assertion guards the description-field and shipped-language gates |
| 447 | **Axes taxonomy and migration chapters catch up** (inbox 2026-07-21-from-tlvphotos-axes-adoption-field-report.md): the keyboard gets a pole in the input-capability taxonomy, a host gets a field recording that it runs ahead of the pack's increment schedule, and MIGRATION.md gains chapters 3.1–3.6 (its list stops at 3.0.0 while VERSION reads 3.6.0). (status note: queued 2026-07-22) | small | *queued* 2026-07-22 | Done when the taxonomy names the keyboard's pole, the ahead-of-schedule field exists, and MIGRATION.md charts every shipped release |
| 448 | **Scope negotiation gets its measure** (found by cold reading, row 445 pilot): the spec proposes cutting scope or splitting stages when a request is larger than its worth, and names no judge or measure for effort against benefit. Define who weighs a request's effort against its stated benefit and by what, and write it into the scope-negotiation clause. (status note: queued 2026-07-22) | small | *queued* 2026-07-22 | Done when the clause names the judge and the inputs and the cold-reader panel reads it without a finding |
| 449 | **The quick-win mark gets its measure** (found by cold reading, row 445 pilot): the mark's effect is stated (a waiting quick win may run ahead of larger queued items), and the reading of "low effort, immediate value" has no stated basis. Define the classifier's inputs for the mark. (status note: queued 2026-07-22) | small | *queued* 2026-07-22 | Done when the mark's clause names its inputs and the panel reads it clean |
| 450 | **Disagreement outcomes get entry routes** (found by cold reading, row 445 pilot): of the three-source disagreement outcomes, only the bug item carries a named entry route; the spec-fix and restructure items have owners and no route from the closed five-route set, and "restructure" shares no word with the route "refactor". Name both routes or extend the route set. (status note: queued 2026-07-22) | small | *queued* 2026-07-22 | Done when each disagreement outcome names its route and the route vocabulary has one word per concept |
| 451 | **The taste category gets a definition** (found by cold reading, row 445 pilot): the ask-the-person versus decide-myself boundary leans on "taste" with no definition; the deferral re-test law (INV-152, base rule 29) already defines the boundary by derivability — an answer no artifact holds is the person's. Fold that definition into the spec clause that uses "taste", so the word carries its measure. (status note: queued 2026-07-22) | small | *queued* 2026-07-22 | Done when "taste" is defined in one home by the derivability test and the panel reads it clean |
| 452 | **One step sequence, stated once** (found by cold reading, row 445 pilot): the pipeline's step list and the document chain's list read as two differing sequences with no stated relation. State the one sequence and the relation (documents are stations of the same walk), in one home. (status note: queued 2026-07-22) | small | *queued* 2026-07-22 | Done when the two lists resolve to one stated sequence and cross-references point at it |
| 453 | **Review pages persist edits** (Alexander 2026-07-22, relayed from the tlvphotos window's journal — the deposit was noted there as a pack candidate and never sent): a decisions/review page opened for the person accepts edits in place and writes them back into repo files through a small local HTTP server (stdlib, one shared script all pages use), replacing the save-a-JSON-download dance. Generalizes the commentable-review law; every project's review pages get it from the pack. (status note: queued 2026-07-22) | surface | *queued* 2026-07-22 | Done when a review page's edit lands in the repo file it belongs to, the round-trip is covered by a test, and the shared script ships with the pack |
| 454 | **The deep-pass schedule gets one written home** (Alexander 2026-07-22): periodic whole-document passes stay in the method, on a stated ladder — per delivery the machine gates; per changed section the cold-reader panel; per push the whole-spec prover re-verify (already law, M-6); per MINOR release a full-document fresh cold read plus a design-review similarity sweep, because cross-section drift is visible only whole. One clause states what runs, how, and when; the release checklist cites it. (status note: queued 2026-07-22) | small | *queued* 2026-07-22 | Done when one spec clause states the ladder, the MINOR checklist cites it, and a skipped scheduled pass reds the release gate |
| 455 | **An all-project transcript audit mines the working record** (Alexander 2026-07-22, voiced in the live-spec window after a sibling window's reply carried a banned contrast frame the scanner missed): one deep sweep over every project window's session transcripts (live-spec, track-coach, tlvphotos, promoter) reading for classes, each finding routed to an owner: a friction seen twice with no owning queue row, a law voiced in chat and never mechanized, a deferral marker that fails the derivability re-test, a register leak that slipped the nets (the comma-less «а не» hole fixed 2026-07-22 is the worked instance), a worker report breaking the language laws, a bug fixed before its red test existed (the red-first ordering is invisible in commits and visible only in transcripts), and a session that read raw material itself where the routing rule owed a worker dispatch. Per the one-time-versus-standing classifier (row 440) this row is BOTH: the sweep is the one-time half, and the audit proposes its own standing cadence and mechanism as part of its deliverable. (status note: queued 2026-07-22) | large | *queued* 2026-07-22 | Done when the sweep's findings each name an owner (queue row, base rule, hook pattern, or agreed non-problem), the fixed scanner holes are red-proven, and a standing audit cadence is proposed with its cost stated |
| 456 | **The format family reaches the other core documents** (Alexander 2026-07-22, asking whether the architecture and the other specs convert too): row 445 converts PRODUCT_SPEC.md; TEST_MATRIX.md and ROADMAP.md already have their own follow-on movements in its plan. This row holds the remaining document: ARCHITECTURE.md gets its own format definition in the same family — derived from its reading job as a component inventory (each node: purpose, interfaces, owned requirement codes, the tests that prove it), with the glossary and the slot-filling law shared with the spec format and the context/user-story shape left to the spec whose job it serves. The standing spec-to-architecture bond is already gated (the traceability suite runs in every full suite pass and the matrix coverage checklist is gate (d) of every push) and the new format must keep those nets green through the conversion. Back-propagation named (Alexander 2026-07-23): the test matrix groups its rows by this document's nodes, so a node renamed or regrouped here re-lays the matrix's block headings — a mechanical re-lay the traceability test reds until done; this row's landing walks the matrix's node blocks against the converted architecture in the same delivery. (status note: queued 2026-07-22) | surface | *queued* 2026-07-22 | Done when ARCHITECTURE.md has its own format definition, the converted document passes a cold-reader panel and the existing traceability nets unchanged, the matrix's node blocks re-checked against the converted node set, and the migration chapter covers it |
| 457 | **Cost telemetry and the budget discipline** (Alexander 2026-07-22: the $200 weekly plan barely fits the week; his live word amends the 2026-07-15 quality-over-budget grant — quality passes stay, and cost joins correctness as a criterion for HOW they run): the measured week 07-15..22 burned $14,338 API-equivalent across the machine (live-spec $6,486, tlvphotos $6,053, track-coach $1,475), two-thirds of it cache re-reads at an average 252k-token standing context over ~25,600 model calls, with the sonnet+haiku share under 0.5% against a written routing rule. Standing mechanism, four arms: (a) the usage-audit script (playbook tools/usage-audit.py) runs on a weekly cadence and its report lands where the owner reads it, showing burn by project, tier shares, and average context; (b) the milestone context-wipe returns as a mechanism — at a movement end or breakpoint the seat offers ending the session and reopening fresh, since the resume file carries the state; (c) the routing rule gains a lived floor — mechanical multi-step stages route to sonnet and the weekly report makes the tier mix visible; (d) long tool outputs go to a file with the verdict line printed inline, so suite logs stop living in the standing context. A cost-audit deposit with the tlvphotos share and levers went to that window's inbox 2026-07-22. (status note: queued 2026-07-22) | surface | *queued* 2026-07-22 | Done when the weekly report exists on its stated cadence, the milestone wipe offer fires, the report shows the tier mix and average context, and a following week's measured burn is lower at unchanged gates |
| 458 | **The cold-reader text audit becomes a pack skill** (Alexander 2026-07-22: «как аудировать тексты — это отдельный скилл, может дашь его отдельно в лайвспек?»): the loop proven inside row 445 — the mechanical lints first (vocabulary, weak words, requirement shape, style), then a fresh cold reader with zero context returning the places a stranger stops, fixes from the source, repeated until two consecutive clean reads — is packaged as its own working skill applicable to any human-facing text: a spec section, a README, a decision page, marketing copy, an article. The skill states the writing register it audits against and the reader-prompt it uses, and any host extracts it by adoption. Its first outside consumer is the promoter window's presentation-text rewrite (the recipe went to that inbox inline, 2026-07-22, so the wish there is runnable before this skill ships). (status note: queued 2026-07-22) | surface | *queued* 2026-07-22 | Done when the skill exists with its eval (a text a bare session passes and the skill reds), the register and the reader loop are stated inside it, and one outside host has run it on a real text |
| 459 | **A bug row carries its axis, class verdict, and spec verdict before it lands** (Alexander 2026-07-22, on the rotation-bug case: a reported bug first needs its axis named, then the class hunted, then the spec checked, and only then a test — the same walk a wish takes, one principle everywhere): the bug door's entry gains three mandatory row fields, mechanically gated at landing. `axis:` names the composition axis the break sits on, from the project's declared axis set (INV-244); `class:` records the lookalike sweep's verdict — the same axis walked across sibling surfaces and platforms, each checked pair listed with its result, settling one-off versus family (INV-125; the recursive sweep is ROADMAP 437's machinery); `spec:` records whether the spec is silent on or under-describes the broken behaviour, a yes routing a spec delta ahead of the matrix row. A landed bug row missing any field reds the gate; the prover's bug-delta read carries the three fields as a lens. The one-time half: tlvphotos's axes catch-up rides its migration (rows 437, 447 adjacent). (status note: queued 2026-07-22) | surface | *queued* 2026-07-22 | Done when a bug row without the three fields cannot land (red-proven), the lookalike sweep's checked pairs are listed on the row, a spec-silent bug demonstrably lands its spec delta first, and the prover's bug lens reads the fields |
| 460 | **The register ratchet over the prose corpus** (Alexander 2026-07-22, voiced while the row-445 assembly ran: the skills and the wider documentation carry the banned contrast frames and further register debt; his order lands the pack-skills rewrite before the 4.0.0 push, and this row holds the rest): a census (docs/audit/2026-07-22-register-census.md) records per-file violation counts across four reader tiers — pack skills, public reader docs, working docs, archive. This row owns (a) the rewrite of the public and working doc tiers to the register bar, and (b) the standing mechanism: a per-tier violation-count ratchet wired as a gate, seeded from the census, that only tightens, so a push raising a tier's count reds. The archive tier (journal, prover records, attic) is measured and left as written. His word 2026-07-23 ~18:30 re-scopes the row: the public tier (texts strangers and he read) stays the work; the working tier (journal, prover records, internal notes) moves to possibly-never, returning only on either named trigger — its size interferes past the doc bounds, or its register contaminates outbound drafts through reading (the pack-marinated-author pattern). (status note: queued 2026-07-22) | surface | *queued* 2026-07-22 | Done when the public and working tiers pass the register lint, the ratchet gate is red-proven on a seeded regression, and the census file carries the was and became numbers per tier |
| 465 | **text-audit's lint-fix and vocabulary rules interact** (docs/evals/2026-07-23-text-audit/scores.md): the first real eval found that fixing a contrast-by-denial frame at the lint step can insert a partial definition of an audited coined term, which then mutes the cold reader's coined-term finding to non-blocking. Close the interaction — either the step-1 lint fix may not introduce defining content for a term still under audit, or the vocabulary fallback flags the coined term before any rewording. (status note: queued 2026-07-23) | small | *queued* 2026-07-23 | Done when the text-audit skill states the rule that closes the interaction and its eval proves the coined-term finding survives a lint-step rewrite |
| 466 | **A future conversion proves claims, not only codes** (method improvement drawn from row 445): a per-unit zero-drop check counts code tokens, so a claim whose code survives elsewhere can vanish while every code still resolves — the exact class that cost row 445 the zones-may-overlap decision and the final fourteen restorations, each caught late by the content-test suite. A future format conversion adds a claim-level drop check, with the row-445 census-and-mapping Part-3 depth (each code's carried sentences tracked, not only its presence) as the worked instance to generalize from. (status note: queued 2026-07-23) | surface | *queued* 2026-07-23 | Done when the conversion recipe carries a claim-level drop check that reds a lost sentence whose code survives, red-proven on a seeded drop, and the method doc names the Part-3 mapping depth as its input |
| 467 | **spec-author's body size and the text-audit duplication fold** (skill-rewrite/SKILL-CREATOR-REVIEW.md, R2 and R1, deferred as a milestone-fold): spec-author is the one skill body meaningfully over the ~500-line ideal with everything inline (676 lines). Move the facet sweep or the compose-across-axes section to references/ the way build-pipeline moved its tables, and confirm the comprehension-gate pointer to text-audit left no restated loop behind (the review's B1, R1, N1 fixes already landed in its fix section). (status note: queued 2026-07-23) | small | *queued* 2026-07-23 | Done when spec-author reads near the 500-line ideal with its long sections in references/ and no sibling restates the text-audit loop |
| 469 | **A persistent metric gap becomes an owned queue row** (Alexander 2026-07-22 ~19:34, relayed by the tlvphotos window's inbox deposit inbox/2026-07-22-from-owner-turn-standing-metric-gaps-into-queued-builds.md: he keeps being shown statistics and supplies the "so what" by hand every time): the pack gains the analytics sibling of the nothing-handed-in-is-lost law — a metric standing past a declared floor, held over N consecutive reads, is itself a finding and opens a queue row mechanically, the way a bug reproduced twice does. The build: the rule's spec clause, the per-host wiring shape (a host's report family declares its floors and read cadence; the sweep compares and deposits the row), and the worked first instance on the tlvphotos evidence in the deposit (shares 12 / arrivals-by-share 0 over weeks; works-per-visit 1.15). The host owns the product read; the pack owns the reflex. (status note: queued 2026-07-23) | surface | *queued* 2026-07-23 | Done when the clause exists with its gate, a host-declared floor held over N reads demonstrably opens a row without the owner asking, and the tlvphotos instance is the recorded first run |
| 471 | **A report carries a forward time estimate, and a landing carries promised-vs-actual** (row 470, symptom 4: 0 of 46 report-shaped replies in the sampled window carried an estimate; the agreed rule lived only in auto-memory): the rule becomes a personal-profile contract line plus a Stop-hook arm on report-shaped replies, and the landing report carries the calibration line. (status note: queued 2026-07-23) | small | *queued* 2026-07-23 | Done when the profile line exists, the hook arm reds a fixture report with no estimate and stays silent on a non-report reply, and a landed row's report shows promised-vs-actual |
| 472 | **A done-claim states its remainder up front** (row 470, symptom 3: three episodes of a landing announced whole with the remaining pile in the same breath or later): the report shape where remaining work stands beside or before the done sentence; a Stop-hook arm on the done-then-remainder ordering plus the communicator rule sharpening. (status note: queued 2026-07-23) | small | *queued* 2026-07-23 | Done when the communicator names the shape, the hook reds a fixture reply whose remainder trails its done-claim, and a done-with-remainder-up-front reply passes |
| 473 | **A cross-project finding deposits the session it is found** (row 470, symptom 5: the 07-21 findings shipped 07-22 18:38 in one batch, one file repaired by the receiver): the sending-side twin of the inbox deposit protocol (INV-249) — a conduct-judge member reading the action trace, a reply naming a cross-project finding with no inbox write that session flagged. (status note: queued 2026-07-23) | small | *queued* 2026-07-23 | Done when the member stands in the conduct law, a fixture trace with an undeposited finding reds, and a deposited-same-session trace passes |
| 474 | **The judge chain runs once per reply** (row 470, extra pattern: ten near-identical judge sub-sessions spawned for one outgoing reply on 2026-07-22): dedupe the Stop-hook judge chain by reply hash so one reply buys one judge run — cost and noise carry no added verdict. (status note: queued 2026-07-23) | small | *queued* 2026-07-23 | Done when repeated Stop firings on one reply spawn one judge run, proven on a fixture, and the hook meter shows the duplicate class gone |
| 475 | **The enforcement-coverage registry: every agreed working rule names its net, the net's reach, and where it is armed** (row 470, the class finding — partial execution is the reminder-dependent-capability class: where a machine exists and reads its true subject the rule holds, where it is absent, mis-aimed, or uninstalled the rule decays): one standing table (rule → net → reach → armed-where), seeded from the evidence digest's own table, with a check that reds a working rule carrying no net and no stated no-net reason; the rule-side sibling of row 446's per-gate reach line. (status note: queued 2026-07-23) | surface | *queued* 2026-07-23 | Done when the registry exists with every profile and base working rule rowed, the check red-proves on a seeded netless rule, and each tlvphotos symptom maps to a registry row |
| 479 | **A worker never restores the shared working tree** (found 2026-07-23, second uncommitted-work destruction in one day: an applier's `-o` clobber — root-fixed as row 478 — and a caps-sweep worker's `git checkout -- TEST_MATRIX.md` that discarded the uncommitted conversion, recovered only because the converter was deterministic): the worker contract gains the clause — a worker runs no working-tree restore command (`git checkout --`, `git restore`, `git stash`, `git reset`) on shared files; recovery of a broken file belongs to the orchestrator, and a worker that believes a file needs restoring HALTs and reports. The orchestrator-side twin: a finished build stage is committed before the next worker touches its files. (status note: queued 2026-07-23) | small | *queued* 2026-07-23 | Done when the delegation protocol and the worker contract carry the clause, briefs inherit it from the template, and the orchestrator habit (commit the stage before the next worker) stands in build-pipeline's delegation reference |
| 480 | **ROADMAP.md gets its format — the queue member of the 4.0.0 format family** (second item of the standing format order, Alexander 2026-07-22): define the queue member's format beside the spec's and the matrix's — the shared glossary, lowercase-italic keyword form, no-capitals rule, and comprehension gate inherited from the family; the body holds the LIVE queue only, so the no-history law finally reaches this document: a row that lands moves to the dated archive in its landing commit, its delivery report riding with it, and the body carries open, in-work, and deferred rows alone (the 2026-07-23 survey: 72% of the 345 body rows are landed and carry inline delivery reports, the file at 98% of its 700 KB bound); a closed status vocabulary in lowercase italics with the class vocabulary held to its declared four words; the row shape re-declared with its columns matching the header (35 tail rows drifted to a sixth cell the header never declared); the five parsing consumers (rotate-doc, doc-rotation gate, landing-next-steps gate, crosscut counter, doc bound) repointed in the same delivery. Then convert ROADMAP.md under the content-preserving vehicle, landed rows archived verbatim. (status note: in-work 2026-07-23) | surface | *in-work* 2026-07-23 | Done when the format definition stands in the family's docs and is proven; ROADMAP.md's body reads live-only in the new format with content preservation proven across body plus archive (word-token + punctuation multisets modulo named deltas); the status and class vocabularies are closed and lint-held; every parsing consumer reads the new shape with its tests green; and lints + cold reader reach two consecutive clean reads. Non-goals: the ARCHITECTURE format stays row 456; no re-prioritization of the queue rides the conversion — every open row's meaning survives, only the form and the home move |
| 481 | **The live queue stays short — a staleness sweep at the milestone gate** (Alexander 2026-07-23 ~18:18: does the roadmap have a mechanism that shrinks it, should the limits be much stricter, the roadmap is no five-year plan): the live body after row 480's conversion still holds ~95 open rows, and no mechanism bounds their age or count — a wish queued once can sit forever. The wish: the milestone gate gathers the stale rows — queued past a threshold with no movement and no revisit trigger — into a short review list for the human, who declines, defers with a named trigger, or keeps each; a decline is his word, since the wishes are his. Threshold [default: 30 days], tuned with him. Rides on row 480's format (statuses dated, so staleness is computable). (status note: queued 2026-07-23) | small | *queued* 2026-07-23 | Done when the milestone gate produces the stale-row review list mechanically, the spec states the sweep beside the deferred-row re-scan, the first real sweep reached him, and the threshold stands in the settings ladder as a tunable |
