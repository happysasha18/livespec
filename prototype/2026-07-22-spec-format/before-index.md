| INV-150 | every declared law names its enforcing net (mechanical gate, prover station, or design review), recorded beside it in the declared-laws home [INV-101]; declaring a property promotes it to a blocking net | Every declared cross-cutting law names the one net that enforces it — a mechanical gate, the prover's judgment station, or the design review's recommendation — and declaring a property is what promotes it from a soft watch to a blocking net. | Throwing a wish |
| INV-151 | a request enters at the highest document in the derivation chain whose sentences must change to satisfy it; the door set is closed, and a request matching no kind becomes one plain question [INV-4] | A request enters the pipeline at the highest document whose sentences must change to satisfy it, the set of entry doors being closed on purpose, so a request that matches no door becomes one plain question to the human rather than a guessed route. | Throwing a wish |
| INV-153 | The request classifier [INV-151], property net [INV-150], deferral test [INV-152], and earned message [INV-189, INV-191] are one principle stated four times: everything routes to the home whose declared sentence governs it, and a homeless thing is itself the finding; the first three route a thing arriving inside one project, the fourth routes a thing leaving one project for another, manufactured traffic being a homeless item with a stamp on it | Every arriving item — a request, a property to enforce, a deferred row, or a message leaving one project for another — routes to the one home whose declared sentence governs it, and an item that fits no home is itself the finding rather than something forced into the wrong place. | Throwing a wish |
| INV-104 | An edit touching a spec-backed literal or clause owes its docs and its red-first test in the same session as the fix; the door-step tripwire reads the edit's content. | A fix that touches a spec-backed literal — a version string, a pinned count, a named vocabulary, a promised wording — owes its documentation update and its red-first test in the same session as the fix, whatever the size of the diff. | Throwing a wish |
| INV-128 | every request enters through a three-source impact read beside the door [T-12] and work-kind [T-16]: the footprint is read from the spec (what behaviour changes), the architecture (which module owns it), and the code (what gets touched) at one intake moment, producing one named footprint — presentation-only · single-module · cross-cutting — spoken in the capture echo and written in the row's `footprint:` note beside door/kind/map [INV-43, INV-108]; the footprint decides the route (presentation → light road; single-module → the matrix step against the module's interface; cross-cutting → the full pipeline), weight matched to reach, the footprint not the size picking the road; the read names any source disagreement as a finding routed to its owner (bug row / spec fix / restructure row [INV-37]) rather than silently trusting one source — pulling the architecture step's spec-to-code reconciliation forward to entry; the three-source read is the verdict derive-before-fork [INV-121] rests on (only the genuinely-open fork reaches the human); the footprint re-classifies mid-work when an edit reaches past its layer (mirroring the door re-fire), the landing report recording held-or-reclassified; the station carries the boundary-health law — a right boundary keeps a typical request in one module, repeated cross-cuts on the same module pair being the signal to move a boundary (only through the architecture step's re-prove [INV-37], on the recorded-footprint evidence not a hunch); recorded live 2026-07-12, the entry station (P1-P6) of the fourteen-principle architect draft, deeper mechanical enforcement on the follow-on rows | Every request gets a three-source impact read at intake — the spec for what behaviour changes, the architecture for which module owns it, the code for what actually gets touched — producing one named footprint of presentation-only, single-module, or cross-cutting that sizes how far the route reaches. | Throwing a wish |
| INV-144 | When product and spec diverge the spec defines correct: name the divergence, fix a wrong product to it, complete a silent spec with a pinned test; changing a confirmed-wrong spec takes the human's word [INV-9]. | When the product and the spec diverge the spec is the definition of correct, so a wrong product is fixed to it and a spec silent where the product is right is completed with a pinned test, while changing a spec confirmed to be the error takes the human's word. | Throwing a wish |
| INV-129 | every queue-take re-scans each deferred row's revisit trigger [T-8]; a fired trigger returns its row to the runnable head [INV-49], so a deferred wish never waits on a trigger nobody reads; prover finding F3, 2026-07-12 | Every deferred row's revisit trigger is re-scanned at each queue-take instead of only at milestones, and a fired trigger returns its row to the runnable head, so a deferred wish never waits on a trigger nobody reads. | Throwing a wish |
| INV-130 | a withdrawn decision converges: after two withdrawals the recommended option is taken as a surfaced `[default]`; an answered question closes forever [INV-59] but a withdrawal re-asks in plainer terms with no cap of its own [INV-9], so a genuine taste call could loop unbounded — the bound is two, and on the second withdrawal of the same decision the session takes the recommended option and surfaces it as a `[default]` in the landing report, silence staying consent from there and it never re-asked [INV-31], the same convergence an answered question already has now given to the withdrawal path; a later real change of mind rides the ordinary channel as a new wish, never a reopening of the closed decision; homes — the decision-page clause + communicator's rule 10; prover finding F6, 2026-07-12 | A withdrawn decision converges after two withdrawals of the same question, at which point the session takes the recommended option, surfaces it as a default in the landing report, and closes it so it is never re-asked. | Throwing a wish |
| INV-131 | a mid-work re-door [INV-16] that creates a new surface re-runs the parallel-lanes independence edges; a new edge pulls the re-doored lane back to serial and the board says so; prover finding F7, 2026-07-12 | A mid-work re-door that creates a new surface re-runs the independence checks between the parallel lanes, and a fresh collision pulls the re-doored lane back to serial with the departures board saying so, so the board never asserts a stale independence after the ground moved. | Throwing a wish |
| INV-133 | a critical non-bug [T-11] heads the queue and never preempts a rolling lane; the capture echo [INV-27] says that bound back at intake, and the human's word owns priority [INV-9]; prover finding F5, 2026-07-12 | A critical mark on a non-bug wish sends it to the head of the queue while the rolling lane keeps running, and the capture echo says that bound back at intake so the human can re-door it a bug if he meant a live break that must stop the work now. | Throwing a wish |
| INV-132 | the reverse direction of the feature-coverage check [INV-73] has teeth through a heading convention: every H3 heading carries either its `[feature: F-x]` tag (a person-facing scenario the coverage table maps [E-29]) or the explicit `[not a scenario]` marker (a machinery, rules, or reference section legitimately untagged), and an H3 carrying neither is unambiguously red — the checker could not otherwise tell a scenario whose tag was forgotten from a section that never had one, so an untagged scenario could ship green and uncovered; the convention lives on H3 headings (the level every scenario uses, sub-parts nesting under an already-tagged-or-marked heading), making the reverse direction mechanical rather than a hand-walk; homes — this feature-coverage clause + spec-author; prover finding F4, 2026-07-12 | Every H3 heading in the spec carries either its feature tag, marking a person-facing scenario the coverage table maps, or an explicit not-a-scenario marker for machinery and reference sections, so a heading carrying neither goes red and a forgotten scenario tag can no longer ship uncovered. | Machines |
| INV-134 | A landed feature-or-refactor row carries its footprint note [INV-128] and a suite check reddens a landed row that omits it; binds forward [INV-159]. | A landed feature-or-refactor row carries the footprint note its intake read named, and a suite check reddens any landed row that omits it, putting a mechanical floor under the footprint read. | Throwing a wish |
