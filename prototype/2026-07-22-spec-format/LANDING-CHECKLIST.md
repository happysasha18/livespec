# Row 445 stage 3 — landing checklist (working note, delete at landing; state as of 2026-07-22 ~22:00)

Order Alexander set: 4.0.0 ships with ALL texts at the register bar — spec assembly, ten skills rewritten, the new text-audit skill, README (done, uncommitted), working docs tier.

## In flight (background workers)
- [x] composing-across-axes conversion DONE: 19 req / 111 crit / 2 GAPs, 57/57 codes zero-drop, panel closed (rounds 3-4 consecutive clean). NINE single-homed codes rescued (INV-126/127/138/163/172/180/243/244/248). Prose grows 1.18x here (dense law, no strippable history) — honest per-unit number for the report. → final 11-unit assembly RUNNING
- [x] text-audit skill DONE: loop closed at round 17 (R16+R17 consecutive clean; README 2 rounds), lints clean; version stamped 3.6.0 → bump with the 4.0.0 sweep; row-458 "one outside host ran it" leg stays open post-landing (promoter window). SPEC-side wiring routed to the cleanup worker (what-live-spec-is unit + re-assembly); pack-list footers routed to the seam pass.
- [x] SEAM PASS done: "door" wins over "wish intake" (glossary is the defining home; 7 sites swept back in feedback-intake); 30 base-rule citations verified, zero drift; 1 contradicting gloss fixed (measurement family); text-audit added to 6 skill footers + README + OVERVIEW; no pins touched. traceability now 168 pass / 3 known fails: craft-ladder pin (re-pin sweep), SPEC-side text-audit wiring (cleanup worker has it), evals/text-audit.md lacks dated bare-run record.
- [ ] TWO spec-vs-skills renames to settle at re-pin (step 7): assembled spec says "delivery report" + "harness task panel"; all skills + live pins say "landing report" + "harness task list". The spec's names were declared sharpens (assembly item 4, alias-registry driven). Resolution at re-pin: sweep SKILLS to the spec's names AND re-pin the two tests (test_no_silent_drop.py:29, test_traceability.py:2512) in the same move — one name pack-wide.
- [x] text-audit eval RUN for real (2026-07-23): bare arm caught the 3 planted defects (detection rows rescored MET-BARE honestly); skill's proven value = the method rows (classification, lints-first, fixes-from-source, zero-context reader, stated close) RED→GREEN; parity test 2 passed. Records in docs/evals/2026-07-23-text-audit/. QUEUE-ROW CANDIDATE at landing: lint-step fix can pre-define a coined term and soften the cold reader's verdict (rule interaction, flagged in scores.md).
- [ ] 10 skill register rewrites (one per skills/<name>/; notes per skill in prototype/2026-07-22-spec-format/skill-rewrite/)
  - [x] design-reviewer (11 scissors fixed, 0 pins, own tests 14/14)
  - [x] feedback-collector (7 scissors + 3 metaphors fixed, 3 pinned substrings preserved verbatim, own tests 9/9)
  - [x] feedback-intake (1 scissors + 14 metaphor sites fixed, 0 pins broken, lints clean; "wish door" coinage → "wish intake")
  - [x] product-prover (26 scissors + 2 self-praise lint hits fixed, pins preserved, own assertions green; kept "recommend rather than ask" as lawful directive)
  - [x] communicator (8 scissors + negation-opener + 2 plainness fixes; register rules' normative content intact, quoted specimens kept; 39 tests green; 499 lines, under the 500 cap)
  - [x] test-author (11 scissors + 2 metaphors + 2 clarity fixes; 54 pinned-file tests green; TWO metaphor sentences kept ONLY because tests pin them verbatim — "mirror" test_mirror_assertion_ban.py:23, "never a render inventing its own structure" test_norm_conformance_law.py:30 → re-pin sweep (step 7) rewrites sentence+pin together)
  - [x] publish (7 scissors + 3 metaphors + long-sentence splits; 36 pinning tests green; no pin changed)
  - [x] build-pipeline (30 scissors + 11 metaphors across 9 files; TWO PINS CHANGED, recorded in notes for the re-pin sweep: test_traceability.py "craft ladder" sentence, test_class_hunt.py "four moves" sentence; kept Trains/pen/door pinned vocabulary)
  - [x] live-spec-base (22 scissors + 5 metaphors + 1 lint fix in the 34-rule rulebook; rule numbers/anchors intact; 2 cold reads clean; ONE pin blocks a fix: test_release_tier_rule.py:29 pins the scissors phrase "stays a stated rule rather than a blocking check" in rule 32 — restored verbatim, OLD/NEW in notes → re-pin sweep rewrites rule sentence + test together)
  - [x] spec-author (21 scissors + 8 inflation + 4 metaphors; 0 pins changed, 24 pinned phrases verified intact; 13 CONTENT-STALE passages flagged in notes — the skill still TEACHES the old spec shape)

## Done, waiting on integration
- [x] FINAL 11-unit assembly at prototype/2026-07-22-spec-format/assembly/: 566,366 B vs 783,678 source (−27.7%); 276 req / 1,301 crit / 196 glossary; zero-drop 345/345 both ways; all 7 gates + 5 lints green; byte-identical reproducible; ratchet seed 209.2 B/criterion; DELTA.md names the two intentionally unconverted blocks (manual index → generated table; Open decisions → DECISIONS.md)
- [x] Open-decisions repair: D-1/D-6/D-7 moved to DECISIONS.md ("Open" section); pilot R20.1/R20.2 gap lines; bounds R27 crit 4 restored; mappings updated; lints green
- [x] SOURCE-COVERAGE.md: 98.9% + Resolution section; Reference = the one intentionally unconverted section (retired manual index)
- [x] TEST-REPIN-PLAN.md: 79 content-reader + 40 mixed + 12 shape test files; 7 unmapped pins all from the Reference essay (covered by unit 11)
- [x] Register census docs/audit/2026-07-22-register-census.md (tier totals; ratchet baseline)
- [x] README.md rewritten, lint-clean, facts verified — UNCOMMITTED, goes in the 4.0.0 commit; regenerate README.html at landing
- [x] ROADMAP row 460 queued (doc-tier rewrite + ratchet); NEXT_STEPS free row now 461

## Remaining steps to the landing (order)
1. Unit 11 lands → message assembler: re-run assembly with 11 units (picks up pilot/bounds repairs too); verify gates + zero-drop over 11 units.
2. Skill wave completes → cross-skill seam pass (ONE worker): one name per concept across all ten skills + base-rule number citations still accurate + no term defined twice; fix in place. Input: every skill-rewrite/*.md notes file (collect each worker's local renames — known instance: feedback-intake renamed "wish door" → "wish intake"; the seam pass picks ONE name globally and sweeps all skills + assembled SPEC + guardrail configs for the loser name).
2b. [x] spec-author CONTENT updated: all 13 stale passages now teach the requirements genre + new comprehension-gate and change-record sections; extended lint 0 errors; cold-read round 4 zero blocking (single clean — seam pass gives the confirming read). ONE pin changed (test_scenario_heading_tag.py "H3" sentence, OLD/NEW in notes → re-pin sweep). 2 non-blocking register-pass leftovers noted for polish.
2c. [x] Judge law extended AS A CLASS (his 22:30 word): LAW 3's second pole now covers grading one's own result's worth/payoff, any language, examples multi-lingual (~/.claude/hooks/register-judge-personal.md); regex overlay stays the personal instant floor only, never ships; playbook copy of affirmation overlay synced.
3. [x pack lists — seam pass + cleanup worker did them] Remaining half, MANDATORY pre-push (his 22:55 word reinforces): skill-creator review of ALL ELEVEN changed skills, and the review explicitly covers DEFINITION quality per skill: trigger description fires when it should, boundaries with sibling skills have no gap/overlap, and the roster itself is right (nothing tonight begs to split into its own skill or merge). Batch: one reviewer worker per 3-4 skills, findings fixed before push.
4a. [x] Definitional-frame cleanup DONE: 90→0 in assembly (fixes in units + assemble.py authored regions, mappings updated), 4→0 ARCHITECTURE.md (no pins), re-assembly 565,709 B / 276 req / 1,301 crit / 196 glossary / 208.9 B-per-crit, zero-drop 345/345, gates green. text-audit wired as TENTH working skill in spec glossary (R2 names roles; skill roster lives in glossary only — no criterion change).
4b. LEGACY residual fixer RUNNING: 10 "Case: X, not Y" headings → positive names (his no-scissors law settles it), 3 appositives, 1 negation-opener heading, specimens backticked; docs/spec-format.md updated if it prescribes the heading shape.
4. [x] Style lint EXTENDED (scripts/spec-style-lint.py `_rather_instead_scissors` arm + 35 tests in test_prose_gate.py, red-proven vs pre-change logic; high-precision design, residual class documented in docstring). FOUND: assembly carries ~90 genuine frames (panels were blind to the class), ARCHITECTURE.md 4, spec-author 2. → cleanup worker RUNNING (fixes in units + mappings, re-assembly, ARCHITECTURE.md; spec-author's 2 routed to its content updater). KNOWN RED until promotion: test_convergence_locks::test_live_spec_sits_at_the_clean_floor reds on OLD root PRODUCT_SPEC.md (99 hits) — resolves when the cleaned assembly replaces root; ARCH's 4 fixed by cleanup worker.
5. [x] Working-docs tier rewritten: 16 files cleaned (~68 frames), 9 verified clean untouched; DECISIONS.md authority gate green (3 in-decision contrasts kept as decided substance, listed in notes); 1 pin restored verbatim ("once per machine"); lints clean; cold read clean. Notes: skill-rewrite/working-docs.md.
5b. [x] Chat-side inflation net extended (his 22:28 word, "сразу окупилось" class = self-result-grading): 6 patterns added to ~/.claude/hooks/affirmation-personal.json, proven 5/5 caught + 3/3 lawful clean.
6. [x] PROMOTED 23:37: root PRODUCT_SPEC.md = final assembly (565,735 B, spec-style-lint 0/0 after 4b's heading renames + appositive fixes; docs/spec-format.md needed no change — it already prescribed positive case names). Attic baseline + tag stand from stage 0. NOW RUNNING IN PARALLEL: re-pin sweep (4 phases: content pins → skill pins + two term sweeps → arm 7 gates → freeze --compaction + full suite); fresh full prover audit of root doc (strongest tier, record docs/prover/2026-07-22-row445-4.0.0-full-audit.md); MIGRATION 3.x→4.0.0 chapter.
7. Re-pin content tests from TEST-REPIN-PLAN + all skill-rewrite notes files (one worker, whole sweep); rebuild/verify shape-readers; arm the seven format gates; seed the bytes-per-criterion ratchet (207.0) and the register ratchet (census numbers).
8. FULL suite → read the log's own N/N green (never a wrapper exit code). Freeze re-baseline: python3 scripts/spec-freeze.py --freeze PRODUCT_SPEC.md ARCHITECTURE.md TEST_MATRIX.md --compaction.
9. [x] Prover audit DONE (docs/prover/2026-07-22-row445-4.0.0-full-audit.md): LANDS WITH FIXES — 8 MUST-FIX / 7 SHOULD-FIX / 5 obs. Reference table independently re-derived 333/333 clean; 40/42 GAPs genuine.
9b. MUST-FIX WAVE (launch AFTER re-pin sweep returns — it owns tests/ + freeze now). ONE worker, units+assembly+root+tests it breaks; F7 may run beside it (ARCHITECTURE.md only):
   F1 GAP at R160.3 repoints to R9.4 (answer exists in-doc). F2 glossary "cross-link mode" def aligned to R66 (seam-scoped pass with one whole-doc step). F3 one name: sweep body's 34 "senior agent" → "the seat" (build-loop-b's seat entry declared the one name), drop duplicate glossary entry, keep senior-agent only as role-pointer if glossary needs it; re-pin tests this touches. F4 R191.4+Context+glossary "description field" aligned to INV-271 (generated table = locations only; descriptions live in criteria+glossary). F5 intro corrected against actual structure + "ten"→"eleven" sections + glossary "scenario" entry. F6 marker conventions: [target] on its own line per convention (R102.2/R102.4 bare-in-bracket forms fixed; verify index generator handles), F-codes move to headings; architecture's 12 untagged features get tagged headings so R224.2's two-way trace resolves (coordinate with F7).
   F7 ARCHITECTURE.md reconciled to 4.0.0: retire "Formal index" refs (2), ten→eleven skills, text-audit gets its architecture home (node/ownership), feature tags for trace, "Last reconciled" → 2026-07-22. Then re-freeze rides the landing.
   F8 THE FORMAT-LAW SECTION: INV-250..271 (approved stage-1 delta, docs/prover/2026-07-22-row445-spec-format-delta.md is the source) authored INTO the spec as its own requirements section (the format machinery: genre, generated index, delta classifier, ratchet, panel gate, seven gates' clauses) — conversion discipline: mapping (source = the delta doc), lints, short cold-read panel (2 clean), then re-assemble + re-promote + re-freeze + suite.
   F9 (added 09:07, from the re-pin sweep's channel-tests fragment): agents-together unit dropped real content — headline: the recorded "agent zones MAY OVERLAP" decision (INV-197/225 area) absent from the converted doc; 13-17 old-sentence pins with no mapping row (log: scratchpad repin-frag-channels.md, REDO section). Fix-wave worker judges each: restore as criterion vs record as intentionally-unconverted rationale; sweep then retires the rationale pins with named reasons. NOTE THE CLASS: per-unit zero-drop counts CODES; a claim whose code survives elsewhere can still vanish — the content-test suite is the claim-level net, which is why no red gets weakened without a mapping-verified reason.
   SHOULD-FIX 7 → queue rows at landing (R96 far status, R83.3/R256.6 one-home dup, keyword *where* declared, + 4 more in the record).
10a. [x] MIGRATION 4.0.0 chapter WRITTEN (host action, 7 numbered changes, how-a-host-takes-it; every script/gate name verified; lints clean, 0 new findings). RECHECK after re-pin sweep: item-7 wording framed the two renames as spec-side because skills still carried old names at write time — once the sweep renames skills, restate plainly (one-line edit).
10. JOURNAL entry for row 445 (stages 1-3, panel history, the found-and-fixed drops: Composing-across-axes essay, three open decisions, too-heavy-surface rule) + queue rows owed at landing: the surface-registry status defect (ASSEMBLY-NOTES item 10/14 candidates), ASSEMBLY-NOTES deferred items (7, 12 partial, 14, 15 candidate).
11. VERSION 4.0.0 + skill frontmatter version sweep (version-is-one-fact) + commit (narrow paths, fence re-check: multiple windows share the tree) + prover re-check before push (host profile law) + push.
12. NEXT_STEPS rewrite to post-landing state; report to Alexander with: final size numbers (784→~530 KB), census was/became, cost actuals; say the memory can be wiped.

## Standing constraints
- Push on green = his standing grant; full prover re-check before every push (host profile).
- Nothing partial ever shown/committed as done; suite log is the verdict.
- Workers never touch tests/ during the wave; all pin changes flow through notes files → one re-pin sweep.
- Cost: route mechanical to sonnet; opus for judgment; keep lead context lean.
