# COLD-READER PANEL — rounds to acceptance

Each round spawned fresh readers with no conversion context, given only `section.md` and the stranger instruction: read as a first-time reader, list every stop (an ungrounded term, an unfilled relational word, a sentence needing a reread), and classify each blocking or non-blocking. A finding counts as blocking only when the rule's required behaviour stays unclear after the criterion's Context paragraph and any adjacent `[GAP]` note. The gate: repeat, fixing blocking findings from the source text, until two consecutive rounds return zero blocking findings.

## Round 1 — two readers

- **Reader A:** 1 blocking — R9 criterion 3, "a payload too small to make a split worth its cost." A stranger cannot tell whether a given monolith qualifies, because no measure and no judge is named inline. Also noted the "node" family and "user tier" as the nearest non-blocking undefined terms.
- **Reader B:** 0 blocking. Noted the same R9 c3 phrase as non-blocking because a `[GAP]` note sat beside it; flagged "node" and "user tier" as non-blocking.

**Fixes applied from the source.** The source states the delivery-separability lens is "a senior read the prover carries rather than a gate … only the design can say whether that reason holds." So R9 criterion 3 was reworded to name the design as the judge inline — "a payload the design judges too small to make a split worth its cost … the design the judge of whether the named reason holds" — and its `[GAP]` note was sharpened to record the missing measure while noting the judgment is deliberately left to the design. Separately, "architecture node" and "tier" (both pooled-glossary terms) were added to the carried-terms sentence, since both readers named them as the nearest undefined terms.

## Round 2 — two readers

- **Reader C:** 0 blocking. R9 c3 now non-blocking (judge named, `[GAP]` owns the measure). Flagged R9 c8 as the hardest sentence to parse (non-blocking).
- **Reader D:** 0 blocking. R9 c3 non-blocking. Named R9 c8 as "the clearest single readability stop in the file" (non-blocking) and R3 c5's forward reference to the pack-to-host split as the most useful non-blocking fix.

**Fix applied (readability, no behaviour change).** R9 criterion 8 was rewritten to remove the "bites/biting" idiom and un-stack its appositives: "for a lens the prover applies, it may ask whether that lens's dual applies to the document here. The system shall read this as a prompt that surfaces a missing lens, and not as a rule that every lens ship paired, since one dual folds into a lens already run while another is nameable yet seldom applies." R3 c5's forward reference was left as written, since both readers recovered the rule and the phrase carries the INV-163 cross-reference to its home requirement.

## Round 3 — two readers (final text)

- **Reader E:** 0 blocking. Every stop resolved from Context, an inline contrast, or an adjacent `[GAP]` note.
- **Reader F:** 0 blocking. Same verdict; noted the remaining friction is stylistic (classification tails, cross-reference analogies), none hiding a rule's behaviour.

## Round 4 — two readers (identical final text, no edits since round 3)

- **Reader G:** 0 blocking. The two dangling amounts (R9 c3 payload, R8 c12 co-occurrence) are each disarmed by their `[GAP]` note; undefined terms sit only in classification or kinship clauses, never in the sentence stating the duty.
- **Reader H:** 0 blocking. Per-requirement walk; R17 called the best-explained requirement. Confirmed the two content holes are `[GAP]`-flagged and every criterion states its required behaviour.

## Verdict

Two consecutive rounds (3 and 4) returned zero blocking findings on the exact shipped text of `section.md`, with no edits between them. The panel gate is satisfied. The residual reader friction all readers named — dense classification tails and analogies to terms defined in sibling sections ("a fact no node owns", "the standard-facet sweep", "the composition-lens family", the forward reference to the pack-to-host split) — is non-blocking here and resolves in the assembled document, where those terms live in their home sections and the pooled glossary.
