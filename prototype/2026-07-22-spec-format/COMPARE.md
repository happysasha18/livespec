# Spec format: before and after

This page sets the old three-times-stated form beside the new one-home form, for the index and for the body, and closes with the byte numbers.

## Index row INV-128 (the worst-accretion row)

### Before — one row carrying an accreted anchor line, a description sentence, and a section

```
| INV-128 | every request enters through a three-source impact read beside the door [T-12] and work-kind [T-16]: the footprint is read from the spec (what behaviour changes), the architecture (which module owns it), and the code (what gets touched) at one intake moment, producing one named footprint — presentation-only · single-module · cross-cutting — spoken in the capture echo and written in the row's `footprint:` note beside door/kind/map [INV-43, INV-108]; the footprint decides the route (presentation → light road; single-module → the matrix step against the module's interface; cross-cutting → the full pipeline), weight matched to reach, the footprint not the size picking the road; the read names any source disagreement as a finding routed to its owner (bug row / spec fix / restructure row [INV-37]) rather than silently trusting one source — pulling the architecture step's spec-to-code reconciliation forward to entry; the three-source read is the verdict derive-before-fork [INV-121] rests on (only the genuinely-open fork reaches the human); the footprint re-classifies mid-work when an edit reaches past its layer (mirroring the door re-fire), the landing report recording held-or-reclassified; the station carries the boundary-health law — a right boundary keeps a typical request in one module, repeated cross-cuts on the same module pair being the signal to move a boundary (only through the architecture step's re-prove [INV-37], on the recorded-footprint evidence not a hunch); recorded live 2026-07-12, the entry station (P1-P6) of the fourteen-principle architect draft, deeper mechanical enforcement on the follow-on rows | Every request gets a three-source impact read at intake — the spec for what behaviour changes, the architecture for which module owns it, the code for what actually gets touched — producing one named footprint of presentation-only, single-module, or cross-cutting that sizes how far the route reaches. | Throwing a wish |
```

### After — one row: code, the description sentence verbatim, a bare related list

```
| INV-128 | Every request gets a three-source impact read at intake — the spec for what behaviour changes, the architecture for which module owns it, the code for what actually gets touched — producing one named footprint of presentation-only, single-module, or cross-cutting that sizes how far the route reaches. related: T-12, T-16, INV-43, INV-108, INV-37, INV-121 |
```

### The provenance that moved to the journal export

```
every request enters through a three-source impact read beside the door [T-12] and work-kind [T-16]: the footprint is read from the spec (what behaviour changes), the architecture (which module owns it), and the code (what gets touched) at one intake moment, producing one named footprint — presentation-only · single-module · cross-cutting — spoken in the capture echo and written in the row's `footprint:` note beside door/kind/map [INV-43, INV-108]; the footprint decides the route (presentation → light road; single-module → the matrix step against the module's interface; cross-cutting → the full pipeline), weight matched to reach, the footprint not the size picking the road; the read names any source disagreement as a finding routed to its owner (bug row / spec fix / restructure row [INV-37]) rather than silently trusting one source — pulling the architecture step's spec-to-code reconciliation forward to entry; the three-source read is the verdict derive-before-fork [INV-121] rests on (only the genuinely-open fork reaches the human); the footprint re-classifies mid-work when an edit reaches past its layer (mirroring the door re-fire), the landing report recording held-or-reclassified; the station carries the boundary-health law — a right boundary keeps a typical request in one module, repeated cross-cuts on the same module pair being the signal to move a boundary (only through the architecture step's re-prove [INV-37], on the recorded-footprint evidence not a hunch); recorded live 2026-07-12, the entry station (P1-P6) of the fourteen-principle architect draft, deeper mechanical enforcement on the follow-on rows
```

The rule sentence in the after row is byte-identical to the Description column of the before row. The `related:` codes are harvested from the before anchor line. Everything else in the anchor line is preserved in the journal export.

## Body subsection: intake, one paragraph

### Before

> **A withdrawn decision converges:** after two withdrawals the recommended option is taken as a surfaced `[default]`. An answered question closes forever [INV-59], but a withdrawal re-asks in plainer terms with no cap of its own [INV-9], so a genuine taste call could loop unbounded. The bound is two: on the second withdrawal of the same decision the session takes the recommended option and surfaces it as a `[default]` in the landing report, silence stays consent from there and it is never re-asked [INV-31] — the same convergence an answered question already has [INV-59], now given to the withdrawal path. A later real change of mind rides the ordinary channel as a new wish, never a reopening of the closed decision. [INV-130]

### After (phase-2 sample)

> A withdrawn decision converges [INV-130]: on the second withdrawal of the same decision the recommended option is surfaced as a `[default]` in the landing report, silence stays consent from there, and it is never re-asked [INV-59, INV-31]. A later real change of mind rides the ordinary channel as a new wish.

The phase-2 paragraph keeps every behavioural fact and every code cite, and drops the sentences that re-derive the INV-130 rule already carried by its cite.

## Numbers

| Slice | Before (bytes) | After (bytes) | After / before |
|---|---|---|---|
| Index (12 rows) | 8,715 | 3,522 | 0.4041 |
| Body (one subsection) | 6,278 | 5,151 | 0.8205 |

Harvested provenance moved to the journal export: 5,731 bytes, preserved in JOURNAL.md.

Full file today: 783,678 bytes. Projected after phase 1 only: 622,986 bytes. Projected after phase 1 plus phase 2: 530,715 bytes. The assumptions behind these projections are stated in NUMBERS.md.

## Verification

- Each after-index rule sentence is byte-identical to its Description-column source: 12 of 12.
- Every code cited in before-body appears in after-body: 25 of 25.
- journal-export.md contains every anchor line verbatim, so every dropped sentence is preserved: 12 of 12.
