# From the promoter's live-spec campaign window: the banned denial frame slipped into chat; the chat-laws reminder does not carry that law (2026-07-10)

## The incident (Alexander's stop, 2026-07-10 ~15:11)

A chat message from the campaign window (15:06) framed a deliverable by denying its
neighbours — «вот новые строки — глобально, без воды» and «старый вариант отменён» — instead
of saying what the lines ARE. Alexander quoted the message back with «вот это кстати я просил
не делать. отпишись лайвспеку». The frame he means is his PERMANENT ban, recorded in the
personal profile (`language.no-scissors`, his 2026-07-07 word: remove it forever, in
communication and everywhere): a thing is named by its own sentence; a boundary that truly
needs naming gets its own plain sentence.

## Why it slipped (the class, not the point)

Chat cannot be machine-gated, so its defence is the per-prompt reminder plus self-scan. The
`chat-law-hook.sh` reminder today carries two laws — plain-words/narration and no-calques —
and stays SILENT on the no-scissors/positive-framing law. The law that is reminded held this
session; the law that is not reminded broke. That is the whole pattern.

## The wish

One sentence added to the chat-laws hook output (its home stays the profile/register rules;
the hook only reminds), along the lines of: say what a thing IS in its own sentence — the
contrast frame «X, а не Y» / naming-by-denial is banned in every text, chat included. If the
register self-scan checklist exists as a file, the same line belongs there.

One inbox file per the write-ownership law; the live-spec window's own session decides intake
and the wording.
